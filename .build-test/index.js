#!/usr/bin/env node
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// src/cli/command-registry.ts
var CommandRegistry;
var init_command_registry = __esm({
  "src/cli/command-registry.ts"() {
    "use strict";
    CommandRegistry = class {
      commands = /* @__PURE__ */ new Map();
      register(command) {
        this.commands.set(command.name, command);
      }
      find(name) {
        return this.commands.get(name);
      }
      list(subsystem) {
        const all = Array.from(this.commands.values());
        if (subsystem) return all.filter((c) => c.subsystem === subsystem);
        return all;
      }
      /**
       * Resolve a command from a list of argv tokens by matching the longest
       * registered name that is a space-joined prefix of `tokens`. Capability
       * CLI names can be multi-word (e.g. "kernel oracle query").
       * Returns the command and how many tokens it consumed.
       */
      resolve(tokens) {
        for (let i = Math.min(tokens.length, 4); i >= 1; i--) {
          const cmd = this.commands.get(tokens.slice(0, i).join(" "));
          if (cmd) return { command: cmd, consumed: i };
        }
        return { command: void 0, consumed: 0 };
      }
    };
  }
});

// src/config.ts
import { existsSync, readFileSync, writeFileSync } from "fs";
import { join } from "path";
function getServerPort() {
  const env2 = process.env.CAP_STORE_PORT;
  if (env2 && /^\d+$/.test(env2.trim())) return Number.parseInt(env2.trim(), 10);
  try {
    const p = join(process.cwd(), ".runtime", "backend.port");
    if (existsSync(p)) {
      const v = readFileSync(p, "utf8").trim();
      if (/^\d+$/.test(v)) return Number.parseInt(v, 10);
    }
  } catch {
  }
  return 9420;
}
function defaultDataDir() {
  if (isWin) {
    const local = process.env.LOCALAPPDATA ?? process.env.APPDATA ?? "";
    return local ? `${local}\\vivim\\cap-store` : ".";
  }
  const home = process.env.HOME ?? process.env.XDG_DATA_HOME ?? "";
  if (home.includes(".local")) return `${home}/share/vivim/cap-store`;
  return home ? `${home}/.local/share/vivim/cap-store` : ".";
}
var isWin, config;
var init_config = __esm({
  "src/config.ts"() {
    "use strict";
    isWin = process.platform === "win32";
    config = {
      // Server
      host: process.env.CAP_STORE_HOST ?? "127.0.0.1",
      port: Number.parseInt(process.env.CAP_STORE_PORT ?? "9420", 10),
      // Data
      dataDir: process.env.CAP_STORE_DATA_DIR ?? defaultDataDir(),
      dbPath: process.env.CAP_STORE_DB_PATH ?? `${defaultDataDir()}/cap-store.sqlite`,
      // Auth
      authToken: process.env.CAP_STORE_AUTH_TOKEN ?? null,
      // CORS
      corsOrigin: (process.env.CAP_STORE_CORS_ORIGIN ?? "http://localhost:5175").split(","),
      // Logging
      logLevel: process.env.CAP_STORE_LOG_LEVEL ?? "info",
      // Fleet
      autoStartFleet: process.env.CAP_STORE_AUTO_START_FLEET === "true",
      chromePath: process.env.CAP_STORE_CHROME_PATH ?? null,
      // Chrome profile root — single source of truth for where slave profiles live.
      // Frontend will later write this via config_entry; env overrides for now.
      // Defaults to <dataDir>/chrome-profiles so profiles survive across runs and
      // are not lost in a Unix-only /tmp path.
      profileBaseDir: process.env.CAP_STORE_PROFILE_DIR ?? (isWin ? `${defaultDataDir()}\\chrome-profiles` : `${defaultDataDir()}/chrome-profiles`),
      fleetPortRangeStart: Number.parseInt(process.env.CAP_STORE_FLEET_PORT_START ?? "9222", 10),
      fleetPortRangeEnd: Number.parseInt(process.env.CAP_STORE_FLEET_PORT_END ?? "9250", 10),
      // Health
      healthProbeIntervalMs: Number.parseInt(process.env.CAP_STORE_HEALTH_PROBE_MS ?? "30000", 10),
      // Circuit breaker
      circuitBreakerThreshold: Number.parseInt(process.env.CAP_STORE_CIRCUIT_THRESHOLD ?? "5", 10),
      circuitBreakerResetMs: Number.parseInt(process.env.CAP_STORE_CIRCUIT_RESET_MS ?? "30000", 10),
      // HPE retention
      hpeRetentionDays: Number.parseInt(process.env.CAP_STORE_HPE_RETENTION_DAYS ?? "30", 10),
      // Storage hardening (Unit 36.1)
      storage: {
        encryptDb: process.env.CAP_STORE_ENCRYPT_DB === "true"
      }
    };
  }
});

// src/cli/commands/automate.ts
async function api(path, init) {
  const resp = await fetch(`${API}${path}`, {
    ...init,
    headers: {
      "Content-Type": "application/json",
      "X-Source": "cli",
      ...init?.headers
    },
    signal: AbortSignal.timeout(1e4)
  });
  const body = await resp.json();
  if (!resp.ok) throw new Error(`API error ${resp.status}: ${JSON.stringify(body)}`);
  return body;
}
async function runAutomate(args) {
  const action = args[0];
  try {
    switch (action) {
      case "navigate": {
        const url = args[1] ?? "http://localhost:5173";
        await api("/api/automate/navigate", {
          method: "POST",
          body: JSON.stringify({ url })
        });
        break;
      }
      case "click": {
        const selector = args[1];
        if (!selector) {
          console.error("Usage: automate click <selector>");
          process.exit(1);
        }
        const result = await api("/api/automate/click", {
          method: "POST",
          body: JSON.stringify({ selector: { selector } })
        });
        console.log(JSON.stringify(result, null, 2));
        break;
      }
      case "type": {
        const selector = args[1];
        const text = args[2];
        if (!selector || text === void 0) {
          console.error("Usage: automate type <selector> <text>");
          process.exit(1);
        }
        const result = await api("/api/automate/type", {
          method: "POST",
          body: JSON.stringify({ selector: { selector }, text })
        });
        console.log(JSON.stringify(result, null, 2));
        break;
      }
      case "text": {
        const selector = args[1];
        if (!selector) {
          console.error("Usage: automate text <selector>");
          process.exit(1);
        }
        const result = await api(
          `/api/automate/text?selector=${encodeURIComponent(selector)}`
        );
        console.log(result.text);
        break;
      }
      case "value": {
        const selector = args[1];
        if (!selector) {
          console.error("Usage: automate value <selector>");
          process.exit(1);
        }
        const result = await api(
          `/api/automate/value?selector=${encodeURIComponent(selector)}`
        );
        console.log(result.value);
        break;
      }
      case "exists": {
        const selector = args[1];
        if (!selector) {
          console.error("Usage: automate exists <selector>");
          process.exit(1);
        }
        const result = await api(
          `/api/automate/exists?selector=${encodeURIComponent(selector)}`
        );
        console.log(result.exists ? "yes" : "no");
        break;
      }
      case "screenshot": {
        const result = await api("/api/automate/screenshot", {
          method: "POST"
        });
        if (result.screenshot) {
          const path = `screenshot-${Date.now()}.png`;
          const { writeFileSync: writeFileSync3 } = await import("fs");
          writeFileSync3(path, Buffer.from(result.screenshot, "base64"));
          console.log(`Screenshot saved: ${path}`);
        }
        break;
      }
      case "page": {
        const result = await api(
          "/api/automate/page"
        );
        console.log(`Title: ${result.title}`);
        console.log(`URL: ${result.url}`);
        console.log("---");
        console.log(result.content);
        break;
      }
      case "reset": {
        await api("/api/automate/reset", { method: "POST" });
        break;
      }
      default: {
        console.log(`
Frontend Automation CLI

Commands:
  navigate <url>                    Navigate to URL
  click <css-selector>              Click an element
  text <css-selector>               Get element text content
  value <css-selector>              Get input value
  exists <css-selector>             Check if element exists
  screenshot                        Take screenshot (saves PNG)
  page                              Get page content

Examples:
  automate navigate http://localhost:5173
  automate type "input[placeholder*='message']" "HELLO"
  click "button[data-testid='send']"
  automate text "h1"
  automate screenshot
`);
      }
    }
  } catch (err) {
    console.error(`Error: ${err instanceof Error ? err.message : String(err)}`);
    process.exit(1);
  }
}
var API;
var init_automate = __esm({
  "src/cli/commands/automate.ts"() {
    "use strict";
    init_config();
    API = `http://localhost:${getServerPort()}`;
    if (import.meta.main) {
      runAutomate(process.argv.slice(2)).catch((err) => {
        console.error(err);
        process.exit(1);
      });
    }
  }
});

// src/api/setup-client.ts
function createSetupClient(opts) {
  const baseUrl = opts.baseUrl ?? `http://localhost:${getServerPort()}`;
  const fetchFn = opts.fetch ?? globalThis.fetch;
  const source = opts.source;
  async function request(path, init) {
    const headers = new Headers(init?.headers);
    headers.set("X-Source", source);
    if (!headers.has("Content-Type") && init?.body) {
      headers.set("Content-Type", "application/json");
    }
    const resp = await fetchFn(`${baseUrl}${path}`, {
      ...init,
      headers,
      signal: init?.signal ?? AbortSignal.timeout(15e3)
    });
    const body = await resp.json();
    if (!resp.ok) {
      throw new Error(`API error ${resp.status}: ${JSON.stringify(body)}`);
    }
    return body;
  }
  return {
    // Health
    health: () => request("/health"),
    // Workspace
    getWorkspace: () => request("/api/setup/workspace"),
    setWorkspace: (path) => request("/api/setup/workspace", {
      method: "POST",
      body: JSON.stringify({ path })
    }),
    // Profiles
    getProfiles: () => request("/api/setup/profiles"),
    // Setup flow
    launchVisible: (req) => request("/api/setup/launch-visible", {
      method: "POST",
      body: JSON.stringify(req)
    }),
    verify: (req) => request("/api/setup/verify", {
      method: "POST",
      body: JSON.stringify(req)
    }),
    complete: (req) => request("/api/setup/complete", {
      method: "POST",
      body: JSON.stringify(req)
    })
  };
}
var init_setup_client = __esm({
  "src/api/setup-client.ts"() {
    "use strict";
    init_config();
  }
});

// src/cli/commands/moments.ts
async function runMoments(args) {
  const sub = args[0] ?? "list";
  try {
    switch (sub) {
      case "list":
        await SUBCOMMANDS.list();
        break;
      case "launch":
        if (!args[1] || !args[2]) {
          console.error("Usage: moments launch <providerId> <accountSlug>");
          process.exit(1);
        }
        await SUBCOMMANDS.launch(args[1], args[2]);
        break;
      case "verify":
        if (!args[1] || !args[2]) {
          console.error("Usage: moments verify <port> <providerId>");
          process.exit(1);
        }
        await SUBCOMMANDS.verify(Number.parseInt(args[1]), args[2]);
        break;
      case "complete":
        if (!args[1] || !args[2] || !args[3] || !args[4]) {
          console.error(
            "Usage: moments complete <providerId> <accountSlug> <profileDir> <debugPort>"
          );
          process.exit(1);
        }
        await SUBCOMMANDS.complete(args[1], args[2], args[3], Number.parseInt(args[4]));
        break;
      case "health":
        await SUBCOMMANDS.health();
        break;
      case "setup":
        if (!args[1] || !args[2]) {
          console.error("Usage: moments setup <providerId> <accountSlug>");
          process.exit(1);
        }
        await SUBCOMMANDS.setup(args[1], args[2]);
        break;
      default:
        console.log(`
User Journey Moments \u2014 CLI

Commands:
  list                          List all provider accounts
  launch <provider> <slug>      Launch Chrome for login
  verify <port> <provider>      Verify login state
  complete <provider> <slug> <dir> <port>  Save account
  health                        Check server health
  setup <provider> <slug>       Full setup flow (interactive)

All commands call the same API endpoints as the frontend wizard.
The X-Source header tracks which surface initiated each request.
`);
    }
  } catch (err) {
    console.error(`Error: ${err instanceof Error ? err.message : String(err)}`);
    process.exit(1);
  }
}
var client, SUBCOMMANDS;
var init_moments = __esm({
  "src/cli/commands/moments.ts"() {
    "use strict";
    init_setup_client();
    client = createSetupClient({
      source: "cli",
      baseUrl: process.env.VIVIM_API_URL
    });
    SUBCOMMANDS = {
      async list() {
        const profiles = await client.getProfiles();
        if (profiles.profiles.length === 0) {
          console.log("  (none)");
        }
        for (const p of profiles.profiles) {
          const icon = p.providerId === "chatgpt" ? "\u{1F916}" : p.providerId === "claude" ? "\u{1FAA8}" : "\u{1F48E}";
          console.log(`  ${icon} ${p.providerId}/${p.accountSlug}`);
        }
      },
      async launch(providerId, accountSlug) {
        const workspace = process.env.VIVIM_WORKSPACE ?? "C:\\.config\\vivim";
        console.log(`
Launching Chrome for ${providerId}...`);
        const result = await client.launchVisible({
          providerId,
          accountSlug,
          workspace
        });
        console.log(`  Port: ${result.debugPort}`);
        console.log(`  Profile: ${result.profileDir}`);
        console.log(`  Login URL: ${result.loginUrl}`);
        console.log("\nLog in in the Chrome window, then run:");
        console.log(`  bun run devops moments verify ${result.debugPort} ${providerId}`);
      },
      async verify(port, providerId) {
        const result = await client.verify({ port, providerId });
        if (result.loggedIn) {
          console.log(`  \u2713 Logged in (method: ${result.method})`);
        } else {
          console.log(`  URL: ${result.url}`);
        }
        return result.loggedIn;
      },
      async complete(providerId, accountSlug, profileDir, debugPort) {
        const workspace = process.env.VIVIM_WORKSPACE ?? "C:\\.config\\vivim";
        console.log(`
Completing setup for ${providerId}/${accountSlug}...`);
        const result = await client.complete({
          providerId,
          accountSlug,
          workspace,
          profileDir,
          debugPort
        });
        console.log(`  \u2713 Account saved: ${result.accountId}`);
      },
      async health() {
        const result = await client.health();
        console.log(`
  Status: ${result.status}`);
        console.log(`  Version: ${result.version}`);
      },
      async setup(providerId, accountSlug) {
        console.log(`
=== Setting up ${providerId}/${accountSlug} ===
`);
        const workspace = process.env.VIVIM_WORKSPACE ?? "C:\\.config\\vivim";
        console.log("[1/3] Launching Chrome...");
        const launch = await client.launchVisible({ providerId, accountSlug, workspace });
        console.log(`  PID: ${launch.pid}, Port: ${launch.debugPort}`);
        console.log("  (Log in the Chrome window, then press Enter here)");
        await new Promise((resolve4) => {
          process.stdin.resume();
          process.stdin.on("data", () => resolve4(void 0));
        });
        const verifyResult = await client.verify({ port: launch.debugPort, providerId });
        if (!verifyResult.loggedIn) {
          console.log("  \u2717 Login not detected. Try again with: bun run devops moments verify");
          return;
        }
        console.log(`  \u2713 Detected (method: ${verifyResult.method})`);
        await client.complete({
          providerId,
          workspace,
          accountSlug,
          profileDir: launch.profileDir,
          debugPort: launch.debugPort
        });
        console.log("\n\u2713 Setup complete!\n");
      }
    };
    if (import.meta.main) {
      runMoments(process.argv.slice(2)).catch((err) => {
        console.error(err);
        process.exit(1);
      });
    }
  }
});

// src/cli/commands/builtins.ts
import { z } from "zod";
function registerBuiltinCommands(registry2) {
  const automate = {
    name: "automate",
    description: "Agent-driven frontend automation (navigate, click, type, screenshot, ...)",
    subsystem: "extension",
    schema: z.object({ args: z.array(z.string()).optional() }).passthrough(),
    examples: [
      "automate navigate http://localhost:5173",
      `automate click "button[data-testid='send']"`,
      "automate screenshot"
    ],
    handler: async (args) => {
      const a = args ?? {};
      await runAutomate(a.args ?? []);
      return { data: null };
    }
  };
  const moments = {
    name: "moments",
    description: "User-journey moments \u2014 provider account setup (list, launch, verify, complete, health, setup)",
    subsystem: "backend",
    schema: z.object({ args: z.array(z.string()).optional() }).passthrough(),
    examples: ["moments list", "moments launch chatgpt my-account", "moments health"],
    handler: async (args) => {
      const a = args ?? {};
      await runMoments(a.args ?? []);
      return { data: null };
    }
  };
  registry2.register(automate);
  registry2.register(moments);
}
var init_builtins = __esm({
  "src/cli/commands/builtins.ts"() {
    "use strict";
    init_automate();
    init_moments();
  }
});

// src/cli/commands/registry-bridge.ts
import { z as z2 } from "zod";
function jsonSchemaToZod(schema) {
  const shape = {};
  const props = schema?.properties ?? {};
  const required = new Set(schema?.required ?? []);
  for (const [key, def] of Object.entries(props)) {
    let zod = z2.string();
    switch (def.type) {
      case "number":
      case "integer":
        zod = z2.number();
        break;
      case "boolean":
        zod = z2.boolean();
        break;
      case "array":
        zod = z2.array(z2.any());
        break;
      case "object":
        zod = z2.record(z2.any());
        break;
      default:
        zod = z2.string();
    }
    shape[key] = required.has(key) ? zod : zod.optional();
  }
  return z2.object(shape);
}
function syncCliFromUnified(reg, registry2) {
  const caps = reg.list({ surface: "cli" });
  const seen = /* @__PURE__ */ new Set();
  let skipped = 0;
  for (const cap of caps) {
    const cli = cap.cliCommand;
    if (!cli) continue;
    const names = [cli.name, ...cli.aliases ?? []];
    for (const name of names) {
      if (seen.has(name)) {
        console.warn(
          `[cli-bridge] alias collision: "${name}" already registered (skipping ${cap.slug})`
        );
        skipped++;
        continue;
      }
      seen.add(name);
      const cmd = {
        name,
        description: cap.description,
        subsystem: cap.category === "conversation" ? "cap-store" : cap.category === "admin" ? "backend" : cap.category === "system" ? "backend" : cap.category === "user" ? "backend" : cap.category === "canvas" ? "extension" : cap.category === "discovery" ? "extension" : "cap-store",
        schema: jsonSchemaToZod(cap.inputSchema),
        examples: cli.examples ?? [],
        handler: async (args) => {
          const a = args ?? {};
          const input = argvToInput(
            a.args ?? [],
            stripMeta(a.flags ?? {}),
            cap.inputSchema
          );
          const output = await reg.execute(cap.id, input, { metadata: {} });
          return { data: output };
        }
      };
      registry2.register(cmd);
    }
  }
  if (skipped > 0) {
    console.warn(`[cli-bridge] ${skipped} aliases skipped due to collisions`);
  }
}
async function fetchCliCapabilities(remote) {
  const res = await fetch(`${remote}/api/capabilities?surface=cli`);
  if (!res.ok) throw new Error(`failed to fetch capabilities: ${res.status}`);
  return await res.json();
}
function matchCapability(caps, tokens) {
  for (let i = Math.min(tokens.length, 4); i >= 1; i--) {
    const joined = tokens.slice(0, i).join(" ");
    const cap2 = caps.find(
      (c) => c.cliCommand?.name === joined || c.cliCommand?.aliases?.includes(joined)
    );
    if (cap2) return { cap: cap2, rest: tokens.slice(i) };
  }
  const head = tokens[0];
  if (!head) return void 0;
  const cap = caps.find((c) => c.cliCommand?.aliases?.includes(head));
  if (cap) return { cap, rest: tokens.slice(1) };
  return void 0;
}
function argvToInput(args, flags, schema) {
  const props = schema?.properties ?? {};
  const required = schema?.required ?? [];
  const input = {};
  for (const [key, val] of Object.entries(flags)) {
    const def = props[key];
    if (!def) continue;
    if (key in props) input[key] = coerce(def.type, val);
  }
  const positionalTargets = [];
  for (const key of required) if (!(key in input)) positionalTargets.push(key);
  for (const key of Object.keys(props)) if (!(key in input)) positionalTargets.push(key);
  let ai = 0;
  for (const key of positionalTargets) {
    if (ai >= args.length) break;
    const def = props[key];
    if (!def) continue;
    input[key] = coerce(def.type, args[ai] ?? "");
    ai++;
  }
  return input;
}
function stripMeta(flags) {
  const out = {};
  for (const [k, v] of Object.entries(flags)) {
    if (k === "json" || k === "remote" || k === "auth") continue;
    out[k] = v;
  }
  return out;
}
async function executeRemote(remote, capId, args, flags) {
  const cleanFlags = stripMeta(flags);
  const cap = await fetch(`${remote}/api/capabilities?surface=cli`).then(
    (r) => r.json()
  );
  const found = cap.find((c) => c.id === capId);
  const input = found ? argvToInput(args, cleanFlags, found.inputSchema) : { _rawArgs: args, ...cleanFlags };
  const res = await fetch(`${remote}/api/capabilities/${capId}/execute`, {
    method: "POST",
    headers: { "Content-Type": "application/json", "X-Source": "cli" },
    body: JSON.stringify({ input })
  });
  if (!res.ok) {
    const err = await res.text();
    throw new Error(`remote execute failed (${res.status}): ${err}`);
  }
  const data = await res.json();
  return data.output;
}
function coerce(type, val) {
  if (type === "number" || type === "integer") {
    const n = Number(val);
    return Number.isNaN(n) ? val : n;
  }
  if (type === "boolean") return val === "true" || val === "1" || val === "";
  return val;
}
var init_registry_bridge = __esm({
  "src/cli/commands/registry-bridge.ts"() {
    "use strict";
  }
});

// src/cli/output-formatter.ts
var OutputFormatter;
var init_output_formatter = __esm({
  "src/cli/output-formatter.ts"() {
    "use strict";
    OutputFormatter = class {
      format(data, mode) {
        switch (mode) {
          case "json":
            return JSON.stringify(data, null, 2);
          case "pretty":
            return this.prettyPrint(data);
          case "table":
            return this.tablePrint(data);
          case "watch":
            return JSON.stringify(data, null, 2);
          default:
            return JSON.stringify(data);
        }
      }
      prettyPrint(data) {
        if (data === null || data === void 0) return String(data);
        if (typeof data === "string") return data;
        if (Array.isArray(data)) {
          return data.map((item) => this.prettyPrint(item)).join("\n");
        }
        return JSON.stringify(data, null, 2);
      }
      tablePrint(data) {
        if (!Array.isArray(data) || data.length === 0) return "No data";
        const first = data[0];
        if (typeof first !== "object" || first === null) {
          return data.join("\n");
        }
        const keys = Object.keys(first);
        const header = keys.join(" | ");
        const sep = keys.map(() => "---").join(" | ");
        const rows = data.map(
          (row) => keys.map((k) => String(row[k] ?? "")).join(" | ")
        );
        return [header, sep, ...rows].join("\n");
      }
    };
  }
});

// src/cli/index.ts
function parseArgs(argv) {
  const raw = argv.slice(2);
  const tokens = [];
  const flags = {};
  for (const arg of raw) {
    if (!arg) continue;
    if (arg.startsWith("--")) {
      const [key, ...rest] = arg.slice(2).split("=");
      if (key) {
        flags[key] = rest.length > 0 ? rest.join("=") : "";
      }
    } else {
      tokens.push(arg);
    }
  }
  return { tokens, flags };
}
function serverUrl() {
  const port = process.env.PORT ?? process.env.CAP_STORE_PORT ?? String(DEFAULT_PORT);
  return `http://localhost:${port}`;
}
function connectCapabilityRegistry(reg) {
  capabilityRegistry = reg;
  syncCliFromUnified(reg, registry);
  registerBuiltinCommands(registry);
}
async function showHelp() {
  const cmds = registry.list();
  if (cmds.length === 0) {
    try {
      const remote = serverUrl();
      const remoteCaps = await fetchCliCapabilities(remote);
      if (remoteCaps.length === 0) {
        console.log("No commands registered. Start the server first: bun run serve");
        return;
      }
      console.log(`Available commands (from ${remote}):
`);
      for (const cap of remoteCaps) {
        const aliases = cap.cliCommand?.aliases?.length ? ` [${cap.cliCommand.aliases.join(", ")}]` : "";
        console.log(`  ${cap.cliCommand?.name ?? cap.slug}${aliases} \u2014 ${cap.description}`);
      }
      console.log(`
  Total: ${remoteCaps.length} commands`);
      return;
    } catch {
      console.log("No commands registered. Start the server first: bun run serve");
      return;
    }
  }
  console.log("Available commands:");
  const bySubsystem = /* @__PURE__ */ new Map();
  for (const cmd of cmds) {
    const group = cmd.subsystem ?? "general";
    const list = bySubsystem.get(group) ?? [];
    list.push({ name: cmd.name, description: cmd.description });
    bySubsystem.set(group, list);
  }
  for (const [group, entries] of bySubsystem) {
    console.log(`
  [${group}]`);
    for (const e of entries) {
      console.log(`    ${e.name.padEnd(28)} ${e.description}`);
    }
  }
  const total = cmds.length;
  console.log(`
  Total: ${total} commands`);
}
async function main() {
  const { tokens, flags } = parseArgs(process.argv);
  registerBuiltinCommands(registry);
  if (tokens[0] === "serve") {
    const { createServerWithEngines: createServerWithEngines2 } = await init_index().then(() => index_exports);
    const port = Number(process.env.PORT ?? process.env.CAP_STORE_PORT ?? DEFAULT_PORT);
    const ctx = await createServerWithEngines2(port);
    console.log(`vivim server listening on :${ctx.port}`);
    return;
  }
  if (tokens[0] === "help" || tokens.length === 0) {
    await showHelp();
    return;
  }
  const { command, consumed } = registry.resolve(tokens);
  if (command) {
    const args = tokens.slice(consumed);
    const mode = flags.json ?? "pretty";
    const result = await command.handler({ args, flags });
    console.log(formatter.format(result.data, mode));
    return;
  }
  try {
    const remote = serverUrl();
    const remoteCaps = await fetchCliCapabilities(remote);
    const matched = matchCapability(remoteCaps, tokens);
    if (matched) {
      const { cap, rest } = matched;
      const result = await executeRemote(remote, cap.id, rest, flags);
      const mode = flags.json ?? "pretty";
      console.log(formatter.format(result, mode));
      return;
    }
  } catch {
  }
  console.error(`Unknown command: ${tokens.join(" ")}`);
  console.error("Start the server with: bun run serve");
  process.exit(1);
}
var registry, formatter, DEFAULT_PORT, capabilityRegistry;
var init_cli = __esm({
  "src/cli/index.ts"() {
    "use strict";
    init_command_registry();
    init_builtins();
    init_registry_bridge();
    init_registry_bridge();
    init_output_formatter();
    registry = new CommandRegistry();
    formatter = new OutputFormatter();
    DEFAULT_PORT = 9420;
    capabilityRegistry = null;
    main().catch((err) => {
      console.error(err);
      process.exit(1);
    });
  }
});

// src/errors.ts
var CapStoreError, ValidationError, NotFoundError, CdpTimeoutError, CdpConnectionError, ChromeNotFoundError, EngineError, CapabilityNotFoundError, CanvasSpawnError, SandboxTimeoutError, SandboxBudgetError, SandboxPermissionError, ConsentViolationError;
var init_errors = __esm({
  "src/errors.ts"() {
    "use strict";
    CapStoreError = class extends Error {
      code;
      details;
      constructor(code, message, details) {
        super(message);
        this.name = "CapStoreError";
        this.code = code;
        this.details = details;
      }
      toJSON() {
        return { error: this.message, code: this.code, details: this.details };
      }
    };
    ValidationError = class extends CapStoreError {
      constructor(message, details) {
        super("ValidationError", message, details);
      }
    };
    NotFoundError = class extends CapStoreError {
      constructor(message) {
        super("NotFoundError", message);
      }
    };
    CdpTimeoutError = class extends CapStoreError {
      constructor(method) {
        super("CdpTimeoutError", `CDP command ${method} timed out`);
      }
    };
    CdpConnectionError = class extends CapStoreError {
      constructor(message) {
        super("CdpConnectionError", message);
      }
    };
    ChromeNotFoundError = class extends CapStoreError {
      constructor() {
        super("ChromeNotFoundError", "Chrome binary not found");
      }
    };
    EngineError = class extends CapStoreError {
      constructor(message) {
        super("EngineError", message);
      }
    };
    CapabilityNotFoundError = class extends CapStoreError {
      constructor(slug) {
        super("CapabilityNotFoundError", `Capability not found: ${slug}`);
      }
    };
    CanvasSpawnError = class extends CapStoreError {
      constructor(message) {
        super("CanvasSpawnError", message);
      }
    };
    SandboxTimeoutError = class extends CapStoreError {
      constructor(handlerSlug, budgetMs) {
        super("SandboxTimeoutError", `Handler ${handlerSlug} exceeded ${budgetMs}ms budget`);
      }
    };
    SandboxBudgetError = class extends CapStoreError {
      constructor(handlerSlug, kind, used, budget) {
        super("SandboxBudgetError", `${handlerSlug} ${kind} ${used} > ${budget}`);
      }
    };
    SandboxPermissionError = class extends CapStoreError {
      constructor(handlerSlug, denied) {
        super("SandboxPermissionError", `${handlerSlug} denied: ${denied}`);
      }
    };
    ConsentViolationError = class extends CapStoreError {
      constructor(host) {
        super("ConsentViolationError", `Outbound call to ${host} denied (no user consent)`);
      }
    };
  }
});

// src/engines/capability-event-bus.ts
var CapabilityEventBus;
var init_capability_event_bus = __esm({
  "src/engines/capability-event-bus.ts"() {
    "use strict";
    CapabilityEventBus = class _CapabilityEventBus {
      static instance = null;
      handlers = /* @__PURE__ */ new Map();
      onceHandlers = /* @__PURE__ */ new Map();
      wsSubscriptions = /* @__PURE__ */ new Map();
      recent = [];
      // Optional durable mirror (agentic backbone). When set, every emit is also
      // appended to the EventRecord outbox for cross-surface replay.
      durable = null;
      static getInstance() {
        if (!_CapabilityEventBus.instance) {
          _CapabilityEventBus.instance = new _CapabilityEventBus();
        }
        return _CapabilityEventBus.instance;
      }
      /** Reset singleton (for testing only). */
      static resetInstance() {
        _CapabilityEventBus.instance = null;
      }
      /** Attach a durable EventRecord outbox so emits are persisted too. */
      setDurableStore(store) {
        this.durable = store;
      }
      // ── Emit ───────────────────────────────────────────────────────────────
      emit(event) {
        const type = event.type;
        if (this.durable) {
          this.durable.append({
            source: "capability",
            type,
            payload: event
          }).catch(() => {
          });
        }
        const handlers = this.handlers.get(type);
        if (handlers) {
          for (const handler of handlers) {
            handler(event);
          }
        }
        const onceHandlers = this.onceHandlers.get(type);
        if (onceHandlers) {
          for (const handler of onceHandlers) {
            handler(event);
          }
          onceHandlers.clear();
        }
        this.recent.push(event);
        if (this.recent.length > 200) this.recent.shift();
        for (const [ws, entityMap] of this.wsSubscriptions) {
          for (const [entityType, entityIds] of entityMap) {
            if (entityIds.has("*")) {
              try {
                ws.send(JSON.stringify(event));
              } catch {
              }
              continue;
            }
            const eventAny = event;
            const idFields = {
              capability: "capabilityId",
              account: "accountId",
              fleet: "slaveId",
              conversation: "conversationId",
              provider: "providerId",
              config: "engineId",
              telemetry: "scheduleName"
            };
            const idField = idFields[entityType];
            if (idField && entityIds.has(eventAny[idField])) {
              try {
                ws.send(JSON.stringify(event));
              } catch {
              }
            }
          }
        }
      }
      // ── Subscribe (persistent) ─────────────────────────────────────────────
      on(type, handler) {
        let set = this.handlers.get(type);
        if (!set) {
          set = /* @__PURE__ */ new Set();
          this.handlers.set(type, set);
        }
        set.add(handler);
        return () => {
          set?.delete(handler);
        };
      }
      // ── Subscribe (once) ───────────────────────────────────────────────────
      once(type, handler) {
        let set = this.onceHandlers.get(type);
        if (!set) {
          set = /* @__PURE__ */ new Set();
          this.onceHandlers.set(type, set);
        }
        set.add(handler);
        return () => {
          set?.delete(handler);
        };
      }
      // ── WebSocket subscriptions ─────────────────────────────────────────────
      subscribe(ws, entityType, entityId) {
        let entityMap = this.wsSubscriptions.get(ws);
        if (!entityMap) {
          entityMap = /* @__PURE__ */ new Map();
          this.wsSubscriptions.set(ws, entityMap);
        }
        let entityIds = entityMap.get(entityType);
        if (!entityIds) {
          entityIds = /* @__PURE__ */ new Set();
          entityMap.set(entityType, entityIds);
        }
        entityIds.add(entityId);
      }
      unsubscribe(ws, entityType, entityId) {
        const entityMap = this.wsSubscriptions.get(ws);
        if (!entityMap) return;
        const entityIds = entityMap.get(entityType);
        if (entityIds) {
          entityIds.delete(entityId);
          if (entityIds.size === 0) entityMap.delete(entityType);
        }
        if (entityMap.size === 0) this.wsSubscriptions.delete(ws);
      }
      unsubscribeAll(ws) {
        this.wsSubscriptions.delete(ws);
      }
      // ── Cleanup ────────────────────────────────────────────────────────────
      removeAllListeners(type) {
        if (type) {
          this.handlers.delete(type);
          this.onceHandlers.delete(type);
        } else {
          this.handlers.clear();
          this.onceHandlers.clear();
        }
      }
      /** Return a bounded copy of recently emitted events (for debug inspection). */
      snapshot() {
        return [...this.recent];
      }
      /** Clear the recent-event buffer (debug reset). */
      clearRecent() {
        this.recent = [];
      }
    };
  }
});

// src/engines/capability-shape-registry.ts
import { readdir } from "fs/promises";
import { join as join2 } from "path";
var init_capability_shape_registry = __esm({
  "src/engines/capability-shape-registry.ts"() {
    "use strict";
    init_errors();
  }
});

// src/executor/cdp.ts
var DEFAULT_OPTIONS, BunCdpClient;
var init_cdp = __esm({
  "src/executor/cdp.ts"() {
    "use strict";
    init_errors();
    DEFAULT_OPTIONS = {
      timeoutMs: 3e4,
      maxRetries: 3,
      retryDelayMs: 1e3,
      pingIntervalMs: 3e4
    };
    BunCdpClient = class {
      constructor(debugUrl, opts) {
        this.debugUrl = debugUrl;
        this.opts = { ...DEFAULT_OPTIONS, ...opts };
      }
      debugUrl;
      ws = null;
      msgId = 0;
      pending = /* @__PURE__ */ new Map();
      handlers = /* @__PURE__ */ new Map();
      _connected = false;
      connecting = false;
      retryCount = 0;
      retryTimer = null;
      pingTimer = null;
      destroyed = false;
      opts;
      get connected() {
        return this._connected;
      }
      async connect() {
        if (this._connected) return;
        if (this.connecting) {
          return new Promise((resolve4) => {
            const check = () => {
              if (this._connected) resolve4();
              else setTimeout(check, 50);
            };
            check();
          });
        }
        this.connecting = true;
        this.destroyed = false;
        try {
          await this.initConnection();
          this._connected = true;
          this.connecting = false;
          this.retryCount = 0;
          this.startPing();
        } catch (err) {
          this.connecting = false;
          throw err;
        }
      }
      initConnection() {
        return new Promise((resolve4, reject) => {
          try {
            const ws = new WebSocket(this.debugUrl);
            this.ws = ws;
            ws.onopen = () => {
              resolve4();
            };
            ws.onmessage = (event) => {
              this.handleMessage(event.data);
            };
            ws.onclose = () => {
              this._connected = false;
              this.ws = null;
              this.stopPing();
              if (!this.destroyed) {
                this.scheduleReconnect();
              }
            };
            ws.onerror = () => {
              reject(new CdpConnectionError("WebSocket connection failed"));
            };
          } catch (err) {
            reject(new CdpConnectionError(`Failed to create WebSocket: ${err.message}`));
          }
        });
      }
      handleMessage(data) {
        let msg;
        try {
          msg = JSON.parse(data);
        } catch {
          return;
        }
        const id = msg.id;
        if (id === void 0) {
          const method = msg.method;
          if (method) {
            const handlers = this.handlers.get(method);
            if (handlers) {
              for (const handler of handlers) {
                try {
                  handler(msg.params);
                } catch {
                }
              }
            }
          }
          return;
        }
        const pending = this.pending.get(id);
        if (!pending) return;
        this.pending.delete(id);
        clearTimeout(pending.timer);
        if (msg.error) {
          const errMsg = msg.error.message ?? "CDP command failed";
          pending.reject(new CdpConnectionError(`${pending.method}: ${errMsg}`));
        } else {
          pending.resolve(msg.result);
        }
      }
      async send(method, params, opts) {
        if (!this._connected && opts?.retries !== 0) {
          await this.connect();
        }
        if (!this._connected) {
          throw new CdpConnectionError("Not connected to CDP endpoint");
        }
        const id = ++this.msgId;
        const timeoutMs = opts?.timeoutMs ?? this.opts.timeoutMs;
        const message = { id, method };
        if (opts?.sessionId) message.sessionId = opts.sessionId;
        if (params) message.params = params;
        return new Promise((resolve4, reject) => {
          const timer = setTimeout(() => {
            this.pending.delete(id);
            reject(new CdpTimeoutError(method));
          }, timeoutMs);
          this.pending.set(id, {
            resolve: resolve4,
            reject,
            timer,
            method
          });
          try {
            this.ws?.send(JSON.stringify(message));
          } catch (err) {
            this.pending.delete(id);
            clearTimeout(timer);
            reject(new CdpConnectionError(`Send failed: ${err.message}`));
          }
        });
      }
      on(event, handler) {
        if (!this.handlers.has(event)) {
          this.handlers.set(event, /* @__PURE__ */ new Set());
        }
        this.handlers.get(event)?.add(handler);
      }
      off(event, handler) {
        this.handlers.get(event)?.delete(handler);
      }
      async disconnect() {
        this.destroyed = true;
        this.connecting = false;
        this.stopPing();
        this.cancelRetry();
        const err = new CdpConnectionError("Client disconnected");
        for (const [_id, pending] of this.pending) {
          clearTimeout(pending.timer);
          pending.reject(err);
        }
        this.pending.clear();
        if (this.ws) {
          try {
            this.ws.close(1e3, "Client disconnect");
          } catch {
          }
          this.ws = null;
        }
        this._connected = false;
      }
      scheduleReconnect() {
        if (this.destroyed) return;
        if (this.retryCount >= this.opts.maxRetries) return;
        const delay = this.opts.retryDelayMs * 2 ** this.retryCount;
        this.retryCount++;
        this.retryTimer = setTimeout(async () => {
          if (this.destroyed) return;
          try {
            await this.initConnection();
            this._connected = true;
            this.retryCount = 0;
            this.startPing();
          } catch {
            this.scheduleReconnect();
          }
        }, delay);
      }
      cancelRetry() {
        if (this.retryTimer) {
          clearTimeout(this.retryTimer);
          this.retryTimer = null;
        }
      }
      startPing() {
        this.stopPing();
        this.pingTimer = setInterval(() => {
          this.send("Runtime.evaluate", { expression: "1" }).catch(() => {
          });
        }, this.opts.pingIntervalMs);
      }
      stopPing() {
        if (this.pingTimer) {
          clearInterval(this.pingTimer);
          this.pingTimer = null;
        }
      }
    };
  }
});

// src/executor/chrome-instance-profile.ts
import { existsSync as existsSync2 } from "fs";
async function resolveChromeBinary(channel = "system") {
  if (channel !== "system" && cachedByChannel[channel] !== void 0) {
    const cached = cachedByChannel[channel];
    if (cached) return cached;
    if (cached === null) throw new ChromeNotFoundError();
  }
  if (process.env.CHROME_PATH && existsSync2(process.env.CHROME_PATH)) {
    cachedByChannel[channel] = process.env.CHROME_PATH;
    return process.env.CHROME_PATH;
  }
  const explicit = channel !== "system" ? channel : "chrome";
  const table = process.platform === "win32" ? WIN_PATHS[explicit] ?? WIN_PATHS.chrome : POSIX_PATHS[explicit] ?? POSIX_PATHS.chrome;
  for (const p of table) {
    if (p.includes("/") || p.includes("\\")) {
      if (existsSync2(p)) {
        cachedByChannel[channel] = p;
        return p;
      }
    } else {
      const found = await probePath(p);
      if (found) {
        cachedByChannel[channel] = found;
        return found;
      }
    }
  }
  if (channel === "system") {
    for (const fam of ["chrome", "chromium", "edge"]) {
      try {
        return await resolveChromeBinary(fam);
      } catch {
      }
    }
  }
  cachedByChannel[channel] = null;
  throw new ChromeNotFoundError();
}
async function probePath(cmd) {
  try {
    const result = Bun.spawnSync(process.platform === "win32" ? ["where", cmd] : ["which", cmd], {
      stdout: "pipe",
      stderr: "pipe"
    });
    if (result.exitCode === 0) {
      const out = result.stdout.toString().trim().split("\n")[0]?.trim();
      if (out) return out;
    }
  } catch {
  }
  return null;
}
function buildChromeArgs(profile) {
  const args = [];
  const IS_WIN2 = process.platform === "win32";
  if (profile.debugPort) args.push(`--remote-debugging-port=${profile.debugPort}`);
  if (profile.userDataDir) {
    const safeDir = profile.userDataDir.replace(/[^a-zA-Z0-9.\-_:\\/ ]/g, "_");
    args.push(`--user-data-dir=${safeDir}`);
  }
  switch (profile.mode) {
    case "headless-new":
      args.push("--headless=new");
      args.push("--disable-gpu");
      break;
    case "headless":
      args.push("--headless");
      args.push("--disable-gpu");
      break;
    case "headed":
      args.push("--window-position=100,100");
      break;
  }
  if (profile.mode !== "headed" && IS_WIN2) {
    args.push("--window-position=-32000,-32000");
  }
  if (profile.windowSize) {
    args.push(`--window-size=${profile.windowSize.width},${profile.windowSize.height}`);
  }
  args.push("--disable-features=VizDisplayCompositor");
  args.push("--disable-background-timer-throttling");
  args.push("--disable-backgrounding-occluded-windows");
  args.push("--disable-renderer-backgrounding");
  args.push("--no-default-browser-check");
  args.push("--no-pings");
  args.push("--no-first-run");
  if (profile.mode !== "headed") {
    args.push("--no-startup-window");
  }
  args.push("--disable-session-crashed-bubble");
  args.push("--disable-blink-features=AutomationControlled");
  args.push(
    "--user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"
  );
  if (profile.extraArgs?.length) args.push(...profile.extraArgs);
  return args;
}
function makeProfile(partial) {
  const mode = partial.mode ?? "headless-new";
  return {
    channel: partial.channel ?? "system",
    mode,
    headless: mode !== "headed",
    userDataDir: partial.userDataDir,
    debugPort: partial.debugPort,
    windowSize: partial.windowSize,
    disableGpu: partial.disableGpu,
    launchTimeoutMs: partial.launchTimeoutMs ?? 15e3,
    extraArgs: partial.extraArgs ?? []
  };
}
var WIN_PATHS, POSIX_PATHS, cachedByChannel;
var init_chrome_instance_profile = __esm({
  "src/executor/chrome-instance-profile.ts"() {
    "use strict";
    init_errors();
    WIN_PATHS = {
      chrome: [
        "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",
        "C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe",
        `${process.env.LOCALAPPDATA}\\Google\\Chrome\\Application\\chrome.exe`
      ],
      chromium: [
        "C:\\Program Files\\Chromium\\Application\\chrome.exe",
        `${process.env.LOCALAPPDATA}\\Chromium\\Application\\chrome.exe`
      ],
      edge: [
        "C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe",
        "C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe"
      ]
    };
    POSIX_PATHS = {
      chrome: ["google-chrome", "google-chrome-stable", "chromium-browser", "chromium"],
      chromium: ["chromium", "chromium-browser"],
      edge: ["microsoft-edge", "microsoft-edge-stable"]
    };
    cachedByChannel = {};
  }
});

// src/executor/launcher.ts
import { rmSync } from "fs";
function clearSingletonLock(userDataDir) {
  if (!userDataDir) return;
  for (const name of ["SingletonLock", "SingletonCookie", "SingletonSocket"]) {
    try {
      rmSync(`${userDataDir}/${name}`, { force: true });
    } catch {
    }
  }
}
async function isPortInUse(port) {
  try {
    const resp = await fetch(`http://127.0.0.1:${port}/json/version`, {
      signal: AbortSignal.timeout(1e3)
    });
    return resp.ok;
  } catch {
    return false;
  }
}
async function freePort(start, span = 100) {
  for (let p = start; p < start + span; p++) {
    if (!await isPortInUse(p)) return p;
  }
  return start;
}
async function launchProfile(profile) {
  const binary = await resolveChromeBinary(profile.channel);
  let debugPort = profile.debugPort ?? 9222;
  if (await isPortInUse(debugPort)) {
    debugPort = await freePort(debugPort + 1);
  }
  clearSingletonLock(profile.userDataDir);
  const args = buildChromeArgs({ ...profile, debugPort });
  console.log(
    `[launcher] Spawning (${profile.channel}/${profile.mode}): ${binary} ${args.join(" ")}`
  );
  const proc = Bun.spawn([binary, ...args], {
    stdout: "ignore",
    stderr: "ignore",
    env: { ...process.env },
    detached: profile.mode === "headed"
  });
  const pid = proc.pid;
  const ready = await waitForPort(debugPort, profile.launchTimeoutMs);
  if (!ready) {
    try {
      proc.kill("SIGKILL");
    } catch {
    }
    throw new ChromeLaunchTimeoutError(debugPort, profile.launchTimeoutMs, binary);
  }
  return { process: proc, binary, debugPort, pid, profileDir: profile.userDataDir };
}
async function launchChrome(opts) {
  const profile = makeProfile({
    userDataDir: opts?.userDataDir ?? opts?.profileDir ?? (IS_WIN ? `${process.env.LOCALAPPDATA}\\Temp\\chrome-profile-${Date.now()}` : `/tmp/chrome-profile-${Date.now()}`),
    channel: "system",
    mode: opts?.visible ? "headed" : "headless-new",
    debugPort: opts?.debugPort,
    windowSize: opts?.windowSize,
    disableGpu: opts?.disableGpu,
    extraArgs: opts?.extraArgs ?? [],
    launchTimeoutMs: 15e3
  });
  return launchProfile(profile);
}
async function waitForPort(port, timeoutMs) {
  const start = Date.now();
  while (Date.now() - start < timeoutMs) {
    if (await isPortInUse(port)) return true;
    await Bun.sleep(200);
  }
  return false;
}
async function killChrome(pid) {
  if (!pid) return;
  if (IS_WIN) {
    Bun.spawn({
      cmd: ["taskkill", "/PID", String(pid), "/F", "/T"],
      stdout: "ignore",
      stderr: "ignore"
    });
    return;
  }
  try {
    process.kill(pid, "SIGTERM");
  } catch {
    return;
  }
  const start = Date.now();
  while (Date.now() - start < 5e3) {
    if (!await isChromeRunning(pid)) return;
    await Bun.sleep(100);
  }
  try {
    process.kill(pid, "SIGKILL");
  } catch {
  }
}
async function isChromeRunning(pid) {
  if (!pid) return false;
  try {
    if (IS_WIN) {
      const proc = Bun.spawn({
        cmd: ["tasklist", "/FI", `PID eq ${pid}`, "/NH"],
        stdout: "pipe",
        stderr: "ignore"
      });
      const text = await new Response(proc.stdout).text();
      return !text.includes("INFO: No tasks") && text.includes(String(pid));
    }
    process.kill(pid, 0);
    return true;
  } catch {
    return false;
  }
}
var IS_WIN, ChromeLaunchTimeoutError;
var init_launcher = __esm({
  "src/executor/launcher.ts"() {
    "use strict";
    init_chrome_instance_profile();
    IS_WIN = process.platform === "win32";
    ChromeLaunchTimeoutError = class extends Error {
      constructor(port, timeoutMs, binary) {
        super(`Chrome did not open debug port ${port} within ${timeoutMs}ms (${binary})`);
        this.port = port;
        this.timeoutMs = timeoutMs;
        this.binary = binary;
        this.name = "ChromeLaunchTimeoutError";
      }
      port;
      timeoutMs;
      binary;
    };
  }
});

// src/executor/port-reaper.ts
var PortReaper;
var init_port_reaper = __esm({
  "src/executor/port-reaper.ts"() {
    "use strict";
    PortReaper = class {
      opts;
      knownPids = /* @__PURE__ */ new Map();
      // port -> pid
      periodicTimer = null;
      constructor(opts) {
        this.opts = {
          defaultPortRange: opts?.defaultPortRange ?? [9222, 9230],
          periodicIntervalMs: opts?.periodicIntervalMs ?? 3e4
        };
      }
      trackPid(port, pid) {
        this.knownPids.set(port, pid);
      }
      untrackPid(port) {
        this.knownPids.delete(port);
      }
      async reap(portRange) {
        const start = Date.now();
        const range = portRange ?? this.opts.defaultPortRange;
        const orphans = await this.findOrphans(range);
        let reaped = 0;
        let failed = 0;
        const reapedOrphans = [];
        for (const orphan of orphans) {
          try {
            const ok = await this.reapProcess(orphan.pid);
            if (ok) {
              reaped++;
              reapedOrphans.push({ pid: orphan.pid, port: orphan.port });
            } else {
              failed++;
            }
          } catch {
            failed++;
          }
        }
        return {
          reaped,
          failed,
          orphans: reapedOrphans,
          durationMs: Date.now() - start
        };
      }
      async reapProcess(pid) {
        if (process.platform === "win32") {
          const proc = Bun.spawn({
            cmd: ["taskkill", "/PID", String(pid), "/F", "/T"],
            stdout: "pipe",
            stderr: "pipe"
          });
          await proc.exited;
          return proc.exitCode === 0;
        }
        try {
          process.kill(pid, "SIGTERM");
        } catch {
          return false;
        }
        const start = Date.now();
        while (Date.now() - start < 2e3) {
          if (!this.isProcessRunning(pid)) return true;
          await Bun.sleep(100);
        }
        try {
          process.kill(pid, "SIGKILL");
          return true;
        } catch {
          return false;
        }
      }
      async findOrphans(portRange) {
        const orphans = [];
        const [start, end] = portRange;
        for (let port = start; port <= end; port++) {
          const pid = await this.getPidOnPort(port);
          if (pid === null) continue;
          const expectedPid = this.knownPids.get(port);
          if (expectedPid !== void 0 && expectedPid === pid) continue;
          const cmd = await this.getProcessCommand(pid);
          if (!this.isChromeProcess(cmd)) continue;
          orphans.push({ pid, port, cmd });
        }
        return orphans;
      }
      startPeriodicReap(intervalMs) {
        this.stopPeriodicReap();
        const ms = intervalMs ?? this.opts.periodicIntervalMs;
        this.periodicTimer = setInterval(() => {
          this.reap().catch(() => {
          });
        }, ms);
      }
      stopPeriodicReap() {
        if (this.periodicTimer) {
          clearInterval(this.periodicTimer);
          this.periodicTimer = null;
        }
      }
      async getPidOnPort(port) {
        if (process.platform === "win32") {
          try {
            const proc = Bun.spawn({
              cmd: ["cmd", "/c", `netstat -ano | findstr :${port} | findstr LISTENING`],
              stdout: "pipe",
              stderr: "ignore"
            });
            const output = (await new Response(proc.stdout).text()).trim();
            for (const line of output.split("\n").filter(Boolean)) {
              const parts = line.trim().split(/\s+/);
              const pid = Number.parseInt(parts[parts.length - 1] ?? "", 10);
              if (Number.isFinite(pid) && pid > 0) return pid;
            }
          } catch {
          }
          return null;
        }
        try {
          const proc = Bun.spawn({
            cmd: ["lsof", "-t", `-i:${port}`],
            stdout: "pipe",
            stderr: "ignore"
          });
          const pidText = (await new Response(proc.stdout).text()).trim();
          const firstPid = pidText.split("\n").filter(Boolean)[0];
          if (firstPid) {
            const pid = Number.parseInt(firstPid, 10);
            if (Number.isFinite(pid) && pid > 0) return pid;
          }
        } catch {
        }
        return null;
      }
      async getProcessCommand(pid) {
        if (process.platform === "win32") {
          try {
            const proc = Bun.spawn({
              cmd: ["wmic", "process", "where", `ProcessId=${pid}`, "get", "CommandLine"],
              stdout: "pipe",
              stderr: "ignore"
            });
            const output = (await new Response(proc.stdout).text()).trim();
            const lines = output.split("\n").filter(Boolean);
            return lines[1] ?? "";
          } catch {
            return "";
          }
        }
        try {
          const proc = Bun.spawn({
            cmd: ["cat", `/proc/${pid}/cmdline`],
            stdout: "pipe",
            stderr: "ignore"
          });
          const output = (await new Response(proc.stdout).text()).trim();
          return output.replace(/\0/g, " ");
        } catch {
          return "";
        }
      }
      isChromeProcess(cmd) {
        const lower = cmd.toLowerCase();
        return lower.includes("chrome") || lower.includes("chromium");
      }
      isProcessRunning(pid) {
        try {
          process.kill(pid, 0);
          return true;
        } catch {
          return false;
        }
      }
    };
  }
});

// src/executor/profile-allocator.ts
var profile_allocator_exports = {};
__export(profile_allocator_exports, {
  DEFAULT_PROFILE_BASE: () => DEFAULT_PROFILE_BASE,
  ProfileAllocator: () => ProfileAllocator
});
import { existsSync as existsSync3 } from "fs";
import { mkdir, readFile, readdir as readdir2, rm, writeFile } from "fs/promises";
import { join as join3, resolve } from "path";
var DEFAULT_PROFILE_BASE, ProfileAllocator;
var init_profile_allocator = __esm({
  "src/executor/profile-allocator.ts"() {
    "use strict";
    DEFAULT_PROFILE_BASE = "chrome-profiles";
    ProfileAllocator = class _ProfileAllocator {
      baseDir;
      constructor(baseDir) {
        this.baseDir = baseDir ?? DEFAULT_PROFILE_BASE;
      }
      /**
       * Sanitize a directory name component (e.g. email) for filesystem use.
       * Replaces @ with -at- to avoid Chrome --user-data-dir issues.
       */
      static sanitizeDirName(name) {
        return name.replace(/@/g, "-at-");
      }
      getPath(providerSlug, accountId) {
        return resolve(join3(this.baseDir, providerSlug, _ProfileAllocator.sanitizeDirName(accountId)));
      }
      async allocate(providerSlug, accountId) {
        const dir = this.getPath(providerSlug, accountId);
        await mkdir(dir, { recursive: true });
        const metaPath = join3(dir, ".profile-meta.json");
        if (!existsSync3(metaPath)) {
          const now = (/* @__PURE__ */ new Date()).toISOString();
          const meta = {
            providerSlug,
            accountId,
            allocatedAt: now,
            lastUsed: now
          };
          await writeFile(metaPath, JSON.stringify(meta, null, 2));
        }
        return dir;
      }
      async release(providerSlug, accountId) {
        const dir = this.getPath(providerSlug, accountId);
        const metaPath = join3(dir, ".profile-meta.json");
        if (existsSync3(metaPath)) {
          const raw = await readFile(metaPath, "utf-8");
          const meta = JSON.parse(raw);
          meta.lastUsed = (/* @__PURE__ */ new Date()).toISOString();
          await writeFile(metaPath, JSON.stringify(meta, null, 2));
        }
      }
      async list() {
        const results = [];
        if (!existsSync3(this.baseDir)) return results;
        const providers = await readdir2(this.baseDir, { withFileTypes: true });
        for (const provider of providers) {
          if (!provider.isDirectory()) continue;
          const accountsDir = join3(this.baseDir, provider.name);
          const accounts = await readdir2(accountsDir, { withFileTypes: true });
          for (const account of accounts) {
            if (!account.isDirectory()) continue;
            const dir = join3(accountsDir, account.name);
            const metaPath = join3(dir, ".profile-meta.json");
            let lastUsed = /* @__PURE__ */ new Date(0);
            if (existsSync3(metaPath)) {
              try {
                const raw = await readFile(metaPath, "utf-8");
                const meta = JSON.parse(raw);
                lastUsed = new Date(meta.lastUsed);
              } catch {
              }
            }
            results.push({
              providerSlug: provider.name,
              accountId: account.name,
              path: dir,
              lastUsed
            });
          }
        }
        return results;
      }
      async clean(olderThanDays = 30) {
        const profiles = await this.list();
        const cutoff = Date.now() - olderThanDays * 24 * 60 * 60 * 1e3;
        let removed = 0;
        for (const profile of profiles) {
          if (profile.lastUsed.getTime() < cutoff) {
            try {
              await rm(profile.path, { recursive: true, force: true });
              removed++;
            } catch {
            }
          }
        }
        return removed;
      }
      /**
       * True when the profile directory holds a persisted authenticated session.
       * Chrome stores session cookies in `Cookies` / `Cookies-journal`; their
       * presence (non-empty) is the source of truth for "is this provider
       * authenticated" (FR-7/FR-8/FR-23). We never copy sessions from an external
       * profile — this only reads the owning profile dir.
       */
      async isAuthenticated(profileDir) {
        if (!existsSync3(profileDir)) return false;
        for (const cookieFile of ["Cookies", "Cookies-journal", "Network/Cookies"]) {
          try {
            const full = join3(profileDir, cookieFile);
            if (existsSync3(full)) {
              const stat2 = await import("fs/promises").then((m) => m.stat(full));
              if (stat2.size > 0) return true;
            }
          } catch {
          }
        }
        return false;
      }
      /** True when the provider has no persisted authenticated session (FR-7). */
      async requiresFirstRun(profileDir) {
        return !await this.isAuthenticated(profileDir);
      }
    };
  }
});

// src/executor/fleet-supervisor.ts
var PROVIDER_URLS, SlaveNotRunningError, CircuitOpenError, PortOccupiedError, FleetSupervisor;
var init_fleet_supervisor = __esm({
  "src/executor/fleet-supervisor.ts"() {
    "use strict";
    init_cdp();
    init_launcher();
    init_port_reaper();
    init_profile_allocator();
    PROVIDER_URLS = {
      chatgpt: "https://chatgpt.com/",
      claude: "https://claude.ai/",
      gemini: "https://gemini.google.com/"
    };
    SlaveNotRunningError = class extends Error {
      constructor(instanceId) {
        super(`Slave not running: ${instanceId}`);
        this.name = "SlaveNotRunningError";
      }
    };
    CircuitOpenError = class extends Error {
      constructor(instanceId) {
        super(`Circuit breaker open: ${instanceId}`);
        this.name = "CircuitOpenError";
      }
    };
    PortOccupiedError = class extends Error {
      constructor(range) {
        super(`All ports in range ${range} occupied`);
        this.name = "PortOccupiedError";
      }
    };
    FleetSupervisor = class {
      constructor(store, opts) {
        this.store = store;
        this.opts = {
          portRange: opts?.portRange ?? [9222, 9332],
          healthProbeIntervalMs: opts?.healthProbeIntervalMs ?? 3e4,
          healthProbeTimeoutMs: opts?.healthProbeTimeoutMs ?? 5e3,
          autoRestart: opts?.autoRestart ?? true,
          maxRestarts: opts?.maxRestarts ?? 3,
          circuitBreakerThreshold: opts?.circuitBreakerThreshold ?? 5,
          circuitBreakerResetMs: opts?.circuitBreakerResetMs ?? 6e4,
          chromeProfileBase: opts?.chromeProfileBase ?? "chrome-profiles"
        };
        this.nextPort = this.opts.portRange[0];
        this.profileAllocator = new ProfileAllocator(this.opts.chromeProfileBase);
        this.portReaper = new PortReaper({ defaultPortRange: this.opts.portRange });
      }
      store;
      instances = /* @__PURE__ */ new Map();
      circuits = /* @__PURE__ */ new Map();
      healthTimer = null;
      nextPort;
      profileAllocator;
      portReaper;
      opts;
      // ── Boot ───────────────────────────────────────────────────────────────
      async boot() {
        await this.portReaper.reap(this.opts.portRange);
        if (this.opts.autoRestart) {
          this.startHealthProbe();
        }
      }
      // ── Lifecycle ──────────────────────────────────────────────────────────
      async spawn(providerSlug, accountId, opts) {
        const id = `${providerSlug}_${accountId}_${Date.now()}`;
        const compositeAccountId = `${providerSlug}_${accountId}`;
        const existingAccount = await this.store.getAccount(compositeAccountId);
        const profileDir = existingAccount?.profileDir ?? await this.profileAllocator.allocate(providerSlug, accountId);
        const debugPort = opts?.debugPort ?? existingAccount?.debugPort ?? this.allocatePort();
        const instance2 = {
          id,
          providerSlug,
          accountId,
          debugPort,
          profileDir,
          status: "starting",
          pid: null,
          consecutiveFailures: 0,
          lastHealthCheck: Date.now(),
          createdAt: Date.now()
        };
        this.instances.set(id, instance2);
        try {
          const launchOpts = {
            visible: opts?.visible ?? false,
            debugPort,
            profileDir,
            extraArgs: opts?.extraArgs ?? []
          };
          const result = await launchChrome(launchOpts);
          instance2.pid = result.pid;
          instance2.debugPort = result.debugPort;
          instance2.status = "running";
          this.portReaper.trackPid(result.debugPort, result.pid);
          const loginUrl = PROVIDER_URLS[providerSlug] ?? `https://${providerSlug}.com`;
          try {
            const navCdp = new BunCdpClient(`ws://127.0.0.1:${result.debugPort}/devtools/browser`);
            await navCdp.connect();
            await navCdp.send("Target.createTarget", { url: loginUrl });
            await navCdp.disconnect();
          } catch {
          }
          await this.store.createFleetEvent({
            slaveId: id,
            providerId: providerSlug,
            eventType: "spawned",
            detailJson: JSON.stringify({ pid: result.pid, port: result.debugPort })
          });
        } catch (err) {
          instance2.status = "error";
          instance2.consecutiveFailures++;
          await this.store.createFleetEvent({
            slaveId: id,
            providerId: providerSlug,
            eventType: "spawn_failed",
            detailJson: JSON.stringify({ error: String(err) })
          });
        }
        return instance2;
      }
      async kill(instanceId) {
        const instance2 = this.instances.get(instanceId);
        if (!instance2) throw new SlaveNotRunningError(instanceId);
        instance2.status = "stopping";
        if (instance2.pid !== null) {
          await killChrome(instance2.pid);
          this.portReaper.untrackPid(instance2.debugPort);
        }
        instance2.status = "stopped";
        instance2.pid = null;
        await this.store.createFleetEvent({
          slaveId: instanceId,
          providerId: instance2.providerSlug,
          eventType: "killed"
        });
      }
      async killAll() {
        for (const id of this.instances.keys()) {
          await this.kill(id);
        }
      }
      async ensureRunning(instanceId) {
        const instance2 = this.instances.get(instanceId);
        if (!instance2) throw new SlaveNotRunningError(instanceId);
        if (instance2.status === "running") return instance2;
        if (instance2.status === "crashed" || instance2.status === "error") {
          if (!this.opts.autoRestart) {
            throw new SlaveNotRunningError(instanceId);
          }
          if (instance2.consecutiveFailures >= this.opts.maxRestarts) {
            throw new CircuitOpenError(instanceId);
          }
          const cb = this.getCircuit(instanceId);
          if (cb.state === "open") {
            throw new CircuitOpenError(instanceId);
          }
          return this.spawn(instance2.providerSlug, instance2.accountId, {
            visible: false,
            debugPort: instance2.debugPort
          });
        }
        if (instance2.status === "stopped") {
          return this.spawn(instance2.providerSlug, instance2.accountId, {
            visible: false,
            debugPort: instance2.debugPort
          });
        }
        return instance2;
      }
      // ── Queries ────────────────────────────────────────────────────────────
      getInstance(instanceId) {
        return this.instances.get(instanceId) ?? null;
      }
      getAllInstances() {
        return [...this.instances.values()];
      }
      getInstancesByProvider(providerSlug) {
        return [...this.instances.values()].filter((i) => i.providerSlug === providerSlug);
      }
      // ── Health ─────────────────────────────────────────────────────────────
      async healthCheck(instanceId) {
        const instance2 = this.instances.get(instanceId);
        if (!instance2) {
          return { ok: false, latencyMs: 0, status: "stopped", error: "Instance not found" };
        }
        if (instance2.status === "stopped") {
          return { ok: false, latencyMs: 0, status: "stopped" };
        }
        const start = Date.now();
        try {
          const cdp = new BunCdpClient(`ws://127.0.0.1:${instance2.debugPort}/devtools/browser`);
          await cdp.connect();
          await cdp.send("Browser.getVersion");
          await cdp.disconnect();
          const latencyMs = Date.now() - start;
          instance2.status = "running";
          instance2.lastHealthCheck = Date.now();
          instance2.consecutiveFailures = 0;
          const cb = this.getCircuit(instanceId);
          cb.failures = 0;
          if (cb.state === "half_open") {
            cb.state = "closed";
            cb.openedAt = null;
          }
          return { ok: true, latencyMs, status: "running" };
        } catch (err) {
          const latencyMs = Date.now() - start;
          instance2.consecutiveFailures++;
          instance2.lastHealthCheck = Date.now();
          instance2.status = "error";
          const cb = this.getCircuit(instanceId);
          cb.failures++;
          if (cb.failures >= this.opts.circuitBreakerThreshold && cb.state !== "open") {
            cb.state = "open";
            cb.openedAt = Date.now();
          }
          return {
            ok: false,
            latencyMs,
            status: "error",
            error: err instanceof Error ? err.message : String(err)
          };
        }
      }
      async healthCheckAll() {
        const results = /* @__PURE__ */ new Map();
        for (const id of this.instances.keys()) {
          results.set(id, await this.healthCheck(id));
        }
        return results;
      }
      getCircuitState(instanceId) {
        return this.getCircuit(instanceId).state;
      }
      // ── Health probe timer ─────────────────────────────────────────────────
      startHealthProbe(intervalMs) {
        this.stopHealthProbe();
        const ms = intervalMs ?? this.opts.healthProbeIntervalMs;
        this.healthTimer = setInterval(() => {
          void this.healthCheckAll();
        }, ms);
      }
      stopHealthProbe() {
        if (this.healthTimer) {
          clearInterval(this.healthTimer);
          this.healthTimer = null;
        }
      }
      // ── Helpers ────────────────────────────────────────────────────────────
      allocatePort() {
        const port = this.nextPort;
        if (port > this.opts.portRange[1]) {
          throw new PortOccupiedError(`${this.opts.portRange[0]}-${this.opts.portRange[1]}`);
        }
        this.nextPort++;
        return port;
      }
      getCircuit(instanceId) {
        let cb = this.circuits.get(instanceId);
        if (!cb) {
          cb = { state: "closed", failures: 0, openedAt: null };
          this.circuits.set(instanceId, cb);
        }
        if (cb.state === "open" && cb.openedAt) {
          if (Date.now() - cb.openedAt >= this.opts.circuitBreakerResetMs) {
            cb.state = "half_open";
          }
        }
        return cb;
      }
    };
  }
});

// src/engines/composer-typing.ts
async function typeMessage(transport, slaveId, selector, text, composerType) {
  const safeSelector = JSON.stringify(selector);
  const safeText = JSON.stringify(text);
  let expression;
  switch (composerType) {
    case "textarea":
      expression = `(() => {
        const el = document.querySelector(${safeSelector});
        if (!el) throw new EngineError('Composer not found: ' + ${safeSelector});
        el.focus();
        const setter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value')?.set;
        setter?.call(el, ${safeText});
        el.dispatchEvent(new Event('input', { bubbles: true }));
        el.dispatchEvent(new Event('change', { bubbles: true }));
      })()`;
      break;
    case "contenteditable":
      expression = `(() => {
        const el = document.querySelector(${safeSelector});
        if (!el) throw new EngineError('Composer not found: ' + ${safeSelector});
        el.focus();
        el.textContent = '';
        document.execCommand('insertText', false, ${safeText});
      })()`;
      break;
    case "quill":
      expression = `(() => {
        const el = document.querySelector(${safeSelector});
        if (!el) throw new EngineError('Composer not found: ' + ${safeSelector});
        const quill = el.__quill || el.closest('.ql-container')?.__quill;
        if (quill) {
          quill.setContents([]);
          quill.insertText(0, ${safeText});
        } else {
          el.focus();
          el.textContent = ${safeText};
          el.dispatchEvent(new Event('input', { bubbles: true }));
        }
      })()`;
      break;
    case "codemirror":
      expression = `(() => {
        const el = document.querySelector(${safeSelector});
        if (!el) throw new EngineError('Composer not found: ' + ${safeSelector});
        const cm = el.closest('.CodeMirror')?.CodeMirror;
        if (cm) {
          cm.setValue(${safeText});
        } else {
          el.focus();
          const setter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value')?.set;
          setter?.call(el, ${safeText});
          el.dispatchEvent(new Event('input', { bubbles: true }));
        }
      })()`;
      break;
    default:
      expression = `(() => {
        const el = document.querySelector(${safeSelector});
        if (!el) throw new EngineError('Composer not found: ' + ${safeSelector});
        el.focus();
        el.value = ${safeText};
        el.dispatchEvent(new Event('input', { bubbles: true }));
      })()`;
  }
  await transport.send(slaveId, "Runtime.evaluate", { expression });
}
async function submitMessage(transport, slaveId, sendSelector, key = "Enter") {
  if (sendSelector) {
    const safeSelector = JSON.stringify(sendSelector);
    await transport.send(slaveId, "Runtime.evaluate", {
      expression: `document.querySelector(${safeSelector})?.click()`
    });
    return;
  }
  await transport.send(slaveId, "Input.dispatchKeyEvent", {
    type: "keyDown",
    key,
    code: key === "Enter" ? "Enter" : key
  });
  await transport.send(slaveId, "Input.dispatchKeyEvent", {
    type: "keyUp",
    key,
    code: key === "Enter" ? "Enter" : key
  });
}
var init_composer_typing = __esm({
  "src/engines/composer-typing.ts"() {
    "use strict";
  }
});

// src/engines/harness/program-schema.ts
function configToProgram(configJson) {
  const parsed = JSON.parse(configJson);
  if (!parsed || typeof parsed !== "object")
    throw new ValidationError("ProgramConfig must be an object");
  const cfg = parsed;
  if (typeof cfg.schemaVersion !== "number")
    throw new ValidationError("ProgramConfig.schemaVersion missing");
  if (!cfg.recipe || typeof cfg.recipe !== "object")
    throw new ValidationError("ProgramConfig.recipe missing");
  return cfg;
}
var init_program_schema = __esm({
  "src/engines/harness/program-schema.ts"() {
    "use strict";
    init_errors();
  }
});

// src/engines/chrome-governor.ts
var chrome_governor_exports = {};
__export(chrome_governor_exports, {
  AsyncMutex: () => AsyncMutex,
  CDPProxy: () => CDPProxy,
  ChromeGovernor: () => ChromeGovernor,
  HealthMonitor: () => HealthMonitor,
  TraceLog: () => TraceLog,
  circuitRecordFailure: () => circuitRecordFailure,
  circuitRecordSuccess: () => circuitRecordSuccess,
  circuitTryAcquire: () => circuitTryAcquire,
  createCircuitBreaker: () => createCircuitBreaker
});
import { join as join4 } from "path";
function createCircuitBreaker() {
  return {
    state: "closed",
    failureCount: 0,
    lastFailureAt: null,
    lastSuccessAt: null,
    openedAt: null
  };
}
function circuitRecordSuccess(cb, threshold, resetMs) {
  const now = Date.now();
  cb.lastSuccessAt = now;
  cb.failureCount = 0;
  if (cb.state === "half_open") {
    cb.state = "closed";
    cb.openedAt = null;
  }
  void threshold;
  void resetMs;
}
function circuitRecordFailure(cb, threshold, _resetMs) {
  const now = Date.now();
  cb.failureCount++;
  cb.lastFailureAt = now;
  if (cb.state === "half_open") {
    cb.state = "open";
    cb.openedAt = now;
    return "open";
  }
  if (cb.failureCount >= threshold) {
    cb.state = "open";
    cb.openedAt = now;
    return "open";
  }
  return cb.state;
}
function circuitTryAcquire(cb, resetMs) {
  if (cb.state === "closed") return true;
  if (cb.state === "half_open") return true;
  if (cb.openedAt && Date.now() - cb.openedAt >= resetMs) {
    cb.state = "half_open";
    return true;
  }
  return false;
}
var AsyncMutex, CDPProxy, TraceLog, HealthMonitor, ChromeGovernor;
var init_chrome_governor = __esm({
  "src/engines/chrome-governor.ts"() {
    "use strict";
    init_errors();
    init_fleet_supervisor();
    init_composer_typing();
    init_program_schema();
    AsyncMutex = class {
      locked = false;
      queue = [];
      async acquire() {
        if (!this.locked) {
          this.locked = true;
          return;
        }
        return new Promise((resolve4) => {
          this.queue.push(resolve4);
        });
      }
      release() {
        const next = this.queue.shift();
        if (next) {
          next();
        } else {
          this.locked = false;
        }
      }
    };
    CDPProxy = class {
      constructor(slaves, mutexes, transport, eventBus, browserHarness) {
        this.slaves = slaves;
        this.mutexes = mutexes;
        this.transport = transport;
        this.eventBus = eventBus;
        this.browserHarness = browserHarness;
      }
      slaves;
      mutexes;
      transport;
      eventBus;
      browserHarness;
      /** 019 — in-memory snapshot of DB-backed capabilities, loaded at boot. */
      capabilitySnapshot;
      /** Wire the boot-loaded capability snapshot (source of truth for execution). */
      setCapabilitySnapshot(snapshot) {
        this.capabilitySnapshot = snapshot;
      }
      async send(slaveId, method, params) {
        const slave = await this.ensureConnected(slaveId);
        if (slave.circuitState === "open")
          throw new EngineError(`Circuit breaker open for slave: ${slaveId}`);
        const mutex = this.getMutex(slaveId);
        await mutex.acquire();
        try {
          const start = Date.now();
          const result = await this.transport?.send(slaveId, method, params);
          this.eventBus?.emit("cdp:executed", {
            slaveId,
            method,
            durationMs: Date.now() - start
          });
          return result;
        } finally {
          mutex.release();
        }
      }
      async capture(slaveId, pattern2, timeoutMs) {
        const _slave = await this.ensureConnected(slaveId);
        const mutex = this.getMutex(slaveId);
        await mutex.acquire();
        try {
          const result = await this.transport?.capture(slaveId, pattern2, timeoutMs);
          if (!result) throw new EngineError("CDP transport not configured");
          return result;
        } finally {
          mutex.release();
        }
      }
      async executeHarnessPlan(slaveId, dag) {
        const slave = await this.ensureConnected(slaveId);
        if (slave.circuitState === "open")
          throw new EngineError(`Circuit breaker open for slave: ${slaveId}`);
        if (!this.transport) throw new EngineError("CDP transport not configured");
        const mutex = this.getMutex(slaveId);
        await mutex.acquire();
        try {
          const order = this.orderNodes(dag);
          let stepsCompleted = 0;
          let capturedBody;
          for (const idx of order) {
            const node = dag.nodes[idx];
            if (!node) continue;
            const action = node.action ?? node.moduleId ?? node.type;
            const params = { ...node.params ?? {}, ...node.input ?? {} };
            switch (action) {
              case "type_text": {
                const selector = typeof params.selector === "string" ? params.selector : "textarea";
                const text = typeof params.text === "string" ? params.text : "";
                const composerType = typeof params.composerType === "string" ? params.composerType : "textarea";
                await typeMessage(this.transport, slaveId, selector, text, composerType);
                stepsCompleted++;
                break;
              }
              case "submit": {
                const sendSelector = typeof params.sendSelector === "string" ? params.sendSelector : void 0;
                const key = typeof params.key === "string" ? params.key : "Enter";
                await submitMessage(this.transport, slaveId, sendSelector, key);
                stepsCompleted++;
                break;
              }
              case "click": {
                const selector = typeof params.selector === "string" ? params.selector : "button";
                await this.transport?.send(slaveId, "Runtime.evaluate", {
                  expression: `document.querySelector(${JSON.stringify(selector)})?.click()`,
                  returnByValue: true
                });
                stepsCompleted++;
                break;
              }
              case "wait": {
                const ms = typeof params.timeoutMs === "number" ? params.timeoutMs : 1e3;
                await new Promise((r) => setTimeout(r, ms));
                stepsCompleted++;
                break;
              }
              case "navigate": {
                const url = typeof params.url === "string" ? params.url : "";
                if (url)
                  await this.transport?.send(slaveId, "Runtime.evaluate", {
                    expression: `window.location.href = ${JSON.stringify(url)}`,
                    returnByValue: true
                  });
                stepsCompleted++;
                break;
              }
              case "capture": {
                const pattern2 = params.pattern instanceof RegExp ? params.pattern : void 0;
                const timeoutMs = typeof params.timeoutMs === "number" ? params.timeoutMs : 5e3;
                const cap = await this.capture(slaveId, pattern2 ?? /.*/s, timeoutMs);
                if (cap?.body) capturedBody = cap.body;
                stepsCompleted++;
                break;
              }
              case "evaluate": {
                const expression = typeof params.expression === "string" ? params.expression : "undefined";
                await this.transport?.send(slaveId, "Runtime.evaluate", {
                  expression,
                  returnByValue: true
                });
                stepsCompleted++;
                break;
              }
              // ── Extended browser-automation vocabulary (recipe-compiler) ──
              case "scroll": {
                const x = typeof params.x === "number" ? params.x : 0;
                const y = typeof params.y === "number" ? params.y : 0;
                const expr = typeof params.selector === "string" ? `document.querySelector(${JSON.stringify(params.selector)})?.scrollIntoView()` : `window.scrollBy(${x},${y})`;
                await this.transport?.send(slaveId, "Runtime.evaluate", {
                  expression: expr,
                  returnByValue: true
                });
                stepsCompleted++;
                break;
              }
              case "hover":
              case "select":
              case "press":
              case "upload":
              case "wait_selector":
              case "wait_text":
              case "screenshot":
              case "assert":
              case "mock_request":
              case "cookie_set":
              case "observe": {
                if (this.browserHarness) {
                  await this.browserHarness.runAction(slaveId, action, params);
                }
                stepsCompleted++;
                break;
              }
              case "tab_open": {
                await this.transport?.send(slaveId, "Target.createTarget", {
                  url: params.url ?? "about:blank"
                });
                stepsCompleted++;
                break;
              }
              case "tab_close": {
                if (params.targetId)
                  await this.transport?.send(slaveId, "Target.closeTarget", {
                    targetId: params.targetId
                  });
                else await this.transport?.send(slaveId, "Page.close", {});
                stepsCompleted++;
                break;
              }
              case "tab_switch": {
                await this.transport?.send(slaveId, "Target.activateTarget", { targetId: params.targetId }).catch(() => {
                });
                stepsCompleted++;
                break;
              }
              case "extract_markdown": {
                await this.transport?.send(slaveId, "Runtime.evaluate", {
                  expression: `document.body.innerText.replace(/\\n{3,}/g,'\\n\\n').trim()`,
                  returnByValue: true
                });
                stepsCompleted++;
                break;
              }
              case "human_gate": {
                this.eventBus?.emit("harness:human_gate", { slaveId, prompt: params.prompt });
                stepsCompleted++;
                break;
              }
              default:
                stepsCompleted++;
            }
            this.eventBus?.emit("harness:step", { slaveId, action, step: stepsCompleted });
          }
          return { success: true, stepsCompleted, capturedBody };
        } catch (err) {
          return {
            success: false,
            stepsCompleted: 0,
            error: err instanceof Error ? err.message : String(err)
          };
        } finally {
          mutex.release();
        }
      }
      /** Returns node indices in dependency order (edges) or declaration order. */
      orderNodes(dag) {
        if (!dag.edges.length) return dag.nodes.map((_, i) => i);
        const indeg = new Array(dag.nodes.length).fill(0);
        const adj = /* @__PURE__ */ new Map();
        for (const e of dag.edges) {
          indeg[e.to] = (indeg[e.to] ?? 0) + 1;
          const list = adj.get(e.from) ?? [];
          list.push(e.to);
          adj.set(e.from, list);
        }
        const queue = [];
        for (let i = 0; i < indeg.length; i++) if (indeg[i] === 0) queue.push(i);
        const out = [];
        while (queue.length) {
          const n = queue.shift();
          if (n === void 0) break;
          out.push(n);
          for (const m of adj.get(n) ?? []) {
            indeg[m]--;
            if (indeg[m] === 0) queue.push(m);
          }
        }
        return out.length === dag.nodes.length ? out : dag.nodes.map((_, i) => i);
      }
      async getPageState(slaveId) {
        await this.ensureConnected(slaveId);
        if (!this.transport) return { url: "", title: "", readyState: "unavailable" };
        return this.transport.getPageState(slaveId);
      }
      async captureScreenshot(slaveId, format) {
        await this.ensureConnected(slaveId);
        if (!this.transport) throw new EngineError("CDP transport not configured");
        return this.transport.captureScreenshot(slaveId, format);
      }
      /**
       * Resolve and (if needed) connect a slave's CDP client. The slaves map is a
       * live view of the fleet, so this always reflects the current set — including
       * instances spawned during this request.
       */
      async ensureConnected(slaveId) {
        const slave = this.slaves.get(slaveId);
        if (!slave) throw new EngineError(`Slave not found: ${slaveId}`);
        if (this.transport?.connect && this.transport.isConnected && !this.transport.isConnected(slaveId)) {
          await this.transport.connect(slaveId, slave.debugPort);
        }
        return slave;
      }
      getMutex(slaveId) {
        let mutex = this.mutexes.get(slaveId);
        if (!mutex) {
          mutex = new AsyncMutex();
          this.mutexes.set(slaveId, mutex);
        }
        return mutex;
      }
    };
    TraceLog = class {
      constructor(store) {
        this.store = store;
      }
      store;
      async record(entry) {
        return this.store.createTraceEntry(entry);
      }
      async getTrace(slaveId, limit) {
        return this.store.getTrace(slaveId, limit);
      }
      async getConversationTrace(conversationId) {
        const all = await this.store.getTrace("*", 1e3);
        return all.filter((e) => e.conversationId === conversationId);
      }
    };
    HealthMonitor = class {
      constructor(store, slaves, circuitBreakers, cdpProxy, config2, eventBus) {
        this.store = store;
        this.slaves = slaves;
        this.circuitBreakers = circuitBreakers;
        this.cdpProxy = cdpProxy;
        this.config = config2;
        this.eventBus = eventBus;
      }
      store;
      slaves;
      circuitBreakers;
      cdpProxy;
      config;
      eventBus;
      timerHandle = null;
      start(intervalMs) {
        this.stop();
        const interval = intervalMs ?? this.config.healthProbeIntervalMs;
        this.timerHandle = setInterval(() => {
          void this.probeAll();
        }, interval);
      }
      stop() {
        if (this.timerHandle !== null) {
          clearInterval(this.timerHandle);
          this.timerHandle = null;
        }
      }
      async probe(slaveId) {
        const slave = this.slaves.get(slaveId);
        if (!slave) return false;
        try {
          await this.cdpProxy.send(slaveId, "Browser.getVersion");
          const prevStatus = slave.status;
          slave.status = "running";
          slave.lastHealthCheck = Date.now();
          slave.consecutiveFailures = 0;
          const cb = this.getOrCreateCircuit(slaveId);
          circuitRecordSuccess(
            cb,
            this.config.circuitBreakerThreshold,
            this.config.circuitBreakerResetMs
          );
          await this.store.upsertCircuitState({
            id: `cb_${slaveId}`,
            slaveId,
            state: cb.state,
            failureCount: cb.failureCount,
            lastFailureAt: cb.lastFailureAt,
            lastSuccessAt: cb.lastSuccessAt,
            openedAt: cb.openedAt
          });
          await this.store.createHealthTick({
            slaveId,
            providerId: slave.providerId,
            status: "running",
            responseMs: Date.now() - slave.lastHealthCheck,
            error: null,
            ts: Date.now()
          });
          if (prevStatus !== "running") {
            this.eventBus?.emit("fleet:slave_status", { slaveId, status: "running" });
          }
          return true;
        } catch (err) {
          const prevStatus = slave.status;
          slave.consecutiveFailures++;
          slave.lastHealthCheck = Date.now();
          slave.status = "error";
          const cb = this.getOrCreateCircuit(slaveId);
          const newState = circuitRecordFailure(
            cb,
            this.config.circuitBreakerThreshold,
            this.config.circuitBreakerResetMs
          );
          slave.circuitState = newState;
          await this.store.upsertCircuitState({
            id: `cb_${slaveId}`,
            slaveId,
            state: cb.state,
            failureCount: cb.failureCount,
            lastFailureAt: cb.lastFailureAt,
            lastSuccessAt: cb.lastSuccessAt,
            openedAt: cb.openedAt
          });
          await this.store.createHealthTick({
            slaveId,
            providerId: slave.providerId,
            status: "error",
            responseMs: null,
            error: err instanceof Error ? err.message : String(err),
            ts: Date.now()
          });
          if (prevStatus !== "error") {
            this.eventBus?.emit("fleet:slave_status", { slaveId, status: "error" });
          }
          if (slave.consecutiveFailures >= this.config.circuitBreakerThreshold) {
            this.eventBus?.emit("fleet:crash_detected", {
              slaveId,
              failures: slave.consecutiveFailures
            });
          }
          if (newState !== cb.state || newState === "open") {
            this.eventBus?.emit("fleet:circuit_changed", { slaveId, state: newState });
          }
          return false;
        }
      }
      async recalculateCircuit(slaveId) {
        const cb = this.getOrCreateCircuit(slaveId);
        const resetMs = this.config.circuitBreakerResetMs;
        if (cb.state === "open" && cb.openedAt && Date.now() - cb.openedAt >= resetMs) {
          cb.state = "half_open";
          const slave = this.slaves.get(slaveId);
          if (slave) slave.circuitState = "half_open";
          this.eventBus?.emit("fleet:circuit_changed", { slaveId, state: "half_open" });
          await this.store.upsertCircuitState({
            id: `cb_${slaveId}`,
            slaveId,
            state: cb.state,
            failureCount: cb.failureCount,
            lastFailureAt: cb.lastFailureAt,
            lastSuccessAt: cb.lastSuccessAt,
            openedAt: cb.openedAt
          });
        }
      }
      async probeAll() {
        for (const slaveId of this.slaves.keys()) {
          await this.probe(slaveId);
        }
      }
      getOrCreateCircuit(slaveId) {
        let cb = this.circuitBreakers.get(slaveId);
        if (!cb) {
          cb = createCircuitBreaker();
          this.circuitBreakers.set(slaveId, cb);
        }
        return cb;
      }
      get isRunning() {
        return this.timerHandle !== null;
      }
    };
    ChromeGovernor = class {
      constructor(store, config2, eventBus, transport, fleetSupervisor) {
        this.store = store;
        this.config = config2;
        this.eventBus = eventBus;
        this.cdpTransport = transport ?? null;
        this.fleetSupervisor = fleetSupervisor ?? new FleetSupervisor(store, {
          portRange: this.config.portRange,
          healthProbeIntervalMs: this.config.healthProbeIntervalMs ?? 3e4,
          healthProbeTimeoutMs: this.config.healthProbeTimeoutMs ?? 5e3,
          autoRestart: this.config.autoRestart ?? true,
          maxRestarts: this.config.maxRestarts ?? 3,
          circuitBreakerThreshold: this.config.circuitBreakerThreshold ?? 5,
          circuitBreakerResetMs: this.config.circuitBreakerResetMs ?? 6e4,
          chromeProfileBase: this.config.profileBaseDir ?? "chrome-profiles"
        });
      }
      store;
      config;
      eventBus;
      fleetSupervisor;
      cdpTransport = null;
      _cdpProxy = null;
      mutexes = /* @__PURE__ */ new Map();
      traceLog = null;
      healthMonitor = null;
      circuitBreakers = /* @__PURE__ */ new Map();
      /** Memoized provider-free generic browser slave (automation backbone). */
      _genericSlaveId = null;
      /** Extended browser-automation recipe actions (set by bootstrap). */
      browserHarness;
      /** 019 — in-memory snapshot of DB-backed capabilities, loaded at boot. */
      capabilitySnapshot;
      /** Wire the boot-loaded capability snapshot (source of truth for execution). */
      setCapabilitySnapshot(snapshot) {
        this.capabilitySnapshot = snapshot;
      }
      // ── Boot ───────────────────────────────────────────────────────────────
      /** Execute a harness plan on a slave (forwards to the CDPProxy, Governor Canon intact). */
      async runHarnessPlan(slaveId, dag) {
        if (!this._cdpProxy) throw new EngineError("CDP proxy not initialised");
        return this._cdpProxy.executeHarnessPlan(slaveId, dag);
      }
      async boot() {
        await this.seedAccounts();
      }
      // ── Lifecycle (3.2 LifecycleManager) ───────────────────────────────────
      async spawn(providerId, accountId, opts) {
        const instance2 = await this.fleetSupervisor.spawn(providerId, accountId, {
          visible: opts?.visible ?? false,
          debugPort: opts?.debugPort,
          extraArgs: opts?.extraArgs ?? []
        });
        return this.toChromeSlave(instance2);
      }
      async launch(providerId, opts) {
        return this.spawn(providerId, "default", opts);
      }
      async kill(slaveId) {
        await this.fleetSupervisor.kill(slaveId);
      }
      async killAll() {
        await this.fleetSupervisor.killAll();
        this._genericSlaveId = null;
      }
      async ensureRunning(slaveId) {
        await this.fleetSupervisor.ensureRunning(slaveId);
        const result = this.fleetSupervisor.getInstance(slaveId);
        if (!result) throw new EngineError(`Slave not found: ${slaveId}`);
        return this.toChromeSlave(result);
      }
      /**
       * Find or spawn a Chrome slave for a specific provider+account.
       * Used by ConversationManager to derive slave from conversation's provider/account.
       */
      async ensureRunningForAccount(providerId, accountId, opts) {
        const existing = this.getAllSlaves({ providerId }).find((s) => s.accountId === accountId);
        if (existing) {
          return this.ensureRunning(existing.slaveId);
        }
        return this.spawn(providerId, accountId, opts);
      }
      deriveProfile(providerId, accountId) {
        const base = this.config.profileBaseDir ?? (process.platform === "win32" ? "C:\\.config\\vivim" : "/.config/vivim");
        return join4(base, providerId, accountId);
      }
      /**
       * Re-login path (FR-9/FR-10): kill the running slave and relaunch it visible
       * for a one-time manual authentication. Self-service — no full restart.
       */
      async recoverAuth(providerId, accountId) {
        const instance2 = await this.fleetSupervisor.recoverAuth(providerId, accountId);
        return this.toChromeSlave(instance2);
      }
      /**
       * Aggregate fleet super-state (FR-3): idle | active | degraded | terminal.
       */
      getSuperState() {
        return this.fleetSupervisor.getSuperState();
      }
      allocatePort() {
        return this.config.portRange[0];
      }
      async seedAccounts() {
        this.eventBus?.emit("governor:accounts-seeded", {});
      }
      async reapOrphanedPorts() {
        this.eventBus?.emit("governor:orphans-reaped", {});
      }
      /** Convert a FleetSupervisor instance into the public ChromeSlave shape. */
      toChromeSlave(inst) {
        return {
          slaveId: inst.id,
          providerId: inst.providerSlug,
          accountId: inst.accountId,
          debugPort: inst.debugPort,
          profileDir: inst.profileDir,
          status: inst.status,
          superState: "idle",
          pid: inst.pid,
          consecutiveFailures: inst.consecutiveFailures,
          circuitState: "closed",
          lastHealthCheck: inst.lastHealthCheck,
          channel: inst.channel,
          mode: inst.mode,
          firstRun: inst.firstRun
        };
      }
      // Internal slaves map for compatibility
      get slaves() {
        const instances = this.fleetSupervisor.getAllInstances();
        const map = /* @__PURE__ */ new Map();
        for (const inst of instances) {
          map.set(inst.id, this.toChromeSlave(inst));
        }
        return map;
      }
      getAllSlaves(opts) {
        const all = [...this.slaves.values()];
        if (opts?.providerId) return all.filter((s) => s.providerId === opts.providerId);
        return all;
      }
      getSlave(slaveId) {
        return this.slaves.get(slaveId) ?? null;
      }
      // ── CDP Transport Injection ─────────────────────────────────────────────
      setCdpTransport(transport) {
        this.cdpTransport = transport;
        this._cdpProxy = null;
      }
      /** Returns the raw CDP transport (for advanced consumers like SelectorHealer). */
      getTransport() {
        return this.cdpTransport;
      }
      // ── CDP (3.3 CDPProxy) ──────────────────────────────────────────────────
      get cdp() {
        if (!this.cdpTransport) {
          throw new EngineError("CDP transport not configured. Call setCdpTransport() first.");
        }
        this._cdpProxy = new CDPProxy(
          this.slaves,
          this.mutexes,
          this.cdpTransport,
          this.eventBus,
          this.browserHarness
        );
        return this._cdpProxy;
      }
      // ── Mediated CDP surface (DISC-3) ────────────────────────────────────────
      //
      // Governor Canon: ONLY the governor may issue raw CDP domain-enable / evaluate.
      // Every engine must call these helpers instead of sending CDP directly, so
      // Runtime is enabled exactly once (no double-enable) and all evaluate traffic
      // funnels through a single audited path.
      /**
       * Enable a set of CDP domains through the governor — the single I/O authority.
       * Centralises `Runtime.enable` so callers never double-enable the Runtime domain.
       */
      async enableDomains(slaveId, domains) {
        for (const domain of domains) {
          await this.cdp.send(slaveId, `${domain}.enable`).catch(() => {
          });
        }
      }
      /**
       * Evaluate a JS expression in the page through the governor-mediated transport.
       * This is the ONLY sanctioned path for `Runtime.evaluate` — engines call
       * `governor.evaluate(...)`, never send CDP directly.
       */
      async evaluate(slaveId, expression, opts) {
        const result = await this.cdp.send(slaveId, "Runtime.evaluate", {
          expression,
          returnByValue: opts?.returnByValue ?? true,
          awaitPromise: opts?.awaitPromise ?? false
        });
        if (result?.exceptionDetails) {
          throw new EngineError(`Runtime.evaluate threw: ${JSON.stringify(result.exceptionDetails)}`);
        }
        return result?.result?.value;
      }
      // ── Capability execution (Stage 3: Slave executes) ───────────────────────
      //
      // Resolves a registered `cap:cdp:*` capability to a live slave and fires the
      // real CDP command through the mediated transport (Governor Canon intact).
      // Drives the full chain: conversation/provider → slave → CDP send → trace.
      /**
       * Resolve the target slave for a capability execution.
       * Accepts either a conversationId (resolved via the conversation's provider to
       * a running slave) or a direct providerId. Falls back to the provider-free
       * generic browser when no provider-bound slave exists.
       */
      async resolveSlaveForExecution(ref, resolver) {
        let providerId = null;
        const providerSlaves = this.getAllSlaves({ providerId: ref });
        if (providerSlaves.length > 0) {
          providerId = ref;
        } else if (resolver.getConversationProviderId) {
          providerId = await resolver.getConversationProviderId(ref);
        }
        if (providerId) {
          const slaves = this.getAllSlaves({ providerId });
          if (slaves.length > 0) return slaves[0];
          return this.spawn(providerId, "default");
        }
        return this.ensureGenericBrowser();
      }
      /**
       * Core CDP send used by capability execution. Resolves the slave from a
       * conversationId/providerId reference, fires the real CDP command, and records
       * a trace entry. `params` are forwarded verbatim as the CDP command parameters.
       */
      async executeCdpMethod(ref, cdpMethod, params, resolver) {
        if (!this.cdpTransport) {
          throw new EngineError("CDP transport not configured. Call setCdpTransport() first.");
        }
        const slave = await this.resolveSlaveForExecution(ref, resolver ?? {});
        const start = Date.now();
        try {
          const result = await this.cdp.send(slave.slaveId, cdpMethod, params);
          await this.recordTrace({
            method: "executeCapability",
            conversationId: ref,
            slaveId: slave.slaveId,
            paramsJson: JSON.stringify({ cdpMethod, params }),
            resultJson: JSON.stringify(result),
            durationMs: Date.now() - start,
            error: null
          }).catch(() => {
          });
          return result;
        } catch (err) {
          const message = err instanceof Error ? err.message : "execution failed";
          await this.recordTrace({
            method: "executeCapability",
            conversationId: ref,
            slaveId: slave.slaveId,
            paramsJson: JSON.stringify({ cdpMethod, params }),
            resultJson: null,
            durationMs: Date.now() - start,
            error: message
          }).catch(() => {
          });
          throw err;
        }
      }
      /**
       * Execute a registered capability by slug against a live slave (Stage 3).
       * `ref` is a conversationId or providerId. The capability must be a `cap:cdp:*`
       * (discovered) capability — its CDP method is read from the capability id
       * (`cap:cdp:Runtime.evaluate`) and its input schema parameters are forwarded as
       * the CDP command parameters.
       */
      async executeCapability(ref, slug, opts) {
        const cap = opts?.capabilityLookup?.(slug);
        if (cap?.id.startsWith("cap:cdp:")) {
          const cdpMethod = cap.id.slice("cap:cdp:".length);
          return this.executeCdpMethod(ref, cdpMethod, opts?.params ?? {}, opts?.resolver);
        }
        if (this.capabilitySnapshot) {
          const providerId = opts?.resolver ? await opts.resolver.getConversationProviderId?.(ref) : void 0;
          let entry = null;
          if (cap) {
            entry = this.capabilitySnapshot.getById(cap.id, providerId ?? void 0) ?? this.capabilitySnapshot.getById(cap.id);
          }
          entry = entry ?? this.capabilitySnapshot.getBySlug(slug, providerId ?? void 0);
          if (entry) {
            if (!entry.executable || !entry.configJson) {
              throw new EngineError(`Capability '${slug}' has no executable program in snapshot`);
            }
            return this.executeSnapshotProgram(ref, entry, opts?.params ?? {}, opts?.resolver);
          }
        }
        throw new EngineError(`Capability not found for slug: ${slug}`);
      }
      /**
       * 019 — execute a snapshot-resolved capability's best program via the browser
       * harness. The program's configJson is a Recipe (action + params); only
       * browser-automation actions are supported through the governor transport.
       */
      async executeSnapshotProgram(ref, entry, params, resolver) {
        if (!this.browserHarness) {
          throw new EngineError("Browser harness not configured; cannot execute snapshot capability");
        }
        const recipe2 = configToProgram(entry.configJson).recipe;
        const slave = await this.resolveSlaveForExecution(ref, resolver ?? {});
        const slaveId = slave.slaveId;
        const results = [];
        for (const step of recipe2.steps) {
          const { kind, outputKey: _outputKey, ...stepParams } = step;
          try {
            const result = await this.browserHarness.runAction(slaveId, String(kind), {
              ...stepParams,
              ...params
            });
            results.push(result);
          } catch (err) {
            const wrapped = err instanceof Error ? err : new EngineError(String(err));
            throw new EngineError(
              `Snapshot program step '${String(kind)}' failed for capability ${entry.globalId}: ${wrapped.message}`,
              { cause: wrapped }
            );
          }
        }
        return { ok: true, capabilityId: entry.globalId, steps: recipe2.steps.length, results };
      }
      // ── Provider-free generic browser (automation backbone) ───────────────────
      //
      // A neutral Chrome slave not bound to any chat provider. The system can drive
      // the open web through it for any automation task (research, monitoring,
      // scraping, testing). Governor Canon: all CDP still funnels through `this.cdp`.
      /**
       * Find-or-spawn the shared generic browser slave. Memoized per governor
       * lifetime; `killAll()` clears it so the next call relaunches.
       */
      async ensureGenericBrowser(opts) {
        if (this._genericSlaveId) {
          const existing = this.getSlave(this._genericSlaveId);
          if (existing) return existing;
        }
        const slave = await this.spawn("generic", "default", {
          ...opts,
          extraArgs: [...opts?.extraArgs ?? [], "--no-first-run", "--disable-default-args"]
        });
        this._genericSlaveId = slave.slaveId;
        return slave;
      }
      /** Reset the memoized generic slave (e.g. after killAll). */
      clearGenericBrowser() {
        this._genericSlaveId = null;
      }
      /** Wire the extended browser-automation harness actions (called at boot). */
      setBrowserHarness(harness) {
        this.browserHarness = harness;
      }
      /**
       * Capture a screenshot of a slave (base64 PNG) through the governor transport.
       * Convenience used by capability handlers + observe tap.
       */
      async captureScreenshot(slaveId, region) {
        const params = { format: "png" };
        if (region) {
          params.captureBeyondViewport = true;
          params.clip = { x: region.x, y: region.y, width: region.w, height: region.h, scale: 1 };
        }
        const res = await this.cdp.send(slaveId, "Page.captureScreenshot", params);
        if (!res?.data) throw new EngineError("ChromeGovernor: screenshot failed");
        return res.data;
      }
      /** Get the full accessibility tree for a slave (role/name). */
      async getAccessibilityTree(slaveId) {
        await this.enableDomains(slaveId, ["Accessibility", "Runtime"]);
        const res = await this.cdp.send(slaveId, "Accessibility.getFullAXTree", {});
        if (!res?.nodes) throw new EngineError("ChromeGovernor: empty AX tree");
        return { role: "root", children: Object.values(res.nodes) };
      }
      // ── Trace (3.4 TraceLog) ────────────────────────────────────────────────
      setTraceLog(store) {
        this.traceLog = new TraceLog(store);
      }
      async recordTrace(entry) {
        if (!this.traceLog) throw new EngineError("TraceLog not configured. Call setTraceLog() first.");
        return this.traceLog.record(entry);
      }
      async getTrace(slaveId, limit) {
        if (!this.traceLog) throw new EngineError("TraceLog not configured.");
        return this.traceLog.getTrace(slaveId, limit);
      }
      async getConversationTrace(conversationId) {
        if (!this.traceLog) throw new EngineError("TraceLog not configured.");
        return this.traceLog.getConversationTrace(conversationId);
      }
      // ── Health (3.4 HealthMonitor) ──────────────────────────────────────────
      setHealthMonitor(store) {
        this.healthMonitor = new HealthMonitor(
          store,
          this.slaves,
          this.circuitBreakers,
          this.cdp,
          this.config,
          this.eventBus
        );
      }
      startHealthProbe(intervalMs) {
        this.healthMonitor?.start(intervalMs);
      }
      stopHealthProbe() {
        this.healthMonitor?.stop();
      }
      async probeHealth(slaveId) {
        if (!this.healthMonitor)
          throw new EngineError("HealthMonitor not configured. Call setHealthMonitor() first.");
        return this.healthMonitor.probe(slaveId);
      }
      async getHealth(slaveId) {
        const slave = this.slaves.get(slaveId);
        if (!slave) throw new EngineError(`Slave not found: ${slaveId}`);
        return {
          slaveId,
          status: slave.status,
          circuitState: slave.circuitState,
          consecutiveFailures: slave.consecutiveFailures,
          lastHealthCheck: slave.lastHealthCheck,
          uptimeMs: Date.now() - slave.lastHealthCheck
        };
      }
      /** Public probe used by the harness health adapter (reuses the private health probe). */
      async probe(slaveId) {
        return this.probeHealth(slaveId);
      }
      async getAllHealth() {
        const result = /* @__PURE__ */ new Map();
        for (const slave of this.slaves.values()) {
          result.set(slave.slaveId, {
            slaveId: slave.slaveId,
            status: slave.status,
            circuitState: slave.circuitState,
            consecutiveFailures: slave.consecutiveFailures,
            lastHealthCheck: slave.lastHealthCheck,
            uptimeMs: Date.now() - slave.lastHealthCheck
          });
        }
        return result;
      }
    };
  }
});

// src/engines/provider-discovery.ts
var init_provider_discovery = __esm({
  "src/engines/provider-discovery.ts"() {
    "use strict";
    init_errors();
  }
});

// seeds/providers/manifests.ts
var PROVIDER_MANIFESTS;
var init_manifests = __esm({
  "seeds/providers/manifests.ts"() {
    "use strict";
    PROVIDER_MANIFESTS = [
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "chatgpt",
          display_name: "ChatGPT",
          description: "OpenAI's ChatGPT assistant",
          category: "ai",
          provider_type: "llm",
          website_url: "https://chatgpt.com",
          documentation_url: "https://platform.openai.com/docs",
          auth_type: "browser",
          has_multi_account: true,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9252, 9280],
            extra_args: ["--no-first-run"]
          },
          capabilities: [
            "select_model",
            "send_message",
            "edit_message",
            "regenerate_response",
            "upload_file",
            "create_new_chat",
            "navigate_chat",
            "delete_chat",
            "rename_chat",
            "browse_with_bing"
          ]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://chatgpt.com",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://chatgpt.com",
            endpoint_type: "chat",
            selector: {
              composer: "#prompt-textarea",
              send_button: "[data-testid='send-button']"
            },
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          },
          {
            label: "Login",
            url: "https://chatgpt.com/auth/login",
            endpoint_type: "login",
            selector: {
              email_input: "input[name='email']",
              continue_button: "button[type='submit']"
            }
          }
        ],
        models: [
          {
            slug: "gpt-4o",
            display_name: "GPT-4o",
            is_default: true,
            context_window: 128e3,
            max_output_tokens: 16384,
            supports_streaming: true,
            supports_vision: true,
            supports_tools: true
          },
          {
            slug: "gpt-4o-mini",
            display_name: "GPT-4o Mini",
            context_window: 128e3,
            max_output_tokens: 16384,
            supports_streaming: true,
            supports_vision: true,
            supports_tools: true
          },
          {
            slug: "o3",
            display_name: "o3",
            context_window: 2e5,
            max_output_tokens: 1e5,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "o4-mini",
            display_name: "o4-mini",
            context_window: 2e5,
            max_output_tokens: 1e5,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to ChatGPT",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "select_model",
            ui_component_override: "dropdown_selector",
            ui_label_override: "Select GPT Model",
            ui_icon_override: "cpu",
            ui_position_override: "header",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://chatgpt.com"
          },
          {
            key: "auth_type",
            value: "email"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "claude",
          display_name: "Claude",
          description: "Anthropic's Claude AI assistant",
          category: "ai",
          provider_type: "llm",
          website_url: "https://claude.ai",
          documentation_url: "https://docs.anthropic.com",
          auth_type: "browser",
          has_multi_account: true,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9222, 9250],
            extra_args: ["--disable-features=Translate", "--no-first-run"]
          },
          capabilities: [
            "select_model",
            "send_message",
            "edit_message",
            "regenerate_response",
            "toggle_extended_thinking",
            "upload_file",
            "create_new_chat",
            "navigate_chat",
            "delete_chat",
            "rename_chat",
            "deep_research"
          ]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://claude.ai",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://claude.ai/chat",
            endpoint_type: "chat",
            selector: {
              composer: "[contenteditable]",
              send_button: "[aria-label='Send Message']"
            },
            composer_type: "prosemirror",
            send_method: "both",
            content_editable: true
          },
          {
            label: "Login",
            url: "https://claude.ai/login",
            endpoint_type: "login",
            selector: {
              email_input: "input[type='email']",
              continue_button: "button[type='submit']"
            }
          }
        ],
        models: [
          {
            slug: "claude-sonnet-4-20250514",
            display_name: "Sonnet 4",
            is_default: true,
            context_window: 2e5,
            max_output_tokens: 64e3,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "claude-opus-4-20250514",
            display_name: "Opus 4",
            context_window: 2e5,
            max_output_tokens: 64e3,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "claude-haiku-4-20250514",
            display_name: "Haiku 4",
            context_window: 2e5,
            max_output_tokens: 64e3,
            supports_streaming: true,
            supports_vision: true,
            supports_tools: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "retry_with_fallback",
                config: {
                  fallback_selector: "textarea"
                }
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to Claude",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "select_model",
            ui_component_override: "dropdown_selector",
            ui_label_override: "Select Claude Model",
            ui_icon_override: "cpu",
            ui_position_override: "header",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "toggle_extended_thinking",
            ui_component_override: "toggle_switch",
            ui_label_override: "Extended Thinking",
            ui_position_override: "header",
            ui_priority_override: "secondary",
            existential_rule_override: "message_has_thinking_block"
          },
          {
            global_capability_id: "deep_research",
            ui_component_override: "action_button",
            ui_label_override: "Deep Research",
            ui_icon_override: "flask",
            ui_position_override: "composer",
            ui_priority_override: "secondary",
            min_plan_tier_override: "pro"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://claude.ai"
          },
          {
            key: "auth_type",
            value: "email"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "deepseek",
          display_name: "DeepSeek",
          description: "DeepSeek AI assistant with strong coding and reasoning capabilities",
          category: "ai",
          provider_type: "llm",
          website_url: "https://chat.deepseek.com",
          documentation_url: "https://platform.deepseek.com/api-docs",
          auth_type: "browser",
          has_multi_account: false,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9312, 9340]
          },
          capabilities: [
            "send_message",
            "edit_message",
            "regenerate_response",
            "create_new_chat",
            "navigate_chat",
            "delete_chat",
            "rename_chat"
          ]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://chat.deepseek.com",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://chat.deepseek.com",
            endpoint_type: "chat",
            selector: {
              composer: "textarea",
              send_button: "button[aria-label='Send']"
            },
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          },
          {
            label: "Login",
            url: "https://chat.deepseek.com/sign_in",
            endpoint_type: "login",
            selector: {
              email_input: "input[type='email']",
              continue_button: "button[type='submit']"
            }
          }
        ],
        models: [
          {
            slug: "deepseek-chat",
            display_name: "DeepSeek Chat (V3)",
            is_default: true,
            context_window: 65536,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "deepseek-reasoner",
            display_name: "DeepSeek Reasoner (R1)",
            context_window: 65536,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_thinking: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to DeepSeek",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://chat.deepseek.com"
          },
          {
            key: "auth_type",
            value: "email"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "facebook",
          display_name: "Facebook Messenger",
          description: "Facebook Messenger channel",
          category: "messaging",
          provider_type: "messaging",
          website_url: "https://www.facebook.com/messages",
          auth_type: "browser",
          has_multi_account: false,
          profile_strategy: "single",
          fleet_config: {
            port_range: [9360, 9380],
            extra_args: ["--no-first-run"]
          },
          capabilities: ["channel_add", "channel_connect", "message_send", "message_receive"]
        },
        endpoints: [
          {
            label: "Web",
            url: "https://www.facebook.com/messages",
            endpoint_type: "landing",
            is_default: true
          }
        ],
        models: [],
        capabilities_config: [],
        config: [
          {
            key: "poll_strategy",
            value: "cdp-scrape"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "gemini",
          display_name: "Gemini",
          description: "Google's Gemini AI assistant",
          category: "ai",
          provider_type: "llm",
          website_url: "https://gemini.google.com",
          documentation_url: "https://ai.google.dev/docs",
          auth_type: "browser",
          has_multi_account: true,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9282, 9310],
            extra_args: ["--no-first-run"]
          },
          capabilities: [
            "select_model",
            "send_message",
            "edit_message",
            "regenerate_response",
            "upload_file",
            "create_new_chat",
            "navigate_chat",
            "delete_chat",
            "rename_chat"
          ]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://gemini.google.com",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://gemini.google.com/app",
            endpoint_type: "chat",
            selector: {
              composer: ".ql-editor",
              send_button: "button[aria-label='Send message']"
            },
            composer_type: "quill",
            send_method: "both",
            content_editable: true
          },
          {
            label: "Login",
            url: "https://accounts.google.com",
            endpoint_type: "login"
          }
        ],
        models: [
          {
            slug: "gemini-2.5-pro",
            display_name: "Gemini 2.5 Pro",
            is_default: true,
            context_window: 1048576,
            max_output_tokens: 65536,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "gemini-2.5-flash",
            display_name: "Gemini 2.5 Flash",
            context_window: 1048576,
            max_output_tokens: 65536,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "gemini-2.0-flash",
            display_name: "Gemini 2.0 Flash",
            context_window: 1048576,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_vision: true,
            supports_tools: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to Gemini",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "select_model",
            ui_component_override: "dropdown_selector",
            ui_label_override: "Select Gemini Model",
            ui_icon_override: "cpu",
            ui_position_override: "header",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://gemini.google.com"
          },
          {
            key: "auth_type",
            value: "google"
          }
        ]
      },
      {
        provider: {
          slug: "generic",
          display_name: "Generic",
          description: "Format-agnostic fallback provider for the parser chain",
          category: "system",
          provider_type: "system",
          website_url: "https://cap-store.local",
          auth_type: "none",
          has_multi_account: false,
          profile_strategy: "none",
          fleet_config: {},
          capabilities: []
        },
        endpoints: [],
        models: [],
        capabilities_config: [],
        config: []
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "qwen",
          display_name: "Qwen",
          description: "Alibaba's Qwen AI assistant",
          category: "ai",
          provider_type: "llm",
          website_url: "https://tongyi.aliyun.com",
          documentation_url: "https://help.aliyun.com/zh/model-studio/",
          auth_type: "browser",
          has_multi_account: false,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9372, 9400]
          },
          capabilities: ["send_message", "select_model", "create_new_chat", "navigate_chat"]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://tongyi.aliyun.com",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://tongyi.aliyun.com/qianwen",
            endpoint_type: "chat",
            selector: {
              composer: "textarea",
              send_button: "button[aria-label='Send']"
            },
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          }
        ],
        models: [
          {
            slug: "qwen-max",
            display_name: "Qwen Max",
            is_default: true,
            context_window: 32768,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "qwen-plus",
            display_name: "Qwen Plus",
            context_window: 131072,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "qwen-turbo",
            display_name: "Qwen Turbo",
            context_window: 131072,
            max_output_tokens: 8192,
            supports_streaming: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to Qwen",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "select_model",
            ui_component_override: "dropdown_selector",
            ui_label_override: "Select Qwen Model",
            ui_icon_override: "cpu",
            ui_position_override: "header",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://tongyi.aliyun.com"
          },
          {
            key: "auth_type",
            value: "email"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "slack",
          display_name: "Slack",
          description: "Slack messaging channel",
          category: "messaging",
          provider_type: "messaging",
          website_url: "https://api.slack.com",
          auth_type: "oauth",
          has_multi_account: false,
          profile_strategy: "single",
          fleet_config: {
            port_range: [9400, 9420],
            extra_args: []
          },
          capabilities: ["channel_add", "channel_connect", "message_send", "message_receive"]
        },
        endpoints: [
          {
            label: "API",
            url: "https://slack.com/api",
            endpoint_type: "landing",
            is_default: true
          }
        ],
        models: [],
        capabilities_config: [],
        config: [
          {
            key: "poll_strategy",
            value: "webhook"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "studio-ai",
          display_name: "Studio AI",
          description: "Google AI Studio \u2014 Gemini API playground and prototyping environment",
          category: "ai",
          provider_type: "llm",
          website_url: "https://aistudio.google.com",
          documentation_url: "https://ai.google.dev/docs",
          auth_type: "browser",
          has_multi_account: false,
          profile_strategy: "per_account",
          fleet_config: {
            port_range: [9342, 9370]
          },
          capabilities: ["send_message", "select_model", "create_new_chat", "navigate_chat"]
        },
        endpoints: [
          {
            label: "Landing",
            url: "https://aistudio.google.com",
            endpoint_type: "landing",
            is_default: true
          },
          {
            label: "Chat",
            url: "https://aistudio.google.com/prompts/new_chat",
            endpoint_type: "chat",
            selector: {
              composer: "rich-textarea",
              send_button: "button[aria-label='Send message']"
            },
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          }
        ],
        models: [
          {
            slug: "gemini-2.5-pro-preview",
            display_name: "Gemini 2.5 Pro Preview",
            is_default: true,
            context_window: 1048576,
            max_output_tokens: 65536,
            supports_streaming: true,
            supports_vision: true,
            supports_thinking: true,
            supports_tools: true
          },
          {
            slug: "gemini-2.5-flash-preview",
            display_name: "Gemini 2.5 Flash Preview",
            context_window: 1048576,
            max_output_tokens: 65536,
            supports_streaming: true,
            supports_vision: true,
            supports_tools: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              },
              {
                type: "navigate_home"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to Studio AI",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          },
          {
            global_capability_id: "select_model",
            ui_component_override: "dropdown_selector",
            ui_label_override: "Select Model",
            ui_icon_override: "cpu",
            ui_position_override: "header",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://aistudio.google.com"
          },
          {
            key: "auth_type",
            value: "google"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "system",
          display_name: "System",
          description: "System-level fallback parsers \u2014 universal fallback chain",
          category: "system",
          provider_type: "system",
          website_url: "https://vivim.app",
          auth_type: "none",
          has_multi_account: false,
          profile_strategy: "shared",
          capabilities: []
        },
        endpoints: [],
        models: [],
        capabilities_config: [],
        config: []
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "telegram",
          display_name: "Telegram",
          description: "Telegram messaging channel",
          category: "messaging",
          provider_type: "messaging",
          website_url: "https://core.telegram.org",
          auth_type: "api_key",
          has_multi_account: false,
          profile_strategy: "single",
          fleet_config: {
            port_range: [9380, 9400],
            extra_args: []
          },
          capabilities: ["channel_add", "channel_connect", "message_send", "message_receive"]
        },
        endpoints: [
          {
            label: "Bot API",
            url: "https://api.telegram.org",
            endpoint_type: "landing",
            is_default: true
          }
        ],
        models: [],
        capabilities_config: [],
        config: [
          {
            key: "poll_strategy",
            value: "polling"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "whatsapp",
          display_name: "WhatsApp",
          description: "WhatsApp Web messaging channel",
          category: "messaging",
          provider_type: "messaging",
          website_url: "https://web.whatsapp.com",
          auth_type: "browser",
          has_multi_account: false,
          profile_strategy: "single",
          fleet_config: {
            port_range: [9340, 9360],
            extra_args: ["--no-first-run"]
          },
          capabilities: ["channel_add", "channel_connect", "message_send", "message_receive"]
        },
        endpoints: [
          {
            label: "Web",
            url: "https://web.whatsapp.com",
            endpoint_type: "landing",
            is_default: true
          }
        ],
        models: [],
        capabilities_config: [],
        config: [
          {
            key: "poll_strategy",
            value: "cdp-scrape"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "z-ai",
          display_name: "Z AI",
          description: "Z.AI \u2014 Z.ai's multimodal AI assistant",
          category: "ai",
          provider_type: "llm",
          website_url: "https://z.ai",
          auth_type: "api",
          has_multi_account: false,
          profile_strategy: "shared",
          capabilities: ["send_message", "select_model"]
        },
        endpoints: [
          {
            label: "API",
            url: "https://api.z.ai/v1",
            endpoint_type: "api",
            is_default: true,
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          }
        ],
        models: [
          {
            slug: "z-ai-default",
            display_name: "Z AI Default",
            is_default: true,
            context_window: 128e3,
            max_output_tokens: 4096,
            supports_streaming: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "send_message",
            recovery_strategies: [
              {
                type: "retry_selector"
              }
            ],
            ui_component_override: "text_input",
            ui_label_override: "Send to Z AI",
            ui_icon_override: "arrow-up-circle",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "base_url",
            value: "https://api.z.ai/v1"
          },
          {
            key: "auth_type",
            value: "api_key"
          }
        ]
      },
      {
        $schema: "https://vivim.app/cap-store/v1/provider-manifest.schema.json",
        provider: {
          slug: "opencode",
          display_name: "OpenCode Local Agent",
          description: "Local agentic backbone powered by the opencode CLI. Runs one-shot agentic tasks with no API key using verified free models.",
          category: "ai",
          provider_type: "local-agent",
          website_url: "https://opencode.ai",
          auth_type: "none",
          has_multi_account: false,
          profile_strategy: "single",
          capabilities: ["agent_run"]
        },
        endpoints: [
          {
            label: "Local CLI",
            url: "opencode://local",
            endpoint_type: "api",
            is_default: true,
            composer_type: "textarea",
            send_method: "both",
            content_editable: false
          }
        ],
        models: [
          {
            slug: "qwen3.5-3b-free",
            display_name: "Qwen 3.5 3B (free)",
            is_default: true,
            context_window: 131072,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "glm4.5-air-free",
            display_name: "GLM 4.5 Air (free)",
            context_window: 128e3,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "deepseek-v3.2-free",
            display_name: "DeepSeek V3.2 (free)",
            context_window: 128e3,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          },
          {
            slug: "grok4-fast-free",
            display_name: "Grok 4 Fast (free)",
            context_window: 131072,
            max_output_tokens: 8192,
            supports_streaming: true,
            supports_tools: true
          }
        ],
        capabilities_config: [
          {
            global_capability_id: "agent_run",
            recovery_strategies: [{ type: "retry_selector" }],
            ui_component_override: "text_input",
            ui_label_override: "Run agent task",
            ui_icon_override: "terminal",
            ui_position_override: "composer",
            ui_priority_override: "primary"
          }
        ],
        config: [
          {
            key: "transport",
            value: "opencode-cli"
          },
          {
            key: "run_mode",
            value: "one-shot"
          }
        ]
      }
    ];
  }
});

// src/ids.ts
var ids_exports = {};
__export(ids_exports, {
  deriveBindingId: () => deriveBindingId,
  deriveCapabilityId: () => deriveCapabilityId,
  deriveProgramId: () => deriveProgramId,
  deriveSelectorId: () => deriveSelectorId,
  deriveSlaveId: () => deriveSlaveId,
  hashContent: () => hashContent,
  newId: () => newId,
  ulid: () => ulid
});
import { ulid } from "ulid";
function newId() {
  return ulid();
}
function deriveSlaveId(providerId, accountId) {
  return `slave:${providerId}:${accountId}`;
}
function deriveCapabilityId(providerId, slug) {
  return `cap:${providerId}:${slug}`;
}
function deriveBindingId(globalCapId, providerId) {
  return `bind:${globalCapId}:${providerId}`;
}
function deriveProgramId(bindingId, version) {
  return `prog:${bindingId}:v${version}`;
}
function deriveSelectorId(capabilityId, providerId, name) {
  return `sel:${capabilityId}:${providerId}:${name}`;
}
function hashContent(content) {
  try {
    const crypto2 = __require("crypto");
    return crypto2.createHash("sha256").update(content, "utf8").digest("hex");
  } catch {
    let h = 2166136261;
    for (let i = 0; i < content.length; i++) {
      h ^= content.charCodeAt(i);
      h = Math.imul(h, 16777619);
    }
    return `fnv1a:${(h >>> 0).toString(16)}`;
  }
}
var init_ids = __esm({
  "src/ids.ts"() {
    "use strict";
  }
});

// src/schema/provider-manifest.ts
import { z as z3 } from "zod";
var EndpointSchema, ParserSchema, ModelSchema, CapabilityConfigSchema, ConfigEntrySchema, StreamTransportSchema, SseFormatSchema, StreamConfigSchema, ProviderManifestSchema;
var init_provider_manifest = __esm({
  "src/schema/provider-manifest.ts"() {
    "use strict";
    EndpointSchema = z3.object({
      label: z3.string(),
      url: z3.string().url(),
      endpoint_type: z3.enum(["landing", "chat", "login", "api", "auth"]),
      is_default: z3.boolean().optional().default(false),
      selector: z3.record(z3.string()).optional(),
      composer_type: z3.enum(["textarea", "contenteditable", "prosemirror", "quill"]).optional().default("textarea"),
      send_method: z3.enum(["enter_key", "button_click", "both"]).optional().default("both"),
      content_editable: z3.boolean().optional().default(false)
    });
    ParserSchema = z3.object({
      name: z3.string(),
      file: z3.string().optional(),
      // Optional for inline parsers
      version: z3.number().int().positive(),
      is_active: z3.boolean().optional().default(true),
      fallback: z3.string().optional(),
      logic_type: z3.enum(["file", "inline", "composed"]).optional().default("inline"),
      logic_code: z3.string().optional()
      // Inline TypeScript/JavaScript for DB-driven loading
    });
    ModelSchema = z3.object({
      slug: z3.string(),
      display_name: z3.string(),
      is_default: z3.boolean().optional().default(false),
      context_window: z3.number().int().positive().optional(),
      max_output_tokens: z3.number().int().positive().optional(),
      supports_streaming: z3.boolean().optional().default(false),
      supports_vision: z3.boolean().optional().default(false),
      supports_thinking: z3.boolean().optional().default(false),
      supports_tools: z3.boolean().optional().default(false),
      pricing_input_per_1m: z3.number().nonnegative().optional(),
      pricing_output_per_1m: z3.number().nonnegative().optional()
    });
    CapabilityConfigSchema = z3.object({
      global_capability_id: z3.string(),
      recovery_strategies: z3.array(
        z3.object({
          type: z3.enum([
            "retry_selector",
            "retry_with_fallback",
            "navigate_home",
            "restart_chrome",
            "mark_broken"
          ]),
          config: z3.record(z3.unknown()).optional()
        })
      ).optional(),
      ui_component_override: z3.string().optional(),
      ui_label_override: z3.string().optional(),
      ui_icon_override: z3.string().optional(),
      ui_position_override: z3.string().optional(),
      ui_order_override: z3.number().int().optional(),
      ui_group_override: z3.string().optional(),
      ui_priority_override: z3.string().optional(),
      interaction_mode_override: z3.string().optional(),
      ui_states_override: z3.array(z3.string()).optional(),
      ui_visibility_rule_override: z3.string().optional(),
      existential_rule_override: z3.string().optional(),
      ui_input_schema_override: z3.record(z3.unknown()).optional(),
      mutation_effects_override: z3.record(z3.unknown()).optional(),
      recovery_behavior_override: z3.string().optional(),
      state_persistence_override: z3.string().optional(),
      data_flow_override: z3.string().optional(),
      min_plan_tier_override: z3.string().optional(),
      depends_on_override: z3.array(z3.string()).optional()
    });
    ConfigEntrySchema = z3.object({
      key: z3.string(),
      value: z3.string(),
      type: z3.string().optional().default("string"),
      is_secret: z3.boolean().optional().default(false)
    });
    StreamTransportSchema = z3.enum([
      "sse",
      "batchexecute",
      "websocket",
      "sse-patch",
      "json"
    ]);
    SseFormatSchema = z3.enum(["openai", "anthropic", "gemini", "generic"]);
    StreamConfigSchema = z3.object({
      streamTransport: StreamTransportSchema,
      streamTerminalJson: z3.string().default("[]"),
      sseFormat: SseFormatSchema.nullable().optional(),
      deltaPathJson: z3.string().refine(
        (v) => {
          try {
            const parsed = JSON.parse(v);
            return Array.isArray(parsed) && parsed.every((p) => typeof p === "string");
          } catch {
            return false;
          }
        },
        { message: "deltaPathJson must be a JSON array of string paths" }
      ).nullable().optional(),
      contentType: z3.string().nullable().optional(),
      completionDetectorsJson: z3.string().default("[]"),
      isActive: z3.number().int().min(0).max(1).default(1),
      version: z3.number().int().positive().default(1)
    });
    ProviderManifestSchema = z3.object({
      $schema: z3.string().optional(),
      provider: z3.object({
        slug: z3.string().min(1).max(64),
        display_name: z3.string().min(1),
        description: z3.string(),
        category: z3.string().default("ai"),
        provider_type: z3.string().default("llm"),
        website_url: z3.string().url(),
        documentation_url: z3.string().url().optional(),
        auth_type: z3.string().default("browser"),
        has_multi_account: z3.boolean().default(false),
        profile_strategy: z3.string().default("per_account"),
        fleet_config: z3.object({
          chrome_path: z3.string().optional(),
          port_range: z3.tuple([z3.number(), z3.number()]).optional(),
          extra_args: z3.array(z3.string()).optional()
        }).optional(),
        capabilities: z3.array(z3.string()).default([])
      }),
      endpoints: z3.array(EndpointSchema).default([]),
      parsers: z3.array(ParserSchema).default([]),
      models: z3.array(ModelSchema).default([]),
      capabilities_config: z3.array(CapabilityConfigSchema).default([]),
      config: z3.array(ConfigEntrySchema).default([])
    });
  }
});

// src/engines/stream-align.ts
import { createHash } from "crypto";
function getAtPath(root, path) {
  let cur = root;
  for (const token of path.replace(/\[(\d+)\]/g, ".$1").split(".").filter(Boolean)) {
    if (cur == null) return void 0;
    if (Array.isArray(cur)) {
      const idx = Number(token);
      cur = Number.isNaN(idx) ? void 0 : cur[idx];
    } else if (typeof cur === "object") {
      cur = cur[token];
    } else {
      return void 0;
    }
  }
  return cur;
}
var DELTA_PATH_CANDIDATES, StreamAlignmentEngine;
var init_stream_align = __esm({
  "src/engines/stream-align.ts"() {
    "use strict";
    init_errors();
    DELTA_PATH_CANDIDATES = [
      "choices[0].delta.content",
      "choices[0].message.content",
      "choices[0].text",
      "delta.content",
      "message.content",
      "content",
      "text",
      "data.content",
      "data.text",
      "outputs[0].text",
      "response"
    ];
    StreamAlignmentEngine = class {
      constructor(streamParser) {
        this.streamParser = streamParser;
      }
      streamParser;
      /**
       * Align a set of captured raw stream bodies against the active parser for
       * `providerId`. Produces a report the CLI can print and the discovery runner
       * can persist.
       */
      async alignCaptured(bodies, providerId, configuredFormat) {
        const samples = bodies.filter((b) => b && b.trim().length > 0);
        if (samples.length === 0) {
          return {
            providerId,
            sampleCount: 0,
            parserName: null,
            parserConfiguredFormat: configuredFormat ?? null,
            inferredFormat: "custom",
            confidence: 0,
            blockCount: 0,
            textBlocks: 0,
            detectedDeltaPath: null,
            streamFieldCandidates: [],
            mismatches: [
              "No stream body was captured \u2014 interaction may not have triggered a response."
            ],
            suggestions: [
              "Verify the composer selector and send action; increase the capture timeout."
            ],
            ok: false
          };
        }
        let parserName = null;
        let totalConfidence = 0;
        let blockCount = 0;
        let textBlocks = 0;
        for (const body of samples) {
          const result = await this.streamParser.parse(body, providerId);
          parserName ??= result.parserName;
          totalConfidence += result.confidence;
          blockCount += result.blocks.length;
          for (const b of result.blocks) {
            if (b.type === "text" || b.type === "code" || b.type === "reasoning") textBlocks++;
          }
        }
        const inferredFormat = samples[0] ? this.inferFormat(samples[0]) : "custom";
        const jsonSample = samples[0] ? this.extractJsonSample(samples[0]) : null;
        const { path: detectedDeltaPath, candidates: streamFieldCandidates } = this.detectDeltaPath(jsonSample);
        const mismatches = [];
        const suggestions = [];
        if (configuredFormat && configuredFormat !== inferredFormat) {
          mismatches.push(
            `Configured format '${configuredFormat}' does not match inferred wire format '${inferredFormat}'.`
          );
          suggestions.push(
            `Set ProviderStreamConfig.streamTransport to '${inferredFormat}' (or update the parser).`
          );
        }
        if (textBlocks === 0) {
          mismatches.push("Parser produced zero text/code blocks from the captured stream.");
          suggestions.push(
            detectedDeltaPath ? `Wire the parser to deltaPath '${detectedDeltaPath}'.` : "No delta path detected \u2014 verify the provider uses SSE/JSON delta streaming."
          );
        }
        if (inferredFormat !== "html" && !detectedDeltaPath) {
          mismatches.push("Could not locate a response delta path in the captured JSON.");
          suggestions.push(
            "Inspect the captured body and set an explicit deltaPath in ProviderStreamConfig."
          );
        }
        return {
          providerId,
          sampleCount: samples.length,
          parserName,
          parserConfiguredFormat: configuredFormat ?? null,
          inferredFormat,
          confidence: totalConfidence / samples.length,
          blockCount,
          textBlocks,
          detectedDeltaPath,
          streamFieldCandidates,
          mismatches,
          suggestions,
          ok: mismatches.length === 0
        };
      }
      /** Infer the wire format of a raw captured body. */
      inferFormat(body) {
        const trimmed = body.trim();
        if (/^(data:\s*)/m.test(trimmed) || /^data:\s*\[DONE\]/m.test(trimmed)) return "sse";
        if (/event:\s*\w+/m.test(trimmed) && /data:/m.test(trimmed)) return "sse";
        if (trimmed.startsWith("{") || trimmed.startsWith("[")) {
          try {
            JSON.parse(trimmed);
            return "json";
          } catch {
          }
        }
        if (/<[a-z][\s\S]*>/i.test(trimmed)) return "html";
        return "custom";
      }
      /**
       * Detect the response delta path from a JSON sample. Tries the well-known
       * candidates and returns the first that resolves to a non-empty string, plus
       * every candidate that resolves at all (for suggestions).
       */
      detectDeltaPath(jsonSample) {
        if (!jsonSample) return { path: null, candidates: [] };
        let root;
        try {
          root = JSON.parse(jsonSample);
        } catch {
          return { path: null, candidates: [] };
        }
        const candidates = [];
        for (const candidate of DELTA_PATH_CANDIDATES) {
          const value = getAtPath(root, candidate);
          if (typeof value === "string" && value.length > 0) {
            candidates.push(candidate);
          }
        }
        return { path: candidates[0] ?? null, candidates };
      }
      /**
       * Unit 2.16 — validate that a configured delta path resolves against a sample
       * of the provider's streamed JSON.
       */
      validateDeltaPath(deltaPath, sampleJson) {
        let root;
        try {
          root = JSON.parse(sampleJson);
        } catch (err) {
          return {
            valid: false,
            resolvedValue: void 0,
            error: `Sample is not valid JSON: ${err instanceof Error ? err.message : String(err)}`
          };
        }
        const value = getAtPath(root, deltaPath);
        if (value === void 0) {
          return {
            valid: false,
            resolvedValue: void 0,
            error: `Path '${deltaPath}' did not resolve.`
          };
        }
        if (typeof value === "string" && value.length === 0) {
          return {
            valid: false,
            resolvedValue: value,
            error: `Path '${deltaPath}' resolved to an empty string.`
          };
        }
        return { valid: true, resolvedValue: value };
      }
      /**
       * Unit 2.15 — deterministic content hash for a parser's source/logic. Used to
       * autocompute `provider_parser.parser_hash` so the StreamParserEngine cache and
       * the registrar stay in sync without manual hashes.
       */
      computeParserHash(source) {
        if (!source) throw new EngineError("computeParserHash: source must be non-empty");
        return createHash("sha256").update(source).digest("hex");
      }
      // ── Standalone helper (for import by registrar) ────────────────────────
      static computeParserHash(source) {
        if (!source) throw new EngineError("computeParserHash: source must be non-empty");
        return createHash("sha256").update(source).digest("hex");
      }
      // ── private ─────────────────────────────────────────────────────────────
      extractJsonSample(body) {
        const format = this.inferFormat(body);
        if (format === "json") return body.trim();
        if (format === "sse") {
          for (const line of body.split("\n")) {
            const m = line.match(/^data:\s*(.*)$/);
            const payload = m?.[1]?.trim();
            if (!payload || payload === "[DONE]") continue;
            try {
              JSON.parse(payload);
              return payload;
            } catch {
            }
          }
        }
        return null;
      }
    };
  }
});

// src/engines/provider-registrar.ts
var provider_registrar_exports = {};
__export(provider_registrar_exports, {
  ProviderRegistrar: () => ProviderRegistrar
});
import { resolve as resolve2 } from "path";
var ProviderRegistrar;
var init_provider_registrar = __esm({
  "src/engines/provider-registrar.ts"() {
    "use strict";
    init_manifests();
    init_errors();
    init_ids();
    init_provider_manifest();
    init_stream_align();
    ProviderRegistrar = class {
      constructor(store, auditor, eventBus, seedsDir) {
        this.store = store;
        this.auditor = auditor;
        this.eventBus = eventBus;
        this.seedsDir = seedsDir ?? resolve2(import.meta.dir, "../../seeds/providers");
      }
      store;
      auditor;
      eventBus;
      seedsDir;
      // ── Public API ──────────────────────────────────────────────────────────────
      async register(manifest) {
        const now = Date.now();
        const tablesAffected = [];
        let rowsAdded = 0;
        let rowsModified = 0;
        const existing = await this.store.getDefinitionBySlug(manifest.provider.slug);
        const status = existing ? "updated" : "created";
        const providerId = manifest.provider.slug;
        const defRow = {
          id: providerId,
          slug: manifest.provider.slug,
          display_name: manifest.provider.display_name,
          description: manifest.provider.description ?? null,
          category: manifest.provider.category,
          provider_type: manifest.provider.provider_type,
          is_active: 1,
          website_url: manifest.provider.website_url ?? null,
          documentation_url: manifest.provider.documentation_url ?? null,
          auth_type: manifest.provider.auth_type,
          has_multi_account: manifest.provider.has_multi_account ? 1 : 0,
          profile_strategy: manifest.provider.profile_strategy,
          fleet_config_json: JSON.stringify(manifest.provider.fleet_config ?? {}),
          capabilities_json: JSON.stringify(manifest.provider.capabilities),
          models_json: JSON.stringify([]),
          created_at: existing?.created_at ?? now,
          updated_at: now
        };
        await this.store.upsertDefinition(defRow);
        tablesAffected.push("provider_definition");
        if (existing) rowsModified++;
        else rowsAdded++;
        await this.store.deleteProviderEndpoints(providerId);
        for (const ep of manifest.endpoints) {
          const epRow = {
            id: newId(),
            provider_id: providerId,
            url: ep.url,
            label: ep.label,
            endpoint_type: ep.endpoint_type,
            is_default: ep.is_default ? 1 : 0,
            selectors_json: JSON.stringify(ep.selector ?? {}),
            composer_type: ep.composer_type ?? "textarea",
            send_method: ep.send_method ?? "both",
            content_editable: ep.content_editable ? 1 : 0,
            created_at: now,
            updated_at: now
          };
          await this.store.upsertEndpoint(epRow);
          rowsAdded++;
        }
        if (manifest.endpoints.length > 0) tablesAffected.push("provider_endpoint");
        await this.store.deleteProviderParsers(providerId);
        const parserNameToId = /* @__PURE__ */ new Map();
        for (const parser of manifest.parsers) {
          const logicType = parser.logic_type ?? "inline";
          if (logicType === "inline" && !parser.logic_code) {
            throw new EngineError(
              `Provider ${manifest.provider.slug} parser '${parser.name}': inline logic_type requires logic_code`
            );
          }
          const parserId = newId();
          const parserRow = {
            id: parserId,
            provider_id: providerId,
            parser_name: parser.name,
            parser_version: parser.version,
            parser_logic_type: logicType,
            parser_file_path: parser.file ?? null,
            parser_logic_code: parser.logic_code ?? null,
            // Unit 2.15 — autocompute a stable hash so the parser cache stays in sync.
            parser_hash: StreamAlignmentEngine.computeParserHash(
              parser.logic_code ?? parser.file ?? `${parser.name}:${parser.version}`
            ),
            is_active: parser.is_active ? 1 : 0,
            fallback_parser_id: null,
            created_at: now,
            updated_at: now
          };
          await this.store.upsertParser(parserRow);
          parserNameToId.set(parser.name, parserId);
          rowsAdded++;
        }
        for (const parser of manifest.parsers) {
          if (parser.fallback && parserNameToId.has(parser.name) && parserNameToId.has(parser.fallback)) {
            const fromId = parserNameToId.get(parser.name);
            const fallbackId = parserNameToId.get(parser.fallback);
            if (fromId && fallbackId) {
              await this.store.setParserFallback(fromId, fallbackId);
            }
          }
        }
        if (manifest.parsers.length > 0) tablesAffected.push("provider_parser");
        await this.store.deleteProviderCapabilities(providerId);
        for (const cap of manifest.capabilities_config) {
          const capRow = {
            id: newId(),
            provider_id: providerId,
            global_capability_id: cap.global_capability_id,
            recovery_strategies_json: JSON.stringify(cap.recovery_strategies ?? []),
            ui_component_override: cap.ui_component_override ?? null,
            ui_label_override: cap.ui_label_override ?? null,
            ui_icon_override: cap.ui_icon_override ?? null,
            ui_position_override: cap.ui_position_override ?? null,
            ui_order_override: cap.ui_order_override ?? null,
            ui_group_override: cap.ui_group_override ?? null,
            ui_priority_override: cap.ui_priority_override ?? null,
            interaction_mode_override: cap.interaction_mode_override ?? null,
            ui_states_override_json: cap.ui_states_override ? JSON.stringify(cap.ui_states_override) : null,
            ui_visibility_rule_override: cap.ui_visibility_rule_override ?? null,
            existential_rule_override: cap.existential_rule_override ?? null,
            ui_input_schema_override: cap.ui_input_schema_override ? JSON.stringify(cap.ui_input_schema_override) : null,
            mutation_effects_override_json: cap.mutation_effects_override ? JSON.stringify(cap.mutation_effects_override) : null,
            recovery_behavior_override: cap.recovery_behavior_override ?? null,
            state_persistence_override: cap.state_persistence_override ?? null,
            data_flow_override: cap.data_flow_override ?? null,
            min_plan_tier_override: cap.min_plan_tier_override ?? null,
            depends_on_override_json: cap.depends_on_override ? JSON.stringify(cap.depends_on_override) : null,
            confidence: 1,
            success_count: 0,
            fail_count: 0,
            consecutive_failures: 0,
            avg_latency_ms: 0,
            p95_latency_ms: 0,
            last_used_at: null,
            selector_hit_count: 0,
            selector_miss_count: 0,
            selector_last_miss_at: null,
            selector_last_error: null,
            created_at: now,
            updated_at: now
          };
          await this.store.upsertCapability(capRow);
          rowsAdded++;
        }
        if (manifest.capabilities_config.length > 0) tablesAffected.push("provider_capability");
        await this.store.deleteProviderConfigs(providerId);
        for (const cfg of manifest.config) {
          const cfgRow = {
            id: newId(),
            provider_id: providerId,
            config_key: cfg.key,
            config_value: cfg.value,
            config_type: cfg.type ?? "string",
            is_secret: cfg.is_secret ? 1 : 0,
            created_at: now,
            updated_at: now
          };
          await this.store.upsertConfig(cfgRow);
          rowsAdded++;
        }
        if (manifest.config.length > 0) tablesAffected.push("provider_config");
        await this.store.deleteProviderModels(providerId);
        for (const model of manifest.models) {
          const modelRow = {
            id: newId(),
            provider_id: providerId,
            model_slug: model.slug,
            display_name: model.display_name,
            is_active: 1,
            is_default: model.is_default ? 1 : 0,
            capabilities_json: "[]",
            context_window: model.context_window ?? null,
            max_output_tokens: model.max_output_tokens ?? null,
            supports_streaming: model.supports_streaming ? 1 : 0,
            supports_vision: model.supports_vision ? 1 : 0,
            supports_thinking: model.supports_thinking ? 1 : 0,
            supports_tools: model.supports_tools ? 1 : 0,
            pricing_input_per_1m: model.pricing_input_per_1m ?? null,
            pricing_output_per_1m: model.pricing_output_per_1m ?? null,
            created_at: now,
            updated_at: now
          };
          await this.store.upsertModel(modelRow);
          rowsAdded++;
        }
        if (manifest.models.length > 0) tablesAffected.push("provider_model");
        this.eventBus?.emit({
          type: "provider:seeded",
          providerId,
          capabilities: manifest.capabilities_config.length
        });
        if (this.auditor) {
          await this.auditor.registerAndAudit(manifest);
        }
        return {
          providerId,
          slug: manifest.provider.slug,
          status,
          tablesAffected,
          rowsAdded,
          rowsModified
        };
      }
      async seedAll() {
        const result = { seeded: [], skipped: [], errors: [] };
        for (const raw of PROVIDER_MANIFESTS) {
          try {
            const manifest = ProviderManifestSchema.parse(raw);
            const registerResult = await this.register(manifest);
            result.seeded.push(registerResult);
          } catch (err) {
            const msg = err instanceof Error ? err.message : String(err);
            result.errors.push({ file: "(in-repo manifest)", error: msg });
          }
        }
        return result;
      }
      async seedProvider(providerSlug) {
        for (const raw of PROVIDER_MANIFESTS) {
          const candidate = ProviderManifestSchema.safeParse(raw);
          if (candidate.success && candidate.data.provider.slug === providerSlug) {
            return this.register(candidate.data);
          }
        }
        throw new EngineError(`Provider manifest not found for slug: ${providerSlug}`);
      }
      async verifySeeds() {
        const result = { valid: true, providers: [] };
        for (const raw of PROVIDER_MANIFESTS) {
          const parseResult = ProviderManifestSchema.safeParse(raw);
          const slug = parseResult.success && parseResult.data.provider.slug ? parseResult.data.provider.slug : "(manifest)";
          if (!parseResult.success) {
            result.valid = false;
            result.providers.push({
              slug,
              status: "schema_mismatch",
              details: parseResult.error.issues.map((i) => `${i.path.join(".")}: ${i.message}`).join("; ")
            });
            continue;
          }
          result.providers.push({ slug, status: "ok", details: "Valid manifest" });
        }
        return result;
      }
      async reloadFromSeeds() {
        return this.seedAll();
      }
      // ── 1.3 Provider Taxonomy Layer ────────────────────────────────────────────
      async registerCapability(input) {
        return this.store.registerCapability(input);
      }
      async overrideCapability(input) {
        return this.store.overrideCapability(input);
      }
      async listCapabilities(providerId) {
        return this.store.listCapabilities(providerId);
      }
    };
  }
});

// src/engines/sandbox-runner.ts
var sandbox_runner_exports = {};
__export(sandbox_runner_exports, {
  SandboxRunner: () => SandboxRunner
});
import vm from "vm";
var SandboxRunner;
var init_sandbox_runner = __esm({
  "src/engines/sandbox-runner.ts"() {
    "use strict";
    init_errors();
    init_ids();
    SandboxRunner = class {
      constructor(auditStore, options = {}) {
        this.auditStore = auditStore;
        this.defaultBudget = options.defaultBudget ?? {
          cpuMs: 1e3,
          memoryBytes: 64 * 1024 * 1024
        };
        this.memoryProbe = options.memoryProbe ?? (() => process.memoryUsage());
      }
      auditStore;
      defaultBudget;
      memoryProbe;
      async run(code, input, permissions, options = {}) {
        const budget = { ...this.defaultBudget, ...options.budget };
        const handlerSlug = options.handlerSlug ?? input.handlerSlug ?? "inline";
        const auditId = newId();
        const sandbox = /* @__PURE__ */ Object.create(null);
        sandbox.input = input;
        sandbox.console = { log: () => {
        }, error: () => {
        }, warn: () => {
        } };
        if (permissions.canUseClipboard) {
          sandbox.navigator = { clipboard: this.makeClipboardGuard(handlerSlug) };
        }
        if (options.globals) {
          for (const [key, value] of Object.entries(options.globals)) {
            sandbox[key] = value;
          }
        }
        const context = vm.createContext(Object.freeze(sandbox));
        const isOverBudget = () => this.memoryProbe().heapUsed > budget.memoryBytes;
        let memoryBreached = isOverBudget();
        const memoryTimer = setInterval(() => {
          if (isOverBudget()) {
            memoryBreached = true;
          }
        }, 5);
        const finish = async (ok, error2, output) => {
          clearInterval(memoryTimer);
          const row = {
            id: auditId,
            handlerSlug,
            ok,
            error: error2,
            permissions,
            ts: Date.now()
          };
          await this.auditStore.create(row);
          return { ok, error: error2 ?? void 0, output, auditId };
        };
        try {
          const script = new vm.Script(`(async () => { ${code} })()`);
          const output = await script.runInContext(context, { timeout: budget.cpuMs });
          if (memoryBreached || isOverBudget()) {
            return await finish(
              false,
              new SandboxBudgetError(
                handlerSlug,
                "memory",
                this.memoryProbe().heapUsed,
                budget.memoryBytes
              ).message
            );
          }
          return await finish(true, null, output);
        } catch (err) {
          clearInterval(memoryTimer);
          const message = err instanceof Error ? err.message : String(err);
          const code2 = err.code;
          if (code2 === "ERR_SCRIPT_EXECUTION_TIMEOUT") {
            return await finish(false, new SandboxTimeoutError(handlerSlug, budget.cpuMs).message);
          }
          if (err instanceof SandboxTimeoutError || err instanceof SandboxBudgetError) {
            return await finish(false, message);
          }
          return await finish(false, message);
        }
      }
      /**
       * The clipboard gate is the only network/file-adjacent surface exposed when
       * canUseClipboard is true. It throws by default so a handler must be granted
       * explicit passthrough wiring by the host — the sandbox itself performs no
       * real clipboard I/O.
       */
      makeClipboardGuard(handlerSlug) {
        return {
          readText: async () => {
            throw new SandboxPermissionError(handlerSlug, "navigator.clipboard.readText");
          },
          writeText: async (_text) => {
            throw new SandboxPermissionError(handlerSlug, "navigator.clipboard.writeText");
          }
        };
      }
    };
  }
});

// src/schema/streaming.ts
import { z as z4 } from "zod";
function isLegacyBlock(obj) {
  const b = obj;
  return typeof b === "object" && b !== null && typeof b.kind === "string" && typeof b.index === "number";
}
function migrateLegacyBlock(legacy) {
  switch (legacy.kind) {
    case "text":
      return { type: "text", text: legacy.content };
    case "thinking":
      return { type: "reasoning", text: legacy.content };
    case "code":
      return { type: "code", text: legacy.content, language: legacy.language };
    case "image":
      return {
        type: "file",
        mediaType: "image/png",
        url: legacy.url ?? legacy.content,
        filename: legacy.alt
      };
    case "tool_use":
      return {
        type: "tool-call",
        toolCallId: `tc_${legacy.index}`,
        toolName: legacy.toolName ?? "unknown",
        input: legacy.input ?? {}
      };
    case "artifact":
      return {
        type: "custom",
        kind: "artifact",
        data: { content: legacy.content, artifactType: legacy.artifactType }
      };
    case "citation":
      return {
        type: "source",
        sourceId: `src_${legacy.index}`,
        url: legacy.url ?? legacy.source,
        title: legacy.content
      };
    case "error":
      return { type: "error", message: legacy.message ?? legacy.content, code: legacy.code };
    case "meta":
      return { type: "meta", key: legacy.key ?? "unknown", value: legacy.value };
    default:
      return { type: "text", text: legacy.content };
  }
}
function migrateLegacyParts(blocks) {
  return blocks.map(migrateLegacyBlock);
}
var ToolCallStateSchema, richTextSchema, TextPartSchema, ReasoningPartSchema, CodePartSchema, FilePartSchema, ToolCallPartSchema, ToolResultPartSchema, SourcePartSchema, CustomPartSchema, ErrorPartSchema, MetaPartSchema, StepStartPartSchema, ContentPartSchema;
var init_streaming = __esm({
  "src/schema/streaming.ts"() {
    "use strict";
    ToolCallStateSchema = z4.enum([
      "pending",
      "input-streaming",
      "input-available",
      "output-available",
      "output-error",
      "approval-requested",
      "approval-responded",
      "output-denied"
    ]);
    richTextSchema = z4.union([
      z4.string(),
      z4.object({ ast: z4.array(z4.any()) })
    ]);
    TextPartSchema = z4.object({
      type: z4.literal("text"),
      text: richTextSchema,
      state: z4.enum(["streaming", "done"]).optional(),
      lang: z4.string().optional()
    });
    ReasoningPartSchema = z4.object({
      type: z4.literal("reasoning"),
      text: richTextSchema,
      state: z4.enum(["streaming", "done"]).optional(),
      signature: z4.string().optional()
    });
    CodePartSchema = z4.object({
      type: z4.literal("code"),
      text: z4.string(),
      language: z4.string().optional()
    });
    FilePartSchema = z4.object({
      type: z4.literal("file"),
      mediaType: z4.string(),
      url: z4.string(),
      filename: z4.string().optional(),
      data: z4.string().optional()
    });
    ToolCallPartSchema = z4.object({
      type: z4.literal("tool-call"),
      toolCallId: z4.string(),
      toolName: z4.string(),
      input: z4.record(z4.unknown()),
      state: ToolCallStateSchema.optional(),
      approvalId: z4.string().optional()
    });
    ToolResultPartSchema = z4.object({
      type: z4.literal("tool-result"),
      toolCallId: z4.string(),
      output: z4.unknown().optional(),
      isError: z4.boolean().optional()
    });
    SourcePartSchema = z4.object({
      type: z4.literal("source"),
      sourceId: z4.string(),
      url: z4.string().optional(),
      title: z4.string().optional(),
      mediaType: z4.string().optional()
    });
    CustomPartSchema = z4.object({
      type: z4.literal("custom"),
      kind: z4.string(),
      data: z4.unknown(),
      state: z4.enum(["streaming", "done"]).optional()
    });
    ErrorPartSchema = z4.object({
      type: z4.literal("error"),
      message: z4.string(),
      code: z4.string().optional()
    });
    MetaPartSchema = z4.object({
      type: z4.literal("meta"),
      key: z4.string(),
      value: z4.unknown()
    });
    StepStartPartSchema = z4.object({ type: z4.literal("step-start") });
    ContentPartSchema = z4.discriminatedUnion("type", [
      TextPartSchema,
      ReasoningPartSchema,
      CodePartSchema,
      FilePartSchema,
      ToolCallPartSchema,
      ToolResultPartSchema,
      SourcePartSchema,
      CustomPartSchema,
      ErrorPartSchema,
      MetaPartSchema,
      StepStartPartSchema
    ]);
  }
});

// src/engines/safe-eval.ts
function assertTrustedExpressionSource(source, label) {
  if (FORBIDDEN_TOKENS.test(source)) {
    throw new EngineError(
      `Refused to evaluate untrusted ${label}: expression references a forbidden token. Only author-defined DSL expressions are permitted.`
    );
  }
}
var FORBIDDEN_TOKENS;
var init_safe_eval = __esm({
  "src/engines/safe-eval.ts"() {
    "use strict";
    init_errors();
    FORBIDDEN_TOKENS = /\b(?:constructor|__proto__|prototype|process|globalThis|global|window|document|self|require|import|eval|Function|fetch|XMLHttpRequest|setTimeout|setInterval|setImmediate|queueMicrotask|Proxy|Reflect|Worker|postMessage|atob|btoa)\b/;
  }
});

// src/engines/stream-parser.ts
var stream_parser_exports = {};
__export(stream_parser_exports, {
  StreamParserEngine: () => StreamParserEngine
});
function errorBlock(_providerId, message) {
  return [{ type: "error", message, code: "PARSE_FAILED" }];
}
function classifyBlocks(blocks) {
  const diag = {
    textBlocks: 0,
    toolCallBlocks: 0,
    fileBlocks: 0,
    errorBlocks: 0,
    reasoningBlocks: 0,
    codeBlocks: 0,
    sourceBlocks: 0
  };
  for (const b of blocks) {
    switch (b.type) {
      case "text":
        diag.textBlocks++;
        break;
      case "tool-call":
        diag.toolCallBlocks++;
        break;
      case "file":
        diag.fileBlocks++;
        break;
      case "error":
        diag.errorBlocks++;
        break;
      case "reasoning":
        diag.reasoningBlocks++;
        break;
      case "code":
        diag.codeBlocks++;
        break;
      case "source":
        diag.sourceBlocks++;
        break;
    }
  }
  return diag;
}
function detectWireFormat(raw) {
  const trimmed = raw.trimStart();
  if (trimmed.startsWith("data:") || trimmed.includes("\n\n")) {
    if (trimmed.includes("data:")) return "sse";
  }
  if (trimmed.startsWith("[")) {
    try {
      const arr = JSON.parse(trimmed);
      if (Array.isArray(arr)) return "json-array";
    } catch {
    }
  }
  if (trimmed.includes(")]}'") || trimmed.startsWith(")]}")) return "xssi";
  if (trimmed.includes("$rpc")) return "batchexecute";
  const lines = trimmed.split("\n").filter((l) => l.trim());
  if (lines.length > 0 && lines.every((l) => {
    try {
      JSON.parse(l);
      return true;
    } catch {
      return false;
    }
  }))
    return "ndjson";
  if (lines.some((l) => l.startsWith("data:") || l.startsWith("event:"))) return "sse";
  return "plain-text";
}
function normalizeBlocks(blocks) {
  if (blocks.length === 0) return blocks;
  if (isLegacyBlock(blocks[0])) {
    return migrateLegacyParts(blocks);
  }
  return blocks;
}
var StreamParserEngine;
var init_stream_parser = __esm({
  "src/engines/stream-parser.ts"() {
    "use strict";
    init_errors();
    init_streaming();
    init_safe_eval();
    StreamParserEngine = class _StreamParserEngine {
      constructor(store, config2, sandbox, logStore) {
        this.store = store;
        this.config = config2;
        this.sandbox = sandbox;
        this.logStore = logStore;
      }
      store;
      config;
      sandbox;
      logStore;
      parserCache = /* @__PURE__ */ new Map();
      inlineCache = /* @__PURE__ */ new Map();
      /**
       * Provider-prime cache: compiled modules from the generated protocol, keyed by
       * `${providerId}/${parserName}`. When populated by `primeFromProtocol()`, the
       * hot parse path reads ONLY from here and performs ZERO DB queries. The DB
       * fallback chain in `resolveFallbackChain()` remains as a runtime safety net.
       */
      primedParsers = /* @__PURE__ */ new Map();
      // Hardened execution environment for inline parser code (Unit 31.1). When
      // present, inline parser logic is compiled inside a frozen vm context with a
      // CPU/memory budget and an audit row — replacing the raw `new Function` path.
      static SANDBOX_PERMISSIONS = {
        canFetch: [],
        canReadFile: [],
        canWriteFile: [],
        canUseClipboard: false
      };
      async parse(rawBody, providerId) {
        const start = Date.now();
        const primed = this.resolvePrimed(providerId, rawBody);
        if (primed) {
          const module2 = primed;
          const blocks2 = normalizeBlocks(module2.parse(rawBody));
          const confidence2 = typeof module2.getConfidence === "function" ? module2.getConfidence(rawBody) : 0.5;
          const result2 = {
            blocks: blocks2,
            confidence: confidence2,
            parserName: module2.name,
            parserVersion: module2.version,
            durationMs: Date.now() - start,
            blockDiagnostics: classifyBlocks(blocks2),
            wireFormat: detectWireFormat(rawBody),
            fallbackDepth: 0,
            rawSizeBytes: rawBody.length
          };
          this.logParseResult(result2, providerId);
          return result2;
        }
        const chain = await this.resolveFallbackChain(providerId);
        let module = null;
        let blocks = [];
        for (const row of chain) {
          try {
            module = await this.loadModuleFromRow(row);
            blocks = normalizeBlocks(module.parse(rawBody));
            break;
          } catch {
            module = null;
          }
        }
        if (!module) {
          blocks = errorBlock(providerId, "all parsers failed \u2014 check provider_parser table");
          module = {
            name: "error",
            version: 0,
            providerId,
            parse: () => blocks,
            detectCompletion: () => true,
            getConfidence: () => 0
          };
        }
        const confidence = typeof module.getConfidence === "function" ? module.getConfidence(rawBody) : 0.5;
        const result = {
          blocks,
          confidence,
          parserName: module.name,
          parserVersion: module.version,
          durationMs: Date.now() - start,
          blockDiagnostics: classifyBlocks(blocks),
          wireFormat: detectWireFormat(rawBody),
          fallbackDepth: Math.max(0, chain.length - 1),
          rawSizeBytes: rawBody.length
        };
        this.logParseResult(result, providerId);
        return result;
      }
      async detectCompletion(rawBody, providerId) {
        const primed = this.resolvePrimed(providerId, rawBody);
        if (primed) return primed.detectCompletion(rawBody);
        const chain = await this.resolveFallbackChain(providerId);
        for (const row of chain) {
          try {
            const module = await this.loadModuleFromRow(row);
            return module.detectCompletion(rawBody);
          } catch {
          }
        }
        return true;
      }
      /**
       * Resolve a parser module from the protocol-primed cache. Splits an optional
       * `@version` suffix and walks the provider's primed parsers (no DB access).
       * Returns null when the provider was not primed, falling back to the DB chain.
       */
      resolvePrimed(providerId, rawBodyForSelection = null) {
        if (this.primedParsers.size === 0) return null;
        const [baseId, version] = providerId.split("@");
        const entries = [...this.primedParsers.entries()].filter(
          ([k]) => k === `${baseId}/` || k.startsWith(`${baseId}/`)
        );
        if (entries.length === 0) return null;
        if (version) {
          const exact = this.primedParsers.get(`${baseId}/${version}`);
          if (exact) return exact;
        }
        if (rawBodyForSelection != null) {
          let best2 = null;
          let bestC = -1;
          for (const [, mod] of entries) {
            const c = typeof mod.getConfidence === "function" ? mod.getConfidence(rawBodyForSelection) : 0;
            if (c > bestC) {
              bestC = c;
              best2 = mod;
            }
          }
          if (best2 && bestC > 0) return best2;
        }
        let best = null;
        let bestV = -1;
        for (const [, mod] of entries) {
          if (mod.version > bestV) {
            bestV = mod.version;
            best = mod;
          }
        }
        return best;
      }
      /**
       * Resolve the chain of parser rows to try, walking the `fallbackParserId`
       * edge from the by-need resolved provider parser. `providerId` may carry an
       * `@version` suffix (e.g. `claude@2`) for semver-aware selection; `@latest`
       * or no suffix picks the highest active version.
       *
       * Guards against cycles via a visited-set. Terminal parsers (no fallback
       * edge) end the chain. This replaces the previous hardcoded
       * provider→generic→system ordering.
       */
      async resolveFallbackChain(providerId) {
        const atIdx = providerId.indexOf("@");
        const baseId = atIdx >= 0 ? providerId.slice(0, atIdx) : providerId;
        const version = atIdx >= 0 ? providerId.slice(atIdx + 1) : "latest";
        let start = await this.store.getParserByProviderAndVersion(baseId, version);
        if (!start && baseId !== "generic") {
          start = await this.store.getParserByProviderAndVersion("generic", "latest");
        }
        if (!start) return [];
        const chain = [];
        const visited = /* @__PURE__ */ new Set();
        let cursor = start;
        while (cursor && !visited.has(cursor.id)) {
          visited.add(cursor.id);
          chain.push(cursor);
          if (!cursor.fallbackParserId) break;
          cursor = await this.store.getParserById(cursor.fallbackParserId);
        }
        return chain;
      }
      async reloadParser(providerId) {
        const atIdx = providerId.indexOf("@");
        const baseId = atIdx >= 0 ? providerId.slice(0, atIdx) : providerId;
        const version = atIdx >= 0 ? providerId.slice(atIdx + 1) : "latest";
        const row = await this.store.getParserByProviderAndVersion(baseId, version);
        if (row) this.parserCache.delete(row.id);
      }
      async preloadAll() {
        for (const providerId of this.config?.preloadProviders ?? []) {
          try {
            const row = await this.store.getParserByProviderAndVersion(providerId, "latest");
            if (row) await this.loadModuleFromRow(row);
          } catch {
          }
        }
      }
      /**
       * Prime the parser cache from the generated protocol's inline logic_code, so
       * the hot parse path performs ZERO DB reads. The DB-backed fallback chain
       * (resolveFallbackChain) remains as a runtime safety net if a protocol-derived
       * module is missing or fails to compile. Compiled modules are keyed by their
       * parser hash; the generated protocol carries the same hash the DB row has.
       */
      async primeFromProtocol(protocol3) {
        for (const p of protocol3.providers) {
          for (const pr of p.parsers) {
            if (!pr.isActive || !pr.logicCode) continue;
            try {
              const module = await this.loadInlineParser(pr.logicCode, pr.hash);
              const tagged = {
                ...module,
                name: pr.name,
                version: pr.version,
                providerId: p.slug
              };
              this.primedParsers.set(`${p.slug}/${pr.version}`, tagged);
              this.primedParsers.set(`${p.slug}/${pr.name.split("/")[1] ?? pr.version}`, tagged);
            } catch {
            }
          }
        }
      }
      // ── private ─────────────────────────────────────────────────────────────
      async loadModuleFromRow(row) {
        const cached = this.parserCache.get(row.id);
        if (cached && cached.hash === row.hash) return cached.module;
        let module;
        if (row.logicType === "inline" && row.logicCode) {
          module = await this.loadInlineParser(row.logicCode, row.hash);
        } else if (row.logicType === "file" && row.filePath) {
          if (!this.config?.allowFileLogic) {
            throw new EngineError(
              `Parser '${row.name}' uses file logic but allowFileLogic is false \u2014 parsing logic must live in the DB`
            );
          }
          module = await this.loadFileParser(row.filePath);
        } else {
          throw new EngineError(`Parser '${row.name}' has no logic (logicType=${row.logicType})`);
        }
        this.parserCache.set(row.id, { module, hash: row.hash });
        return module;
      }
      async loadInlineParser(code, hash) {
        const cached = this.inlineCache.get(hash);
        if (cached) return cached;
        const mod = { exports: {} };
        if (!this.sandbox) {
          try {
            assertTrustedExpressionSource(code, "inline parser");
            const factory = new Function("module", "exports", code);
            factory(mod, mod.exports);
          } catch (error2) {
            throw new EngineError(`Failed to compile inline parser: ${error2}`);
          }
        } else {
          const res = await this.sandbox.run(code, {}, _StreamParserEngine.SANDBOX_PERMISSIONS, {
            handlerSlug: `parser:${hash}`,
            globals: { module: mod, exports: mod.exports }
          });
          if (!res.ok) {
            throw new EngineError(
              `Failed to compile inline parser: ${res.error ?? "unknown sandbox error"}`
            );
          }
        }
        const candidate = mod.exports.default ?? mod.exports;
        if (typeof candidate.parse !== "function") {
          throw new EngineError("Inline parser has no parse() method");
        }
        const module = candidate;
        this.inlineCache.set(hash, module);
        return module;
      }
      async loadFileParser(filePath) {
        const imported = await import(filePath);
        const candidate = imported.default ?? imported;
        if (typeof candidate.parse !== "function") {
          throw new EngineError(`Parser at ${filePath} has no parse() method`);
        }
        return candidate;
      }
      /**
       * Best-effort diagnostic logging. Failures are swallowed — logging must never
       * break the parse path. When logStore is absent (default), this is a no-op.
       */
      logParseResult(result, providerId) {
        if (!this.logStore) return;
        const d = result.blockDiagnostics;
        void this.logStore.logExecution({
          providerId,
          parserName: result.parserName,
          parserVersion: result.parserVersion,
          conversationId: null,
          messageId: null,
          confidence: result.confidence,
          blockCount: result.blocks.length,
          textBlocks: d.textBlocks,
          toolCallBlocks: d.toolCallBlocks,
          fileBlocks: d.fileBlocks,
          errorBlocks: d.errorBlocks,
          durationMs: result.durationMs,
          rawSizeBytes: result.rawSizeBytes,
          wireFormat: result.wireFormat,
          fallbackUsed: result.fallbackDepth > 0 ? 1 : 0,
          metadataJson: JSON.stringify({
            reasoningBlocks: d.reasoningBlocks,
            codeBlocks: d.codeBlocks,
            sourceBlocks: d.sourceBlocks,
            fallbackDepth: result.fallbackDepth
          })
        }).catch(() => {
        });
      }
    };
  }
});

// src/storage/prisma.ts
import { join as join5, resolve as resolve3 } from "path";
import { env } from "process";
import { PrismaClient } from "@prisma/client";
async function initPrismaWal(prisma3) {
  if (walApplied) return;
  walApplied = true;
  const p = prisma3 ?? getPrisma();
  try {
    const { configurePrisma: configurePrisma2 } = await Promise.resolve().then(() => (init_db(), db_exports));
    await configurePrisma2({ prisma: p });
  } catch (err) {
    console.warn("[db] initPrismaWal delegation skipped:", err);
  }
}
function getPrisma() {
  if (!client2) {
    let url = env.DATABASE_URL ?? "file:./dev.db";
    if (url.startsWith("file:.")) {
      const relPath = url.slice(5);
      const schemaDir = join5(import.meta.dir, "..", "..", "prisma");
      const absPath = resolve3(schemaDir, relPath);
      url = `file:${absPath}`;
    }
    client2 = new PrismaClient({
      log: env.NODE_ENV === "development" ? ["warn", "error"] : ["error"],
      datasources: { db: { url } }
    });
    if (!walApplied) {
      initPrismaWal(client2).catch(() => {
      });
    }
  }
  return client2;
}
async function closePrisma() {
  if (client2) {
    await client2.$disconnect();
    client2 = null;
  }
}
var client2, walApplied;
var init_prisma = __esm({
  "src/storage/prisma.ts"() {
    "use strict";
    client2 = null;
    walApplied = false;
  }
});

// src/storage/impl/slave-setup-store-impl.ts
var slave_setup_store_impl_exports = {};
__export(slave_setup_store_impl_exports, {
  SlaveSetupStoreImpl: () => SlaveSetupStoreImpl
});
import { mkdir as mkdir2, readFile as readFile2, writeFile as writeFile2 } from "fs/promises";
import { join as join6 } from "path";
async function ensureConfigDir() {
  try {
    await mkdir2(CONFIG_DIR, { recursive: true });
  } catch {
  }
}
async function readConfig() {
  try {
    const txt = await readFile2(CONFIG_FILE, "utf-8");
    return JSON.parse(txt);
  } catch {
    return {};
  }
}
async function writeConfig(config2) {
  await ensureConfigDir();
  await writeFile2(CONFIG_FILE, JSON.stringify(config2, null, 2), "utf-8");
}
var CONFIG_DIR, CONFIG_FILE, SlaveSetupStoreImpl;
var init_slave_setup_store_impl = __esm({
  "src/storage/impl/slave-setup-store-impl.ts"() {
    "use strict";
    CONFIG_DIR = process.platform === "win32" ? "C:\\.config\\vivim" : "/.config/vivim";
    CONFIG_FILE = join6(CONFIG_DIR, "setup-config.json");
    SlaveSetupStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async getWorkspaceHint() {
        const config2 = await readConfig();
        return typeof config2.workspacePath === "string" ? config2.workspacePath : null;
      }
      async setWorkspaceHint(path) {
        const config2 = await readConfig();
        config2.workspacePath = path;
        config2.workspaceSetAt = Date.now();
        await writeConfig(config2);
      }
      async upsertAccount(account) {
        const now = Date.now();
        await this.db.prisma.providerAccount.upsert({
          where: { id: account.id },
          create: {
            id: account.id,
            providerId: account.providerId,
            email: account.accountSlug,
            planTier: account.planTier,
            providerStateJson: "{}",
            profileDir: account.profileDir,
            loginState: account.loginState,
            debugPort: account.debugPort,
            createdAt: account.created_at ?? now,
            updatedAt: now
          },
          update: {
            email: account.accountSlug,
            planTier: account.planTier,
            profileDir: account.profileDir ?? void 0,
            loginState: account.loginState ?? void 0,
            debugPort: account.debugPort ?? void 0,
            updatedAt: now
          }
        });
      }
      async getAccount(providerId, accountId) {
        const row = await this.db.prisma.providerAccount.findFirst({
          where: { providerId, email: accountId }
        });
        if (!row) return null;
        return {
          id: row.id,
          providerId: row.providerId,
          accountSlug: row.email,
          displayName: row.email,
          planTier: row.planTier,
          loginState: row.loginState ?? "unknown",
          profileDir: row.profileDir,
          debugPort: row.debugPort,
          created_at: Number(row.createdAt),
          updated_at: Number(row.updatedAt)
        };
      }
      async listAccounts() {
        const rows = await this.db.prisma.providerAccount.findMany();
        return rows.map((r) => ({
          id: r.id,
          providerId: r.providerId,
          accountSlug: r.email,
          displayName: r.email,
          planTier: r.planTier ?? "free",
          loginState: r.loginState ?? "unknown",
          profileDir: r.profileDir,
          debugPort: r.debugPort,
          created_at: Number(r.createdAt ?? Date.now()),
          updated_at: Number(r.updatedAt ?? Date.now())
        }));
      }
    };
  }
});

// src/storage/db.ts
var db_exports = {};
__export(db_exports, {
  CapStoreDb: () => CapStoreDb,
  closeDb: () => closeDb,
  configurePrisma: () => configurePrisma,
  getDb: () => getDb,
  setDb: () => setDb
});
function getDb() {
  if (!_db) {
    _db = new CapStoreDb();
  }
  return _db;
}
function setDb(db) {
  _db = db;
}
async function configurePrisma(db, policy) {
  const pragmas = { ...DEFAULT_PRAGMAS, ...policy };
  await db.prisma.$queryRawUnsafe(`PRAGMA journal_mode = ${pragmas.journalMode}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA synchronous = ${pragmas.synchronous}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA cache_size = ${pragmas.cacheSize}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA temp_store = ${pragmas.tempStore}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA mmap_size = ${pragmas.mmapSize}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA busy_timeout = ${pragmas.busyTimeoutMs}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA wal_autocheckpoint = ${pragmas.walAutocheckpoint}`);
  await db.prisma.$queryRawUnsafe(`PRAGMA foreign_keys = ${pragmas.foreignKeys ? "ON" : "OFF"}`);
  const journalMode = await db.prisma.$queryRawUnsafe("PRAGMA journal_mode");
  console.log(`[db] pragmas configured \u2014 journal_mode=${journalMode[0]?.journal_mode}`);
}
async function closeDb() {
  if (_db) {
    await _db.close();
    _db = null;
  }
}
var CapStoreDb, _db, DEFAULT_PRAGMAS;
var init_db = __esm({
  "src/storage/db.ts"() {
    "use strict";
    init_ids();
    init_prisma();
    CapStoreDb = class {
      prisma;
      constructor(_path) {
        this.prisma = getPrisma();
      }
      async close() {
        await closePrisma();
      }
      // ── Migration helper ───────────────────────────────────────────────────
      async applyMigration(filename, sql) {
        const checksum = await Bun.CryptoHasher.hash("sha256", sql, "hex");
        await this.prisma.$executeRawUnsafe(sql);
        await this.prisma.migrationLog.create({
          data: {
            id: newId(),
            filename,
            checksum,
            appliedAt: Date.now()
          }
        });
      }
      async hasMigration(filename) {
        const row = await this.prisma.migrationLog.findFirst({
          where: { filename },
          select: { id: true }
        });
        return row !== null;
      }
      // ── L1: Provider CRUD ──────────────────────────────────────────────────
      async getProvider(id) {
        return this.prisma.providerDefinition.findUnique({ where: { id } });
      }
      async getProviderBySlug(slug) {
        return this.prisma.providerDefinition.findUnique({ where: { slug } });
      }
      async listProviders(opts) {
        return this.prisma.providerDefinition.findMany({
          where: opts?.isActive !== void 0 ? { isActive: opts.isActive ? 1 : 0 } : void 0,
          orderBy: { displayName: "asc" }
        });
      }
      async upsertProvider(def) {
        const now = Date.now();
        return this.prisma.providerDefinition.upsert({
          where: { id: def.id },
          create: {
            id: def.id,
            slug: def.slug,
            displayName: def.displayName,
            description: def.description ?? null,
            category: def.category ?? "ai",
            providerType: def.providerType ?? "llm",
            isActive: def.isActive ?? 1,
            websiteUrl: def.websiteUrl ?? null,
            documentationUrl: def.documentationUrl ?? null,
            authType: def.authType ?? "browser",
            hasMultiAccount: def.hasMultiAccount ?? 0,
            profileStrategy: def.profileStrategy ?? "per_account",
            fleetConfigJson: def.fleetConfigJson ?? "{}",
            capabilitiesJson: def.capabilitiesJson ?? "{}",
            modelsJson: def.modelsJson ?? "[]",
            createdAt: def.createdAt ?? now,
            updatedAt: now
          },
          update: {
            slug: def.slug,
            displayName: def.displayName,
            description: def.description ?? null,
            category: def.category ?? "ai",
            providerType: def.providerType ?? "llm",
            isActive: def.isActive ?? 1,
            websiteUrl: def.websiteUrl ?? null,
            documentationUrl: def.documentationUrl ?? null,
            authType: def.authType ?? "browser",
            hasMultiAccount: def.hasMultiAccount ?? 0,
            profileStrategy: def.profileStrategy ?? "per_account",
            fleetConfigJson: def.fleetConfigJson ?? "{}",
            capabilitiesJson: def.capabilitiesJson ?? "{}",
            modelsJson: def.modelsJson ?? "[]",
            updatedAt: now
          }
        });
      }
      // Account
      async getAccount(id) {
        return this.prisma.providerAccount.findUnique({ where: { id } });
      }
      async getAccountsByProvider(providerId) {
        return this.prisma.providerAccount.findMany({
          where: { providerId },
          orderBy: { isDefault: "desc" }
        });
      }
      async upsertAccount(account) {
        const now = Date.now();
        return this.prisma.providerAccount.upsert({
          where: { id: account.id },
          create: {
            id: account.id,
            providerId: account.providerId,
            email: account.email,
            planTier: account.planTier ?? "free",
            isDefault: account.isDefault ?? 0,
            isKind: account.isKind ?? 0,
            loginState: account.loginState ?? "unknown",
            loginAttempts: account.loginAttempts ?? 0,
            lastLoginAt: account.lastLoginAt ?? null,
            providerStateJson: account.providerStateJson ?? "{}",
            debugPort: account.debugPort ?? null,
            profileDir: account.profileDir ?? null,
            chromeSlaveId: account.chromeSlaveId ?? null,
            createdAt: account.createdAt ?? now,
            updatedAt: now
          },
          update: {
            email: account.email,
            planTier: account.planTier ?? "free",
            loginState: account.loginState ?? "unknown",
            debugPort: account.debugPort ?? null,
            chromeSlaveId: account.chromeSlaveId ?? null,
            updatedAt: now
          }
        });
      }
      // ── L3: Capability CRUD ────────────────────────────────────────────────
      async getCapability(id) {
        return this.prisma.capabilityTaxonomy.findUnique({ where: { id } });
      }
      async getCapabilityBySlug(slug) {
        return this.prisma.capabilityTaxonomy.findUnique({ where: { slug } });
      }
      async getBinding(globalId, providerId) {
        return this.prisma.capabilityBinding.findUnique({
          where: { globalId_providerId: { globalId, providerId } }
        });
      }
      async getSelectors(capabilityId, providerId) {
        return this.prisma.selectorStrategy.findMany({
          where: { capabilityId, providerId, isActive: 1 },
          orderBy: { priority: "asc" }
        });
      }
      // ── L4: Conversation CRUD ──────────────────────────────────────────────
      async getConversation(id) {
        return this.prisma.conversation.findUnique({ where: { id } });
      }
      async listConversations(opts) {
        return this.prisma.conversation.findMany({
          where: opts?.providerId ? { providerId: opts.providerId } : void 0,
          orderBy: { createdAt: "desc" },
          take: opts?.limit ?? 50
        });
      }
      async createConversation(input) {
        const now = Date.now();
        return this.prisma.conversation.create({
          data: {
            id: input.id,
            providerSessionId: input.providerSessionId,
            providerId: input.providerId,
            title: input.title ?? null,
            state: "active",
            contextJson: "{}",
            createdAt: now,
            updatedAt: now
          }
        });
      }
      /**
       * Idempotently ensure a valid ProviderSession exists for (providerId, accountId),
       * creating the prerequisite VivimSession + default ProviderAccount when missing.
       * Conversations require a real providerSessionId FK (schema cascade), so callers
       * MUST obtain the session id from here rather than inventing a literal string.
       */
      async ensureProviderSession(input) {
        const providerId = input.providerId;
        const accountId = input.accountId ?? `${providerId}_default`;
        const now = Date.now();
        const existing = await this.prisma.providerSession.findFirst({
          where: { providerId, accountId },
          select: { id: true }
        });
        if (existing) return { id: existing.id };
        const vivimSession = await this.prisma.vivimSession.create({
          data: { id: newId(), state: "idle", contextJson: "{}", createdAt: now, updatedAt: now }
        });
        await this.prisma.providerAccount.upsert({
          where: { id: accountId },
          create: {
            id: accountId,
            providerId,
            email: `${accountId}@local`,
            planTier: "free",
            isDefault: 1,
            isKind: 0,
            loginState: "unknown",
            loginAttempts: 0,
            providerStateJson: "{}",
            createdAt: now,
            updatedAt: now
          },
          update: { updatedAt: now }
        });
        const session = await this.prisma.providerSession.create({
          data: {
            id: newId(),
            vivimSessionId: vivimSession.id,
            providerId,
            accountId,
            state: "idle",
            contextJson: "{}",
            createdAt: now,
            updatedAt: now
          }
        });
        return { id: session.id };
      }
      async createMessage(input) {
        const now = Date.now();
        return this.prisma.conversationMessage.create({
          data: {
            id: input.id,
            conversationId: input.conversationId,
            role: input.role,
            content: input.content,
            blocksJson: input.blocksJson ?? "[]",
            blockCount: 0,
            sequenceIndex: 0,
            latencyMs: input.latencyMs ?? null,
            metadataJson: JSON.stringify(input.metadata ?? {}),
            createdAt: now
          }
        });
      }
      async getMessages(conversationId, opts) {
        return this.prisma.conversationMessage.findMany({
          where: {
            conversationId,
            ...opts?.before ? { id: { lt: opts.before } } : {}
          },
          orderBy: { sequenceIndex: "asc" },
          take: opts?.limit ?? 100
        });
      }
      // ── L3: Outcome recording ──────────────────────────────────────────────
      async createOutcome(input) {
        return this.prisma.outcome.create({
          data: {
            id: input.id,
            capabilityId: input.capabilityId,
            providerId: input.providerId,
            ok: input.ok,
            error: input.error ?? null,
            durationMs: input.durationMs ?? null,
            selectorStrategyId: input.selectorStrategyId ?? null,
            selectorUsed: input.selectorUsed ?? null,
            selectorHit: input.selectorHit ?? null,
            ts: Date.now()
          }
        });
      }
      // ── L2: Trace ──────────────────────────────────────────────────────────
      async createTraceEntry(input) {
        return this.prisma.traceEntry.create({
          data: {
            id: input.id,
            engine: input.engine,
            method: input.method,
            conversationId: input.conversationId ?? null,
            providerId: input.providerId ?? null,
            slaveId: input.slaveId ?? null,
            cdpMethod: input.cdpMethod ?? null,
            cdpParamsJson: input.cdpParams ? JSON.stringify(input.cdpParams) : null,
            cdpResultJson: input.cdpResult ? JSON.stringify(input.cdpResult) : null,
            durationMs: input.durationMs,
            ok: input.ok ? 1 : 0,
            error: input.error ?? null,
            ts: Date.now()
          }
        });
      }
      // ── L8: Config ─────────────────────────────────────────────────────────
      async getConfig(engineId) {
        return this.prisma.configEntry.findMany({
          where: { engineId }
        });
      }
      async setConfig(engineId, configJson) {
        const existing = await this.prisma.configEntry.findFirst({ where: { engineId } });
        const now = Date.now();
        if (existing) {
          return this.prisma.configEntry.update({
            where: { id: existing.id },
            data: { configJson }
          });
        }
        return this.prisma.configEntry.create({
          data: {
            id: `cfg_${engineId}_${Date.now()}`,
            engineId,
            configJson,
            scopeType: "global",
            scopeId: void 0,
            createdAt: BigInt(now),
            updatedAt: BigInt(now)
          }
        });
      }
      async getConfigValue(engineId, _key) {
        const entry = await this.prisma.configEntry.findFirst({
          where: { engineId }
        });
        return entry?.configJson ?? null;
      }
      // ── Setup: Workspace + Profile ──────────────────────────────────────────
      async getWorkspaceHint() {
        const { SlaveSetupStoreImpl: SlaveSetupStoreImpl2 } = await Promise.resolve().then(() => (init_slave_setup_store_impl(), slave_setup_store_impl_exports));
        const store = new SlaveSetupStoreImpl2(this);
        return store.getWorkspaceHint();
      }
      async setWorkspaceHint(path) {
        const { SlaveSetupStoreImpl: SlaveSetupStoreImpl2 } = await Promise.resolve().then(() => (init_slave_setup_store_impl(), slave_setup_store_impl_exports));
        const store = new SlaveSetupStoreImpl2(this);
        return store.setWorkspaceHint(path);
      }
      async listAccounts() {
        const { SlaveSetupStoreImpl: SlaveSetupStoreImpl2 } = await Promise.resolve().then(() => (init_slave_setup_store_impl(), slave_setup_store_impl_exports));
        const store = new SlaveSetupStoreImpl2(this);
        return store.listAccounts();
      }
    };
    _db = null;
    DEFAULT_PRAGMAS = {
      journalMode: "WAL",
      synchronous: "NORMAL",
      cacheSize: -64e3,
      tempStore: "MEMORY",
      mmapSize: 268435456,
      busyTimeoutMs: 5e3,
      walAutocheckpoint: 1e3,
      foreignKeys: true
    };
  }
});

// src/storage/impl/discovery-store-impl.ts
var init_discovery_store_impl = __esm({
  "src/storage/impl/discovery-store-impl.ts"() {
    "use strict";
  }
});

// src/storage/impl/governor-store-impl.ts
var governor_store_impl_exports = {};
__export(governor_store_impl_exports, {
  GovernorStoreImpl: () => GovernorStoreImpl
});
function toAccountRow(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    accountSlug: r.email,
    displayName: r.email,
    planTier: r.planTier,
    apiKeyRef: null,
    isActive: 1,
    profileDir: r.profileDir ?? null,
    debugPort: r.debugPort ?? null,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
function toFleetRow(r) {
  return {
    id: r.id,
    slaveId: r.slaveId,
    providerId: r.providerId ?? "",
    eventType: r.eventType,
    detailJson: r.eventDataJson,
    ts: r.ts
  };
}
function toCircuitRow(r) {
  return {
    id: r.id,
    slaveId: r.slaveId,
    state: r.state,
    failureCount: r.failCount,
    lastFailureAt: r.lastFailAt,
    lastSuccessAt: r.lastSuccessAt,
    openedAt: r.openedAt
  };
}
function toHealthTickRow(r) {
  return {
    id: r.id,
    slaveId: r.slaveId ?? "",
    providerId: r.providerId,
    status: r.status,
    responseMs: r.responseMs,
    error: r.error,
    ts: r.ts
  };
}
function toTraceRow(r) {
  return {
    id: r.id,
    slaveId: r.slaveId ?? "",
    conversationId: r.conversationId,
    method: r.method,
    paramsJson: r.cdpParamsJson,
    resultJson: r.cdpResultJson,
    durationMs: r.durationMs,
    error: r.error,
    ts: r.ts
  };
}
function toHarnessCommandRow(r) {
  return {
    id: r.id,
    commandId: r.commandId,
    version: r.version,
    kind: r.kind,
    paramsSchemaJson: r.paramsSchemaJson,
    adaptorRef: r.adaptorRef,
    description: r.description,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
var GovernorStoreImpl;
var init_governor_store_impl = __esm({
  "src/storage/impl/governor-store-impl.ts"() {
    "use strict";
    init_ids();
    GovernorStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async getAccount(accountId) {
        const row = await this.db.prisma.providerAccount.findUnique({ where: { id: accountId } });
        return row ? toAccountRow(row) : null;
      }
      async getAccountsByProvider(providerId) {
        const rows = await this.db.prisma.providerAccount.findMany({ where: { providerId } });
        return rows.map((r) => toAccountRow(r));
      }
      async upsertAccount(account) {
        const now = Date.now();
        await this.db.prisma.providerAccount.upsert({
          where: { id: account.id },
          create: {
            id: account.id,
            providerId: account.providerId,
            email: account.accountSlug,
            planTier: account.planTier,
            providerStateJson: "{}",
            createdAt: account.createdAt || now,
            updatedAt: now
          },
          update: {
            email: account.accountSlug,
            planTier: account.planTier,
            updatedAt: now
          }
        });
      }
      async deleteAccount(accountId) {
        await this.db.prisma.providerAccount.delete({ where: { id: accountId } });
      }
      async createFleetEvent(event) {
        const row = await this.db.prisma.fleetEvent.create({
          data: {
            id: newId(),
            slaveId: event.slaveId,
            providerId: event.providerId,
            eventType: event.eventType,
            eventDataJson: event.detailJson ?? "{}",
            ts: event.ts ?? Date.now()
          }
        });
        return toFleetRow(row);
      }
      async getFleetEvents(slaveId, limit) {
        const rows = await this.db.prisma.fleetEvent.findMany({
          where: { slaveId },
          orderBy: [{ ts: "desc" }, { id: "desc" }],
          take: limit ?? 100
        });
        return rows.map((r) => toFleetRow(r));
      }
      async getCircuitState(slaveId) {
        const row = await this.db.prisma.circuitBreakerState.findUnique({ where: { slaveId } });
        return row ? toCircuitRow(row) : null;
      }
      async upsertCircuitState(state) {
        const now = Date.now();
        await this.db.prisma.circuitBreakerState.upsert({
          where: { slaveId: state.slaveId },
          create: {
            id: state.id,
            slaveId: state.slaveId,
            state: state.state,
            failCount: state.failureCount,
            lastFailAt: state.lastFailureAt,
            lastSuccessAt: state.lastSuccessAt,
            openedAt: state.openedAt,
            createdAt: now,
            updatedAt: now
          },
          update: {
            state: state.state,
            failCount: state.failureCount,
            lastFailAt: state.lastFailureAt,
            lastSuccessAt: state.lastSuccessAt,
            openedAt: state.openedAt,
            updatedAt: now
          }
        });
      }
      async createHealthTick(tick) {
        const row = await this.db.prisma.healthTick.create({
          data: {
            id: newId(),
            providerId: tick.providerId,
            slaveId: tick.slaveId,
            status: tick.status,
            responseMs: tick.responseMs,
            error: tick.error,
            ts: tick.ts
          }
        });
        return toHealthTickRow(row);
      }
      async createTraceEntry(entry) {
        const row = await this.db.prisma.traceEntry.create({
          data: {
            id: newId(),
            engine: "governor",
            method: entry.method,
            conversationId: entry.conversationId ?? null,
            slaveId: entry.slaveId ?? null,
            cdpParamsJson: entry.paramsJson ?? null,
            cdpResultJson: entry.resultJson ?? null,
            durationMs: entry.durationMs ?? 0,
            ok: entry.error ? 0 : 1,
            error: entry.error ?? null,
            ts: Date.now()
          }
        });
        return toTraceRow(row);
      }
      async getTrace(slaveId, limit) {
        const rows = await this.db.prisma.traceEntry.findMany({
          where: slaveId === "*" ? {} : { slaveId },
          orderBy: [{ ts: "desc" }, { id: "desc" }],
          take: limit ?? 100
        });
        return rows.map((r) => toTraceRow(r));
      }
      async getProviderFleetConfig(providerSlug) {
        const def = await this.db.prisma.providerDefinition.findUnique({
          where: { slug: providerSlug },
          select: { fleetConfigJson: true }
        });
        if (!def?.fleetConfigJson) return null;
        try {
          const raw = JSON.parse(def.fleetConfigJson);
          return {
            channel: typeof raw.channel === "string" ? raw.channel : void 0,
            mode: typeof raw.mode === "string" ? raw.mode : void 0,
            extraArgs: Array.isArray(raw.extra_args) ? raw.extra_args.filter((a) => typeof a === "string") : void 0,
            portRange: Array.isArray(raw.port_range) && raw.port_range.length === 2 ? [Number(raw.port_range[0]), Number(raw.port_range[1])] : void 0
          };
        } catch {
          return null;
        }
      }
      // ── Harness Command Registry (017-harness-command-registry) ──
      async getHarnessCommand(commandId, version) {
        const row = await this.db.prisma.harnessCommand.findUnique({
          where: { commandId_version: { commandId, version } }
        });
        return row ? toHarnessCommandRow(row) : null;
      }
      async listHarnessCommands(commandId) {
        const rows = await this.db.prisma.harnessCommand.findMany({ where: { commandId } });
        return rows.map((r) => toHarnessCommandRow(r));
      }
      async upsertHarnessCommand(cmd) {
        await this.db.prisma.harnessCommand.upsert({
          where: { commandId_version: { commandId: cmd.commandId, version: cmd.version } },
          create: {
            id: cmd.id,
            commandId: cmd.commandId,
            version: cmd.version,
            kind: cmd.kind,
            paramsSchemaJson: cmd.paramsSchemaJson,
            adaptorRef: cmd.adaptorRef,
            description: cmd.description,
            createdAt: cmd.createdAt,
            updatedAt: cmd.updatedAt
          },
          update: {
            kind: cmd.kind,
            paramsSchemaJson: cmd.paramsSchemaJson,
            adaptorRef: cmd.adaptorRef,
            description: cmd.description,
            updatedAt: cmd.updatedAt
          }
        });
      }
    };
  }
});

// src/storage/impl/parser-store-impl.ts
var parser_store_impl_exports = {};
__export(parser_store_impl_exports, {
  ParserStoreImpl: () => ParserStoreImpl
});
function parseSemver(v) {
  const m = v.match(/^v?(\d+)(?:\.(\d+))?(?:\.(\d+))?/);
  if (!m) return [0, 0, 0];
  return [Number(m[1]), Number(m[2] ?? 0), Number(m[3] ?? 0)];
}
function cmpSemver(a, b) {
  const pa = parseSemver(a);
  const pb = parseSemver(b);
  for (let i = 0; i < 3; i++) {
    if (pa[i] !== pb[i]) return (pa[i] ?? 0) - (pb[i] ?? 0);
  }
  return 0;
}
function toParserRow(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    name: r.name,
    version: r.version,
    logicType: r.parserLogicType,
    filePath: r.parserFilePath,
    logicCode: r.parserLogicCode,
    hash: r.parserHash ?? "",
    isActive: r.isActive,
    fallbackParserId: r.fallbackParserId,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
var ParserStoreImpl;
var init_parser_store_impl = __esm({
  "src/storage/impl/parser-store-impl.ts"() {
    "use strict";
    ParserStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async getParser(providerId) {
        const r = await this.p.providerParser.findFirst({
          where: { providerId },
          orderBy: { parserVersion: "desc" }
        });
        return r ? toParserRow(r) : null;
      }
      async getActiveParser(providerId) {
        const r = await this.p.providerParser.findFirst({
          where: { providerId, isActive: 1 }
        });
        return r ? toParserRow(r) : null;
      }
      async getParserByProviderAndVersion(providerId, version) {
        const rows = await this.p.providerParser.findMany({
          where: { providerId },
          orderBy: { parserVersion: "desc" }
        });
        if (rows.length === 0) return null;
        if (!version || version === "latest") {
          const active = rows.find((r) => r.isActive === 1);
          const chosen2 = active ?? rows[0];
          if (!chosen2) return null;
          return toParserRow(chosen2);
        }
        const target = parseSemver(version);
        const candidates = rows.filter((r) => cmpSemver(`${r.version}.0.0`, `${target[0]}.0.0`) >= 0).sort((a, b) => cmpSemver(`${b.version}.0.0`, `${a.version}.0.0`));
        const chosen = candidates[0] ?? rows[0];
        if (!chosen) return null;
        return toParserRow(chosen);
      }
      async getParserById(id) {
        const r = await this.p.providerParser.findFirst({ where: { id } });
        return r ? toParserRow(r) : null;
      }
      async upsertParser(parser) {
        const now = Date.now();
        await this.p.providerParser.upsert({
          where: { id: parser.id },
          create: {
            id: parser.id,
            providerId: parser.providerId,
            name: parser.name,
            version: parser.version,
            parserLogicType: parser.logicType,
            parserFilePath: parser.filePath,
            parserLogicCode: parser.logicCode,
            parserHash: parser.hash,
            isActive: parser.isActive,
            fallbackParserId: parser.fallbackParserId,
            createdAt: parser.createdAt || now,
            updatedAt: now
          },
          update: {
            name: parser.name,
            version: parser.version,
            parserLogicType: parser.logicType,
            parserFilePath: parser.filePath,
            parserLogicCode: parser.logicCode,
            parserHash: parser.hash,
            isActive: parser.isActive,
            fallbackParserId: parser.fallbackParserId,
            updatedAt: now
          }
        });
      }
      async listParsers(providerId) {
        const rows = await this.p.providerParser.findMany({
          where: { providerId },
          orderBy: { parserVersion: "desc" }
        });
        return rows.map(toParserRow);
      }
      async getParserByFile(filePath) {
        const r = await this.p.providerParser.findFirst({
          where: { parserFilePath: filePath }
        });
        return r ? toParserRow(r) : null;
      }
      async getParserByHash(hash) {
        const r = await this.p.providerParser.findFirst({
          where: { parserHash: hash }
        });
        return r ? toParserRow(r) : null;
      }
      async getGenericParser() {
        const r = await this.p.providerParser.findFirst({
          where: { providerId: "generic", isActive: 1 }
        });
        return r ? toParserRow(r) : null;
      }
      async getSystemFallbackParser() {
        const r = await this.p.providerParser.findFirst({
          where: { providerId: "system", isActive: 1 }
        });
        return r ? toParserRow(r) : null;
      }
    };
  }
});

// src/storage/impl/provider-store-impl.ts
var provider_store_impl_exports = {};
__export(provider_store_impl_exports, {
  ProviderStoreImpl: () => ProviderStoreImpl
});
var ProviderStoreImpl;
var init_provider_store_impl = __esm({
  "src/storage/impl/provider-store-impl.ts"() {
    "use strict";
    ProviderStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      // ── Definitions ────────────────────────────────────────────────────────────
      async upsertDefinition(def) {
        const now = Date.now();
        await this.p.providerDefinition.upsert({
          where: { slug: def.slug },
          create: {
            id: def.id,
            slug: def.slug,
            displayName: def.display_name,
            description: def.description,
            category: def.category,
            providerType: def.provider_type,
            isActive: def.is_active,
            websiteUrl: def.website_url,
            documentationUrl: def.documentation_url,
            authType: def.auth_type,
            hasMultiAccount: def.has_multi_account,
            profileStrategy: def.profile_strategy,
            fleetConfigJson: def.fleet_config_json,
            capabilitiesJson: def.capabilities_json,
            modelsJson: def.models_json,
            createdAt: def.created_at,
            updatedAt: now
          },
          update: {
            slug: def.slug,
            displayName: def.display_name,
            description: def.description,
            category: def.category,
            providerType: def.provider_type,
            isActive: def.is_active,
            websiteUrl: def.website_url,
            documentationUrl: def.documentation_url,
            authType: def.auth_type,
            hasMultiAccount: def.has_multi_account,
            profileStrategy: def.profile_strategy,
            fleetConfigJson: def.fleet_config_json,
            capabilitiesJson: def.capabilities_json,
            modelsJson: def.models_json,
            updatedAt: now
          }
        });
      }
      async getDefinition(id) {
        const r = await this.p.providerDefinition.findUnique({ where: { id } });
        if (!r) return null;
        return {
          id: r.id,
          slug: r.slug,
          display_name: r.displayName,
          description: r.description,
          category: r.category,
          provider_type: r.providerType,
          is_active: r.isActive,
          website_url: r.websiteUrl,
          documentation_url: r.documentationUrl,
          auth_type: r.authType,
          has_multi_account: r.hasMultiAccount,
          profile_strategy: r.profileStrategy,
          fleet_config_json: r.fleetConfigJson,
          capabilities_json: r.capabilitiesJson,
          models_json: r.modelsJson,
          created_at: r.createdAt,
          updated_at: r.updatedAt
        };
      }
      async getDefinitionBySlug(slug) {
        const r = await this.p.providerDefinition.findUnique({ where: { slug } });
        if (!r) return null;
        return {
          id: r.id,
          slug: r.slug,
          display_name: r.displayName,
          description: r.description,
          category: r.category,
          provider_type: r.providerType,
          is_active: r.isActive,
          website_url: r.websiteUrl,
          documentation_url: r.documentationUrl,
          auth_type: r.authType,
          has_multi_account: r.hasMultiAccount,
          profile_strategy: r.profileStrategy,
          fleet_config_json: r.fleetConfigJson,
          capabilities_json: r.capabilitiesJson,
          models_json: r.modelsJson,
          created_at: r.createdAt,
          updated_at: r.updatedAt
        };
      }
      async listDefinitions(opts) {
        const where = opts?.isActive !== void 0 ? { isActive: opts.isActive ? 1 : 0 } : void 0;
        const rows = await this.p.providerDefinition.findMany({
          where,
          orderBy: { displayName: "asc" }
        });
        return rows.map((r) => ({
          id: r.id,
          slug: r.slug,
          display_name: r.displayName,
          description: r.description,
          category: r.category,
          provider_type: r.providerType,
          is_active: r.isActive,
          website_url: r.websiteUrl,
          documentation_url: r.documentationUrl,
          auth_type: r.authType,
          has_multi_account: r.hasMultiAccount,
          profile_strategy: r.profileStrategy,
          fleet_config_json: r.fleetConfigJson,
          capabilities_json: r.capabilitiesJson,
          models_json: r.modelsJson,
          created_at: r.createdAt,
          updated_at: r.updatedAt
        }));
      }
      // ── Endpoints ──────────────────────────────────────────────────────────────
      async upsertEndpoint(ep) {
        const now = Date.now();
        await this.p.providerEndpoint.upsert({
          where: { id: ep.id },
          create: {
            id: ep.id,
            providerId: ep.provider_id,
            url: ep.url,
            label: ep.label,
            endpointType: ep.endpoint_type,
            isDefault: ep.is_default,
            selectorsJson: ep.selectors_json,
            composerType: ep.composer_type,
            sendMethod: ep.send_method,
            contentEditable: ep.content_editable,
            createdAt: ep.created_at,
            updatedAt: now
          },
          update: {
            url: ep.url,
            label: ep.label,
            endpointType: ep.endpoint_type,
            isDefault: ep.is_default,
            selectorsJson: ep.selectors_json,
            composerType: ep.composer_type,
            sendMethod: ep.send_method,
            contentEditable: ep.content_editable,
            updatedAt: now
          }
        });
      }
      async deleteProviderEndpoints(providerId) {
        await this.p.providerEndpoint.deleteMany({ where: { providerId } });
      }
      // ── Parsers ────────────────────────────────────────────────────────────────
      async upsertParser(parser) {
        const now = Date.now();
        await this.p.providerParser.upsert({
          where: { id: parser.id },
          create: {
            id: parser.id,
            providerId: parser.provider_id,
            parserName: parser.parser_name,
            parserVersion: parser.parser_version,
            parserLogicType: parser.parser_logic_type,
            parserFilePath: parser.parser_file_path,
            parserLogicCode: parser.parser_logic_code,
            parserHash: parser.parser_hash,
            isActive: parser.is_active,
            fallbackParserId: parser.fallback_parser_id,
            createdAt: parser.created_at,
            updatedAt: now
          },
          update: {
            parserName: parser.parser_name,
            parserVersion: parser.parser_version,
            parserLogicType: parser.parser_logic_type,
            parserFilePath: parser.parser_file_path,
            parserLogicCode: parser.parser_logic_code,
            parserHash: parser.parser_hash,
            isActive: parser.is_active,
            fallbackParserId: parser.fallback_parser_id,
            updatedAt: now
          }
        });
      }
      async setParserFallback(parserId, fallbackParserId) {
        await this.p.providerParser.update({
          where: { id: parserId },
          data: { fallbackParserId, updatedAt: Date.now() }
        });
      }
      async deleteProviderParsers(providerId) {
        await this.p.providerParser.deleteMany({ where: { providerId } });
      }
      // ── Stream Config (derived parser findings) ────────────────────────────────
      // Persists the derived streaming parameters from a discovery/alignment report.
      // On create, any prior active version for the same provider+transport is
      // superseded so only one active config is live per transport.
      async upsertStreamConfig(config2) {
        const now = Date.now();
        const existing = await this.p.providerStreamConfig.findUnique({ where: { id: config2.id } });
        if (existing) {
          await this.p.providerStreamConfig.update({
            where: { id: config2.id },
            data: {
              streamTransport: config2.stream_transport,
              streamTerminalJson: config2.stream_terminal_json ?? "[]",
              sseFormat: config2.sse_format ?? null,
              deltaPathJson: config2.delta_path_json ?? null,
              contentType: config2.content_type ?? null,
              completionDetectorsJson: config2.completion_detectors_json ?? "[]",
              harnessJs: config2.harness_js ?? null,
              isActive: config2.is_active ?? 1,
              supersededById: config2.superseded_by ?? null,
              updatedAt: now
            }
          });
          return;
        }
        if (config2.version && config2.version > 1) {
          await this.p.providerStreamConfig.updateMany({
            where: {
              providerId: config2.provider_id,
              streamTransport: config2.stream_transport,
              isActive: 1
            },
            data: { isActive: 0, supersededById: config2.id, updatedAt: now }
          });
        }
        await this.p.providerStreamConfig.create({
          data: {
            id: config2.id,
            providerId: config2.provider_id,
            streamTransport: config2.stream_transport,
            streamTerminalJson: config2.stream_terminal_json ?? "[]",
            sseFormat: config2.sse_format ?? null,
            deltaPathJson: config2.delta_path_json ?? null,
            contentType: config2.content_type ?? null,
            completionDetectorsJson: config2.completion_detectors_json ?? "[]",
            harnessJs: config2.harness_js ?? null,
            isActive: config2.is_active ?? 1,
            version: config2.version ?? 1,
            supersededById: config2.superseded_by ?? null,
            createdAt: config2.created_at,
            updatedAt: now
          }
        });
      }
      // ── Capabilities ───────────────────────────────────────────────────────────
      async upsertCapability(cap) {
        const now = Date.now();
        await this.p.providerCapability.upsert({
          where: { id: cap.id },
          create: {
            id: cap.id,
            providerId: cap.provider_id,
            globalCapabilityId: cap.global_capability_id,
            recoveryStrategiesJson: cap.recovery_strategies_json,
            uiComponentOverride: cap.ui_component_override,
            uiLabelOverride: cap.ui_label_override,
            uiIconOverride: cap.ui_icon_override,
            uiPositionOverride: cap.ui_position_override,
            uiOrderOverride: cap.ui_order_override,
            uiGroupOverride: cap.ui_group_override,
            uiPriorityOverride: cap.ui_priority_override,
            interactionModeOverride: cap.interaction_mode_override,
            uiStatesOverrideJson: cap.ui_states_override_json,
            uiVisibilityRuleOverride: cap.ui_visibility_rule_override,
            existentialRuleOverride: cap.existential_rule_override,
            uiInputSchemaOverride: cap.ui_input_schema_override,
            mutationEffectsOverrideJson: cap.mutation_effects_override_json,
            recoveryBehaviorOverride: cap.recovery_behavior_override,
            statePersistenceOverride: cap.state_persistence_override,
            dataFlowOverride: cap.data_flow_override,
            minPlanTierOverride: cap.min_plan_tier_override,
            dependsOnOverrideJson: cap.depends_on_override_json,
            confidence: cap.confidence,
            successCount: cap.success_count,
            failCount: cap.fail_count,
            consecutiveFailures: cap.consecutive_failures,
            avgLatencyMs: cap.avg_latency_ms,
            p95LatencyMs: cap.p95_latency_ms,
            lastUsedAt: cap.last_used_at,
            selectorHitCount: cap.selector_hit_count,
            selectorMissCount: cap.selector_miss_count,
            selectorLastMissAt: cap.selector_last_miss_at,
            selectorLastError: cap.selector_last_error,
            createdAt: cap.created_at,
            updatedAt: now
          },
          update: {
            globalCapabilityId: cap.global_capability_id,
            recoveryStrategiesJson: cap.recovery_strategies_json,
            uiComponentOverride: cap.ui_component_override,
            uiLabelOverride: cap.ui_label_override,
            uiIconOverride: cap.ui_icon_override,
            uiPositionOverride: cap.ui_position_override,
            uiOrderOverride: cap.ui_order_override,
            uiGroupOverride: cap.ui_group_override,
            uiPriorityOverride: cap.ui_priority_override,
            interactionModeOverride: cap.interaction_mode_override,
            uiStatesOverrideJson: cap.ui_states_override_json,
            uiVisibilityRuleOverride: cap.ui_visibility_rule_override,
            existentialRuleOverride: cap.existential_rule_override,
            uiInputSchemaOverride: cap.ui_input_schema_override,
            mutationEffectsOverrideJson: cap.mutation_effects_override_json,
            recoveryBehaviorOverride: cap.recovery_behavior_override,
            statePersistenceOverride: cap.state_persistence_override,
            dataFlowOverride: cap.data_flow_override,
            minPlanTierOverride: cap.min_plan_tier_override,
            dependsOnOverrideJson: cap.depends_on_override_json,
            confidence: cap.confidence,
            successCount: cap.success_count,
            failCount: cap.fail_count,
            consecutiveFailures: cap.consecutive_failures,
            avgLatencyMs: cap.avg_latency_ms,
            p95LatencyMs: cap.p95_latency_ms,
            lastUsedAt: cap.last_used_at,
            selectorHitCount: cap.selector_hit_count,
            selectorMissCount: cap.selector_miss_count,
            selectorLastMissAt: cap.selector_last_miss_at,
            selectorLastError: cap.selector_last_error,
            updatedAt: now
          }
        });
      }
      async deleteProviderCapabilities(providerId) {
        await this.p.providerCapability.deleteMany({ where: { providerId } });
      }
      // ── Configs ────────────────────────────────────────────────────────────────
      async upsertConfig(config2) {
        const now = Date.now();
        await this.p.providerConfig.upsert({
          where: { id: config2.id },
          create: {
            id: config2.id,
            providerId: config2.provider_id,
            configKey: config2.config_key,
            configValue: config2.config_value,
            configType: config2.config_type,
            isSecret: config2.is_secret,
            createdAt: config2.created_at,
            updatedAt: now
          },
          update: {
            configKey: config2.config_key,
            configValue: config2.config_value,
            configType: config2.config_type,
            isSecret: config2.is_secret,
            updatedAt: now
          }
        });
      }
      async deleteProviderConfigs(providerId) {
        await this.p.providerConfig.deleteMany({ where: { providerId } });
      }
      // ── Models ─────────────────────────────────────────────────────────────────
      async upsertModel(model) {
        const now = Date.now();
        await this.p.providerModel.upsert({
          where: { id: model.id },
          create: {
            id: model.id,
            providerId: model.provider_id,
            modelSlug: model.model_slug,
            displayName: model.display_name,
            isActive: model.is_active,
            isDefault: model.is_default,
            capabilitiesJson: model.capabilities_json,
            contextWindow: model.context_window,
            maxOutputTokens: model.max_output_tokens,
            supportsStreaming: model.supports_streaming,
            supportsVision: model.supports_vision,
            supportsThinking: model.supports_thinking,
            supportsTools: model.supports_tools,
            pricingInputPer1m: model.pricing_input_per_1m,
            pricingOutputPer1m: model.pricing_output_per_1m,
            createdAt: model.created_at,
            updatedAt: now
          },
          update: {
            modelSlug: model.model_slug,
            displayName: model.display_name,
            isActive: model.is_active,
            isDefault: model.is_default,
            capabilitiesJson: model.capabilities_json,
            contextWindow: model.context_window,
            maxOutputTokens: model.max_output_tokens,
            supportsStreaming: model.supports_streaming,
            supportsVision: model.supports_vision,
            supportsThinking: model.supports_thinking,
            supportsTools: model.supports_tools,
            pricingInputPer1m: model.pricing_input_per_1m,
            pricingOutputPer1m: model.pricing_output_per_1m,
            updatedAt: now
          }
        });
      }
      async deleteProviderModels(providerId) {
        await this.p.providerModel.deleteMany({ where: { providerId } });
      }
      // ── 1.3 Provider Taxonomy Layer ────────────────────────────────────────────
      async registerCapability(input) {
        const now = Date.now();
        const taxonomyId = `${input.providerId}.${input.slug}.${input.version ?? "v1"}`;
        await this.p.capabilityTaxonomy.upsert({
          where: { slug: taxonomyId },
          create: {
            id: taxonomyId,
            slug: taxonomyId,
            name: input.title,
            category: input.category ?? "general",
            description: input.description ?? null,
            inputType: "void",
            uiComponent: "action_button",
            createdAt: now,
            updatedAt: now
          },
          update: {
            name: input.title,
            description: input.description ?? null,
            updatedAt: now
          }
        });
        await this.p.capabilityBinding.upsert({
          where: { globalId_providerId: { globalId: taxonomyId, providerId: input.providerId } },
          create: {
            id: `${taxonomyId}_${input.providerId}`,
            globalId: taxonomyId,
            providerId: input.providerId,
            createdAt: now,
            updatedAt: now
          },
          update: { updatedAt: now }
        });
        if (input.intent) {
          await this.p.capabilityIntent.create({
            data: {
              id: `${taxonomyId}_intent_${now}`,
              capabilityId: taxonomyId,
              intentText: input.intent,
              patternsJson: JSON.stringify([]),
              createdAt: now
            }
          });
        }
        return { id: taxonomyId };
      }
      async overrideCapability(input) {
        const now = Date.now();
        await this.p.providerOverride.upsert({
          where: {
            providerId_capabilityId_overrideType: {
              providerId: input.providerId,
              capabilityId: input.capabilityId,
              overrideType: input.overrideType
            }
          },
          create: {
            id: `${input.providerId}_${input.capabilityId}_${input.overrideType}_${now}`,
            providerId: input.providerId,
            capabilityId: input.capabilityId,
            overrideType: input.overrideType,
            overrideJson: input.overrideJson,
            createdAt: now
          },
          update: { overrideJson: input.overrideJson }
        });
      }
      async listCapabilities(providerId) {
        const bindings = await this.p.capabilityBinding.findMany({
          where: { providerId },
          include: { capability: true }
        });
        return bindings.map(
          (b) => ({
            id: b.capability.id,
            slug: b.capability.slug,
            title: b.capability.name,
            description: b.capability.description ?? void 0
          })
        );
      }
    };
  }
});

// src/storage/impl/sandbox-audit-store-impl.ts
var sandbox_audit_store_impl_exports = {};
__export(sandbox_audit_store_impl_exports, {
  SandboxAuditStoreImpl: () => SandboxAuditStoreImpl
});
var SandboxAuditStoreImpl;
var init_sandbox_audit_store_impl = __esm({
  "src/storage/impl/sandbox-audit-store-impl.ts"() {
    "use strict";
    SandboxAuditStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      get p() {
        return this.db.prisma;
      }
      async create(row) {
        await this.p.sandboxAudit.create({
          data: {
            id: row.id,
            handlerSlug: row.handlerSlug,
            ok: row.ok ? 1 : 0,
            error: row.error,
            permissionsJson: JSON.stringify(row.permissions),
            ts: row.ts
          }
        });
      }
      async list(limit = 100) {
        const rows = await this.p.sandboxAudit.findMany({
          orderBy: { ts: "desc" },
          take: limit
        });
        return rows.map((r) => ({
          id: r.id,
          handlerSlug: r.handlerSlug,
          ok: r.ok === 1,
          error: r.error,
          permissions: JSON.parse(r.permissionsJson),
          ts: Number(r.ts)
        }));
      }
    };
  }
});

// src/cli/discovery-stack.ts
var init_discovery_stack = __esm({
  "src/cli/discovery-stack.ts"() {
    "use strict";
    init_capability_event_bus();
    init_capability_shape_registry();
    init_chrome_governor();
    init_provider_discovery();
    init_provider_registrar();
    init_sandbox_runner();
    init_stream_align();
    init_stream_parser();
    init_db();
    init_discovery_store_impl();
    init_governor_store_impl();
    init_parser_store_impl();
    init_provider_store_impl();
    init_sandbox_audit_store_impl();
  }
});

// src/engines/discovery-session-runner.ts
var init_discovery_session_runner = __esm({
  "src/engines/discovery-session-runner.ts"() {
    "use strict";
    init_errors();
    init_composer_typing();
  }
});

// src/engines/capability-bootstrap.ts
function makeCapability(partial, handler) {
  return {
    ...partial,
    surfaces: partial.surfaces ?? ALL_SURFACES,
    handler,
    isAsync: true,
    requiresConfirmation: false,
    tags: []
  };
}
function registerDefaultCapabilities(registry2, services2) {
  const defaults = [
    // ── Conversation ──────────────────────────────────────────────
    makeCapability(
      {
        id: "cap:conversation:list",
        slug: "conversation_list",
        name: "List Conversations",
        description: "List conversations, optionally filtered by provider.",
        category: "conversation",
        inputSchema: { type: "object", properties: { providerId: { type: "string" } } },
        outputSchema: { type: "array" },
        cliCommand: {
          name: "conversations list",
          aliases: ["cls"],
          examples: ["conversations list --provider=claude"]
        },
        ui: { component: "conversation-list", position: "sidebar", order: 1 },
        mcpToolName: "conversation_list",
        apiEndpoint: { method: "GET", path: "/api/conversations" }
      },
      async (input) => {
        const providerId = input.providerId ? String(input.providerId) : void 0;
        return services2.conversationStore.listConversations({ providerId, limit: 100 });
      }
    ),
    makeCapability(
      {
        id: "cap:conversation:create",
        slug: "conversation_create",
        name: "Create Conversation",
        description: "Create a new conversation for a provider.",
        category: "conversation",
        inputSchema: {
          type: "object",
          properties: { providerId: { type: "string" }, title: { type: "string" } },
          required: ["providerId"]
        },
        outputSchema: { type: "object", properties: { id: { type: "string" } } },
        cliCommand: {
          name: "conversations create",
          aliases: ["cc"],
          examples: ["conversations create claude"]
        },
        ui: { component: "action-button", position: "sidebar", order: 2 },
        mcpToolName: "conversation_create",
        apiEndpoint: { method: "POST", path: "/api/conversations" }
      },
      async (input) => {
        const conv = await services2.conversationStore.createConversation({
          providerSessionId: String(input.providerId ?? ""),
          providerId: String(input.providerId ?? ""),
          title: input.title ? String(input.title) : null
        });
        return { id: conv.id };
      }
    ),
    makeCapability(
      {
        id: "cap:conversation:send",
        slug: "conversation_send",
        name: "Send Message",
        description: "Send a user message to a conversation and return the assistant response.",
        category: "conversation",
        inputSchema: {
          type: "object",
          properties: { conversationId: { type: "string" }, message: { type: "string" } },
          required: ["conversationId", "message"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "conversations send",
          aliases: ["cs"],
          examples: ['cs <id> --message "hello"']
        },
        ui: { component: "composer", position: "composer", order: 1 },
        mcpToolName: "conversation_send",
        apiEndpoint: { method: "POST", path: "/api/conversations/{id}/send" }
      },
      async (input) => {
        const result = await services2.conversationManager.send(
          String(input.conversationId ?? ""),
          String(input.message ?? "")
        );
        return { ok: result.ok, text: result.text ?? null, error: result.error ?? null };
      }
    ),
    makeCapability(
      {
        id: "cap:conversation:delete",
        slug: "conversation_delete",
        name: "Delete Conversation",
        description: "Delete a conversation by id.",
        category: "conversation",
        inputSchema: {
          type: "object",
          properties: { conversationId: { type: "string" } },
          required: ["conversationId"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "conversations delete",
          aliases: ["cd"],
          examples: ["conversations delete <id>"]
        },
        ui: { component: "action-button", position: "sidebar", order: 3 },
        mcpToolName: "conversation_delete",
        apiEndpoint: { method: "DELETE", path: "/api/conversations/{id}" }
      },
      async (input) => {
        await services2.db.prisma.conversation.delete({
          where: { id: String(input.conversationId ?? "") }
        });
        return { ok: true };
      }
    ),
    makeCapability(
      {
        id: "cap:knowledge:search",
        slug: "knowledge_search",
        name: "Knowledge Search",
        description: "Semantic search across conversations, facts, and entities.",
        category: "knowledge",
        inputSchema: { type: "object", properties: { q: { type: "string" } }, required: ["q"] },
        outputSchema: { type: "array" },
        cliCommand: {
          name: "knowledge search",
          aliases: ["ksearch"],
          examples: ['knowledge search "pricing"']
        },
        ui: { component: "knowledge-search", position: "sidebar", order: 9 },
        mcpToolName: "knowledge_search",
        apiEndpoint: { method: "GET", path: "/api/knowledge/search" }
      },
      async () => []
    ),
    makeCapability(
      {
        id: "cap:knowledge:ingest",
        slug: "knowledge_ingest",
        name: "Ingest Export",
        description: "Import an external conversation export (ChatGPT/Claude/Gemini JSON).",
        category: "knowledge",
        inputSchema: {
          type: "object",
          properties: { source: { type: "string" }, filePath: { type: "string" } },
          required: ["source", "filePath"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "knowledge ingest",
          aliases: ["kingest"],
          examples: ["knowledge ingest chatgpt /tmp/export.json"]
        },
        ui: { component: "action-button", position: "sidebar", order: 10 },
        mcpToolName: "knowledge_ingest",
        apiEndpoint: { method: "POST", path: "/api/knowledge/ingest" }
      },
      async () => ({ jobId: "pending" })
    ),
    makeCapability(
      {
        id: "cap:knowledge:synthesize",
        slug: "knowledge_synthesize",
        name: "Synthesize Answer",
        description: "Synthesize an answer across multiple past conversations.",
        category: "knowledge",
        inputSchema: {
          type: "object",
          properties: { question: { type: "string" } },
          required: ["question"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "knowledge synthesize",
          aliases: ["ksynth"],
          examples: ['knowledge synthesize "what did we decide?"']
        },
        ui: { component: "action-button", position: "sidebar", order: 11 },
        mcpToolName: "knowledge_synthesize",
        apiEndpoint: { method: "POST", path: "/api/knowledge/synthesize" }
      },
      async () => ({ answer: "", sources: [], confidence: 0 })
    ),
    // ── Memory ─────────────────────────────────────────────────
    makeCapability(
      {
        id: "cap:memory:query",
        slug: "memory_query",
        name: "Query Memory",
        description: "Query episodic/semantic/procedural memory.",
        category: "memory",
        inputSchema: {
          type: "object",
          properties: { query: { type: "string" } },
          required: ["query"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "memory query",
          aliases: ["mq"],
          examples: ['memory query "last deploy"']
        },
        ui: { component: "action-button", position: "sidebar", order: 12 },
        mcpToolName: "memory_query",
        apiEndpoint: { method: "GET", path: "/api/memory/query" }
      },
      async () => ({ results: [] })
    ),
    makeCapability(
      {
        id: "cap:memory:assert",
        slug: "memory_assert",
        name: "Assert Fact",
        description: "Assert a semantic fact into memory.",
        category: "memory",
        inputSchema: {
          type: "object",
          properties: { fact: { type: "string" } },
          required: ["fact"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "memory assert",
          aliases: ["massert"],
          examples: ['memory assert "deploy on fridays"']
        },
        ui: { component: "action-button", position: "sidebar", order: 13 },
        mcpToolName: "memory_assert",
        apiEndpoint: { method: "POST", path: "/api/memory/assert" }
      },
      async () => ({ ok: true })
    ),
    makeCapability(
      {
        id: "cap:memory:forget",
        slug: "memory_forget",
        name: "Forget Fact",
        description: "Remove a previously asserted semantic fact from memory.",
        category: "memory",
        inputSchema: {
          type: "object",
          properties: { factId: { type: "string" } },
          required: ["factId"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "memory forget",
          aliases: ["mforget"],
          examples: ["memory forget <factId>"]
        },
        ui: { component: "action-button", position: "sidebar", order: 14 },
        mcpToolName: "memory_forget",
        apiEndpoint: { method: "DELETE", path: "/api/memory/{id}" }
      },
      async () => ({ ok: true })
    ),
    // ── Admin ──────────────────────────────────────────────────
    makeCapability(
      {
        id: "cap:admin:seed",
        slug: "admin_seed",
        name: "Seed Providers",
        description: "Re-seed provider manifests from seeds/.",
        category: "admin",
        inputSchema: { type: "object" },
        outputSchema: { type: "object" },
        cliCommand: { name: "admin seed", aliases: ["aseed"], examples: ["admin seed"] },
        ui: { component: "action-button", position: "admin", order: 1 },
        mcpToolName: "admin_seed",
        apiEndpoint: { method: "POST", path: "/api/admin/seed" }
      },
      async () => ({ ok: true })
    ),
    makeCapability(
      {
        id: "cap:admin:config_get",
        slug: "config_get",
        name: "Get Config",
        description: "Get configuration for an engine.",
        category: "admin",
        inputSchema: { type: "object", properties: { engine: { type: "string" } } },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "admin config get",
          aliases: ["acget"],
          examples: ["admin config get governor"]
        },
        ui: { component: "action-button", position: "admin", order: 2 },
        mcpToolName: "config_get",
        apiEndpoint: { method: "GET", path: "/api/admin/config/{engine}" }
      },
      async (input) => {
        const engine = String(input.engine ?? "");
        const config2 = await services2.db.getConfig(engine);
        return { engine, config: config2 ?? {} };
      }
    ),
    makeCapability(
      {
        id: "cap:admin:config_set",
        slug: "config_set",
        name: "Set Config",
        description: "Update configuration for an engine.",
        category: "admin",
        inputSchema: {
          type: "object",
          properties: { engine: { type: "string" }, patch: { type: "object" } },
          required: ["engine", "patch"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "admin config set",
          aliases: ["acset"],
          examples: ["admin config set governor --patch {...}"]
        },
        ui: { component: "action-button", position: "admin", order: 3 },
        mcpToolName: "config_set",
        apiEndpoint: { method: "POST", path: "/api/admin/config/{engine}" }
      },
      async (input) => {
        const engine = String(input.engine ?? "");
        const patch = input.patch ?? {};
        await services2.db.prisma.configEntry.upsert({
          where: {
            engineId_scopeType_scopeId: { engineId: engine, scopeType: "engine", scopeId: "" }
          },
          create: {
            id: `cfg:${engine}`,
            engineId: engine,
            scopeType: "engine",
            scopeId: "",
            configJson: JSON.stringify(patch),
            createdAt: Date.now(),
            updatedAt: Date.now()
          },
          update: { configJson: JSON.stringify(patch) }
        });
        return { ok: true, engine, config: patch };
      }
    ),
    makeCapability(
      {
        id: "cap:system:health",
        slug: "system_health",
        name: "System Health",
        description: "Return liveness + readiness.",
        category: "system",
        inputSchema: { type: "object" },
        outputSchema: { type: "object" },
        cliCommand: { name: "system health", aliases: ["shealth"], examples: ["system health"] },
        ui: { component: "system-health", position: "admin", order: 4 },
        mcpToolName: "system_health",
        apiEndpoint: { method: "GET", path: "/api/health" }
      },
      async () => ({ status: "ok", ts: Date.now() })
    ),
    makeCapability(
      {
        id: "cap:system:version",
        slug: "system_version",
        name: "System Version",
        description: "Return the running vivim version.",
        category: "system",
        inputSchema: { type: "object" },
        outputSchema: { type: "object" },
        cliCommand: { name: "system version", aliases: ["sver"], examples: ["system version"] },
        ui: { component: "system-version", position: "admin", order: 5 },
        mcpToolName: "system_version",
        apiEndpoint: { method: "GET", path: "/api/version" }
      },
      async () => ({ version: "1.0.0" })
    ),
    // ── Health (24.4) ───────────────────────────────────────────
    makeCapability(
      {
        id: "cap:provider:health_get",
        slug: "provider_health_get",
        name: "Get Provider Health",
        description: "Return fleet health snapshot for all running slaves.",
        category: "provider",
        inputSchema: { type: "object" },
        outputSchema: { type: "array" },
        cliCommand: { name: "health", aliases: ["h"], examples: ["health"] },
        ui: { component: "system-health", position: "admin", order: 6 },
        mcpToolName: "provider_health_get",
        apiEndpoint: { method: "GET", path: "/api/telemetry/health" }
      },
      async () => {
        const health = await services2.governor.getAllHealth();
        return Array.from(health.entries()).map(([slaveId, h]) => ({ ...h, slaveId }));
      }
    ),
    // ── Config history (24.4) ───────────────────────────────────
    makeCapability(
      {
        id: "cap:admin:config_history",
        slug: "config_history",
        name: "Config History",
        description: "Show config change history for an engine.",
        category: "admin",
        inputSchema: {
          type: "object",
          properties: { engine: { type: "string" } },
          required: ["engine"]
        },
        outputSchema: { type: "array" },
        cliCommand: {
          name: "config history",
          aliases: ["chist"],
          examples: ["config history governor"]
        },
        ui: { component: "action-button", position: "admin", order: 4 },
        mcpToolName: "config_history",
        apiEndpoint: { method: "GET", path: "/api/admin/config/{engine}/history" }
      },
      async (input) => {
        const engine = String(input.engine ?? "");
        const entry = await services2.db.prisma.configEntry.findUnique({
          where: {
            engineId_scopeType_scopeId: { engineId: engine, scopeType: "engine", scopeId: "" }
          }
        });
        return entry ? [{ engineId: engine, configJson: entry.configJson }] : [];
      }
    ),
    // ── Admin audit (24.4) ──────────────────────────────────────
    makeCapability(
      {
        id: "cap:admin:audit",
        slug: "admin_audit",
        name: "Audit Provider",
        description: "Run a drift/audit check against a provider (kernel oracle).",
        category: "admin",
        inputSchema: {
          type: "object",
          properties: { providerId: { type: "string" } },
          required: ["providerId"]
        },
        outputSchema: { type: "object" },
        cliCommand: { name: "admin audit", aliases: ["aaud"], examples: ["admin audit claude"] },
        ui: { component: "action-button", position: "admin", order: 5 },
        mcpToolName: "admin_audit",
        apiEndpoint: { method: "GET", path: "/api/admin/audit/{providerId}" }
      },
      async (input) => {
        return {
          providerId: String(input.providerId),
          note: "audit requires kernel oracle \u2014 run via /api/capabilities/oracle_* once kernel caps are wired"
        };
      }
    ),
    // ── Admin drift (24.4) ──────────────────────────────────────
    makeCapability(
      {
        id: "cap:admin:drift",
        slug: "admin_drift",
        name: "Config Drift",
        description: "Report config drift across the system (kernel oracle).",
        category: "admin",
        inputSchema: { type: "object", properties: { providerId: { type: "string" } } },
        outputSchema: { type: "object" },
        cliCommand: { name: "admin drift", aliases: ["adri"], examples: ["admin drift"] },
        ui: { component: "action-button", position: "admin", order: 6 },
        mcpToolName: "admin_drift",
        apiEndpoint: { method: "GET", path: "/api/admin/drift" }
      },
      async (input) => {
        const providerId = input.providerId ? String(input.providerId) : void 0;
        return {
          providerId,
          note: "drift requires kernel oracle \u2014 run via /api/capabilities/oracle_* once kernel caps are wired"
        };
      }
    ),
    // ── Telemetry (24.4) ────────────────────────────────────────
    makeCapability(
      {
        id: "cap:telemetry:summary",
        slug: "telemetry_summary",
        name: "Telemetry Summary",
        description: "Aggregate telemetry summary for a provider over a date range.",
        category: "telemetry",
        inputSchema: {
          type: "object",
          properties: {
            providerId: { type: "string" },
            from: { type: "string" },
            to: { type: "string" }
          },
          required: ["providerId", "from", "to"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "telemetry summary",
          aliases: ["tsum"],
          examples: ["telemetry summary claude --from 2024-01-01 --to 2024-01-31"]
        },
        ui: { component: "action-button", position: "admin", order: 7 },
        mcpToolName: "telemetry_summary",
        apiEndpoint: { method: "GET", path: "/api/telemetry/{providerId}/summary" }
      },
      async (input) => {
        return {
          providerId: String(input.providerId),
          from: String(input.from),
          to: String(input.to),
          note: "telemetry aggregation pending \u2014 wire TelemetryEngine store"
        };
      }
    ),
    makeCapability(
      {
        id: "cap:telemetry:compare",
        slug: "telemetry_compare",
        name: "Telemetry Compare",
        description: "Compare telemetry across two date ranges.",
        category: "telemetry",
        inputSchema: {
          type: "object",
          properties: { from: { type: "string" }, to: { type: "string" } },
          required: ["from", "to"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "telemetry compare",
          aliases: ["tcmp"],
          examples: ["telemetry compare --from 2024-01-01 --to 2024-01-31"]
        },
        ui: { component: "action-button", position: "admin", order: 8 },
        mcpToolName: "telemetry_compare",
        apiEndpoint: { method: "GET", path: "/api/telemetry/compare" }
      },
      async (input) => {
        return {
          from: String(input.from),
          to: String(input.to),
          note: "telemetry aggregation pending \u2014 wire TelemetryEngine store"
        };
      }
    )
  ];
  for (const cap of defaults) {
    registry2.register(cap);
  }
}
function registerNlInterpretCapability(registry2, nlclEngine) {
  const handler = async (input, capCtx) => {
    const text = String(input.text ?? "");
    const extra = input.ctx && typeof input.ctx === "object" ? input.ctx : {};
    const nlCtx = {
      surface: "api",
      providerId: extra.providerId ?? capCtx.providerId,
      accountId: extra.accountId,
      conversationId: extra.conversationId ?? capCtx.conversationId,
      slaveId: extra.slaveId ?? capCtx.slaveId,
      userId: extra.userId ?? capCtx.userId,
      metadata: { ...extra.metadata, ...capCtx.metadata }
    };
    return nlclEngine.interpret(text, nlCtx);
  };
  registry2.register(
    makeCapability(
      {
        id: "cap:nlcl:interpret",
        slug: "nl_interpret",
        name: "Interpret Natural Language",
        description: "Resolve natural language to a capability chain (self-referential NL parsing).",
        category: "nlcl",
        inputSchema: {
          type: "object",
          properties: { text: { type: "string" }, ctx: { type: "object" } },
          required: ["text"]
        },
        outputSchema: { type: "object" },
        cliCommand: { name: "nl", aliases: ["interpret"], examples: ['nl "list providers"'] },
        ui: { component: "composer", position: "composer", order: 0 },
        mcpToolName: "nl_interpret",
        apiEndpoint: { method: "POST", path: "/api/interpret" }
      },
      handler
    )
  );
}
var ALL_SURFACES;
var init_capability_bootstrap = __esm({
  "src/engines/capability-bootstrap.ts"() {
    "use strict";
    init_discovery_stack();
    init_composer_typing();
    init_discovery_session_runner();
    ALL_SURFACES = ["cli", "ui", "workflow", "mcp", "api"];
  }
});

// src/engines/capability-bootstrap-generated.ts
import { readFileSync as readFileSync2 } from "fs";
import { join as join7 } from "path";
function loadPool() {
  const poolPath = join7(
    import.meta.dir,
    "..",
    "..",
    "..",
    "seeds",
    "taxonomy",
    "pool.taxonomy.json"
  );
  try {
    const raw = JSON.parse(readFileSync2(poolPath, "utf-8"));
    return raw.nodes.filter((n) => n.kind === "capability").map((n) => n);
  } catch {
    console.warn("[bootstrap-generated] pool.taxonomy.json not found, returning empty set");
    return [];
  }
}
function createHandlerMap(services2) {
  return {
    // ── Conversation ──
    conversation_list: async (input) => services2.conversationStore.listConversations({
      providerId: input.providerId ? String(input.providerId) : void 0,
      limit: 100
    }),
    conversation_create: async (input) => {
      const conv = await services2.conversationStore.createConversation({
        providerSessionId: String(input.providerId ?? ""),
        providerId: String(input.providerId ?? ""),
        title: input.title ? String(input.title) : null
      });
      return { id: conv.id };
    },
    conversation_send: async (input) => {
      const result = await services2.conversationManager.send(
        String(input.conversationId ?? ""),
        String(input.message ?? "")
      );
      return { ok: result.ok, text: result.text ?? null, error: result.error ?? null };
    },
    conversation_delete: async (input) => {
      await services2.db.prisma.conversation.delete({
        where: { id: String(input.conversationId ?? "") }
      });
      return { ok: true };
    },
    // ── Knowledge ──
    knowledge_search: async () => [],
    knowledge_ingest: async () => ({ jobId: "pending" }),
    knowledge_synthesize: async () => ({ answer: "", sources: [], confidence: 0 }),
    // ── Memory ──
    memory_query: async () => ({ results: [] }),
    memory_assert: async () => ({ ok: true }),
    memory_forget: async () => ({ ok: true }),
    // ── Admin ──
    admin_seed: async () => ({ ok: true }),
    config_get: async (input) => {
      const engine = String(input.engine ?? "");
      const config2 = await services2.db.getConfig(engine);
      return { engine, config: config2 ?? {} };
    },
    config_set: async (input) => {
      const engine = String(input.engine ?? "");
      const patch = JSON.stringify(input.patch ?? {});
      await services2.db.setConfig(engine, patch);
      return { ok: true };
    },
    // ── Health ──
    health_check: async () => ({ ok: true, timestamp: Date.now() }),
    system_status: async () => ({ ok: true })
  };
}
function createFallbackHandler(slug) {
  return async () => {
    throw new CapabilityNotFoundError(slug);
  };
}
function registerGeneratedCapabilities(registry2, services2) {
  const pool = loadPool();
  const handlerMap = createHandlerMap(services2);
  let registered = 0;
  let skipped = 0;
  for (const node of pool) {
    const handler = handlerMap[node.slug] ?? createFallbackHandler(node.slug);
    const ui = node.ui_component ? {
      component: node.ui_component,
      position: node.ui_position ?? "sidebar",
      group: node.ui_group ?? void 0,
      order: node.ui_order ?? 100
    } : void 0;
    const partial = {
      id: node.id,
      slug: node.slug,
      name: node.label,
      description: node.description,
      category: node.category,
      surfaces: node.surfaces,
      inputSchema: node.inputSchema ?? { type: "object", properties: {} },
      outputSchema: node.outputSchema ?? { type: "object" },
      cliCommand: node.cliCommand ?? void 0,
      ui,
      uiAction: node.uiAction ?? void 0,
      mcpToolName: node.mcpToolName ?? void 0,
      apiEndpoint: node.apiEndpoint ?? void 0,
      workflowNodeType: node.workflowNodeType ?? void 0,
      isAsync: node.isAsync,
      requiresConfirmation: node.requiresConfirmation,
      tags: []
    };
    try {
      registry2.register(makeCapability(partial, handler));
      registered++;
    } catch (_err) {
      skipped++;
    }
  }
  console.log(
    `[bootstrap-generated] Registered ${registered} capabilities from taxonomy pool${skipped > 0 ? ` (${skipped} skipped)` : ""}`
  );
}
var init_capability_bootstrap_generated = __esm({
  "src/engines/capability-bootstrap-generated.ts"() {
    "use strict";
    init_errors();
    init_capability_bootstrap();
  }
});

// src/engines/cdp-discovery.ts
function discoverCdpMethods(source) {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const m of source) {
    if (seen.has(m.fullName)) continue;
    seen.add(m.fullName);
    out.push(m);
  }
  return out;
}
var CDP_PROTOCOL_CATALOG;
var init_cdp_discovery = __esm({
  "src/engines/cdp-discovery.ts"() {
    "use strict";
    init_errors();
    CDP_PROTOCOL_CATALOG = [
      // Runtime
      {
        domain: "Runtime",
        method: "enable",
        fullName: "Runtime.enable",
        description: "Enables reporting of execution contexts creation.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "disable",
        fullName: "Runtime.disable",
        description: "Disables reporting of execution contexts creation.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "evaluate",
        fullName: "Runtime.evaluate",
        description: "Evaluates expression on global object.",
        parameters: [
          {
            name: "expression",
            type: "string",
            optional: false,
            description: "Expression to evaluate."
          },
          {
            name: "returnByValue",
            type: "boolean",
            optional: true,
            description: "Whether the result is expected to be a JSON object."
          },
          {
            name: "awaitPromise",
            type: "boolean",
            optional: true,
            description: "Whether execution should await for resulting promise."
          }
        ],
        returns: [
          { name: "result", type: "RemoteObject", optional: false, description: "Evaluation result." }
        ],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "callFunctionOn",
        fullName: "Runtime.callFunctionOn",
        description: "Calls function with given declaration on given object.",
        parameters: [
          {
            name: "objectId",
            type: "string",
            optional: false,
            description: "Identifier of the object to call function on."
          },
          {
            name: "functionDeclaration",
            type: "string",
            optional: false,
            description: "Declaration of the function to call."
          }
        ],
        returns: [
          { name: "result", type: "RemoteObject", optional: false, description: "Call result." }
        ],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "getProperties",
        fullName: "Runtime.getProperties",
        description: "Returns properties of a given object.",
        parameters: [
          {
            name: "objectId",
            type: "string",
            optional: false,
            description: "Id of the object to return properties for."
          }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "awaitPromise",
        fullName: "Runtime.awaitPromise",
        description: "Awaits promise to be resolved.",
        parameters: [
          {
            name: "promiseObjectId",
            type: "string",
            optional: false,
            description: "Id of the promise."
          }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Runtime",
        method: "globalLexicalScopeNames",
        fullName: "Runtime.globalLexicalScopeNames",
        description: "Returns names of all registered global lexical scopes.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      // Page
      {
        domain: "Page",
        method: "enable",
        fullName: "Page.enable",
        description: "Enables page domain notifications.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Page",
        method: "disable",
        fullName: "Page.disable",
        description: "Disables page domain notifications.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Page",
        method: "navigate",
        fullName: "Page.navigate",
        description: "Navigates current page to given URL.",
        parameters: [
          { name: "url", type: "string", optional: false, description: "URL to navigate to." }
        ],
        returns: [
          {
            name: "frameId",
            type: "string",
            optional: false,
            description: "Id of the frame that was navigated."
          }
        ],
        deprecated: false
      },
      {
        domain: "Page",
        method: "reload",
        fullName: "Page.reload",
        description: "Reloads the page.",
        parameters: [
          {
            name: "ignoreCache",
            type: "boolean",
            optional: true,
            description: "If true, the cache is ignored."
          }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Page",
        method: "close",
        fullName: "Page.close",
        description: "Closes the page.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Page",
        method: "captureScreenshot",
        fullName: "Page.captureScreenshot",
        description: "Captures a screenshot of the page.",
        parameters: [
          {
            name: "format",
            type: "string",
            optional: true,
            description: "Image compression format (png, jpeg)."
          }
        ],
        returns: [
          { name: "data", type: "string", optional: false, description: "Base64-encoded image data." }
        ],
        deprecated: false
      },
      {
        domain: "Page",
        method: "handleJavaScriptDialog",
        fullName: "Page.handleJavaScriptDialog",
        description: "Accepts or dismisses a JS dialog.",
        parameters: [
          {
            name: "accept",
            type: "boolean",
            optional: false,
            description: "Whether to accept the dialog."
          }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Page",
        method: "crash",
        fullName: "Page.crash",
        description: "Crashes the renderer.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      // DOM
      {
        domain: "DOM",
        method: "enable",
        fullName: "DOM.enable",
        description: "Enables DOM agent for the given page.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "disable",
        fullName: "DOM.disable",
        description: "Disables DOM agent for the given page.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "getDocument",
        fullName: "DOM.getDocument",
        description: "Returns the root DOM node.",
        parameters: [
          { name: "depth", type: "number", optional: true, description: "Maximum depth to traverse." }
        ],
        returns: [{ name: "root", type: "Node", optional: false, description: "Root node." }],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "querySelector",
        fullName: "DOM.querySelector",
        description: "Returns node id for a given selector.",
        parameters: [
          { name: "nodeId", type: "number", optional: false, description: "Id of the node to query." },
          { name: "selector", type: "string", optional: false, description: "CSS selector." }
        ],
        returns: [{ name: "nodeId", type: "number", optional: false, description: "Matched node id." }],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "getBoxModel",
        fullName: "DOM.getBoxModel",
        description: "Returns box model for a node.",
        parameters: [{ name: "nodeId", type: "number", optional: true, description: "Node id." }],
        returns: [],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "setAttributeValue",
        fullName: "DOM.setAttributeValue",
        description: "Sets attribute for an element.",
        parameters: [
          { name: "nodeId", type: "number", optional: false, description: "Node id." },
          { name: "name", type: "string", optional: false, description: "Attribute name." },
          { name: "value", type: "string", optional: false, description: "Attribute value." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "removeNode",
        fullName: "DOM.removeNode",
        description: "Removes a node.",
        parameters: [{ name: "nodeId", type: "number", optional: false, description: "Node id." }],
        returns: [],
        deprecated: false
      },
      {
        domain: "DOM",
        method: "getOuterHTML",
        fullName: "DOM.getOuterHTML",
        description: "Returns the outer HTML of a node.",
        parameters: [{ name: "nodeId", type: "number", optional: false, description: "Node id." }],
        returns: [{ name: "outerHTML", type: "string", optional: false, description: "Outer HTML." }],
        deprecated: false
      },
      // Network
      {
        domain: "Network",
        method: "enable",
        fullName: "Network.enable",
        description: "Enables network tracking.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Network",
        method: "disable",
        fullName: "Network.disable",
        description: "Disables network tracking.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Network",
        method: "setBlockedURLs",
        fullName: "Network.setBlockedURLs",
        description: "Blocks URLs from loading.",
        parameters: [
          { name: "urls", type: "array", optional: false, description: "URL patterns to block." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Network",
        method: "getCookies",
        fullName: "Network.getCookies",
        description: "Returns cookies for the page.",
        parameters: [],
        returns: [{ name: "cookies", type: "array", optional: false, description: "Cookie array." }],
        deprecated: false
      },
      {
        domain: "Network",
        method: "getResponseBody",
        fullName: "Network.getResponseBody",
        description: "Returns response body for a request.",
        parameters: [
          { name: "requestId", type: "string", optional: false, description: "Request id." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Network",
        method: "setUserAgentOverride",
        fullName: "Network.setUserAgentOverride",
        description: "Overrides user agent.",
        parameters: [
          { name: "userAgent", type: "string", optional: false, description: "User agent string." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Network",
        method: "clearBrowserCache",
        fullName: "Network.clearBrowserCache",
        description: "Clears browser cache.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      // Target
      {
        domain: "Target",
        method: "createTarget",
        fullName: "Target.createTarget",
        description: "Creates a new target.",
        parameters: [{ name: "url", type: "string", optional: false, description: "Initial URL." }],
        returns: [{ name: "targetId", type: "string", optional: false, description: "Target id." }],
        deprecated: false
      },
      {
        domain: "Target",
        method: "closeTarget",
        fullName: "Target.closeTarget",
        description: "Closes target by id.",
        parameters: [{ name: "targetId", type: "string", optional: false, description: "Target id." }],
        returns: [],
        deprecated: false
      },
      {
        domain: "Target",
        method: "getTargets",
        fullName: "Target.getTargets",
        description: "Retrieves targets available.",
        parameters: [],
        returns: [
          { name: "targetInfos", type: "array", optional: false, description: "Target infos." }
        ],
        deprecated: false
      },
      {
        domain: "Target",
        method: "attachToTarget",
        fullName: "Target.attachToTarget",
        description: "Attaches to a target.",
        parameters: [{ name: "targetId", type: "string", optional: false, description: "Target id." }],
        returns: [],
        deprecated: false
      },
      // Input
      {
        domain: "Input",
        method: "dispatchKeyEvent",
        fullName: "Input.dispatchKeyEvent",
        description: "Dispatches key event.",
        parameters: [
          { name: "type", type: "string", optional: false, description: "Event type." },
          { name: "key", type: "string", optional: true, description: "Key name." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Input",
        method: "dispatchMouseEvent",
        fullName: "Input.dispatchMouseEvent",
        description: "Dispatches mouse event.",
        parameters: [
          { name: "type", type: "string", optional: false, description: "Event type." },
          { name: "x", type: "number", optional: true, description: "X coordinate." },
          { name: "y", type: "number", optional: true, description: "Y coordinate." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Input",
        method: "insertText",
        fullName: "Input.insertText",
        description: "Inserts text.",
        parameters: [{ name: "text", type: "string", optional: false, description: "Text to insert." }],
        returns: [],
        deprecated: false
      },
      // Fetch
      {
        domain: "Fetch",
        method: "enable",
        fullName: "Fetch.enable",
        description: "Enables interception of network requests.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Fetch",
        method: "disable",
        fullName: "Fetch.disable",
        description: "Disables interception of network requests.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Fetch",
        method: "fulfillRequest",
        fullName: "Fetch.fulfillRequest",
        description: "Fulfills a request with given body.",
        parameters: [
          { name: "requestId", type: "string", optional: false, description: "Request id." },
          { name: "body", type: "string", optional: false, description: "Response body." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Fetch",
        method: "failRequest",
        fullName: "Fetch.failRequest",
        description: "Causes the request to fail.",
        parameters: [
          { name: "requestId", type: "string", optional: false, description: "Request id." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Fetch",
        method: "continueRequest",
        fullName: "Fetch.continueRequest",
        description: "Continues the request.",
        parameters: [
          { name: "requestId", type: "string", optional: false, description: "Request id." }
        ],
        returns: [],
        deprecated: false
      },
      // Emulation
      {
        domain: "Emulation",
        method: "setDeviceMetricsOverride",
        fullName: "Emulation.setDeviceMetricsOverride",
        description: "Overrides device metrics.",
        parameters: [
          { name: "width", type: "number", optional: false, description: "Width." },
          { name: "height", type: "number", optional: false, description: "Height." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Emulation",
        method: "setUserAgentOverride",
        fullName: "Emulation.setUserAgentOverride",
        description: "Overrides user agent.",
        parameters: [
          { name: "userAgent", type: "string", optional: false, description: "User agent." }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Emulation",
        method: "setGeolocationOverride",
        fullName: "Emulation.setGeolocationOverride",
        description: "Overrides geolocation.",
        parameters: [
          { name: "latitude", type: "number", optional: true, description: "Latitude." },
          { name: "longitude", type: "number", optional: true, description: "Longitude." }
        ],
        returns: [],
        deprecated: false
      },
      // Tracing
      {
        domain: "Tracing",
        method: "start",
        fullName: "Tracing.start",
        description: "Starts tracing.",
        parameters: [
          {
            name: "categories",
            type: "string",
            optional: true,
            description: "Comma-separated tracing categories."
          }
        ],
        returns: [],
        deprecated: false
      },
      {
        domain: "Tracing",
        method: "end",
        fullName: "Tracing.end",
        description: "Stops tracing.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      // Debugger
      {
        domain: "Debugger",
        method: "enable",
        fullName: "Debugger.enable",
        description: "Enables debugger.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Debugger",
        method: "disable",
        fullName: "Debugger.disable",
        description: "Disables debugger.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Debugger",
        method: "pause",
        fullName: "Debugger.pause",
        description: "Stops on the next JS statement.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Debugger",
        method: "resume",
        fullName: "Debugger.resume",
        description: "Resumes execution.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      // CSS
      {
        domain: "CSS",
        method: "enable",
        fullName: "CSS.enable",
        description: "Enables CSS agent.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "CSS",
        method: "disable",
        fullName: "CSS.disable",
        description: "Disables CSS agent.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "CSS",
        method: "getComputedStyleForNode",
        fullName: "CSS.getComputedStyleForNode",
        description: "Returns computed styles for a node.",
        parameters: [{ name: "nodeId", type: "number", optional: false, description: "Node id." }],
        returns: [],
        deprecated: false
      },
      // Log
      {
        domain: "Log",
        method: "enable",
        fullName: "Log.enable",
        description: "Enables log domain.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Log",
        method: "disable",
        fullName: "Log.disable",
        description: "Disables log domain.",
        parameters: [],
        returns: [],
        deprecated: false
      },
      {
        domain: "Log",
        method: "clear",
        fullName: "Log.clear",
        description: "Clears log.",
        parameters: [],
        returns: [],
        deprecated: false
      }
    ];
  }
});

// src/engines/harness/make-harness-capability.ts
var init_make_harness_capability = __esm({
  "src/engines/harness/make-harness-capability.ts"() {
    "use strict";
  }
});

// src/engines/cdp-capability-registrar.ts
function kebab(s) {
  return s.replace(/[^a-zA-Z0-9]+/g, "-").replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase().replace(/-+/g, "-").replace(/^-|-$/g, "");
}
function cdpMethodToCapability(desc, opts) {
  const slug = `cdp-${kebab(desc.fullName)}`;
  const properties = {};
  const required = [];
  for (const p of desc.parameters) {
    properties[p.name] = { type: p.type === "any" ? "string" : p.type, description: p.description };
    if (!p.optional) required.push(p.name);
  }
  const destructive = DESTRUCTIVE_FULL.has(desc.fullName);
  return {
    id: `cap:cdp:${desc.fullName}`,
    slug,
    name: desc.fullName,
    description: desc.description || `CDP command ${desc.fullName}`,
    category: "cdp",
    // Provider scope is carried on tags so the binding lookup stays stateless.
    surfaces: ["cli", "ui", "api", "mcp", "workflow"],
    inputSchema: { type: "object", properties, required },
    outputSchema: { type: "object" },
    handler: async (input, ctx) => opts.executeCdp(desc.fullName, input, ctx),
    cliCommand: {
      name: `cdp ${desc.domain} ${desc.method}`,
      aliases: [slug],
      examples: [`cdp ${desc.domain} ${desc.method}`]
    },
    ui: {
      component: "action-button",
      position: "composer",
      group: "cdp",
      order: 100,
      requiresConfirmation: destructive
    },
    mcpToolName: slug,
    apiEndpoint: { method: "POST", path: `/api/cdp/${slug}` },
    isAsync: true,
    requiresConfirmation: destructive,
    tags: [
      "cdp",
      "discovered",
      desc.domain,
      ...opts.providerId ? [`provider:${opts.providerId}`] : []
    ]
  };
}
function bindingStatusFor(opts) {
  if (opts.verified) return { status: "active", confidence: opts.verified.confidence };
  return { status: "prospect", confidence: 0 };
}
function registerDiscoveredCdpMethods(registry2, descs, opts) {
  const registered = [];
  const skipped = [];
  const bound = [];
  const { status, confidence } = bindingStatusFor(opts);
  for (const desc of discoverCdpMethods(descs)) {
    try {
      registry2.register(cdpMethodToCapability(desc, opts));
      registered.push(desc.fullName);
      if (opts.providerId && opts.bindingStore) {
        try {
          opts.bindingStore.ensureCdpBinding({
            capabilityId: `cap:cdp:${desc.fullName}`,
            providerId: opts.providerId,
            status,
            confidence,
            reason: opts.verified ? "d2-live-protocol-verified" : "d2-pending-verification"
          }).catch(() => {
          });
          bound.push(desc.fullName);
        } catch {
        }
      }
    } catch {
      skipped.push(desc.fullName);
    }
  }
  return { registered, skipped, bound };
}
var DESTRUCTIVE_FULL;
var init_cdp_capability_registrar = __esm({
  "src/engines/cdp-capability-registrar.ts"() {
    "use strict";
    init_cdp_discovery();
    init_make_harness_capability();
    init_program_schema();
    DESTRUCTIVE_FULL = /* @__PURE__ */ new Set([
      "Page.navigate",
      "Page.reload",
      "Page.close",
      "Page.crash",
      "DOM.setAttributeValue",
      "DOM.removeNode",
      "Network.setBlockedURLs",
      "Target.closeTarget",
      "Input.dispatchKeyEvent",
      "Input.dispatchMouseEvent",
      "Input.insertText",
      "Fetch.fulfillRequest",
      "Fetch.failRequest",
      "Tracing.start",
      "Emulation.setDeviceMetricsOverride",
      "Emulation.setGeolocationOverride",
      "Debugger.pause"
    ]);
  }
});

// src/engines/config-manager.ts
var ConfigManager;
var init_config_manager = __esm({
  "src/engines/config-manager.ts"() {
    "use strict";
    init_errors();
    ConfigManager = class {
      constructor(store, eventBus) {
        this.store = store;
        this.eventBus = eventBus;
      }
      store;
      eventBus;
      schemas = /* @__PURE__ */ new Map();
      cache = /* @__PURE__ */ new Map();
      // ── Schema registration ────────────────────────────────────────────────
      registerSchema(engineId, schema, defaults) {
        schema.parse(defaults);
        this.schemas.set(engineId, { schema, defaults });
      }
      // ── Read ───────────────────────────────────────────────────────────────
      getConfig(engineId, scope) {
        const { scopeType, scopeId } = this.resolveScope(scope);
        const cacheKey = `${engineId}:${scopeType}:${scopeId ?? "null"}`;
        const cached = this.cache.get(cacheKey);
        if (cached) return cached;
        const registration = this.schemas.get(engineId);
        if (!registration) {
          throw new EngineError(`No schema registered for engine: ${engineId}`);
        }
        return registration.defaults;
      }
      // ── Write ──────────────────────────────────────────────────────────────
      async updateConfig(engineId, patch, actor, scope) {
        const { scopeType, scopeId } = this.resolveScope(scope);
        const registration = this.schemas.get(engineId);
        if (!registration) {
          throw new EngineError(`No schema registered for engine: ${engineId}`);
        }
        const current = this.getConfig(engineId, scope);
        const merged = { ...current, ...patch };
        registration.schema.parse(merged);
        const configJson = JSON.stringify(merged);
        const entry = await this.store.upsertConfigEntry(engineId, scopeType, scopeId, configJson, 1);
        await this.store.insertConfigAudit({
          engineId,
          entryId: entry.id,
          action: "update",
          fromJson: JSON.stringify(current),
          toJson: configJson,
          actor,
          ts: Date.now()
        });
        const cacheKey = `${engineId}:${scopeType}:${scopeId ?? "null"}`;
        this.cache.set(cacheKey, merged);
        this.eventBus?.emit("config:changed", { engineId, scopeType, scopeId });
        return merged;
      }
      // ── Audit ──────────────────────────────────────────────────────────────
      async getConfigHistory(engineId, limit = 50) {
        return this.store.getConfigAuditHistory(engineId, limit);
      }
      // ── Reload ─────────────────────────────────────────────────────────────
      async reloadConfig(engineId, scope) {
        const { scopeType, scopeId } = this.resolveScope(scope);
        const cacheKey = `${engineId}:${scopeType}:${scopeId ?? "null"}`;
        const entry = await this.store.getConfigEntry(engineId, scopeType, scopeId);
        if (entry) {
          const parsed = JSON.parse(entry.configJson);
          this.cache.set(cacheKey, parsed);
          return parsed;
        }
        this.cache.delete(cacheKey);
        return this.getConfig(engineId, scope);
      }
      // ── Helpers ────────────────────────────────────────────────────────────
      resolveScope(scope) {
        return {
          scopeType: scope?.scopeType ?? "global",
          scopeId: scope?.scopeId ?? null
        };
      }
    };
  }
});

// src/engines/kernel/kernel-context.ts
function createKernel(deps) {
  const logger = deps.logger ?? new ConsoleKernelLogger();
  deps.registry.registerEngine({
    id: "kernel",
    kind: "engine",
    layer: "kernel",
    dependencies: [],
    status: "registered",
    config: {},
    metadata: { description: "VIVIM Kernel \u2014 self-understanding layer" }
  });
  const ctx = {
    kernel: null,
    registry: deps.registry,
    tracer: deps.tracer,
    provenance: deps.provenance,
    eventBus: deps.eventBus,
    config: deps.config,
    store: deps.store ?? null,
    logger,
    oracle: null
  };
  const kernel = new KernelImpl(ctx);
  ctx.kernel = kernel;
  return kernel;
}
var ConsoleKernelLogger, KernelImpl;
var init_kernel_context = __esm({
  "src/engines/kernel/kernel-context.ts"() {
    "use strict";
    ConsoleKernelLogger = class _ConsoleKernelLogger {
      prefix;
      fields;
      constructor(prefix = "kernel", fields = {}) {
        this.prefix = prefix;
        this.fields = fields;
      }
      info(msg, data) {
        console.log(`[${this.prefix}] ${msg}`, { ...this.fields, ...data });
      }
      warn(msg, data) {
        console.warn(`[${this.prefix}] ${msg}`, { ...this.fields, ...data });
      }
      error(msg, data) {
        console.error(`[${this.prefix}] ${msg}`, { ...this.fields, ...data });
      }
      debug(msg, data) {
        if (process.env.DEBUG) {
          console.debug(`[${this.prefix}] ${msg}`, { ...this.fields, ...data });
        }
      }
      child(prefix) {
        return new _ConsoleKernelLogger(`${this.prefix}:${prefix}`, this.fields);
      }
      withFields(fields) {
        return new _ConsoleKernelLogger(this.prefix, { ...this.fields, ...fields });
      }
    };
    KernelImpl = class {
      ctx;
      started = false;
      constructor(ctx) {
        this.ctx = ctx;
      }
      context() {
        return this.ctx;
      }
      async start() {
        if (this.started) return;
        this.ctx.logger.info("kernel starting");
        this.ctx.registry.markRunning("kernel");
        this.started = true;
        this.ctx.logger.info("kernel started", {
          engines: this.ctx.registry.describe().engines.length
        });
      }
      async stop() {
        if (!this.started) return;
        this.ctx.logger.info("kernel stopping");
        this.ctx.registry.markStopped("kernel");
        this.started = false;
        this.ctx.logger.info("kernel stopped");
      }
      snapshot() {
        return this.ctx.registry.describe();
      }
    };
  }
});

// src/engines/kernel/kernel-provenance.ts
var KernelProvenance;
var init_kernel_provenance = __esm({
  "src/engines/kernel/kernel-provenance.ts"() {
    "use strict";
    init_ids();
    KernelProvenance = class {
      buffer = [];
      bufferCapacity;
      store;
      constructor(opts) {
        this.bufferCapacity = opts?.bufferCapacity ?? 1e3;
        this.store = opts?.store ?? null;
      }
      setStore(store) {
        this.store = store;
      }
      async record(node) {
        const fullNode = {
          ...node,
          id: newId(),
          timestamp: Date.now()
        };
        this.buffer.push(fullNode);
        if (this.buffer.length > this.bufferCapacity) {
          this.buffer = this.buffer.slice(-Math.floor(this.bufferCapacity / 2));
        }
        if (this.store) {
          try {
            await this.store.insertProvenanceNode({
              traceId: fullNode.traceId,
              parentId: fullNode.parentId,
              kind: fullNode.kind,
              engineId: fullNode.engineId,
              description: fullNode.description,
              input: fullNode.input,
              output: fullNode.output,
              duration: fullNode.duration
            });
          } catch (err) {
            console.error("[provenance] persist failed", err);
          }
        }
        return fullNode.id;
      }
      getChain(traceId) {
        const nodes = this.buffer.filter((n) => n.traceId === traceId).sort((a, b) => a.timestamp - b.timestamp);
        const rootCause = nodes.find((n) => n.kind === "error") ?? nodes[0];
        const totalDuration = nodes.reduce((sum, n) => sum + (n.duration ?? 0), 0);
        return { traceId, nodes, rootCause, totalDuration };
      }
      getByEngine(engineId, limit = 50) {
        return this.buffer.filter((n) => n.engineId === engineId).slice(-limit);
      }
      getByKind(kind, limit = 50) {
        return this.buffer.filter((n) => n.kind === kind).slice(-limit);
      }
      getRecent(limit = 100) {
        return this.buffer.slice(-limit);
      }
      clear() {
        this.buffer = [];
      }
      size() {
        return this.buffer.length;
      }
    };
  }
});

// src/engines/kernel/kernel-registry.ts
var KernelRegistry;
var init_kernel_registry = __esm({
  "src/engines/kernel/kernel-registry.ts"() {
    "use strict";
    init_errors();
    KernelRegistry = class {
      engines = /* @__PURE__ */ new Map();
      stores = /* @__PURE__ */ new Map();
      capabilities = /* @__PURE__ */ new Map();
      routes = /* @__PURE__ */ new Map();
      registerCallbacks = /* @__PURE__ */ new Set();
      statusChangeCallbacks = /* @__PURE__ */ new Set();
      registerEngine(desc) {
        const now = Date.now();
        const entry = {
          ...desc,
          registeredAt: now,
          updatedAt: now
        };
        this.engines.set(desc.id, entry);
        for (const cb of this.registerCallbacks) {
          try {
            cb(entry);
          } catch {
          }
        }
      }
      registerStore(desc) {
        const now = Date.now();
        const entry = { ...desc, registeredAt: now, updatedAt: now };
        this.stores.set(desc.id, entry);
      }
      registerCapability(desc) {
        const now = Date.now();
        const entry = { ...desc, registeredAt: now, updatedAt: now };
        this.capabilities.set(desc.id, entry);
      }
      registerRoute(desc) {
        const now = Date.now();
        const entry = { ...desc, registeredAt: now, updatedAt: now };
        this.routes.set(desc.id, entry);
      }
      describe() {
        return {
          engines: [...this.engines.values()],
          stores: [...this.stores.values()],
          capabilities: [...this.capabilities.values()],
          routes: [...this.routes.values()],
          timestamp: Date.now()
        };
      }
      listEngines(filter) {
        let result = [...this.engines.values()];
        if (filter?.layer) result = result.filter((e) => e.layer === filter.layer);
        if (filter?.kind) result = result.filter((e) => e.kind === filter.kind);
        if (filter?.status) result = result.filter((e) => e.status === filter.status);
        return result;
      }
      getEngine(id) {
        return this.engines.get(id) ?? null;
      }
      getDependencies(id) {
        const desc = this.engines.get(id);
        if (!desc) return [];
        const deps = [];
        const visited = /* @__PURE__ */ new Set();
        const queue = [...desc.dependencies];
        for (const depId of queue) {
          if (visited.has(depId)) continue;
          visited.add(depId);
          deps.push(depId);
          const dep = this.engines.get(depId);
          if (dep) queue.push(...dep.dependencies);
        }
        return deps;
      }
      dependentsOf(id) {
        const result = [];
        for (const [, desc] of this.engines) {
          if (desc.dependencies.includes(id)) {
            result.push(desc.id);
          }
        }
        return result;
      }
      updateHealth(id, health) {
        const desc = this.engines.get(id);
        if (!desc) throw new EngineError(`Engine ${id} not found in registry`);
        const _from = desc.status;
        desc.health = health;
        desc.updatedAt = Date.now();
        if (health.status === "healthy") {
          this.markRunning(id);
        } else if (health.status === "unhealthy") {
          this.markError(id, health.details ? JSON.stringify(health.details) : "unhealthy");
        }
      }
      markWired(id) {
        this.updateStatus(id, "wired");
      }
      markRunning(id) {
        this.updateStatus(id, "running");
      }
      markError(id, error2) {
        const desc = this.engines.get(id);
        if (desc) desc.metadata = { ...desc.metadata, error: error2 };
        this.updateStatus(id, "error");
      }
      markStopped(id) {
        this.updateStatus(id, "stopped");
      }
      onRegister(callback) {
        this.registerCallbacks.add(callback);
        return () => {
          this.registerCallbacks.delete(callback);
        };
      }
      onStatusChange(callback) {
        this.statusChangeCallbacks.add(callback);
        return () => {
          this.statusChangeCallbacks.delete(callback);
        };
      }
      updateStatus(id, to) {
        const desc = this.engines.get(id);
        if (!desc) return;
        const from = desc.status;
        desc.status = to;
        desc.updatedAt = Date.now();
        for (const cb of this.statusChangeCallbacks) {
          try {
            cb(id, from, to);
          } catch {
          }
        }
      }
    };
  }
});

// src/engines/kernel/kernel-tracer.ts
var KernelTracer;
var init_kernel_tracer = __esm({
  "src/engines/kernel/kernel-tracer.ts"() {
    "use strict";
    init_errors();
    init_ids();
    KernelTracer = class {
      ringBuffer = [];
      ringCapacity;
      activeSpans = /* @__PURE__ */ new Map();
      store = null;
      persistThreshold;
      constructor(opts) {
        this.ringCapacity = opts?.ringCapacity ?? 500;
        this.persistThreshold = opts?.persistThreshold ?? 450;
        this.store = opts?.store ?? null;
      }
      newTraceId() {
        return ulid();
      }
      async span(name, parent, fn, attrs) {
        const spanId = this.start(name, parent, attrs);
        try {
          const result = await fn();
          await this.end(spanId);
          return result;
        } catch (err) {
          const error2 = err instanceof Error ? err : new EngineError(String(err));
          await this.error(spanId, error2);
          throw err;
        }
      }
      start(name, parent, attrs) {
        const id = ulid();
        const traceId = parent ? this.getTraceIdFromParent(parent) : ulid();
        const span = {
          id,
          traceId,
          parentId: parent,
          name,
          startTime: Date.now(),
          status: "ok",
          attrs: attrs ?? {}
        };
        this.ringBuffer.push(span);
        this.activeSpans.set(id, span);
        this.checkOverflow();
        return id;
      }
      async end(spanId, extra) {
        const span = this.activeSpans.get(spanId) ?? this.ringBuffer.find((s) => s.id === spanId);
        if (!span) return;
        span.endTime = Date.now();
        span.duration = span.endTime - span.startTime;
        if (extra) span.attrs = { ...span.attrs, ...extra };
        this.activeSpans.delete(spanId);
      }
      async error(spanId, error2) {
        const span = this.activeSpans.get(spanId) ?? this.ringBuffer.find((s) => s.id === spanId);
        if (!span) return;
        span.endTime = Date.now();
        span.duration = span.endTime - span.startTime;
        span.status = "error";
        span.error = error2.message;
        this.activeSpans.delete(spanId);
      }
      getTrace(traceId) {
        return this.ringBuffer.filter((s) => s.traceId === traceId);
      }
      getRecentSpans(limit = 20) {
        return this.ringBuffer.slice(-limit);
      }
      getSpansByEngine(engineId, limit = 20) {
        return this.ringBuffer.filter((s) => s.engineId === engineId).slice(-limit);
      }
      getActiveSpans() {
        return [...this.activeSpans.values()];
      }
      getTraceIdFromParent(spanId) {
        const parent = this.ringBuffer.find((s) => s.id === spanId);
        return parent?.traceId ?? ulid();
      }
      checkOverflow() {
        if (this.ringBuffer.length >= this.persistThreshold && this.store) {
          const batch = this.ringBuffer.splice(0, this.ringBuffer.length - this.ringCapacity);
          this.store.batchInsertSpans(
            batch.map((s) => ({
              traceId: s.traceId,
              parentId: s.parentId,
              name: s.name,
              startTime: s.startTime,
              endTime: s.endTime,
              duration: s.duration,
              status: s.status,
              error: s.error,
              attrs: s.attrs,
              engineId: s.engineId
            }))
          ).catch((err) => console.error("[tracer] persist batch failed", err));
        }
      }
    };
  }
});

// src/engines/kernel/oracle-actuator.ts
var CATEGORY_TO_ACTION, DEFAULT_POLICY, OracleActuator;
var init_oracle_actuator = __esm({
  "src/engines/kernel/oracle-actuator.ts"() {
    "use strict";
    init_errors();
    CATEGORY_TO_ACTION = {
      stalled: { kind: "restart-engine", autoFixable: true },
      "broken-wire": { kind: "reconnect", autoFixable: false },
      "missing-dep": { kind: "notify", autoFixable: false },
      "health-degraded": { kind: "reset-circuit", autoFixable: true },
      "config-missing": { kind: "reconfigure", autoFixable: true },
      "schema-mismatch": { kind: "notify", autoFixable: false },
      stub: { kind: "notify", autoFixable: false }
    };
    DEFAULT_POLICY = {
      stalledEngineRestart: { enabled: true, timeoutMs: 3e4 },
      circuitBreakerReset: { enabled: true, cooldownMs: 5e3 },
      configFallback: { enabled: true, defaults: {} },
      notify: { enabled: true, channels: ["log"] }
    };
    OracleActuator = class {
      constructor(registry2, diagnostic, store = null) {
        this.registry = registry2;
        this.diagnostic = diagnostic;
        this.store = store;
      }
      registry;
      diagnostic;
      store;
      actions = [];
      policy = structuredClone(DEFAULT_POLICY);
      healCallbacks = /* @__PURE__ */ new Set();
      onHeal(callback) {
        this.healCallbacks.add(callback);
        return () => {
          this.healCallbacks.delete(callback);
        };
      }
      async heal(issueId) {
        const issue = await this.findIssue(issueId);
        if (!issue) throw new NotFoundError(`Diagnostic issue ${issueId} not found`);
        const mapping = CATEGORY_TO_ACTION[issue.category];
        const action = {
          id: `heal_${issue.id}_${this.actions.length}`,
          issueId: issue.id,
          kind: mapping.kind,
          engineId: issue.engineId,
          description: `Heal ${issue.category} on ${issue.engineId} via ${mapping.kind}`,
          parameters: {},
          status: "pending"
        };
        this.actions.push(action);
        await this.execute(action, issue, mapping.autoFixable);
        return action;
      }
      async getActions(limit = 50) {
        return this.actions.slice(-limit);
      }
      async getAction(id) {
        return this.actions.find((a) => a.id === id) ?? null;
      }
      async undo(actionId) {
        const action = this.actions.find((a) => a.id === actionId);
        if (!action) throw new NotFoundError(`Heal action ${actionId} not found`);
        if (action.kind === "restart-engine" || action.kind === "reset-circuit") {
          this.registry.markRunning(action.engineId);
        }
        action.status = "pending";
        action.result = "undone";
      }
      async getAutoHealPolicy() {
        return structuredClone(this.policy);
      }
      async setAutoHealPolicy(policy) {
        this.policy = structuredClone(policy);
        if (this.store) {
          try {
            await this.store.insertEvent("kernel:autoheal-policy", "kernel", this.policy);
          } catch {
          }
        }
      }
      // Auto-heal a freshly produced issue list: execute only what policy allows.
      async autoHeal(issues) {
        const executed = [];
        for (const issue of issues) {
          const mapping = CATEGORY_TO_ACTION[issue.category];
          if (!mapping.autoFixable) continue;
          if (!this.policyAllows(mapping.kind)) continue;
          const action = await this.heal(issue.id);
          executed.push(action);
        }
        return executed;
      }
      async execute(action, issue, autoFixable) {
        if (!autoFixable) {
          action.status = "completed";
          action.result = "manual intervention required \u2014 notification only";
          action.executedAt = Date.now();
          this.emitHeal(action);
          return;
        }
        if (!this.policyAllows(action.kind)) {
          action.status = "completed";
          action.result = "skipped \u2014 disabled by AutoHealPolicy";
          action.executedAt = Date.now();
          this.emitHeal(action);
          return;
        }
        action.status = "executing";
        action.executedAt = Date.now();
        this.emitHeal(action);
        try {
          await this.apply(action, issue);
          action.status = "completed";
          action.result = "success";
        } catch (err) {
          action.status = "failed";
          action.result = String(err);
        }
        this.emitHeal(action);
      }
      async apply(action, issue) {
        switch (action.kind) {
          case "restart-engine":
            this.registry.markStopped(action.engineId);
            this.registry.markRunning(action.engineId);
            break;
          case "reset-circuit":
            this.registry.markRunning(action.engineId);
            break;
          case "reconfigure": {
            const desc = this.registry.getEngine(action.engineId);
            if (desc) {
              const merged = { ...desc.config, ...this.policy.configFallback.defaults };
              desc.config = merged;
            }
            break;
          }
          case "reconnect":
            this.registry.markWired(action.engineId);
            break;
          case "clear-cache":
            break;
          case "notify":
            break;
          default:
            break;
        }
        void issue;
      }
      policyAllows(kind) {
        switch (kind) {
          case "restart-engine":
            return this.policy.stalledEngineRestart.enabled;
          case "reset-circuit":
            return this.policy.circuitBreakerReset.enabled;
          case "reconfigure":
            return this.policy.configFallback.enabled;
          case "notify":
            return this.policy.notify.enabled;
          default:
            return false;
        }
      }
      async findIssue(issueId) {
        return this.diagnostic.getIssue(issueId);
      }
      emitHeal(action) {
        for (const cb of this.healCallbacks) {
          try {
            cb(action);
          } catch {
          }
        }
      }
    };
  }
});

// src/engines/kernel/oracle-diagnostic.ts
var KNOWN_STUB_MARKERS, OracleDiagnosticEngine;
var init_oracle_diagnostic = __esm({
  "src/engines/kernel/oracle-diagnostic.ts"() {
    "use strict";
    init_ids();
    KNOWN_STUB_MARKERS = ["executeHarnessPlan", "not implemented", "not yet implemented"];
    OracleDiagnosticEngine = class {
      constructor(registry2, store = null) {
        this.registry = registry2;
        this.store = store;
      }
      registry;
      store;
      knownStubs = /* @__PURE__ */ new Map();
      registerKnownStub(engineId, method) {
        const existing = this.knownStubs.get(engineId) ?? [];
        existing.push(method);
        this.knownStubs.set(engineId, existing);
      }
      async scan() {
        const issues = [];
        issues.push(...await this.checkStubs());
        issues.push(...await this.checkWiring());
        issues.push(...await this.checkHealth());
        issues.push(...await this.checkSchema());
        issues.push(...await this.checkConfig());
        return issues;
      }
      async scanEngine(engineId) {
        const all = await this.scan();
        return all.filter((i) => i.engineId === engineId);
      }
      async getIssue(id) {
        const all = await this.scan();
        return all.find((i) => i.id === id) ?? null;
      }
      async getIssuesBySeverity(severity) {
        const all = await this.scan();
        return all.filter((i) => i.severity === severity);
      }
      async getIssuesByEngine(engineId) {
        return this.scanEngine(engineId);
      }
      async checkStubs() {
        const issues = [];
        for (const engine of this.registry.listEngines()) {
          const reported = engine.metadata?.stubMethods ?? [];
          const known = this.knownStubs.get(engine.id) ?? [];
          const methods = /* @__PURE__ */ new Set([...reported, ...known]);
          for (const method of methods) {
            if (KNOWN_STUB_MARKERS.includes(method) || method.toLowerCase().includes("stub")) {
              issues.push({
                id: `stub:${engine.id}:${method}`,
                severity: "warning",
                category: "stub",
                engineId: engine.id,
                description: `Engine ${engine.id} has stub method ${method}`,
                evidence: [`metadata.stubMethods includes "${method}"`],
                suggestedFix: `Implement ${method} per its atomic spec`,
                autoFixable: false,
                detectedAt: Date.now()
              });
            }
          }
        }
        return issues;
      }
      async checkWiring() {
        const issues = [];
        for (const engine of this.registry.listEngines()) {
          for (const dep of engine.dependencies) {
            const depEngine = this.registry.getEngine(dep);
            if (!depEngine) {
              issues.push({
                id: `missing-dep:${engine.id}:${dep}`,
                severity: "critical",
                category: "missing-dep",
                engineId: engine.id,
                description: `Engine ${engine.id} depends on missing engine ${dep}`,
                evidence: [`dependencies: ${JSON.stringify(engine.dependencies)}`],
                suggestedFix: `Register engine ${dep} or remove the dependency`,
                autoFixable: false,
                detectedAt: Date.now()
              });
            } else if (depEngine.status === "error") {
              issues.push({
                id: `broken-wire:${engine.id}:${dep}`,
                severity: "warning",
                category: "broken-wire",
                engineId: engine.id,
                description: `Engine ${engine.id} depends on errored engine ${dep}`,
                evidence: [
                  `${dep} status = error`,
                  `error: ${String(depEngine.metadata?.error ?? "unknown")}`
                ],
                suggestedFix: `Heal engine ${dep} (restart or reconnect)`,
                autoFixable: false,
                detectedAt: Date.now()
              });
            }
          }
        }
        return issues;
      }
      async checkHealth() {
        const issues = [];
        for (const engine of this.registry.listEngines()) {
          const status = engine.health?.status;
          if (status === "unhealthy") {
            issues.push({
              id: `health:${engine.id}:unhealthy`,
              severity: "critical",
              category: "health-degraded",
              engineId: engine.id,
              description: `Engine ${engine.id} is unhealthy`,
              evidence: [JSON.stringify(engine.health?.details ?? {})],
              suggestedFix: "Restart or reconfigure engine",
              autoFixable: true,
              detectedAt: Date.now()
            });
          } else if (status === "degraded") {
            issues.push({
              id: `health:${engine.id}:degraded`,
              severity: "warning",
              category: "health-degraded",
              engineId: engine.id,
              description: `Engine ${engine.id} is degraded`,
              evidence: [JSON.stringify(engine.health?.details ?? {})],
              suggestedFix: "Reset circuit breaker or reconfigure engine",
              autoFixable: true,
              detectedAt: Date.now()
            });
          }
        }
        return issues;
      }
      async checkSchema() {
        const issues = [];
        if (!this.store) {
          issues.push({
            id: "schema:nostore",
            severity: "info",
            category: "schema-mismatch",
            engineId: "kernel",
            description: "No KernelStore attached \u2014 cannot verify kernel tables",
            evidence: ["store is null"],
            suggestedFix: "Attach a KernelStore to enable schema verification",
            autoFixable: false,
            detectedAt: Date.now()
          });
          return issues;
        }
        const expected = ["kernel_spans", "kernel_provenance", "kernel_topology", "kernel_events"];
        void expected;
        try {
          await this.store.queryRecentSpans(1);
        } catch (err) {
          issues.push({
            id: "schema:spans",
            severity: "critical",
            category: "schema-mismatch",
            engineId: "kernel",
            description: "kernel_spans table query failed",
            evidence: [String(err)],
            suggestedFix: "Run prisma migration for kernel tables (0.5)",
            autoFixable: false,
            detectedAt: Date.now()
          });
        }
        return issues;
      }
      async checkConfig() {
        const issues = [];
        for (const engine of this.registry.listEngines()) {
          const required = engine.metadata?.requiredConfig ?? [];
          for (const key of required) {
            if (engine.config[key] === void 0) {
              issues.push({
                id: `config:${engine.id}:${key}`,
                severity: "warning",
                category: "config-missing",
                engineId: engine.id,
                description: `Engine ${engine.id} missing required config "${key}"`,
                evidence: [`requiredConfig: ${JSON.stringify(required)}`],
                suggestedFix: `Provide config "${key}" via ConfigManager`,
                autoFixable: true,
                detectedAt: Date.now()
              });
            }
          }
        }
        return issues;
      }
    };
  }
});

// src/engines/kernel/oracle-event-stream.ts
var OracleEventStream;
var init_oracle_event_stream = __esm({
  "src/engines/kernel/oracle-event-stream.ts"() {
    "use strict";
    OracleEventStream = class {
      constructor(diagnostic, actuator, registry2, eventBus, opts) {
        this.diagnostic = diagnostic;
        this.actuator = actuator;
        this.registry = registry2;
        this.eventBus = eventBus;
        this.recentCapacity = opts?.recentCapacity ?? 200;
        this.wireActuator();
        this.wireRegistry();
      }
      diagnostic;
      actuator;
      registry;
      eventBus;
      subscribers = /* @__PURE__ */ new Set();
      recent = [];
      recentCapacity;
      scanTimer = null;
      knownIssues = /* @__PURE__ */ new Map();
      subscribe(callback) {
        this.subscribers.add(callback);
        return () => {
          this.subscribers.delete(callback);
        };
      }
      getRecentEvents(limit = 10) {
        return this.recent.slice(-limit);
      }
      startPeriodicScan(intervalMs = 6e4) {
        this.stopPeriodicScan();
        this.scanTimer = setInterval(() => {
          void this.runScan();
        }, intervalMs);
      }
      stopPeriodicScan() {
        if (this.scanTimer) {
          clearInterval(this.scanTimer);
          this.scanTimer = null;
        }
      }
      emit(event) {
        const full = { ...event, timestamp: Date.now() };
        this.recent.push(full);
        if (this.recent.length > this.recentCapacity) {
          this.recent = this.recent.slice(-Math.floor(this.recentCapacity / 2));
        }
        for (const cb of this.subscribers) {
          try {
            cb(full);
          } catch {
          }
        }
        if (this.eventBus) {
          try {
            this.eventBus.emit({ type: "kernel:oracle", ...full });
          } catch {
          }
        }
      }
      async runScan() {
        const issues = await this.diagnostic.scan();
        for (const issue of issues) {
          if (!this.knownIssues.has(issue.id)) {
            this.knownIssues.set(issue.id, issue);
            this.emit({ kind: "issue-detected", engineId: issue.engineId, data: issue });
          }
        }
        for (const [id, issue] of this.knownIssues) {
          if (!issues.find((i) => i.id === id)) {
            this.emit({ kind: "issue-resolved", engineId: issue.engineId, data: { issueId: id } });
            this.knownIssues.delete(id);
          }
        }
      }
      wireActuator() {
        this.actuator.onHeal((action) => {
          if (action.status === "executing") {
            this.emit({
              kind: "heal-started",
              engineId: action.engineId,
              data: { actionId: action.id, kind: action.kind }
            });
          } else if (action.status === "completed") {
            this.emit({
              kind: "heal-completed",
              engineId: action.engineId,
              data: { actionId: action.id, result: action.result ?? "success" }
            });
          } else if (action.status === "failed") {
            this.emit({
              kind: "heal-failed",
              engineId: action.engineId,
              data: { actionId: action.id, error: action.result }
            });
          }
        });
      }
      wireRegistry() {
        this.registry.onStatusChange((id, from, to) => {
          this.emit({
            kind: "health-changed",
            engineId: id,
            data: { from, to }
          });
          if (from !== to) {
            this.emit({ kind: "topology-changed", data: { changed: [id], from, to } });
          }
        });
      }
    };
  }
});

// src/engines/kernel/oracle-query.ts
function layerStatus(current, engineStatus) {
  const rank = {
    error: 4,
    stopped: 3,
    registered: 2,
    wired: 1,
    running: 0,
    none: 0
  };
  const currRank = rank[current] ?? 0;
  const engRank = engineStatus ? rank[engineStatus] ?? 0 : 0;
  return engRank > currRank ? engineStatus : current;
}
var OracleQueryEngine;
var init_oracle_query = __esm({
  "src/engines/kernel/oracle-query.ts"() {
    "use strict";
    OracleQueryEngine = class {
      constructor(registry2, tracer, provenance, config2) {
        this.registry = registry2;
        this.tracer = tracer;
        this.provenance = provenance;
        this.config = config2;
      }
      registry;
      tracer;
      provenance;
      config;
      async query(q) {
        const base = { query: q, timestamp: Date.now() };
        switch (q.type) {
          case "topology": {
            const answer = await this.describe();
            return { ...base, answer, confidence: 1, suggestions: [] };
          }
          case "health": {
            const answer = await this.health();
            return { ...base, answer, confidence: 0.95, suggestions: [] };
          }
          case "trace": {
            const traceId = String(q.filter?.traceId ?? "");
            const answer = this.tracer.getTrace(traceId);
            return {
              ...base,
              answer,
              confidence: 1,
              suggestions: answer.length === 0 ? ["No spans found for this traceId"] : []
            };
          }
          case "provenance": {
            const traceId = String(q.filter?.traceId ?? "");
            const answer = this.provenance.getChain(traceId);
            return {
              ...base,
              answer,
              confidence: 1,
              suggestions: answer.nodes.length === 0 ? ["No causal nodes for this traceId"] : []
            };
          }
          case "config": {
            const engineId = String(q.filter?.engineId ?? "");
            const answer = this.readConfig(engineId);
            return {
              ...base,
              answer,
              confidence: 1,
              suggestions: answer ? [] : [`No config for engine ${engineId}`]
            };
          }
          case "capability": {
            const answer = await this.capabilitySummary();
            return { ...base, answer, confidence: 0.9, suggestions: [] };
          }
          case "all": {
            const answer = {
              topology: await this.describe(),
              health: await this.health(),
              capabilities: await this.capabilitySummary()
            };
            return { ...base, answer, confidence: 0.9, suggestions: [] };
          }
          default:
            return { ...base, answer: null, confidence: 0, suggestions: ["Unknown query type"] };
        }
      }
      async describe() {
        const topology = this.registry.describe();
        const layers = {};
        const dependencyGraph = {};
        const healthSummary = { healthy: 0, degraded: 0, unhealthy: 0, unknown: 0 };
        for (const engine of topology.engines) {
          const layer = engine.layer ?? "uncategorized";
          if (!layers[layer]) layers[layer] = { engines: [], status: "none" };
          layers[layer].engines.push(engine.id);
          const worst = layerStatus(layers[layer].status, engine.status);
          layers[layer].status = worst;
          dependencyGraph[engine.id] = engine.dependencies;
          const status = engine.health?.status ?? "unknown";
          if (status === "healthy") healthSummary.healthy++;
          else if (status === "degraded") healthSummary.degraded++;
          else if (status === "unhealthy") healthSummary.unhealthy++;
          else healthSummary.unknown++;
        }
        return {
          totalEngines: topology.engines.length,
          totalStores: topology.stores.length,
          totalCapabilities: topology.capabilities.length,
          layers,
          dependencyGraph,
          healthSummary
        };
      }
      async health() {
        const engines = this.registry.listEngines();
        const snapshotEngines = [];
        const issues = [];
        let scoreSum = 0;
        let scoreCount = 0;
        for (const engine of engines) {
          const health = engine.health ?? {
            status: "unknown",
            lastCheck: engine.updatedAt
          };
          snapshotEngines.push({
            id: engine.id,
            status: health.status,
            score: health.score,
            lastCheck: health.lastCheck
          });
          if (typeof health.score === "number") {
            scoreSum += health.score;
            scoreCount++;
          }
          if (health.status === "unhealthy") {
            issues.push({
              id: `health:${engine.id}:unhealthy`,
              severity: "critical",
              category: "health-degraded",
              engineId: engine.id,
              description: `Engine ${engine.id} is unhealthy`,
              evidence: [JSON.stringify(health.details ?? {})],
              suggestedFix: "Investigate engine logs and restart if stalled",
              autoFixable: true,
              detectedAt: Date.now()
            });
          } else if (health.status === "degraded") {
            issues.push({
              id: `health:${engine.id}:degraded`,
              severity: "warning",
              category: "health-degraded",
              engineId: engine.id,
              description: `Engine ${engine.id} is degraded`,
              evidence: [JSON.stringify(health.details ?? {})],
              suggestedFix: "Reset circuit breaker or reconfigure engine",
              autoFixable: true,
              detectedAt: Date.now()
            });
          }
        }
        return {
          aggregateScore: scoreCount === 0 ? 0 : Math.round(scoreSum / scoreCount),
          engines: snapshotEngines,
          issues,
          timestamp: Date.now()
        };
      }
      async explain(target) {
        const chain = this.provenance.getChain(target);
        const relatedTraces = [...new Set(chain.nodes.map((n) => n.traceId))];
        const suggestions = this.generateSuggestions(chain);
        const description = this.describeChain(chain);
        return { target, description, causalChain: chain, relatedTraces, suggestions };
      }
      async capabilitySummary() {
        const capabilities = this.registry.describe().capabilities;
        const byLayer = {};
        const list = [];
        for (const cap of capabilities) {
          const layer = cap.layer ?? "uncategorized";
          byLayer[layer] = (byLayer[layer] ?? 0) + 1;
          list.push({
            id: cap.id,
            layer: cap.layer,
            status: cap.status,
            surfaces: cap.metadata?.surfaces
          });
        }
        return { total: capabilities.length, byLayer, capabilities: list };
      }
      readConfig(engineId) {
        const desc = this.registry.getEngine(engineId);
        if (desc) return desc.config;
        try {
          return this.config.getConfig(engineId);
        } catch {
          return null;
        }
      }
      generateSuggestions(chain) {
        const suggestions = [];
        const errorNode = chain.nodes.find((n) => n.kind === "error");
        if (errorNode) {
          suggestions.push(`Error originated in ${errorNode.engineId}: ${errorNode.description}`);
          suggestions.push("Use oracle heal to restart or reconfigure the failing engine");
        }
        if (chain.totalDuration && chain.totalDuration > 1e4) {
          suggestions.push("Causal chain exceeded 10s \u2014 investigate slow selector/parser stage");
        }
        if (chain.nodes.length === 0) {
          suggestions.push("No causal nodes recorded \u2014 enable provenance capture for this path");
        }
        return suggestions;
      }
      describeChain(chain) {
        if (chain.nodes.length === 0) return `No provenance recorded for ${chain.traceId}`;
        const steps = chain.nodes.map((n) => `${n.kind}@${n.engineId}: ${n.description}`).join(" \u2192 ");
        return `Chain (${chain.nodes.length} nodes, ${chain.totalDuration ?? 0}ms): ${steps}`;
      }
    };
  }
});

// src/engines/kernel/kernel-bootstrap.ts
function createInMemoryConfigStore() {
  const entries = /* @__PURE__ */ new Map();
  return {
    async getConfigEntry(engineId, scopeType, scopeId) {
      const key = `${engineId}:${scopeType}:${scopeId ?? "null"}`;
      return entries.get(key) ?? null;
    },
    async upsertConfigEntry(engineId, scopeType, scopeId, configJson, schemaVersion) {
      const key = `${engineId}:${scopeType}:${scopeId ?? "null"}`;
      const now = Date.now();
      const existing = entries.get(key);
      const row = {
        id: existing?.id ?? ulid(),
        engineId,
        scopeType,
        scopeId,
        configJson,
        schemaVersion,
        createdAt: existing?.createdAt ?? now,
        updatedAt: now
      };
      entries.set(key, row);
      return row;
    },
    async insertConfigAudit(row) {
      return { ...row, id: ulid() };
    },
    async getConfigAuditHistory() {
      return [];
    },
    async getConfigEntryById(id) {
      for (const [, entry] of entries) {
        if (entry.id === id) return entry;
      }
      return null;
    }
  };
}
function bootstrapKernel(deps) {
  const registry2 = new KernelRegistry();
  const tracer = new KernelTracer({ store: deps.store });
  const provenance = new KernelProvenance({ store: deps.store });
  const config2 = new ConfigManager(createInMemoryConfigStore(), { emit: () => {
  } });
  const logger = new ConsoleKernelLogger();
  const kernel = createKernel({
    registry: registry2,
    tracer,
    provenance,
    eventBus: deps.eventBus,
    config: config2,
    store: deps.store,
    logger
  });
  if (deps.governor) {
    registry2.registerEngine({
      id: "chrome-governor",
      kind: "engine",
      layer: "chrome",
      dependencies: [],
      status: "registered",
      config: {},
      metadata: { description: "Chrome browser lifecycle + CDP proxy" }
    });
  }
  if (deps.conversationManager) {
    registry2.registerEngine({
      id: "conversation-manager",
      kind: "engine",
      layer: "conversation",
      dependencies: ["chrome-governor"],
      status: "registered",
      config: {},
      metadata: { description: "8-step send pipeline orchestrator" }
    });
  }
  if (deps.registry) {
    registry2.registerEngine({
      id: "unified-registry",
      kind: "engine",
      layer: "capability",
      dependencies: [],
      status: "registered",
      config: {},
      metadata: { description: "Unified capability registry (CLI/UI/MCP/API)" }
    });
  }
  if (deps.nlclEngine) {
    registry2.registerEngine({
      id: "nlcl-engine",
      kind: "engine",
      layer: "comms",
      dependencies: ["chrome-governor", "conversation-manager", "unified-registry"],
      status: "registered",
      config: {
        resolver: "deterministic",
        aiFallback: true,
        commandCount: deps.nlclEngine.listCommands().length
      },
      metadata: {
        description: "Natural Language Command Layer \u2014 the comms system",
        surfaces: ["cli", "ui", "frontend", "mcp", "api"]
      }
    });
    const commands = deps.nlclEngine.listCommands();
    for (const cmd of commands) {
      registry2.registerCapability({
        id: `nlcl:${cmd.id}`,
        kind: "capability",
        layer: "comms",
        dependencies: ["nlcl-engine"],
        status: "registered",
        config: {
          intent: cmd.intent,
          executor: cmd.executor,
          category: cmd.category
        },
        metadata: {
          description: cmd.description,
          examples: cmd.examples,
          aliases: cmd.aliases
        }
      });
    }
    logger.info("NLCL registered with kernel", {
      commands: commands.length,
      categories: Object.keys(deps.nlclEngine.getHelp().categories).length
    });
  }
  if (deps.db) {
    registry2.registerStore({
      id: "capstore-db",
      kind: "store",
      layer: "storage",
      dependencies: [],
      status: "running",
      config: {},
      metadata: { description: "Primary database (Prisma/SQLite)" }
    });
  }
  const routes = [
    { id: "route:conversations", path: "/api/conversations" },
    { id: "route:setup", path: "/api/setup" },
    { id: "route:knowledge", path: "/api/knowledge" },
    { id: "route:mux", path: "/api/route" },
    { id: "route:autonomous", path: "/api/autonomous" },
    { id: "route:nlcl", path: "/api/nlcl" },
    { id: "route:health", path: "/health" },
    { id: "route:websocket", path: "/ws" }
  ];
  for (const route of routes) {
    registry2.registerRoute({
      id: route.id,
      kind: "route",
      layer: "api",
      dependencies: [],
      status: "running",
      config: { path: route.path },
      metadata: {}
    });
  }
  logger.info("kernel bootstrap complete", {
    engines: registry2.describe().engines.length,
    capabilities: registry2.describe().capabilities.length,
    routes: registry2.describe().routes.length
  });
  const diagnostic = new OracleDiagnosticEngine(registry2, deps.store ?? null);
  const query = new OracleQueryEngine(registry2, tracer, provenance, config2);
  const actuator = new OracleActuator(registry2, diagnostic, deps.store ?? null);
  const events = new OracleEventStream(diagnostic, actuator, registry2, deps.eventBus);
  kernel.context().oracle = { query, diagnostic, actuator, events };
  const oracleEngines = [
    { id: "oracle-query", description: "Structured system-state queries" },
    { id: "oracle-diagnostic", description: "Problem detection (stubs, wires, health)" },
    { id: "oracle-actuator", description: "Self-healing corrective actions" },
    { id: "oracle-event-stream", description: "Real-time oracle event broadcast" }
  ];
  for (const oe of oracleEngines) {
    registry2.registerEngine({
      id: oe.id,
      kind: "engine",
      layer: "oracle",
      dependencies: ["kernel"],
      status: "running",
      config: {},
      metadata: { description: oe.description }
    });
  }
  return kernel;
}
var init_kernel_bootstrap = __esm({
  "src/engines/kernel/kernel-bootstrap.ts"() {
    "use strict";
    init_ids();
    init_config_manager();
    init_kernel_context();
    init_kernel_provenance();
    init_kernel_registry();
    init_kernel_tracer();
    init_oracle_actuator();
    init_oracle_diagnostic();
    init_oracle_event_stream();
    init_oracle_query();
  }
});

// src/engines/nlcl/catalog.ts
import { z as z5 } from "zod";
function pattern(id, intent, description, opts) {
  return {
    id,
    intent,
    description,
    patterns: opts.patterns,
    aliases: opts.aliases ?? [],
    examples: opts.examples ?? [],
    inputSchema: opts.inputSchema ?? z5.object({}).passthrough(),
    outputSchema: opts.outputSchema ?? z5.unknown(),
    executor: opts.executor,
    execute: opts.execute,
    category: opts.category,
    surfaces: opts.surfaces ?? ["cli", "ui", "frontend", "mcp", "api"],
    requiresConfirmation: opts.requiresConfirmation ?? false,
    classification: opts.classification ?? "read",
    aiFallback: opts.aiFallback ?? false,
    tags: opts.tags ?? [],
    capabilityId: opts.capabilityId
  };
}
function extractEmails(text) {
  const emailRegex = /[\w.-]+@[\w.-]+\.\w+/g;
  const matches = text.match(emailRegex);
  return matches ?? [];
}
function dayToCron(day) {
  const days = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"];
  const dayIdx = days.indexOf(day.toLowerCase());
  if (dayIdx >= 0) {
    return `0 9 * * ${dayIdx}`;
  }
  if (day === "day") return "0 9 * * *";
  if (day === "hour") return "0 * * * *";
  return "0 9 * * *";
}
function getDefaultCommandPatterns() {
  return [
    ...filePatterns,
    ...browserPatterns,
    ...llmPatterns,
    ...emailPatterns,
    ...appPatterns,
    ...conversationPatterns,
    ...systemPatterns,
    ...canvasPatterns,
    ...channelPatterns,
    ...sessionPatterns,
    ...workflowPatterns,
    ...memoryPatterns,
    ...automationPatterns
  ];
}
var filePatterns, browserPatterns, llmPatterns, emailPatterns, appPatterns, conversationPatterns, systemPatterns, canvasPatterns, channelPatterns, workflowPatterns, sessionPatterns, memoryPatterns, automationPatterns;
var init_catalog = __esm({
  "src/engines/nlcl/catalog.ts"() {
    "use strict";
    filePatterns = [
      pattern("file.open", "file.open", "Open a file by name or path", {
        patterns: [
          {
            regex: /(?:open|launch|show|view)\s+(?:my\s+|the\s+|a\s+)?(.+?)(?:\s+(?:file|document|doc|pdf))?$/,
            priority: 10,
            keywords: ["open", "launch", "show", "view"],
            extract: (m) => {
              const name = (m[1] ?? "").trim();
              if (name.match(/^[A-Z]:[\\/]|^\/|^~\//)) {
                return { path: name };
              }
              return { name };
            }
          }
        ],
        aliases: ["open file", "open document"],
        examples: [
          "open my resume",
          "open report.pdf",
          "open C:\\Users\\me\\doc.txt",
          "open my budget"
        ],
        inputSchema: z5.object({
          name: z5.string().optional(),
          path: z5.string().optional()
        }),
        executor: "file",
        category: "file",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("file.list", "file.list", "List files in a folder", {
        patterns: [
          {
            regex: /(?:list|show|open)\s+(?:my\s+)?(documents|desktop|downloads|pictures|files|folder)/,
            priority: 12,
            keywords: ["list", "documents", "desktop", "downloads", "pictures"],
            extract: (m) => ({ folder: (m[1] ?? "documents").toLowerCase() })
          },
          {
            regex: /(?:what|which)\s+(?:files|documents)\s+(?:are\s+)?(?:in|on)\s+(?:my\s+)?(documents|desktop|downloads|pictures)/,
            priority: 11,
            extract: (m) => ({ folder: (m[1] ?? "documents").toLowerCase() })
          }
        ],
        aliases: ["list files", "show files", "open documents"],
        examples: ["list my documents", "show desktop", "what's in my downloads"],
        inputSchema: z5.object({ folder: z5.string().optional() }),
        executor: "file",
        category: "file",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("file.search", "file.search", "Search for files by name", {
        patterns: [
          {
            regex: /(?:find|search\s+for|look\s+for|where\s+(?:is|are))\s+(.+?)(?:\s+(?:file|document|doc))?$/,
            priority: 9,
            keywords: ["find", "search for", "look for"],
            extract: (m) => ({ query: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["find file", "search files"],
        examples: ["find my resume", "search for budget", "where is my tax document"],
        inputSchema: z5.object({ query: z5.string().optional(), name: z5.string().optional() }),
        executor: "file",
        category: "file",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("file.create", "file.create", "Create a new file", {
        patterns: [
          {
            regex: /(?:create|make|new)\s+(?:a\s+)?(?:file|document|doc|note)\s+(?:called\s+|named\s+)?(.+?)(?:\s+(?:in|under)\s+(documents|desktop|downloads))?$/,
            priority: 8,
            keywords: ["create", "make", "new file"],
            extract: (m) => ({
              name: (m[1] ?? "").trim(),
              folder: (m[2] ?? "documents").toLowerCase()
            })
          }
        ],
        aliases: ["create file", "new file", "make document"],
        examples: ["create a file called notes.txt", "make a new document named todo"],
        inputSchema: z5.object({
          name: z5.string(),
          content: z5.string().optional(),
          folder: z5.string().optional()
        }),
        executor: "file",
        category: "file",
        classification: "write",
        execute: async () => ({})
      }),
      pattern("file.read", "file.read", "Read file contents", {
        patterns: [
          {
            regex: /(?:read|show\s+contents|display)\s+(?:of\s+)?(.+?)(?:\s+(?:file|document))?$/,
            priority: 8,
            keywords: ["read", "show contents"],
            extract: (m) => ({ name: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["read file", "show file contents"],
        examples: ["read my notes", "show contents of config.json"],
        inputSchema: z5.object({ name: z5.string() }),
        executor: "file",
        category: "file",
        classification: "read",
        execute: async () => ({})
      })
    ];
    browserPatterns = [
      pattern("browser.navigate", "browser.navigate", "Navigate to a URL", {
        patterns: [
          {
            regex: /(?:go\s+to|navigate\s+to|open|visit)\s+(https?:\/\/[^\s]+|[\w-]+\.[\w.-]+[^\s]*)/,
            priority: 15,
            keywords: ["go to", "navigate to", "visit"],
            extract: (m) => ({ url: (m[1] ?? "").trim() })
          },
          {
            regex: /(?:go\s+to|open|visit)\s+(cnn|bbc|youtube|github|gmail|google|reddit|twitter|x\.com|facebook|instagram|wikipedia|amazon|netflix|linkedin)/,
            priority: 14,
            extract: (m) => {
              const site = (m[1] ?? "").toLowerCase();
              const urlMap = {
                cnn: "https://cnn.com",
                bbc: "https://bbc.com",
                youtube: "https://youtube.com",
                github: "https://github.com",
                gmail: "https://mail.google.com",
                google: "https://google.com",
                reddit: "https://reddit.com",
                twitter: "https://twitter.com",
                "x.com": "https://x.com",
                facebook: "https://facebook.com",
                instagram: "https://instagram.com",
                wikipedia: "https://wikipedia.org",
                amazon: "https://amazon.com",
                netflix: "https://netflix.com",
                linkedin: "https://linkedin.com"
              };
              return { url: urlMap[site] ?? `https://${site}.com` };
            }
          }
        ],
        aliases: ["go to", "navigate", "visit"],
        examples: ["go to cnn.com", "open youtube", "visit github.com"],
        inputSchema: z5.object({ url: z5.string() }),
        executor: "browser",
        category: "browser",
        classification: "navigate",
        execute: async () => ({})
      }),
      pattern("browser.search", "browser.search", "Search the web", {
        patterns: [
          {
            regex: /(?:search\s+(?:for\s+)?|google|look\s+up)\s*(.+?)(?:\s+on\s+(google|bing|duckduckgo|youtube))?$/,
            priority: 13,
            keywords: ["search", "google", "look up"],
            extract: (m) => ({
              query: (m[1] ?? "").trim(),
              engine: (m[2] ?? "google").toLowerCase()
            })
          }
        ],
        aliases: ["search", "google", "look up"],
        examples: [
          "search for best restaurants",
          "google weather tomorrow",
          "look up python docs on bing"
        ],
        inputSchema: z5.object({
          query: z5.string(),
          engine: z5.string().optional()
        }),
        executor: "browser",
        category: "browser",
        classification: "navigate",
        execute: async () => ({})
      }),
      pattern("browser.open", "browser.open", "Open Chrome browser", {
        patterns: [
          {
            regex: /(?:open|launch|start)\s+(?:chrome|browser|the\s+browser)/,
            priority: 16,
            keywords: ["open chrome", "launch chrome", "start browser"],
            extract: () => ({})
          }
        ],
        aliases: ["open chrome", "launch browser", "start chrome"],
        examples: ["open chrome", "launch browser", "start chrome"],
        inputSchema: z5.object({ url: z5.string().optional() }),
        executor: "browser",
        category: "browser",
        classification: "navigate",
        execute: async () => ({})
      }),
      pattern("browser.extract", "browser.extract", "Extract text from current page", {
        patterns: [
          {
            regex: /(?:extract|read|get)\s+(?:the\s+)?(?:text|content|page)(?:\s+content)?\s+(?:from\s+)?(?:this\s+)?(?:page|tab|site)?/,
            priority: 7,
            keywords: ["extract", "read page", "get content"],
            extract: () => ({})
          }
        ],
        aliases: ["extract text", "read page", "get page content"],
        examples: ["extract the text from this page", "read page content"],
        inputSchema: z5.object({}),
        executor: "browser",
        category: "browser",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("browser.screenshot", "browser.screenshot", "Take a screenshot", {
        patterns: [
          {
            regex: /(?:take\s+)?(?:a\s+)?screenshot(?:\s+of\s+(?:this\s+)?(?:page|tab|screen))?/,
            priority: 12,
            keywords: ["screenshot", "capture screen"],
            extract: () => ({})
          }
        ],
        aliases: ["screenshot", "capture screen", "take screenshot"],
        examples: ["screenshot", "take a screenshot of this page"],
        inputSchema: z5.object({}),
        executor: "browser",
        category: "browser",
        classification: "read",
        execute: async () => ({})
      })
    ];
    llmPatterns = [
      pattern("llm.ask", "llm.ask", "Ask a provider LLM a question", {
        patterns: [
          {
            regex: /(?:ask|talk\s+to|tell|question)\s+(chatgpt|gpt|claude|gemini|ai|the\s+ai)\s+(?:about\s+|to\s+|that\s+)?(.+)$/,
            priority: 15,
            keywords: ["ask", "chatgpt", "claude", "gemini"],
            extract: (m) => {
              const providerRaw = (m[1] ?? "").toLowerCase();
              const providerMap = {
                chatgpt: "chatgpt",
                gpt: "chatgpt",
                claude: "claude",
                gemini: "gemini",
                ai: "chatgpt",
                "the ai": "chatgpt"
              };
              return {
                providerId: providerMap[providerRaw] ?? "chatgpt",
                prompt: (m[2] ?? "").trim()
              };
            }
          },
          {
            regex: /(?:ask|tell)\s+(.+?)\s+(?:to\s+)?(?:chatgpt|claude|gemini|ai)$/,
            priority: 14,
            extract: (m) => ({
              prompt: (m[1] ?? "").trim(),
              providerId: "chatgpt"
            })
          }
        ],
        aliases: ["ask", "question", "tell ai"],
        examples: [
          "ask chatgpt about quantum physics",
          "tell claude to explain recursion",
          "ask gemini what is the weather"
        ],
        inputSchema: z5.object({
          prompt: z5.string(),
          providerId: z5.string().optional(),
          query: z5.string().optional(),
          message: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      pattern("llm.summarize", "llm.summarize", "Summarize content using provider LLM", {
        patterns: [
          {
            regex: /(?:summarize|summarise|tldr|tl;dr)\s+(?:(?:the\s+)?(?:news|article|page|content|text|document)\s+)?(?:about\s+)?(.+)?$/,
            priority: 14,
            keywords: ["summarize", "summarise", "tldr"],
            extract: (m) => ({
              topic: (m[1] ?? "").trim() || void 0,
              content: void 0
            })
          }
        ],
        aliases: ["summarize", "tldr", "summarise"],
        examples: ["summarize the news", "summarize about AI", "tldr this article"],
        inputSchema: z5.object({
          content: z5.string().optional(),
          topic: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      pattern("web.summarize", "web.summarize", "Navigate to a URL and summarize its content", {
        patterns: [
          {
            regex: /(?:go\s+to|open|visit)\s+(https?:\/\/[^\s]+|[\w-]+\.[\w.-]+)\s+(?:and\s+)?(?:summarize|summarise|tldr|read\s+and\s+summarize)/,
            priority: 20,
            keywords: ["go to", "summarize"],
            extract: (m) => ({ url: (m[1] ?? "").trim() })
          },
          {
            regex: /(?:summarize|summarise)\s+(https?:\/\/[^\s]+|[\w-]+\.[\w.-]+)/,
            priority: 19,
            extract: (m) => ({ url: (m[1] ?? "").trim() })
          },
          {
            regex: /(?:summarize|summarise)\s+(?:the\s+)?(?:news|latest)\s+(?:from\s+|on\s+)?(cnn|bbc|reuters|nytimes|wsj|techcrunch|the\s+verge|hacker\s+news)/,
            priority: 18,
            extract: (m) => {
              const site = (m[1] ?? "").toLowerCase();
              const urlMap = {
                cnn: "https://cnn.com",
                bbc: "https://bbc.com/news",
                reuters: "https://reuters.com",
                nytimes: "https://nytimes.com",
                wsj: "https://wsj.com",
                techcrunch: "https://techcrunch.com",
                "the verge": "https://theverge.com",
                "hacker news": "https://news.ycombinator.com"
              };
              return { url: urlMap[site] ?? `https://${site}.com`, topic: "news" };
            }
          }
        ],
        aliases: ["summarize website", "read and summarize"],
        examples: [
          "go to cnn and summarize the news",
          "summarize bbc.com",
          "summarize the news from cnn"
        ],
        inputSchema: z5.object({
          url: z5.string().optional(),
          content: z5.string().optional(),
          topic: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      // Unit 1.5 — QUERY endpoint for safe body-bearing read
      pattern("web.query", "web.query", "Query an HTTP endpoint with body (safe read)", {
        patterns: [
          {
            regex: /(?:query|fetch|get|read)\s+(?:from\s+)?(https?:\/\/[^\s]+)\s+(?:with|body)?\s*(.+)?$/,
            priority: 15,
            keywords: ["query", "fetch", "http get"],
            extract: (m) => ({ url: (m[1] ?? "").trim(), body: (m[2] ?? "").trim() || void 0 })
          }
        ],
        aliases: ["query", "http query", "fetch"],
        examples: ["query https://api.example.com/data", "fetch from https://api.github.com with body"],
        inputSchema: z5.object({ url: z5.string(), body: z5.string().optional() }),
        executor: "web",
        category: "web",
        classification: "read",
        capabilityId: "cap:web:query",
        execute: async () => ({})
      }),
      pattern("llm.translate", "llm.translate", "Translate text using provider LLM", {
        patterns: [
          {
            regex: /translate\s+(.+?)\s+(?:to|into)\s+(\w+)(?:\s+from\s+(\w+))?/,
            priority: 14,
            keywords: ["translate"],
            extract: (m) => ({
              text: (m[1] ?? "").trim(),
              targetLanguage: (m[2] ?? "").trim(),
              sourceLanguage: (m[3] ?? void 0)?.trim()
            })
          }
        ],
        aliases: ["translate"],
        examples: ["translate hello world to spanish", "translate bonjour to english from french"],
        inputSchema: z5.object({
          text: z5.string(),
          targetLanguage: z5.string(),
          sourceLanguage: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      pattern("llm.explain", "llm.explain", "Explain something using provider LLM", {
        patterns: [
          {
            regex: /(?:explain|what\s+is|what\s+are|define|describe)\s+(.+?)(?:\s+(?:to\s+me|please))?$/,
            priority: 10,
            keywords: ["explain", "what is", "define", "describe"],
            extract: (m) => ({ topic: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["explain", "what is", "define", "describe"],
        examples: ["explain quantum computing", "what is machine learning", "define recursion"],
        inputSchema: z5.object({
          topic: z5.string(),
          content: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      pattern("llm.rewrite", "llm.rewrite", "Rewrite text using provider LLM", {
        patterns: [
          {
            regex: /rewrite\s+(.+?)\s+(?:in\s+a\s+)?(\w+)\s+(?:style|tone|way)/,
            priority: 12,
            keywords: ["rewrite"],
            extract: (m) => ({
              text: (m[1] ?? "").trim(),
              style: (m[2] ?? "").trim()
            })
          }
        ],
        aliases: ["rewrite"],
        examples: ["rewrite this paragraph in a formal style", "rewrite the email in a casual tone"],
        inputSchema: z5.object({
          text: z5.string(),
          style: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      }),
      pattern("llm.code", "llm.code", "Generate code using provider LLM", {
        patterns: [
          {
            regex: /(?:write|create|generate|make)\s+(?:code|a\s+function|a\s+script|a\s+program)(?:\s+(?:in|using|with)\s+(\w+))?\s+(?:that|to|which)\s+(.+)/,
            priority: 13,
            keywords: ["write code", "generate code", "create function"],
            extract: (m) => ({
              language: (m[1] ?? void 0)?.trim(),
              task: (m[2] ?? "").trim()
            })
          },
          {
            regex: /code\s+(.+?)\s+(?:in|using)\s+(\w+)/,
            priority: 12,
            extract: (m) => ({
              task: (m[1] ?? "").trim(),
              language: (m[2] ?? "").trim()
            })
          }
        ],
        aliases: ["write code", "generate code", "code"],
        examples: [
          "write code that sorts a list in python",
          "create a function to validate email in javascript"
        ],
        inputSchema: z5.object({
          task: z5.string(),
          language: z5.string().optional()
        }),
        executor: "provider-llm",
        category: "llm",
        classification: "communication",
        aiFallback: true,
        execute: async () => ({})
      })
    ];
    emailPatterns = [
      pattern("email.send", "email.send", "Send an email", {
        patterns: [
          {
            regex: /(?:send|write|compose)\s+(?:an\s+)?email\s+(?:to\s+)?([\w.+-]+@[\w-]+\.[\w.-]+|[\w\s]+)(?:\s+(?:about|re|subject)\s+(.+?))?(?:\s+saying\s+(.+))?$/,
            priority: 15,
            keywords: ["send email", "write email", "compose email"],
            extract: (m) => ({
              to: (m[1] ?? "").trim(),
              subject: (m[2] ?? "").trim() || void 0,
              body: (m[3] ?? "").trim() || void 0
            })
          },
          {
            regex: /email\s+([\w.+-]+@[\w-]+\.[\w.-]+)\s+(?:about\s+)?(.+)/,
            priority: 14,
            extract: (m) => ({
              to: (m[1] ?? "").trim(),
              subject: (m[2] ?? "").trim()
            })
          }
        ],
        aliases: ["send email", "email", "compose email"],
        examples: [
          "send email to john@example.com about the meeting",
          "email boss@example.com saying I will be late"
        ],
        inputSchema: z5.object({
          to: z5.string(),
          subject: z5.string().optional(),
          body: z5.string().optional()
        }),
        executor: "email",
        category: "email",
        classification: "communication",
        requiresConfirmation: true,
        execute: async () => ({})
      })
    ];
    appPatterns = [
      pattern("app.launch", "app.launch", "Launch a native application", {
        patterns: [
          {
            regex: /(?:open|launch|start|run)\s+(notepad|calculator|calc|terminal|cmd|powershell|explorer|paint|word|excel|chrome|edge|settings|textedit|safari|finder|notes|mail|calendar|files|file|firefox|screenshot|snipping|snip)/,
            priority: 13,
            keywords: ["open", "launch", "start app"],
            extract: (m) => ({ app: (m[1] ?? "").toLowerCase().trim() })
          }
        ],
        aliases: ["open app", "launch app"],
        examples: ["open notepad", "launch calculator", "start terminal"],
        inputSchema: z5.object({ app: z5.string(), name: z5.string().optional() }),
        executor: "app",
        category: "app",
        classification: "system",
        execute: async () => ({})
      })
    ];
    conversationPatterns = [
      pattern("conversation.create", "conversation.create", "Create a new conversation", {
        patterns: [
          {
            regex: /(?:new|create|start)\s+(?:a\s+)?(?:conversation|chat|session)(?:\s+(?:with|on)\s+(chatgpt|claude|gemini))?/,
            priority: 12,
            keywords: ["new conversation", "create chat", "start session"],
            extract: (m) => ({
              providerId: (m[1] ?? void 0)?.toLowerCase()
            })
          }
        ],
        aliases: ["new chat", "new conversation", "create conversation"],
        examples: ["new conversation", "create a chat with claude", "start a session on gemini"],
        inputSchema: z5.object({
          providerId: z5.string().optional(),
          title: z5.string().optional()
        }),
        executor: "conversation",
        category: "conversation",
        classification: "system",
        capabilityId: "cap:conversation:create",
        execute: async () => ({})
      }),
      pattern("conversation.list", "conversation.list", "List conversations", {
        patterns: [
          {
            regex: /(?:list|show|my)\s+(?:my\s+)?(?:conversations|chats|sessions)/,
            priority: 11,
            keywords: ["list conversations", "show chats"],
            extract: () => ({})
          }
        ],
        aliases: ["list chats", "show conversations", "my conversations"],
        examples: ["list my conversations", "show chats"],
        inputSchema: z5.object({ limit: z5.number().optional() }),
        executor: "conversation",
        category: "conversation",
        classification: "read",
        capabilityId: "cap:conversation:list",
        execute: async () => ({})
      }),
      pattern("conversation.switch", "conversation.switch", "Switch to a different provider", {
        patterns: [
          {
            regex: /(?:switch\s+to|use|change\s+to)\s+(chatgpt|claude|gemini|gpt)/,
            priority: 14,
            keywords: ["switch to", "use provider"],
            extract: (m) => {
              const p = (m[1] ?? "").toLowerCase();
              return { providerId: p === "gpt" ? "chatgpt" : p };
            }
          }
        ],
        aliases: ["switch to", "use provider"],
        examples: ["switch to claude", "use gemini", "change to chatgpt"],
        inputSchema: z5.object({ providerId: z5.string() }),
        executor: "conversation",
        category: "conversation",
        classification: "navigate",
        capabilityId: "cap:conversation:switch",
        execute: async () => ({})
      })
    ];
    systemPatterns = [
      pattern("system.health", "system.health", "Check system health", {
        patterns: [
          {
            regex: /(?:what'?s?\s+)?(?:the\s+)?(?:system\s+)?health|how\s+(?:is|are)\s+(?:you|the\s+system|things)|status\s+check/,
            priority: 12,
            keywords: ["health", "status", "how are you"],
            extract: () => ({})
          }
        ],
        aliases: ["health", "status", "how are you"],
        examples: ["what's the system health", "how are you", "status check"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:system:health",
        execute: async () => ({})
      }),
      pattern("system.providers", "system.providers", "List available providers", {
        patterns: [
          {
            regex: /(?:list|show|what)\s+(?:my\s+)?(?:providers|ai\s+providers|llms)/,
            priority: 12,
            keywords: ["providers", "list providers"],
            extract: () => ({})
          },
          {
            regex: /(?:which|what)\s+(?:providers|ai|llms)\s+(?:do\s+i\s+have|are\s+available)/,
            priority: 11,
            extract: () => ({})
          }
        ],
        aliases: ["list providers", "providers", "show providers"],
        examples: ["list my providers", "which providers do I have", "show providers"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:provider:health_get",
        execute: async () => ({})
      }),
      pattern("system.fleet", "system.fleet", "Show Chrome fleet status", {
        patterns: [
          {
            regex: /(?:fleet|chrome)\s+(?:status|state|info)|what'?s?\s+(?:the\s+)?fleet\s+(?:status|doing)/,
            priority: 12,
            keywords: ["fleet", "chrome status"],
            extract: () => ({})
          }
        ],
        aliases: ["fleet status", "chrome status"],
        examples: ["fleet status", "what's the fleet doing", "chrome status"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:fleet:status",
        execute: async () => ({})
      }),
      pattern("system.capabilities", "system.capabilities", "List available capabilities", {
        patterns: [
          {
            regex: /(?:list|show|what)\s+(?:my\s+)?(?:capabilities|commands|actions)|what\s+can\s+(?:you|i)\s+do/,
            priority: 11,
            keywords: ["capabilities", "commands", "what can you do"],
            extract: () => ({})
          }
        ],
        aliases: ["capabilities", "commands", "what can you do"],
        examples: ["what can you do", "list capabilities", "show commands"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:system:capabilities",
        execute: async () => ({})
      }),
      pattern("system.version", "system.version", "Show system version", {
        patterns: [
          {
            regex: /(?:what'?s?\s+)?(?:your\s+|the\s+)?version|vivim\s+version/,
            priority: 13,
            keywords: ["version"],
            extract: () => ({})
          }
        ],
        aliases: ["version", "vivim version"],
        examples: ["what version", "vivim version"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:system:version",
        execute: async () => ({})
      }),
      pattern("system.workspace", "system.workspace", "Show current workspace", {
        patterns: [
          {
            regex: /(?:what'?s?\s+)?(?:my|the)\s+workspace|where\s+(?:is|am)\s+(?:my|i|the)\s+(?:workspace|working\s+directory)/,
            priority: 12,
            keywords: ["workspace", "working directory"],
            extract: () => ({})
          }
        ],
        aliases: ["workspace", "where am i"],
        examples: ["what's my workspace", "where is my working directory"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:setup:workspace_get",
        execute: async () => ({})
      }),
      pattern("help", "system.help", "Show help and available commands", {
        patterns: [
          {
            regex: /^(help|commands|what\s+can\s+you\s+do|how\s+do\s+i\s+use\s+this)/,
            priority: 20,
            keywords: ["help", "commands"],
            extract: () => ({})
          }
        ],
        aliases: ["help", "commands", "?"],
        examples: ["help", "commands", "what can you do"],
        inputSchema: z5.object({}),
        executor: "system",
        category: "system",
        classification: "system",
        capabilityId: "cap:help",
        execute: async () => ({})
      })
    ];
    canvasPatterns = [
      pattern(
        "canvas.set_background",
        "canvas.set_background",
        "Change canvas background (by image query or layer)",
        {
          patterns: [
            {
              regex: /(?:change|set)\s+(?:my\s+)?(?:canvas\s+background|background)\s+(?:to|as)\s+(?:an?\s+)?(?:image\s+of\s+)?(.+)$/,
              priority: 18,
              keywords: ["change background", "set background", "canvas background"],
              extract: (m) => ({ imageQuery: (m[1] ?? "").trim().replace(/^the\s+/i, "") })
            },
            {
              regex: /(?:canvas|background)\s+(image|picture|photo):\s*(.+)$/i,
              priority: 17,
              keywords: ["canvas", "background"],
              extract: (m) => ({ imageQuery: (m[2] ?? "").trim().replace(/^the\s+/i, "") })
            }
          ],
          aliases: ["canvas background", "change background"],
          examples: ["change my canvas background to an image of the moon made out of cheese"],
          inputSchema: z5.object({
            imageQuery: z5.string().optional(),
            imageBase64: z5.string().optional(),
            layerId: z5.string().optional()
          }),
          executor: "capability",
          category: "canvas",
          classification: "system",
          aiFallback: true,
          capabilityId: "cap:canvas:set_background",
          execute: async () => ({})
        }
      ),
      pattern("canvas.add_layer", "canvas.add_layer", "Add a layer to the canvas", {
        patterns: [
          {
            regex: /(?:add|create)\s+(?:a\s+)?(?:layer|panel|window)\s+(?:showing|displaying|with)?\s*(.+)$/,
            priority: 16,
            keywords: ["add layer", "create layer", "canvas layer"],
            extract: (m) => ({ kind: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["add layer", "create layer"],
        examples: ["add a layer showing my providers", "create a layer for chat"],
        inputSchema: z5.object({
          kind: z5.string().optional(),
          title: z5.string().optional(),
          config: z5.record(z5.unknown()).optional()
        }),
        executor: "capability",
        category: "canvas",
        classification: "system",
        capabilityId: "cap:canvas:add_layer",
        execute: async () => ({})
      }),
      pattern("canvas.remove_layer", "canvas.remove_layer", "Remove a layer from the canvas", {
        patterns: [
          {
            regex: /(?:remove|delete)\s+(?:the\s+)?(?:layer|panel)\s+(?:named\s+)?(.+)$/,
            priority: 15,
            keywords: ["remove layer", "delete layer"],
            extract: (m) => ({ layerId: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["remove layer", "delete layer"],
        examples: ["remove the chat layer", "delete layer inst:chat:abc"],
        inputSchema: z5.object({
          layerId: z5.string()
        }),
        executor: "capability",
        category: "canvas",
        classification: "system",
        requiresConfirmation: true,
        capabilityId: "cap:canvas:remove_layer",
        execute: async () => ({})
      }),
      pattern("canvas.set_layout", "canvas.set_layout", "Set canvas layout mode", {
        patterns: [
          {
            regex: /(?:set|change)\s+(?:canvas\s+)?layout\s+(?:to\s+)?(grid|list|freeform)$/,
            priority: 14,
            keywords: ["set layout", "canvas layout"],
            extract: (m) => ({ layout: (m[1] ?? "freeform").toLowerCase() })
          }
        ],
        aliases: ["set layout"],
        examples: ["set layout to grid", "set canvas layout to list"],
        inputSchema: z5.object({
          layout: z5.enum(["grid", "list", "freeform"]).optional()
        }),
        executor: "capability",
        category: "canvas",
        classification: "system",
        capabilityId: "cap:canvas:set_layout",
        execute: async () => ({})
      }),
      pattern("canvas.set_theme", "canvas.set_theme", "Set canvas theme", {
        patterns: [
          {
            regex: /(?:set|change|switch)\s+(?:canvas\s+)?theme\s+(?:to\s+)?(light|dark|auto)$/,
            priority: 13,
            keywords: ["set theme", "canvas theme"],
            extract: (m) => ({ theme: (m[1] ?? "auto").toLowerCase() })
          }
        ],
        aliases: ["set theme", "dark mode", "light mode"],
        examples: ["set theme to dark", "switch canvas to light mode"],
        inputSchema: z5.object({
          theme: z5.enum(["light", "dark", "auto"]).optional()
        }),
        executor: "capability",
        category: "canvas",
        classification: "system",
        capabilityId: "cap:canvas:set_theme",
        execute: async () => ({})
      })
    ];
    channelPatterns = [
      pattern("channel.add", "channel.add", "Add a streaming channel", {
        patterns: [
          {
            regex: /(?:add|connect)\s+(?:my\s+|a\s+)?(whatsapp|facebook|telegram|slack|dispatch)\s+(?:feed|channel|messaging)/i,
            priority: 16,
            keywords: ["add channel", "connect channel", "stream channel"],
            extract: (m) => ({ providerId: (m[1] ?? "").toLowerCase() })
          }
        ],
        aliases: ["add channel", "add feed"],
        examples: ["add my whatsapp channel", "connect my facebook messaging feed"],
        inputSchema: z5.object({
          providerId: z5.string(),
          name: z5.string().optional()
        }),
        executor: "capability",
        category: "channel",
        classification: "system",
        capabilityId: "cap:channel:add",
        execute: async () => ({})
      }),
      pattern("channel.connect", "channel.connect", "Connect a streaming channel", {
        patterns: [
          {
            regex: /connect\s+(?:my\s+)?(.+)$/,
            priority: 15,
            keywords: ["connect channel"],
            extract: (m) => ({ channelId: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["connect channel"],
        examples: ["connect my whatsapp", "connect channel ch:whatsapp:abc"],
        inputSchema: z5.object({
          channelId: z5.string()
        }),
        executor: "capability",
        category: "channel",
        classification: "system",
        capabilityId: "cap:channel:connect",
        execute: async () => ({})
      }),
      pattern("channel.list", "channel.list", "List streaming channels", {
        patterns: [
          {
            regex: /(?:list|show|my)\s+(?:my\s+)?(?:channels|feeds|messaging)/,
            priority: 14,
            keywords: ["list channels", "show feeds"],
            extract: () => ({})
          }
        ],
        aliases: ["list channels", "show feeds"],
        examples: ["list my channels", "show messaging feeds"],
        inputSchema: z5.object({}),
        executor: "capability",
        category: "channel",
        classification: "read",
        capabilityId: "cap:channel:list",
        execute: async () => ({})
      }),
      pattern("channel.remove", "channel.remove", "Remove a streaming channel", {
        patterns: [
          {
            regex: /(?:remove|delete|disconnect)\s+(?:my\s+)?(.+?)\s+(?:channel|feed)/,
            priority: 13,
            keywords: ["remove channel", "delete feed"],
            extract: (m) => ({ channelId: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["remove channel", "delete feed"],
        examples: ["remove my whatsapp channel", "delete channel ch:telegram:abc"],
        inputSchema: z5.object({
          channelId: z5.string()
        }),
        executor: "capability",
        category: "channel",
        classification: "destructive",
        requiresConfirmation: true,
        capabilityId: "cap:channel:remove",
        execute: async () => ({})
      })
    ];
    workflowPatterns = [
      pattern("workflow.newsletter", "workflow.newsletter", "Create a newsletter workflow", {
        patterns: [
          {
            regex: /(?:create|make)(?: an?)?(?: weekly | daily )?newsletter(?: for (.+))?/i,
            priority: 12,
            keywords: ["newsletter", "create newsletter"],
            extract: (m) => ({
              recipients: extractEmails(m[1] ?? ""),
              windowDays: 7
            })
          }
        ],
        aliases: ["create newsletter"],
        examples: ["create a weekly newsletter for team@x.io"],
        inputSchema: z5.object({
          recipients: z5.array(z5.string()).optional(),
          windowDays: z5.number().optional(),
          title: z5.string().optional()
        }),
        executor: "capability",
        category: "workflow",
        classification: "system",
        capabilityId: "cap:workflow:create_newsletter",
        execute: async () => ({})
      }),
      pattern("schedule.register", "schedule.register", "Register a schedule", {
        patterns: [
          {
            regex: /(?:every|each) (monday|tuesday|wednesday|thursday|friday|saturday|sunday|hour|day) (.+)/i,
            priority: 11,
            keywords: ["schedule", "every day", "every week"],
            extract: (m) => ({
              cron: dayToCron(m[1] ?? ""),
              action: "cap:workflow:create_newsletter"
            })
          }
        ],
        aliases: ["schedule", "automate"],
        examples: ["every monday send newsletter", "schedule daily summary"],
        inputSchema: z5.object({
          cron: z5.string(),
          action: z5.string(),
          actionConfig: z5.record(z5.unknown()).optional(),
          name: z5.string().optional()
        }),
        executor: "capability",
        category: "schedule",
        classification: "system",
        capabilityId: "cap:schedule:register",
        execute: async () => ({})
      })
    ];
    sessionPatterns = [
      pattern("session.load", "session.load", "Load an interactive session", {
        patterns: [
          {
            regex: /(?:load|start|open)\s+(?:my\s+)?(chatgpt|claude|gemini|gpt|ai)\s+(?:session|chat|conversation)/i,
            priority: 14,
            keywords: ["load session", "start chat"],
            extract: (m) => ({
              providerId: (m[1] ?? "").toLowerCase() === "gpt" ? "chatgpt" : (m[1] ?? "").toLowerCase()
            })
          }
        ],
        aliases: ["load session", "start chat", "open ai"],
        examples: ["load my chatgpt session", "start claude chat", "open ai conversation"],
        inputSchema: z5.object({
          providerId: z5.string(),
          accountId: z5.string().optional()
        }),
        executor: "capability",
        category: "session",
        classification: "system",
        capabilityId: "cap:session:load",
        execute: async () => ({})
      }),
      pattern("session.start", "session.start", "Start a new session", {
        patterns: [
          {
            regex: /(?:start|begin)\s+(?:a\s+)?new\s+(?:session|chat)/i,
            priority: 13,
            keywords: ["start session", "new chat"],
            extract: () => ({})
          }
        ],
        aliases: ["start session", "new chat"],
        examples: ["start a new session", "begin session on claude"],
        inputSchema: z5.object({
          providerId: z5.string().optional(),
          accountId: z5.string().optional()
        }),
        executor: "capability",
        category: "session",
        classification: "system",
        capabilityId: "cap:session:start",
        execute: async () => ({})
      }),
      pattern("session.list", "session.list", "List active sessions", {
        patterns: [
          {
            regex: /(?:list|show)\s+(?:my\s+)?(?:sessions|chats)/i,
            priority: 12,
            keywords: ["list sessions", "show chats"],
            extract: () => ({})
          }
        ],
        aliases: ["list sessions", "my sessions"],
        examples: ["list sessions", "show my chats"],
        inputSchema: z5.object({}),
        executor: "capability",
        category: "session",
        classification: "read",
        capabilityId: "cap:session:list",
        execute: async () => ({})
      })
    ];
    memoryPatterns = [
      pattern("memory.recall", "memory.recall", "Recall something you know", {
        patterns: [
          {
            regex: /(?:remember|recall|what\s+(?:do\s+you\s+know|is)\s+(?:about\s+)?(.+)|what\s+(?:is|was)\s+(?:the\s+)?(.+?)\s+you\s+told\s+me)/,
            priority: 12,
            keywords: ["recall", "remember", "what do you know"],
            extract: (m) => ({ topic: (m[1] ?? m[2] ?? "").trim() })
          }
        ],
        aliases: ["recall", "remember", "remind me"],
        examples: ["recall the secret key", "remember what we discussed about quantum computing"],
        inputSchema: z5.object({ topic: z5.string().optional() }),
        executor: "memory",
        category: "memory",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("memory.store", "memory.store", "Store something for later", {
        patterns: [
          {
            regex: /(?:remember|store|save)\s+(?:this\s+)?(?:key|secret|password|api\s+key)\s+(?:is\s+)?(.+)$/,
            priority: 11,
            keywords: ["remember", "store", "save"],
            extract: (m) => ({ key: "secret", value: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["store", "save", "remember"],
        examples: ["remember my api key is abc123", "store this password: hunter2"],
        inputSchema: z5.object({
          key: z5.string().optional(),
          value: z5.string().optional()
        }),
        executor: "memory",
        category: "memory",
        classification: "write",
        execute: async () => ({})
      })
    ];
    automationPatterns = [
      pattern("auto.research", "auto.research", "Research a topic and produce a report", {
        patterns: [
          {
            regex: /research\s+(?:a\s+|an\s+)?(.+?)(?:\s+report)?$/,
            priority: 12,
            keywords: ["research", "report", "investigate"],
            extract: (m) => ({
              role: "researcher",
              topic: (m[1] ?? "").trim(),
              queryUrl: (m[1] ?? "").trim()
            })
          }
        ],
        aliases: ["study", "investigate"],
        examples: ["research the economic AI crisis", "research quantum computing report"],
        executor: "generic-browser",
        category: "automation",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("auto.extract", "auto.extract", "Extract structured data from a URL", {
        patterns: [
          {
            regex: /extract\s+(?:from\s+)?(.+?)(?:\s+data)?$/,
            priority: 12,
            keywords: ["extract", "scrape", "pull"],
            extract: (m) => ({ role: "extractor", url: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["scrape", "pull"],
        examples: ["extract from https://example.com", "scrape the table"],
        executor: "generic-browser",
        category: "automation",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("auto.summarize", "auto.summarize", "Summarize / rewrite / translate a page", {
        patterns: [
          {
            regex: /(?:summar|rewrite|translate)\w*\s+(?:the\s+)?(.+?)$/,
            priority: 12,
            keywords: ["summarize", "tl;dr", "rewrite", "translate"],
            extract: (m) => ({ role: "synthesizer", url: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["tldr", "rewrite", "translate"],
        examples: ["summarize https://example.com", "translate this article"],
        executor: "generic-browser",
        category: "automation",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("auto.monitor", "auto.monitor", "Monitor a URL or selector for changes", {
        patterns: [
          {
            regex: /monitor\s+(?:the\s+)?(.+?)(?:\s+for\s+changes)?$/,
            priority: 12,
            keywords: ["monitor", "watch", "track"],
            extract: (m) => ({ role: "monitor", url: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["watch", "track"],
        examples: ["monitor https://shop.example.com", "watch the price"],
        executor: "generic-browser",
        category: "automation",
        classification: "read",
        execute: async () => ({})
      }),
      pattern("auto.test", "auto.test", "Run a UI smoke/regression test (human-gated if destructive)", {
        patterns: [
          {
            regex: /test\s+(?:the\s+)?(.+?)(?:\s+ui)?$/,
            priority: 12,
            keywords: ["test", "smoke", "regression"],
            extract: (m) => ({ role: "tester", url: (m[1] ?? "").trim() })
          }
        ],
        aliases: ["smoke", "regression"],
        examples: ["test https://app.example.com", "ui smoke test"],
        executor: "generic-browser",
        category: "automation",
        classification: "destructive",
        requiresConfirmation: true,
        execute: async () => ({})
      })
    ];
  }
});

// src/engines/nlcl/command-registry.ts
var CommandPatternRegistry;
var init_command_registry2 = __esm({
  "src/engines/nlcl/command-registry.ts"() {
    "use strict";
    init_errors();
    CommandPatternRegistry = class {
      patterns = /* @__PURE__ */ new Map();
      intentIndex = /* @__PURE__ */ new Map();
      categoryIndex = /* @__PURE__ */ new Map();
      surfaceIndex = /* @__PURE__ */ new Map();
      registerCallbacks = /* @__PURE__ */ new Set();
      register(pattern2) {
        if (this.patterns.has(pattern2.id)) {
          throw new EngineError(`CommandPattern ${pattern2.id} already registered`);
        }
        if (this.intentIndex.has(pattern2.intent)) {
          throw new EngineError(
            `Intent ${pattern2.intent} already registered by ${this.intentIndex.get(pattern2.intent)?.id}`
          );
        }
        this.patterns.set(pattern2.id, pattern2);
        this.intentIndex.set(pattern2.intent, pattern2);
        const catSet = this.categoryIndex.get(pattern2.category) ?? /* @__PURE__ */ new Set();
        catSet.add(pattern2.id);
        this.categoryIndex.set(pattern2.category, catSet);
        for (const surface of pattern2.surfaces) {
          const surfSet = this.surfaceIndex.get(surface) ?? /* @__PURE__ */ new Set();
          surfSet.add(pattern2.id);
          this.surfaceIndex.set(surface, surfSet);
        }
        for (const cb of this.registerCallbacks) {
          try {
            cb(pattern2);
          } catch {
          }
        }
      }
      unregister(id) {
        const pattern2 = this.patterns.get(id);
        if (!pattern2) throw new EngineError(`CommandPattern ${id} not found`);
        this.patterns.delete(id);
        this.intentIndex.delete(pattern2.intent);
        const catSet = this.categoryIndex.get(pattern2.category);
        if (catSet) {
          catSet.delete(id);
          if (catSet.size === 0) this.categoryIndex.delete(pattern2.category);
        }
        for (const surface of pattern2.surfaces) {
          const surfSet = this.surfaceIndex.get(surface);
          if (surfSet) {
            surfSet.delete(id);
            if (surfSet.size === 0) this.surfaceIndex.delete(surface);
          }
        }
      }
      get(id) {
        return this.patterns.get(id);
      }
      getByIntent(intent) {
        return this.intentIndex.get(intent);
      }
      /** Get pattern by id (alias for get for semantic clarity). */
      getPattern(id) {
        return this.patterns.get(id);
      }
      list(filter) {
        let ids;
        if (filter?.category) {
          ids = this.categoryIndex.get(filter.category);
          if (!ids) return [];
        }
        if (filter?.surface) {
          const surfIds = this.surfaceIndex.get(filter.surface);
          if (!surfIds) return [];
          ids = ids ? new Set([...ids].filter((id) => surfIds.has(id))) : surfIds;
        }
        let result;
        if (ids) {
          result = [...ids].map((id) => this.patterns.get(id)).filter((p) => p != null);
        } else {
          result = [...this.patterns.values()];
        }
        if (filter?.executor) {
          result = result.filter((p) => p.executor === filter.executor);
        }
        if (filter?.tag) {
          result = result.filter((p) => p.tags.includes(filter.tag));
        }
        return result;
      }
      listByCategory() {
        const result = {};
        for (const [category, ids] of this.categoryIndex) {
          result[category] = [...ids].map((id) => this.patterns.get(id)).filter((p) => p != null);
        }
        return result;
      }
      search(query) {
        const q = query.trim().toLowerCase();
        if (!q) return [];
        return [...this.patterns.values()].filter(
          (p) => p.id.toLowerCase().includes(q) || p.intent.toLowerCase().includes(q) || p.description.toLowerCase().includes(q) || p.aliases.some((a) => a.toLowerCase().includes(q)) || p.examples.some((e) => e.toLowerCase().includes(q)) || p.tags.some((t) => t.toLowerCase().includes(q))
        );
      }
      onRegister(callback) {
        this.registerCallbacks.add(callback);
        return () => {
          this.registerCallbacks.delete(callback);
        };
      }
      size() {
        return this.patterns.size;
      }
      exportForSurface(surface) {
        return this.list({ surface }).map((p) => ({
          id: p.id,
          intent: p.intent,
          description: p.description,
          examples: p.examples,
          aliases: p.aliases,
          category: p.category
        }));
      }
    };
  }
});

// src/engines/nlcl/executors/file-executor.ts
import { existsSync as existsSync4 } from "fs";
import { mkdir as mkdir3, readFile as readFile3, readdir as readdir3, stat, writeFile as writeFile3 } from "fs/promises";
import { platform } from "os";
import { basename, extname, join as join8 } from "path";
var _isWindows, FILE_OPEN_COMMANDS, SEARCH_LOCATIONS, FileExecutor;
var init_file_executor = __esm({
  "src/engines/nlcl/executors/file-executor.ts"() {
    "use strict";
    init_ids();
    _isWindows = platform() === "win32";
    FILE_OPEN_COMMANDS = {
      win32: "explorer",
      darwin: "open",
      linux: "xdg-open"
    };
    SEARCH_LOCATIONS = {
      win32: ["Documents", "Desktop", "Downloads", "Pictures"],
      darwin: ["Documents", "Desktop", "Downloads", "Pictures"],
      linux: ["Documents", "Desktop", "Downloads", "Pictures"]
    };
    FileExecutor = class {
      id = "file";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          switch (intent.intent) {
            case "file.open":
              return await this.openFile(intent, ctx, traceId, start);
            case "file.list":
              return await this.listFiles(intent, ctx, traceId, start);
            case "file.search":
              return await this.searchFiles(intent, ctx, traceId, start);
            case "file.create":
              return await this.createFile(intent, ctx, traceId, start);
            case "file.read":
              return await this.readFile(intent, ctx, traceId, start);
            default:
              return this.fail(intent, traceId, start, `Unknown file intent: ${intent.intent}`);
          }
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      async openFile(intent, ctx, traceId, start) {
        const fileName = intent.input.name;
        const filePath = intent.input.path;
        if (filePath) {
          const resolved = this.resolvePath(filePath, ctx);
          if (!existsSync4(resolved)) {
            return this.fail(intent, traceId, start, `File not found: ${filePath}`);
          }
          await this.launchFile(resolved);
          return {
            ok: true,
            intent: intent.intent,
            output: { path: resolved, opened: true },
            text: `Opened ${basename(resolved)}`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "read"
          };
        }
        if (fileName) {
          const found = await this.findFile(fileName, ctx);
          if (!found) {
            return this.fail(intent, traceId, start, `Could not find "${fileName}" in common locations`);
          }
          await this.launchFile(found);
          return {
            ok: true,
            intent: intent.intent,
            output: { path: found, opened: true },
            text: `Opened ${basename(found)}`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "read"
          };
        }
        return this.fail(intent, traceId, start, "No file name or path specified");
      }
      async listFiles(intent, _ctx, traceId, start) {
        const folder = intent.input.folder ?? "Documents";
        const home = this.getHomeDir();
        const locations = SEARCH_LOCATIONS[platform()] ?? ["Documents"];
        const targetFolder = locations.find((l) => l.toLowerCase() === folder.toLowerCase()) ?? folder;
        const dirPath = join8(home, targetFolder);
        if (!existsSync4(dirPath)) {
          return this.fail(intent, traceId, start, `Folder not found: ${targetFolder}`);
        }
        const entries = await readdir3(dirPath, { withFileTypes: true });
        const files = entries.filter((e) => e.isFile()).map((e) => ({ name: e.name, extension: extname(e.name) })).slice(0, 50);
        const dirs = entries.filter((e) => e.isDirectory()).map((e) => e.name).slice(0, 20);
        return {
          ok: true,
          intent: intent.intent,
          output: { folder: targetFolder, files, dirs, totalFiles: files.length },
          text: `${files.length} files in ${targetFolder}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      async searchFiles(intent, _ctx, traceId, start) {
        const query = intent.input.query ?? intent.input.name ?? "";
        if (!query) {
          return this.fail(intent, traceId, start, "No search query specified");
        }
        const home = this.getHomeDir();
        const locations = SEARCH_LOCATIONS[platform()] ?? ["Documents"];
        const results = [];
        for (const loc of locations) {
          const dirPath = join8(home, loc);
          if (!existsSync4(dirPath)) continue;
          try {
            const entries = await readdir3(dirPath, { withFileTypes: true });
            for (const entry of entries) {
              if (entry.name.toLowerCase().includes(query.toLowerCase())) {
                const fullPath = join8(dirPath, entry.name);
                let size;
                try {
                  size = (await stat(fullPath)).size;
                } catch {
                }
                results.push({ path: fullPath, name: entry.name, size });
              }
            }
          } catch {
          }
        }
        return {
          ok: true,
          intent: intent.intent,
          output: { query, results: results.slice(0, 20), totalFound: results.length },
          text: results.length > 0 ? `Found ${results.length} file(s) matching "${query}"` : `No files found matching "${query}"`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      async createFile(intent, _ctx, traceId, start) {
        const name = intent.input.name;
        const content = intent.input.content ?? "";
        const folder = intent.input.folder ?? "Documents";
        if (!name) {
          return this.fail(intent, traceId, start, "No file name specified");
        }
        const home = this.getHomeDir();
        const locations = SEARCH_LOCATIONS[platform()] ?? ["Documents"];
        const targetFolder = locations.find((l) => l.toLowerCase() === folder.toLowerCase()) ?? folder;
        const dirPath = join8(home, targetFolder);
        const filePath = join8(dirPath, name);
        if (!existsSync4(dirPath)) {
          await mkdir3(dirPath, { recursive: true });
        }
        await writeFile3(filePath, content, "utf-8");
        return {
          ok: true,
          intent: intent.intent,
          output: { path: filePath, name, created: true },
          text: `Created ${name} in ${targetFolder}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "write"
        };
      }
      async readFile(intent, ctx, traceId, start) {
        const name = intent.input.name;
        if (!name) {
          return this.fail(intent, traceId, start, "No file name specified");
        }
        const found = await this.findFile(name, ctx);
        if (!found) {
          return this.fail(intent, traceId, start, `File not found: ${name}`);
        }
        const content = await readFile3(found, "utf-8");
        const truncated = content.length > 1e4 ? `${content.slice(0, 1e4)}... (truncated)` : content;
        return {
          ok: true,
          intent: intent.intent,
          output: { path: found, content: truncated, size: content.length },
          text: truncated.slice(0, 500),
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      // ── Helpers ─────────────────────────────────────────────────────────────
      getHomeDir() {
        return process.env.HOME ?? process.env.USERPROFILE ?? ".";
      }
      resolvePath(path, ctx) {
        if (path.startsWith("~")) {
          return join8(this.getHomeDir(), path.slice(1));
        }
        if (ctx.workspacePath && !path.startsWith("/") && !path.match(/^[A-Z]:/i)) {
          return join8(ctx.workspacePath, path);
        }
        return path;
      }
      async findFile(name, ctx) {
        const home = this.getHomeDir();
        const locations = SEARCH_LOCATIONS[platform()] ?? ["Documents"];
        if (existsSync4(name)) return name;
        if (ctx.workspacePath && existsSync4(join8(ctx.workspacePath, name))) {
          return join8(ctx.workspacePath, name);
        }
        const lowerName = name.toLowerCase();
        for (const loc of locations) {
          const dirPath = join8(home, loc);
          if (!existsSync4(dirPath)) continue;
          try {
            const entries = await readdir3(dirPath, { withFileTypes: true });
            for (const entry of entries) {
              if (entry.name.toLowerCase() === lowerName || entry.name.toLowerCase().replace(/\.[^.]+$/, "") === lowerName) {
                return join8(dirPath, entry.name);
              }
              if (entry.name.toLowerCase().includes(lowerName)) {
                return join8(dirPath, entry.name);
              }
            }
          } catch {
          }
        }
        return null;
      }
      async launchFile(filePath) {
        const _ext = extname(filePath).toLowerCase();
        const cmd = FILE_OPEN_COMMANDS[platform()] ?? "xdg-open";
        const { exec } = await import("child_process");
        exec(`${cmd} "${filePath}"`, (err) => {
          if (err) console.error(`[FileExecutor] Failed to launch ${filePath}:`, err.message);
        });
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/browser-executor.ts
var BrowserExecutor;
var init_browser_executor = __esm({
  "src/engines/nlcl/executors/browser-executor.ts"() {
    "use strict";
    init_ids();
    BrowserExecutor = class {
      constructor(governor, conversationManager) {
        this.governor = governor;
        this.conversationManager = conversationManager;
      }
      governor;
      conversationManager;
      id = "browser";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          switch (intent.intent) {
            case "browser.navigate":
              return await this.navigate(intent, ctx, traceId, start);
            case "browser.search":
              return await this.search(intent, ctx, traceId, start);
            case "browser.extract":
              return await this.extract(intent, ctx, traceId, start);
            case "browser.screenshot":
              return await this.screenshot(intent, ctx, traceId, start);
            case "browser.open":
              return await this.openBrowser(intent, ctx, traceId, start);
            default:
              return this.fail(intent, traceId, start, `Unknown browser intent: ${intent.intent}`);
          }
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      async navigate(intent, ctx, traceId, start) {
        const url = this.normalizeUrl(intent.input.url);
        if (!url) {
          return this.fail(intent, traceId, start, "No URL specified");
        }
        if (this.governor && ctx.slaveId) {
          await this.governor.cdp.send(ctx.slaveId, "Page.navigate", { url });
          return {
            ok: true,
            intent: intent.intent,
            output: { url, navigated: true },
            text: `Navigated to ${url}`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "navigate"
          };
        }
        await this.openExternal(url);
        return {
          ok: true,
          intent: intent.intent,
          output: { url, opened: true },
          text: `Opened ${url} in default browser`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "navigate"
        };
      }
      async search(intent, ctx, traceId, start) {
        const query = intent.input.query;
        if (!query) {
          return this.fail(intent, traceId, start, "No search query specified");
        }
        const engine = intent.input.engine ?? "google";
        const searchUrls = {
          google: `https://www.google.com/search?q=${encodeURIComponent(query)}`,
          bing: `https://www.bing.com/search?q=${encodeURIComponent(query)}`,
          duckduckgo: `https://duckduckgo.com/?q=${encodeURIComponent(query)}`,
          youtube: `https://www.youtube.com/results?search_query=${encodeURIComponent(query)}`
        };
        const url = searchUrls[engine.toLowerCase()] ?? searchUrls.google ?? "https://google.com";
        if (this.governor && ctx.slaveId) {
          await this.governor.cdp.send(ctx.slaveId, "Page.navigate", { url });
        } else {
          await this.openExternal(url);
        }
        return {
          ok: true,
          intent: intent.intent,
          output: { query, engine, url },
          text: `Searching "${query}" on ${engine}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "navigate"
        };
      }
      async extract(intent, ctx, traceId, start) {
        if (!this.governor || !ctx.slaveId) {
          return this.fail(intent, traceId, start, "Browser not available \u2014 no governor/slave");
        }
        const pageState = await this.governor.cdp.getPageState(ctx.slaveId);
        let bodyText = "";
        try {
          const result = await this.governor.cdp.send(ctx.slaveId, "Runtime.evaluate", {
            expression: 'document.body ? document.body.innerText : ""',
            returnByValue: true
          });
          bodyText = result?.result?.value ?? "";
        } catch {
        }
        const truncated = bodyText.length > 5e4 ? `${bodyText.slice(0, 5e4)}... (truncated)` : bodyText;
        return {
          ok: true,
          intent: intent.intent,
          output: {
            url: pageState?.url,
            title: pageState?.title,
            text: truncated,
            length: bodyText.length
          },
          text: `Extracted ${bodyText.length} characters from ${pageState?.title ?? "page"}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      async screenshot(intent, ctx, traceId, start) {
        if (!this.governor || !ctx.slaveId) {
          return this.fail(intent, traceId, start, "Browser not available \u2014 no governor/slave");
        }
        const result = await this.governor.cdp.send(ctx.slaveId, "Page.captureScreenshot", {
          format: "png"
        });
        const data = result?.data;
        return {
          ok: true,
          intent: intent.intent,
          output: { captured: true, format: "png", dataLength: data?.length ?? 0 },
          text: "Screenshot captured",
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      async openBrowser(intent, ctx, traceId, start) {
        const url = intent.input.url ? this.normalizeUrl(intent.input.url) : void 0;
        if (this.governor && ctx.providerId && ctx.accountId) {
          const slave = await this.governor.ensureRunningForAccount(ctx.providerId, ctx.accountId);
          if (url) {
            await this.governor.cdp.send(slave.slaveId, "Page.navigate", { url });
          }
          return {
            ok: true,
            intent: intent.intent,
            output: { slaveId: slave.slaveId, url },
            text: `Chrome opened (slave: ${slave.slaveId})`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "navigate"
          };
        }
        if (url) {
          await this.openExternal(url);
        } else {
          await this.openExternal("about:blank");
        }
        return {
          ok: true,
          intent: intent.intent,
          output: { opened: true },
          text: "Browser opened",
          latencyMs: Date.now() - start,
          traceId,
          classification: "navigate"
        };
      }
      // ── Helpers ─────────────────────────────────────────────────────────────
      normalizeUrl(url) {
        if (!url) return "";
        if (url.match(/^https?:\/\//i)) return url;
        if (url.match(/^[\w-]+\.[\w.-]+/)) return `https://${url}`;
        return `https://${url}`;
      }
      async openExternal(url) {
        const { platform: platform3 } = await import("os");
        const os = platform3();
        const cmd = os === "win32" ? "start" : os === "darwin" ? "open" : "xdg-open";
        const { exec } = await import("child_process");
        const fullCmd = os === "win32" ? `${cmd} "" "${url}"` : `${cmd} "${url}"`;
        exec(fullCmd, (err) => {
          if (err) console.error(`[BrowserExecutor] Failed to open ${url}:`, err.message);
        });
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/provider-llm-executor.ts
var ProviderLLMExecutor;
var init_provider_llm_executor = __esm({
  "src/engines/nlcl/executors/provider-llm-executor.ts"() {
    "use strict";
    init_ids();
    ProviderLLMExecutor = class {
      constructor(conversationManager, conversationStore) {
        this.conversationManager = conversationManager;
        this.conversationStore = conversationStore;
      }
      conversationManager;
      conversationStore;
      id = "provider-llm";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          const prompt = this.buildPrompt(intent, ctx);
          if (!prompt) {
            return this.fail(intent, traceId, start, "Could not build prompt from command");
          }
          const providerId = intent.input.providerId ?? ctx.providerId;
          if (!providerId) {
            return this.fail(intent, traceId, start, "No provider specified");
          }
          if (!this.conversationManager) {
            return this.fail(intent, traceId, start, "ConversationManager not available");
          }
          let conversationId = ctx.conversationId;
          if (!conversationId && this.conversationStore) {
            const conv = await this.conversationManager.createConversation(
              providerId,
              `NLCL: ${prompt.slice(0, 50)}`
            );
            conversationId = conv.id;
          }
          if (!conversationId) {
            return this.fail(intent, traceId, start, "No conversation available");
          }
          const result = await this.conversationManager.send(conversationId, prompt);
          return {
            ok: result.ok,
            intent: intent.intent,
            output: {
              providerId,
              conversationId,
              messageId: result.messageId,
              response: result.text,
              blocks: result.blocks
            },
            text: result.text || result.error || "No response",
            error: result.error,
            latencyMs: Date.now() - start,
            traceId,
            classification: "communication",
            followUp: result.ok ? void 0 : "Provider may be unavailable. Try a different provider."
          };
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      buildPrompt(intent, _ctx) {
        const action = intent.intent;
        const input = intent.input;
        switch (action) {
          case "provider.query":
          case "llm.ask": {
            return input.prompt ?? input.query ?? input.message ?? "";
          }
          case "llm.summarize": {
            const content = input.content;
            const topic = input.topic;
            if (content) {
              return topic ? `Please summarize the following content, focusing on ${topic}:

${content}` : `Please summarize the following content:

${content}`;
            }
            return topic ? `Please summarize the latest news about ${topic}` : "Please summarize the current page content";
          }
          case "llm.translate": {
            const text = input.text;
            const targetLang = input.targetLanguage;
            const sourceLang = input.sourceLanguage;
            const sourcePart = sourceLang ? `from ${sourceLang} ` : "";
            return `Please translate the following ${sourcePart}to ${targetLang}:

${text}`;
          }
          case "llm.explain": {
            const topic = input.topic;
            const content = input.content;
            return content ? `Please explain this:

${content}` : `Please explain ${topic}`;
          }
          case "llm.rewrite": {
            const text = input.text;
            const style = input.style;
            return style ? `Please rewrite the following in a ${style} style:

${text}` : `Please rewrite the following:

${text}`;
          }
          case "llm.code": {
            const task = input.task;
            const language = input.language;
            return language ? `Write ${language} code that: ${task}` : `Write code that: ${task}`;
          }
          case "browser.summarize":
          case "web.summarize": {
            const url = input.url;
            const content = input.content;
            if (content) {
              return `Please summarize the following web page content:

${content}`;
            }
            return url ? `Please read and summarize the content from ${url}` : "Please summarize the current page";
          }
          default: {
            const raw = input.raw ?? input.prompt ?? intent.rawInput;
            return raw;
          }
        }
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "communication"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/system-executor.ts
var SystemExecutor;
var init_system_executor = __esm({
  "src/engines/nlcl/executors/system-executor.ts"() {
    "use strict";
    init_ids();
    SystemExecutor = class {
      constructor(db, governor, registry2) {
        this.db = db;
        this.governor = governor;
        this.registry = registry2;
      }
      db;
      governor;
      registry;
      id = "system";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          switch (intent.intent) {
            case "system.health":
              return await this.health(intent, traceId, start);
            case "system.providers":
              return await this.listProviders(intent, traceId, start);
            case "system.fleet":
              return await this.fleetStatus(intent, traceId, start);
            case "system.capabilities":
              return await this.listCapabilities(intent, traceId, start);
            case "system.version":
              return {
                ok: true,
                intent: intent.intent,
                output: { version: "1.0.0", uptime: process.uptime() },
                text: `vivim v1.0.0 (uptime: ${Math.round(process.uptime())}s)`,
                latencyMs: Date.now() - start,
                traceId,
                classification: "system"
              };
            case "system.workspace":
              return {
                ok: true,
                intent: intent.intent,
                output: { workspace: ctx.workspacePath ?? process.cwd() },
                text: `Workspace: ${ctx.workspacePath ?? process.cwd()}`,
                latencyMs: Date.now() - start,
                traceId,
                classification: "system"
              };
            default:
              return this.fail(intent, traceId, start, `Unknown system intent: ${intent.intent}`);
          }
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      async health(intent, traceId, start) {
        const memUsage = process.memoryUsage();
        return {
          ok: true,
          intent: intent.intent,
          output: {
            status: "ok",
            uptime: process.uptime(),
            memory: {
              rss: Math.round(memUsage.rss / 1024 / 1024),
              heapUsed: Math.round(memUsage.heapUsed / 1024 / 1024),
              heapTotal: Math.round(memUsage.heapTotal / 1024 / 1024)
            }
          },
          text: "System healthy",
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
      async listProviders(intent, traceId, start) {
        if (!this.db) {
          return this.fail(intent, traceId, start, "Database not available");
        }
        const providers = await this.db.prisma.providerDefinition.findMany({
          select: { id: true, slug: true, displayName: true }
        });
        const providerList = providers.map((p) => `${p.slug} (${p.displayName})`).join(", ");
        return {
          ok: true,
          intent: intent.intent,
          output: { providers, count: providers.length },
          text: providers.length > 0 ? `Available providers: ${providerList}` : "No providers registered",
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
      async fleetStatus(intent, traceId, start) {
        if (!this.governor) {
          return this.fail(intent, traceId, start, "Governor not available");
        }
        const slaves = this.governor.getAllSlaves();
        return {
          ok: true,
          intent: intent.intent,
          output: { slaves, count: slaves.length },
          text: slaves.length > 0 ? `${slaves.length} Chrome slave(s) running` : "No Chrome slaves running",
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
      async listCapabilities(intent, traceId, start) {
        if (!this.registry) {
          return this.fail(intent, traceId, start, "Registry not available");
        }
        const caps = this.registry.list();
        return {
          ok: true,
          intent: intent.intent,
          output: {
            capabilities: caps.map((c) => ({ slug: c.slug, name: c.name, category: c.category })),
            count: caps.length
          },
          text: `${caps.length} capabilities registered`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/conversation-executor.ts
var ConversationExecutor;
var init_conversation_executor = __esm({
  "src/engines/nlcl/executors/conversation-executor.ts"() {
    "use strict";
    init_ids();
    ConversationExecutor = class {
      constructor(conversationManager) {
        this.conversationManager = conversationManager;
      }
      conversationManager;
      id = "conversation";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        if (!this.conversationManager) {
          return this.fail(intent, traceId, start, "ConversationManager not available");
        }
        try {
          switch (intent.intent) {
            case "conversation.create":
              return await this.create(this.conversationManager, intent, ctx, traceId, start);
            case "conversation.list":
              return await this.list(this.conversationManager, intent, ctx, traceId, start);
            case "conversation.send":
              return await this.send(this.conversationManager, intent, ctx, traceId, start);
            case "conversation.switch":
              return await this.switchProvider(this.conversationManager, intent, ctx, traceId, start);
            case "conversation.messages":
              return await this.messages(this.conversationManager, intent, ctx, traceId, start);
            default:
              return this.fail(intent, traceId, start, `Unknown conversation intent: ${intent.intent}`);
          }
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      async create(cm, intent, ctx, traceId, start) {
        const providerId = intent.input.providerId ?? ctx.providerId ?? "chatgpt";
        const title = intent.input.title;
        const conv = await cm.createConversation(providerId, title);
        return {
          ok: true,
          intent: intent.intent,
          output: { conversationId: conv.id, providerId },
          text: `New ${providerId} conversation created`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
      async list(cm, intent, _ctx, traceId, start) {
        const limit = intent.input.limit ?? 20;
        const _convs = await cm.getMessages;
        const store = cm.store;
        const conversations = store?.listConversations ? await store.listConversations(limit) : [];
        return {
          ok: true,
          intent: intent.intent,
          output: { conversations, count: Array.isArray(conversations) ? conversations.length : 0 },
          text: `${Array.isArray(conversations) ? conversations.length : 0} conversations`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      async send(cm, intent, ctx, traceId, start) {
        const conversationId = intent.input.conversationId ?? ctx.conversationId;
        const message = intent.input.message;
        if (!conversationId) {
          return this.fail(intent, traceId, start, "No conversation specified");
        }
        if (!message) {
          return this.fail(intent, traceId, start, "No message specified");
        }
        const result = await cm.send(conversationId, message);
        return {
          ok: result.ok,
          intent: intent.intent,
          output: { messageId: result.messageId, text: result.text, blocks: result.blocks },
          text: result.text || result.error || "No response",
          error: result.error,
          latencyMs: Date.now() - start,
          traceId,
          classification: "communication"
        };
      }
      async switchProvider(cm, intent, _ctx, traceId, start) {
        const providerId = intent.input.providerId;
        if (!providerId) {
          return this.fail(intent, traceId, start, "No provider specified");
        }
        const conv = await cm.createConversation(providerId, `Switched to ${providerId}`);
        return {
          ok: true,
          intent: intent.intent,
          output: { conversationId: conv.id, providerId },
          text: `Switched to ${providerId}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "navigate"
        };
      }
      async messages(cm, intent, ctx, traceId, start) {
        const conversationId = intent.input.conversationId ?? ctx.conversationId;
        if (!conversationId) {
          return this.fail(intent, traceId, start, "No conversation specified");
        }
        const limit = intent.input.limit ?? 50;
        const messages = await cm.getMessages(conversationId, { limit });
        return {
          ok: true,
          intent: intent.intent,
          output: { messages, count: messages.length },
          text: `${messages.length} messages`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "read"
        };
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/capability-executor.ts
var CapabilityExecutor;
var init_capability_executor = __esm({
  "src/engines/nlcl/executors/capability-executor.ts"() {
    "use strict";
    init_ids();
    CapabilityExecutor = class {
      constructor(registry2) {
        this.registry = registry2;
      }
      registry;
      id = "capability";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        if (!this.registry) {
          return this.fail(intent, traceId, start, "Capability registry not available");
        }
        try {
          const capabilityId = intent.input.capabilityId ?? intent.input.slug;
          if (!capabilityId) {
            return this.fail(intent, traceId, start, "No capability specified");
          }
          const cap = this.registry.get(capabilityId) ?? this.registry.getBySlug(capabilityId);
          if (!cap) {
            return this.fail(intent, traceId, start, `Capability not found: ${capabilityId}`);
          }
          const capCtx = {
            conversationId: ctx.conversationId,
            providerId: ctx.providerId,
            slaveId: ctx.slaveId,
            userId: ctx.userId,
            metadata: ctx.metadata
          };
          const input = { ...intent.input };
          input.capabilityId = void 0;
          input.slug = void 0;
          const result = await this.registry.execute(cap.id, input, capCtx);
          return {
            ok: true,
            intent: intent.intent,
            output: result,
            text: typeof result === "string" ? result : `${cap.name} executed`,
            latencyMs: Date.now() - start,
            traceId,
            classification: cap.requiresConfirmation ? "write" : "read"
          };
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/email-executor.ts
var EmailExecutor;
var init_email_executor = __esm({
  "src/engines/nlcl/executors/email-executor.ts"() {
    "use strict";
    init_ids();
    EmailExecutor = class {
      id = "email";
      mailAdapter = null;
      setMailAdapter(adapter) {
        this.mailAdapter = adapter;
      }
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          switch (intent.intent) {
            case "email.send":
              return await this.sendEmail(intent, ctx, traceId, start);
            case "email.compose":
              return await this.composeEmail(intent, ctx, traceId, start);
            default:
              return this.fail(intent, traceId, start, `Unknown email intent: ${intent.intent}`);
          }
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      async sendEmail(intent, _ctx, traceId, start) {
        const to = intent.input.to;
        const subject = intent.input.subject ?? "No Subject";
        const body = intent.input.body ?? "";
        if (!to) {
          return this.fail(intent, traceId, start, "No recipient specified");
        }
        if (this.mailAdapter) {
          const result = await this.mailAdapter.send({ to, subject, body });
          return {
            ok: result.ok,
            intent: intent.intent,
            output: { to, subject, messageId: result.messageId },
            text: result.ok ? `Email sent to ${to}` : `Failed to send: ${result.error}`,
            error: result.error,
            latencyMs: Date.now() - start,
            traceId,
            classification: "communication"
          };
        }
        const mailtoUrl = `mailto:${to}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
        await this.openExternal(mailtoUrl);
        return {
          ok: true,
          intent: intent.intent,
          output: { to, subject, via: "mailto" },
          text: `Email draft opened for ${to}`,
          latencyMs: Date.now() - start,
          traceId,
          classification: "communication",
          followUp: "Mail client opened. Review and send manually."
        };
      }
      async composeEmail(intent, _ctx, traceId, start) {
        const to = intent.input.to ?? "";
        const subject = intent.input.subject ?? "";
        const body = intent.input.body ?? "";
        const mailtoUrl = `mailto:${to}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
        await this.openExternal(mailtoUrl);
        return {
          ok: true,
          intent: intent.intent,
          output: { to, subject, via: "mailto" },
          text: "Email composer opened",
          latencyMs: Date.now() - start,
          traceId,
          classification: "communication"
        };
      }
      async openExternal(url) {
        const { platform: platform3 } = await import("os");
        const os = platform3();
        const cmd = os === "win32" ? "start" : os === "darwin" ? "open" : "xdg-open";
        const { exec } = await import("child_process");
        const fullCmd = os === "win32" ? `${cmd} "" "${url}"` : `${cmd} "${url}"`;
        exec(fullCmd, (err) => {
          if (err) console.error(`[EmailExecutor] Failed to open ${url}:`, err.message);
        });
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "communication"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/app-executor.ts
import { platform as platform2 } from "os";
var APP_MAP, AppExecutor;
var init_app_executor = __esm({
  "src/engines/nlcl/executors/app-executor.ts"() {
    "use strict";
    init_ids();
    APP_MAP = {
      win32: {
        notepad: ["notepad.exe"],
        calculator: ["calc.exe"],
        calc: ["calc.exe"],
        terminal: ["wt.exe", "cmd.exe"],
        cmd: ["cmd.exe"],
        powershell: ["powershell.exe"],
        explorer: ["explorer.exe"],
        taskmanager: ["taskmgr.exe"],
        paint: ["mspaint.exe"],
        word: ["winword.exe"],
        excel: ["excel.exe"],
        chrome: ["chrome.exe"],
        edge: ["msedge.exe"],
        settings: ["start", "ms-settings:"],
        snipping: ["snippingtool.exe"],
        "snip & sketch": ["ms-screenclip:"]
      },
      darwin: {
        calculator: ["open", "-a", "Calculator"],
        calc: ["open", "-a", "Calculator"],
        terminal: ["open", "-a", "Terminal"],
        textedit: ["open", "-a", "TextEdit"],
        notepad: ["open", "-a", "TextEdit"],
        safari: ["open", "-a", "Safari"],
        chrome: ["open", "-a", "Google Chrome"],
        finder: ["open", "-a", "Finder"],
        notes: ["open", "-a", "Notes"],
        mail: ["open", "-a", "Mail"],
        calendar: ["open", "-a", "Calendar"],
        "app store": ["open", "-a", "App Store"],
        activity: ["open", "-a", "Activity Monitor"],
        screenshot: ["screencapture", "-i"]
      },
      linux: {
        terminal: ["x-terminal-emulator"],
        file: ["nautilus"],
        files: ["nautilus"],
        calculator: ["gnome-calculator"],
        calc: ["gnome-calculator"],
        text: ["gedit"],
        notepad: ["gedit"],
        chrome: ["google-chrome"],
        firefox: ["firefox"],
        settings: ["gnome-control-center"]
      }
    };
    AppExecutor = class {
      id = "app";
      async execute(intent, _ctx) {
        const start = Date.now();
        const traceId = newId();
        try {
          const appName = (intent.input.app ?? intent.input.name ?? "").toLowerCase().trim();
          if (!appName) {
            return this.fail(intent, traceId, start, "No application specified");
          }
          const os = platform2();
          const appMap = APP_MAP[os] ?? APP_MAP.linux;
          if (!appMap) {
            return this.fail(intent, traceId, start, `No app map for platform: ${os}`);
          }
          const cmdParts = appMap[appName];
          if (!cmdParts) {
            const available = Object.keys(appMap).join(", ");
            return this.fail(
              intent,
              traceId,
              start,
              `Unknown app "${appName}". Available: ${available}`
            );
          }
          const { exec } = await import("child_process");
          const exe = cmdParts[0] ?? appName;
          const args = cmdParts.slice(1);
          exec(`${exe} ${args.join(" ")}`, (err) => {
            if (err) console.error(`[AppExecutor] Failed to launch ${appName}:`, err.message);
          });
          return {
            ok: true,
            intent: intent.intent,
            output: { app: appName, launched: true, command: cmdParts.join(" ") },
            text: `Launched ${appName}`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "system"
          };
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: "system"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/workflow-executor.ts
var WorkflowExecutor;
var init_workflow_executor = __esm({
  "src/engines/nlcl/executors/workflow-executor.ts"() {
    "use strict";
    init_ids();
    WorkflowExecutor = class {
      constructor(registry2) {
        this.registry = registry2;
      }
      registry;
      id = "workflow";
      async execute(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        if (!this.registry) {
          return {
            ok: false,
            intent: intent.intent,
            error: "Capability registry not available",
            latencyMs: Date.now() - start,
            traceId,
            classification: "system"
          };
        }
        try {
          const capabilityId = intent.input.capabilityId ?? intent.input.slug;
          if (!capabilityId) {
            return {
              ok: false,
              intent: intent.intent,
              error: "No capability specified",
              latencyMs: Date.now() - start,
              traceId,
              classification: "system"
            };
          }
          const cap = this.registry.get(capabilityId) ?? this.registry.getBySlug(capabilityId);
          if (!cap) {
            return {
              ok: false,
              intent: intent.intent,
              error: `Capability not found: ${capabilityId}`,
              latencyMs: Date.now() - start,
              traceId,
              classification: "system"
            };
          }
          const capCtx = {
            conversationId: ctx.conversationId,
            providerId: ctx.providerId,
            slaveId: ctx.slaveId,
            userId: ctx.userId,
            metadata: ctx.metadata
          };
          const input = { ...intent.input };
          input.capabilityId = void 0;
          input.slug = void 0;
          const output = await this.registry.execute(cap.id, input, capCtx);
          return {
            ok: true,
            intent: intent.intent,
            output,
            latencyMs: Date.now() - start,
            traceId,
            classification: "system",
            capabilityId: cap.id
          };
        } catch (err) {
          return {
            ok: false,
            intent: intent.intent,
            error: err instanceof Error ? err.message : String(err),
            latencyMs: Date.now() - start,
            traceId,
            classification: "system"
          };
        }
      }
    };
  }
});

// src/engines/nlcl/executors/generic-browser-executor.ts
var GenericBrowserExecutor;
var init_generic_browser_executor = __esm({
  "src/engines/nlcl/executors/generic-browser-executor.ts"() {
    "use strict";
    init_ids();
    GenericBrowserExecutor = class {
      constructor(governor, orchestrator) {
        this.governor = governor;
        this.orchestrator = orchestrator;
      }
      governor;
      orchestrator;
      id = "generic-browser";
      async execute(intent, _ctx) {
        const start = Date.now();
        const traceId = newId();
        if (!this.orchestrator) {
          return this.fail(intent, traceId, start, "Automation orchestrator not configured");
        }
        try {
          const params = intent.input ?? {};
          const role = params.role ?? this.inferRole(intent.intent);
          const recipeId = params.recipeId;
          const destructive = params.destructive === "true" || intent.classification === "destructive";
          const result = await this.orchestrator.run({
            role,
            recipeId,
            intent: intent.intent,
            params,
            destructive
          });
          return {
            ok: true,
            intent: intent.intent,
            output: result,
            text: `Ran ${role} via ${result.recipeId} (${result.steps} steps)`,
            latencyMs: Date.now() - start,
            traceId,
            classification: intent.classification ?? "read"
          };
        } catch (err) {
          return this.fail(intent, traceId, start, err instanceof Error ? err.message : String(err));
        }
      }
      /** Map a raw automation intent to a default agent role. */
      inferRole(intent) {
        if (intent.includes("research") || intent.includes("report")) return "researcher";
        if (intent.includes("extract") || intent.includes("scrape")) return "extractor";
        if (intent.includes("summar") || intent.includes("rewrite") || intent.includes("translate"))
          return "synthesizer";
        if (intent.includes("monitor") || intent.includes("watch")) return "monitor";
        if (intent.includes("test") || intent.includes("smoke")) return "tester";
        return "researcher";
      }
      fail(intent, traceId, start, error2) {
        return {
          ok: false,
          intent: intent.intent,
          error: error2,
          latencyMs: Date.now() - start,
          traceId,
          classification: intent.classification ?? "read"
        };
      }
    };
  }
});

// src/engines/nlcl/executors/index.ts
var init_executors = __esm({
  "src/engines/nlcl/executors/index.ts"() {
    "use strict";
    init_file_executor();
    init_browser_executor();
    init_provider_llm_executor();
    init_system_executor();
    init_conversation_executor();
    init_capability_executor();
    init_email_executor();
    init_app_executor();
    init_workflow_executor();
    init_generic_browser_executor();
  }
});

// src/engines/nlcl/fuzzy-matcher.ts
function jaroWinkler(a, b, prefixScale = 0.1) {
  const s1 = a.toLowerCase();
  const s2 = b.toLowerCase();
  if (s1 === s2) return 1;
  const len1 = s1.length;
  const len2 = s2.length;
  if (len1 === 0 || len2 === 0) return 0;
  const matchDistance = Math.floor(Math.max(len1, len2) / 2) - 1;
  const matchDistanceClamped = matchDistance < 0 ? 0 : matchDistance;
  const s1Matches = new Array(len1).fill(false);
  const s2Matches = new Array(len2).fill(false);
  let matches = 0;
  let transpositions = 0;
  for (let i = 0; i < len1; i++) {
    const start = Math.max(0, i - matchDistanceClamped);
    const end = Math.min(i + matchDistanceClamped + 1, len2);
    for (let j = start; j < end; j++) {
      if (s2Matches[j]) continue;
      if (s1[i] !== s2[j]) continue;
      s1Matches[i] = true;
      s2Matches[j] = true;
      matches++;
      break;
    }
  }
  if (matches === 0) return 0;
  let k = 0;
  for (let i = 0; i < len1; i++) {
    if (!s1Matches[i]) continue;
    while (k < s2Matches.length && !s2Matches[k]) k++;
    if (s1[i] !== s2[k]) transpositions++;
    k++;
  }
  const jaro = (matches / len1 + matches / len2 + (matches - transpositions / 2) / matches) / 3;
  let prefix = 0;
  const maxPrefix = Math.min(4, Math.min(len1, len2));
  for (let i = 0; i < maxPrefix; i++) {
    if (s1[i] === s2[i]) prefix++;
    else break;
  }
  return jaro + prefix * prefixScale * (1 - jaro);
}
function bigrams(s) {
  const grams = /* @__PURE__ */ new Map();
  const str = s.toLowerCase().replace(/\s+/g, " ").trim();
  if (str.length === 0) return grams;
  if (str.length === 1) {
    grams.set(str, 1);
    return grams;
  }
  for (let i = 0; i < str.length - 1; i++) {
    const g = str.slice(i, i + 2);
    grams.set(g, (grams.get(g) ?? 0) + 1);
  }
  return grams;
}
function diceCoefficient(a, b) {
  const bg1 = bigrams(a);
  const bg2 = bigrams(b);
  if (bg1.size === 0 && bg2.size === 0) return a === b ? 1 : 0;
  if (bg1.size === 0 || bg2.size === 0) return 0;
  let intersection = 0;
  for (const [g, count] of bg1) {
    const other = bg2.get(g);
    if (other) intersection += Math.min(count, other);
  }
  const total = [...bg1.values()].reduce((a2, c) => a2 + c, 0) + [...bg2.values()].reduce((a2, c) => a2 + c, 0);
  if (total === 0) return 0;
  return 2 * intersection / total;
}
function fuzzySimilarity(a, b) {
  const jw = jaroWinkler(a, b);
  const dice = diceCoefficient(a, b);
  return Math.max(jw, dice * 0.95);
}
var init_fuzzy_matcher = __esm({
  "src/engines/nlcl/fuzzy-matcher.ts"() {
    "use strict";
  }
});

// src/engines/nlcl/text-normalizer.ts
function normalizeText(input) {
  return defaultNormalizer.normalize(input);
}
var STOPWORDS, FILLER_PREFIXES, TextNormalizer, defaultNormalizer;
var init_text_normalizer = __esm({
  "src/engines/nlcl/text-normalizer.ts"() {
    "use strict";
    STOPWORDS = /* @__PURE__ */ new Set([
      "a",
      "an",
      "the",
      "and",
      "or",
      "but",
      "if",
      "then",
      "else",
      "when",
      "at",
      "by",
      "for",
      "with",
      "about",
      "against",
      "between",
      "into",
      "through",
      "during",
      "before",
      "after",
      "above",
      "below",
      "to",
      "from",
      "up",
      "down",
      "in",
      "out",
      "on",
      "off",
      "over",
      "under",
      "again",
      "further",
      "this",
      "that",
      "these",
      "those",
      "i",
      "me",
      "my",
      "we",
      "our",
      "you",
      "your",
      "he",
      "she",
      "it",
      "its",
      "they",
      "them",
      "their",
      "is",
      "are",
      "was",
      "were",
      "be",
      "been",
      "being",
      "have",
      "has",
      "had",
      "do",
      "does",
      "did",
      "of",
      "as",
      "can",
      "could",
      "would",
      "should",
      "will",
      "just",
      "so",
      "than",
      "too",
      "very",
      "s",
      "t",
      "my",
      "some",
      "such",
      "no",
      "nor",
      "not",
      "only",
      "own",
      "same",
      "how",
      "what",
      "who",
      "whom",
      "which",
      "where",
      "why",
      "please",
      "thanks",
      "thank",
      "hey",
      "ok",
      "okay"
    ]);
    FILLER_PREFIXES = [
      "please",
      "can you",
      "could you",
      "would you",
      "will you",
      "i want to",
      "i'd like to",
      "i would like to",
      "let's",
      "lets",
      "hey",
      "ok",
      "okay",
      "can we",
      "could we"
    ];
    TextNormalizer = class {
      /**
       * Full normalization for string-similarity matching (fuzzy / Jaro-Winkler).
       * Lowercases, strips leading filler, collapses whitespace.
       *
       * Contraction expansion is intentionally NOT applied: the deterministic parser
       * must normalize identically to the pre-SOTA baseline so existing patterns that
       * embed apostrophes (e.g. "what's my health") keep matching. Fuzzy/semantic
       * layers normalize both query and candidate phrases with this same function,
       * so cross-layer consistency is preserved without it.
       */
      normalize(input) {
        if (!input) return "";
        let text = input.toLowerCase().trim();
        let changed = true;
        while (changed) {
          changed = false;
          for (const prefix of FILLER_PREFIXES) {
            const re = new RegExp(`^${prefix}\\s+`);
            if (re.test(text)) {
              text = text.replace(re, "");
              changed = true;
            }
          }
        }
        return text.replace(/\s+/g, " ").trim();
      }
      /**
       * Tokenize for TF-IDF / semantic matching: normalize + split + stem + drop stopwords.
       */
      tokenize(input) {
        const normalized = this.normalize(input);
        if (!normalized) return [];
        return normalized.split(/\s+/).map((tok) => this.stem(tok)).filter((tok) => tok.length > 1 && !STOPWORDS.has(tok));
      }
      /**
       * Light English stemmer (Porter-lite). Conservative: only strips common suffixes
       * that don't change word meaning for command matching.
       */
      stem(token) {
        if (token.length <= 3) return token;
        const suffixes = [
          ["ing", ""],
          ["ies", "y"],
          ["ied", "y"],
          ["edly", ""],
          ["edness", ""],
          ["edness", ""],
          ["ness", ""],
          ["ment", ""],
          ["ments", ""],
          ["ation", ""],
          ["izations", "ize"],
          ["ization", "ize"],
          ["edly", ""],
          ["ed", ""],
          ["es", ""],
          ["ly", ""],
          ["ally", ""],
          ["ous", ""],
          ["ful", ""],
          ["ious", ""],
          ["er", ""],
          ["est", ""],
          ["s", ""]
        ];
        for (const [suffix, replacement] of suffixes) {
          if (token.endsWith(suffix) && token.length - suffix.length >= 3) {
            return token.slice(0, token.length - suffix.length) + replacement;
          }
        }
        return token;
      }
      removeStopwords(tokens) {
        return tokens.filter((t) => !STOPWORDS.has(t));
      }
    };
    defaultNormalizer = new TextNormalizer();
  }
});

// src/engines/nlcl/pattern-match.ts
function extractPatternInput(pattern2, rawInput) {
  const normalized = normalizeText(rawInput);
  for (const nlPattern of pattern2.patterns) {
    const match = normalized.match(nlPattern.regex);
    if (match) {
      const extracted = nlPattern.extract(match, rawInput);
      const validated = pattern2.inputSchema.safeParse(extracted);
      if (validated.success) {
        return validated.data;
      }
    }
  }
  return null;
}
function buildIntentFromPattern(pattern2, rawInput, confidence, matchedPattern) {
  const extracted = extractPatternInput(pattern2, rawInput);
  return {
    patternId: pattern2.id,
    intent: pattern2.intent,
    input: extracted ?? {},
    confidence,
    rawInput,
    matchedPattern,
    alternatives: [],
    resolvedAt: Date.now()
  };
}
var init_pattern_match = __esm({
  "src/engines/nlcl/pattern-match.ts"() {
    "use strict";
    init_text_normalizer();
  }
});

// src/engines/nlcl/fuzzy-resolver.ts
function collectPhrases(pattern2) {
  const phrases = [...pattern2.aliases, ...pattern2.examples];
  for (const p of pattern2.patterns) {
    if (p.keywords) phrases.push(...p.keywords);
  }
  return phrases;
}
var FuzzyResolver;
var init_fuzzy_resolver = __esm({
  "src/engines/nlcl/fuzzy-resolver.ts"() {
    "use strict";
    init_fuzzy_matcher();
    init_pattern_match();
    init_text_normalizer();
    FuzzyResolver = class {
      name = "fuzzy";
      registry;
      threshold;
      constructor(registry2, threshold = 0.7) {
        this.registry = registry2;
        this.threshold = threshold;
      }
      async resolve(rawInput, ctx) {
        const input = normalizeText(rawInput);
        if (!input) return null;
        const candidates = this.registry.list({ surface: ctx.surface });
        let best = null;
        for (const pattern2 of candidates) {
          const phrases = collectPhrases(pattern2);
          for (const phrase of phrases) {
            const norm = normalizeText(phrase);
            if (!norm) continue;
            const score = fuzzySimilarity(input, norm);
            if (score > (best?.score ?? 0)) {
              best = { pattern: pattern2, score, matched: phrase };
            }
          }
        }
        if (!best || best.score < this.threshold) return null;
        return buildIntentFromPattern(best.pattern, rawInput, best.score, `fuzzy:${best.matched}`);
      }
    };
  }
});

// src/engines/nlcl/tfidf.ts
function cosineSimilarity(a, b) {
  if (a.size === 0 || b.size === 0) return 0;
  const [small, large] = a.size <= b.size ? [a, b] : [b, a];
  let dot = 0;
  for (const [tok, weight] of small) {
    const other = large.get(tok);
    if (other) dot += weight * other;
  }
  return dot;
}
var Tfidf;
var init_tfidf = __esm({
  "src/engines/nlcl/tfidf.ts"() {
    "use strict";
    init_text_normalizer();
    Tfidf = class {
      idf = /* @__PURE__ */ new Map();
      docCount = 0;
      vocabulary = /* @__PURE__ */ new Set();
      minTokenLength;
      constructor(opts = {}) {
        this.minTokenLength = opts.minTokenLength ?? 2;
      }
      /** Fit IDF over a corpus of raw documents. Call once (or when corpus changes). */
      fit(corpus) {
        this.docCount = corpus.length;
        this.idf.clear();
        this.vocabulary.clear();
        const df = /* @__PURE__ */ new Map();
        for (const doc of corpus) {
          const tokens = this.tokenize(doc);
          const seen = new Set(tokens);
          for (const tok of seen) {
            this.vocabulary.add(tok);
            df.set(tok, (df.get(tok) ?? 0) + 1);
          }
        }
        for (const [tok, freq] of df) {
          this.idf.set(tok, Math.log((this.docCount + 1) / (freq + 1)) + 1);
        }
      }
      /** Transform a raw text into a normalized TF-IDF sparse vector. */
      transform(text) {
        const tokens = this.tokenize(text);
        if (tokens.length === 0) return /* @__PURE__ */ new Map();
        const tf = /* @__PURE__ */ new Map();
        for (const tok of tokens) {
          tf.set(tok, (tf.get(tok) ?? 0) + 1);
        }
        const vec = /* @__PURE__ */ new Map();
        let norm = 0;
        for (const [tok, count] of tf) {
          const idf = this.idf.get(tok);
          if (idf === void 0) continue;
          const weight = count / tokens.length * idf;
          vec.set(tok, weight);
          norm += weight * weight;
        }
        norm = Math.sqrt(norm) || 1;
        for (const [tok, weight] of vec) {
          vec.set(tok, weight / norm);
        }
        return vec;
      }
      tokenize(text) {
        return defaultNormalizer.tokenize(text).filter((t) => t.length >= this.minTokenLength);
      }
    };
  }
});

// src/engines/nlcl/semantic-resolver.ts
function buildPatternDocument(pattern2) {
  const parts = [
    pattern2.intent,
    pattern2.description,
    ...pattern2.aliases,
    ...pattern2.examples
  ];
  for (const p of pattern2.patterns) {
    if (p.keywords) parts.push(...p.keywords);
  }
  return parts.join(" ");
}
var SemanticResolver;
var init_semantic_resolver = __esm({
  "src/engines/nlcl/semantic-resolver.ts"() {
    "use strict";
    init_pattern_match();
    init_tfidf();
    SemanticResolver = class {
      name = "semantic";
      registry;
      threshold;
      tfidf = new Tfidf();
      index = [];
      indexedSize = -1;
      constructor(registry2, threshold = 0.6) {
        this.registry = registry2;
        this.threshold = threshold;
      }
      async resolve(rawInput, ctx) {
        this.ensureIndex();
        const candidates = this.index.filter(
          (entry) => entry.pattern.surfaces.includes(ctx.surface)
        );
        if (candidates.length === 0) return null;
        const queryVec = this.tfidf.transform(rawInput);
        if (queryVec.size === 0) return null;
        let best = null;
        for (const entry of candidates) {
          const score = cosineSimilarity(queryVec, entry.vector);
          if (score > (best?.score ?? 0)) {
            best = { pattern: entry.pattern, score };
          }
        }
        if (!best || best.score < this.threshold) return null;
        return buildIntentFromPattern(best.pattern, rawInput, best.score, "semantic:tfidf-cosine");
      }
      /** Rebuild the TF-IDF index if the registry has changed since last build. */
      ensureIndex() {
        const size = this.registry.size();
        if (size === this.indexedSize && this.index.length > 0) return;
        const patterns = this.registry.list();
        this.tfidf.fit(patterns.map((p) => buildPatternDocument(p)));
        this.index = patterns.map((pattern2) => ({
          pattern: pattern2,
          vector: this.tfidf.transform(buildPatternDocument(pattern2))
        }));
        this.indexedSize = size;
      }
    };
  }
});

// src/engines/nlcl/layered-resolver.ts
var LayeredResolver;
var init_layered_resolver = __esm({
  "src/engines/nlcl/layered-resolver.ts"() {
    "use strict";
    init_fuzzy_resolver();
    init_intent_resolver();
    init_semantic_resolver();
    LayeredResolver = class {
      name = "layered";
      deterministic;
      fuzzy;
      semantic;
      llmFallback;
      fuzzyThreshold;
      semanticThreshold;
      llmThreshold;
      lastLayer = "none";
      lastScores = {};
      constructor(registry2, opts = {}) {
        this.deterministic = new DeterministicResolver(registry2);
        this.fuzzy = new FuzzyResolver(registry2, opts.fuzzyThreshold ?? 0.7);
        this.semantic = new SemanticResolver(registry2, opts.semanticThreshold ?? 0.6);
        this.llmFallback = opts.llmFallback ?? null;
        this.fuzzyThreshold = opts.fuzzyThreshold ?? 0.7;
        this.semanticThreshold = opts.semanticThreshold ?? 0.6;
        this.llmThreshold = opts.llmThreshold ?? 0.5;
      }
      async resolve(rawInput, ctx) {
        const scores = {};
        const det = await this.deterministic.resolve(rawInput, ctx);
        if (det) scores.deterministic = det.confidence;
        if (det) {
          this.lastLayer = "deterministic";
          this.lastScores = scores;
          return det;
        }
        const fz = await this.fuzzy.resolve(rawInput, ctx);
        if (fz) scores.fuzzy = fz.confidence;
        if (fz && fz.confidence >= this.fuzzyThreshold) {
          this.lastLayer = "fuzzy";
          this.lastScores = scores;
          return fz;
        }
        const sem = await this.semantic.resolve(rawInput, ctx);
        if (sem) scores.semantic = sem.confidence;
        if (sem && sem.confidence >= this.semanticThreshold) {
          this.lastLayer = "semantic";
          this.lastScores = scores;
          if (fz) sem.alternatives.unshift(fz);
          return sem;
        }
        if (this.llmFallback) {
          const llm = await this.llmFallback.resolve(rawInput, ctx);
          if (llm) scores.llm = llm.confidence;
          if (llm && llm.confidence >= this.llmThreshold) {
            this.lastLayer = "llm";
            this.lastScores = scores;
            const alts = [fz, sem].filter(Boolean);
            llm.alternatives = [...alts, ...llm.alternatives];
            return llm;
          }
        }
        this.lastLayer = "none";
        this.lastScores = scores;
        return fz ?? sem ?? null;
      }
      getLastLayer() {
        return this.lastLayer;
      }
      getLastScores() {
        return this.lastScores;
      }
      getSubResolvers() {
        return {
          deterministic: this.deterministic,
          fuzzy: this.fuzzy,
          semantic: this.semantic,
          llm: this.llmFallback ?? null
        };
      }
    };
  }
});

// src/engines/nlcl/entity-resolution.ts
function addDays(base, days) {
  const d = new Date(base);
  d.setDate(d.getDate() + days);
  return d;
}
function isoDate(d) {
  return d.toISOString().slice(0, 10);
}
function normalizeRelativeDate(text, now = /* @__PURE__ */ new Date()) {
  const lowered = text.toLowerCase().trim();
  if (/^today$/.test(lowered)) return isoDate(now);
  if (/^tomorrow$/.test(lowered)) return isoDate(addDays(now, 1));
  if (/^yesterday$/.test(lowered)) return isoDate(addDays(now, -1));
  const inDays = lowered.match(/^in (\d+) days?$/);
  if (inDays?.[1]) return isoDate(addDays(now, Number(inDays[1])));
  const nextWeekday = lowered.match(
    /^next (monday|tuesday|wednesday|thursday|friday|saturday|sunday)$/
  );
  if (nextWeekday?.[1]) {
    const target = [
      "sunday",
      "monday",
      "tuesday",
      "wednesday",
      "thursday",
      "friday",
      "saturday"
    ].indexOf(nextWeekday[1] ?? "");
    const current = now.getDay();
    let delta = target - current;
    if (delta <= 0) delta += 7;
    return isoDate(addDays(now, delta));
  }
  return text;
}
function normalizeRelativeTime(text) {
  const lowered = text.toLowerCase().trim();
  if (lowered === "noon") return "12:00";
  if (lowered === "midnight") return "00:00";
  const hm = lowered.match(/^(\d{1,2})(?::(\d{2}))?\s*(am|pm)$/);
  if (hm) {
    let hour = Number(hm[1]);
    const minute = hm[2] ?? "00";
    const meridiem = hm[3];
    if (meridiem === "pm" && hour < 12) hour += 12;
    if (meridiem === "am" && hour === 12) hour = 0;
    return `${String(hour).padStart(2, "0")}:${minute}`;
  }
  return text;
}
function normalizeNumberWords(text) {
  let result = text;
  result = result.replace(/\b(\d+(?:\.\d+)?)\s*(k|m|b)\b/gi, (_m, num, suffix) => {
    const base = Number(num);
    const mult = { k: 1e3, m: 1e6, b: 1e9 }[suffix.toLowerCase()] ?? 1;
    return String(base * mult);
  });
  result = result.replace(/\b[a-z-]+\b/gi, (word) => {
    const w = word.toLowerCase();
    if (w in NUMBER_WORDS) return String(NUMBER_WORDS[w]);
    return word;
  });
  return result;
}
function normalizeBoolean(text) {
  const lowered = text.toLowerCase().trim();
  if (["yes", "y", "true", "on", "enable", "enabled"].includes(lowered)) return true;
  if (["no", "n", "false", "off", "disable", "disabled"].includes(lowered)) return false;
  return text;
}
function resolveEntityValues(input, now = /* @__PURE__ */ new Date()) {
  const out = {};
  for (const [key, value] of Object.entries(input)) {
    if (typeof value === "string") {
      out[key] = resolveSingleValue(key, value, now);
    } else if (value && typeof value === "object" && !Array.isArray(value)) {
      out[key] = resolveEntityValues(value, now);
    } else {
      out[key] = value;
    }
  }
  return out;
}
function resolveSingleValue(key, value, now) {
  const loweredKey = key.toLowerCase();
  if (loweredKey.includes("date") || loweredKey === "when") {
    return normalizeRelativeDate(value, now);
  }
  if (loweredKey.includes("time") || loweredKey === "at") {
    return normalizeRelativeTime(value);
  }
  if (loweredKey.includes("count") || loweredKey.includes("number") || loweredKey === "num") {
    return normalizeNumberWords(value);
  }
  if (loweredKey.includes("enabled") || loweredKey === "confirm" || loweredKey === "force") {
    const b = normalizeBoolean(value);
    return typeof b === "boolean" ? String(b) : value;
  }
  return value;
}
var NUMBER_WORDS;
var init_entity_resolution = __esm({
  "src/engines/nlcl/entity-resolution.ts"() {
    "use strict";
    NUMBER_WORDS = {
      zero: 0,
      one: 1,
      two: 2,
      three: 3,
      four: 4,
      five: 5,
      six: 6,
      seven: 7,
      eight: 8,
      nine: 9,
      ten: 10,
      eleven: 11,
      twelve: 12,
      thirteen: 13,
      fourteen: 14,
      fifteen: 15,
      sixteen: 16,
      seventeen: 17,
      eighteen: 18,
      nineteen: 19,
      twenty: 20,
      thirty: 30,
      forty: 40,
      fifty: 50,
      sixty: 60,
      seventy: 70,
      eighty: 80,
      ninety: 90,
      hundred: 100,
      thousand: 1e3
    };
  }
});

// src/engines/nlcl/nl-parser.ts
var DEFAULT_PARSE_OPTIONS, NLCommandParser;
var init_nl_parser = __esm({
  "src/engines/nlcl/nl-parser.ts"() {
    "use strict";
    init_entity_resolution();
    init_text_normalizer();
    DEFAULT_PARSE_OPTIONS = {
      maxAlternatives: 3,
      minConfidence: 0.3,
      surface: "frontend"
    };
    NLCommandParser = class {
      constructor(registry2) {
        this.registry = registry2;
      }
      registry;
      parse(rawInput, _ctx, opts) {
        const options = { ...DEFAULT_PARSE_OPTIONS, ...opts };
        const input = rawInput.trim();
        if (!input) return null;
        const normalized = normalizeText(input);
        const candidates = this.registry.list({ surface: opts?.surface });
        const matches = [];
        for (const pattern2 of candidates) {
          const match = this.tryMatch(pattern2, normalized, rawInput);
          if (match) {
            const priorityBonus = (pattern2.patterns[0]?.priority ?? 0) * 1e-3;
            matches.push({
              pattern: pattern2,
              intent: match,
              score: match.confidence + priorityBonus
            });
          }
        }
        matches.sort((a, b) => b.score - a.score);
        if (matches.length === 0) return null;
        const best = matches[0];
        if (!best) return null;
        if (best.intent.confidence < options.minConfidence) return null;
        const alternatives = matches.slice(1, options.maxAlternatives + 1).map((m) => m.intent);
        return {
          ...best.intent,
          alternatives,
          resolvedAt: Date.now()
        };
      }
      tryMatch(pattern2, normalizedInput, rawInput) {
        for (const nlPattern of pattern2.patterns) {
          const match = normalizedInput.match(nlPattern.regex);
          if (match) {
            const extracted = nlPattern.extract(match, rawInput);
            const validated = pattern2.inputSchema.safeParse(extracted);
            if (validated.success) {
              const resolved = resolveEntityValues(validated.data);
              return {
                patternId: pattern2.id,
                intent: pattern2.intent,
                input: resolved,
                confidence: this.scoreMatch(match, nlPattern, normalizedInput),
                rawInput,
                matchedPattern: nlPattern.regex.source,
                alternatives: [],
                resolvedAt: Date.now(),
                capabilityId: pattern2.capabilityId
              };
            }
          }
        }
        for (const alias of pattern2.aliases) {
          if (this.matchesAlias(normalizedInput, alias)) {
            return {
              patternId: pattern2.id,
              intent: pattern2.intent,
              input: {},
              confidence: 0.7,
              rawInput,
              matchedPattern: `alias:${alias}`,
              alternatives: [],
              resolvedAt: Date.now(),
              capabilityId: pattern2.capabilityId
            };
          }
        }
        if (pattern2.patterns.some((p) => p.keywords)) {
          const keywordScore = this.scoreKeywords(pattern2, normalizedInput);
          if (keywordScore.confidence > 0) {
            const resolved = resolveEntityValues(keywordScore.input);
            return {
              patternId: pattern2.id,
              intent: pattern2.intent,
              input: resolved,
              confidence: keywordScore.confidence,
              rawInput,
              matchedPattern: "keyword-fallback",
              alternatives: [],
              resolvedAt: Date.now(),
              capabilityId: pattern2.capabilityId
            };
          }
        }
        return null;
      }
      scoreMatch(match, _pattern, input) {
        const captureGroups = match.length - 1;
        const inputLength = input.length;
        const matchLength = match[0]?.length ?? 0;
        const coverage = inputLength > 0 ? matchLength / inputLength : 0;
        const baseConfidence = 0.6 + coverage * 0.39;
        const captureBonus = Math.min(captureGroups * 0.02, 0.1);
        return Math.min(baseConfidence + captureBonus, 1);
      }
      matchesAlias(input, alias) {
        const normalizedAlias = normalizeText(alias);
        return input === normalizedAlias || input.startsWith(`${normalizedAlias} `);
      }
      scoreKeywords(pattern2, input) {
        const allKeywords = pattern2.patterns.flatMap((p) => p.keywords ?? []);
        if (allKeywords.length === 0) return { confidence: 0, input: {} };
        const inputWords = new Set(input.split(" "));
        let matched = 0;
        for (const kw of allKeywords) {
          if (inputWords.has(kw.toLowerCase())) matched++;
        }
        const confidence = matched / allKeywords.length;
        if (confidence < 0.4) return { confidence: 0, input: {} };
        const remainder = input.split(" ").filter((w) => !allKeywords.some((kw) => kw.toLowerCase() === w)).join(" ");
        return {
          confidence: confidence * 0.6,
          input: remainder ? { query: remainder, raw: input } : { raw: input }
        };
      }
    };
  }
});

// src/engines/nlcl/intent-resolver.ts
function createResolver(config2, registry2, adapters) {
  switch (config2.type) {
    case "deterministic":
      return new DeterministicResolver(registry2);
    case "local-llm": {
      if (!adapters?.localLLM) {
        if (config2.fallbackToDeterministic) return new DeterministicResolver(registry2);
        throw new EngineError("Local LLM adapter not provided and fallback disabled");
      }
      const llm = new LocalLLMResolver(adapters.localLLM, registry2, config2.minConfidence);
      if (config2.fallbackToDeterministic) {
        return new HybridResolver(registry2, llm, config2.minConfidence);
      }
      return llm;
    }
    case "provider-llm": {
      if (!adapters?.providerLLM) {
        if (config2.fallbackToDeterministic) return new DeterministicResolver(registry2);
        throw new EngineError("Provider LLM adapter not provided and fallback disabled");
      }
      const llm = new ProviderLLMResolver(adapters.providerLLM, registry2);
      if (config2.fallbackToDeterministic) {
        return new HybridResolver(registry2, llm, config2.minConfidence);
      }
      return llm;
    }
    case "hybrid": {
      const fallbacks = [];
      if (adapters?.localLLM) {
        fallbacks.push(new LocalLLMResolver(adapters.localLLM, registry2, config2.minConfidence));
      }
      if (adapters?.providerLLM) {
        fallbacks.push(new ProviderLLMResolver(adapters.providerLLM, registry2));
      }
      const primary = fallbacks[0] ?? null;
      return new HybridResolver(registry2, primary ?? void 0, config2.minConfidence);
    }
    case "layered": {
      const llms = [];
      if (adapters?.localLLM) {
        llms.push(
          new LocalLLMResolver(
            adapters.localLLM,
            registry2,
            config2.llmThreshold ?? config2.minConfidence
          )
        );
      }
      if (adapters?.providerLLM) {
        llms.push(new ProviderLLMResolver(adapters.providerLLM, registry2));
      }
      const llmFallback = llms[0] ?? (config2.fallbackToDeterministic ? void 0 : void 0);
      return new LayeredResolver(registry2, {
        llmFallback,
        fuzzyThreshold: config2.fuzzyThreshold,
        semanticThreshold: config2.semanticThreshold,
        llmThreshold: config2.llmThreshold ?? config2.minConfidence
      });
    }
  }
}
function unresolvedIntent(rawInput) {
  return {
    patternId: "unresolved",
    intent: "unresolved",
    input: { raw: rawInput },
    confidence: 0,
    rawInput,
    matchedPattern: "none",
    alternatives: [],
    resolvedAt: Date.now()
  };
}
var DeterministicResolver, LocalLLMResolver, ProviderLLMResolver, HybridResolver;
var init_intent_resolver = __esm({
  "src/engines/nlcl/intent-resolver.ts"() {
    "use strict";
    init_errors();
    init_layered_resolver();
    init_nl_parser();
    DeterministicResolver = class {
      name = "deterministic";
      parser;
      constructor(registry2) {
        this.parser = new NLCommandParser(registry2);
      }
      async resolve(rawInput, ctx) {
        return this.parser.parse(rawInput, ctx, {
          surface: ctx.surface,
          maxAlternatives: 3,
          minConfidence: 0.3
        });
      }
    };
    LocalLLMResolver = class {
      name = "local-llm";
      adapter;
      registry;
      minConfidence;
      constructor(adapter, registry2, minConfidence = 0.5) {
        this.adapter = adapter;
        this.registry = registry2;
        this.minConfidence = minConfidence;
      }
      async resolve(rawInput, ctx) {
        const catalog = this.registry.exportForSurface(ctx.surface);
        const prompt = this.buildPrompt(rawInput, catalog);
        try {
          const response = await this.adapter.complete(prompt, { temperature: 0.1, maxTokens: 512 });
          return this.parseLLMResponse(response, rawInput);
        } catch {
          return null;
        }
      }
      buildPrompt(rawInput, catalog) {
        const catalogStr = catalog.map((c) => `- ${c.intent}: ${c.description} (e.g. ${c.examples.slice(0, 2).join(", ")})`).join("\n");
        return `You are a command intent classifier. Given a user's natural language input, classify it into one of the following intents. Respond as JSON: {"intent":"<intent>","input":{<params>},"confidence":<0-1>}

Available intents:
${catalogStr}

User input: "${rawInput}"

JSON response:`;
      }
      parseLLMResponse(response, rawInput) {
        try {
          const jsonMatch = response.match(/\{[\s\S]*\}/);
          if (!jsonMatch) return null;
          const parsed = JSON.parse(jsonMatch[0]);
          if (!parsed.intent) return null;
          const pattern2 = this.registry.getByIntent(parsed.intent);
          if (!pattern2) return null;
          const validated = pattern2.inputSchema.safeParse(parsed.input ?? {});
          if (!validated.success) return null;
          return {
            patternId: pattern2.id,
            intent: pattern2.intent,
            input: validated.data,
            confidence: Math.min(parsed.confidence ?? 0.7, 1),
            rawInput,
            matchedPattern: "local-llm",
            alternatives: [],
            resolvedAt: Date.now()
          };
        } catch {
          return null;
        }
      }
    };
    ProviderLLMResolver = class {
      name = "provider-llm";
      adapter;
      registry;
      constructor(adapter, registry2) {
        this.adapter = adapter;
        this.registry = registry2;
      }
      async resolve(rawInput, ctx) {
        const catalog = this.registry.exportForSurface(ctx.surface);
        const prompt = this.buildPrompt(rawInput, catalog);
        try {
          const response = await this.adapter.query(prompt);
          const localParser = new LocalLLMResolver(
            { complete: async (p) => p === prompt ? response : "" },
            this.registry
          );
          return localParser.resolve(rawInput, ctx);
        } catch {
          return null;
        }
      }
      buildPrompt(rawInput, catalog) {
        const catalogStr = catalog.map((c) => `- ${c.intent}: ${c.description} (e.g. ${c.examples.slice(0, 2).join(", ")})`).join("\n");
        return `Classify this user command into one intent. Respond as JSON: {"intent":"<intent>","input":{<params>},"confidence":<0-1>}

Intents:
${catalogStr}

Input: "${rawInput}"
JSON:`;
      }
    };
    HybridResolver = class {
      name = "hybrid";
      deterministic;
      llmFallback;
      minConfidence;
      constructor(registry2, llmFallback, minConfidence = 0.5) {
        this.deterministic = new DeterministicResolver(registry2);
        this.llmFallback = llmFallback ?? null;
        this.minConfidence = minConfidence;
      }
      async resolve(rawInput, ctx) {
        const deterministic = await this.deterministic.resolve(rawInput, ctx);
        if (deterministic && deterministic.confidence >= this.minConfidence) {
          return deterministic;
        }
        if (this.llmFallback) {
          const llmResult = await this.llmFallback.resolve(rawInput, ctx);
          if (llmResult && llmResult.confidence >= this.minConfidence) {
            if (deterministic) {
              llmResult.alternatives.unshift(deterministic);
            }
            return llmResult;
          }
        }
        return deterministic;
      }
    };
  }
});

// src/engines/nlcl/intent-router.ts
function toCapCtx(ctx) {
  return {
    conversationId: ctx.conversationId,
    providerId: ctx.providerId,
    slaveId: ctx.slaveId,
    userId: ctx.userId,
    metadata: ctx.metadata
  };
}
var IntentRouter;
var init_intent_router = __esm({
  "src/engines/nlcl/intent-router.ts"() {
    "use strict";
    init_errors();
    init_ids();
    IntentRouter = class {
      executors = /* @__PURE__ */ new Map();
      registry;
      constructor(registry2) {
        this.registry = registry2;
      }
      setRegistry(registry2) {
        this.registry = registry2;
      }
      registerExecutor(executor) {
        if (this.executors.has(executor.id)) {
          throw new EngineError(`Executor ${executor.id} already registered`);
        }
        this.executors.set(executor.id, executor);
      }
      getExecutor(id) {
        return this.executors.get(id);
      }
      listExecutors() {
        return [...this.executors.keys()];
      }
      async route(intent, ctx) {
        const start = Date.now();
        const traceId = newId();
        if (intent.patternId === "unresolved" || intent.intent === "unresolved") {
          return {
            ok: false,
            intent: "unresolved",
            error: `Could not understand: "${intent.rawInput}"`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "system",
            followUp: 'Try rephrasing, or type "help" to see available commands.'
          };
        }
        if (intent.capabilityId && this.registry) {
          try {
            const output = await this.registry.execute(
              intent.capabilityId,
              intent.input ?? {},
              toCapCtx(ctx)
            );
            return {
              ok: true,
              intent: intent.intent,
              output,
              latencyMs: Date.now() - start,
              traceId,
              classification: "system",
              capabilityId: intent.capabilityId
            };
          } catch (err) {
            const message = err instanceof Error ? err.message : String(err);
            return {
              ok: false,
              intent: intent.intent,
              error: message,
              latencyMs: Date.now() - start,
              traceId,
              classification: "system",
              capabilityId: intent.capabilityId
            };
          }
        }
        const pattern2 = this.findPattern(intent);
        if (!pattern2) {
          return {
            ok: false,
            intent: intent.intent,
            error: `No executor for intent: ${intent.intent}`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "system"
          };
        }
        const executor = this.executors.get(pattern2.executor);
        if (!executor) {
          return {
            ok: false,
            intent: intent.intent,
            error: `Executor "${pattern2.executor}" not registered`,
            latencyMs: Date.now() - start,
            traceId,
            classification: "system"
          };
        }
        return executor.execute(intent, ctx);
      }
      async routeComposite(composite, ctx) {
        const start = Date.now();
        const traceId = newId();
        const results = [];
        let pipelineData;
        for (let i = 0; i < composite.steps.length; i++) {
          const step = composite.steps[i];
          if (!step) continue;
          const stepCtx = { ...ctx };
          if (composite.joinStrategy === "pipeline" && pipelineData && i > 0) {
            step.input = {
              ...step.input,
              content: typeof pipelineData === "string" ? pipelineData : JSON.stringify(pipelineData)
            };
          }
          const result = await this.routeStep(step, stepCtx);
          results.push(result);
          if (composite.joinStrategy === "sequential" && !result.ok) {
            return {
              ok: false,
              intent: "composite",
              error: `Step ${i + 1} failed: ${result.error}`,
              output: { results, failedAt: i + 1 },
              latencyMs: Date.now() - start,
              traceId,
              classification: "system"
            };
          }
          if (composite.joinStrategy === "pipeline") {
            pipelineData = result.output;
          }
        }
        const lastResult = results[results.length - 1];
        return {
          ok: true,
          intent: "composite",
          output: { results, finalOutput: lastResult?.output },
          text: lastResult?.text ?? "Composite command completed",
          latencyMs: Date.now() - start,
          traceId,
          classification: lastResult?.classification ?? "system"
        };
      }
      /** Route a single step via registry if capabilityId present, else router. */
      async routeStep(step, ctx) {
        if (step.capabilityId && this.registry) {
          try {
            const output = await this.registry.execute(
              step.capabilityId,
              step.input ?? {},
              toCapCtx(ctx)
            );
            return {
              ok: true,
              intent: step.intent,
              output,
              latencyMs: 0,
              traceId: newId(),
              classification: "system",
              capabilityId: step.capabilityId
            };
          } catch (err) {
            const message = err instanceof Error ? err.message : String(err);
            return {
              ok: false,
              intent: step.intent,
              error: message,
              latencyMs: 0,
              traceId: newId(),
              classification: "system",
              capabilityId: step.capabilityId
            };
          }
        }
        return this.route(step, ctx);
      }
      patternRegistry = /* @__PURE__ */ new Map();
      registerPatternIntent(intent, executor) {
        this.patternRegistry.set(intent, { executor });
      }
      findPattern(intent) {
        return this.patternRegistry.get(intent.intent) ?? null;
      }
    };
  }
});

// src/engines/nlcl/parameter-extraction.ts
import { z as z6 } from "zod";
function extractParameters(rawInput, schema, ctx) {
  const input = {};
  const missing = [];
  const ambiguous = [];
  const shape = schema instanceof z6.ZodObject ? schema.shape : {};
  const schemaKeys = Object.keys(shape);
  for (const key of schemaKeys) {
    const value = extractValueForProperty(key, rawInput, ctx);
    if (value.found !== void 0) {
      input[key] = value.found;
    }
    if (value.ambiguous) {
      ambiguous.push(key);
    }
  }
  for (const key of schemaKeys) {
    const zodType = shape[key];
    const isOptional = zodType?.isOptional?.() ?? false;
    if (!isOptional && !(key in input)) {
      const _ctxValue = ctx.providerId || ctx.conversationId || ctx.slaveId;
      if (key === "providerId" && ctx.providerId) continue;
      if (key === "conversationId" && ctx.conversationId) continue;
      if (key === "slaveId" && ctx.slaveId) continue;
      missing.push(key);
    }
  }
  return { input, missing, ambiguous };
}
function extractValueForProperty(property, rawInput, ctx) {
  const _lower = rawInput.toLowerCase();
  if (property === "providerId" || property === "provider") {
    const match = rawInput.match(/\b(chatgpt|claude|gemini|openai|anthropic)\b/i);
    if (match) return { found: match[1]?.toLowerCase() };
    if (ctx.providerId) return { found: ctx.providerId };
  }
  if (property === "conversationId" || property === "conversation") {
    const quoted = rawInput.match(/['"]([^'"]+)['"]/i);
    if (quoted) return { found: quoted[1] };
    if (ctx.conversationId) return { found: ctx.conversationId };
  }
  if (property === "slaveId") {
    const match = rawInput.match(/\b[a-f0-9]{8}\b/i);
    if (match) return { found: match[0] };
    if (ctx.slaveId) return { found: ctx.slaveId };
  }
  if (/limit|count|number|n\b/i.test(property)) {
    const numMatch = rawInput.match(/\b(\d+)\b/);
    if (numMatch?.[1]) {
      return { found: Number.parseInt(numMatch[1], 10) };
    }
  }
  if (property.endsWith("text") || property.endsWith("query") || property.endsWith("name")) {
    const quoted = rawInput.match(/"([^"]+)"/i);
    if (quoted) return { found: quoted[1] };
  }
  if (/true|false|yes|no/i.test(rawInput)) {
    const boolMatch = rawInput.match(/\b(true|false|yes|no)\b/i);
    if (boolMatch?.[1]) {
      return { found: ["yes", "true"].includes(boolMatch[1].toLowerCase()) };
    }
  }
  return {};
}
function validateInput(input, schema) {
  const result = schema.safeParse(input);
  if (result.success) {
    return { ok: true, value: result.data };
  }
  return {
    ok: false,
    errors: result.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`)
  };
}
var init_parameter_extraction = __esm({
  "src/engines/nlcl/parameter-extraction.ts"() {
    "use strict";
  }
});

// src/engines/nlcl/types.ts
function classificationAtLeast(classification, threshold) {
  return (CLASSIFICATION_PRIORITY[classification] ?? 0) >= (CLASSIFICATION_PRIORITY[threshold] ?? 0);
}
var DEFAULT_NLCL_CONFIG, CLASSIFICATION_PRIORITY;
var init_types = __esm({
  "src/engines/nlcl/types.ts"() {
    "use strict";
    DEFAULT_NLCL_CONFIG = {
      resolver: {
        // SOTA default: full 5-layer pipeline (Deterministic → Fuzzy → Semantic → LLM).
        type: "layered",
        fallbackToDeterministic: true,
        minConfidence: 0.5,
        fuzzyThreshold: 0.7,
        semanticThreshold: 0.6,
        llmThreshold: 0.5
      },
      defaultTimeoutMs: 3e4,
      enableAIFallback: true,
      maxAlternatives: 3,
      auditLog: true
    };
    CLASSIFICATION_PRIORITY = {
      read: 0,
      navigate: 1,
      system: 2,
      communication: 3,
      write: 4,
      destructive: 5,
      financial: 6
    };
  }
});

// src/engines/nlcl/nlcl-engine.ts
var COMPOSITE_SPLITTERS, NLCLEngine;
var init_nlcl_engine = __esm({
  "src/engines/nlcl/nlcl-engine.ts"() {
    "use strict";
    init_ids();
    init_catalog();
    init_command_registry2();
    init_executors();
    init_intent_resolver();
    init_intent_router();
    init_nl_parser();
    init_parameter_extraction();
    init_types();
    COMPOSITE_SPLITTERS = [
      /\s+and\s+(?:then\s+)?/i,
      /\s+then\s+/i,
      /\s+after\s+(?:that|which)\s+/i,
      /,\s+(?:then|after)\s+/i
    ];
    NLCLEngine = class {
      registry;
      router;
      resolver;
      parser;
      config;
      deps;
      auditLog = [];
      constructor(deps = {}) {
        this.deps = deps;
        this.config = { ...DEFAULT_NLCL_CONFIG, ...deps.config };
        this.registry = new CommandPatternRegistry();
        this.router = new IntentRouter(deps.registry);
        this.parser = new NLCommandParser(this.registry);
        this.resolver = createResolver(this.config.resolver, this.registry, {
          localLLM: deps.localLLM,
          providerLLM: deps.providerLLM
        });
        this.registerDefaultPatterns();
        this.registerExecutors();
      }
      // ── Public API ──────────────────────────────────────────────────────────
      async interpret(rawInput, ctx) {
        const start = Date.now();
        if (!rawInput.trim()) {
          return {
            ok: false,
            intent: "empty",
            error: "Empty input",
            latencyMs: 0,
            traceId: newId(),
            classification: "system"
          };
        }
        const composite = this.detectComposite(rawInput);
        if (composite) {
          const result2 = await this.executeComposite(composite, ctx);
          this.audit(rawInput, "composite", result2.ok, Date.now() - start);
          return result2;
        }
        const intent = await this.resolver.resolve(rawInput, ctx);
        if (!intent || intent.confidence < this.config.resolver.minConfidence) {
          if (this.config.enableAIFallback) {
            const aiIntent = await this.tryAIFallback(rawInput, ctx);
            if (aiIntent) {
              const result3 = await this.routeWithConfirmation(aiIntent, ctx, start);
              this.audit(rawInput, aiIntent.intent, result3.ok, Date.now() - start);
              return result3;
            }
          }
          const unresolved = unresolvedIntent(rawInput);
          const result2 = await this.router.route(unresolved, ctx);
          this.audit(rawInput, "unresolved", false, Date.now() - start);
          return result2;
        }
        const result = await this.routeWithConfirmation(intent, ctx, start);
        this.audit(rawInput, intent.intent, result.ok, Date.now() - start);
        return result;
      }
      /**
       * Routes intent with parameter extraction and confirmation flow.
       */
      async routeWithConfirmation(intent, ctx, _start) {
        const pattern2 = this.registry.getPattern(intent.patternId);
        if (pattern2?.capabilityId) {
          const extraction = extractParameters(intent.rawInput, pattern2.inputSchema, ctx);
          if (extraction.missing.length > 0) {
            return {
              ok: false,
              intent: intent.intent,
              error: `Missing parameters: ${extraction.missing.join(", ")}`,
              text: `What ${extraction.missing.join(", ")}?`,
              latencyMs: 0,
              traceId: newId(),
              classification: pattern2.classification,
              clarification: {
                prompt: `Missing: ${extraction.missing.join(", ")}`,
                missing: extraction.missing
              },
              capabilityId: pattern2.capabilityId
            };
          }
          const validation = validateInput(extraction.input, pattern2.inputSchema);
          if (!validation.ok) {
            return {
              ok: false,
              intent: intent.intent,
              error: validation.errors.join("; "),
              latencyMs: 0,
              traceId: newId(),
              classification: pattern2.classification,
              clarification: {
                prompt: `Invalid parameters: ${validation.errors.join("; ")}`
              },
              capabilityId: pattern2.capabilityId
            };
          }
        }
        if (pattern2 && (pattern2.requiresConfirmation || this.isDestructive(pattern2.classification))) {
          const token = newId();
          return {
            ok: true,
            intent: intent.intent,
            latencyMs: 0,
            traceId: newId(),
            classification: pattern2.classification,
            requiresConfirmation: true,
            confirmation: {
              token,
              prompt: `Confirm: ${pattern2.description}`
            },
            capabilityId: pattern2.capabilityId
          };
        }
        return this.router.route(intent, ctx);
      }
      isDestructive(classification) {
        return classificationAtLeast(classification, "destructive");
      }
      // ── Introspection ───────────────────────────────────────────────────────
      getRegistry() {
        return this.registry;
      }
      getRouter() {
        return this.router;
      }
      getResolver() {
        return this.resolver;
      }
      listCommands(filter) {
        return this.registry.list(filter);
      }
      getHelp() {
        const byCategory = this.registry.listByCategory();
        const categories = {};
        for (const [cat, patterns] of Object.entries(byCategory)) {
          categories[cat] = patterns.map((p) => `${p.examples[0] ?? p.intent} \u2014 ${p.description}`);
        }
        return { categories, totalCommands: this.registry.size() };
      }
      getAuditLog(limit = 50) {
        return this.auditLog.slice(-limit);
      }
      // ── Pluggability ────────────────────────────────────────────────────────
      registerPattern(pattern2) {
        this.registry.register(pattern2);
        this.router.registerPatternIntent(pattern2.intent, pattern2.executor);
      }
      registerExecutor(executor) {
        this.router.registerExecutor(executor);
      }
      setResolver(resolver) {
        this.resolver = resolver;
      }
      // ── Internal ────────────────────────────────────────────────────────────
      registerDefaultPatterns() {
        const patterns = getDefaultCommandPatterns();
        for (const p of patterns) {
          this.registry.register(p);
          this.router.registerPatternIntent(p.intent, p.executor);
        }
      }
      registerExecutors() {
        const {
          governor,
          automationOrchestrator,
          conversationManager,
          conversationStore,
          registry: registry2,
          db
        } = this.deps;
        const fileExec = new FileExecutor();
        const browserExec = new BrowserExecutor(governor, conversationManager);
        const llmExec = new ProviderLLMExecutor(conversationManager, conversationStore);
        const systemExec = new SystemExecutor(db, governor, registry2);
        const convExec = new ConversationExecutor(conversationManager);
        const capExec = new CapabilityExecutor(registry2);
        const emailExec = new EmailExecutor();
        const appExec = new AppExecutor();
        const workflowExec = new WorkflowExecutor(registry2);
        const autoExec = new GenericBrowserExecutor(governor, automationOrchestrator);
        const executors = [
          fileExec,
          browserExec,
          llmExec,
          systemExec,
          convExec,
          capExec,
          emailExec,
          appExec,
          workflowExec,
          autoExec
        ];
        for (const exec of executors) {
          this.router.registerExecutor(exec);
        }
      }
      detectComposite(rawInput) {
        for (const splitter of COMPOSITE_SPLITTERS) {
          const parts = rawInput.split(splitter);
          if (parts.length >= 2) {
            const steps = [];
            for (const part of parts) {
              const trimmed = part.trim();
              if (!trimmed) continue;
              const intent = this.parser.parse(trimmed, { surface: "frontend" }, {
                surface: "frontend"
              });
              if (intent) {
                steps.push(intent);
              } else {
                return null;
              }
            }
            if (steps.length >= 2) {
              const joinStrategy = this.inferJoinStrategy(steps);
              return { steps, joinStrategy };
            }
          }
        }
        return null;
      }
      inferJoinStrategy(steps) {
        const hasSummarize = steps.some(
          (s) => s.intent.includes("summarize") || s.intent.includes("extract")
        );
        const hasNavigate = steps.some(
          (s) => s.intent.includes("navigate") || s.intent.includes("browser")
        );
        if (hasNavigate && hasSummarize) return "pipeline";
        return "sequential";
      }
      async executeComposite(composite, ctx) {
        if (composite.joinStrategy === "pipeline") {
          return this.executePipeline(composite.steps, ctx);
        }
        return this.router.routeComposite(composite, ctx);
      }
      async executePipeline(steps, ctx) {
        const start = Date.now();
        const traceId = newId();
        const results = [];
        let pipelineData = void 0;
        for (let i = 0; i < steps.length; i++) {
          const step = steps[i];
          if (!step) continue;
          const stepCtx = { ...ctx };
          if (pipelineData && i > 0) {
            const content = typeof pipelineData === "string" ? pipelineData : pipelineData?.text ?? JSON.stringify(pipelineData);
            step.input = { ...step.input, content };
          }
          const result = await this.router.route(step, stepCtx);
          results.push(result);
          if (!result.ok) {
            return {
              ok: false,
              intent: "composite",
              error: `Step ${i + 1} failed: ${result.error}`,
              output: { results, failedAt: i + 1 },
              latencyMs: Date.now() - start,
              traceId,
              classification: "system"
            };
          }
          pipelineData = result.output;
        }
        const lastResult = results[results.length - 1];
        return {
          ok: true,
          intent: "composite",
          output: { results, finalOutput: lastResult?.output },
          text: lastResult?.text ?? "Done",
          latencyMs: Date.now() - start,
          traceId,
          classification: lastResult?.classification ?? "communication"
        };
      }
      async tryAIFallback(rawInput, ctx) {
        if (this.resolver.name === "hybrid" || this.resolver.name === "layered" || this.resolver.name === "local-llm" || this.resolver.name === "provider-llm") {
          return null;
        }
        if (this.deps.providerLLM) {
          const providerResolver = createResolver(
            { type: "provider-llm", fallbackToDeterministic: false, minConfidence: 0.5 },
            this.registry,
            { providerLLM: this.deps.providerLLM }
          );
          return providerResolver.resolve(rawInput, ctx);
        }
        if (this.deps.localLLM) {
          const llmResolver = createResolver(
            { type: "local-llm", fallbackToDeterministic: false, minConfidence: 0.5 },
            this.registry,
            { localLLM: this.deps.localLLM }
          );
          return llmResolver.resolve(rawInput, ctx);
        }
        return null;
      }
      audit(input, intent, ok, latencyMs) {
        if (!this.config.auditLog) return;
        this.auditLog.push({ ts: Date.now(), input, intent, ok, latencyMs });
        if (this.auditLog.length > 1e3) {
          this.auditLog = this.auditLog.slice(-500);
        }
      }
    };
  }
});

// src/engines/unified-registry.ts
function validateCapability(cap) {
  if (!cap.id || !cap.slug || !cap.name) {
    throw new EngineError("Capability must have id, slug, and name");
  }
  if (typeof cap.handler !== "function") {
    throw new EngineError(`Capability ${cap.id} must have a handler function`);
  }
  if (cap.surfaces.includes("cli") && !cap.cliCommand) {
    throw new EngineError(`Capability ${cap.id} exposed to CLI must have cliCommand`);
  }
  if (cap.surfaces.includes("mcp") && !cap.mcpToolName) {
    throw new EngineError(`Capability ${cap.id} exposed to MCP must have mcpToolName`);
  }
  if (cap.surfaces.includes("api") && !cap.apiEndpoint) {
    throw new EngineError(`Capability ${cap.id} exposed to API must have apiEndpoint`);
  }
  if (cap.surfaces.includes("ui") && !cap.ui && !cap.uiAction) {
    throw new EngineError(`Capability ${cap.id} exposed to UI must have ui or uiAction block`);
  }
}
var UnifiedCapabilityRegistry;
var init_unified_registry = __esm({
  "src/engines/unified-registry.ts"() {
    "use strict";
    init_errors();
    UnifiedCapabilityRegistry = class {
      capabilities = /* @__PURE__ */ new Map();
      slugIndex = /* @__PURE__ */ new Map();
      register(capability) {
        validateCapability(capability);
        if (this.capabilities.has(capability.id)) {
          throw new EngineError(`Capability ${capability.id} already registered`);
        }
        if (this.slugIndex.has(capability.slug)) {
          throw new EngineError(`Slug ${capability.slug} already registered`);
        }
        this.capabilities.set(capability.id, capability);
        this.slugIndex.set(capability.slug, capability);
      }
      unregister(id) {
        const cap = this.capabilities.get(id);
        if (!cap) throw new EngineError(`Capability ${id} not found`);
        this.capabilities.delete(id);
        this.slugIndex.delete(cap.slug);
      }
      get(id) {
        return this.capabilities.get(id) ?? null;
      }
      getBySlug(slug) {
        return this.slugIndex.get(slug) ?? null;
      }
      list(filter) {
        let result = Array.from(this.capabilities.values());
        if (filter?.surface) {
          result = result.filter((c) => c.surfaces.includes(filter.surface));
        }
        if (filter?.category) {
          result = result.filter((c) => c.category === filter.category);
        }
        if (filter?.tag) {
          result = result.filter((c) => c.tags.includes(filter.tag));
        }
        return result;
      }
      async execute(id, input, ctx) {
        const cap = this.capabilities.get(id);
        if (!cap) throw new EngineError(`Capability ${id} not found`);
        const required = cap.inputSchema.required ?? [];
        for (const key of required) {
          if (!(key in input)) {
            throw new EngineError(`Missing required input: ${key}`);
          }
        }
        return cap.handler(input, ctx);
      }
      exportForCli() {
        return this.list({ surface: "cli" }).map((cap) => ({
          name: cap.cliCommand?.name ?? cap.slug,
          aliases: cap.cliCommand?.aliases ?? [],
          description: cap.description,
          schema: cap.inputSchema
        }));
      }
      exportForMcp() {
        return this.list({ surface: "mcp" }).map((cap) => ({
          name: cap.mcpToolName ?? cap.slug,
          description: cap.description,
          inputSchema: cap.inputSchema
        }));
      }
      exportForUi() {
        return this.list({ surface: "ui" }).map((cap) => ({
          id: cap.id,
          slug: cap.slug,
          name: cap.name,
          ui: cap.ui,
          inputSchema: cap.inputSchema,
          apiEndpoint: cap.apiEndpoint,
          requiresConfirmation: cap.requiresConfirmation
        }));
      }
    };
  }
});

// src/server/auth-gate.ts
function createAuthMiddleware() {
  const token = process.env.AUTH_TOKEN;
  return (req) => {
    if (!token) return null;
    const authHeader = req.headers.get("Authorization");
    if (!authHeader || !authHeader.startsWith("Bearer ")) {
      return new Response(
        JSON.stringify({ error: "Authentication required", code: "AuthRequired" }),
        { status: 401, headers: { "Content-Type": "application/json" } }
      );
    }
    const supplied = authHeader.slice(7);
    if (supplied !== token) {
      return new Response(JSON.stringify({ error: "Invalid token", code: "AuthRequired" }), {
        status: 401,
        headers: { "Content-Type": "application/json" }
      });
    }
    return null;
  };
}
var init_auth_gate = __esm({
  "src/server/auth-gate.ts"() {
    "use strict";
  }
});

// src/engines/automation/agents.ts
function getAgentRole(id) {
  const role = AGENT_ROLES[id];
  if (!role) throw new Error(`Unknown agent role: ${id}`);
  return role;
}
var RESEARCH_TRUST, EXTRACTOR_TRUST, SYNTHESIZER_TRUST, MONITOR_TRUST, TESTER_TRUST, AGENT_ROLES;
var init_agents = __esm({
  "src/engines/automation/agents.ts"() {
    "use strict";
    RESEARCH_TRUST = {
      level: "read",
      humanGate: false,
      maxSteps: 40,
      requiresConfirmation: false
    };
    EXTRACTOR_TRUST = {
      level: "read",
      humanGate: false,
      maxSteps: 20,
      requiresConfirmation: false
    };
    SYNTHESIZER_TRUST = {
      level: "read",
      humanGate: false,
      maxSteps: 10,
      requiresConfirmation: false
    };
    MONITOR_TRUST = {
      level: "read",
      humanGate: false,
      maxSteps: 15,
      requiresConfirmation: false
    };
    TESTER_TRUST = {
      level: "config",
      humanGate: true,
      maxSteps: 30,
      requiresConfirmation: true
    };
    AGENT_ROLES = {
      researcher: {
        id: "researcher",
        description: "Gather and synthesize information from many sources into a report.",
        trust: RESEARCH_TRUST,
        fanOut: { strategy: "breadth", maxSources: 10, depth: 2 },
        defaultRecipe: "auto:research:report",
        loopPolicy: { maxIterations: 8, stopOn: "sufficient_sources" },
        output: { format: "markdown", aggregate: "report" }
      },
      extractor: {
        id: "extractor",
        description: "Pull structured data from pages (tables, feeds, JSON-LD).",
        trust: EXTRACTOR_TRUST,
        fanOut: { strategy: "targeted", maxSources: 4, depth: 1 },
        defaultRecipe: "auto:extract:structured",
        loopPolicy: { maxIterations: 4, stopOn: "extract_complete" },
        output: { format: "json", aggregate: "collection" }
      },
      synthesizer: {
        id: "synthesizer",
        description: "Turn gathered pages into a TL;DR / summary / rewrite.",
        trust: SYNTHESIZER_TRUST,
        fanOut: { strategy: "none", maxSources: 1, depth: 1 },
        defaultRecipe: "auto:content:summarize",
        loopPolicy: { maxIterations: 2, stopOn: "summarized" },
        output: { format: "markdown", aggregate: "single" }
      },
      monitor: {
        id: "monitor",
        description: "Watch a URL/selector over time and report changes.",
        trust: MONITOR_TRUST,
        fanOut: { strategy: "none", maxSources: 1, depth: 1 },
        defaultRecipe: "auto:monitor:watch",
        loopPolicy: { maxIterations: 1, stopOn: "captured" },
        output: { format: "diff", aggregate: "snapshot" }
      },
      tester: {
        id: "tester",
        description: "Run a UI smoke/regression test with human gate on destructive steps.",
        trust: TESTER_TRUST,
        fanOut: { strategy: "none", maxSources: 1, depth: 1 },
        defaultRecipe: "auto:test:ui",
        loopPolicy: { maxIterations: 3, stopOn: "asserted" },
        output: { format: "report", aggregate: "single" }
      }
    };
  }
});

// src/server/response.ts
function corsHeaders() {
  return {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, PUT, PATCH, DELETE, OPTIONS, QUERY",
    "Access-Control-Allow-Headers": "Content-Type, Authorization"
  };
}
function json(data, status = 200) {
  const etag = `"${Date.now()}"`;
  const replacer = (_key, value) => typeof value === "bigint" ? value.toString() : value;
  return new Response(JSON.stringify(data, replacer), {
    status,
    headers: { "Content-Type": "application/json", ETag: etag, ...corsHeaders() }
  });
}
function errorResponse(message, code, status = 500, details) {
  return json({ error: message, code, details }, status);
}
var init_response = __esm({
  "src/server/response.ts"() {
    "use strict";
  }
});

// src/server/automation-router.ts
function createAutomationRouter(deps) {
  const { orchestrator } = deps;
  return async function automationRouter(req, url) {
    if (!url.pathname.startsWith("/api/automate/")) return null;
    try {
      if (url.pathname === "/api/automate/recipes" && req.method === "GET") {
        return json({
          ok: true,
          recipes: orchestrator.listRecipes().map((r) => ({ id: r.id, description: r.description }))
        });
      }
      if (url.pathname === "/api/automate/roles" && req.method === "GET") {
        return json({
          ok: true,
          roles: Object.values(AGENT_ROLES).map((r) => ({
            id: r.id,
            description: r.description,
            trust: r.trust
          }))
        });
      }
      if (url.pathname === "/api/automate/run" && req.method === "POST") {
        const body = await req.json().catch(() => ({}));
        const { role, recipeId, intent, params, destructive } = body;
        if (!role) {
          return errorResponse("Missing required field: role", "BadRequest", 400);
        }
        const result = await orchestrator.run({
          role,
          recipeId,
          intent: intent ?? "auto",
          params: params ?? {},
          destructive: destructive ?? false
        });
        return json({ ok: true, ...result });
      }
      return errorResponse(`Unknown automation route: ${url.pathname}`, "NotFound", 404);
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      const code = err instanceof EngineError ? 422 : 500;
      return errorResponse(msg, "AutomationError", code);
    }
  };
}
var init_automation_router = __esm({
  "src/server/automation-router.ts"() {
    "use strict";
    init_agents();
    init_errors();
    init_response();
  }
});

// src/server/autonomous-router.ts
function json2(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { "Content-Type": "application/json" }
  });
}
function error(message, status = 400) {
  return json2({ error: message }, status);
}
function createAutonomousRouter(deps) {
  const { autonomousEngine } = deps;
  return async function handleAutonomousRoute(req, url) {
    const path = url.pathname;
    if (path === "/api/autonomous/execute" && req.method === "POST") {
      const body = await req.json();
      if (!body.goal?.description) return error("goal.description is required");
      const task = await autonomousEngine.execute(body.goal);
      return json2({ taskId: task.id, status: task.status });
    }
    if (path === "/api/autonomous/tasks" && req.method === "GET") {
      const status = url.searchParams.get("status") ?? void 0;
      const limit = Number.parseInt(url.searchParams.get("limit") ?? "50", 10);
      const tasks = await autonomousEngine.listTasks({ status, limit });
      return json2({
        tasks: tasks.map((t) => ({
          id: t.id,
          status: t.status,
          goal: t.goal.description,
          startedAt: t.startedAt,
          completedAt: t.completedAt
        }))
      });
    }
    if (path === "/api/autonomous/gates" && req.method === "GET") {
      const gates = await autonomousEngine.getPendingGates();
      return json2({ gates });
    }
    const gateResolveMatch = path.match(/^\/api\/autonomous\/gates\/([^/]+)\/resolve$/);
    if (gateResolveMatch && req.method === "POST") {
      const gateId = gateResolveMatch[1] ?? "";
      const body = await req.json();
      if (!body.response || !body.resolvedBy) return error("response and resolvedBy are required");
      await autonomousEngine.resolveGate(gateId, body.response, body.resolvedBy);
      return json2({ ok: true, gateId, response: body.response });
    }
    const statusMatch = path.match(/^\/api\/autonomous\/status\/([^/]+)$/);
    if (statusMatch && req.method === "GET") {
      const task = await autonomousEngine.getStatus(statusMatch[1] ?? "");
      if (!task) return error("Task not found", 404);
      return json2({ task });
    }
    const cancelMatch = path.match(/^\/api\/autonomous\/([^/]+)\/cancel$/);
    if (cancelMatch && req.method === "POST") {
      const taskId = cancelMatch[1] ?? "";
      await autonomousEngine.cancel(taskId);
      return json2({ ok: true, taskId });
    }
    const replayMatch = path.match(/^\/api\/autonomous\/([^/]+)\/replay$/);
    if (replayMatch && req.method === "POST") {
      const body = await req.json().catch(() => ({}));
      const task = await autonomousEngine.replay(replayMatch[1] ?? "", body.fromStep);
      return json2({ taskId: task.id, status: task.status });
    }
    const traceMatch = path.match(/^\/api\/autonomous\/([^/]+)\/trace$/);
    if (traceMatch && req.method === "GET") {
      const task = await autonomousEngine.getStatus(traceMatch[1] ?? "");
      if (!task) return error("Task not found", 404);
      const trace = task.steps.map((s) => ({
        stepId: s.id,
        stepIndex: s.stepIndex,
        action: s.action,
        classification: s.classification,
        status: s.status,
        startedAt: s.startedAt,
        completedAt: s.completedAt,
        error: s.error,
        result: s.result
      }));
      return json2({ taskId: task.id, trace });
    }
    return null;
  };
}
var init_autonomous_router = __esm({
  "src/server/autonomous-router.ts"() {
    "use strict";
  }
});

// src/server/source-middleware.ts
function extractSource(req) {
  const raw = req.headers.get("X-Source") ?? "unknown";
  const validSources = ["cli", "frontend", "agent", "script"];
  return validSources.includes(raw) ? raw : "unknown";
}
var init_source_middleware = __esm({
  "src/server/source-middleware.ts"() {
    "use strict";
  }
});

// src/server/capability-router.ts
function toDetail(cap) {
  return {
    id: cap.id,
    slug: cap.slug,
    name: cap.name,
    description: cap.description,
    category: cap.category,
    surfaces: cap.surfaces,
    inputSchema: cap.inputSchema,
    outputSchema: cap.outputSchema,
    cliCommand: cap.cliCommand,
    ui: cap.ui,
    uiAction: cap.uiAction,
    apiEndpoint: cap.apiEndpoint,
    workflowNodeType: cap.workflowNodeType,
    mcpToolName: cap.mcpToolName,
    requiresConfirmation: cap.requiresConfirmation,
    tags: cap.tags
  };
}
function createCapabilityRouter(ctx) {
  return async function capabilityRouter(req, url) {
    const registry2 = ctx.registry;
    const _source = extractSource(req);
    if (!registry2) {
      return errorResponse("Capability registry not available", "NotAvailable", 503);
    }
    if (req.method === "GET" && url.pathname === "/api/capabilities") {
      const surface = url.searchParams.get("surface") ?? void 0;
      const category = url.searchParams.get("category") ?? void 0;
      const tag = url.searchParams.get("tag") ?? void 0;
      const caps = registry2.list({ surface, category, tag });
      return json(caps.map(toDetail));
    }
    const execMatch = url.pathname.match(/^\/api\/capabilities\/([^/]+)\/execute$/);
    if (req.method === "POST" && execMatch) {
      const id = decodeURIComponent(execMatch[1] ?? "");
      const cap = registry2.get(id) ?? registry2.getBySlug(id);
      if (!cap) {
        return errorResponse(`Capability ${id} not found`, "NotFound", 404);
      }
      let body = {};
      try {
        const parsed = await req.json();
        if (parsed && typeof parsed === "object") {
          body = parsed;
        }
      } catch {
        body = {};
      }
      const input = body.input ?? {};
      const required = cap.inputSchema.required ?? [];
      for (const key of required) {
        if (!(key in input)) {
          return errorResponse(`Missing required input: ${key}`, "ValidationError", 400);
        }
      }
      const capCtx = {
        conversationId: body.ctx?.conversationId,
        providerId: body.ctx?.providerId,
        slaveId: body.ctx?.slaveId,
        userId: body.ctx?.userId,
        metadata: body.ctx?.metadata ?? {}
      };
      try {
        const start = Date.now();
        const output = await registry2.execute(cap.id, input, capCtx);
        const latencyMs = Date.now() - start;
        ctx.eventBus?.emit({
          type: "capability:executed",
          capabilityId: cap.id,
          latencyMs
        });
        return json({
          ok: true,
          capabilityId: cap.id,
          output,
          traceId: globalThis.crypto?.randomUUID?.() ?? "n/a",
          latencyMs
        });
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        const isValidation = message.startsWith("Missing required input");
        return errorResponse(message, "ExecutionError", isValidation ? 400 : 500);
      }
    }
    const detailMatch = url.pathname.match(/^\/api\/capabilities\/([^/]+)$/);
    if (req.method === "GET" && detailMatch) {
      const id = decodeURIComponent(detailMatch[1] ?? "");
      const cap = registry2.get(id) ?? registry2.getBySlug(id);
      if (!cap) {
        return errorResponse(`Capability ${id} not found`, "NotFound", 404);
      }
      return json(toDetail(cap));
    }
    return errorResponse("Not found", "NotFound", 404);
  };
}
var init_capability_router = __esm({
  "src/server/capability-router.ts"() {
    "use strict";
    init_response();
    init_source_middleware();
  }
});

// src/server/conversation-router.ts
function flattenResolved(resolved) {
  return [
    ...resolved.composer,
    ...resolved.header,
    ...resolved.message,
    ...resolved.sidebar,
    ...resolved.inline
  ];
}
function createConversationRouter(ctx) {
  return async (req) => {
    const url = new URL(req.url);
    const { pathname } = url;
    const method = req.method;
    try {
      if (pathname === "/api/providers" && method === "GET") {
        const providers = await ctx.db.listProviders();
        return json(providers);
      }
      if (pathname.match(/^\/api\/providers\/[^/]+$/) && method === "GET") {
        const id = pathname.split("/")[3];
        if (!id) return errorResponse("Invalid provider id", "ValidationError", 400);
        const provider = await ctx.db.getProvider(id);
        if (!provider) return errorResponse("Provider not found", "NotFoundError", 404);
        return json(provider);
      }
      const capMatch = pathname.match(/^\/api\/providers\/([^/]+)\/capabilities$/);
      if (capMatch && method === "GET") {
        const providerId = capMatch[1];
        if (!providerId) return errorResponse("Invalid provider id", "ValidationError", 400);
        if (!ctx.resolutionEngine) return errorResponse("Engine not wired", "InternalError", 500);
        const planTier = url.searchParams.get("planTier") ?? "free";
        const resolved = await ctx.resolutionEngine.resolve(providerId, planTier);
        return json({ ...resolved, capabilities: flattenResolved(resolved) });
      }
      const convCapMatch = pathname.match(/^\/api\/conversations\/([^/]+)\/capabilities$/);
      if (convCapMatch && method === "GET") {
        const conversationId = convCapMatch[1];
        if (!conversationId) return errorResponse("Invalid conversation id", "ValidationError", 400);
        if (!ctx.resolutionEngine) return errorResponse("Engine not wired", "InternalError", 500);
        const conversation = await ctx.db.getConversation(conversationId);
        if (!conversation) return errorResponse("Conversation not found", "NotFoundError", 404);
        const providerId = conversation.providerId;
        const planTier = url.searchParams.get("planTier") ?? "free";
        const resolved = await ctx.resolutionEngine.resolve(providerId, planTier);
        return json({ ...resolved, capabilities: flattenResolved(resolved) });
      }
      const execMatch = pathname.match(
        /^\/api\/conversations\/([^/]+)\/capabilities\/([^/]+)\/execute$/
      );
      if (execMatch && method === "POST") {
        const conversationId = execMatch[1];
        const slug = execMatch[2];
        if (!conversationId || !slug) {
          return errorResponse("Invalid conversation or capability", "ValidationError", 400);
        }
        if (!ctx.resolutionEngine) return errorResponse("Engine not wired", "InternalError", 500);
        const conversation = await ctx.db.getConversation(conversationId);
        if (!conversation) return errorResponse("Conversation not found", "NotFoundError", 404);
        const providerId = conversation.providerId;
        const resolved = await ctx.resolutionEngine.resolve(providerId, "free");
        const capability = flattenResolved(resolved).find((c) => c.slug === slug);
        if (!capability) return errorResponse("Capability not found", "NotFoundError", 404);
        const traceId = crypto.randomUUID();
        ctx.eventBus.emit({
          type: "capability:progress",
          step: 0,
          total: 1,
          description: `Dispatched ${slug}`,
          moduleId: capability.id,
          slaveId: conversationId
        });
        const governor = ctx.governor;
        let executed;
        let ok = true;
        if (governor?.executeCapability) {
          try {
            const body = await req.json().catch(() => ({}));
            executed = await governor.executeCapability(conversationId, slug, {
              resolver: {
                getConversationProviderId: async (id) => {
                  const conv = await ctx.db.getConversation(id);
                  return conv ? conv.providerId : null;
                }
              },
              capabilityLookup: (s) => ctx.registry?.getBySlug(s) ?? null,
              params: body ?? {}
            });
          } catch (err) {
            ok = false;
            executed = { error: err instanceof Error ? err.message : "execution failed" };
          }
        } else {
          executed = { status: "dispatched", slug, conversationId };
        }
        ctx.eventBus.emit({
          type: "capability:executed",
          capabilityId: capability.id,
          providerId,
          traceId,
          ok,
          latencyMs: 0
        });
        return json({ ok, slug, conversationId, traceId, result: executed });
      }
      if (pathname === "/api/sandbox/debug" && method === "GET") {
        return json({
          providers: await ctx.db.listProviders(),
          recentEvents: ctx.eventBus.snapshot()
        });
      }
      if (pathname === "/api/sandbox/debug" && method === "POST") {
        ctx.eventBus.removeAllListeners();
        ctx.eventBus.clearRecent();
        return json({ status: "reset" });
      }
      if (pathname === "/api/fleet/status" && method === "GET") {
        return json([]);
      }
      if (pathname === "/api/fleet/start" && method === "POST") {
        if (!ctx.governor) return errorResponse("Engine not wired", "InternalError", 500);
        const body = await req.json();
        const slave = await ctx.governor.spawn(body.providerId, body.accountId);
        return json(slave, 201);
      }
      if (pathname === "/api/conversations" && method === "GET") {
        const limit = Number(url.searchParams.get("limit") ?? "50");
        const conversations = await ctx.db.listConversations({ limit });
        return json(conversations);
      }
      if (pathname === "/api/conversations" && method === "POST") {
        const body = await req.json();
        const session = await ctx.db.ensureProviderSession({
          providerId: body.providerId,
          accountId: body.accountId
        });
        const conv = await ctx.db.createConversation({
          id: crypto.randomUUID(),
          providerSessionId: session.id,
          providerId: body.providerId,
          title: body.title
        });
        return json(conv, 201);
      }
      const sendMatch = pathname.match(/^\/api\/conversations\/([^/]+)\/send$/);
      if (sendMatch && method === "POST") {
        const conversationId = sendMatch[1];
        if (!conversationId) return errorResponse("Invalid conversation id", "ValidationError", 400);
        if (!ctx.conversationManager) return errorResponse("Engine not wired", "InternalError", 500);
        const body = await req.json();
        const result = await ctx.conversationManager.send(conversationId, body.message);
        return json(result);
      }
      const msgsMatch = pathname.match(/^\/api\/conversations\/([^/]+)\/messages$/);
      if (msgsMatch && method === "GET") {
        const conversationId = msgsMatch[1];
        if (!conversationId) return errorResponse("Invalid conversation id", "ValidationError", 400);
        const limit = Number(url.searchParams.get("limit") ?? "100");
        const messages = await ctx.db.getMessages(conversationId, { limit });
        return json(messages);
      }
      if (pathname === "/api/admin/seed" && method === "POST") {
        return json({ status: "ok" });
      }
      if (pathname === "/api/health/providers" && method === "GET") {
        const healthKernel = ctx.healthKernel;
        if (!healthKernel) {
          return json({});
        }
        const allHealth = healthKernel.getAllHealth();
        const result = {};
        for (const [providerId, h] of allHealth) {
          result[providerId] = h;
        }
        return json(result);
      }
      const mirrorMatch = pathname.match(/^\/api\/conversations\/([^/]+)\/mirror$/);
      if (mirrorMatch && method === "GET") {
        const convId = mirrorMatch[1];
        if (!convId) return errorResponse("Invalid conversation id", "ValidationError", 400);
        const mirror = ctx.mirror;
        if (!mirror) return json({ chrome: {}, ui: {}, lastSyncAt: 0, pendingUpdates: 0 });
        const state = await mirror.projectState(convId);
        return json(state);
      }
      const configMatch = pathname.match(/^\/api\/config\/([^/]+)$/);
      if (configMatch && method === "GET") {
        const engineId = configMatch[1];
        if (!engineId) return errorResponse("Invalid engine id", "ValidationError", 400);
        const config2 = await ctx.db.getConfig(engineId);
        return json(config2);
      }
      if (pathname === "/api/config/governor" && method === "GET") {
        const govConfig = ctx.governor?.config ?? {};
        return json({
          fleetConfig: {
            portRange: govConfig.portRange ?? [9300, 9400],
            healthProbeIntervalMs: govConfig.healthProbeIntervalMs ?? 3e4,
            autoRestart: govConfig.autoRestart ?? true,
            maxRestarts: govConfig.maxRestarts ?? 3,
            circuitBreakerThreshold: govConfig.circuitBreakerThreshold ?? 5,
            circuitBreakerResetMs: govConfig.circuitBreakerResetMs ?? 6e4
          },
          chromeConfig: {
            path: process.env.CHROME_PATH ?? null,
            extraArgs: [],
            disableGpu: false
          }
        });
      }
      if (pathname === "/api/config/governor" && method === "PUT") {
        const body = await req.json();
        await ctx.db.setConfig("governor", JSON.stringify(body));
        return json({ ok: true, note: "Restart required for fleet config changes" });
      }
      return errorResponse("Not found", "NotFoundError", 404);
    } catch (err) {
      const message = err instanceof Error ? err.message : "Internal error";
      return errorResponse(message, "InternalError", 500);
    }
  };
}
var init_conversation_router = __esm({
  "src/server/conversation-router.ts"() {
    "use strict";
    init_response();
  }
});

// src/server/knowledge-router.ts
function createKnowledgeRouter(ctx) {
  return async (req) => {
    const url = new URL(req.url);
    const { pathname } = url;
    const method = req.method;
    const _source = extractSource(req);
    try {
      if (pathname === "/api/knowledge/ingest" && method === "POST") {
        const body = await req.json();
        if (!body.source || !body.filePath) {
          return errorResponse("source and filePath required", "ValidationError", 400);
        }
        const result = await ctx.knowledgeIngestion?.ingest({
          source: body.source,
          filePath: body.filePath,
          deduplicate: body.deduplicate ?? true,
          extractEntities: body.extractEntities ?? true,
          extractDecisions: body.extractDecisions ?? true,
          generateEmbeddings: body.generateEmbeddings ?? true
        });
        if (!result) return errorResponse("Engine not wired", "InternalError", 500);
        return json(result, 201);
      }
      if (pathname === "/api/knowledge/search" && method === "GET") {
        const q = url.searchParams.get("q") ?? "";
        if (!q) return errorResponse("q parameter required", "ValidationError", 400);
        const limit = Number(url.searchParams.get("limit") ?? "20");
        const results = await ctx.semanticSearch?.search({ text: q, limit });
        if (!results) return errorResponse("Engine not wired", "InternalError", 500);
        return json(results);
      }
      if (pathname === "/api/knowledge/synthesize" && method === "POST") {
        const body = await req.json();
        if (!body.question) return errorResponse("question required", "ValidationError", 400);
        const result = await ctx.synthesizer?.synthesize({
          question: body.question,
          maxSources: body.maxSources ?? 10,
          synthesisStyle: body.synthesisStyle ?? "summary",
          scope: {}
        });
        if (!result) return errorResponse("Engine not wired", "InternalError", 500);
        return json(result);
      }
      if (pathname === "/api/knowledge/export" && method === "GET") {
        const format = url.searchParams.get("format") ?? "json";
        const scope = url.searchParams.get("scope") ?? "full";
        const result = await ctx.exportEngine?.export({
          format,
          scope,
          outputPath: `export_${Date.now()}.${format}`,
          includeEmbeddings: url.searchParams.get("includeEmbeddings") !== "false"
        });
        if (!result) return errorResponse("Engine not wired", "InternalError", 500);
        return json(result);
      }
      if (pathname === "/api/knowledge/entities" && method === "GET") {
        const entityType = url.searchParams.get("type") ?? void 0;
        const entities = await ctx.db.prisma.entity.findMany({
          where: entityType ? { type: entityType } : void 0,
          take: 100
        });
        return json(entities);
      }
      if (pathname === "/api/knowledge/decisions" && method === "GET") {
        const conversationId = url.searchParams.get("conversationId") ?? void 0;
        const decisions = await ctx.db.prisma.decisionRecord.findMany({
          where: conversationId ? { conversationId } : void 0,
          take: 100
        });
        return json(decisions);
      }
      if (pathname === "/api/knowledge/topics" && method === "GET") {
        const topics = await ctx.db.prisma.topic.findMany({ take: 100 });
        return json(topics);
      }
      if (pathname === "/api/knowledge/topics" && method === "POST") {
        const body = await req.json();
        if (!body.name) return errorResponse("name required", "ValidationError", 400);
        const now = Date.now();
        const topic = await ctx.db.prisma.topic.create({
          data: {
            id: newId(),
            name: body.name,
            description: body.description ?? null,
            createdAt: now,
            updatedAt: now
          }
        });
        return json(topic, 201);
      }
      if (pathname === "/api/knowledge/jobs" && method === "GET") {
        const jobs = await ctx.db.prisma.importJob.findMany({
          orderBy: { startedAt: "desc" },
          take: 50
        });
        return json(jobs);
      }
      const jobMatch = pathname.match(/^\/api\/knowledge\/jobs\/([^/]+)$/);
      if (jobMatch && method === "GET") {
        const jobId = jobMatch[1];
        if (!jobId) return errorResponse("Invalid job id", "ValidationError", 400);
        const job = await ctx.db.prisma.importJob.findUnique({ where: { id: jobId } });
        if (!job) return errorResponse("Job not found", "NotFoundError", 404);
        return json(job);
      }
      return errorResponse("Not found", "NotFoundError", 404);
    } catch (err) {
      const message = err instanceof Error ? err.message : "Internal error";
      return errorResponse(message, "InternalError", 500);
    }
  };
}
var init_knowledge_router = __esm({
  "src/server/knowledge-router.ts"() {
    "use strict";
    init_ids();
    init_response();
    init_source_middleware();
  }
});

// src/server/mux-router.ts
function createMuxRouter(ctx) {
  return async (req) => {
    const url = new URL(req.url);
    const { pathname } = url;
    const method = req.method;
    const _source = extractSource(req);
    try {
      if (pathname === "/api/route/auto" && method === "POST") {
        if (!ctx.providerMux) {
          return errorResponse("ProviderMuxEngine not wired", "InternalError", 500);
        }
        const body = await req.json();
        if (!body.message) {
          return errorResponse("message required", "ValidationError", 400);
        }
        const result = await ctx.providerMux.autoRoute(body.message, body.capabilityId);
        return json(result);
      }
      if (pathname === "/api/route/mux" && method === "POST") {
        if (!ctx.providerMux) {
          return errorResponse("ProviderMuxEngine not wired", "InternalError", 500);
        }
        const body = await req.json();
        if (!body.message) {
          return errorResponse("message required", "ValidationError", 400);
        }
        const result = await ctx.providerMux.mux({
          message: body.message,
          strategy: body.strategy ?? "fan_out",
          targetProviderIds: body.targetProviderIds,
          maxProviders: body.maxProviders ?? 3,
          synthesisEnabled: body.synthesisEnabled ?? false,
          costBudgetCents: body.costBudgetCents,
          timeoutMs: body.timeoutMs ?? 3e4,
          conversationId: body.conversationId,
          capabilityId: body.capabilityId
        });
        return json(result);
      }
      if (pathname === "/api/route/fanout" && method === "POST") {
        if (!ctx.providerMux) {
          return errorResponse("ProviderMuxEngine not wired", "InternalError", 500);
        }
        const body = await req.json();
        if (!body.message || !body.providerIds?.length) {
          return errorResponse("message and providerIds required", "ValidationError", 400);
        }
        const result = await ctx.providerMux.mux({
          message: body.message,
          strategy: "fan_out",
          targetProviderIds: body.providerIds,
          maxProviders: body.providerIds.length,
          synthesisEnabled: body.synthesisEnabled ?? false,
          timeoutMs: body.timeoutMs ?? 3e4
        });
        return json(result);
      }
      if (pathname === "/api/route/cost-report" && method === "GET") {
        if (!ctx.costOptimizer) {
          return errorResponse("CostOptimizer not wired", "InternalError", 500);
        }
        const providerId = url.searchParams.get("providerId");
        const defaultFrom = Date.now() - 30 * 24 * 60 * 60 * 1e3;
        const from = Number(url.searchParams.get("from") ?? String(defaultFrom));
        const to = Number(url.searchParams.get("to") ?? String(Date.now()));
        if (!providerId) {
          const summaries = await ctx.costOptimizer.getProviderSummaries(from, to);
          return json({ summaries });
        }
        const report = await ctx.costOptimizer.getCostReport(providerId, from, to);
        return json(report);
      }
      if (pathname === "/api/route/preferences" && method === "GET") {
        if (!ctx.providerMux) {
          return errorResponse("ProviderMuxEngine not wired", "InternalError", 500);
        }
        const capabilityId = url.searchParams.get("capabilityId") ?? void 0;
        const muxStore = ctx.providerMux.store;
        const prefs = await muxStore.getRoutingPreferences(capabilityId);
        return json({ preferences: prefs });
      }
      return errorResponse("Not found", "NotFound", 404);
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      return errorResponse(message, "InternalError", 500);
    }
  };
}
var init_mux_router = __esm({
  "src/server/mux-router.ts"() {
    "use strict";
    init_response();
    init_source_middleware();
  }
});

// src/server/nlcl-router.ts
function createNLCLRouter(engine) {
  return async function nlclRouter(req) {
    const url = new URL(req.url);
    const path = url.pathname.replace("/api/nlcl/", "");
    switch (path) {
      case "interpret": {
        if (req.method !== "POST") {
          return errorResponse("Method not allowed", "MethodNotAllowed", 405);
        }
        try {
          const body = await req.json();
          const rawInput = body.input?.trim();
          if (!rawInput) {
            return errorResponse('Missing "input" field', "ValidationError", 400);
          }
          const ctx = {
            surface: body.surface ?? "api",
            providerId: body.providerId,
            conversationId: body.conversationId,
            workspacePath: body.workspacePath,
            metadata: {}
          };
          const result = await engine.interpret(rawInput, ctx);
          return json(result);
        } catch (err) {
          return errorResponse(
            err instanceof Error ? err.message : "Interpretation failed",
            "NLCLError",
            500
          );
        }
      }
      case "commands": {
        const category = url.searchParams.get("category") ?? void 0;
        const commands = engine.listCommands({ category });
        return json({
          commands: commands.map((c) => ({
            id: c.id,
            intent: c.intent,
            description: c.description,
            category: c.category,
            examples: c.examples,
            aliases: c.aliases,
            executor: c.executor
          })),
          total: commands.length
        });
      }
      case "help": {
        const help = engine.getHelp();
        return json(help);
      }
      case "audit": {
        const limit = Number.parseInt(url.searchParams.get("limit") ?? "50");
        const log = engine.getAuditLog(limit);
        return json({ entries: log, count: log.length });
      }
      case "parse": {
        if (req.method !== "POST") {
          return errorResponse("Method not allowed", "MethodNotAllowed", 405);
        }
        try {
          const body = await req.json();
          const rawInput = body.input?.trim();
          if (!rawInput) {
            return errorResponse('Missing "input" field', "ValidationError", 400);
          }
          const ctx = { surface: "api", metadata: {} };
          const result = await engine.interpret(rawInput, ctx);
          return json({
            ok: result.ok,
            intent: result.intent,
            text: result.text,
            output: result.output,
            error: result.error,
            latencyMs: result.latencyMs
          });
        } catch (err) {
          return errorResponse(
            err instanceof Error ? err.message : "Parse failed",
            "NLCLError",
            500
          );
        }
      }
      default:
        return errorResponse(`Unknown NLCL endpoint: ${path}`, "NotFound", 404);
    }
  };
}
var init_nlcl_router = __esm({
  "src/server/nlcl-router.ts"() {
    "use strict";
    init_response();
  }
});

// src/__generated__/provider-protocol.dev.ts
var provider_protocol_dev_exports = {};
__export(provider_protocol_dev_exports, {
  default: () => provider_protocol_dev_default,
  protocol: () => protocol
});
var _protocol, provider_protocol_dev_default, protocol;
var init_provider_protocol_dev = __esm({
  "src/__generated__/provider-protocol.dev.ts"() {
    "use strict";
    _protocol = {
      schemaVersion: 1,
      generatedAt: 1784412491132,
      providerCount: 13,
      providers: [
        {
          slug: "chatgpt",
          displayName: "ChatGPT",
          description: "OpenAI's ChatGPT assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://chatgpt.com",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://chatgpt\\.com",
          url: "https://chatgpt.com",
          loginUrl: "https://chatgpt.com/auth/login",
          composerSelectors: ["#prompt-textarea"],
          sendButtonSelectors: ["[data-testid='send-button']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://chatgpt.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://chatgpt.com",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "#prompt-textarea",
              sendButtonSelector: "[data-testid='send-button']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Login",
              url: "https://chatgpt.com/auth/login",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "chatgpt/001_openai_delta",
              version: 1,
              logicCode: `
function extractOpenAIBlock(data) {
  const api = data.choices && data.choices[0] && data.choices[0].delta && data.choices[0].delta.content;
  if (api !== undefined && api !== null) return { type: 'text', text: String(api) };
  if (data.o === 'patch' && Array.isArray(data.v)) {
    const parts = [];
    for (const patch of data.v) {
      if (patch && typeof patch === 'object' && typeof patch.p === 'string' && typeof patch.v === 'string') {
        if (patch.p.indexOf('/message/content/parts/') === 0 && (patch.o === 'append' || patch.o === 'add' || patch.o === 'replace')) parts.push(patch.v);
      }
    }
    if (parts.length > 0) return { type: 'text', text: parts.join('') };
  }
  if (data.o === 'add' && data.v && data.v.message && data.v.message.content && data.v.message.content.parts) {
    return fromParts(data.v.message.content.parts);
  }
  const cp = data.message && data.message.content && data.message.content.parts;
  if (Array.isArray(cp)) return fromParts(cp);
  return null;
}
function fromParts(parts) {
  const blocks = [];
  for (const p of parts) {
    if (typeof p === 'string') blocks.push({ type: 'text', text: p });
    else if (p && typeof p === 'object' && 'asset_pointer' in p) blocks.push({ type: 'file', url: String(p.asset_pointer), mediaType: 'image/png', filename: '' });
  }
  if (blocks.length === 0) return null;
  return blocks.length === 1 ? blocks[0] : blocks[0];
}
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const block = extractOpenAIBlock(data);
      if (block) {
        if (Array.isArray(block)) blocks.push.apply(blocks, block);
        else blocks.push(block);
      }
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finish_reason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('choices') && b.includes('delta')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'chatgpt/001_openai_delta', version: 1, providerId: 'chatgpt', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "e3bc30a621c72a9aa916b583dd3a36b0a7f85ce075a76f1ed7e472c5687b70ff",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select GPT Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to ChatGPT",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "email", base_url: "https://chatgpt.com" }
        },
        {
          slug: "claude",
          displayName: "Claude",
          description: "Anthropic's Claude AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://claude.ai",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://claude\\.ai",
          url: "https://claude.ai/chat",
          loginUrl: "https://claude.ai/login",
          composerSelectors: ["[contenteditable]", "textarea"],
          sendButtonSelectors: ["[aria-label='Send Message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://claude.ai",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://claude.ai/chat",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "[contenteditable]",
              sendButtonSelector: "[aria-label='Send Message']",
              composerType: "prosemirror",
              sendMethod: "both",
              contentEditable: true
            },
            {
              label: "Login",
              url: "https://claude.ai/login",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "claude/001_streaming_sse",
              version: 1,
              logicCode: "\nfunction parse(rawBody) {\n  const blocks = [];\n  const lines = String(rawBody).split('\\n');\n  for (const line of lines) {\n    const trimmed = line.trim();\n    if (!trimmed.startsWith('data:')) continue;\n    const payload = trimmed.slice(5).trim();\n    if (payload === '[DONE]') break;\n    try {\n      const json = JSON.parse(payload);\n      if (json.type === 'content_block_start' && json.content_block) {\n        const cb = json.content_block;\n        if (cb.type === 'thinking') blocks.push({ type: 'reasoning', text: '' });\n        else if (cb.type === 'tool_use') blocks.push({ type: 'tool-call', toolCallId: 'tc_' + blocks.length, toolName: String(cb.name || ''), input: cb.input || {} });\n        else if (cb.type === 'image' || cb.type === 'image_url') blocks.push({ type: 'file', mediaType: cb.source && cb.source.type === 'image/jpeg' ? 'image/jpeg' : 'image/png', url: String((cb.source && cb.source.url) || cb.url || ''), filename: String(cb.alt || '') });\n        else if (cb.type === 'text') blocks.push({ type: 'text', text: String(cb.text || '') });\n      }\n      if (json.type === 'content_block_delta' && json.delta) {\n        const delta = json.delta;\n        if (typeof delta.text === 'string') {\n          const last = blocks[blocks.length - 1];\n          if (last && last.type === 'text') last.text += delta.text;\n          else blocks.push({ type: 'text', text: delta.text });\n        } else if (typeof delta.thinking === 'string') {\n          const last = blocks[blocks.length - 1];\n          if (last && last.type === 'reasoning') last.text += delta.thinking;\n          else blocks.push({ type: 'reasoning', text: delta.thinking });\n        }\n      }\n      if (json.type === 'message_start' && json.message) blocks.push({ type: 'meta', key: 'message_id', value: json.message.id });\n      if (json.type === 'message_stop' || json.type === 'error') {\n        const last = blocks[blocks.length - 1];\n        if (last && last.type !== 'meta') blocks.push({ type: 'meta', key: 'stopped', value: json.type });\n      }\n    } catch (_e) { /* skip unparseable lines */ }\n  }\n  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });\n  return blocks;\n}\nfunction detectCompletion(rawBody) {\n  return String(rawBody).includes('message_stop') || String(rawBody).includes('[DONE]');\n}\nfunction getConfidence(rawBody) {\n  const b = String(rawBody);\n  const hasData = b.includes('data:');\n  const hasContent = b.includes('content_block_delta') || b.includes('content_block_start');\n  if (!hasData) return 0;\n  if (!hasContent) return b.includes('message_stop') ? 0.7 : 0.3;\n  return 1;\n}\nmodule.exports.default = { name: 'claude/001_streaming_sse', version: 1, providerId: 'claude', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };\n",
              hash: "9d411b264658a07722e44cd81c6a99fd9daea0cb8fbd3be181a1e50c70bc2131",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "deep_research",
              recoveryStrategies: [],
              uiComponentOverride: "action_button",
              uiLabelOverride: "Deep Research",
              uiIconOverride: "flask",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "secondary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "pro",
              confidence: 1
            },
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Claude Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [
                { type: "retry_selector" },
                { type: "retry_with_fallback", config: { fallback_selector: "textarea" } },
                { type: "navigate_home" }
              ],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Claude",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "toggle_extended_thinking",
              recoveryStrategies: [],
              uiComponentOverride: "toggle_switch",
              uiLabelOverride: "Extended Thinking",
              uiIconOverride: "",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "secondary",
              interactionModeOverride: "",
              existentialRuleOverride: "message_has_thinking_block",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "email", base_url: "https://claude.ai" }
        },
        {
          slug: "deepseek",
          displayName: "DeepSeek",
          description: "DeepSeek AI assistant with strong coding and reasoning capabilities",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://chat.deepseek.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://chat\\.deepseek\\.com",
          url: "https://chat.deepseek.com",
          loginUrl: "https://chat.deepseek.com/sign_in",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ["button[aria-label='Send']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://chat.deepseek.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://chat.deepseek.com",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "textarea",
              sendButtonSelector: "button[aria-label='Send']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Login",
              url: "https://chat.deepseek.com/sign_in",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to DeepSeek",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "email", base_url: "https://chat.deepseek.com" }
        },
        {
          slug: "facebook",
          displayName: "Facebook Messenger",
          description: "Facebook Messenger channel",
          category: "messaging",
          providerType: "messaging",
          authType: "browser",
          websiteUrl: "https://www.facebook.com/messages",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://www\\.facebook\\.com/messages",
          url: "https://www.facebook.com/messages",
          loginUrl: "https://www.facebook.com/messages/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Web",
              url: "https://www.facebook.com/messages",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "cdp-scrape" }
        },
        {
          slug: "gemini",
          displayName: "Gemini",
          description: "Google's Gemini AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://gemini.google.com",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://gemini\\.google\\.com",
          url: "https://gemini.google.com/app",
          loginUrl: "https://accounts.google.com",
          composerSelectors: [".ql-editor", "textarea"],
          sendButtonSelectors: ["button[aria-label='Send message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://gemini.google.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://gemini.google.com/app",
              endpointType: "chat",
              isDefault: false,
              composerSelector: ".ql-editor",
              sendButtonSelector: "button[aria-label='Send message']",
              composerType: "quill",
              sendMethod: "both",
              contentEditable: true
            },
            {
              label: "Login",
              url: "https://accounts.google.com",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "gemini/002_ai_studio",
              version: 2,
              logicCode: `
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const text = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (text !== undefined && text !== null) blocks.push({ type: 'text', text: String(text) });
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finishReason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('candidates') && b.includes('parts')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'gemini/002_ai_studio', version: 2, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "33f7644ff246d8620374c97b1d52add8a55723e3cf6a4ef291a62e88b32d5132",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            },
            {
              name: "gemini/001_batchexecute",
              version: 1,
              logicCode: `
function safeJsonParse(s) { try { return JSON.parse(s); } catch (_e) { return s; } }
function decodeEnvelope(raw) {
  const frames = [];
  let text = String(raw);
  if (text.indexOf(")]}'\\n") === 0) text = text.slice(5);
  const lines = text.split('\\n');
  for (const line of lines) {
    const t = line.trim();
    if (!t) continue;
    if (/^\\d+$/.test(t)) continue;
    if (t.charAt(0) !== '[') continue;
    try {
      const arr = JSON.parse(t);
      if (!Array.isArray(arr)) continue;
      let isTerminal = false;
      for (const child of arr) { if (Array.isArray(child) && child[0] === 'e') { isTerminal = true; break; } }
      for (const child of arr) {
        if (Array.isArray(child) && child[0] === 'wrb.fr') {
          frames.push({ rpc: String(child[1] || ''), payload: typeof child[2] === 'string' ? safeJsonParse(child[2]) : child[2], error: child[5] != null ? child[5] : null, isTerminal: isTerminal });
        }
      }
      if (isTerminal && frames.length === 0) frames.push({ rpc: '', payload: null, error: null, isTerminal: true });
    } catch (_e) { /* skip */ }
  }
  return frames;
}
function parseStreamChunk(frame) {
  const payload = frame.payload;
  if (!Array.isArray(payload)) return null;
  // Real gemini batchexecute: payload[4] holds [[deltaText]] (or payload[3]).
  const candidate = payload[4] != null ? payload[4] : payload[3];
  const textArr = Array.isArray(candidate) ? candidate[0] : undefined;
  // text lives at textArr[0]; some envelopes double-wrap as textArr[1].
  let deltaArr = Array.isArray(textArr) ? (textArr[1] != null ? textArr[1] : textArr[0]) : undefined;
  if (typeof deltaArr === 'undefined' && Array.isArray(textArr)) deltaArr = textArr[0];
  let textDelta = '';
  if (Array.isArray(deltaArr)) textDelta = deltaArr.filter(function (s) { return typeof s === 'string'; }).join('');
  else if (typeof deltaArr === 'string') textDelta = deltaArr;
  return textDelta ? { textDelta: textDelta } : null;
}
function parse(rawBody) {
  const blocks = [];
  const frames = decodeEnvelope(rawBody);
  for (const frame of frames) {
    if (frame.isTerminal) continue;
    if (frame.rpc) {
      const delta = parseStreamChunk(frame);
      if (delta && delta.textDelta) blocks.push({ type: 'text', text: delta.textDelta });
      else blocks.push({ type: 'meta', key: 'gemini_' + frame.rpc, value: frame.payload });
    }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[["e"') || b.includes('"e"') || b.includes('"isTerminal":true');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.indexOf(')]}\\'') === 0 || b.includes('wrb.fr')) return 1;
  if (b.includes('[[')) return 0.6;
  return 0.2;
}
module.exports.default = { name: 'gemini/001_batchexecute', version: 1, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "61abc26e092bbdd21e4679bcfcec4e6923ef1d9ca8f82298d479c1bf5cfe6fdc",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Gemini Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Gemini",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "google", base_url: "https://gemini.google.com" }
        },
        {
          slug: "generic",
          displayName: "Generic",
          description: "Format-agnostic fallback provider for the parser chain",
          category: "system",
          providerType: "system",
          authType: "none",
          websiteUrl: "https://cap-store.local",
          profileStrategy: "none",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://cap-store\\.local",
          url: "https://cap-store.local",
          loginUrl: "https://cap-store.local/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [],
          parsers: [
            {
              name: "generic/001_format_agnostic",
              version: 1,
              logicCode: `
function parse(rawBody) {
  const b = String(rawBody);
  const blocks = [];
  // SSE data: frames
  const lines = b.split('\\n');
  let sawSse = false;
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    sawSse = true;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') continue;
    try {
      const data = JSON.parse(payload);
      const choices = data.choices && data.choices[0];
      const delta = choices && choices.delta && choices.delta.content;
      if (typeof delta === 'string' && delta) { blocks.push({ type: 'text', text: delta }); continue; }
      const parts = choices && choices.message && choices.message.content && choices.message.content.parts;
      if (Array.isArray(parts)) { blocks.push({ type: 'text', text: parts.join('') }); continue; }
      const studio = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (typeof studio === 'string' && studio) { blocks.push({ type: 'text', text: studio }); continue; }
    } catch (_e) { /* not JSON, leave to raw fallback */ }
  }
  // Gemini batchexecute array
  if (!blocks.length && b.indexOf('[[') === 0) {
    try {
      const arr = JSON.parse(b);
      const walk = function (node) {
        if (typeof node === 'string') blocks.push({ type: 'text', text: node });
        else if (Array.isArray(node)) node.forEach(walk);
      };
      walk(arr);
    } catch (_e) { /* leave to raw */ }
  }
  if (!blocks.length) blocks.push({ type: 'text', text: b });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[DONE]') || b.includes('message_stop') || b.indexOf('[["e"') === 0;
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.includes('choices') || b.includes('candidates') || b.includes('wrb.fr')) return 0.4;
  if (b.includes('data:') || b.includes('[[')) return 0.2;
  return 0.1;
}
module.exports.default = { name: 'generic/001_format_agnostic', version: 1, providerId: 'generic', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "71497fc3099f4014bd4e21200ddf46f30e92f4594aa9ff109c7c21413545923e",
              isActive: true,
              fallbackParserName: "system/001_raw_text"
            }
          ],
          streamConfigs: [],
          capabilities: [],
          config: {}
        },
        {
          slug: "qwen",
          displayName: "Qwen",
          description: "Alibaba's Qwen AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://tongyi.aliyun.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://tongyi\\.aliyun\\.com",
          url: "https://tongyi.aliyun.com/qianwen",
          loginUrl: "https://tongyi.aliyun.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ["button[aria-label='Send']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://tongyi.aliyun.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://tongyi.aliyun.com/qianwen",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "textarea",
              sendButtonSelector: "button[aria-label='Send']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Qwen Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Qwen",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "email", base_url: "https://tongyi.aliyun.com" }
        },
        {
          slug: "slack",
          displayName: "Slack",
          description: "Slack messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "oauth",
          websiteUrl: "https://api.slack.com",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://api\\.slack\\.com",
          url: "https://api.slack.com",
          loginUrl: "https://api.slack.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "API",
              url: "https://slack.com/api",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "webhook" }
        },
        {
          slug: "studio-ai",
          displayName: "Studio AI",
          description: "Google AI Studio \u2014 Gemini API playground and prototyping environment",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://aistudio.google.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://aistudio\\.google\\.com",
          url: "https://aistudio.google.com/prompts/new_chat",
          loginUrl: "https://aistudio.google.com/login",
          composerSelectors: ["rich-textarea"],
          sendButtonSelectors: ["button[aria-label='Send message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://aistudio.google.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://aistudio.google.com/prompts/new_chat",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "rich-textarea",
              sendButtonSelector: "button[aria-label='Send message']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Studio AI",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "google", base_url: "https://aistudio.google.com" }
        },
        {
          slug: "system",
          displayName: "System",
          description: "System-level fallback parsers \u2014 universal fallback chain",
          category: "system",
          providerType: "system",
          authType: "none",
          websiteUrl: "https://vivim.app",
          profileStrategy: "shared",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://vivim\\.app",
          url: "https://vivim.app",
          loginUrl: "https://vivim.app/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [],
          parsers: [
            {
              name: "system/001_raw_text",
              version: 1,
              logicCode: "\nfunction parse(rawBody) {\n  const b = String(rawBody);\n  return b.length > 0 ? [{ type: 'text', text: b }] : [];\n}\nfunction detectCompletion() { return true; }\nfunction getConfidence() { return 0.05; }\nmodule.exports.default = { name: 'system/001_raw_text', version: 1, providerId: 'system', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };\n",
              hash: "4403068b29982edbedecd623d76794a334ceacd8d5b7dac27b15929da0f4cab3",
              isActive: true,
              fallbackParserName: ""
            }
          ],
          streamConfigs: [],
          capabilities: [],
          config: {}
        },
        {
          slug: "telegram",
          displayName: "Telegram",
          description: "Telegram messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "api_key",
          websiteUrl: "https://core.telegram.org",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://core\\.telegram\\.org",
          url: "https://core.telegram.org",
          loginUrl: "https://core.telegram.org/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Bot API",
              url: "https://api.telegram.org",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "polling" }
        },
        {
          slug: "whatsapp",
          displayName: "WhatsApp",
          description: "WhatsApp Web messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "browser",
          websiteUrl: "https://web.whatsapp.com",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://web\\.whatsapp\\.com",
          url: "https://web.whatsapp.com",
          loginUrl: "https://web.whatsapp.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Web",
              url: "https://web.whatsapp.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "cdp-scrape" }
        },
        {
          slug: "z-ai",
          displayName: "Z AI",
          description: "Z.AI \u2014 Z.ai's multimodal AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "api",
          websiteUrl: "https://z.ai",
          profileStrategy: "shared",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://z\\.ai",
          url: "https://z.ai",
          loginUrl: "https://z.ai/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "API",
              url: "https://api.z.ai/v1",
              endpointType: "api",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Z AI",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: { auth_type: "api_key", base_url: "https://api.z.ai/v1" }
        }
      ]
    };
    provider_protocol_dev_default = _protocol;
    protocol = _protocol;
  }
});

// src/__generated__/provider-protocol.ts
var provider_protocol_exports = {};
__export(provider_protocol_exports, {
  default: () => provider_protocol_default,
  protocol: () => protocol2
});
var _protocol2, provider_protocol_default, protocol2;
var init_provider_protocol = __esm({
  "src/__generated__/provider-protocol.ts"() {
    "use strict";
    _protocol2 = {
      schemaVersion: 1,
      generatedAt: 1784427929206,
      providerCount: 13,
      providers: [
        {
          slug: "chatgpt",
          displayName: "ChatGPT",
          description: "OpenAI's ChatGPT assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://chatgpt.com",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://chatgpt\\.com",
          url: "https://chatgpt.com",
          loginUrl: "https://chatgpt.com/auth/login",
          composerSelectors: ["#prompt-textarea"],
          sendButtonSelectors: ["[data-testid='send-button']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://chatgpt.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://chatgpt.com",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "#prompt-textarea",
              sendButtonSelector: "[data-testid='send-button']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Login",
              url: "https://chatgpt.com/auth/login",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "chatgpt/001_openai_delta",
              version: 1,
              logicCode: `
function extractOpenAIBlock(data) {
  const api = data.choices && data.choices[0] && data.choices[0].delta && data.choices[0].delta.content;
  if (api !== undefined && api !== null) return { type: 'text', text: String(api) };
  if (data.o === 'patch' && Array.isArray(data.v)) {
    const parts = [];
    for (const patch of data.v) {
      if (patch && typeof patch === 'object' && typeof patch.p === 'string' && typeof patch.v === 'string') {
        if (patch.p.indexOf('/message/content/parts/') === 0 && (patch.o === 'append' || patch.o === 'add' || patch.o === 'replace')) parts.push(patch.v);
      }
    }
    if (parts.length > 0) return { type: 'text', text: parts.join('') };
  }
  if (data.o === 'add' && data.v && data.v.message && data.v.message.content && data.v.message.content.parts) {
    return fromParts(data.v.message.content.parts);
  }
  const cp = data.message && data.message.content && data.message.content.parts;
  if (Array.isArray(cp)) return fromParts(cp);
  return null;
}
function fromParts(parts) {
  const blocks = [];
  for (const p of parts) {
    if (typeof p === 'string') blocks.push({ type: 'text', text: p });
    else if (p && typeof p === 'object' && 'asset_pointer' in p) blocks.push({ type: 'file', url: String(p.asset_pointer), mediaType: 'image/png', filename: '' });
  }
  if (blocks.length === 0) return null;
  return blocks.length === 1 ? blocks[0] : blocks[0];
}
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const block = extractOpenAIBlock(data);
      if (block) {
        if (Array.isArray(block)) blocks.push.apply(blocks, block);
        else blocks.push(block);
      }
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finish_reason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('choices') && b.includes('delta')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'chatgpt/001_openai_delta', version: 1, providerId: 'chatgpt', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "e3bc30a621c72a9aa916b583dd3a36b0a7f85ce075a76f1ed7e472c5687b70ff",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select GPT Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to ChatGPT",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "email",
            base_url: "https://chatgpt.com",
            composer_is_content_editable: "false",
            composer_selector: "#prompt-textarea",
            display_name: "ChatGPT",
            landing_url: "https://chatgpt.com/",
            provider_id: "chatgpt",
            ready_selector: "#prompt-textarea, .ProseMirror",
            sse_format: "openai",
            stream_terminal: "[DONE]",
            stream_transport: "sse",
            stream_url_pattern: "backend-api/conversation"
          }
        },
        {
          slug: "claude",
          displayName: "Claude",
          description: "Anthropic's Claude AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://claude.ai",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://claude\\.ai",
          url: "https://claude.ai/chat",
          loginUrl: "https://claude.ai/login",
          composerSelectors: ["[contenteditable]", "textarea"],
          sendButtonSelectors: ["[aria-label='Send Message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://claude.ai",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://claude.ai/chat",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "[contenteditable]",
              sendButtonSelector: "[aria-label='Send Message']",
              composerType: "prosemirror",
              sendMethod: "both",
              contentEditable: true
            },
            {
              label: "Login",
              url: "https://claude.ai/login",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "claude/001_streaming_sse",
              version: 1,
              logicCode: "\nfunction parse(rawBody) {\n  const blocks = [];\n  const lines = String(rawBody).split('\\n');\n  for (const line of lines) {\n    const trimmed = line.trim();\n    if (!trimmed.startsWith('data:')) continue;\n    const payload = trimmed.slice(5).trim();\n    if (payload === '[DONE]') break;\n    try {\n      const json = JSON.parse(payload);\n      if (json.type === 'content_block_start' && json.content_block) {\n        const cb = json.content_block;\n        if (cb.type === 'thinking') blocks.push({ type: 'reasoning', text: '' });\n        else if (cb.type === 'tool_use') blocks.push({ type: 'tool-call', toolCallId: 'tc_' + blocks.length, toolName: String(cb.name || ''), input: cb.input || {} });\n        else if (cb.type === 'image' || cb.type === 'image_url') blocks.push({ type: 'file', mediaType: cb.source && cb.source.type === 'image/jpeg' ? 'image/jpeg' : 'image/png', url: String((cb.source && cb.source.url) || cb.url || ''), filename: String(cb.alt || '') });\n        else if (cb.type === 'text') blocks.push({ type: 'text', text: String(cb.text || '') });\n      }\n      if (json.type === 'content_block_delta' && json.delta) {\n        const delta = json.delta;\n        if (typeof delta.text === 'string') {\n          const last = blocks[blocks.length - 1];\n          if (last && last.type === 'text') last.text += delta.text;\n          else blocks.push({ type: 'text', text: delta.text });\n        } else if (typeof delta.thinking === 'string') {\n          const last = blocks[blocks.length - 1];\n          if (last && last.type === 'reasoning') last.text += delta.thinking;\n          else blocks.push({ type: 'reasoning', text: delta.thinking });\n        }\n      }\n      if (json.type === 'message_start' && json.message) blocks.push({ type: 'meta', key: 'message_id', value: json.message.id });\n      if (json.type === 'message_stop' || json.type === 'error') {\n        const last = blocks[blocks.length - 1];\n        if (last && last.type !== 'meta') blocks.push({ type: 'meta', key: 'stopped', value: json.type });\n      }\n    } catch (_e) { /* skip unparseable lines */ }\n  }\n  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });\n  return blocks;\n}\nfunction detectCompletion(rawBody) {\n  return String(rawBody).includes('message_stop') || String(rawBody).includes('[DONE]');\n}\nfunction getConfidence(rawBody) {\n  const b = String(rawBody);\n  const hasData = b.includes('data:');\n  const hasContent = b.includes('content_block_delta') || b.includes('content_block_start');\n  if (!hasData) return 0;\n  if (!hasContent) return b.includes('message_stop') ? 0.7 : 0.3;\n  return 1;\n}\nmodule.exports.default = { name: 'claude/001_streaming_sse', version: 1, providerId: 'claude', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };\n",
              hash: "9d411b264658a07722e44cd81c6a99fd9daea0cb8fbd3be181a1e50c70bc2131",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "deep_research",
              recoveryStrategies: [],
              uiComponentOverride: "action_button",
              uiLabelOverride: "Deep Research",
              uiIconOverride: "flask",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "secondary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "pro",
              confidence: 1
            },
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Claude Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [
                { type: "retry_selector" },
                { type: "retry_with_fallback", config: { fallback_selector: "textarea" } },
                { type: "navigate_home" }
              ],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Claude",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "toggle_extended_thinking",
              recoveryStrategies: [],
              uiComponentOverride: "toggle_switch",
              uiLabelOverride: "Extended Thinking",
              uiIconOverride: "",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "secondary",
              interactionModeOverride: "",
              existentialRuleOverride: "message_has_thinking_block",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "email",
            base_url: "https://claude.ai",
            composer_is_content_editable: "true",
            composer_selector: 'div[contenteditable="true"]',
            display_name: "Claude",
            landing_url: "https://claude.ai/new",
            locale: "en",
            locale_variants: "[object Object]",
            provider_id: "claude",
            ready_selector: 'div[contenteditable="true"], fieldset textarea',
            stream_terminal: "message_stop",
            stream_transport: "sse",
            stream_url_pattern: "claude.ai/api"
          }
        },
        {
          slug: "deepseek",
          displayName: "DeepSeek",
          description: "DeepSeek AI assistant with strong coding and reasoning capabilities",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://chat.deepseek.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://chat\\.deepseek\\.com",
          url: "https://chat.deepseek.com",
          loginUrl: "https://chat.deepseek.com/sign_in",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ["button[aria-label='Send']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://chat.deepseek.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://chat.deepseek.com",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "textarea",
              sendButtonSelector: "button[aria-label='Send']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Login",
              url: "https://chat.deepseek.com/sign_in",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to DeepSeek",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "email",
            base_url: "https://chat.deepseek.com",
            composer_is_content_editable: "false",
            composer_selector: "textarea",
            display_name: "DeepSeek",
            landing_url: "https://chat.deepseek.com/",
            provider_id: "deepseek",
            ready_selector: "textarea",
            sse_format: "openai",
            stream_terminal: "[DONE]",
            stream_transport: "sse",
            stream_url_pattern: "deepseek.com"
          }
        },
        {
          slug: "facebook",
          displayName: "Facebook Messenger",
          description: "Facebook Messenger channel",
          category: "messaging",
          providerType: "messaging",
          authType: "browser",
          websiteUrl: "https://www.facebook.com/messages",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://www\\.facebook\\.com/messages",
          url: "https://www.facebook.com/messages",
          loginUrl: "https://www.facebook.com/messages/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Web",
              url: "https://www.facebook.com/messages",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "cdp-scrape" }
        },
        {
          slug: "gemini",
          displayName: "Gemini",
          description: "Google's Gemini AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://gemini.google.com",
          profileStrategy: "per_account",
          hasMultiAccount: true,
          isActive: true,
          urlPattern: "^https://gemini\\.google\\.com",
          url: "https://gemini.google.com/app",
          loginUrl: "https://accounts.google.com",
          composerSelectors: [".ql-editor", "textarea"],
          sendButtonSelectors: ["button[aria-label='Send message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://gemini.google.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://gemini.google.com/app",
              endpointType: "chat",
              isDefault: false,
              composerSelector: ".ql-editor",
              sendButtonSelector: "button[aria-label='Send message']",
              composerType: "quill",
              sendMethod: "both",
              contentEditable: true
            },
            {
              label: "Login",
              url: "https://accounts.google.com",
              endpointType: "login",
              isDefault: false,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [
            {
              name: "gemini/002_ai_studio",
              version: 2,
              logicCode: `
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const text = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (text !== undefined && text !== null) blocks.push({ type: 'text', text: String(text) });
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finishReason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('candidates') && b.includes('parts')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'gemini/002_ai_studio', version: 2, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "33f7644ff246d8620374c97b1d52add8a55723e3cf6a4ef291a62e88b32d5132",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            },
            {
              name: "gemini/001_batchexecute",
              version: 1,
              logicCode: `
function safeJsonParse(s) { try { return JSON.parse(s); } catch (_e) { return s; } }
function decodeEnvelope(raw) {
  const frames = [];
  let text = String(raw);
  if (text.indexOf(")]}'\\n") === 0) text = text.slice(5);
  const lines = text.split('\\n');
  for (const line of lines) {
    const t = line.trim();
    if (!t) continue;
    if (/^\\d+$/.test(t)) continue;
    if (t.charAt(0) !== '[') continue;
    try {
      const arr = JSON.parse(t);
      if (!Array.isArray(arr)) continue;
      let isTerminal = false;
      for (const child of arr) { if (Array.isArray(child) && child[0] === 'e') { isTerminal = true; break; } }
      for (const child of arr) {
        if (Array.isArray(child) && child[0] === 'wrb.fr') {
          frames.push({ rpc: String(child[1] || ''), payload: typeof child[2] === 'string' ? safeJsonParse(child[2]) : child[2], error: child[5] != null ? child[5] : null, isTerminal: isTerminal });
        }
      }
      if (isTerminal && frames.length === 0) frames.push({ rpc: '', payload: null, error: null, isTerminal: true });
    } catch (_e) { /* skip */ }
  }
  return frames;
}
function parseStreamChunk(frame) {
  const payload = frame.payload;
  if (!Array.isArray(payload)) return null;
  // Real gemini batchexecute: payload[4] holds [[deltaText]] (or payload[3]).
  const candidate = payload[4] != null ? payload[4] : payload[3];
  const textArr = Array.isArray(candidate) ? candidate[0] : undefined;
  // text lives at textArr[0]; some envelopes double-wrap as textArr[1].
  let deltaArr = Array.isArray(textArr) ? (textArr[1] != null ? textArr[1] : textArr[0]) : undefined;
  if (typeof deltaArr === 'undefined' && Array.isArray(textArr)) deltaArr = textArr[0];
  let textDelta = '';
  if (Array.isArray(deltaArr)) textDelta = deltaArr.filter(function (s) { return typeof s === 'string'; }).join('');
  else if (typeof deltaArr === 'string') textDelta = deltaArr;
  return textDelta ? { textDelta: textDelta } : null;
}
function parse(rawBody) {
  const blocks = [];
  const frames = decodeEnvelope(rawBody);
  for (const frame of frames) {
    if (frame.isTerminal) continue;
    if (frame.rpc) {
      const delta = parseStreamChunk(frame);
      if (delta && delta.textDelta) blocks.push({ type: 'text', text: delta.textDelta });
      else blocks.push({ type: 'meta', key: 'gemini_' + frame.rpc, value: frame.payload });
    }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[["e"') || b.includes('"e"') || b.includes('"isTerminal":true');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.indexOf(')]}\\'') === 0 || b.includes('wrb.fr')) return 1;
  if (b.includes('[[')) return 0.6;
  return 0.2;
}
module.exports.default = { name: 'gemini/001_batchexecute', version: 1, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "61abc26e092bbdd21e4679bcfcec4e6923ef1d9ca8f82298d479c1bf5cfe6fdc",
              isActive: true,
              fallbackParserName: "generic/001_format_agnostic"
            }
          ],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Gemini Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Gemini",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "google",
            base_url: "https://gemini.google.com",
            composer_is_content_editable: "true",
            composer_selector: ".ql-editor",
            display_name: "Gemini",
            landing_url: "https://gemini.google.com/app",
            provider_id: "gemini",
            ready_selector: ".ql-editor, rich-textarea .ql-editor",
            stream_terminal: '[["e"',
            stream_transport: "batchexecute",
            stream_url_pattern: "batchexecute"
          }
        },
        {
          slug: "generic",
          displayName: "Generic",
          description: "Format-agnostic fallback provider for the parser chain",
          category: "system",
          providerType: "system",
          authType: "none",
          websiteUrl: "https://cap-store.local",
          profileStrategy: "none",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://cap-store\\.local",
          url: "https://cap-store.local",
          loginUrl: "https://cap-store.local/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [],
          parsers: [
            {
              name: "generic/001_format_agnostic",
              version: 1,
              logicCode: `
function parse(rawBody) {
  const b = String(rawBody);
  const blocks = [];
  // SSE data: frames
  const lines = b.split('\\n');
  let sawSse = false;
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    sawSse = true;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') continue;
    try {
      const data = JSON.parse(payload);
      const choices = data.choices && data.choices[0];
      const delta = choices && choices.delta && choices.delta.content;
      if (typeof delta === 'string' && delta) { blocks.push({ type: 'text', text: delta }); continue; }
      const parts = choices && choices.message && choices.message.content && choices.message.content.parts;
      if (Array.isArray(parts)) { blocks.push({ type: 'text', text: parts.join('') }); continue; }
      const studio = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (typeof studio === 'string' && studio) { blocks.push({ type: 'text', text: studio }); continue; }
    } catch (_e) { /* not JSON, leave to raw fallback */ }
  }
  // Gemini batchexecute array
  if (!blocks.length && b.indexOf('[[') === 0) {
    try {
      const arr = JSON.parse(b);
      const walk = function (node) {
        if (typeof node === 'string') blocks.push({ type: 'text', text: node });
        else if (Array.isArray(node)) node.forEach(walk);
      };
      walk(arr);
    } catch (_e) { /* leave to raw */ }
  }
  if (!blocks.length) blocks.push({ type: 'text', text: b });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[DONE]') || b.includes('message_stop') || b.indexOf('[["e"') === 0;
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.includes('choices') || b.includes('candidates') || b.includes('wrb.fr')) return 0.4;
  if (b.includes('data:') || b.includes('[[')) return 0.2;
  return 0.1;
}
module.exports.default = { name: 'generic/001_format_agnostic', version: 1, providerId: 'generic', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`,
              hash: "71497fc3099f4014bd4e21200ddf46f30e92f4594aa9ff109c7c21413545923e",
              isActive: true,
              fallbackParserName: "system/001_raw_text"
            }
          ],
          streamConfigs: [],
          capabilities: [],
          config: {}
        },
        {
          slug: "qwen",
          displayName: "Qwen",
          description: "Alibaba's Qwen AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://tongyi.aliyun.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://tongyi\\.aliyun\\.com",
          url: "https://tongyi.aliyun.com/qianwen",
          loginUrl: "https://tongyi.aliyun.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ["button[aria-label='Send']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://tongyi.aliyun.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://tongyi.aliyun.com/qianwen",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "textarea",
              sendButtonSelector: "button[aria-label='Send']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Qwen Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Qwen",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "email",
            base_url: "https://tongyi.aliyun.com",
            composer_is_content_editable: "false",
            composer_selector: 'textarea, [role="textbox"], [contenteditable="true"]',
            display_name: "Qwen",
            landing_url: "https://tongyi.aliyun.com/",
            provider_id: "qwen",
            ready_selector: 'textarea, [role="textbox"], [contenteditable="true"]',
            sse_format: "openai",
            stream_terminal: "[DONE]",
            stream_transport: "sse",
            stream_url_pattern: "tongyi.aliyun.com"
          }
        },
        {
          slug: "slack",
          displayName: "Slack",
          description: "Slack messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "oauth",
          websiteUrl: "https://api.slack.com",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://api\\.slack\\.com",
          url: "https://api.slack.com",
          loginUrl: "https://api.slack.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "API",
              url: "https://slack.com/api",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "webhook" }
        },
        {
          slug: "studio-ai",
          displayName: "Studio AI",
          description: "Google AI Studio \u2014 Gemini API playground and prototyping environment",
          category: "ai",
          providerType: "llm",
          authType: "browser",
          websiteUrl: "https://aistudio.google.com",
          profileStrategy: "per_account",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://aistudio\\.google\\.com",
          url: "https://aistudio.google.com/prompts/new_chat",
          loginUrl: "https://aistudio.google.com/login",
          composerSelectors: ["rich-textarea"],
          sendButtonSelectors: ["button[aria-label='Send message']", 'button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Landing",
              url: "https://aistudio.google.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            },
            {
              label: "Chat",
              url: "https://aistudio.google.com/prompts/new_chat",
              endpointType: "chat",
              isDefault: false,
              composerSelector: "rich-textarea",
              sendButtonSelector: "button[aria-label='Send message']",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "select_model",
              recoveryStrategies: [],
              uiComponentOverride: "dropdown_selector",
              uiLabelOverride: "Select Model",
              uiIconOverride: "cpu",
              uiPositionOverride: "header",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            },
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }, { type: "navigate_home" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Studio AI",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "google",
            base_url: "https://aistudio.google.com",
            composer_is_content_editable: "false",
            composer_selector: 'textarea, [role="textbox"]',
            display_name: "Studio AI",
            landing_url: "https://aistudio.google.com/",
            provider_id: "studio-ai",
            ready_selector: 'textarea, [role="textbox"]',
            sse_format: "google-ai-studio",
            stream_terminal: "[DONE]",
            stream_transport: "sse",
            stream_url_pattern: "aistudio.google.com"
          }
        },
        {
          slug: "system",
          displayName: "System",
          description: "System-level fallback parsers \u2014 universal fallback chain",
          category: "system",
          providerType: "system",
          authType: "none",
          websiteUrl: "https://vivim.app",
          profileStrategy: "shared",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://vivim\\.app",
          url: "https://vivim.app",
          loginUrl: "https://vivim.app/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [],
          parsers: [
            {
              name: "system/001_raw_text",
              version: 1,
              logicCode: "\nfunction parse(rawBody) {\n  const b = String(rawBody);\n  return b.length > 0 ? [{ type: 'text', text: b }] : [];\n}\nfunction detectCompletion() { return true; }\nfunction getConfidence() { return 0.05; }\nmodule.exports.default = { name: 'system/001_raw_text', version: 1, providerId: 'system', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };\n",
              hash: "4403068b29982edbedecd623d76794a334ceacd8d5b7dac27b15929da0f4cab3",
              isActive: true,
              fallbackParserName: ""
            }
          ],
          streamConfigs: [],
          capabilities: [],
          config: {}
        },
        {
          slug: "telegram",
          displayName: "Telegram",
          description: "Telegram messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "api_key",
          websiteUrl: "https://core.telegram.org",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://core\\.telegram\\.org",
          url: "https://core.telegram.org",
          loginUrl: "https://core.telegram.org/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Bot API",
              url: "https://api.telegram.org",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "polling" }
        },
        {
          slug: "whatsapp",
          displayName: "WhatsApp",
          description: "WhatsApp Web messaging channel",
          category: "messaging",
          providerType: "messaging",
          authType: "browser",
          websiteUrl: "https://web.whatsapp.com",
          profileStrategy: "single",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://web\\.whatsapp\\.com",
          url: "https://web.whatsapp.com",
          loginUrl: "https://web.whatsapp.com/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "Web",
              url: "https://web.whatsapp.com",
              endpointType: "landing",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [],
          config: { poll_strategy: "cdp-scrape" }
        },
        {
          slug: "z-ai",
          displayName: "Z AI",
          description: "Z.AI \u2014 Z.ai's multimodal AI assistant",
          category: "ai",
          providerType: "llm",
          authType: "api",
          websiteUrl: "https://z.ai",
          profileStrategy: "shared",
          hasMultiAccount: false,
          isActive: true,
          urlPattern: "^https://z\\.ai",
          url: "https://z.ai",
          loginUrl: "https://z.ai/login",
          composerSelectors: ["textarea"],
          sendButtonSelectors: ['button[type="submit"]'],
          loginIndicator: { urlPattern: "login|auth|signin|sign-in" },
          endpoints: [
            {
              label: "API",
              url: "https://api.z.ai/v1",
              endpointType: "api",
              isDefault: true,
              composerSelector: "",
              sendButtonSelector: "",
              composerType: "textarea",
              sendMethod: "both",
              contentEditable: false
            }
          ],
          parsers: [],
          streamConfigs: [],
          capabilities: [
            {
              globalCapabilityId: "send_message",
              recoveryStrategies: [{ type: "retry_selector" }],
              uiComponentOverride: "text_input",
              uiLabelOverride: "Send to Z AI",
              uiIconOverride: "arrow-up-circle",
              uiPositionOverride: "composer",
              uiOrderOverride: void 0,
              uiGroupOverride: "",
              uiPriorityOverride: "primary",
              interactionModeOverride: "",
              existentialRuleOverride: "",
              minPlanTierOverride: "",
              confidence: 1
            }
          ],
          config: {
            auth_type: "api_key",
            base_url: "https://api.z.ai/v1",
            composer_is_content_editable: "false",
            composer_selector: 'textarea, [role="textbox"]',
            display_name: "Z.AI",
            landing_url: "https://chat.z.ai/",
            provider_id: "z-ai",
            ready_selector: 'textarea, [role="textbox"]',
            sse_format: "openai",
            stream_terminal: "[DONE]",
            stream_transport: "sse",
            stream_url_pattern: "z.ai"
          }
        }
      ]
    };
    provider_protocol_default = _protocol2;
    protocol2 = _protocol2;
  }
});

// src/engines/provider-protocol-loader.ts
var provider_protocol_loader_exports = {};
__export(provider_protocol_loader_exports, {
  loadProviderProtocol: () => loadProviderProtocol,
  normalizeProtocolSource: () => normalizeProtocolSource
});
function normalizeProtocolSource(raw) {
  const value = (raw ?? "generated").toLowerCase().trim();
  return value === "dev" ? "dev" : "generated";
}
async function loadProviderProtocol(source = "generated") {
  if (source === "dev") {
    const mod2 = await Promise.resolve().then(() => (init_provider_protocol_dev(), provider_protocol_dev_exports));
    return { source, protocol: mod2.default };
  }
  const mod = await Promise.resolve().then(() => (init_provider_protocol(), provider_protocol_exports));
  return { source, protocol: mod.default };
}
var init_provider_protocol_loader = __esm({
  "src/engines/provider-protocol-loader.ts"() {
    "use strict";
  }
});

// src/config/provider-registry.ts
var provider_registry_exports = {};
__export(provider_registry_exports, {
  ProviderRegistry: () => ProviderRegistry,
  createProviderRegistry: () => createProviderRegistry,
  getProviderRegistry: () => getProviderRegistry
});
function escapeRegex(s) {
  return s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function createProviderRegistry(_db2) {
  if (!instance) {
    instance = new ProviderRegistry();
  }
  return instance;
}
function getProviderRegistry() {
  if (!instance)
    throw new Error("ProviderRegistry not created \u2014 call createProviderRegistry() at boot");
  return instance;
}
var instance, ProviderRegistry;
var init_provider_registry = __esm({
  "src/config/provider-registry.ts"() {
    "use strict";
    init_provider_protocol_loader();
    instance = null;
    ProviderRegistry = class {
      config = null;
      protocolSource = "generated";
      /**
       * Builds the in-memory config from the generated protocol file. This is now a
       * pure transform (no DB reads); the `.dev.ts` override is selected by
       * PROVIDER_PROTOCOL_SOURCE. Kept async for boot-call compatibility.
       */
      async initialize() {
        const source = normalizeProtocolSource(process.env.PROVIDER_PROTOCOL_SOURCE);
        const { protocol: protocol3 } = await loadProviderProtocol(source);
        this.protocolSource = source;
        const definitions = /* @__PURE__ */ new Map();
        const endpoints = /* @__PURE__ */ new Map();
        const selectors = /* @__PURE__ */ new Map();
        const urlPatterns = /* @__PURE__ */ new Map();
        const loginIndicators = /* @__PURE__ */ new Map();
        const parsers = /* @__PURE__ */ new Map();
        const capturePatterns = /* @__PURE__ */ new Map();
        const configs = /* @__PURE__ */ new Map();
        for (const p of protocol3.providers) {
          if (!p.isActive) continue;
          const slug = p.slug;
          definitions.set(slug, {
            id: slug,
            slug,
            displayName: p.displayName,
            websiteUrl: p.websiteUrl ?? null,
            authType: p.authType
          });
          const baseUrl = p.websiteUrl || `https://${slug}.com`;
          urlPatterns.set(slug, new RegExp(`^${escapeRegex(baseUrl)}`, "i"));
          const epList = (p.endpoints ?? []).map((e) => ({
            url: e.url,
            label: e.label,
            endpointType: e.endpointType,
            composerSelector: e.composerSelector,
            sendButtonSelector: e.sendButtonSelector,
            composerType: e.composerType,
            sendMethod: e.sendMethod,
            contentEditable: !!e.contentEditable
          }));
          endpoints.set(slug, epList);
          const chatEp = epList.find((e) => e.endpointType === "chat");
          if (chatEp) {
            const composers = [...p.composerSelectors ?? []];
            if (chatEp.composerType === "quill")
              composers.push(
                'div.ql-editor[contenteditable="true"]',
                ".ql-editor",
                "rich-textarea [contenteditable]",
                "textarea"
              );
            else if (chatEp.contentEditable)
              composers.push('div[contenteditable="true"]', '[role="textbox"]', "textarea");
            else if (composers.length === 0) composers.push("textarea");
            const sendButtons = [...p.sendButtonSelectors ?? [], 'button[type="submit"]'];
            selectors.set(slug, { composer: composers, sendButton: sendButtons });
            if (chatEp.url) {
              capturePatterns.set(
                slug,
                new RegExp(`${escapeRegex(chatEp.url).replace(/\/$/, "")}($|\\/|\\?)`)
              );
            }
          }
          parsers.set(
            slug,
            (p.parsers ?? []).map((pr) => ({ name: pr.name, version: pr.version }))
          );
          configs.set(slug, new Map(Object.entries(p.config ?? {})));
          const loginPattern = p.loginIndicator?.urlPattern ?? "login|auth|signin|sign-in";
          loginIndicators.set(slug, { urlPattern: new RegExp(loginPattern, "i") });
        }
        this.config = {
          definitions,
          endpoints,
          selectors,
          urlPatterns,
          loginIndicators,
          parsers,
          capturePatterns,
          configs
        };
      }
      getProtocolSource() {
        return this.protocolSource;
      }
      getConfig() {
        if (!this.config)
          throw new Error("ProviderRegistry not initialized \u2014 call initialize() at boot");
        return this.config;
      }
      getComposerSelectors(providerId) {
        return this.getConfig().selectors.get(providerId)?.composer ?? [
          "textarea",
          '[contenteditable="true"]',
          '[role="textbox"]'
        ];
      }
      getSendButtonSelectors(providerId) {
        return this.getConfig().selectors.get(providerId)?.sendButton ?? ['button[type="submit"]'];
      }
      getProviderUrl(providerId) {
        const def = this.getConfig().definitions.get(providerId);
        const ep = this.getConfig().endpoints.get(providerId);
        const chatEp = ep?.find((e) => e.endpointType === "chat");
        return chatEp?.url ?? def?.websiteUrl ?? `https://${providerId}.com`;
      }
      getLoginUrl(providerId) {
        const ep = this.getConfig().endpoints.get(providerId);
        const loginEp = ep?.find((e) => e.endpointType === "login");
        if (loginEp?.url) return loginEp.url;
        const def = this.getConfig().definitions.get(providerId);
        return def?.websiteUrl ? `${def.websiteUrl}/login` : `https://${providerId}.com/login`;
      }
      getCapturePattern(providerId) {
        return this.getConfig().capturePatterns.get(providerId);
      }
      getLoginIndicator(providerId) {
        return this.getConfig().loginIndicators.get(providerId);
      }
      getProviderList() {
        return Array.from(this.getConfig().definitions.keys());
      }
      getProviderUrlPattern(providerId) {
        return this.getConfig().urlPatterns.get(providerId);
      }
      getDefinitions() {
        return this.getConfig().definitions;
      }
      getEndpoints(providerId) {
        return this.getConfig().endpoints.get(providerId) ?? [];
      }
      getParserList(providerId) {
        return this.getConfig().parsers.get(providerId) ?? [];
      }
    };
  }
});

// src/server/setup-router.ts
function getLoginUrl(providerId) {
  try {
    return getProviderRegistry().getLoginUrl(providerId);
  } catch {
    return `https://${providerId}.com/login`;
  }
}
function createSetupRouter(ctx) {
  return async (req) => {
    const url = new URL(req.url);
    const { pathname } = url;
    const method = req.method;
    const _source = req.headers.get("X-Source") ?? "unknown";
    const audit = (_action, _detail) => {
    };
    try {
      if (pathname === "/api/setup/workspace" && method === "GET") {
        const hint = await ctx.db.getWorkspaceHint?.() ?? null;
        return json({ workspacePath: hint });
      }
      if (pathname === "/api/setup/workspace" && method === "POST") {
        const body = await req.json();
        if (!body.path) return errorResponse("path required", "ValidationError", 400);
        await ctx.db.setWorkspaceHint?.(body.path);
        audit("workspace_set", { path: body.path });
        return json({ ok: true, workspacePath: body.path });
      }
      if (pathname === "/api/setup/launch-visible" && method === "POST") {
        const body = await req.json();
        if (!body.providerId || !body.accountSlug || !body.workspace) {
          return errorResponse(
            "providerId, accountSlug, workspace required",
            "ValidationError",
            400
          );
        }
        const allocator = new ProfileAllocator(body.workspace);
        const profileDir = await allocator.allocate(body.providerId, body.accountSlug);
        const loginUrl = PROVIDER_LOGIN_URLS[body.providerId] ?? `https://${body.providerId}.com`;
        const port = body.port ?? 9222;
        const result = await launchChrome({
          visible: true,
          debugPort: port,
          profileDir,
          extraArgs: [loginUrl]
        });
        audit("chrome_launched", {
          providerId: body.providerId,
          port: result.debugPort,
          pid: result.pid
        });
        return json({
          ok: true,
          profileDir,
          debugPort: result.debugPort,
          pid: result.pid,
          loginUrl
        });
      }
      if (pathname === "/api/setup/verify" && method === "POST") {
        const body = await req.json();
        if (!body.port) return errorResponse("port required", "ValidationError", 400);
        let wsUrl = `ws://127.0.0.1:${body.port}/devtools/browser`;
        try {
          const versionResp = await fetch(`http://127.0.0.1:${body.port}/json/version`, {
            signal: AbortSignal.timeout(3e3)
          });
          if (versionResp.ok) {
            const version = await versionResp.json();
            if (version.webSocketDebuggerUrl) {
              wsUrl = version.webSocketDebuggerUrl;
            }
          }
        } catch {
        }
        const client3 = new BunCdpClient(wsUrl);
        try {
          await client3.connect();
          const version = await client3.send("Browser.getVersion");
          const targets = await client3.send("Target.getTargets");
          const pages = (targets?.targetInfos ?? []).filter((t) => t.type === "page");
          const url2 = pages[0]?.url ?? "";
          let loggedIn = false;
          let method2 = "url_pattern";
          let loggedInUrl = url2;
          const providerId = body.providerId;
          for (const page of pages) {
            if (loggedIn) break;
            if (!providerId) continue;
            try {
              const { sessionId } = await client3.send("Target.attachToTarget", {
                targetId: page.targetId,
                flatten: true
              });
              await new Promise((r) => setTimeout(r, 300));
              const cookieResult = await client3.send("Network.getCookies", {}, { sessionId });
              const cookieNames = new Set((cookieResult?.cookies ?? []).map((c) => c.name));
              if (providerId === "chatgpt") {
                const hasSession = cookieNames.has("__Secure-next-auth.session-token") || cookieNames.has("oai-did") || cookieNames.has("__cf_bm");
                if (hasSession) {
                  loggedIn = true;
                  method2 = "cookie_check";
                  loggedInUrl = page.url;
                }
              } else if (providerId === "gemini") {
                const hasGoogleAuth = cookieNames.has("SID") || cookieNames.has("HSID") || cookieNames.has("SSID") || cookieNames.has("__Secure-1PSID");
                if (hasGoogleAuth) {
                  loggedIn = true;
                  method2 = "cookie_check";
                  loggedInUrl = page.url;
                }
              } else if (providerId === "claude") {
                const hasSession = cookieNames.has("sessionKey") || cookieNames.has("__cf_bm") || cookieNames.has("fit_topsid");
                if (hasSession) {
                  loggedIn = true;
                  method2 = "cookie_check";
                  loggedInUrl = page.url;
                }
              }
              if (!loggedIn) {
                const indicator = LOGIN_INDICATORS[providerId];
                if (indicator?.loggedInSelector) {
                  for (let attempt = 0; attempt < 3 && !loggedIn; attempt++) {
                    try {
                      const evalResult = await client3.send(
                        "Runtime.evaluate",
                        {
                          expression: `(() => {
                          const loggedIn = document.querySelector('${indicator.loggedInSelector}')
                          const loggedOut = ${indicator.loggedOutSelector ? `document.querySelector('${indicator.loggedOutSelector}')` : "null"}
                          return JSON.stringify({ loggedIn: !!loggedIn, loggedOut: !!loggedOut, url: location.href })
                        })()`,
                          returnByValue: true
                        },
                        { sessionId }
                      );
                      const state = JSON.parse(evalResult?.result?.value ?? "{}");
                      if (state.loggedIn) {
                        loggedIn = true;
                        method2 = "dom_check";
                        loggedInUrl = state.url ?? page.url;
                        break;
                      }
                      if (state.loggedOut) {
                        break;
                      }
                      if (attempt < 2) await new Promise((r) => setTimeout(r, 1e3));
                    } catch {
                      if (attempt < 2) await new Promise((r) => setTimeout(r, 1e3));
                    }
                  }
                }
              }
              await client3.send("Target.detachFromTarget", { sessionId }).catch(() => {
              });
            } catch {
              const indicator = LOGIN_INDICATORS[providerId];
              const pattern2 = indicator?.urlPattern ?? /login|auth|signin|sign-in/i;
              if (page.url && !pattern2.test(page.url)) {
                loggedIn = true;
                method2 = "url_pattern";
                loggedInUrl = page.url;
              }
            }
          }
          if (!providerId && pages.length) {
            const indicator = LOGIN_INDICATORS[providerId ?? ""];
            const pattern2 = indicator?.urlPattern ?? /login|auth|signin|sign-in/i;
            loggedIn = !!url2 && !pattern2.test(url2);
            method2 = "url_pattern";
          }
          await client3.disconnect();
          const result = {
            alive: !!version,
            loggedIn,
            url: loggedInUrl,
            port: body.port,
            method: method2
          };
          audit("verify_result", { loggedIn, method: method2, providerId: body.providerId });
          return json({ ok: true, ...result });
        } catch (err) {
          await client3.disconnect().catch(() => {
          });
          return errorResponse(`Verify failed: ${String(err)}`, "VerificationError", 500);
        }
      }
      if (pathname === "/api/setup/complete" && method === "POST") {
        const body = await req.json();
        if (!body.providerId || !body.accountSlug) {
          return errorResponse("providerId, accountSlug required", "ValidationError", 400);
        }
        let provider = await ctx.db.getProvider(body.providerId);
        if (!provider) {
          provider = await ctx.db.upsertProvider({
            id: body.providerId,
            slug: body.providerId,
            displayName: body.providerId.charAt(0).toUpperCase() + body.providerId.slice(1),
            isActive: 1,
            authType: "browser",
            profileStrategy: "per_account",
            createdAt: Date.now()
          });
        }
        const accountId = `${body.providerId}_${body.accountSlug}`;
        await ctx.db.upsertAccount({
          id: accountId,
          providerId: body.providerId,
          email: body.accountSlug,
          planTier: "free",
          loginState: "authenticated",
          profileDir: body.profileDir,
          debugPort: body.debugPort
        });
        if (body.workspace) {
          await ctx.db.setWorkspaceHint(body.workspace);
        }
        audit("setup_complete", { accountId, providerId: body.providerId });
        return json({ ok: true, accountId });
      }
      if (pathname === "/api/setup/restore" && method === "POST") {
        const body = await req.json();
        const workspace = body.workspace ?? await ctx.db.getWorkspaceHint?.() ?? null;
        if (!workspace) {
          return errorResponse("No workspace path configured", "ValidationError", 400);
        }
        const { existsSync: existsSync6 } = await import("fs");
        const { readdir: readdir4 } = await import("fs/promises");
        const { join: join9 } = await import("path");
        if (!existsSync6(workspace)) {
          return errorResponse(`Workspace not found: ${workspace}`, "ValidationError", 400);
        }
        const PROVIDERS = getProviderRegistry().getProviderList();
        const restored = [];
        for (const providerId of PROVIDERS) {
          const providerDir = join9(workspace, providerId);
          if (!existsSync6(providerDir)) continue;
          let entries;
          try {
            entries = await readdir4(providerDir, { withFileTypes: true });
          } catch {
            continue;
          }
          for (const entry of entries) {
            if (!entry.isDirectory()) continue;
            const accountSlug = entry.name;
            const profileDir = join9(providerDir, accountSlug);
            const hasCookies = existsSync6(join9(profileDir, "Default", "Cookies")) || existsSync6(join9(profileDir, "Default", "Network", "Cookies"));
            if (!hasCookies) continue;
            const accountId = `${providerId}_${accountSlug}`;
            const existing = await ctx.db.prisma.providerAccount.findUnique({
              where: { id: accountId }
            });
            if (existing) continue;
            await ctx.db.upsertAccount({
              id: accountId,
              providerId,
              email: accountSlug,
              planTier: "free",
              loginState: "authenticated",
              profileDir,
              debugPort: null
            });
            restored.push({ providerId, accountId, profileDir });
            audit("profile_restored", { providerId, accountId, profileDir });
          }
        }
        return json({ ok: true, restored, count: restored.length });
      }
      if (pathname === "/api/setup/profiles" && method === "GET") {
        const hint = await ctx.db.getWorkspaceHint?.() ?? null;
        if (!hint) {
          return json({ profiles: [], workspacePath: null });
        }
        const { existsSync: existsSync6 } = await import("fs");
        const { readdir: readdir4 } = await import("fs/promises");
        const { join: join9 } = await import("path");
        if (!existsSync6(hint)) {
          return json({ profiles: [], workspacePath: hint });
        }
        const PROVIDERS = getProviderRegistry().getProviderList();
        const profiles = [];
        for (const providerId of PROVIDERS) {
          const providerDir = join9(hint, providerId);
          if (!existsSync6(providerDir)) continue;
          let entries;
          try {
            entries = await readdir4(providerDir, { withFileTypes: true });
          } catch {
            continue;
          }
          for (const entry of entries) {
            if (!entry.isDirectory()) continue;
            const accountSlug = entry.name;
            const profileDir = join9(providerDir, accountSlug);
            const hasCookies = existsSync6(join9(profileDir, "Default", "Cookies")) || existsSync6(join9(profileDir, "Default", "Network", "Cookies"));
            const accountId = `${providerId}_${accountSlug}`;
            const dbAccount = await ctx.db.prisma.providerAccount.findUnique({
              where: { id: accountId }
            });
            profiles.push({
              providerId,
              accountSlug,
              profileDir,
              hasCookies,
              dbLinked: !!dbAccount
            });
          }
        }
        audit("profiles_list", { count: profiles.length });
        return json({ profiles, workspacePath: hint });
      }
      return errorResponse("Not found", "NotFoundError", 404);
    } catch (err) {
      const message = err instanceof Error ? err.message : "Internal error";
      return errorResponse(message, "InternalError", 500);
    }
  };
}
var PROVIDER_LOGIN_URLS, LOGIN_INDICATORS;
var init_setup_router = __esm({
  "src/server/setup-router.ts"() {
    "use strict";
    init_cdp();
    init_launcher();
    init_profile_allocator();
    init_response();
    init_provider_registry();
    PROVIDER_LOGIN_URLS = new Proxy({}, {
      get: (_, providerId) => getLoginUrl(providerId)
    });
    LOGIN_INDICATORS = new Proxy(
      {},
      {
        get: (_, _providerId) => ({ urlPattern: /login|auth|signin|sign-in/i })
      }
    );
  }
});

// src/server/websocket.ts
function setCanvasWsHandler(handler) {
  canvasWsHandler = handler;
}
function registerConversationForwarder(eventBus) {
  const forward = (event) => {
    const e = event;
    if (!e?.conversationId) return;
    const topic = `conversation:${e.conversationId}`;
    for (const session of wsSessions.values()) {
      if (session.subscriptions.has(topic)) {
        try {
          session.ws.send(JSON.stringify(event));
        } catch {
        }
      }
    }
  };
  eventBus.on("conversation:complete", forward);
  eventBus.on("conversation:block", forward);
  eventBus.on("conversation:error", forward);
}
var canvasWsHandler, sessions, wsToSession, wsSessions, handleWebSocket;
var init_websocket = __esm({
  "src/server/websocket.ts"() {
    "use strict";
    canvasWsHandler = null;
    sessions = /* @__PURE__ */ new Map();
    wsToSession = /* @__PURE__ */ new WeakMap();
    wsSessions = sessions;
    handleWebSocket = {
      open(ws) {
        const session = { ws, sessionId: null, role: null, subscriptions: /* @__PURE__ */ new Set() };
        wsToSession.set(ws, session);
      },
      message(ws, raw, eventBus, _registry) {
        try {
          const msg = JSON.parse(typeof raw === "string" ? raw : raw.toString());
          if (msg.type === "hello" && msg.sessionId && msg.role) {
            const session = wsToSession.get(ws);
            if (session) {
              session.sessionId = msg.sessionId;
              session.role = msg.role;
              sessions.set(msg.sessionId, session);
              return;
            }
          }
          if (msg.type === "agent:command" && msg.targetSessionId) {
            const target = sessions.get(msg.targetSessionId);
            if (target?.ws) {
              const rawStr = typeof raw === "string" ? raw : raw.toString();
              target.ws.send(rawStr);
              return;
            }
            ws.send(
              JSON.stringify({
                type: "agent:result",
                correlationId: msg.correlationId,
                ok: false,
                error: "Target session not found"
              })
            );
            return;
          }
          if ((msg.type === "agent:result" || msg.type === "agent:discover") && msg.correlationId) {
            for (const session of sessions.values()) {
              if (session.role === "agent") {
                const rawStr = typeof raw === "string" ? raw : raw.toString();
                session.ws.send(rawStr);
                return;
              }
            }
          }
          if (msg.type === "agent:subscribe" && msg.topic) {
            const session = wsToSession.get(ws);
            if (session) {
              session.subscriptions.add(msg.topic);
              eventBus.subscribe(ws, msg.topic, msg.filter ?? "*");
              ws.send(JSON.stringify({ type: "agent:subscribed", topic: msg.topic, ok: true }));
              return;
            }
          }
          if (msg.type === "agent:query_state" && msg.conversationId) {
            ws.send(
              JSON.stringify({
                type: "agent:state",
                correlationId: msg.correlationId,
                conversationId: msg.conversationId,
                state: { active: true, timestamp: Date.now() }
              })
            );
            return;
          }
          if (msg.type === "agent:execute_workflow" && msg.workflowId) {
            ws.send(
              JSON.stringify({
                type: "agent:workflow_started",
                correlationId: msg.correlationId,
                workflowId: msg.workflowId,
                ok: true
              })
            );
            return;
          }
          if (msg.type === "subscribe" && msg.entityType && msg.entityId) {
            eventBus.subscribe(ws, msg.entityType, msg.entityId);
          } else if (msg.type === "unsubscribe" && msg.entityType && msg.entityId) {
            eventBus.unsubscribe(ws, msg.entityType, msg.entityId);
          }
          if (msg.type === "dev:subscribe") {
            eventBus.subscribe(ws, "dev", "*");
            ws.send(JSON.stringify({ type: "dev:subscribed", ok: true, at: Date.now() }));
            return;
          }
          if (msg.type === "dev:unsubscribe") {
            eventBus.unsubscribe(ws, "dev", "*");
            return;
          }
          const msgType = msg.type;
          if (canvasWsHandler && msgType && (msgType.startsWith("canvas:") || msgType.startsWith("bridge:"))) {
            const rawStr = typeof raw === "string" ? raw : raw.toString();
            canvasWsHandler(ws, rawStr);
            return;
          }
        } catch (_err) {
        }
      },
      close(ws, eventBus) {
        const session = wsToSession.get(ws);
        if (session?.sessionId) {
          sessions.delete(session.sessionId);
        }
        eventBus.unsubscribeAll(ws);
      }
    };
  }
});

// src/engines/content-unit-decomposer.ts
function blockToUnit(block, index, conversationId, messageId, idGenerator) {
  const now = Date.now();
  const base = {
    id: idGenerator(),
    messageId,
    conversationId,
    sequenceIndex: index,
    metadataJson: "{}",
    qualityScore: null,
    createdAt: now
  };
  switch (block.type) {
    case "text":
      return {
        ...base,
        unitType: "text",
        content: typeof block.text === "string" ? block.text : JSON.stringify(block.text),
        mimeType: "text/plain"
      };
    case "reasoning":
      return {
        ...base,
        unitType: "reasoning",
        content: typeof block.text === "string" ? block.text : JSON.stringify(block.text),
        mimeType: "text/plain",
        metadataJson: JSON.stringify({ signature: block.signature, state: block.state })
      };
    case "code":
      return {
        ...base,
        unitType: "code",
        content: block.text,
        mimeType: block.language ? `text/x-${block.language}` : "text/plain",
        metadataJson: JSON.stringify({ language: block.language })
      };
    case "file":
      return {
        ...base,
        unitType: "file",
        content: block.url,
        mimeType: block.mediaType,
        metadataJson: JSON.stringify({ filename: block.filename, hasData: !!block.data })
      };
    case "tool-call":
      return {
        ...base,
        unitType: "tool-call",
        content: JSON.stringify({ toolName: block.toolName, input: block.input }),
        mimeType: "application/json",
        metadataJson: JSON.stringify({
          toolCallId: block.toolCallId,
          state: block.state,
          approvalId: block.approvalId
        })
      };
    case "tool-result":
      return {
        ...base,
        unitType: "tool-result",
        content: JSON.stringify(block.output ?? ""),
        mimeType: "application/json",
        metadataJson: JSON.stringify({
          toolCallId: block.toolCallId,
          isError: block.isError
        })
      };
    case "source":
      return {
        ...base,
        unitType: "source",
        content: block.title ?? block.url ?? "",
        mimeType: block.mediaType ?? "text/html",
        metadataJson: JSON.stringify({ sourceId: block.sourceId, url: block.url })
      };
    case "error":
      return {
        ...base,
        unitType: "error",
        content: block.message,
        mimeType: "text/plain",
        metadataJson: JSON.stringify({ code: block.code })
      };
    case "meta":
      return {
        ...base,
        unitType: "meta",
        content: JSON.stringify({ key: block.key, value: block.value }),
        mimeType: "application/json"
      };
    case "custom":
      return {
        ...base,
        unitType: `custom:${block.kind}`,
        content: JSON.stringify(block.data),
        mimeType: "application/json",
        metadataJson: JSON.stringify({ kind: block.kind, state: block.state })
      };
    case "step-start":
      return {
        ...base,
        unitType: "step-start",
        content: "",
        mimeType: null
      };
    default:
      return {
        ...base,
        unitType: "unknown",
        content: JSON.stringify(block),
        mimeType: "application/json"
      };
  }
}
function decomposeToContentUnits(blocks, conversationId, messageId, idGenerator = () => `cu_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`) {
  return blocks.map((block, i) => blockToUnit(block, i, conversationId, messageId, idGenerator));
}
var init_content_unit_decomposer = __esm({
  "src/engines/content-unit-decomposer.ts"() {
    "use strict";
  }
});

// src/engines/provider-selectors.ts
function getComposerSelectors(providerId) {
  try {
    return getProviderRegistry().getComposerSelectors(providerId);
  } catch {
    return ["textarea", '[contenteditable="true"]', '[role="textbox"]'];
  }
}
function getSendButtonSelectors(providerId) {
  try {
    return getProviderRegistry().getSendButtonSelectors(providerId);
  } catch {
    return ['button[type="submit"]'];
  }
}
function getProviderUrl(providerId) {
  try {
    return getProviderRegistry().getProviderUrl(providerId);
  } catch {
    return `https://${providerId}.com`;
  }
}
function getProviderUrlPattern(providerId) {
  try {
    return getProviderRegistry().getProviderUrlPattern(providerId);
  } catch {
    return void 0;
  }
}
async function findWorkingSelector(cdpSend, selectors) {
  for (const selector of selectors) {
    try {
      const result = await cdpSend("Runtime.evaluate", {
        expression: `!!document.querySelector(${JSON.stringify(selector)})`,
        returnByValue: true
      });
      if (result?.result?.value === true) {
        return selector;
      }
    } catch {
    }
  }
  return null;
}
var COMPOSER_SELECTORS, SEND_BUTTON_SELECTORS, PROVIDER_URLS2, PROVIDER_URL_PATTERNS;
var init_provider_selectors = __esm({
  "src/engines/provider-selectors.ts"() {
    "use strict";
    init_provider_registry();
    COMPOSER_SELECTORS = new Proxy(
      {},
      {
        get: (_, providerId) => getComposerSelectors(providerId)
      }
    );
    SEND_BUTTON_SELECTORS = new Proxy(
      {},
      {
        get: (_, providerId) => getSendButtonSelectors(providerId)
      }
    );
    PROVIDER_URLS2 = new Proxy({}, {
      get: (_, providerId) => getProviderUrl(providerId)
    });
    PROVIDER_URL_PATTERNS = new Proxy(
      {},
      {
        get: (_, providerId) => getProviderUrlPattern(providerId)
      }
    );
  }
});

// src/engines/conversation-manager.ts
var conversation_manager_exports = {};
__export(conversation_manager_exports, {
  CapabilityEventBus: () => CapabilityEventBus,
  ConversationManager: () => ConversationManager
});
function composerTypeForProvider(providerId) {
  switch (providerId) {
    case "claude":
    case "gemini":
      return "contenteditable";
    default:
      return "textarea";
  }
}
function getCapturePattern(providerId) {
  try {
    const raw = getProviderRegistry().getCapturePattern(providerId);
    return raw ? new RegExp(raw) : void 0;
  } catch {
    return void 0;
  }
}
function extractText(blocks) {
  const pieces = [];
  for (const b of blocks) {
    if (b.type === "text" && typeof b.text === "string") pieces.push(b.text);
    if (b.type === "reasoning" && typeof b.text === "string") pieces.push(b.text);
  }
  return pieces.join("");
}
function buildConversationContext(conv, account, resolved, slave) {
  return {
    provider: {
      id: conv.providerId,
      slug: conv.providerId,
      displayName: conv.providerId
    },
    account: {
      email: account.id,
      planTier: account.planTier,
      loginState: "unknown"
    },
    chrome: {
      status: slave.status,
      circuitState: slave.circuitState
    },
    capabilities: {
      total: resolved.total,
      available: resolved.composer.length
    }
  };
}
var CAPTURE_PATTERNS, ConversationManager;
var init_conversation_manager = __esm({
  "src/engines/conversation-manager.ts"() {
    "use strict";
    init_errors();
    init_ids();
    init_content_unit_decomposer();
    init_provider_selectors();
    init_capability_event_bus();
    init_provider_registry();
    CAPTURE_PATTERNS = new Proxy({}, {
      get: (_, providerId) => getCapturePattern(providerId)
    });
    ConversationManager = class {
      constructor(governor, resolution, parser, blocks, store, eventBus, memoizer, memory, contextAssembly, streamingProtocol, nodeStore, contentUnitStore) {
        this.governor = governor;
        this.resolution = resolution;
        this.parser = parser;
        this.blocks = blocks;
        this.store = store;
        this.eventBus = eventBus;
        this.memoizer = memoizer;
        this.memory = memory;
        this.contextAssembly = contextAssembly;
        this.streamingProtocol = streamingProtocol;
        this.nodeStore = nodeStore;
        this.contentUnitStore = contentUnitStore;
      }
      governor;
      resolution;
      parser;
      blocks;
      store;
      eventBus;
      memoizer;
      memory;
      contextAssembly;
      streamingProtocol;
      nodeStore;
      contentUnitStore;
      // ── universal capture ── every message becomes a Node so the database is
      // fully compliant: nothing flowing through the system is dropped.
      // Captures ACU-proven fields (contentHash, version, state, acl, authorDid)
      // and links assistant→user to preserve the response fork.
      async captureAsNode(conversationId, messageId, role, rawSource, blocks, parentId, parseResult) {
        if (!this.nodeStore) return null;
        const now = Date.now();
        const text = extractText(blocks);
        const edgeType = role === "assistant" ? "responds_to" : "follows";
        const blocksSummary = blocks.map((b) => {
          const base = { type: b.type };
          if (b.type === "text" || b.type === "reasoning")
            base.text = typeof b.text === "string" ? b.text : "[rich-text]";
          if (b.type === "code") {
            base.text = b.text;
            base.language = b.language;
          }
          if (b.type === "file") {
            base.url = b.url;
            base.mediaType = b.mediaType;
          }
          if (b.type === "tool-call") {
            base.toolName = b.toolName;
            base.input = b.input;
          }
          if (b.type === "tool-result") {
            base.output = b.output;
            base.isError = b.isError;
          }
          if (b.type === "source") {
            base.url = b.url;
            base.title = b.title;
          }
          if (b.type === "error") {
            base.message = b.message;
          }
          if (b.type === "meta") {
            base.key = b.key;
            base.value = b.value;
          }
          return base;
        });
        const nodeId = newId();
        await this.nodeStore.putNode({
          id: nodeId,
          type: "cap-store.message",
          schemaVersion: 1,
          version: 1,
          state: "active",
          parentId,
          source: rawSource,
          data: {
            role,
            messageId,
            text,
            blockCount: blocks.length,
            // Full block structure for graph-layer queries
            blocks: blocksSummary,
            // Parser diagnostics when available
            ...parseResult ? {
              parserName: parseResult.parserName,
              parserVersion: parseResult.parserVersion,
              confidence: parseResult.confidence,
              wireFormat: parseResult.wireFormat,
              blockDiagnostics: parseResult.blockDiagnostics
            } : {}
          },
          edges: parentId ? [{ type: edgeType, targetId: parentId, properties: { role } }] : [],
          meta: {
            conversationId,
            messageId,
            sourceParser: parseResult?.parserName ?? "conversation-manager",
            parserConfidence: parseResult?.confidence
          },
          acl: { canView: true, canRemix: false, canReshare: false },
          authorDid: role === "user" ? "user" : "assistant",
          contentType: "message",
          securityLevel: 0,
          createdAt: now,
          updatedAt: now
        }).catch(() => {
        });
        return nodeId;
      }
      // ── send: 8-step pipeline with retry (Units 2.x + 3.1, 3.5) ──────────────
      async send(conversationId, message) {
        const MAX_RETRIES = 2;
        for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
          try {
            return await this.sendInternal(conversationId, message);
          } catch (err) {
            const msg = err instanceof Error ? err.message : String(err);
            const recoverable = msg.includes("Slave not running") || msg.includes("Circuit breaker") || msg.includes("CDP command failed") || msg.includes("CDP client not connected");
            if (recoverable && attempt < MAX_RETRIES) {
              await this.recoverSlave(conversationId);
              await new Promise((r) => setTimeout(r, 1e3 * (attempt + 1)));
              continue;
            }
            return {
              ok: false,
              messageId: "",
              blocks: [],
              text: "",
              latencyMs: 0,
              error: msg
            };
          }
        }
        return { ok: false, messageId: "", blocks: [], text: "", latencyMs: 0, error: "Unreachable" };
      }
      async recoverSlave(conversationId) {
        const conv = await this.store.getConversation(conversationId);
        if (!conv) return;
        const account = await this.store.getAccount(conv.providerSessionId);
        if (!account) return;
        const slaves = this.governor.getAllSlaves({ providerId: conv.providerId });
        for (const slave of slaves) {
          if (slave.accountId === account.id) {
            try {
              await this.governor.kill(slave.slaveId);
            } catch {
            }
          }
        }
        try {
          await this.governor.spawn(conv.providerId, account.id, { visible: false });
        } catch {
        }
      }
      async sendInternal(conversationId, message) {
        const totalStart = Date.now();
        const timing = {};
        try {
          let t0 = Date.now();
          const conv = await this.store.getConversation(conversationId);
          if (!conv) throw new EngineError(`Conversation not found: ${conversationId}`);
          const account = await this.store.getAccount(conv.providerSessionId);
          if (!account) throw new EngineError(`Account not found: ${conv.providerSessionId}`);
          timing.resolve = Date.now() - t0;
          t0 = Date.now();
          let memoryContext;
          let assembledContext;
          if (this.contextAssembly) {
            try {
              assembledContext = await this.contextAssembly.assemble(conversationId, message);
              memoryContext = this.assembledToMemoryContext(assembledContext);
            } catch {
            }
          } else if (this.memory) {
            try {
              memoryContext = await this.memory.getAgentContext(conv.providerId, "");
            } catch {
            }
          }
          timing.recall = Date.now() - t0;
          const planTier = account.planTier;
          const cacheKey = `resolve:${conv.providerId}:${planTier}`;
          const resolved = await this.memoizer.getOrCompute(
            cacheKey,
            () => this.resolution.resolve(conv.providerId, planTier),
            5e3
          );
          t0 = Date.now();
          const slave = await this.governor.ensureRunningForAccount(conv.providerId, account.id);
          const slaveId = slave.slaveId;
          timing.ensure = Date.now() - t0;
          const providerUrl = PROVIDER_URLS2[conv.providerId];
          const pagePattern = PROVIDER_URL_PATTERNS[conv.providerId];
          try {
            const pageState = await this.governor.cdp.getPageState(slaveId);
            if (providerUrl && pageState.url && !pagePattern?.test(pageState.url)) {
              await this.governor.cdp.send(slaveId, "Page.navigate", { url: providerUrl });
              await new Promise((r) => setTimeout(r, 3e3));
            }
          } catch {
          }
          const context = buildConversationContext(conv, account, resolved, slave);
          if (memoryContext) {
            context.memory = memoryContext;
          }
          await this.store.updateConversation(conversationId, {
            contextJson: JSON.stringify(context)
          });
          t0 = Date.now();
          const composerCap = resolved.composer[0];
          const cdpSend = (method, params) => this.governor.cdp.send(slaveId, method, params);
          const selectorCandidates = [
            ...composerCap?.selector ? [composerCap.selector] : [],
            ...COMPOSER_SELECTORS[conv.providerId] ?? ["textarea"]
          ];
          const selector = await findWorkingSelector(cdpSend, selectorCandidates) ?? "textarea";
          const sendSelector = composerCap?.sendSelector;
          const composerType = composerCap?.composerType ?? composerTypeForProvider(conv.providerId);
          const dag = {
            nodes: [
              {
                type: "action",
                action: "type_text",
                params: { text: message, selector, composerType }
              },
              { type: "action", action: "submit", params: { key: "Enter", sendSelector } }
            ],
            edges: [{ from: 0, to: 1 }]
          };
          const capturePattern = CAPTURE_PATTERNS[conv.providerId] ?? /\/api\/conversation\//;
          try {
            await this.governor.cdp.send(slaveId, "Network.enable");
          } catch {
          }
          const sendResult = await this.governor.cdp.executeHarnessPlan(slaveId, dag);
          timing.type = Date.now() - t0;
          if (!sendResult.success) {
            timing.total = Date.now() - totalStart;
            return {
              ok: false,
              messageId: "",
              blocks: [],
              text: "",
              latencyMs: timing.total,
              timing,
              error: sendResult.error ?? "Harness plan failed"
            };
          }
          t0 = Date.now();
          let parseResult = {
            blocks: [],
            confidence: 0,
            parserName: "",
            parserVersion: 0,
            durationMs: 0,
            blockDiagnostics: {
              textBlocks: 0,
              toolCallBlocks: 0,
              fileBlocks: 0,
              errorBlocks: 0,
              reasoningBlocks: 0,
              codeBlocks: 0,
              sourceBlocks: 0
            },
            wireFormat: "unknown",
            fallbackDepth: 0,
            rawSizeBytes: 0
          };
          let captureResult;
          try {
            captureResult = await this.governor.cdp.capture(slaveId, capturePattern, 6e4);
            timing.capture = Date.now() - t0;
            t0 = Date.now();
            parseResult = await this.parser.parse(
              captureResult.body ?? "",
              conv.providerId
            );
            timing.parse = Date.now() - t0;
          } catch {
            timing.capture = Date.now() - t0;
          }
          t0 = Date.now();
          await this.store.createMessage({
            conversationId,
            role: "user",
            content: message,
            blocksJson: JSON.stringify([{ type: "text", text: message }]),
            blockCount: 1,
            latencyMs: 0
          });
          const msgRow = await this.store.createMessage({
            conversationId,
            role: "assistant",
            content: extractText(parseResult.blocks),
            blocksJson: JSON.stringify(parseResult.blocks),
            blockCount: parseResult.blocks.length,
            latencyMs: Date.now() - totalStart
          });
          const blockMeta = {
            parserName: parseResult.parserName,
            parserVersion: parseResult.parserVersion,
            confidence: parseResult.confidence,
            wireFormat: parseResult.wireFormat
          };
          await this.blocks.storeBlocks(conversationId, msgRow.id, parseResult.blocks, blockMeta);
          if (this.contentUnitStore) {
            const units = decomposeToContentUnits(parseResult.blocks, conversationId, msgRow.id);
            await this.contentUnitStore.storeUnits(units).catch(() => {
            });
          }
          const userNodeId = await this.captureAsNode(conversationId, msgRow.id, "user", message, [
            { type: "text", text: message }
          ]);
          await this.captureAsNode(
            conversationId,
            msgRow.id,
            "assistant",
            captureResult.body ?? "",
            parseResult.blocks,
            userNodeId ?? void 0,
            parseResult
          );
          await this.store.updateConversation(conversationId, {
            messageCount: conv.messageCount + 2,
            lastMessageAt: Date.now()
          });
          timing.store = Date.now() - t0;
          this.eventBus.emit({
            type: "conversation:complete",
            conversationId,
            message: msgRow
          });
          timing.total = Date.now() - totalStart;
          if (this.memory) {
            const durationMs = timing.total;
            this.memory.recordEpisode({
              providerId: conv.providerId,
              action: "send",
              input: { message },
              output: {
                text: extractText(parseResult.blocks),
                blockCount: parseResult.blocks.length
              },
              success: true,
              durationMs,
              tags: ["conversation", conv.providerId]
            }).catch(() => {
            });
          }
          return {
            ok: true,
            messageId: msgRow.id,
            blocks: parseResult.blocks,
            text: extractText(parseResult.blocks),
            latencyMs: timing.total,
            timing
          };
        } catch (err) {
          const error2 = err instanceof Error ? err.message : String(err);
          this.eventBus.emit({
            type: "conversation:error",
            conversationId,
            error: error2
          });
          return {
            ok: false,
            messageId: "",
            blocks: [],
            text: "",
            latencyMs: Date.now() - totalStart,
            error: error2
          };
        }
      }
      // ── Streaming send (Unit 3.3) ───────────────────────────────────────────
      async sendStreaming(conversationId, message) {
        const start = Date.now();
        const MAX_RETRIES = 2;
        for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
          try {
            return await this.sendStreamingInternal(conversationId, message, start);
          } catch (err) {
            const msg = err instanceof Error ? err.message : String(err);
            const recoverable = msg.includes("Slave not running") || msg.includes("Circuit breaker") || msg.includes("CDP command failed") || msg.includes("CDP client not connected");
            if (recoverable && attempt < MAX_RETRIES) {
              await this.recoverSlave(conversationId);
              await new Promise((r) => setTimeout(r, 1e3 * (attempt + 1)));
              continue;
            }
            return {
              ok: false,
              messageId: "",
              blocks: [],
              text: "",
              latencyMs: Date.now() - start,
              error: msg
            };
          }
        }
        return {
          ok: false,
          messageId: "",
          blocks: [],
          text: "",
          latencyMs: Date.now() - start,
          error: "Unreachable"
        };
      }
      async sendStreamingInternal(conversationId, message, start) {
        const conv = await this.store.getConversation(conversationId);
        if (!conv) throw new EngineError(`Conversation not found: ${conversationId}`);
        const account = await this.store.getAccount(conv.providerSessionId);
        if (!account) throw new EngineError(`Account not found: ${conv.providerSessionId}`);
        const planTier = account.planTier;
        const cacheKey = `resolve:${conv.providerId}:${planTier}`;
        const resolved = await this.memoizer.getOrCompute(
          cacheKey,
          () => this.resolution.resolve(conv.providerId, planTier),
          5e3
        );
        const slave = await this.governor.ensureRunningForAccount(conv.providerId, account.id);
        const slaveId = slave.slaveId;
        const composerCap = resolved.composer[0];
        const cdpSend = (method, params) => this.governor.cdp.send(slaveId, method, params);
        const selectorCandidates = [
          ...composerCap?.selector ? [composerCap.selector] : [],
          ...COMPOSER_SELECTORS[conv.providerId] ?? ["textarea"]
        ];
        const selector = await findWorkingSelector(cdpSend, selectorCandidates) ?? "textarea";
        const sendSelector = composerCap?.sendSelector;
        const composerType = composerCap?.composerType ?? composerTypeForProvider(conv.providerId);
        const capturePattern = CAPTURE_PATTERNS[conv.providerId] ?? /\/api\/conversation\//;
        try {
          await this.governor.cdp.send(slaveId, "Network.enable");
        } catch {
        }
        const dag = {
          nodes: [
            {
              type: "action",
              action: "type_text",
              params: { text: message, selector, composerType }
            },
            { type: "action", action: "submit", params: { key: "Enter", sendSelector } }
          ],
          edges: [{ from: 0, to: 1 }]
        };
        const sendResult = await this.governor.cdp.executeHarnessPlan(slaveId, dag);
        if (!sendResult.success) {
          return {
            ok: false,
            messageId: "",
            blocks: [],
            text: "",
            latencyMs: Date.now() - start,
            error: sendResult.error ?? "Harness plan failed"
          };
        }
        if (this.streamingProtocol) {
          const messageId = await this.streamingProtocol.startConversation(conversationId);
          await new Promise((resolve4) => {
            const timer = setTimeout(() => {
              cleanup();
              resolve4();
            }, 6e4);
            const matchingRequests = /* @__PURE__ */ new Set();
            let lastBody = "";
            const responseHandler = (params) => {
              const event = params;
              if (event.response?.url && event.requestId && capturePattern.test(event.response.url)) {
                matchingRequests.add(event.requestId);
              }
            };
            const loadingFinishedHandler = async (params) => {
              const event = params;
              if (event.requestId && matchingRequests.has(event.requestId)) {
                try {
                  const result = await this.governor.cdp.send(slaveId, "Network.getResponseBody", {
                    requestId: event.requestId
                  });
                  const body = result?.body ?? "";
                  if (body.length > lastBody.length) {
                    const newChunk = body.slice(lastBody.length);
                    lastBody = body;
                    await this.streamingProtocol?.captureChunk(conversationId, messageId, newChunk);
                  }
                } catch {
                }
                cleanup();
                resolve4();
              }
            };
            const cleanup = () => {
              clearTimeout(timer);
            };
            void responseHandler;
            void loadingFinishedHandler;
            setTimeout(async () => {
              try {
                const result = await this.governor.cdp.capture(slaveId, capturePattern, 6e4);
                const body = result.body ?? "";
                if (body !== lastBody) {
                  await this.streamingProtocol?.captureChunk(
                    conversationId,
                    messageId,
                    body.slice(lastBody.length)
                  );
                }
              } catch {
              }
              cleanup();
              resolve4();
            }, 3e4);
          });
          const finalBlocks = await this.streamingProtocol.finishConversation(conversationId, messageId);
          const msgRow2 = await this.store.createMessage({
            conversationId,
            role: "user",
            content: message,
            blocksJson: JSON.stringify([{ type: "text", text: message }]),
            blockCount: 1,
            latencyMs: 0
          });
          await this.store.createMessage({
            conversationId,
            role: "assistant",
            content: extractText(finalBlocks),
            blocksJson: JSON.stringify(finalBlocks),
            blockCount: finalBlocks.length,
            latencyMs: Date.now() - start
          });
          return {
            ok: true,
            messageId: msgRow2.id,
            blocks: finalBlocks,
            text: extractText(finalBlocks),
            latencyMs: Date.now() - start
          };
        }
        let parseResult = {
          blocks: [],
          confidence: 0,
          parserName: "",
          parserVersion: 0,
          durationMs: 0,
          blockDiagnostics: {
            textBlocks: 0,
            toolCallBlocks: 0,
            fileBlocks: 0,
            errorBlocks: 0,
            reasoningBlocks: 0,
            codeBlocks: 0,
            sourceBlocks: 0
          },
          wireFormat: "unknown",
          fallbackDepth: 0,
          rawSizeBytes: 0
        };
        let rawBody = "";
        try {
          const captureResult = await this.governor.cdp.capture(slaveId, capturePattern, 6e4);
          rawBody = captureResult.body ?? "";
          parseResult = await this.parser.parse(rawBody, conv.providerId);
        } catch {
        }
        await this.store.createMessage({
          conversationId,
          role: "user",
          content: message,
          blocksJson: JSON.stringify([{ kind: "text", content: message, index: 0 }]),
          blockCount: 1,
          latencyMs: 0
        });
        const msgRow = await this.store.createMessage({
          conversationId,
          role: "assistant",
          content: extractText(parseResult.blocks),
          blocksJson: JSON.stringify(parseResult.blocks),
          blockCount: parseResult.blocks.length,
          latencyMs: Date.now() - start
        });
        const batchBlockMeta = {
          parserName: parseResult.parserName,
          parserVersion: parseResult.parserVersion,
          confidence: parseResult.confidence,
          wireFormat: parseResult.wireFormat
        };
        await this.blocks.storeBlocks(conversationId, msgRow.id, parseResult.blocks, batchBlockMeta);
        if (this.contentUnitStore) {
          const units = decomposeToContentUnits(parseResult.blocks, conversationId, msgRow.id);
          await this.contentUnitStore.storeUnits(units).catch(() => {
          });
        }
        const streamUserNodeId = await this.captureAsNode(conversationId, msgRow.id, "user", message, [
          { type: "text", text: message }
        ]);
        await this.captureAsNode(
          conversationId,
          msgRow.id,
          "assistant",
          rawBody,
          parseResult.blocks,
          streamUserNodeId ?? void 0,
          parseResult
        );
        await this.store.updateConversation(conversationId, {
          messageCount: conv.messageCount + 2,
          lastMessageAt: Date.now()
        });
        this.eventBus.emit({
          type: "conversation:complete",
          conversationId,
          message: msgRow
        });
        return {
          ok: true,
          messageId: msgRow.id,
          blocks: parseResult.blocks,
          text: extractText(parseResult.blocks),
          latencyMs: Date.now() - start
        };
      }
      // ── Context assembly → memory context bridge ────────────────────────────
      assembledToMemoryContext(assembled) {
        const episodes = assembled.layers.find((l) => l.name === "recent_episodes");
        const topic = assembled.layers.find((l) => l.name === "topic");
        const project = assembled.layers.find((l) => l.name === "project_state");
        return {
          recentEpisodes: episodes ? [
            {
              id: "",
              providerId: "",
              action: episodes.content,
              input: {},
              output: {},
              success: true,
              durationMs: 0,
              timestamp: assembled.assembledAt,
              tags: []
            }
          ] : [],
          relevantFacts: topic ? [
            {
              id: "",
              subject: "context",
              predicate: "topic",
              object: topic.content,
              confidence: 1,
              source: "context-assembly",
              timestamp: assembled.assembledAt
            }
          ] : [],
          applicableRules: project ? [
            {
              id: "",
              name: "project_state",
              condition: "",
              action: project.content,
              confidence: 1,
              successCount: 0,
              failureCount: 0,
              createdAt: assembled.assembledAt,
              updatedAt: assembled.assembledAt
            }
          ] : []
        };
      }
      // ── CRUD ─────────────────────────────────────────────────────────────────
      async createConversation(providerId, title) {
        return this.store.createConversation({
          providerSessionId: `session_${providerId}_${Date.now()}`,
          providerId,
          title: title ?? null
        });
      }
      async getConversation(id) {
        const conv = await this.store.getConversation(id);
        if (!conv) throw new EngineError(`Conversation not found: ${id}`);
        return conv;
      }
      async getMessages(conversationId, opts) {
        return this.store.getMessages(conversationId, opts);
      }
      async truncate(conversationId, beforeMessageId) {
        const messages = await this.store.getMessages(conversationId);
        const idx = messages.findIndex((m) => m.id === beforeMessageId);
        if (idx <= 0) return;
        const toDelete = messages.slice(0, idx);
        for (const msg of toDelete) {
          await this.store.deleteConversation(msg.id);
        }
      }
    };
  }
});

// src/engines/capability-resolution.ts
var capability_resolution_exports = {};
__export(capability_resolution_exports, {
  CapabilityResolutionEngine: () => CapabilityResolutionEngine
});
function tierRank(tier) {
  return TIER_RANK[tier] ?? 0;
}
function safeJsonParse(raw, fallback) {
  if (raw == null || raw === "") return fallback;
  try {
    return JSON.parse(raw);
  } catch {
    return fallback;
  }
}
function toOverrideSource(v) {
  return v === "provider" || v === "tier" ? v : "global";
}
function parseUiSlots(raw) {
  if (!raw || raw === "") return {};
  try {
    const parsed = JSON.parse(raw);
    if (parsed && typeof parsed === "object" && !Array.isArray(parsed)) {
      const out = {};
      for (const [slot, val] of Object.entries(parsed)) {
        if (val && typeof val === "object")
          out[slot] = val;
      }
      return out;
    }
  } catch {
    return {};
  }
  return {};
}
function normalizePosition(pos) {
  return UI_POSITIONS.includes(pos) ? pos : "inline";
}
var TIER_RANK, UI_POSITIONS, CapabilityResolutionEngine;
var init_capability_resolution = __esm({
  "src/engines/capability-resolution.ts"() {
    "use strict";
    TIER_RANK = {
      free: 0,
      pro: 1,
      max: 2,
      enterprise: 3
    };
    UI_POSITIONS = ["composer", "header", "message", "sidebar", "inline"];
    CapabilityResolutionEngine = class {
      constructor(store) {
        this.store = store;
      }
      store;
      async resolve(providerId, planTier, opts) {
        const rows = await this.store.resolveCapabilities(providerId, planTier);
        const activeBindings = opts?.activeBindings ?? await this.store.getActiveBindings(providerId);
        return this.buildResult(rows, planTier, activeBindings, opts?.conversationContext);
      }
      async search(providerId, planTier, query, opts) {
        const rows = await this.store.searchCapabilities(providerId, planTier, query);
        const activeBindings = opts?.activeBindings ?? await this.store.getActiveBindings(providerId);
        const result = this.buildResult(rows, planTier, activeBindings, opts?.conversationContext);
        return this.applySearchFilter(result, query);
      }
      // ── internal ──────────────────────────────────────────────────────────────
      buildResult(rows, planTier, activeBindings, context) {
        const bindingSet = new Set(activeBindings);
        const groups = {
          composer: [],
          header: [],
          message: [],
          sidebar: [],
          inline: []
        };
        for (const row of rows) {
          if (tierRank(row.min_plan_tier) > tierRank(planTier)) continue;
          const cap = this.mapRow(row);
          if (!this.satisfiesExistentialRule(cap.existentialRule, context)) continue;
          if (!this.dependenciesSatisfied(cap.dependsOn, bindingSet)) continue;
          groups[normalizePosition(cap.uiPosition)].push(cap);
        }
        for (const pos of UI_POSITIONS) {
          groups[pos].sort(
            (a, b) => a.uiGroup === b.uiGroup ? a.uiOrder - b.uiOrder : a.uiGroup.localeCompare(b.uiGroup)
          );
        }
        const total = groups.composer.length + groups.header.length + groups.message.length + groups.sidebar.length + groups.inline.length;
        return {
          composer: groups.composer,
          header: groups.header,
          message: groups.message,
          sidebar: groups.sidebar,
          inline: groups.inline,
          total,
          resolvedAt: Date.now()
        };
      }
      mapRow(row) {
        return {
          id: row.id,
          slug: row.slug,
          name: row.name,
          category: row.category,
          uiComponent: row.ui_component,
          uiLabel: row.ui_label,
          uiIcon: row.ui_icon,
          uiPosition: row.ui_position,
          uiOrder: row.ui_order,
          uiGroup: row.ui_group,
          uiLayerDepth: row.ui_layer_depth,
          parentCapabilityId: row.parent_capability_id,
          uiPriority: row.ui_priority,
          interactionMode: row.interaction_mode,
          uiStates: safeJsonParse(row.ui_states_json, []),
          uiVisibilityRule: row.ui_visibility_rule,
          existentialRule: row.existential_rule,
          uiInputSchema: safeJsonParse(row.ui_input_schema, {}),
          mutationEffects: safeJsonParse(row.mutation_effects_json, {}),
          recoveryBehavior: row.recovery_behavior,
          statePersistence: row.state_persistence,
          dataFlow: row.data_flow,
          minPlanTier: row.min_plan_tier ?? "free",
          dependsOn: safeJsonParse(row.depends_on_json, []),
          concurrencySafe: row.concurrency_safe === 1,
          opClassification: row.op_classification,
          requiresUserConfirmation: row.requires_user_confirmation === 1,
          maxResultSize: row.max_result_size,
          resultComponent: row.result_component,
          resultLayout: row.result_layout,
          uiSlots: parseUiSlots(row.ui_component_override),
          searchHints: safeJsonParse(row.search_hints_json, []),
          aliases: safeJsonParse(row.aliases_json, []),
          availability: safeJsonParse(row.availability_json, {}),
          prefetch: row.prefetch === 1,
          overrideSources: {
            uiComponent: toOverrideSource(row.component_from),
            uiLabel: toOverrideSource(row.label_from),
            uiIcon: toOverrideSource(row.icon_from),
            uiPosition: toOverrideSource(row.position_from),
            uiOrder: toOverrideSource(row.order_from),
            uiGroup: toOverrideSource(row.group_from),
            uiPriority: toOverrideSource(row.priority_from),
            interactionMode: toOverrideSource(row.interaction_from),
            uiStates: toOverrideSource(row.states_from),
            uiVisibilityRule: toOverrideSource(row.visibility_from),
            existentialRule: toOverrideSource(row.existential_from),
            uiInputSchema: toOverrideSource(row.input_schema_from),
            mutationEffects: toOverrideSource(row.mutation_from),
            recoveryBehavior: toOverrideSource(row.recovery_from),
            statePersistence: toOverrideSource(row.persistence_from),
            dataFlow: toOverrideSource(row.data_flow_from),
            minPlanTier: toOverrideSource(row.plan_tier_from),
            dependsOn: toOverrideSource(row.depends_from)
          },
          bindingStatus: row.binding_status,
          bindingConfidence: row.binding_confidence,
          tierOverrides: {
            maxModels: row.tier_max_models ?? void 0,
            maxFileSize: row.tier_max_file_size ?? void 0,
            maxOptions: row.tier_max_options ?? void 0,
            customConfig: row.tier_config_json ? safeJsonParse(row.tier_config_json, {}) : void 0
          }
        };
      }
      /**
       * Evaluate a capability's existential rule against the conversation context.
       * Supported forms (whitespace-tolerant):
       *   "key"            → context.key is truthy
       *   "!key"           → context.key is falsy
       *   "key == value"   → String(context.key) === value
       *   "key != value"   → String(context.key) !== value
       * A null/empty rule is always satisfied. An unparseable rule defaults to satisfied.
       */
      satisfiesExistentialRule(rule, context) {
        if (!rule || rule.trim() === "") return true;
        if (!context) return true;
        const expr = rule.trim();
        const eqMatch = expr.match(/^([\w.]+)\s*(==|!=)\s*(.+)$/);
        if (eqMatch) {
          const [, key, op, rawValue] = eqMatch;
          const expected = (rawValue ?? "").trim().replace(/^['"]|['"]$/g, "");
          const actual = context[key];
          const equal = String(actual) === expected;
          return op === "==" ? equal : !equal;
        }
        if (expr.startsWith("!")) {
          const key = expr.slice(1).trim();
          return !context[key];
        }
        if (/^[\w.]+$/.test(expr)) {
          return Boolean(context[expr]);
        }
        return true;
      }
      dependenciesSatisfied(dependsOn, bindings) {
        if (dependsOn.length === 0) return true;
        return dependsOn.every((dep) => bindings.has(dep));
      }
      applySearchFilter(result, query) {
        const q = query.trim().toLowerCase();
        if (q === "") return result;
        const matches = (cap) => cap.name.toLowerCase().includes(q) || cap.slug.toLowerCase().includes(q) || cap.searchHints.some((h) => h.toLowerCase().includes(q)) || cap.aliases.some((a) => a.toLowerCase().includes(q));
        const composer = result.composer.filter(matches);
        const header = result.header.filter(matches);
        const message = result.message.filter(matches);
        const sidebar = result.sidebar.filter(matches);
        const inline = result.inline.filter(matches);
        return {
          composer,
          header,
          message,
          sidebar,
          inline,
          total: composer.length + header.length + message.length + sidebar.length + inline.length,
          resolvedAt: result.resolvedAt
        };
      }
    };
  }
});

// src/engines/stream-block-store.ts
var stream_block_store_exports = {};
__export(stream_block_store_exports, {
  StreamBlockStore: () => StreamBlockStore
});
function toRow(raw) {
  return {
    id: raw.id,
    conversationId: raw.conversationId,
    messageId: raw.messageId,
    blockIndex: raw.blockIndex,
    blockKind: raw.blockKind,
    blockData: raw.blockData,
    blockMeta: raw.blockMeta,
    createdAt: Number(raw.createdAt)
  };
}
var StreamBlockStore;
var init_stream_block_store = __esm({
  "src/engines/stream-block-store.ts"() {
    "use strict";
    init_ids();
    StreamBlockStore = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async storeBlocks(conversationId, messageId, blocks, meta) {
        if (blocks.length === 0) return;
        const now = Date.now();
        const metaJson = meta ? JSON.stringify(meta) : "{}";
        const values = blocks.map((block, i) => ({
          id: newId(),
          conversationId,
          messageId,
          blockIndex: i,
          blockKind: block.type,
          blockData: JSON.stringify(block),
          blockMeta: metaJson,
          createdAt: now
        }));
        await this.db.prisma.streamBlock.createMany({ data: values });
      }
      async getBlocksByConversation(conversationId, opts) {
        const where = { conversationId };
        if (opts?.messageId) where.messageId = opts.messageId;
        if (opts?.blockKind) where.blockKind = opts.blockKind;
        const rows = await this.db.prisma.streamBlock.findMany({
          where,
          orderBy: [{ messageId: "asc" }, { blockIndex: "asc" }],
          take: opts?.limit ?? 100,
          skip: opts?.offset ?? 0
        });
        return rows.map(toRow);
      }
      async getBlocksByMessage(messageId) {
        const rows = await this.db.prisma.streamBlock.findMany({
          where: { messageId },
          orderBy: { blockIndex: "asc" }
        });
        return rows.map(toRow);
      }
    };
  }
});

// src/storage/impl/node-store-impl.ts
var node_store_impl_exports = {};
__export(node_store_impl_exports, {
  NodeStoreImpl: () => NodeStoreImpl
});
function toRow2(n) {
  return {
    id: n.id,
    type: n.type,
    parentId: n.parentId ?? null,
    schemaVersion: n.schemaVersion,
    rawSource: n.source ?? null,
    dataJson: JSON.stringify(n.data ?? {}),
    edgesJson: JSON.stringify(n.edges ?? []),
    metaJson: JSON.stringify(n.meta ?? {}),
    searchText: "",
    conversationId: n.meta?.conversationId ?? null,
    messageId: n.meta?.messageId ?? null,
    sourceParser: n.meta?.sourceParser ?? null,
    contentHash: n.contentHash ?? (n.data ? hashContent(JSON.stringify(n.data)) : null),
    version: n.version,
    state: n.state,
    securityLevel: n.securityLevel ?? null,
    contentType: n.contentType ?? null,
    authorDid: n.authorDid ?? null,
    signature: n.signature ?? null,
    aclJson: JSON.stringify(n.acl ?? {}),
    qualityJson: JSON.stringify(n.quality ?? {}),
    validFrom: n.validFrom ?? null,
    validUntil: n.validUntil ?? null,
    parentVersion: n.parentVersion ?? null,
    // ACU fields
    acuType: n.acuType ?? null,
    lineageKind: n.lineageKind ?? null,
    extractorVersion: n.extractorVersion ?? null,
    parserVersion: n.parserVersion ?? null,
    valueScore: n.valueScore ?? null,
    isHighValue: n.isHighValue ? 1 : 0,
    createdAt: n.createdAt,
    updatedAt: n.updatedAt
  };
}
var NodeStoreImpl;
var init_node_store_impl = __esm({
  "src/storage/impl/node-store-impl.ts"() {
    "use strict";
    init_ids();
    NodeStoreImpl = class {
      constructor(prisma3) {
        this.prisma = prisma3;
      }
      prisma;
      async putNode(node) {
        const row = toRow2(node);
        await this.prisma.node.create({
          data: {
            id: row.id,
            type: row.type,
            parentId: row.parentId,
            schemaVersion: row.schemaVersion,
            rawSource: row.rawSource,
            dataJson: row.dataJson,
            edgesJson: row.edgesJson,
            metaJson: row.metaJson,
            searchText: row.searchText,
            conversationId: row.conversationId,
            messageId: row.messageId,
            sourceParser: row.sourceParser,
            contentHash: row.contentHash,
            version: row.version,
            state: row.state,
            securityLevel: row.securityLevel,
            contentType: row.contentType,
            authorDid: row.authorDid,
            signature: row.signature,
            aclJson: row.aclJson,
            qualityJson: row.qualityJson,
            validFrom: row.validFrom,
            validUntil: row.validUntil,
            parentVersion: row.parentVersion,
            createdAt: row.createdAt,
            updatedAt: row.updatedAt
          }
        });
        await this.prisma.nodeVersion.create({
          data: {
            id: newId(),
            nodeId: row.id,
            version: row.version,
            hash: row.contentHash ?? hashContent(row.dataJson),
            contentRef: row.dataJson,
            op: "create",
            parentVersion: row.parentVersion,
            createdAt: row.createdAt
          }
        });
        const edges = JSON.parse(row.edgesJson);
        for (const e of edges) {
          await this.putEdge({
            id: newId(),
            sourceId: row.id,
            targetId: e.targetId,
            edgeType: e.type,
            label: e.label,
            properties: e.properties,
            createdAt: row.createdAt
          }).catch(() => {
          });
        }
      }
      async getNode(id) {
        const r = await this.prisma.node.findUnique({ where: { id } });
        return r ? r : null;
      }
      async listNodes(opts = {}) {
        const rows = await this.prisma.node.findMany({
          where: {
            type: opts.type,
            parentId: opts.parentId,
            conversationId: opts.conversationId,
            messageId: opts.messageId
          },
          orderBy: { [opts.orderBy ?? "createdAt"]: opts.orderDir ?? "asc" },
          take: opts.limit,
          skip: opts.offset
        });
        return rows;
      }
      async getChildren(id) {
        const rows = await this.prisma.node.findMany({
          where: { parentId: id },
          orderBy: { createdAt: "asc" }
        });
        return rows;
      }
      async getLineage(id) {
        const lineage = [];
        let current = await this.getNode(id);
        while (current) {
          lineage.push(current);
          if (!current.parentId) break;
          current = await this.getNode(current.parentId);
        }
        return lineage;
      }
      async getRawSource(id) {
        const r = await this.prisma.node.findUnique({
          where: { id },
          select: { rawSource: true }
        });
        return r?.rawSource ?? null;
      }
      async putEdge(edge) {
        await this.prisma.nodeEdge.create({
          data: {
            id: edge.id,
            sourceId: edge.sourceId,
            targetId: edge.targetId,
            edgeType: edge.edgeType,
            label: edge.label ?? null,
            propertiesJson: JSON.stringify(edge.properties ?? {}),
            createdAt: edge.createdAt
          }
        });
      }
      async getOutgoingEdges(sourceId) {
        const rows = await this.prisma.nodeEdge.findMany({ where: { sourceId } });
        return rows.map((r) => ({
          type: r.edgeType,
          targetId: r.targetId,
          label: r.label ?? void 0,
          properties: JSON.parse(r.propertiesJson)
        }));
      }
      async getIncomingEdges(targetId) {
        const rows = await this.prisma.nodeEdge.findMany({ where: { targetId } });
        return rows.map((r) => ({
          type: r.edgeType,
          targetId: r.sourceId,
          label: r.label ?? void 0,
          properties: JSON.parse(r.propertiesJson)
        }));
      }
      async countNodes() {
        return this.prisma.node.count();
      }
      // ── Node-layer v2: version chain (time travel) ──
      async updateNode(id, patch) {
        const current = await this.prisma.node.findUnique({ where: { id } });
        if (!current) throw new Error(`updateNode: node ${id} not found`);
        const nextVersion = current.version + 1;
        const nextContentHash = patch.contentHash ?? hashContent(patch.dataJson ?? current.dataJson);
        await this.prisma.node.update({
          where: { id },
          data: {
            version: nextVersion,
            dataJson: patch.dataJson,
            edgesJson: patch.edgesJson,
            metaJson: patch.metaJson,
            searchText: patch.searchText,
            state: patch.state,
            contentHash: nextContentHash,
            aclJson: patch.aclJson,
            qualityJson: patch.qualityJson,
            validUntil: patch.validUntil,
            securityLevel: patch.securityLevel,
            contentType: patch.contentType,
            authorDid: patch.authorDid,
            signature: patch.signature,
            parentVersion: current.version,
            updatedAt: Date.now()
          }
        });
        await this.prisma.nodeVersion.create({
          data: {
            id: newId(),
            nodeId: id,
            version: nextVersion,
            hash: nextContentHash,
            contentRef: patch.dataJson ?? current.dataJson,
            op: "update",
            parentVersion: current.version,
            createdAt: Date.now()
          }
        });
      }
      async getNodeAtVersion(nodeId, version) {
        const r = await this.prisma.nodeVersion.findUnique({
          where: { nodeId_version: { nodeId, version } }
        });
        return r ? r : null;
      }
      async getNodeHistory(nodeId) {
        const rows = await this.prisma.nodeVersion.findMany({
          where: { nodeId },
          orderBy: { version: "asc" }
        });
        return rows;
      }
      // ── Node-layer v2: alias → canonical resolution ──
      async registerAlias(aliasId, canonicalId, method, confidence = 1) {
        await this.prisma.nodeAlias.upsert({
          where: { aliasId },
          create: {
            id: newId(),
            aliasId,
            canonicalId,
            method,
            confidence,
            createdAt: Date.now()
          },
          update: { canonicalId, method, confidence }
        });
      }
      async resolveAlias(aliasId) {
        const r = await this.prisma.nodeAlias.findUnique({ where: { aliasId } });
        return r?.canonicalId ?? null;
      }
      // ── Node-layer v2: rebuildable graph (OG ADR-001) ──
      async rebuildGraphFromNodes() {
        await this.prisma.nodeEdge.deleteMany({});
        const nodes = await this.prisma.node.findMany({
          select: { id: true, edgesJson: true, createdAt: true }
        });
        let count = 0;
        for (const n of nodes) {
          const edges = JSON.parse(n.edgesJson);
          for (const e of edges) {
            await this.prisma.nodeEdge.create({
              data: {
                id: newId(),
                sourceId: n.id,
                targetId: e.targetId,
                edgeType: e.type,
                label: e.label ?? null,
                weight: e.weight ?? null,
                propertiesJson: JSON.stringify(e.properties ?? {}),
                createdAt: n.createdAt
              }
            }).catch(() => {
            });
            count++;
          }
        }
        return count;
      }
    };
  }
});

// src/engines/execution-memoizer.ts
var execution_memoizer_exports = {};
__export(execution_memoizer_exports, {
  ExecutionMemoizer: () => ExecutionMemoizer
});
var ExecutionMemoizer;
var init_execution_memoizer = __esm({
  "src/engines/execution-memoizer.ts"() {
    "use strict";
    ExecutionMemoizer = class {
      constructor(eventBus, config2) {
        this.eventBus = eventBus;
        this.config = {
          defaultTtlMs: 5e3,
          maxEntries: 500,
          invalidationHooks: ["config:changed", "provider:seeded"],
          ...config2
        };
        this.subscribeToInvalidation();
      }
      eventBus;
      cache = /* @__PURE__ */ new Map();
      accessOrder = [];
      config;
      hits = 0;
      misses = 0;
      // ── Core API ───────────────────────────────────────────────────────────
      async getOrCompute(key, compute, ttlMs) {
        const entry = this.cache.get(key);
        const now = Date.now();
        if (entry && entry.expiresAt > now) {
          this.hits++;
          this.touch(key);
          return entry.value;
        }
        this.misses++;
        const value = await compute();
        this.set(key, value, ttlMs);
        return value;
      }
      set(key, value, ttlMs) {
        const now = Date.now();
        const ttl = ttlMs ?? this.config.defaultTtlMs;
        this.cache.set(key, {
          value,
          expiresAt: ttl === Number.POSITIVE_INFINITY ? Number.POSITIVE_INFINITY : now + ttl,
          createdAt: now
        });
        this.touch(key);
        this.evict();
      }
      get(key) {
        const entry = this.cache.get(key);
        if (!entry) return void 0;
        if (entry.expiresAt < Date.now()) {
          this.cache.delete(key);
          this.removeFromAccessOrder(key);
          return void 0;
        }
        this.touch(key);
        return entry.value;
      }
      // ── Invalidation ───────────────────────────────────────────────────────
      invalidate(key) {
        this.cache.delete(key);
        this.removeFromAccessOrder(key);
      }
      invalidateByPrefix(prefix) {
        const keys = [...this.cache.keys()].filter((k) => k.startsWith(prefix));
        for (const key of keys) {
          this.cache.delete(key);
          this.removeFromAccessOrder(key);
        }
      }
      invalidateAll() {
        this.cache.clear();
        this.accessOrder = [];
      }
      // ── Stats ──────────────────────────────────────────────────────────────
      getStats() {
        const total = this.hits + this.misses;
        return {
          size: this.cache.size,
          maxEntries: this.config.maxEntries,
          hits: this.hits,
          misses: this.misses,
          hitRate: total > 0 ? this.hits / total : 0
        };
      }
      // ── Internal ───────────────────────────────────────────────────────────
      touch(key) {
        this.removeFromAccessOrder(key);
        this.accessOrder.push(key);
      }
      removeFromAccessOrder(key) {
        const idx = this.accessOrder.indexOf(key);
        if (idx !== -1) this.accessOrder.splice(idx, 1);
      }
      evict() {
        while (this.cache.size > this.config.maxEntries) {
          const oldest = this.accessOrder.shift();
          if (oldest) this.cache.delete(oldest);
        }
      }
      subscribeToInvalidation() {
        if (!this.eventBus) return;
        for (const hook of this.config.invalidationHooks) {
          this.eventBus.on(hook, () => this.invalidateAll());
        }
      }
    };
  }
});

// src/engines/memory-engine.ts
var memory_engine_exports = {};
__export(memory_engine_exports, {
  MemoryEngine: () => MemoryEngine
});
var MemoryEngine;
var init_memory_engine = __esm({
  "src/engines/memory-engine.ts"() {
    "use strict";
    init_ids();
    MemoryEngine = class {
      constructor(episodic, semantic, procedural, eventBus) {
        this.episodic = episodic;
        this.semantic = semantic;
        this.procedural = procedural;
        this.eventBus = eventBus;
      }
      episodic;
      semantic;
      procedural;
      eventBus;
      consolidationTimer;
      // ── Recording ───────────────────────────────────────────────────────────
      async recordEpisode(input) {
        const episode = {
          id: newId(),
          ...input,
          tags: input.tags ?? [],
          timestamp: Date.now()
        };
        await this.episodic.save(episode);
        this.eventBus.emit({ type: "memory:episode_recorded", data: { id: episode.id } });
      }
      async assertFact(input) {
        const fact = {
          id: newId(),
          ...input,
          confidence: input.confidence ?? 1,
          timestamp: Date.now()
        };
        await this.semantic.save(fact);
        this.eventBus.emit({ type: "memory:fact_asserted", data: { id: fact.id } });
      }
      // ── Node-layer v2: emit a durable `cap-store.memory` Node (OG Memory +
      // FSRS-6). Each memory lands as a universally-stored Node with spaced-
      // repetition fields so the second brain can schedule reviews.
      async recordMemory(input) {
        const now = Date.now();
        const id = newId();
        const memoryData = {
          content: input.content,
          memoryType: input.memoryType,
          category: input.category,
          subcategory: input.subcategory ?? null,
          tags: input.tags ?? [],
          importance: input.importance ?? 0.5,
          relevance: input.relevance ?? 0.5,
          sourceConversationIds: input.sourceConversationIds ?? [],
          sourceMessageIds: input.sourceMessageIds ?? [],
          occurredAt: input.occurredAt ?? now,
          validFrom: input.validFrom ?? now,
          validUntil: input.validUntil ?? null,
          isPinned: input.isPinned ?? false,
          isArchived: input.isArchived ?? false,
          consolidationStatus: "unconsolidated",
          accessCount: 0,
          // FSRS-6 initial state (New card).
          stability: 1,
          difficulty: 0.3,
          dueDate: now,
          lastReview: null,
          reviewCount: 0,
          fsrsState: "New"
        };
        const nodeStore = input.nodeStore;
        if (nodeStore) {
          await nodeStore.putNode({
            id,
            type: "cap-store.memory",
            schemaVersion: 1,
            version: 1,
            state: "active",
            source: input.content,
            data: memoryData,
            edges: [],
            meta: {
              conversationId: input.conversationId,
              messageId: input.messageId,
              sourceParser: "memory-engine"
            },
            acl: { canView: true, canRemix: false, canReshare: false },
            authorDid: "assistant",
            contentType: "memory",
            securityLevel: 0,
            validFrom: memoryData.validFrom,
            validUntil: memoryData.validUntil ?? void 0,
            createdAt: now,
            updatedAt: now
          }).catch(() => {
          });
        }
        this.eventBus.emit({ type: "memory:recorded", data: { id, category: input.category } });
        return id;
      }
      async createRule(input) {
        const rule = {
          id: newId(),
          ...input,
          confidence: input.confidence ?? 0.5,
          successCount: 0,
          failureCount: 0,
          createdAt: Date.now(),
          updatedAt: Date.now()
        };
        await this.procedural.save(rule);
        this.eventBus.emit({ type: "memory:rule_created", data: { id: rule.id } });
      }
      // ── Querying ────────────────────────────────────────────────────────────
      async recallEpisodes(opts) {
        return this.episodic.query({ ...opts, limit: opts.limit ?? 50 });
      }
      async recallFacts(subject, predicate) {
        return this.semantic.findBySubject(subject, predicate);
      }
      async findRules(ctx) {
        return this.procedural.findByContext(ctx);
      }
      // ── Learning ────────────────────────────────────────────────────────────
      async learnFromEpisode(episode) {
        if (episode.success) {
          const existingRules = await this.procedural.findByContext({
            providerId: episode.providerId,
            capabilityId: episode.capabilityId,
            action: episode.action
          });
          if (existingRules.length > 0) {
            const rule = existingRules[0];
            if (rule) {
              rule.successCount++;
              rule.confidence = Math.min(1, rule.confidence + 0.05);
              rule.lastTriggered = Date.now();
              rule.updatedAt = Date.now();
              await this.procedural.save(rule);
            }
          } else {
            await this.createRule({
              name: `auto_${episode.providerId}_${episode.action}`,
              condition: `provider="${episode.providerId}" AND action="${episode.action}"`,
              action: episode.action,
              confidence: 0.3
            });
          }
        }
      }
      async minePatterns(opts) {
        const since = opts?.since ?? Date.now() - 24 * 60 * 60 * 1e3;
        const episodes = await this.episodic.query({
          providerId: opts?.providerId,
          since,
          limit: 1e3
        });
        const actionCounts = /* @__PURE__ */ new Map();
        for (const ep of episodes) {
          const key = `${ep.providerId}:${ep.action}`;
          const existing = actionCounts.get(key) ?? { success: 0, fail: 0 };
          if (ep.success) existing.success++;
          else existing.fail++;
          actionCounts.set(key, existing);
        }
        let rulesCreated = 0;
        let rulesUpdated = 0;
        const existingRules = await this.procedural.findAll();
        for (const [key, counts] of actionCounts) {
          const parts = key.split(":");
          const providerId = parts[0] ?? "";
          const action = parts[1] ?? "";
          const total = counts.success + counts.fail;
          const successRate = counts.success / total;
          if (total < 3) continue;
          const existing = existingRules.find(
            (r) => r.condition.includes(providerId) && r.condition.includes(action)
          );
          if (existing) {
            existing.successCount = counts.success;
            existing.failureCount = counts.fail;
            existing.confidence = successRate;
            existing.updatedAt = Date.now();
            await this.procedural.save(existing);
            rulesUpdated++;
          } else if (successRate > 0.7) {
            await this.createRule({
              name: `mined_${providerId}_${action}`,
              condition: `provider="${providerId}" AND action="${action}"`,
              action,
              confidence: successRate
            });
            rulesCreated++;
          }
        }
        return { patternsFound: actionCounts.size, rulesCreated, rulesUpdated };
      }
      async consolidate() {
        const now = Date.now();
        const pruneThreshold = now - 30 * 24 * 60 * 60 * 1e3;
        const rules = await this.procedural.findAll();
        for (const rule of rules) {
          if (rule.confidence < 0.2 && rule.failureCount > rule.successCount * 2) {
            await this.procedural.delete(rule.id);
          } else if (rule.lastTriggered && rule.lastTriggered < pruneThreshold) {
            rule.confidence *= 0.9;
            rule.updatedAt = now;
            await this.procedural.save(rule);
          }
        }
        this.eventBus.emit({
          type: "memory:consolidated",
          data: { rulesPruned: rules.length }
        });
      }
      // ── Knowledge types (Phase 15) ────────────────────────────────────────
      async recordEntity(input) {
        this.eventBus.emit({
          type: "memory:entity_recorded",
          data: { name: input.name, type: input.type }
        });
      }
      async recordDecision(input) {
        this.eventBus.emit({
          type: "memory:decision_recorded",
          data: { conversationId: input.conversationId, decisionText: input.decisionText }
        });
      }
      async recordPattern(input) {
        this.eventBus.emit({
          type: "memory:pattern_recorded",
          data: { name: input.name, patternType: input.patternType }
        });
      }
      async getTopics() {
        return [];
      }
      async getProjects() {
        return [];
      }
      async assignTopic(conversationId, topicId) {
        this.eventBus.emit({
          type: "memory:topic_assigned",
          data: { conversationId, topicId }
        });
      }
      // ── Agent support ─────────────────────────────────────────────────────
      async getAgentContext(providerId, capabilityId) {
        const recentEpisodes = await this.recallEpisodes({
          providerId,
          capabilityId,
          limit: 10
        });
        const relevantFacts = await this.recallFacts(providerId);
        const applicableRules = await this.findRules({ providerId, capabilityId });
        return { recentEpisodes, relevantFacts, applicableRules };
      }
      // ── Lifecycle ───────────────────────────────────────────────────────
      startConsolidation(intervalMs = 3e5) {
        this.consolidationTimer = setInterval(async () => {
          await this.minePatterns();
          await this.consolidate();
        }, intervalMs);
      }
      stopConsolidation() {
        if (this.consolidationTimer) {
          clearInterval(this.consolidationTimer);
          this.consolidationTimer = void 0;
        }
      }
    };
  }
});

// src/storage/impl/conversation-store-impl.ts
var conversation_store_impl_exports = {};
__export(conversation_store_impl_exports, {
  ConversationStoreImpl: () => ConversationStoreImpl
});
function toConversationRow(r) {
  return {
    id: r.id,
    providerSessionId: r.providerSessionId,
    providerId: r.providerId,
    title: r.title,
    state: r.state,
    messageCount: r.messageCount,
    lastMessageAt: r.lastMessageAt,
    contextJson: r.contextJson,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
function toMessageRow(r) {
  return {
    id: r.id,
    conversationId: r.conversationId,
    role: r.role,
    content: r.content,
    blocksJson: r.blocksJson,
    blockCount: r.blockCount,
    parentMessageId: r.parentMessageId,
    sequenceIndex: r.sequenceIndex,
    latencyMs: r.latencyMs,
    tokenCount: r.tokenCount,
    model: r.model,
    metadataJson: r.metadataJson,
    createdAt: r.createdAt
  };
}
function toAccountRow2(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    planTier: r.planTier,
    displayName: r.email,
    configJson: r.providerStateJson,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
var ConversationStoreImpl;
var init_conversation_store_impl = __esm({
  "src/storage/impl/conversation-store-impl.ts"() {
    "use strict";
    init_ids();
    ConversationStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async getConversation(id) {
        const row = await this.db.prisma.conversation.findUnique({ where: { id } });
        return row ? toConversationRow(row) : null;
      }
      async ensureProviderSession(input) {
        return this.db.ensureProviderSession(input);
      }
      async createConversation(input) {
        const now = Date.now();
        const row = await this.db.prisma.conversation.create({
          data: {
            id: newId(),
            providerSessionId: input.providerSessionId,
            providerId: input.providerId,
            title: input.title ?? null,
            state: input.state ?? "active",
            contextJson: input.contextJson ?? "{}",
            createdAt: now,
            updatedAt: now
          }
        });
        return toConversationRow(row);
      }
      async updateConversation(id, patch) {
        const data = { updatedAt: Date.now() };
        if (patch.title !== void 0) data.title = patch.title;
        if (patch.state !== void 0) data.state = patch.state;
        if (patch.messageCount !== void 0) data.messageCount = patch.messageCount;
        if (patch.lastMessageAt !== void 0) data.lastMessageAt = patch.lastMessageAt;
        if (patch.contextJson !== void 0) data.contextJson = patch.contextJson;
        await this.db.prisma.conversation.update({ where: { id }, data });
      }
      async deleteConversation(id) {
        await this.db.prisma.conversation.delete({ where: { id } });
      }
      async listConversations(opts) {
        const rows = await this.db.prisma.conversation.findMany({
          where: opts?.providerId ? { providerId: opts.providerId } : {},
          orderBy: { updatedAt: "desc" },
          take: opts?.limit ?? 100,
          skip: opts?.offset ?? 0
        });
        return rows.map((r) => toConversationRow(r));
      }
      async createMessage(input) {
        const row = await this.db.prisma.conversationMessage.create({
          data: {
            id: newId(),
            conversationId: input.conversationId,
            role: input.role,
            content: input.content ?? null,
            blocksJson: input.blocksJson ?? "[]",
            blockCount: input.blockCount ?? 0,
            parentMessageId: input.parentMessageId ?? null,
            sequenceIndex: input.sequenceIndex ?? 0,
            latencyMs: input.latencyMs ?? null,
            tokenCount: input.tokenCount ?? null,
            model: input.model ?? null,
            metadataJson: input.metadataJson ?? "{}",
            createdAt: Date.now()
          }
        });
        return toMessageRow(row);
      }
      async getMessage(id) {
        const row = await this.db.prisma.conversationMessage.findUnique({ where: { id } });
        return row ? toMessageRow(row) : null;
      }
      async getMessages(conversationId, opts) {
        const rows = await this.db.prisma.conversationMessage.findMany({
          where: {
            conversationId,
            ...opts?.before ? { id: { lt: opts.before } } : {}
          },
          orderBy: { sequenceIndex: "asc" },
          take: opts?.limit ?? 100
        });
        return rows.map((r) => toMessageRow(r));
      }
      async getLastMessage(conversationId) {
        const row = await this.db.prisma.conversationMessage.findFirst({
          where: { conversationId },
          orderBy: { sequenceIndex: "desc" }
        });
        return row ? toMessageRow(row) : null;
      }
      async getAccount(sessionId) {
        const session = await this.db.prisma.providerSession.findUnique({
          where: { id: sessionId },
          include: { account: true }
        });
        const account = session?.account;
        return account ? toAccountRow2(account) : null;
      }
      async updateMessage(id, patch) {
        const data = {};
        if (patch.content !== void 0) data.content = patch.content;
        if (patch.blocksJson !== void 0) data.blocksJson = patch.blocksJson;
        if (patch.metadataJson !== void 0) data.metadataJson = patch.metadataJson;
        await this.db.prisma.conversationMessage.update({ where: { id }, data });
      }
      async createAttachment(input) {
        const row = await this.db.prisma.messageAttachment.create({
          data: {
            id: newId(),
            messageId: input.messageId,
            filename: input.filename,
            mimeType: input.mimeType,
            sizeBytes: input.sizeBytes,
            storagePath: input.storagePath,
            thumbnailPath: input.thumbnailPath ?? null,
            metadataJson: input.metadataJson ?? "{}",
            createdAt: Date.now()
          }
        });
        return {
          id: row.id,
          messageId: row.messageId,
          filename: row.filename,
          mimeType: row.mimeType,
          sizeBytes: row.sizeBytes,
          storagePath: row.storagePath,
          thumbnailPath: row.thumbnailPath,
          metadataJson: row.metadataJson,
          createdAt: Number(row.createdAt)
        };
      }
      async getAttachments(messageId) {
        const rows = await this.db.prisma.messageAttachment.findMany({
          where: { messageId },
          orderBy: { createdAt: "asc" }
        });
        return rows.map((r) => ({
          id: r.id,
          messageId: r.messageId,
          filename: r.filename,
          mimeType: r.mimeType,
          sizeBytes: r.sizeBytes,
          storagePath: r.storagePath,
          thumbnailPath: r.thumbnailPath,
          metadataJson: r.metadataJson,
          createdAt: Number(r.createdAt)
        }));
      }
      async getAttachment(id) {
        const row = await this.db.prisma.messageAttachment.findUnique({ where: { id } });
        if (!row) return null;
        return {
          id: row.id,
          messageId: row.messageId,
          filename: row.filename,
          mimeType: row.mimeType,
          sizeBytes: row.sizeBytes,
          storagePath: row.storagePath,
          thumbnailPath: row.thumbnailPath,
          metadataJson: row.metadataJson,
          createdAt: Number(row.createdAt)
        };
      }
      async deleteAttachment(id) {
        await this.db.prisma.messageAttachment.delete({ where: { id } });
      }
    };
  }
});

// src/storage/impl/capability-resolution-store-impl.ts
var capability_resolution_store_impl_exports = {};
__export(capability_resolution_store_impl_exports, {
  CapabilityResolutionStoreImpl: () => CapabilityResolutionStoreImpl
});
var CapabilityResolutionStoreImpl;
var init_capability_resolution_store_impl = __esm({
  "src/storage/impl/capability-resolution-store-impl.ts"() {
    "use strict";
    CapabilityResolutionStoreImpl = class {
      constructor(prisma3) {
        this.prisma = prisma3;
      }
      prisma;
      get p() {
        return this.prisma;
      }
      async resolveCapabilities(providerId, planTier) {
        const sql = `
      SELECT
        ct.*,
        COALESCE(cb.status, 'inactive') AS binding_status,
        COALESCE(cb.confidence, 0) AS binding_confidence,
        pc.ui_component_override AS ui_component_override,
        pt.max_models AS tier_max_models,
        pt.max_file_size AS tier_max_file_size,
        pt.max_options AS tier_max_options,
        pt.custom_config_json AS tier_config_json
      FROM capability_taxonomy ct
      LEFT JOIN capability_binding cb ON cb.global_id = ct.id AND cb.provider_id = $1
      LEFT JOIN provider_capability pc ON pc.provider_id = $1 AND pc.global_capability_id = ct.id
      LEFT JOIN capability_tier pt ON pt.capability_id = ct.id AND pt.plan_tier = $2
      WHERE cb.status = 'active' OR cb.id IS NULL
      ORDER BY ct.ui_position, ct.ui_order
    `;
        const rows = await this.p.$queryRawUnsafe(sql, providerId, planTier);
        return rows ?? [];
      }
      async getActiveBindings(providerId) {
        const sql = `
      SELECT global_id FROM capability_binding
      WHERE provider_id = $1 AND status = 'active'
    `;
        const rows = await this.p.$queryRawUnsafe(sql, providerId);
        return (rows ?? []).map((r) => r.global_id);
      }
      async searchCapabilities(providerId, planTier, query) {
        const sql = `
      SELECT
        ct.*,
        COALESCE(cb.status, 'inactive') AS binding_status,
        COALESCE(cb.confidence, 0) AS binding_confidence,
        pc.ui_component_override AS ui_component_override,
        pt.max_models AS tier_max_models,
        pt.max_file_size AS tier_max_file_size,
        pt.max_options AS tier_max_options,
        pt.custom_config_json AS tier_config_json
      FROM capability_taxonomy ct
      LEFT JOIN capability_binding cb ON cb.global_id = ct.id AND cb.provider_id = $1
      LEFT JOIN provider_capability pc ON pc.provider_id = $1 AND pc.global_capability_id = ct.id
      LEFT JOIN capability_tier pt ON pt.capability_id = ct.id AND pt.plan_tier = $2
      WHERE (ct.name ILIKE $3 OR ct.slug ILIKE $3 OR ct.search_hints_json ILIKE $3)
        AND (cb.status = 'active' OR cb.id IS NULL)
      ORDER BY ct.ui_position, ct.ui_order
      LIMIT 20
    `;
        const pattern2 = `%${query}%`;
        const rows = await this.p.$queryRawUnsafe(
          sql,
          providerId,
          planTier,
          pattern2
        );
        return rows ?? [];
      }
    };
  }
});

// src/storage/impl/parser-execution-log-store-impl.ts
var parser_execution_log_store_impl_exports = {};
__export(parser_execution_log_store_impl_exports, {
  ParserExecutionLogStoreImpl: () => ParserExecutionLogStoreImpl
});
function toLogRow(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    parserName: r.parserName,
    parserVersion: r.parserVersion,
    conversationId: r.conversationId,
    messageId: r.messageId,
    confidence: r.confidence,
    blockCount: r.blockCount,
    textBlocks: r.textBlocks,
    toolCallBlocks: r.toolCallBlocks,
    fileBlocks: r.fileBlocks,
    errorBlocks: r.errorBlocks,
    durationMs: r.durationMs,
    rawSizeBytes: r.rawSizeBytes,
    wireFormat: r.wireFormat,
    fallbackUsed: r.fallbackUsed,
    metadataJson: r.metadataJson,
    createdAt: r.createdAt
  };
}
var ParserExecutionLogStoreImpl;
var init_parser_execution_log_store_impl = __esm({
  "src/storage/impl/parser-execution-log-store-impl.ts"() {
    "use strict";
    init_ids();
    ParserExecutionLogStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async logExecution(row) {
        const now = Date.now();
        await this.p.parserExecutionLog.create({
          data: {
            id: newId(),
            providerId: row.providerId,
            parserName: row.parserName,
            parserVersion: row.parserVersion,
            conversationId: row.conversationId,
            messageId: row.messageId,
            confidence: row.confidence,
            blockCount: row.blockCount,
            textBlocks: row.textBlocks,
            toolCallBlocks: row.toolCallBlocks,
            fileBlocks: row.fileBlocks,
            errorBlocks: row.errorBlocks,
            durationMs: row.durationMs,
            rawSizeBytes: row.rawSizeBytes,
            wireFormat: row.wireFormat,
            fallbackUsed: row.fallbackUsed,
            metadataJson: row.metadataJson,
            createdAt: now
          }
        });
      }
      async getRecentByProvider(providerId, limit = 20) {
        const rows = await this.p.parserExecutionLog.findMany({
          where: { providerId },
          orderBy: { createdAt: "desc" },
          take: limit
        });
        return rows.map(toLogRow);
      }
      async getLowConfidenceEntries(threshold = 0.7, limit = 50) {
        const rows = await this.p.parserExecutionLog.findMany({
          where: { confidence: { lt: threshold } },
          orderBy: { createdAt: "desc" },
          take: limit
        });
        return rows.map(toLogRow);
      }
      async getStatsByProvider(providerId) {
        const rows = await this.p.parserExecutionLog.findMany({
          where: { providerId },
          select: {
            confidence: true,
            durationMs: true,
            fallbackUsed: true
          }
        });
        if (rows.length === 0) return null;
        const total = rows.length;
        const avgConf = rows.reduce((s, r) => s + r.confidence, 0) / total;
        const avgDur = rows.reduce((s, r) => s + r.durationMs, 0) / total;
        const fallbacks = rows.filter((r) => r.fallbackUsed).length;
        return {
          totalExecutions: total,
          avgConfidence: avgConf,
          avgDurationMs: avgDur,
          fallbackRate: fallbacks / total
        };
      }
    };
  }
});

// src/storage/impl/content-unit-store-impl.ts
var content_unit_store_impl_exports = {};
__export(content_unit_store_impl_exports, {
  ContentUnitStoreImpl: () => ContentUnitStoreImpl
});
function toRow3(r) {
  return {
    id: r.id,
    messageId: r.messageId,
    conversationId: r.conversationId,
    unitType: r.unitType,
    content: r.content,
    mimeType: r.mimeType,
    metadataJson: r.metadataJson,
    sequenceIndex: r.sequenceIndex,
    qualityScore: r.qualityScore,
    createdAt: Number(r.createdAt)
  };
}
var ContentUnitStoreImpl;
var init_content_unit_store_impl = __esm({
  "src/storage/impl/content-unit-store-impl.ts"() {
    "use strict";
    ContentUnitStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async storeUnits(units) {
        if (units.length === 0) return;
        const now = Date.now();
        await this.p.contentUnit.createMany({
          data: units.map((u) => ({
            id: u.id,
            messageId: u.messageId,
            conversationId: u.conversationId,
            unitType: u.unitType,
            content: u.content,
            mimeType: u.mimeType,
            metadataJson: u.metadataJson,
            sequenceIndex: u.sequenceIndex,
            qualityScore: u.qualityScore,
            createdAt: now
          }))
        });
      }
      async getUnitsByMessage(messageId) {
        const rows = await this.p.contentUnit.findMany({
          where: { messageId },
          orderBy: { sequenceIndex: "asc" }
        });
        return rows.map(toRow3);
      }
      async getUnitsByConversation(conversationId, opts) {
        const where = { conversationId };
        if (opts?.unitType) where.unitType = opts.unitType;
        const rows = await this.p.contentUnit.findMany({
          where,
          orderBy: { sequenceIndex: "asc" },
          take: opts?.limit ?? 100,
          skip: opts?.offset ?? 0
        });
        return rows.map(toRow3);
      }
      async getUnitsByType(conversationId, unitType) {
        const rows = await this.p.contentUnit.findMany({
          where: { conversationId, unitType },
          orderBy: { sequenceIndex: "asc" }
        });
        return rows.map(toRow3);
      }
    };
  }
});

// src/storage/impl/capability-store-impl.ts
var capability_store_impl_exports = {};
__export(capability_store_impl_exports, {
  CapabilityStoreImpl: () => CapabilityStoreImpl
});
function toTaxonomyRow(r) {
  return {
    id: r.id,
    slug: r.slug,
    name: r.name,
    description: r.description,
    kind: r.kind,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
function toBindingRow(r) {
  return {
    id: r.id,
    capabilityId: r.globalId,
    providerId: r.providerId,
    selectorStrategyId: r.selectorStrategyId,
    status: r.status,
    healthScore: r.healthScore,
    lastSuccessAt: r.lastSuccessAt,
    lastFailureAt: r.lastFailureAt,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
function toProgramRow(r) {
  return {
    id: r.id,
    bindingId: r.bindingId,
    version: r.version,
    status: r.status,
    configJson: r.configJson,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
function toSelectorRow(r) {
  return {
    id: r.id,
    name: r.name,
    capabilityId: r.capabilityId,
    providerId: r.providerId,
    selectorValue: r.selector,
    priority: r.priority,
    strategyType: r.strategyType,
    isActive: Boolean(r.isActive),
    hitCount: r.hitCount,
    missCount: r.missCount,
    lastUsedAt: r.lastUsedAt ?? null,
    createdAt: r.createdAt,
    updatedAt: r.updatedAt
  };
}
var CapabilityStoreImpl;
var init_capability_store_impl = __esm({
  "src/storage/impl/capability-store-impl.ts"() {
    "use strict";
    init_ids();
    CapabilityStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async getCapability(id) {
        const r = await this.p.capabilityTaxonomy.findUnique({ where: { id } });
        return r ? toTaxonomyRow(r) : null;
      }
      async getCapabilityBySlug(slug) {
        const r = await this.p.capabilityTaxonomy.findUnique({ where: { slug } });
        return r ? toTaxonomyRow(r) : null;
      }
      async getBinding(capabilityId, providerId) {
        const r = await this.p.capabilityBinding.findFirst({
          where: { globalId: capabilityId, providerId }
        });
        return r ? toBindingRow(r) : null;
      }
      async getProgram(bindingId) {
        const r = await this.p.capabilityProgram.findFirst({
          where: { bindingId },
          orderBy: { version: "desc" }
        });
        return r ? toProgramRow(r) : null;
      }
      async getPrograms(bindingId) {
        const rows = await this.p.capabilityProgram.findMany({
          where: { bindingId },
          orderBy: { version: "desc" }
        });
        return rows.map(toProgramRow);
      }
      async getBestProgramByCapability(capabilitySlug, providerId) {
        const binding = await this.p.capabilityBinding.findFirst({
          where: { globalId: capabilitySlug, providerId }
        });
        if (!binding) return null;
        const r = await this.p.capabilityProgram.findFirst({
          where: { bindingId: binding.id },
          orderBy: { version: "desc" }
        });
        return r ? toProgramRow(r) : null;
      }
      async getSelectors(capabilityId, providerId) {
        const rows = await this.p.selectorStrategy.findMany({
          where: { capabilityId, providerId, isActive: 1 },
          orderBy: { priority: "asc" }
        });
        return rows.map(toSelectorRow);
      }
      async createOutcome(outcome) {
        const id = newId();
        const now = Date.now();
        await this.p.outcome.create({
          data: {
            id,
            capabilityId: outcome.capabilityId,
            providerId: outcome.providerId,
            ok: outcome.ok ? 1 : 0,
            durationMs: outcome.latencyMs,
            error: outcome.error ?? null,
            selectorStrategyId: null,
            selectorUsed: null,
            selectorHit: null,
            ts: now
          }
        });
        return {
          id,
          capabilityId: outcome.capabilityId,
          bindingId: outcome.bindingId ?? null,
          providerId: outcome.providerId,
          accountId: outcome.accountId,
          ok: outcome.ok,
          latencyMs: outcome.latencyMs,
          error: outcome.error ?? null,
          outputJson: outcome.outputJson ?? "",
          traceId: outcome.traceId,
          createdAt: now
        };
      }
      async updateBindingHealth(bindingId, patch) {
        const now = Date.now();
        await this.p.capabilityBinding.update({
          where: { id: bindingId },
          data: {
            ...patch.status !== void 0 ? { status: patch.status } : {},
            ...patch.healthScore !== void 0 ? { healthScore: patch.healthScore } : {},
            ...patch.lastSuccessAt !== void 0 ? { lastSuccessAt: patch.lastSuccessAt } : {},
            ...patch.lastFailureAt !== void 0 ? { lastFailureAt: patch.lastFailureAt } : {},
            updatedAt: now
          }
        });
      }
      async updateSelectorHealth(selectorId, hit) {
        const now = Date.now();
        if (hit) {
          await this.p.selectorStrategy.update({
            where: { id: selectorId },
            data: { hitCount: { increment: 1 }, updatedAt: now }
          });
        } else {
          await this.p.selectorStrategy.update({
            where: { id: selectorId },
            data: { missCount: { increment: 1 }, updatedAt: now }
          });
        }
      }
      async loadSnapshot(providerIds) {
        if (providerIds.length === 0) return [];
        const bindings = await this.p.capabilityBinding.findMany({
          where: { providerId: { in: providerIds }, status: "active" },
          include: { capability: true, programs: true }
        });
        const rows = [];
        for (const b of bindings) {
          const programId = b.bestProgramId ?? b.currentProgramId ?? null;
          const program = programId ? b.programs.find((p) => p.id === programId) ?? null : null;
          rows.push({
            globalId: b.globalId,
            slug: b.capability.slug,
            providerId: b.providerId,
            category: b.capability.category,
            status: b.status,
            confidence: b.confidence,
            programId,
            configJson: program?.configJson ?? null,
            uiComponent: b.capability.uiComponent,
            uiPosition: b.capability.uiPosition,
            uiInputSchema: b.capability.uiInputSchema
          });
        }
        return rows;
      }
    };
  }
});

// src/engines/capability-snapshot.ts
var capability_snapshot_exports = {};
__export(capability_snapshot_exports, {
  CapabilitySnapshot: () => CapabilitySnapshot
});
var CapabilitySnapshot;
var init_capability_snapshot = __esm({
  "src/engines/capability-snapshot.ts"() {
    "use strict";
    CapabilitySnapshot = class {
      constructor(store) {
        this.store = store;
      }
      store;
      bySlugProvider = /* @__PURE__ */ new Map();
      byIdProvider = /* @__PURE__ */ new Map();
      bySlugAny = /* @__PURE__ */ new Map();
      /**
       * Boot loader. Reads active bindings for the given (registered) providers and
       * builds the in-memory maps. Returns the number of entries loaded. Idempotent:
       * re-loading replaces the whole map.
       */
      async load(registeredProviderIds) {
        const rows = await this.store.loadSnapshot(registeredProviderIds);
        this.bySlugProvider.clear();
        this.byIdProvider.clear();
        this.bySlugAny.clear();
        for (const r of rows) {
          const entry = {
            ...r,
            executable: r.programId != null
          };
          this.bySlugProvider.set(`${r.slug}@${r.providerId}`, entry);
          this.byIdProvider.set(`${r.globalId}@${r.providerId}`, entry);
          if (!this.bySlugAny.has(r.slug)) this.bySlugAny.set(r.slug, entry);
        }
        return this.bySlugProvider.size;
      }
      /** Resolve by slug for a provider; falls back to provider-agnostic entry. */
      getBySlug(slug, providerId) {
        if (providerId) {
          const hit = this.bySlugProvider.get(`${slug}@${providerId}`);
          if (hit) return hit;
        }
        return this.bySlugAny.get(slug) ?? null;
      }
      /** Resolve by taxonomy globalId for a provider. */
      getById(globalId, providerId) {
        if (providerId) {
          const hit = this.byIdProvider.get(`${globalId}@${providerId}`);
          if (hit) return hit;
        }
        return null;
      }
      /** All loaded entries (registry bridge / debug surface). */
      all() {
        return [...this.bySlugProvider.values()];
      }
    };
  }
});

// src/storage/impl/episodic-memory-store-impl.ts
var episodic_memory_store_impl_exports = {};
__export(episodic_memory_store_impl_exports, {
  EpisodicMemoryStoreImpl: () => EpisodicMemoryStoreImpl
});
var EpisodicMemoryStoreImpl;
var init_episodic_memory_store_impl = __esm({
  "src/storage/impl/episodic-memory-store-impl.ts"() {
    "use strict";
    EpisodicMemoryStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async save(episode) {
        await this.p.episodicMemory.upsert({
          where: { id: episode.id },
          create: {
            id: episode.id,
            provider_id: episode.providerId,
            capability_id: episode.capabilityId ?? null,
            slave_id: episode.slaveId ?? null,
            action: episode.action,
            input_json: JSON.stringify(episode.input),
            output_json: JSON.stringify(episode.output),
            success: episode.success ? 1 : 0,
            duration_ms: episode.durationMs,
            tags_json: JSON.stringify(episode.tags),
            timestamp: episode.timestamp,
            created_at: Date.now()
          },
          update: {}
        });
      }
      async query(opts) {
        const where = {};
        if (opts.providerId) where.provider_id = opts.providerId;
        if (opts.capabilityId) where.capability_id = opts.capabilityId;
        if (opts.action) where.action = opts.action;
        if (opts.since) where.timestamp = { gte: opts.since };
        if (opts.successOnly) where.success = 1;
        const rows = await this.p.episodicMemory.findMany({
          where,
          orderBy: { timestamp: "desc" },
          take: opts.limit ?? 50
        });
        return rows.map((r) => ({
          id: r.id,
          providerId: r.provider_id,
          capabilityId: r.capability_id ?? void 0,
          slaveId: r.slave_id ?? void 0,
          action: r.action,
          input: JSON.parse(r.input_json ?? "{}"),
          output: JSON.parse(r.output_json ?? "{}"),
          success: r.success === 1,
          durationMs: r.duration_ms,
          timestamp: r.timestamp,
          tags: JSON.parse(r.tags_json ?? "[]")
        }));
      }
      async count() {
        return this.p.episodicMemory.count();
      }
    };
  }
});

// src/storage/impl/semantic-memory-store-impl.ts
var semantic_memory_store_impl_exports = {};
__export(semantic_memory_store_impl_exports, {
  SemanticMemoryStoreImpl: () => SemanticMemoryStoreImpl
});
var SemanticMemoryStoreImpl;
var init_semantic_memory_store_impl = __esm({
  "src/storage/impl/semantic-memory-store-impl.ts"() {
    "use strict";
    SemanticMemoryStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async save(fact) {
        await this.p.semanticMemory.upsert({
          where: { id: fact.id },
          create: {
            id: fact.id,
            subject: fact.subject,
            predicate: fact.predicate,
            object_json: JSON.stringify(fact.object),
            confidence: fact.confidence,
            source: fact.source,
            timestamp: fact.timestamp,
            expires_at: fact.expiresAt ?? null,
            created_at: Date.now()
          },
          update: {}
        });
      }
      async findBySubject(subject, predicate) {
        const where = { subject };
        if (predicate) where.predicate = predicate;
        const rows = await this.p.semanticMemory.findMany({
          where,
          orderBy: { timestamp: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          subject: r.subject,
          predicate: r.predicate,
          object: JSON.parse(r.object_json ?? "{}"),
          confidence: r.confidence,
          source: r.source,
          timestamp: r.timestamp,
          expiresAt: r.expires_at ?? void 0
        }));
      }
      async delete(id) {
        await this.p.semanticMemory.delete({ where: { id } });
      }
    };
  }
});

// src/storage/impl/procedural-memory-store-impl.ts
var procedural_memory_store_impl_exports = {};
__export(procedural_memory_store_impl_exports, {
  ProceduralMemoryStoreImpl: () => ProceduralMemoryStoreImpl
});
var ProceduralMemoryStoreImpl;
var init_procedural_memory_store_impl = __esm({
  "src/storage/impl/procedural-memory-store-impl.ts"() {
    "use strict";
    ProceduralMemoryStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async save(rule) {
        await this.p.proceduralRule.upsert({
          where: { id: rule.id },
          create: {
            id: rule.id,
            name: rule.name,
            condition: rule.condition,
            action: rule.action,
            confidence: rule.confidence,
            success_count: rule.successCount,
            failure_count: rule.failureCount,
            last_triggered: rule.lastTriggered ?? null,
            created_at: rule.createdAt,
            updated_at: rule.updatedAt
          },
          update: {}
        });
      }
      async findByContext(ctx) {
        const where = {};
        if (ctx.providerId) where.condition = { contains: ctx.providerId };
        if (ctx.capabilityId) where.condition = { contains: ctx.capabilityId };
        if (ctx.action) where.action = ctx.action;
        const rows = await this.p.proceduralRule.findMany({
          where,
          orderBy: { confidence: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          name: r.name,
          condition: r.condition,
          action: r.action,
          confidence: r.confidence,
          successCount: r.success_count,
          failureCount: r.failure_count,
          lastTriggered: r.last_triggered ?? void 0,
          createdAt: r.created_at,
          updatedAt: r.updated_at
        }));
      }
      async findAll() {
        const rows = await this.p.proceduralRule.findMany({
          orderBy: { confidence: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          name: r.name,
          condition: r.condition,
          action: r.action,
          confidence: r.confidence,
          successCount: r.success_count,
          failureCount: r.failure_count,
          lastTriggered: r.last_triggered ?? void 0,
          createdAt: r.created_at,
          updatedAt: r.updated_at
        }));
      }
      async delete(id) {
        await this.p.proceduralRule.delete({ where: { id } });
      }
    };
  }
});

// seeds/parsers/harvested/chatgpt-openai-delta.ts
var LOGIC_CODE;
var init_chatgpt_openai_delta = __esm({
  "seeds/parsers/harvested/chatgpt-openai-delta.ts"() {
    "use strict";
    LOGIC_CODE = `
function extractOpenAIBlock(data) {
  const api = data.choices && data.choices[0] && data.choices[0].delta && data.choices[0].delta.content;
  if (api !== undefined && api !== null) return { type: 'text', text: String(api) };
  if (data.o === 'patch' && Array.isArray(data.v)) {
    const parts = [];
    for (const patch of data.v) {
      if (patch && typeof patch === 'object' && typeof patch.p === 'string' && typeof patch.v === 'string') {
        if (patch.p.indexOf('/message/content/parts/') === 0 && (patch.o === 'append' || patch.o === 'add' || patch.o === 'replace')) parts.push(patch.v);
      }
    }
    if (parts.length > 0) return { type: 'text', text: parts.join('') };
  }
  if (data.o === 'add' && data.v && data.v.message && data.v.message.content && data.v.message.content.parts) {
    return fromParts(data.v.message.content.parts);
  }
  const cp = data.message && data.message.content && data.message.content.parts;
  if (Array.isArray(cp)) return fromParts(cp);
  return null;
}
function fromParts(parts) {
  const blocks = [];
  for (const p of parts) {
    if (typeof p === 'string') blocks.push({ type: 'text', text: p });
    else if (p && typeof p === 'object' && 'asset_pointer' in p) blocks.push({ type: 'file', url: String(p.asset_pointer), mediaType: 'image/png', filename: '' });
  }
  if (blocks.length === 0) return null;
  return blocks.length === 1 ? blocks[0] : blocks[0];
}
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const block = extractOpenAIBlock(data);
      if (block) {
        if (Array.isArray(block)) blocks.push.apply(blocks, block);
        else blocks.push(block);
      }
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finish_reason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('choices') && b.includes('delta')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'chatgpt/001_openai_delta', version: 1, providerId: 'chatgpt', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvested/claude-streaming-sse.ts
var LOGIC_CODE2;
var init_claude_streaming_sse = __esm({
  "seeds/parsers/harvested/claude-streaming-sse.ts"() {
    "use strict";
    LOGIC_CODE2 = `
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const json = JSON.parse(payload);
      if (json.type === 'content_block_start' && json.content_block) {
        const cb = json.content_block;
        if (cb.type === 'thinking') blocks.push({ type: 'reasoning', text: '' });
        else if (cb.type === 'tool_use') blocks.push({ type: 'tool-call', toolCallId: 'tc_' + blocks.length, toolName: String(cb.name || ''), input: cb.input || {} });
        else if (cb.type === 'image' || cb.type === 'image_url') blocks.push({ type: 'file', mediaType: cb.source && cb.source.type === 'image/jpeg' ? 'image/jpeg' : 'image/png', url: String((cb.source && cb.source.url) || cb.url || ''), filename: String(cb.alt || '') });
        else if (cb.type === 'text') blocks.push({ type: 'text', text: String(cb.text || '') });
      }
      if (json.type === 'content_block_delta' && json.delta) {
        const delta = json.delta;
        if (typeof delta.text === 'string') {
          const last = blocks[blocks.length - 1];
          if (last && last.type === 'text') last.text += delta.text;
          else blocks.push({ type: 'text', text: delta.text });
        } else if (typeof delta.thinking === 'string') {
          const last = blocks[blocks.length - 1];
          if (last && last.type === 'reasoning') last.text += delta.thinking;
          else blocks.push({ type: 'reasoning', text: delta.thinking });
        }
      }
      if (json.type === 'message_start' && json.message) blocks.push({ type: 'meta', key: 'message_id', value: json.message.id });
      if (json.type === 'message_stop' || json.type === 'error') {
        const last = blocks[blocks.length - 1];
        if (last && last.type !== 'meta') blocks.push({ type: 'meta', key: 'stopped', value: json.type });
      }
    } catch (_e) { /* skip unparseable lines */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('message_stop') || String(rawBody).includes('[DONE]');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  const hasData = b.includes('data:');
  const hasContent = b.includes('content_block_delta') || b.includes('content_block_start');
  if (!hasData) return 0;
  if (!hasContent) return b.includes('message_stop') ? 0.7 : 0.3;
  return 1;
}
module.exports.default = { name: 'claude/001_streaming_sse', version: 1, providerId: 'claude', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvested/gemini-batchexecute.ts
var LOGIC_CODE3;
var init_gemini_batchexecute = __esm({
  "seeds/parsers/harvested/gemini-batchexecute.ts"() {
    "use strict";
    LOGIC_CODE3 = `
function safeJsonParse(s) { try { return JSON.parse(s); } catch (_e) { return s; } }
function decodeEnvelope(raw) {
  const frames = [];
  let text = String(raw);
  if (text.indexOf(")]}'\\n") === 0) text = text.slice(5);
  const lines = text.split('\\n');
  for (const line of lines) {
    const t = line.trim();
    if (!t) continue;
    if (/^\\d+$/.test(t)) continue;
    if (t.charAt(0) !== '[') continue;
    try {
      const arr = JSON.parse(t);
      if (!Array.isArray(arr)) continue;
      let isTerminal = false;
      for (const child of arr) { if (Array.isArray(child) && child[0] === 'e') { isTerminal = true; break; } }
      for (const child of arr) {
        if (Array.isArray(child) && child[0] === 'wrb.fr') {
          frames.push({ rpc: String(child[1] || ''), payload: typeof child[2] === 'string' ? safeJsonParse(child[2]) : child[2], error: child[5] != null ? child[5] : null, isTerminal: isTerminal });
        }
      }
      if (isTerminal && frames.length === 0) frames.push({ rpc: '', payload: null, error: null, isTerminal: true });
    } catch (_e) { /* skip */ }
  }
  return frames;
}
function parseStreamChunk(frame) {
  const payload = frame.payload;
  if (!Array.isArray(payload)) return null;
  // Real gemini batchexecute: payload[4] holds [[deltaText]] (or payload[3]).
  const candidate = payload[4] != null ? payload[4] : payload[3];
  const textArr = Array.isArray(candidate) ? candidate[0] : undefined;
  // text lives at textArr[0]; some envelopes double-wrap as textArr[1].
  let deltaArr = Array.isArray(textArr) ? (textArr[1] != null ? textArr[1] : textArr[0]) : undefined;
  if (typeof deltaArr === 'undefined' && Array.isArray(textArr)) deltaArr = textArr[0];
  let textDelta = '';
  if (Array.isArray(deltaArr)) textDelta = deltaArr.filter(function (s) { return typeof s === 'string'; }).join('');
  else if (typeof deltaArr === 'string') textDelta = deltaArr;
  return textDelta ? { textDelta: textDelta } : null;
}
function parse(rawBody) {
  const blocks = [];
  const frames = decodeEnvelope(rawBody);
  for (const frame of frames) {
    if (frame.isTerminal) continue;
    if (frame.rpc) {
      const delta = parseStreamChunk(frame);
      if (delta && delta.textDelta) blocks.push({ type: 'text', text: delta.textDelta });
      else blocks.push({ type: 'meta', key: 'gemini_' + frame.rpc, value: frame.payload });
    }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[["e"') || b.includes('"e"') || b.includes('"isTerminal":true');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.indexOf(')]}\\'') === 0 || b.includes('wrb.fr')) return 1;
  if (b.includes('[[')) return 0.6;
  return 0.2;
}
module.exports.default = { name: 'gemini/001_batchexecute', version: 1, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvested/generic-format-agnostic.ts
var LOGIC_CODE4;
var init_generic_format_agnostic = __esm({
  "seeds/parsers/harvested/generic-format-agnostic.ts"() {
    "use strict";
    LOGIC_CODE4 = `
function parse(rawBody) {
  const b = String(rawBody);
  const blocks = [];
  // SSE data: frames
  const lines = b.split('\\n');
  let sawSse = false;
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    sawSse = true;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') continue;
    try {
      const data = JSON.parse(payload);
      const choices = data.choices && data.choices[0];
      const delta = choices && choices.delta && choices.delta.content;
      if (typeof delta === 'string' && delta) { blocks.push({ type: 'text', text: delta }); continue; }
      const parts = choices && choices.message && choices.message.content && choices.message.content.parts;
      if (Array.isArray(parts)) { blocks.push({ type: 'text', text: parts.join('') }); continue; }
      const studio = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (typeof studio === 'string' && studio) { blocks.push({ type: 'text', text: studio }); continue; }
    } catch (_e) { /* not JSON, leave to raw fallback */ }
  }
  // Gemini batchexecute array
  if (!blocks.length && b.indexOf('[[') === 0) {
    try {
      const arr = JSON.parse(b);
      const walk = function (node) {
        if (typeof node === 'string') blocks.push({ type: 'text', text: node });
        else if (Array.isArray(node)) node.forEach(walk);
      };
      walk(arr);
    } catch (_e) { /* leave to raw */ }
  }
  if (!blocks.length) blocks.push({ type: 'text', text: b });
  return blocks;
}
function detectCompletion(rawBody) {
  const b = String(rawBody);
  return b.includes('[DONE]') || b.includes('message_stop') || b.indexOf('[["e"') === 0;
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (b.includes('choices') || b.includes('candidates') || b.includes('wrb.fr')) return 0.4;
  if (b.includes('data:') || b.includes('[[')) return 0.2;
  return 0.1;
}
module.exports.default = { name: 'generic/001_format_agnostic', version: 1, providerId: 'generic', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvested/google-ai-studio.ts
var LOGIC_CODE5;
var init_google_ai_studio = __esm({
  "seeds/parsers/harvested/google-ai-studio.ts"() {
    "use strict";
    LOGIC_CODE5 = `
function parse(rawBody) {
  const blocks = [];
  const lines = String(rawBody).split('\\n');
  for (const line of lines) {
    const trimmed = line.trim();
    if (!trimmed.startsWith('data:')) continue;
    const payload = trimmed.slice(5).trim();
    if (payload === '[DONE]') break;
    try {
      const data = JSON.parse(payload);
      const text = data.candidates && data.candidates[0] && data.candidates[0].content && data.candidates[0].content.parts && data.candidates[0].content.parts[0] && data.candidates[0].content.parts[0].text;
      if (text !== undefined && text !== null) blocks.push({ type: 'text', text: String(text) });
    } catch (_e) { /* skip */ }
  }
  if (blocks.length === 0 && rawBody.length > 0) blocks.push({ type: 'text', text: rawBody });
  return blocks;
}
function detectCompletion(rawBody) {
  return String(rawBody).includes('[DONE]') || String(rawBody).includes('"finishReason"');
}
function getConfidence(rawBody) {
  const b = String(rawBody);
  if (!b.includes('data:')) return 0;
  if (b.includes('candidates') && b.includes('parts')) return 1;
  if (b.includes('[DONE]')) return 0.7;
  return 0.3;
}
module.exports.default = { name: 'gemini/002_ai_studio', version: 2, providerId: 'gemini', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvested/system-raw-text.ts
var LOGIC_CODE6;
var init_system_raw_text = __esm({
  "seeds/parsers/harvested/system-raw-text.ts"() {
    "use strict";
    LOGIC_CODE6 = `
function parse(rawBody) {
  const b = String(rawBody);
  return b.length > 0 ? [{ type: 'text', text: b }] : [];
}
function detectCompletion() { return true; }
function getConfidence() { return 0.05; }
module.exports.default = { name: 'system/001_raw_text', version: 1, providerId: 'system', parse: parse, detectCompletion: detectCompletion, getConfidence: getConfidence };
`;
  }
});

// seeds/parsers/harvest.seed.ts
var harvest_seed_exports = {};
__export(harvest_seed_exports, {
  DEFS: () => DEFS,
  seedHarvestedParsers: () => seedHarvestedParsers
});
async function seedHarvestedParsers(store) {
  const now = Date.now();
  const nameToId = /* @__PURE__ */ new Map();
  for (const def of DEFS) {
    const id = `parser:${def.providerId}:${def.name.split("/")[1] ?? def.version}`;
    await store.upsertParser({
      id,
      provider_id: def.providerId,
      parser_name: def.name,
      parser_version: def.version,
      parser_logic_type: "inline",
      parser_file_path: null,
      parser_logic_code: def.logicCode,
      parser_hash: StreamAlignmentEngine.computeParserHash(def.logicCode),
      is_active: 1,
      fallback_parser_id: null,
      created_at: now,
      updated_at: now
    });
    nameToId.set(def.name, id);
  }
  for (const def of DEFS) {
    if (def.fallback && nameToId.has(def.name) && nameToId.has(def.fallback)) {
      const fromId = nameToId.get(def.name);
      const toId = nameToId.get(def.fallback);
      if (fromId && toId) {
        await store.setParserFallback(fromId, toId);
      }
    }
  }
  return DEFS.length;
}
var DEFS;
var init_harvest_seed = __esm({
  "seeds/parsers/harvest.seed.ts"() {
    "use strict";
    init_stream_align();
    init_chatgpt_openai_delta();
    init_claude_streaming_sse();
    init_gemini_batchexecute();
    init_generic_format_agnostic();
    init_google_ai_studio();
    init_system_raw_text();
    DEFS = [
      {
        name: "claude/001_streaming_sse",
        providerId: "claude",
        version: 1,
        logicCode: LOGIC_CODE2,
        fallback: "generic/001_format_agnostic"
      },
      {
        name: "chatgpt/001_openai_delta",
        providerId: "chatgpt",
        version: 1,
        logicCode: LOGIC_CODE,
        fallback: "generic/001_format_agnostic"
      },
      {
        name: "gemini/001_batchexecute",
        providerId: "gemini",
        version: 1,
        logicCode: LOGIC_CODE3,
        fallback: "generic/001_format_agnostic"
      },
      {
        name: "gemini/002_ai_studio",
        providerId: "gemini",
        version: 2,
        logicCode: LOGIC_CODE5,
        fallback: "generic/001_format_agnostic"
      },
      {
        name: "generic/001_format_agnostic",
        providerId: "generic",
        version: 1,
        logicCode: LOGIC_CODE4,
        fallback: "system/001_raw_text"
      },
      { name: "system/001_raw_text", providerId: "system", version: 1, logicCode: LOGIC_CODE6 }
    ];
  }
});

// seeds/automation/automation.seed.ts
var automation_seed_exports = {};
__export(automation_seed_exports, {
  seedAutomation: () => seedAutomation
});
async function seedAutomation(db) {
  const now = Date.now();
  let count = 0;
  for (const role of AGENT_ROLES2) {
    const runId = `seed:agent:${role}`;
    await db.prisma.agentLoopRun.upsert({
      where: { id: runId },
      create: {
        id: runId,
        agentId: `agent:${role}`,
        goal: `Reference ${role} agent role (seeded)`,
        status: "completed",
        inputJson: JSON.stringify({ role }),
        outputJson: JSON.stringify({ kind: "agent-role" }),
        startedAt: now,
        completedAt: now
      },
      update: {
        status: "completed",
        outputJson: JSON.stringify({ kind: "agent-role" }),
        completedAt: now
      }
    });
    await db.prisma.agentStep.upsert({
      where: { id: `${runId}:0` },
      create: {
        id: `${runId}:0`,
        runId,
        stepIndex: 0,
        actionType: "register_role",
        actionJson: JSON.stringify({ role }),
        resultJson: JSON.stringify({ kind: "agent-role" }),
        success: true,
        durationMs: 0,
        createdAt: now
      },
      update: { actionType: "register_role", actionJson: JSON.stringify({ role }), success: true }
    });
    count += 2;
  }
  return count;
}
var AGENT_ROLES2;
var init_automation_seed = __esm({
  "seeds/automation/automation.seed.ts"() {
    "use strict";
    AGENT_ROLES2 = ["researcher", "extractor", "synthesizer", "monitor", "tester"];
  }
});

// src/executor/cdp-transport.ts
var cdp_transport_exports = {};
__export(cdp_transport_exports, {
  CdpTransportImpl: () => CdpTransportImpl
});
var CdpTransportImpl;
var init_cdp_transport = __esm({
  "src/executor/cdp-transport.ts"() {
    "use strict";
    init_cdp();
    CdpTransportImpl = class {
      clients = /* @__PURE__ */ new Map();
      // Governor-gated flag. The transport is only ever installed on the
      // ChromeGovernor (via setCdpTransport), so `evaluate` is inherently
      // governor-gated. `viaGovernor` documents that contract and rejects any
      // attempted uncurated use.
      viaGovernor = true;
      setViaGovernor(viaGovernor) {
        this.viaGovernor = viaGovernor;
      }
      async connect(slaveId, debugPort) {
        if (this.clients.has(slaveId)) return;
        const wsUrl = await this.resolveBrowserWsUrl(debugPort);
        const client3 = new BunCdpClient(wsUrl, {
          timeoutMs: 3e4,
          maxRetries: 3
        });
        await client3.connect();
        const sessionId = await this.resolvePageSession(client3);
        this.clients.set(slaveId, { client: client3, sessionId });
      }
      /** Resolve the exact browser websocket URL, retrying while Chrome boots. */
      async resolveBrowserWsUrl(debugPort, attempts = 20) {
        let lastErr;
        for (let i = 0; i < attempts; i++) {
          try {
            const res = await fetch(`http://127.0.0.1:${debugPort}/json/version`, {
              signal: AbortSignal.timeout(2e3)
            });
            if (res.ok) {
              const data = await res.json();
              if (data.webSocketDebuggerUrl) return data.webSocketDebuggerUrl;
            }
          } catch (err) {
            lastErr = err;
          }
          await new Promise((r) => setTimeout(r, 500));
        }
        throw new Error(`No browser websocket on :${debugPort} (last: ${String(lastErr)})`);
      }
      /** Find or create a page target and return its CDP sessionId. */
      async resolvePageSession(client3) {
        const targets = await client3.send("Target.getTargets");
        let page = targets.targetInfos?.find((t) => t.type === "page");
        if (!page) {
          const created = await client3.send("Target.createTarget", {
            url: "about:blank"
          });
          page = { targetId: created.targetId, type: "page" };
        }
        const attached = await client3.send("Target.attachToTarget", {
          targetId: page.targetId,
          flatten: true
        });
        return attached.sessionId;
      }
      async disconnect(slaveId) {
        const entry = this.clients.get(slaveId);
        if (!entry) return;
        await entry.client.disconnect();
        this.clients.delete(slaveId);
      }
      async disconnectAll() {
        for (const [slaveId] of this.clients) {
          await this.disconnect(slaveId);
        }
      }
      isConnected(slaveId) {
        return this.clients.has(slaveId);
      }
      getEntry(slaveId) {
        const entry = this.clients.get(slaveId);
        if (!entry) {
          throw new Error(`CDP client not connected for slave: ${slaveId}`);
        }
        return entry;
      }
      async send(slaveId, method, params) {
        const { client: client3, sessionId } = this.getEntry(slaveId);
        return client3.send(method, params, { sessionId });
      }
      async capture(slaveId, pattern2, timeoutMs = 3e4) {
        const { client: client3, sessionId } = this.getEntry(slaveId);
        const start = Date.now();
        await client3.send("Network.enable", {}, { sessionId });
        return new Promise((resolve4, reject) => {
          let settled = false;
          const matchingRequests = /* @__PURE__ */ new Set();
          let matchedUrl = "";
          let matchedStatus;
          let matchedHeaders;
          const finish = (body) => {
            if (settled) return;
            settled = true;
            clearTimeout(timer);
            client3.off("Network.responseReceived", responseHandler);
            client3.off("Network.loadingFinished", finishedHandler);
            client3.off("Network.loadingFailed", failedHandler);
            resolve4({
              body,
              url: matchedUrl,
              headers: matchedHeaders,
              status: matchedStatus,
              durationMs: Date.now() - start,
              capturedAt: Date.now()
            });
          };
          const timer = setTimeout(() => {
            if (settled) return;
            finish(matchingRequests.size > 0 ? "" : "");
            if (matchingRequests.size === 0) {
              reject(new Error(`Capture timeout after ${timeoutMs}ms for pattern: ${pattern2.source}`));
            }
          }, timeoutMs);
          const responseHandler = (params) => {
            const event = params;
            const url = event.response?.url ?? "";
            if (!event.requestId || !pattern2.test(url)) return;
            matchingRequests.add(event.requestId);
            matchedUrl = url;
            matchedStatus = event.response?.status;
            matchedHeaders = event.response?.headers;
          };
          const finishedHandler = async (params) => {
            const event = params;
            if (!event.requestId || !matchingRequests.has(event.requestId)) return;
            try {
              const result = await client3.send(
                "Network.getResponseBody",
                { requestId: event.requestId },
                { sessionId }
              );
              finish(result.body);
            } catch {
              finish("");
            }
          };
          const failedHandler = (params) => {
            const event = params;
            if (event.requestId && matchingRequests.has(event.requestId)) {
              finish("");
            }
          };
          client3.on("Network.responseReceived", responseHandler);
          client3.on("Network.loadingFinished", finishedHandler);
          client3.on("Network.loadingFailed", failedHandler);
        });
      }
      async captureStream(slaveId, pattern2, timeoutMs = 6e4) {
        const { client: client3, sessionId } = this.getEntry(slaveId);
        const chunks = [];
        let body = "";
        let resolved = false;
        await client3.send("Network.enable", {}, { sessionId });
        return new Promise((resolve4, reject) => {
          const timer = setTimeout(() => {
            if (!resolved) {
              cleanup();
              if (body || chunks.length > 0) {
                resolve4({ body, chunks });
              } else {
                reject(new Error(`CaptureStream timeout after ${timeoutMs}ms`));
              }
            }
          }, timeoutMs);
          const matchingRequests = /* @__PURE__ */ new Set();
          const responseHandler = (params) => {
            const event = params;
            if (event.response?.url && pattern2.test(event.response.url)) {
              const eventObj = event;
              if (eventObj.requestId) {
                matchingRequests.add(eventObj.requestId);
              }
            }
          };
          const dataHandler = (params) => {
            const event = params;
            if (event.requestId && matchingRequests.has(event.requestId)) {
            }
          };
          const loadingFinishedHandler = async (params) => {
            const event = params;
            if (event.requestId && matchingRequests.has(event.requestId)) {
              try {
                const result = await client3.send(
                  "Network.getResponseBody",
                  { requestId: event.requestId },
                  { sessionId }
                );
                body = result.body;
                resolved = true;
                cleanup();
                resolve4({ body, chunks });
              } catch {
                cleanup();
                resolve4({ body: chunks.join(""), chunks });
              }
            }
          };
          const loadingFailedHandler = (params) => {
            const event = params;
            if (event.requestId && matchingRequests.has(event.requestId)) {
              cleanup();
              reject(new Error("Network request failed"));
            }
          };
          function cleanup() {
            clearTimeout(timer);
            client3.off("Network.responseReceived", responseHandler);
            client3.off("Network.dataReceived", dataHandler);
            client3.off("Network.loadingFinished", loadingFinishedHandler);
            client3.off("Network.loadingFailed", loadingFailedHandler);
          }
          client3.on("Network.responseReceived", responseHandler);
          client3.on("Network.dataReceived", dataHandler);
          client3.on("Network.loadingFinished", loadingFinishedHandler);
          client3.on("Network.loadingFailed", loadingFailedHandler);
        });
      }
      /**
       * Governor-gated evaluate. The transport is only ever installed on the
       * ChromeGovernor, so every Runtime.evaluate funnels through this single method.
       */
      async evaluate(slaveId, expression, opts) {
        if (!this.viaGovernor) {
          throw new Error("cdp-transport.evaluate is governor-gated; call setViaGovernor(true) first");
        }
        const result = await this.send(slaveId, "Runtime.evaluate", {
          expression,
          returnByValue: opts?.returnByValue ?? true,
          awaitPromise: opts?.awaitPromise ?? false
        });
        return result?.result?.value;
      }
      async getPageState(slaveId) {
        const value = await this.evaluate(
          slaveId,
          "JSON.stringify({url: location.href, title: document.title, readyState: document.readyState})"
        );
        try {
          const state = JSON.parse(value ?? "{}");
          return {
            url: state.url ?? "",
            title: state.title ?? "",
            readyState: state.readyState ?? ""
          };
        } catch {
          return { url: "", title: "", readyState: "" };
        }
      }
      async captureScreenshot(slaveId, format = "png") {
        const result = await this.send(slaveId, "Page.captureScreenshot", { format });
        return result?.data ?? "";
      }
    };
  }
});

// src/engines/knowledge-ingestion.ts
var knowledge_ingestion_exports = {};
__export(knowledge_ingestion_exports, {
  KnowledgeIngestionEngine: () => KnowledgeIngestionEngine
});
import { existsSync as existsSync5, readFileSync as readFileSync3 } from "fs";
var KnowledgeIngestionEngine;
var init_knowledge_ingestion = __esm({
  "src/engines/knowledge-ingestion.ts"() {
    "use strict";
    init_errors();
    init_ids();
    KnowledgeIngestionEngine = class {
      constructor(store, conversationStore, blockStore, extractor, eventBus) {
        this.store = store;
        this.conversationStore = conversationStore;
        this.blockStore = blockStore;
        this.extractor = extractor;
        this.eventBus = eventBus;
      }
      store;
      conversationStore;
      blockStore;
      extractor;
      eventBus;
      async ingest(config2) {
        const start = Date.now();
        const jobId = newId();
        await this.store.createImportJob({
          id: jobId,
          source: config2.source,
          filePath: config2.filePath,
          status: "pending",
          configJson: JSON.stringify(config2),
          startedAt: start
        });
        let conversationsImported = 0;
        let messagesImported = 0;
        let duplicatesSkipped = 0;
        const errors = [];
        let parsed;
        try {
          parsed = this.parseFile(config2.filePath, config2.source);
        } catch (err) {
          await this.store.updateImportJob(jobId, {
            status: "failed",
            error: String(err),
            completedAt: Date.now()
          });
          throw err;
        }
        await this.store.updateImportJob(jobId, { status: "importing" });
        let entitiesExtracted = 0;
        let decisionsExtracted = 0;
        const importedConversationIds = [];
        for (const conv of parsed) {
          try {
            const existingId = await this.store.findExistingConversation(config2.source, conv.externalId);
            let conversationId;
            if (existingId && config2.deduplicate) {
              duplicatesSkipped++;
              continue;
            }
            if (existingId) {
              conversationId = existingId;
            } else {
              const created = await this.conversationStore.createConversation({
                providerSessionId: conv.externalId,
                providerId: config2.providerId ?? config2.source,
                title: conv.title ?? null,
                state: "imported"
              });
              conversationId = created.id;
            }
            for (const msg of conv.messages) {
              await this.conversationStore.createMessage({
                conversationId,
                role: msg.role,
                content: msg.content,
                blocksJson: JSON.stringify([]),
                blockCount: 0,
                sequenceIndex: msg.index
              });
              messagesImported++;
            }
            conversationsImported++;
            importedConversationIds.push({ externalId: conv.externalId, conversationId });
          } catch (err) {
            errors.push({ conversationId: conv.externalId, error: String(err) });
          }
        }
        if (config2.extractEntities || config2.extractDecisions) {
          const conversationMessages = [];
          for (const { conversationId } of importedConversationIds) {
            try {
              const msgs = await this.conversationStore.getMessages(conversationId, { limit: 1e3 });
              conversationMessages.push({
                id: conversationId,
                messages: msgs.map((m) => ({ id: m.id, role: m.role, content: m.content ?? "" }))
              });
            } catch (err) {
              errors.push({ conversationId, error: `extract-prep: ${String(err)}` });
            }
          }
          try {
            const extractResult = await this.extractor.batchExtract(conversationMessages);
            entitiesExtracted = extractResult.totalExtracted;
            decisionsExtracted = extractResult.byType?.decision ?? 0;
          } catch (err) {
            errors.push({ conversationId: "(extraction)", error: String(err) });
          }
        }
        const end = Date.now();
        await this.store.updateImportJob(jobId, {
          status: "complete",
          resultJson: JSON.stringify({
            conversationsImported,
            messagesImported,
            entitiesExtracted,
            decisionsExtracted,
            duplicatesSkipped,
            errors
          }),
          completedAt: end
        });
        this.eventBus.emit({
          type: "knowledge:imported",
          jobId,
          source: config2.source,
          conversationsImported,
          messagesImported,
          durationMs: end - start
        });
        return {
          jobId,
          source: config2.source,
          conversationsImported,
          messagesImported,
          entitiesExtracted,
          decisionsExtracted,
          duplicatesSkipped,
          errors,
          durationMs: end - start
        };
      }
      async ingestFile(filePath, source) {
        return this.ingest({
          source,
          filePath,
          deduplicate: true,
          extractEntities: false,
          extractDecisions: false,
          generateEmbeddings: false
        });
      }
      async getJobStatus(jobId) {
        const job = await this.store.getImportJob(jobId);
        if (!job) return null;
        return { jobId: job.id, phase: job.status, current: 0, total: 0 };
      }
      async listJobs(opts) {
        const jobs = await this.store.listImportJobs(opts);
        return jobs.map((j) => ({
          id: j.id,
          source: j.source,
          status: j.status,
          startedAt: j.startedAt
        }));
      }
      async cancelJob(jobId) {
        await this.store.updateImportJob(jobId, { status: "cancelled" });
      }
      parseFile(filePath, source) {
        if (!existsSync5(filePath)) {
          throw new CapStoreError("FileNotFoundError", `File not found: ${filePath}`);
        }
        const raw = readFileSync3(filePath, "utf-8");
        let json3;
        try {
          json3 = JSON.parse(raw);
        } catch {
          throw new CapStoreError("ParseError", `Invalid JSON in ${filePath}`);
        }
        return this.normalizeParsed(source, json3);
      }
      normalizeParsed(source, data) {
        if (source === "chatgpt") {
          const arr = data;
          return arr.map((conv) => ({
            externalId: String(conv.id ?? conv.conversation_id ?? ""),
            title: conv.title,
            messages: (conv.messages ?? conv.message_list ?? []).map((m, i) => ({
              role: String(m.role ?? m.author ?? "user"),
              content: String(m.content ?? m.text ?? ""),
              index: i
            }))
          }));
        }
        if (source === "claude") {
          const root = data;
          const conversations = root.conversations ?? root.accounts ?? [];
          return conversations.map((conv) => ({
            externalId: String(conv.uuid ?? conv.id ?? ""),
            title: conv.name ?? conv.title,
            messages: (conv.chat_messages ?? conv.messages ?? []).map((m, i) => ({
              role: String(m.sender ?? m.role ?? "user"),
              content: String(m.text ?? m.content ?? m.message ?? ""),
              index: i
            }))
          }));
        }
        if (source === "gemini") {
          const arr = Array.isArray(data) ? data : [];
          return arr.map((conv) => ({
            externalId: String(conv.conversationId ?? conv.id ?? ""),
            title: conv.title,
            messages: (conv.messages ?? []).map(
              (m, i) => ({
                role: String(m.author ?? m.role ?? "user"),
                content: String(m.content ?? m.text ?? ""),
                index: i
              })
            )
          }));
        }
        if (source === "generic" || source === "manual") {
          const arr = Array.isArray(data) ? data : [data];
          return arr.map((conv) => ({
            externalId: String(conv.id ?? conv.externalId ?? ""),
            title: conv.title,
            messages: (conv.messages ?? []).map(
              (m, i) => ({
                role: String(m.role ?? "user"),
                content: String(m.content ?? ""),
                index: i
              })
            )
          }));
        }
        throw new CapStoreError("ParseError", `Unsupported import source: ${source}`);
      }
    };
  }
});

// src/storage/impl/knowledge-ingestion-store-impl.ts
var knowledge_ingestion_store_impl_exports = {};
__export(knowledge_ingestion_store_impl_exports, {
  KnowledgeIngestionStoreImpl: () => KnowledgeIngestionStoreImpl
});
var KnowledgeIngestionStoreImpl;
var init_knowledge_ingestion_store_impl = __esm({
  "src/storage/impl/knowledge-ingestion-store-impl.ts"() {
    "use strict";
    KnowledgeIngestionStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async createImportJob(job) {
        await this.db.prisma.importJob.create({
          data: {
            id: job.id,
            source: job.source,
            filePath: job.filePath,
            status: job.status,
            configJson: job.configJson,
            startedAt: job.startedAt
          }
        });
      }
      async updateImportJob(id, patch) {
        const data = {};
        if (patch.status !== void 0) data.status = patch.status;
        if (patch.resultJson !== void 0) data.resultJson = patch.resultJson;
        if (patch.completedAt !== void 0) data.completedAt = patch.completedAt;
        if (patch.error !== void 0) data.error = patch.error;
        await this.db.prisma.importJob.update({ where: { id }, data });
      }
      async getImportJob(id) {
        const r = await this.db.prisma.importJob.findUnique({ where: { id } });
        if (!r) return null;
        return {
          id: r.id,
          source: r.source,
          filePath: r.filePath,
          status: r.status,
          configJson: r.configJson,
          resultJson: r.resultJson,
          startedAt: Number(r.startedAt),
          completedAt: r.completedAt == null ? null : Number(r.completedAt)
        };
      }
      async listImportJobs(opts) {
        const rows = await this.db.prisma.importJob.findMany({
          orderBy: { startedAt: "desc" },
          take: opts?.limit ?? 50
        });
        return rows.map((r) => ({
          id: r.id,
          source: r.source,
          status: r.status,
          startedAt: Number(r.startedAt),
          completedAt: r.completedAt == null ? null : Number(r.completedAt)
        }));
      }
      async findExistingConversation(sourceProviderId, externalId) {
        const row = await this.db.prisma.conversation.findFirst({
          where: { source: sourceProviderId, externalId },
          select: { id: true }
        });
        return row?.id ?? null;
      }
    };
  }
});

// src/engines/knowledge-extractor.ts
var knowledge_extractor_exports = {};
__export(knowledge_extractor_exports, {
  KnowledgeExtractor: () => KnowledgeExtractor
});
var ENTITY_PATTERNS, DECISION_PATTERNS, FACT_PATTERNS, KnowledgeExtractor;
var init_knowledge_extractor = __esm({
  "src/engines/knowledge-extractor.ts"() {
    "use strict";
    init_ids();
    ENTITY_PATTERNS = {
      technology: /\b(React|Vue|Angular|TypeScript|Python|Rust|Go|Node\.js|PostgreSQL|MongoDB|Redis|Docker|Kubernetes|AWS|GCP|Azure)\b/gi,
      person: /\b(?:@|by |from )([A-Z][a-z]+ [A-Z][a-z]+)\b/g,
      project: /\b(?:project|app|system) ["']?([A-Z][a-zA-Z0-9_-]+)["']?\b/g
    };
    DECISION_PATTERNS = [
      /\b(?:I|we) (?:decided|chose|will|should|need to) (.+)/gi,
      /\b(?:let's|lets) (?:go with|use|try) (.+)/gi,
      /\b(?:the|our) (?:decision|choice|approach) (?:is|will be) (.+)/gi
    ];
    FACT_PATTERNS = [/\b(.+?) (?:is|are|was|were) (.+?)[.]/g];
    KnowledgeExtractor = class {
      constructor(store, config2) {
        this.store = store;
        this.config = config2;
      }
      store;
      config;
      async extractFromMessage(conversationId, messageId, role, content, context) {
        const results = [];
        const combinedContext = context || content.slice(0, 200);
        if (this.config.enableEntityExtraction) {
          for (const [type, pattern2] of Object.entries(ENTITY_PATTERNS)) {
            pattern2.lastIndex = 0;
            let match = pattern2.exec(content);
            while (match !== null) {
              const name = match[1] ?? match[0];
              const normalizedType = type === "technology" ? "entity_technology" : type === "person" ? "entity_person" : type === "project" ? "entity_project" : "entity_concept";
              const confidence = normalizedType === "entity_technology" ? 0.9 : 0.7;
              const existing = await this.store.findEntityByName(name, normalizedType);
              if (existing) {
                await this.store.updateEntity(existing.id, {
                  confidence: Math.min(1, confidence + 0.1),
                  lastSeenAt: Date.now()
                });
              } else {
                const entityId = newId();
                await this.store.createEntity({
                  id: entityId,
                  name,
                  type: normalizedType,
                  description: null,
                  confidence,
                  firstSeenAt: Date.now(),
                  lastSeenAt: Date.now()
                });
              }
              results.push({
                type: normalizedType,
                subject: name,
                predicate: "mentioned_in",
                object: { match: match[0] },
                confidence,
                sourceConversationId: conversationId,
                sourceMessageId: messageId,
                context: combinedContext
              });
              match = pattern2.exec(content);
            }
          }
        }
        if (this.config.enableDecisionExtraction) {
          for (const pattern2 of DECISION_PATTERNS) {
            pattern2.lastIndex = 0;
            let match = pattern2.exec(content);
            while (match !== null) {
              const decisionText = match[1]?.trim();
              if (!decisionText) {
                match = pattern2.exec(content);
                continue;
              }
              const decisionId = newId();
              await this.store.createDecision({
                id: decisionId,
                conversationId,
                messageId,
                decisionText,
                rationale: null,
                alternatives: "",
                confidence: 0.8,
                ts: Date.now()
              });
              results.push({
                type: "decision",
                subject: role === "user" ? "user" : "assistant",
                predicate: "decided",
                object: decisionText,
                confidence: 0.8,
                sourceConversationId: conversationId,
                sourceMessageId: messageId,
                context: combinedContext
              });
              match = pattern2.exec(content);
            }
          }
        }
        for (const pattern2 of FACT_PATTERNS) {
          pattern2.lastIndex = 0;
          let match = pattern2.exec(content);
          while (match !== null) {
            const subj = match[1]?.trim();
            const obj = match[2]?.trim();
            if (!subj || !obj) {
              match = pattern2.exec(content);
              continue;
            }
            if (subj.length > 100) {
              match = pattern2.exec(content);
              continue;
            }
            results.push({
              type: "fact",
              subject: subj,
              predicate: "is",
              object: obj,
              confidence: 0.5,
              sourceConversationId: conversationId,
              sourceMessageId: messageId,
              context: combinedContext
            });
            match = pattern2.exec(content);
          }
        }
        return results.filter((r) => r.confidence >= this.config.confidenceThreshold);
      }
      /**
       * Continuous-mode extraction (Unit 33.2). Operates on a single message/segment
       * (a "chunk") rather than a whole conversation. It reuses the exact same
       * extraction logic as batch mode (no prompt divergence) and returns the
       * relation edges — entities / decisions / facts — extracted from that chunk.
       */
      async extractIncremental(chunk) {
        return this.extractFromMessage(
          chunk.conversationId,
          chunk.messageId,
          chunk.role,
          chunk.content,
          chunk.context ?? ""
        );
      }
      async extractFromConversation(conversationId, messages) {
        const all = [];
        for (const msg of messages) {
          const ctx = messages.find((m) => m.id === msg.id);
          const results = await this.extractFromMessage(
            conversationId,
            msg.id,
            msg.role,
            msg.content,
            ctx?.content ?? ""
          );
          all.push(...results);
        }
        return all;
      }
      async batchExtract(conversations) {
        const byType = {};
        let total = 0;
        for (const conv of conversations) {
          const results = await this.extractFromConversation(conv.id, conv.messages);
          for (const r of results) {
            byType[r.type] = (byType[r.type] ?? 0) + 1;
            total++;
          }
        }
        return { totalExtracted: total, byType };
      }
    };
  }
});

// src/storage/impl/knowledge-extractor-store-impl.ts
var knowledge_extractor_store_impl_exports = {};
__export(knowledge_extractor_store_impl_exports, {
  KnowledgeExtractorStoreImpl: () => KnowledgeExtractorStoreImpl
});
var KnowledgeExtractorStoreImpl;
var init_knowledge_extractor_store_impl = __esm({
  "src/storage/impl/knowledge-extractor-store-impl.ts"() {
    "use strict";
    KnowledgeExtractorStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async createEntity(input) {
        await this.db.prisma.entity.create({
          data: {
            id: input.id,
            name: input.name,
            type: input.type,
            description: input.description,
            confidence: input.confidence,
            mentionCount: 0,
            firstSeenAt: input.firstSeenAt,
            lastSeenAt: input.lastSeenAt,
            createdAt: Date.now(),
            updatedAt: Date.now()
          }
        });
      }
      async updateEntity(id, patch) {
        const data = {};
        if (patch.confidence !== void 0) data.confidence = patch.confidence;
        if (patch.lastSeenAt !== void 0) data.lastSeenAt = patch.lastSeenAt;
        data.updatedAt = Date.now();
        await this.db.prisma.entity.update({ where: { id }, data });
      }
      async findEntityByName(name, type) {
        const r = await this.db.prisma.entity.findFirst({
          where: { name, type },
          select: { id: true, name: true, type: true }
        });
        if (!r) return null;
        return { id: r.id, name: r.name, type: r.type };
      }
      async createEntityMention(input) {
        await this.db.prisma.entityMention.create({
          data: {
            id: input.id,
            entityId: input.entityId,
            conversationId: input.conversationId,
            messageId: input.messageId,
            context: input.context,
            confidence: input.confidence,
            ts: input.ts
          }
        });
      }
      async createDecision(input) {
        await this.db.prisma.decisionRecord.create({
          data: {
            id: input.id,
            conversationId: input.conversationId,
            messageId: input.messageId,
            decisionText: input.decisionText,
            rationale: input.rationale,
            alternativesJson: input.alternatives,
            confidence: input.confidence,
            ts: input.ts
          }
        });
      }
      async createPattern(input) {
        await this.db.prisma.patternExtract.create({
          data: {
            id: input.id,
            name: input.name,
            description: input.description,
            patternType: input.patternType,
            occurrences: input.occurrences,
            confidence: input.confidence,
            firstSeenAt: input.firstSeenAt,
            lastSeenAt: input.lastSeenAt,
            createdAt: Date.now(),
            updatedAt: Date.now()
          }
        });
      }
      async updatePattern(id, patch) {
        const data = {};
        if (patch.occurrences !== void 0) data.occurrences = patch.occurrences;
        if (patch.confidence !== void 0) data.confidence = patch.confidence;
        if (patch.lastSeenAt !== void 0) data.lastSeenAt = patch.lastSeenAt;
        data.updatedAt = Date.now();
        await this.db.prisma.patternExtract.update({ where: { id }, data });
      }
      async findPattern(name) {
        const r = await this.db.prisma.patternExtract.findFirst({
          where: { name },
          select: { id: true, name: true }
        });
        return r ? { id: r.id, name: r.name } : null;
      }
      async assertSemanticMemory(input) {
        await this.db.prisma.semanticMemory.create({
          data: {
            id: input.id,
            subject: input.subject,
            predicate: input.predicate,
            objectJson: input.objectJson,
            confidence: input.confidence,
            source: input.source,
            timestamp: input.timestamp,
            expiresAt: input.expiresAt,
            createdAt: Date.now()
          }
        });
      }
    };
  }
});

// src/engines/semantic-search.ts
var semantic_search_exports = {};
__export(semantic_search_exports, {
  SemanticSearchEngine: () => SemanticSearchEngine
});
import { createHash as createHash2 } from "crypto";
var SemanticSearchEngine;
var init_semantic_search = __esm({
  "src/engines/semantic-search.ts"() {
    "use strict";
    init_ids();
    SemanticSearchEngine = class {
      constructor(store, embeddingProvider) {
        this.store = store;
        this.embeddingProvider = embeddingProvider;
      }
      store;
      embeddingProvider;
      async index(text, entityType, entityId) {
        const embedding = await this.embeddingProvider.embed(text);
        const contentHash = createHash2("sha256").update(text).digest("hex");
        await this.store.upsertEmbedding({
          id: newId(),
          entityType,
          entityId,
          embedding: JSON.stringify(embedding),
          model: this.embeddingProvider.name,
          dimensions: this.embeddingProvider.dimensions,
          contentHash,
          createdAt: Date.now()
        });
      }
      async indexBatch(items) {
        const texts = items.map((i) => i.text);
        const embeddings = await this.embeddingProvider.embedBatch(texts);
        const contentHashes = texts.map((t) => createHash2("sha256").update(t).digest("hex"));
        for (let i = 0; i < items.length; i++) {
          const item = items[i];
          const embedding = embeddings[i];
          const contentHash = contentHashes[i];
          if (!item || !embedding || !contentHash) continue;
          await this.store.upsertEmbedding({
            id: newId(),
            entityType: item.entityType,
            entityId: item.entityId,
            embedding: JSON.stringify(embedding),
            model: this.embeddingProvider.name,
            dimensions: this.embeddingProvider.dimensions,
            contentHash,
            createdAt: Date.now()
          });
        }
      }
      async search(query) {
        const queryEmbedding = await this.embeddingProvider.embed(query.text);
        const limit = query.limit ?? 10;
        const threshold = query.threshold ?? 0;
        const results = await this.store.searchByEmbedding(queryEmbedding, {
          limit,
          threshold
        });
        return results.map((r) => ({
          type: r.entityType,
          id: r.entityId,
          score: r.score,
          snippet: "",
          conversationId: null
        }));
      }
      async searchHybrid(query) {
        return this.search(query);
      }
      async reindexAll() {
        return { indexed: 0, skipped: 0, errors: 0 };
      }
      async getStats() {
        const total = await this.store.countEmbeddings();
        return { totalEmbeddings: total };
      }
    };
  }
});

// src/storage/impl/semantic-search-store-impl.ts
var semantic_search_store_impl_exports = {};
__export(semantic_search_store_impl_exports, {
  SemanticSearchStoreImpl: () => SemanticSearchStoreImpl
});
function cosineSimilarity2(a, b) {
  let dot = 0;
  let magA = 0;
  let magB = 0;
  for (let i = 0; i < a.length; i++) {
    const av = a[i];
    const bv = b[i];
    if (av !== void 0 && bv !== void 0) {
      dot += av * bv;
      magA += av ** 2;
      magB += bv ** 2;
    }
  }
  return dot / (Math.sqrt(magA) * Math.sqrt(magB));
}
var SemanticSearchStoreImpl;
var init_semantic_search_store_impl = __esm({
  "src/storage/impl/semantic-search-store-impl.ts"() {
    "use strict";
    SemanticSearchStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async upsertEmbedding(input) {
        await this.db.prisma.memoryEmbedding.upsert({
          where: { entityType_entityId: { entityType: input.entityType, entityId: input.entityId } },
          create: {
            id: input.id,
            entityType: input.entityType,
            entityId: input.entityId,
            embedding: input.embedding,
            model: input.model,
            dimensions: input.dimensions,
            contentHash: input.contentHash,
            createdAt: input.createdAt
          },
          update: {
            embedding: input.embedding,
            model: input.model,
            dimensions: input.dimensions,
            contentHash: input.contentHash,
            createdAt: input.createdAt
          }
        });
      }
      async getEmbedding(entityType, entityId) {
        const r = await this.db.prisma.memoryEmbedding.findUnique({
          where: { entityType_entityId: { entityType, entityId } }
        });
        if (!r) return null;
        return { id: r.id, embedding: r.embedding, model: r.model, dimensions: r.dimensions };
      }
      async searchByEmbedding(queryEmbedding, opts) {
        const where = {};
        if (opts.entityType !== void 0) where.entityType = opts.entityType;
        const rows = await this.db.prisma.memoryEmbedding.findMany({ where });
        const threshold = opts.threshold ?? 0;
        const limit = opts.limit ?? 10;
        const scored = [];
        for (const row of rows) {
          const storedEmbedding = JSON.parse(row.embedding);
          const score = cosineSimilarity2(queryEmbedding, storedEmbedding);
          if (score >= threshold) {
            scored.push({ entityId: row.entityId, entityType: row.entityType, score });
          }
        }
        scored.sort((a, b) => b.score - a.score);
        return scored.slice(0, limit);
      }
      async deleteEmbedding(entityType, entityId) {
        await this.db.prisma.memoryEmbedding.delete({
          where: { entityType_entityId: { entityType, entityId } }
        });
      }
      async countEmbeddings(opts) {
        const where = {};
        if (opts?.entityType !== void 0) where.entityType = opts.entityType;
        const rows = await this.db.prisma.memoryEmbedding.findMany({ where });
        return rows.length;
      }
    };
  }
});

// src/engines/cross-conversation-synthesis.ts
var cross_conversation_synthesis_exports = {};
__export(cross_conversation_synthesis_exports, {
  CrossConversationSynthesizer: () => CrossConversationSynthesizer
});
function parseWindow(window) {
  const match = window.match(/^(\d+)([dhms]?)$/);
  if (!match) return 7 * 24 * 60 * 60 * 1e3;
  const value = Number(match[1]);
  const unit = match[2] ?? "d";
  switch (unit) {
    case "d":
      return value * 24 * 60 * 60 * 1e3;
    case "h":
      return value * 60 * 60 * 1e3;
    case "m":
      return value * 60 * 1e3;
    case "s":
      return value * 1e3;
    default:
      return value * 24 * 60 * 60 * 1e3;
  }
}
var CrossConversationSynthesizer;
var init_cross_conversation_synthesis = __esm({
  "src/engines/cross-conversation-synthesis.ts"() {
    "use strict";
    CrossConversationSynthesizer = class {
      constructor(store, searchEngine, llmProvider) {
        this.store = store;
        this.searchEngine = searchEngine;
        this.llmProvider = llmProvider;
      }
      store;
      searchEngine;
      llmProvider;
      async synthesize(query) {
        const searchResults = await this.searchEngine.search({
          text: query.question,
          limit: query.maxSources * 2,
          threshold: 0.3
        });
        const sources = [];
        const contextParts = [];
        for (const sr of searchResults.slice(0, query.maxSources)) {
          sources.push({
            conversationId: sr.conversationId ?? "",
            messageId: sr.id,
            snippet: "",
            relevance: sr.score
          });
          const facts = await this.store.getFactsForConversation(sr.conversationId ?? "");
          const decisions = await this.store.getDecisionsForConversation(sr.conversationId ?? "");
          const entities = await this.store.getEntitiesForConversation(sr.conversationId ?? "");
          if (facts.length > 0) {
            contextParts.push(
              `Facts: ${facts.map((f) => `${f.subject} ${f.predicate} ${f.object}`).join(", ")}`
            );
          }
          if (decisions.length > 0) {
            contextParts.push(`Decisions: ${decisions.map((d) => d.decisionText).join(", ")}`);
          }
          if (entities.length > 0) {
            contextParts.push(`Entities: ${entities.map((e) => `${e.name} (${e.type})`).join(", ")}`);
          }
        }
        const contextBlock = contextParts.length > 0 ? `
Relevant context:
${contextParts.join("\n")}` : "";
        const prompt = `Question: ${query.question}

Based on the following information from my conversation history, provide a synthesized answer.${contextBlock}

Synthesized answer:`;
        let answer;
        let confidence;
        try {
          const result = await this.llmProvider.synthesize(prompt, query.synthesisStyle);
          answer = result.text;
          confidence = result.confidence;
        } catch {
          answer = "Unable to synthesize answer from available context.";
          confidence = 0;
        }
        const gaps = [];
        if (sources.length === 0) {
          gaps.push("No relevant conversations found for this question.");
        }
        if (confidence < 0.3) {
          gaps.push("Low confidence in synthesized answer \u2014 limited relevant context.");
        }
        return { answer, sources, confidence, gaps };
      }
      async quickAnswer(question) {
        return this.synthesize({
          question,
          scope: {},
          maxSources: 1,
          synthesisStyle: "summary"
        });
      }
      // Phase 28.4: Distill knowledge over a time window
      async distill(window) {
        const ms = parseWindow(window);
        const now = Date.now();
        const since = now - ms;
        const result = await this.synthesize({
          question: "Summarize the work and insights from this period",
          scope: { dateFrom: since, dateTo: now },
          maxSources: 10,
          synthesisStyle: "summary"
        });
        const sources = result.sources.map((s) => s.conversationId).filter(Boolean);
        const topics = result.sources.map((s) => s.snippet.split(" ").slice(0, 5).join(" ")).filter(Boolean);
        return {
          summary: result.answer,
          sources,
          topics
        };
      }
    };
  }
});

// src/storage/impl/cross-conversation-synth-store-impl.ts
var cross_conversation_synth_store_impl_exports = {};
__export(cross_conversation_synth_store_impl_exports, {
  CrossConversationSynthesizerStoreImpl: () => CrossConversationSynthesizerStoreImpl
});
var CrossConversationSynthesizerStoreImpl;
var init_cross_conversation_synth_store_impl = __esm({
  "src/storage/impl/cross-conversation-synth-store-impl.ts"() {
    "use strict";
    CrossConversationSynthesizerStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async getFactsForConversation(conversationId) {
        const rows = await this.db.prisma.semanticMemory.findMany({
          where: { source: conversationId },
          orderBy: { timestamp: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          subject: r.subject,
          predicate: r.predicate,
          object: r.objectJson,
          confidence: r.confidence
        }));
      }
      async getDecisionsForConversation(conversationId) {
        const rows = await this.db.prisma.decisionRecord.findMany({
          where: { conversationId },
          orderBy: { ts: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          decisionText: r.decisionText,
          rationale: r.rationale,
          confidence: r.confidence
        }));
      }
      async getEntitiesForConversation(conversationId) {
        const rows = await this.db.prisma.entityMention.findMany({
          where: { conversationId },
          orderBy: { ts: "desc" }
        });
        return rows.map((r) => {
          const entity = r.entity;
          return {
            id: entity?.id ?? r.entityId,
            name: entity?.name ?? "unknown",
            type: entity?.entityType ?? "unknown",
            confidence: entity?.confidence ?? 0
          };
        });
      }
    };
  }
});

// src/engines/export.ts
var export_exports = {};
__export(export_exports, {
  ExportEngine: () => ExportEngine
});
import { mkdirSync, readFileSync as readFileSync4, writeFileSync as writeFileSync2 } from "fs";
import { dirname } from "path";
var ExportEngine;
var init_export = __esm({
  "src/engines/export.ts"() {
    "use strict";
    init_errors();
    ExportEngine = class {
      constructor(store, encryption) {
        this.store = store;
        this.encryption = encryption;
      }
      store;
      encryption;
      async export(opts) {
        const manifest = {
          version: "1.0",
          exportedAt: Date.now(),
          scope: opts.scope,
          format: opts.format,
          encrypted: Boolean(opts.encryptWithPassphrase),
          recordCounts: {}
        };
        const data = {};
        if (opts.scope === "full" || opts.scope === "conversations") {
          const convos = await this.store.listConversations({
            dateFrom: opts.dateFrom,
            dateTo: opts.dateTo
          });
          const allMessages = {};
          for (const c of convos) {
            allMessages[c.id] = await this.store.listMessages(c.id);
          }
          data.conversations = convos;
          data.messages = allMessages;
          manifest.recordCounts.conversations = convos.length;
        }
        if (opts.scope === "full" || opts.scope === "memory") {
          const memory = await this.store.listMemory();
          data.memory = memory;
          manifest.recordCounts.memory = memory.length;
        }
        if (opts.scope === "full" || opts.scope === "providers") {
          const providers = await this.store.listProviders();
          data.providers = providers;
          manifest.recordCounts.providers = providers.length;
        }
        if (opts.scope === "full" || opts.scope === "config") {
          const config2 = await this.store.listConfig();
          data.config = config2;
          manifest.recordCounts.config = config2.length;
        }
        let output = opts.format === "json" ? this.toJson(data, manifest) : this.toCsv(data, manifest);
        if (opts.encryptWithPassphrase) {
          if (!this.encryption) throw new EngineError("EncryptionEngine required for encrypted export");
          this.encryption.lock();
          await this.encryption.unlock(opts.encryptWithPassphrase);
          const encrypted = this.encryption.encrypt(output);
          output = JSON.stringify({ manifest, encrypted });
          this.encryption.lock();
        }
        mkdirSync(dirname(opts.outputPath), { recursive: true });
        writeFileSync2(opts.outputPath, output, "utf8");
        return manifest;
      }
      async importJson(jsonPath) {
        const raw = readFileSync4(jsonPath, "utf8");
        let data;
        try {
          const parsed = JSON.parse(raw);
          if (parsed.encrypted && this.encryption) {
            this.encryption.lock();
            const decrypted = this.encryption.decrypt(parsed.encrypted);
            data = JSON.parse(decrypted);
          } else {
            data = parsed;
          }
        } catch {
          throw new EngineError(`Failed to parse import file: ${jsonPath}`);
        }
        const imported = {};
        if (data.conversations && Array.isArray(data.conversations)) {
          imported.conversations = data.conversations.length;
        }
        if (data.messages && typeof data.messages === "object") {
          const msgs = data.messages;
          imported.messages = Object.values(msgs).reduce((sum, arr) => sum + arr.length, 0);
        }
        if (data.memory && Array.isArray(data.memory)) {
          imported.memory = data.memory.length;
        }
        if (data.providers && Array.isArray(data.providers)) {
          imported.providers = data.providers.length;
        }
        if (data.config && Array.isArray(data.config)) {
          imported.config = data.config.length;
        }
        return { imported };
      }
      toJson(data, manifest) {
        return JSON.stringify({ manifest, ...data }, null, 2);
      }
      toCsv(data, _manifest) {
        const rows = [];
        if (data.conversations && Array.isArray(data.conversations)) {
          rows.push("type,id,state,title");
          for (const c of data.conversations) {
            const rec = c;
            rows.push(`conversation,${rec.id},${rec.state},${String(rec.title ?? "")}`);
          }
        }
        if (data.memory && Array.isArray(data.memory)) {
          rows.push("type,id,key,value,namespace");
          for (const m of data.memory) {
            const rec = m;
            rows.push(`memory,${rec.id},${rec.key},${rec.value},${rec.namespace}`);
          }
        }
        if (data.providers && Array.isArray(data.providers)) {
          rows.push("type,id,slug,displayName");
          for (const p of data.providers) {
            const rec = p;
            rows.push(`provider,${rec.id},${rec.slug},${rec.displayName}`);
          }
        }
        return rows.join("\n");
      }
    };
  }
});

// src/engines/cost-optimizer.ts
var cost_optimizer_exports = {};
__export(cost_optimizer_exports, {
  CostOptimizer: () => CostOptimizer
});
var CostOptimizer;
var init_cost_optimizer = __esm({
  "src/engines/cost-optimizer.ts"() {
    "use strict";
    CostOptimizer = class {
      constructor(store) {
        this.store = store;
      }
      store;
      async recordCost(providerId, costCents, tokensInput, tokensOutput, model) {
        const { newId: newId2 } = await Promise.resolve().then(() => (init_ids(), ids_exports));
        await this.store.createCostLog({
          id: newId2(),
          providerId,
          costCents,
          tokensInput,
          tokensOutput,
          model: model ?? null,
          ts: Date.now()
        });
      }
      async getCostReport(providerId, from, to) {
        const logs = await this.store.getCostLogs(providerId, from, to);
        let totalCostCents = 0;
        let totalTokensInput = 0;
        let totalTokensOutput = 0;
        const byDay = {};
        for (const log of logs) {
          totalCostCents += log.costCents;
          totalTokensInput += log.tokensInput;
          totalTokensOutput += log.tokensOutput;
          const day = new Date(log.ts).toISOString().slice(0, 10);
          if (!byDay[day]) {
            byDay[day] = { costCents: 0, requests: 0 };
          }
          byDay[day].costCents += log.costCents;
          byDay[day].requests += 1;
        }
        return {
          providerId,
          totalCostCents,
          totalTokensInput,
          totalTokensOutput,
          requestCount: logs.length,
          avgCostPerRequest: logs.length > 0 ? Math.round(totalCostCents / logs.length) : 0,
          byDay
        };
      }
      async estimateCost(_providerId, messageLength) {
        const estimatedTokens = Math.ceil(messageLength / 4);
        const estimatedCostCents = Math.max(1, Math.ceil(estimatedTokens / 1e3 * 1));
        return estimatedCostCents;
      }
      async getCheapestProvider(_capabilityId) {
        const now = Date.now();
        const thirtyDaysAgo = now - 30 * 24 * 60 * 60 * 1e3;
        const getAllLogs = this.store.getAllCostLogs;
        if (!getAllLogs) return null;
        const logs = await getAllLogs(thirtyDaysAgo, now);
        if (logs.length === 0) return null;
        const byProvider = {};
        for (const log of logs) {
          const entry = byProvider[log.providerId];
          if (!entry) {
            byProvider[log.providerId] = { totalCost: log.costCents, count: 1 };
          } else {
            entry.totalCost += log.costCents;
            entry.count += 1;
          }
        }
        let cheapest = null;
        let lowestAvg = Number.POSITIVE_INFINITY;
        for (const [providerId, stats] of Object.entries(byProvider)) {
          const avg = stats.totalCost / stats.count;
          if (avg < lowestAvg) {
            lowestAvg = avg;
            cheapest = providerId;
          }
        }
        return cheapest;
      }
      async getProviderSummaries(from, to) {
        const getAllLogs = this.store.getAllCostLogs;
        if (!getAllLogs) return [];
        const logs = await getAllLogs(from, to);
        const byProvider = {};
        for (const log of logs) {
          const entry = byProvider[log.providerId];
          if (!entry) {
            byProvider[log.providerId] = { totalCost: log.costCents, count: 1 };
          } else {
            entry.totalCost += log.costCents;
            entry.count += 1;
          }
        }
        return Object.entries(byProvider).map(([providerId, stats]) => ({
          providerId,
          totalCostCents: stats.totalCost,
          requestCount: stats.count,
          avgCostPerRequest: Math.round(stats.totalCost / stats.count)
        }));
      }
    };
  }
});

// src/storage/impl/cost-store-impl.ts
var cost_store_impl_exports = {};
__export(cost_store_impl_exports, {
  CostStoreImpl: () => CostStoreImpl
});
var CostStoreImpl;
var init_cost_store_impl = __esm({
  "src/storage/impl/cost-store-impl.ts"() {
    "use strict";
    init_ids();
    CostStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async createCostLog(input) {
        await this.p.providerCostLog.create({
          data: {
            id: input.id,
            providerId: input.providerId,
            costCents: input.costCents,
            tokensInput: input.tokensInput,
            tokensOutput: input.tokensOutput,
            model: input.model,
            ts: input.ts
          }
        });
      }
      async getCostLogs(providerId, from, to) {
        const rows = await this.p.providerCostLog.findMany({
          where: {
            providerId,
            ts: { gte: from, lte: to }
          },
          orderBy: { ts: "desc" }
        });
        return rows.map(this.mapCostLog);
      }
      async createLatencyLog(input) {
        await this.p.providerLatencyLog.create({
          data: {
            id: newId(),
            providerId: input.providerId,
            latencyMs: input.latencyMs,
            capabilityId: input.capabilityId,
            ts: input.ts
          }
        });
      }
      async getLatencyLogs(providerId, from, to) {
        const rows = await this.p.providerLatencyLog.findMany({
          where: {
            providerId,
            ts: { gte: from, lte: to }
          },
          orderBy: { ts: "desc" }
        });
        return rows.map(this.mapLatencyLog);
      }
      async getAllCostLogs(from, to) {
        const rows = await this.p.providerCostLog.findMany({
          where: {
            ts: { gte: from, lte: to }
          },
          orderBy: { ts: "desc" }
        });
        return rows.map(this.mapCostLog);
      }
      // ── Mappers ────────────────────────────────────────────────────────────
      mapCostLog(row) {
        return {
          id: row.id,
          providerId: row.providerId,
          costCents: row.costCents,
          tokensInput: row.tokensInput,
          tokensOutput: row.tokensOutput,
          model: row.model ?? null,
          ts: row.ts
        };
      }
      mapLatencyLog(row) {
        return {
          id: row.id,
          providerId: row.providerId,
          latencyMs: row.latencyMs,
          capabilityId: row.capabilityId ?? null,
          ts: row.ts
        };
      }
    };
  }
});

// src/engines/provider-mux.ts
var provider_mux_exports = {};
__export(provider_mux_exports, {
  ProviderMuxEngine: () => ProviderMuxEngine
});
var ProviderMuxEngine;
var init_provider_mux = __esm({
  "src/engines/provider-mux.ts"() {
    "use strict";
    init_errors();
    init_ids();
    ProviderMuxEngine = class {
      constructor(store, dispatcher, router, eventBus, channelStore, governor) {
        this.store = store;
        this.dispatcher = dispatcher;
        this.router = router;
        this.eventBus = eventBus;
        this.channelStore = channelStore;
        this.governor = governor;
      }
      store;
      dispatcher;
      router;
      eventBus;
      channelStore;
      governor;
      // Phase 27.6: Subscribe a channel to route messages to a conversation
      async subscribeChannel(channelId, targetConversationId) {
        this.eventBus.emit({
          type: "channel:subscription",
          channelId,
          conversationId: targetConversationId,
          active: true
        });
      }
      // Phase 27.6: Unsubscribe a channel
      async unsubscribeChannel(channelId) {
        this.eventBus.emit({
          type: "channel:subscription",
          channelId,
          active: false
        });
      }
      // Phase 27.6: Process inbound channel message
      async processChannelMessage(channelId, message, targetConversationId) {
        this.eventBus.emit({
          type: "channel:message",
          channelId,
          conversationId: targetConversationId,
          message
        });
      }
      async mux(request) {
        const providerIds = request.targetProviderIds ?? await this.resolveProviderIds(request);
        switch (request.strategy) {
          case "fan_out":
            return this.fanOut(request.message, providerIds, request);
          case "round_robin":
            return this.roundRobin(request.message, providerIds, request);
          case "priority":
            return this.priorityDispatch(request.message, providerIds, request);
          case "cost_optimized":
            return this.costOptimized(request.message, providerIds, request);
          case "learned":
            return this.learnedDispatch(request.message, request.capabilityId, providerIds, request);
          default:
            return this.fanOut(request.message, providerIds, request);
        }
      }
      async fanOut(message, providerIds, requestOrTimeout) {
        const timeoutMs = typeof requestOrTimeout === "number" ? requestOrTimeout : requestOrTimeout.timeoutMs;
        const request = typeof requestOrTimeout === "object" ? requestOrTimeout : {
          message,
          timeoutMs,
          synthesisEnabled: true,
          maxProviders: providerIds.length,
          strategy: "fan_out",
          conversationId: void 0
        };
        const sessionId = await this.createSession(message, null, "fan_out");
        const limited = providerIds.slice(0, request.maxProviders ?? providerIds.length);
        const results = await Promise.all(
          limited.map(
            (pid) => this.dispatchWithTimeout(pid, message, sessionId, timeoutMs, request.conversationId)
          )
        );
        return this.buildResponse(sessionId, limited, results, "fan_out", request.synthesisEnabled);
      }
      async autoRoute(message, capabilityId) {
        const prefs = await this.store.getRoutingPreferences(capabilityId);
        const sorted = prefs.sort((a, b) => b.score - a.score);
        const providerIds = sorted.map((p) => p.providerId);
        if (providerIds.length === 0) {
          throw new EngineError(
            `No routing preferences found${capabilityId ? ` for capability ${capabilityId}` : ""}`
          );
        }
        const sessionId = await this.createSession(message, capabilityId ?? null, "learned");
        const selected = providerIds.slice(0, 3);
        const results = await Promise.all(
          selected.map((pid) => this.dispatchWithTimeout(pid, message, sessionId, 3e4))
        );
        return this.buildResponse(sessionId, selected, results, "learned", true);
      }
      async synthesize(responses) {
        if (responses.length === 0) return "";
        if (responses.length === 1) return responses[0]?.response ?? "";
        const parts = responses.map((r) => `[${r.providerId}]: ${r.response}`);
        return parts.join("\n\n---\n\n");
      }
      async recordOutcome(muxSessionId, bestProviderId) {
        const session = await this.store.getMuxSession(muxSessionId);
        if (!session) return;
        await this.store.updateMuxSession(muxSessionId, {
          bestProviderId,
          status: "completed",
          completedAt: Date.now()
        });
        const responses = await this.store.getMuxResponses(muxSessionId);
        for (const resp of responses) {
          const isBest = resp.providerId === bestProviderId;
          const prefs = await this.store.getRoutingPreferences();
          const pref = prefs.find((p) => p.providerId === resp.providerId);
          if (pref) {
            const newScore = isBest ? Math.min(pref.score + 0.1, 1) : Math.max(pref.score - 0.05, 0);
            await this.store.updateRoutingPreference(pref.id, {
              score: newScore,
              sampleCount: pref.sampleCount + 1,
              updatedAt: Date.now()
            });
          }
        }
        this.eventBus.emit({
          type: "capability:executed",
          capabilityId: session.conversationId ?? "unknown",
          providerId: bestProviderId,
          traceId: muxSessionId,
          ok: true,
          latencyMs: 0
        });
      }
      async getRoutingScore(capabilityId, providerId) {
        const prefs = await this.store.getRoutingPreferences(capabilityId);
        const match = prefs.find((p) => p.providerId === providerId);
        return match?.score ?? 0.5;
      }
      // Unit 34.5: ordered fallback providers for mid-task failover. Returns all
      // known providers except the failed one, sorted by routing score (best first).
      async fallbacksFor(providerId) {
        const prefs = await this.store.getRoutingPreferences();
        return prefs.filter((p) => p.providerId !== providerId).sort((a, b) => b.score - a.score).map((p) => p.providerId);
      }
      // ── Private ────────────────────────────────────────────────────────────
      async resolveProviderIds(request) {
        if (request.capabilityId) {
          const prefs = await this.store.getRoutingPreferences(request.capabilityId);
          if (prefs.length > 0) {
            return prefs.sort((a, b) => b.score - a.score).map((p) => p.providerId);
          }
        }
        return [];
      }
      async roundRobin(message, providerIds, request) {
        const sessionId = await this.createSession(message, null, "round_robin");
        const results = [];
        for (const pid of providerIds.slice(0, request.maxProviders)) {
          const result = await this.dispatchWithTimeout(
            pid,
            message,
            sessionId,
            request.timeoutMs,
            request.conversationId
          );
          results.push(result);
        }
        return this.buildResponse(
          sessionId,
          providerIds,
          results,
          "round_robin",
          request.synthesisEnabled
        );
      }
      async priorityDispatch(message, providerIds, request) {
        const sessionId = await this.createSession(message, null, "priority");
        const results = [];
        let lastFailed = null;
        for (const pid of providerIds.slice(0, request.maxProviders)) {
          const result = await this.dispatchWithTimeout(
            pid,
            message,
            sessionId,
            request.timeoutMs,
            request.conversationId
          );
          if (result.ok) {
            results.push(result);
            break;
          }
          lastFailed = result;
        }
        if (results.length === 0 && lastFailed) {
          results.push(lastFailed);
        }
        return this.buildResponse(sessionId, providerIds, results, "priority", request.synthesisEnabled);
      }
      async costOptimized(message, providerIds, request) {
        const sessionId = await this.createSession(message, null, "cost_optimized");
        let accruedCost = 0;
        const results = [];
        for (const pid of providerIds.slice(0, request.maxProviders)) {
          if (request.costBudgetCents !== void 0 && accruedCost >= request.costBudgetCents) break;
          const result = await this.dispatchWithTimeout(
            pid,
            message,
            sessionId,
            request.timeoutMs,
            request.conversationId
          );
          results.push(result);
          accruedCost += result.costCents;
        }
        return this.buildResponse(
          sessionId,
          providerIds,
          results,
          "cost_optimized",
          request.synthesisEnabled
        );
      }
      async learnedDispatch(message, _capabilityId, providerIds, request) {
        const sessionId = await this.createSession(message, _capabilityId ?? null, "learned");
        const results = [];
        for (const pid of providerIds.slice(0, request.maxProviders)) {
          const result = await this.dispatchWithTimeout(
            pid,
            message,
            sessionId,
            request.timeoutMs,
            request.conversationId
          );
          results.push(result);
        }
        return this.buildResponse(sessionId, providerIds, results, "learned", request.synthesisEnabled);
      }
      async dispatchWithTimeout(providerId, message, sessionId, timeoutMs, conversationId) {
        const start = Date.now();
        try {
          const result = await Promise.race([
            this.dispatcher.dispatchToProvider(providerId, message, conversationId),
            new Promise(
              (_, reject) => setTimeout(() => reject(new EngineError(`timeout after ${timeoutMs}ms`)), timeoutMs)
            )
          ]);
          const latencyMs = Date.now() - start;
          await this.store.createMuxResponse({
            id: newId(),
            muxSessionId: sessionId,
            providerId,
            accountId: null,
            ok: result.ok ? 1 : 0,
            response: result.response,
            latencyMs,
            costCents: result.costCents,
            error: result.error ?? null,
            ts: Date.now()
          });
          return { ...result, providerId, latencyMs };
        } catch (err) {
          const latencyMs = Date.now() - start;
          const errorMsg = err instanceof Error ? err.message : String(err);
          await this.store.createMuxResponse({
            id: newId(),
            muxSessionId: sessionId,
            providerId,
            accountId: null,
            ok: 0,
            response: "",
            latencyMs,
            costCents: 0,
            error: errorMsg,
            ts: Date.now()
          });
          return { providerId, ok: false, response: "", latencyMs, costCents: 0, error: errorMsg };
        }
      }
      async createSession(message, conversationId, strategy) {
        const id = newId();
        await this.store.createMuxSession({
          id,
          message,
          conversationId,
          strategy,
          status: "in_progress",
          synthesizedResponse: null,
          bestProviderId: null,
          totalCostCents: 0,
          totalLatencyMs: 0,
          startedAt: Date.now(),
          completedAt: null
        });
        return id;
      }
      async buildResponse(sessionId, _providerIds, results, strategy, synthesisEnabled) {
        const providerResponses = results;
        const totalCostCents = providerResponses.reduce((s, r) => s + r.costCents, 0);
        const totalLatencyMs = providerResponses.reduce((s, r) => s + r.latencyMs, 0);
        let synthesizedResponse = null;
        if (synthesisEnabled && providerResponses.length > 0) {
          const okResponses = providerResponses.filter((r) => r.ok);
          if (okResponses.length > 0) {
            synthesizedResponse = await this.synthesize(
              okResponses.map((r) => ({ providerId: r.providerId, response: r.response }))
            );
          }
        }
        const best = providerResponses.filter((r) => r.ok).sort((a, b) => a.latencyMs - b.latencyMs)[0] ?? null;
        await this.store.updateMuxSession(sessionId, {
          status: synthesizedResponse !== null ? "completed" : "partial",
          synthesizedResponse,
          bestProviderId: best?.providerId ?? null,
          totalCostCents,
          totalLatencyMs,
          completedAt: Date.now()
        });
        return {
          muxSessionId: sessionId,
          providerResponses: providerResponses.map((r) => ({
            providerId: r.providerId,
            accountId: null,
            ok: r.ok,
            response: r.response,
            latencyMs: r.latencyMs,
            costCents: r.costCents,
            error: r.error
          })),
          synthesizedResponse,
          bestProviderId: best?.providerId ?? null,
          totalCostCents,
          totalLatencyMs,
          strategyUsed: strategy
        };
      }
    };
  }
});

// src/storage/impl/mux-store-impl.ts
var mux_store_impl_exports = {};
__export(mux_store_impl_exports, {
  MuxStoreImpl: () => MuxStoreImpl
});
var MuxStoreImpl;
var init_mux_store_impl = __esm({
  "src/storage/impl/mux-store-impl.ts"() {
    "use strict";
    MuxStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async createMuxSession(session) {
        await this.p.muxSession.create({
          data: {
            id: session.id,
            message: session.message,
            conversationId: session.conversationId,
            strategy: session.strategy,
            status: session.status,
            synthesizedResponse: session.synthesizedResponse,
            bestProviderId: session.bestProviderId,
            totalCostCents: session.totalCostCents,
            totalLatencyMs: session.totalLatencyMs,
            startedAt: session.startedAt,
            completedAt: session.completedAt
          }
        });
      }
      async updateMuxSession(id, patch) {
        await this.p.muxSession.update({
          where: { id },
          data: patch
        });
      }
      async getMuxSession(id) {
        const row = await this.p.muxSession.findUnique({ where: { id } });
        if (!row) return null;
        return this.mapSession(row);
      }
      async createMuxResponse(response) {
        await this.p.muxResponseRow.create({
          data: {
            id: response.id,
            muxSessionId: response.muxSessionId,
            providerId: response.providerId,
            accountId: response.accountId,
            ok: response.ok,
            response: response.response,
            latencyMs: response.latencyMs,
            costCents: response.costCents,
            error: response.error,
            ts: response.ts
          }
        });
      }
      async getMuxResponses(sessionId) {
        const rows = await this.p.muxResponseRow.findMany({
          where: { muxSessionId: sessionId },
          orderBy: { ts: "asc" }
        });
        return rows.map(this.mapResponse);
      }
      async createRoutingPreference(input) {
        await this.p.routingPreference.upsert({
          where: {
            capabilityId_providerId: {
              capabilityId: input.capabilityId,
              providerId: input.providerId
            }
          },
          create: {
            id: input.id,
            capabilityId: input.capabilityId,
            providerId: input.providerId,
            score: input.score,
            sampleCount: input.sampleCount,
            updatedAt: input.updatedAt
          },
          update: {
            score: input.score,
            sampleCount: input.sampleCount,
            updatedAt: input.updatedAt
          }
        });
      }
      async updateRoutingPreference(id, patch) {
        await this.p.routingPreference.update({
          where: { id },
          data: patch
        });
      }
      async getRoutingPreferences(capabilityId) {
        const where = capabilityId ? { capabilityId } : {};
        const rows = await this.p.routingPreference.findMany({
          where,
          orderBy: { score: "desc" }
        });
        return rows.map(this.mapPreference);
      }
      // ── Mappers ────────────────────────────────────────────────────────────
      mapSession(row) {
        return {
          id: row.id,
          message: row.message,
          conversationId: row.conversationId ?? null,
          strategy: row.strategy,
          status: row.status,
          synthesizedResponse: row.synthesizedResponse ?? null,
          bestProviderId: row.bestProviderId ?? null,
          totalCostCents: row.totalCostCents,
          totalLatencyMs: row.totalLatencyMs,
          startedAt: row.startedAt,
          completedAt: row.completedAt ?? null
        };
      }
      mapResponse(row) {
        return {
          id: row.id,
          muxSessionId: row.muxSessionId,
          providerId: row.providerId,
          accountId: row.accountId ?? null,
          ok: row.ok,
          response: row.response,
          latencyMs: row.latencyMs,
          costCents: row.costCents,
          error: row.error ?? null,
          ts: row.ts
        };
      }
      mapPreference(row) {
        return {
          id: row.id,
          capabilityId: row.capabilityId,
          providerId: row.providerId,
          score: row.score,
          sampleCount: row.sampleCount,
          updatedAt: row.updatedAt
        };
      }
    };
  }
});

// src/router/router.ts
var router_exports = {};
__export(router_exports, {
  Router: () => Router
});
var Router;
var init_router = __esm({
  "src/router/router.ts"() {
    "use strict";
    init_ids();
    Router = class {
      constructor(store, dispatcher) {
        this.store = store;
        this.dispatcher = dispatcher;
      }
      store;
      dispatcher;
      async route(input) {
        const specs = await this.store.listSpecs({
          providerId: input.providerId,
          activeOnly: true
        });
        const spec = specs.find((s) => s.capability_id === input.capabilityId);
        if (!spec) {
          return {
            requestId: "",
            targetProviderId: "",
            targetAccountId: null,
            ok: false,
            error: `No active route spec for provider=${input.providerId} capability=${input.capabilityId}`
          };
        }
        const targets = await this.store.listTargets(spec.id);
        const activeTargets = targets.filter((t) => t.is_active === 1).sort((a, b) => a.priority - b.priority);
        if (activeTargets.length === 0) {
          return {
            requestId: "",
            targetProviderId: "",
            targetAccountId: null,
            ok: false,
            error: `No active route targets for spec ${spec.id}`
          };
        }
        const requestId = newId();
        const now = Date.now();
        await this.store.createRequest({
          id: requestId,
          route_spec_id: spec.id,
          conversation_id: input.conversationId ?? null,
          status: "pending",
          result_json: null,
          ts: now
        });
        await this.recordEvent(requestId, "matched", { specId: spec.id, input }, now);
        let lastError;
        for (const target of activeTargets) {
          await this.recordEvent(
            requestId,
            "dispatched",
            { targetId: target.id, providerId: target.provider_id },
            now
          );
          try {
            const result = await this.dispatcher.dispatch(target, input);
            if (result.ok) {
              await this.store.updateRequest(requestId, {
                status: "completed",
                result_json: JSON.stringify(result)
              });
              await this.recordEvent(
                requestId,
                "succeeded",
                { targetId: target.id, result },
                Date.now()
              );
              return {
                requestId,
                targetProviderId: target.provider_id,
                targetAccountId: target.account_id,
                ok: true
              };
            }
            lastError = result.error ?? "dispatch failed";
            await this.recordEvent(
              requestId,
              "failed",
              { targetId: target.id, error: lastError },
              Date.now()
            );
          } catch (err) {
            lastError = err instanceof Error ? err.message : String(err);
            await this.recordEvent(
              requestId,
              "failed",
              { targetId: target.id, error: lastError },
              Date.now()
            );
          }
        }
        await this.store.updateRequest(requestId, {
          status: "failed",
          result_json: JSON.stringify({ error: lastError })
        });
        return {
          requestId,
          targetProviderId: activeTargets[0]?.provider_id ?? "",
          targetAccountId: activeTargets[0]?.account_id ?? null,
          ok: false,
          error: lastError ?? "all targets failed"
        };
      }
      async defineSpec(input) {
        const now = Date.now();
        const row = {
          ...input,
          id: newId(),
          created_at: now,
          updated_at: now
        };
        return this.store.createSpec(row);
      }
      async addTarget(_specId, input) {
        const row = {
          ...input,
          id: newId(),
          created_at: Date.now()
        };
        return this.store.createTarget(row);
      }
      async listRequests(specId, opts) {
        return this.store.listRequests(specId, opts);
      }
      async getEvents(requestId) {
        return this.store.listEvents(requestId);
      }
      async recordEvent(requestId, eventType, data, ts) {
        const event = {
          id: newId(),
          route_request_id: requestId,
          event_type: eventType,
          event_data_json: JSON.stringify(data),
          ts
        };
        await this.store.createEvent(event);
      }
    };
  }
});

// src/storage/impl/router-store-impl.ts
var router_store_impl_exports = {};
__export(router_store_impl_exports, {
  RouterStoreImpl: () => RouterStoreImpl
});
var RouterStoreImpl;
var init_router_store_impl = __esm({
  "src/storage/impl/router-store-impl.ts"() {
    "use strict";
    RouterStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async listSpecs(opts) {
        const rows = await this.db.prisma.routeSpec.findMany({
          where: {
            ...opts?.providerId ? { providerId: opts.providerId } : {},
            ...opts?.capabilityId ? { capabilityId: opts.capabilityId } : {},
            ...opts?.activeOnly ? { isActive: 1 } : {}
          },
          orderBy: { createdAt: "desc" }
        });
        return rows.map((r) => ({
          id: r.id,
          name: r.name,
          provider_id: r.providerId,
          capability_id: r.capabilityId,
          is_active: r.isActive,
          config_json: r.configJson,
          created_at: Number(r.createdAt),
          updated_at: Number(r.updatedAt)
        }));
      }
      async getSpec(id) {
        const r = await this.db.prisma.routeSpec.findUnique({ where: { id } });
        if (!r) return null;
        return {
          id: r.id,
          name: r.name,
          provider_id: r.providerId,
          capability_id: r.capabilityId,
          is_active: r.isActive,
          config_json: r.configJson,
          created_at: Number(r.createdAt),
          updated_at: Number(r.updatedAt)
        };
      }
      async createSpec(input) {
        await this.db.prisma.routeSpec.create({
          data: {
            id: input.id,
            name: input.name,
            providerId: input.provider_id,
            capabilityId: input.capability_id,
            isActive: input.is_active,
            configJson: input.config_json,
            createdAt: input.created_at,
            updatedAt: input.updated_at
          }
        });
        return input;
      }
      async updateSpec(id, patch) {
        await this.db.prisma.routeSpec.update({
          where: { id },
          data: {
            ...patch.name !== void 0 ? { name: patch.name } : {},
            ...patch.provider_id !== void 0 ? { providerId: patch.provider_id } : {},
            ...patch.capability_id !== void 0 ? { capabilityId: patch.capability_id } : {},
            ...patch.is_active !== void 0 ? { isActive: patch.is_active } : {},
            ...patch.config_json !== void 0 ? { configJson: patch.config_json } : {},
            updatedAt: Date.now()
          }
        });
      }
      async deleteSpec(id) {
        await this.db.prisma.routeSpec.delete({ where: { id } });
      }
      async listTargets(specId) {
        const rows = await this.db.prisma.routeTarget.findMany({
          where: { routeSpecId: specId },
          orderBy: { priority: "asc" }
        });
        return rows.map((r) => ({
          id: r.id,
          route_spec_id: r.routeSpecId,
          provider_id: r.providerId,
          account_id: r.accountId,
          priority: r.priority,
          is_active: r.isActive,
          created_at: Number(r.createdAt)
        }));
      }
      async createTarget(input) {
        await this.db.prisma.routeTarget.create({
          data: {
            id: input.id,
            routeSpecId: input.route_spec_id,
            providerId: input.provider_id,
            accountId: input.account_id,
            priority: input.priority,
            isActive: input.is_active,
            createdAt: input.created_at
          }
        });
        return input;
      }
      async updateTarget(id, patch) {
        await this.db.prisma.routeTarget.update({
          where: { id },
          data: {
            ...patch.provider_id !== void 0 ? { providerId: patch.provider_id } : {},
            ...patch.account_id !== void 0 ? { accountId: patch.account_id } : {},
            ...patch.priority !== void 0 ? { priority: patch.priority } : {},
            ...patch.is_active !== void 0 ? { isActive: patch.is_active } : {}
          }
        });
      }
      async createRequest(input) {
        await this.db.prisma.routeRequest.create({
          data: {
            id: input.id,
            routeSpecId: input.route_spec_id,
            conversationId: input.conversation_id,
            status: input.status,
            resultJson: input.result_json,
            ts: input.ts
          }
        });
        return input;
      }
      async updateRequest(id, patch) {
        await this.db.prisma.routeRequest.update({
          where: { id },
          data: {
            ...patch.status !== void 0 ? { status: patch.status } : {},
            ...patch.result_json !== void 0 ? { resultJson: patch.result_json } : {}
          }
        });
      }
      async createEvent(input) {
        await this.db.prisma.routeEvent.create({
          data: {
            id: input.id,
            routeRequestId: input.route_request_id,
            eventType: input.event_type,
            eventDataJson: input.event_data_json,
            ts: input.ts
          }
        });
        return input;
      }
      async listRequests(specId, opts) {
        const rows = await this.db.prisma.routeRequest.findMany({
          where: { routeSpecId: specId },
          orderBy: { ts: "desc" },
          take: opts?.limit ?? 50,
          skip: opts?.offset ?? 0
        });
        return rows.map((r) => ({
          id: r.id,
          route_spec_id: r.routeSpecId,
          conversation_id: r.conversationId,
          status: r.status,
          result_json: r.resultJson,
          ts: Number(r.ts)
        }));
      }
      async listEvents(requestId) {
        const rows = await this.db.prisma.routeEvent.findMany({
          where: { routeRequestId: requestId },
          orderBy: { ts: "asc" }
        });
        return rows.map((r) => ({
          id: r.id,
          route_request_id: r.routeRequestId,
          event_type: r.eventType,
          event_data_json: r.eventDataJson,
          ts: Number(r.ts)
        }));
      }
    };
  }
});

// src/engines/autonomous-replay.ts
var ReplayController;
var init_autonomous_replay = __esm({
  "src/engines/autonomous-replay.ts"() {
    "use strict";
    init_errors();
    init_ids();
    ReplayController = class {
      constructor(store, executor, eventBus) {
        this.store = store;
        this.executor = executor;
        this.eventBus = eventBus;
      }
      store;
      executor;
      eventBus;
      async branch(taskId, opts = {}) {
        const prev = await this.loadTask(taskId);
        if (!prev) throw new EngineError(`Task not found: ${taskId}`);
        const branchId = newId();
        const fromIdx = opts.fromStep != null ? prev.steps.findIndex((s) => s.id === opts.fromStep) : 0;
        if (opts.fromStep != null && fromIdx < 0) {
          throw new EngineError(`Step not found: ${opts.fromStep}`);
        }
        const branchSteps = prev.steps.map((s, i) => {
          const cloned = {
            ...s,
            id: newId(),
            taskId: branchId,
            status: "pending",
            result: null,
            error: null,
            startedAt: null,
            completedAt: null
          };
          if (i < fromIdx) {
            cloned.status = s.status;
            cloned.result = s.result;
            cloned.error = s.error;
            cloned.startedAt = s.startedAt;
            cloned.completedAt = s.completedAt;
          } else if (i === fromIdx) {
            if (opts.overrideInput) {
              cloned.actionInput = { ...cloned.actionInput, ...opts.overrideInput };
            }
            if (opts.overrideProvider) {
              cloned.actionInput = { ...cloned.actionInput, provider: opts.overrideProvider };
            }
          }
          return cloned;
        });
        await this.store.createTask({
          id: branchId,
          goalJson: JSON.stringify(prev.goal),
          status: "executing",
          startedAt: Date.now()
        });
        for (const step of branchSteps) {
          await this.store.createStep({
            id: step.id,
            taskId: branchId,
            stepIndex: step.stepIndex,
            description: step.description,
            action: step.action,
            actionInputJson: JSON.stringify(step.actionInput),
            classification: step.classification,
            status: step.status,
            requiresHumanApproval: step.requiresHumanApproval ? 1 : 0
          });
        }
        this.eventBus?.emit({ type: "autonomous:branch_created", branchId, sourceTaskId: taskId });
        for (const step of branchSteps) {
          if (step.status === "complete" || step.status === "skipped") continue;
          step.status = "running";
          step.startedAt = Date.now();
          await this.store.updateStep(step.id, { status: "running", startedAt: step.startedAt });
          try {
            const result = await this.executor(step, branchId);
            step.result = result;
            step.status = "complete";
            step.completedAt = Date.now();
            await this.store.updateStep(step.id, {
              status: "complete",
              resultJson: JSON.stringify(result),
              completedAt: step.completedAt
            });
          } catch (err) {
            step.status = "failed";
            step.error = err instanceof Error ? err.message : String(err);
            step.completedAt = Date.now();
            await this.store.updateStep(step.id, {
              status: "failed",
              error: step.error,
              completedAt: step.completedAt
            });
          }
        }
        const branchFailed = branchSteps.some((s) => s.status === "failed");
        const finalStatus = branchFailed ? "failed" : "complete";
        await this.store.updateTask(branchId, { status: finalStatus, completedAt: Date.now() });
        const branchTask = await this.loadTask(branchId);
        if (!branchTask) throw new EngineError(`Branch task missing: ${branchId}`);
        const diff = prev.steps.map((orig, i) => {
          const branch = branchSteps[i];
          const originalResult = orig.result;
          const branchResult = branch?.result ?? null;
          return {
            stepId: orig.id,
            description: orig.description,
            action: orig.action,
            originalStatus: orig.status,
            branchStatus: branch?.status ?? "pending",
            originalResult,
            branchResult,
            changed: JSON.stringify(originalResult) !== JSON.stringify(branchResult)
          };
        });
        return { branchId, branchTask, diff };
      }
      async loadTask(taskId) {
        const row = await this.store.getTask(taskId);
        if (!row) return null;
        const steps = await this.store.getSteps(taskId);
        return {
          id: row.id,
          goal: JSON.parse(row.goalJson),
          status: row.status,
          steps: steps.map((s) => ({
            id: s.id,
            taskId: s.taskId,
            stepIndex: s.stepIndex,
            description: s.description,
            action: s.action,
            actionInput: JSON.parse(s.actionInputJson),
            classification: s.classification,
            status: s.status,
            result: s.resultJson ? JSON.parse(s.resultJson) : null,
            error: s.error,
            startedAt: s.startedAt,
            completedAt: s.completedAt,
            requiresHumanApproval: s.requiresHumanApproval === 1
          })),
          startedAt: row.startedAt,
          completedAt: row.completedAt,
          result: row.resultJson ? JSON.parse(row.resultJson) : null,
          error: row.error
        };
      }
    };
  }
});

// src/engines/selector-healer.ts
var selector_healer_exports = {};
__export(selector_healer_exports, {
  SelectorHealer: () => SelectorHealer
});
var DEFAULT_CONFIG, SelectorHealer;
var init_selector_healer = __esm({
  "src/engines/selector-healer.ts"() {
    "use strict";
    DEFAULT_CONFIG = {
      maxRelaxNameDistance: 3,
      llmModel: "gpt-4o-mini"
    };
    SelectorHealer = class {
      constructor(grounding, mcpClient, config2 = DEFAULT_CONFIG) {
        this.grounding = grounding;
        this.mcpClient = mcpClient;
        this.config = config2;
      }
      grounding;
      mcpClient;
      config;
      history = /* @__PURE__ */ new Map();
      async heal(params) {
        const { slaveId, failedSelector, capabilityId, providerId, context } = params;
        const historyKey = `${providerId}:${capabilityId}`;
        const result = await this.tryAriaRelaxed(slaveId, failedSelector) ?? await this.tryTextMatch(slaveId, failedSelector) ?? await this.tryDomStructure(slaveId, failedSelector) ?? await this.tryLlmProposal(slaveId, failedSelector, context) ?? await this.tryVisualMatch(slaveId, failedSelector);
        if (result) {
          const prev = this.history.get(historyKey) ?? [];
          prev.push(result);
          this.history.set(historyKey, prev.slice(-100));
        }
        return result;
      }
      getHistory(providerId, capabilityId) {
        return this.history.get(`${providerId}:${capabilityId}`) ?? [];
      }
      // ── Strategy 1: ARIA relaxed ────────────────────────────────────────────
      async tryAriaRelaxed(slaveId, selector) {
        if (selector.type !== "aria") return null;
        const tree = await this.grounding.getAccessibilityTree(slaveId);
        if (!tree) return null;
        const relaxed = {
          type: "aria",
          role: selector.role
        };
        const result = await this.grounding.resolve(slaveId, relaxed);
        if (!result) return null;
        return {
          healed: relaxed,
          strategy: "aria_relaxed",
          confidence: result.confidence * 0.9,
          originalSelector: selector
        };
      }
      // ── Strategy 2: Text match ─────────────────────────────────────────────
      async tryTextMatch(slaveId, selector) {
        const searchText = this.extractSearchText(selector);
        if (!searchText) return null;
        const textSelector = {
          type: "text",
          text: searchText
        };
        const result = await this.grounding.resolve(slaveId, textSelector);
        if (!result) return null;
        return {
          healed: textSelector,
          strategy: "text_match",
          confidence: result.confidence * 0.85,
          originalSelector: selector
        };
      }
      extractSearchText(selector) {
        if (selector.type === "aria") return selector.name ?? null;
        if (selector.type === "text") return selector.text;
        if (selector.type === "css") {
          const match = selector.selector.match(/\[text[~|^$*]?=["']([^"']+)["']\]/);
          return match?.[1] ?? null;
        }
        return null;
      }
      // ── Strategy 3: DOM structure analysis ─────────────────────────────────
      async tryDomStructure(slaveId, selector) {
        if (selector.type !== "css") return null;
        const tree = await this.grounding.getAccessibilityTree(slaveId);
        if (!tree) return null;
        const roleFromSelector = this.extractRoleFromCss(selector.selector);
        if (!roleFromSelector) return null;
        const match = this.findByRoleAndPosition(tree, roleFromSelector, 0);
        if (!match) return null;
        const healed = {
          type: "aria",
          role: match.role,
          name: match.name
        };
        return {
          healed,
          strategy: "dom_structure",
          confidence: 0.75,
          originalSelector: selector
        };
      }
      extractRoleFromCss(css) {
        const tagMatch = css.match(/^(\w+)/);
        const tag = tagMatch?.[1]?.toLowerCase();
        if (!tag) return null;
        const tagToRole = {
          button: "button",
          a: "link",
          input: "textbox",
          textarea: "textbox",
          select: "combobox",
          img: "img",
          h1: "heading",
          h2: "heading",
          h3: "heading",
          nav: "navigation",
          main: "main",
          header: "banner",
          footer: "contentinfo"
        };
        return tagToRole[tag] ?? null;
      }
      findByRoleAndPosition(node, role, position) {
        if (node.role === role) {
          if (position === 0) return node;
          return this.findByRoleAndPosition_inSubtree(node, role, position);
        }
        for (const child of node.children) {
          const found = this.findByRoleAndPosition(child, role, position);
          if (found) return found;
        }
        return null;
      }
      findByRoleAndPosition_inSubtree(node, role, position) {
        let count = 0;
        const dfs = (n) => {
          if (n.role === role) {
            if (count === position) return n;
            count++;
          }
          for (const child of n.children) {
            const found = dfs(child);
            if (found) return found;
          }
          return null;
        };
        return dfs(node);
      }
      // ── Strategy 4: LLM proposal ──────────────────────────────────────────
      async tryLlmProposal(slaveId, selector, context) {
        if (!this.mcpClient) return null;
        const tree = await this.grounding.getAccessibilityTree(slaveId);
        if (!tree) return null;
        const treeSnippet = this.treeToSnippet(tree, 3);
        const prompt = `A CSS selector failed to match any element on the page.
Failed selector: ${JSON.stringify(selector)}
${context ? `Context: ${context}
` : ""}
Accessibility tree (truncated):
${treeSnippet}

Suggest a replacement SemanticSelector (JSON) that would match the intended element.
Return only JSON: { "type": "aria"|"text"|"css", ... }`;
        try {
          const response = await this.mcpClient.callTool("llm_complete", {
            prompt,
            model: this.config.llmModel
          });
          const text = typeof response === "string" ? response : response?.text ?? "";
          const parsed = JSON.parse(text);
          if (parsed && typeof parsed === "object" && "type" in parsed) {
            const result = await this.grounding.resolve(slaveId, parsed);
            if (result) {
              return {
                healed: parsed,
                strategy: "llm_proposal",
                confidence: result.confidence * 0.7,
                originalSelector: selector
              };
            }
          }
        } catch {
        }
        return null;
      }
      treeToSnippet(node, maxDepth) {
        if (maxDepth <= 0) return "";
        const lines = [];
        const indent = "  ".repeat(3 - maxDepth);
        lines.push(`${indent}${node.role}${node.name ? ` "${node.name}"` : ""}`);
        for (const child of node.children.slice(0, 5)) {
          lines.push(this.treeToSnippet(child, maxDepth - 1));
        }
        if (node.children.length > 5) lines.push(`${indent}  ... ${node.children.length - 5} more`);
        return lines.join("\n");
      }
      // ── Strategy 5: Visual match ───────────────────────────────────────────
      async tryVisualMatch(slaveId, selector) {
        const region = this.estimateRegion(selector);
        if (!region) return null;
        const result = await this.grounding.resolveByVisual(slaveId, region, `heal: ${selector.type}`);
        if (!result) return null;
        const healed = {
          type: "css",
          selector: `[data-healed="${result.backendNodeId}"]`
        };
        return {
          healed,
          strategy: "visual_match",
          confidence: result.confidence * 0.6,
          originalSelector: selector
        };
      }
      estimateRegion(selector) {
        if (selector.type === "visual") return selector.screenshotRegion;
        return { x: 0, y: 0, width: 800, height: 600 };
      }
    };
  }
});

// src/engines/semantic-grounding.ts
var semantic_grounding_exports = {};
__export(semantic_grounding_exports, {
  SemanticGroundingEngine: () => SemanticGroundingEngine
});
var SemanticGroundingEngine;
var init_semantic_grounding = __esm({
  "src/engines/semantic-grounding.ts"() {
    "use strict";
    SemanticGroundingEngine = class {
      constructor(transport) {
        this.transport = transport;
      }
      transport;
      treeCache = /* @__PURE__ */ new Map();
      TREE_TTL_MS = 5e3;
      async resolve(slaveId, selector) {
        if (selector.type === "composite") {
          const primary = await this.resolve(slaveId, selector.primary);
          if (primary) return primary;
          for (const fb of selector.fallbacks) {
            const result = await this.resolve(slaveId, fb);
            if (result) return result;
          }
          return null;
        }
        if (selector.type === "css") {
          return this.resolveCSS(slaveId, selector.selector);
        }
        if (selector.type === "visual") {
          return this.resolveByVisual(slaveId, selector.screenshotRegion, selector.description);
        }
        if (selector.type === "frame") {
          return this.resolveFrameAware(slaveId, selector);
        }
        const tree = await this.getAccessibilityTree(slaveId);
        if (!tree) return null;
        if (selector.type === "aria") {
          return this.matchAria(tree, selector);
        }
        if (selector.type === "text") {
          return this.matchText(tree, selector);
        }
        return null;
      }
      async resolveAll(slaveId, selector) {
        if (selector.type === "composite") {
          const results = await this.resolveAll(slaveId, selector.primary);
          if (results.length > 0) return results;
          for (const fb of selector.fallbacks) {
            const r = await this.resolveAll(slaveId, fb);
            if (r.length > 0) return r;
          }
          return [];
        }
        if (selector.type === "css") {
          return this.resolveAllCSS(slaveId, selector.selector);
        }
        const tree = await this.getAccessibilityTree(slaveId);
        if (!tree) return [];
        if (selector.type === "aria") {
          return this.matchAllAria(tree, selector);
        }
        if (selector.type === "text") {
          return this.matchAllText(tree, selector);
        }
        return [];
      }
      async waitFor(slaveId, selector, timeoutMs = 1e4) {
        const deadline = Date.now() + timeoutMs;
        while (Date.now() < deadline) {
          const result = await this.resolve(slaveId, selector);
          if (result) return result;
          await new Promise((r) => setTimeout(r, 200));
        }
        return null;
      }
      async getAccessibilityTree(slaveId) {
        const cached = this.treeCache.get(slaveId);
        if (cached && Date.now() - cached.ts < this.TREE_TTL_MS) return cached.tree;
        try {
          const result = await this.transport.send(slaveId, "Accessibility.getFullAXTree");
          if (!result?.nodes) return null;
          const nodeMap = /* @__PURE__ */ new Map();
          for (const raw of result.nodes) {
            const id = raw.frontendNodeId ?? raw.backendDOMNodeId ?? 0;
            nodeMap.set(id, {
              nodeId: id,
              backendNodeId: raw.backendDOMNodeId ?? 0,
              role: raw.role?.value ?? "unknown",
              name: raw.name?.value,
              description: raw.description?.value,
              children: []
            });
          }
          for (const raw of result.nodes) {
            const id = raw.frontendNodeId ?? raw.backendDOMNodeId ?? 0;
            const node = nodeMap.get(id);
            if (!node) continue;
            for (const childId of raw.childIds ?? []) {
              const child = nodeMap.get(childId);
              if (child) node.children.push(child);
            }
          }
          const root = nodeMap.values().next().value ?? null;
          if (root) this.treeCache.set(slaveId, { tree: root, ts: Date.now() });
          return root ?? null;
        } catch {
          return null;
        }
      }
      async resolveFrameAware(slaveId, selector) {
        const { frameChain, inner } = selector;
        let frameNodeId = 0;
        const doc = await this.transport.send(slaveId, "DOM.getDocument", {
          depth: 0,
          pierce: true
        });
        frameNodeId = doc.root.nodeId;
        for (const frameUrl of frameChain) {
          const frameTree = await this.transport.send(slaveId, "Page.getFrameTree");
          const frame = frameTree.frameTree.childFrames?.find((f) => f.frame.url.includes(frameUrl));
          if (!frame) return null;
          const evalResult = await this.transport.send(slaveId, "Runtime.evaluate", {
            expression: `document.querySelector('iframe[src*="${frameUrl}"]')`
          });
          if (evalResult?.result?.objectId) {
            const resolved = await this.transport.send(slaveId, "DOM.describeNode", {
              objectId: evalResult.result.objectId
            });
            frameNodeId = resolved?.node?.contentDocument?.nodeId ?? 0;
            if (!frameNodeId) return null;
          }
        }
        if (inner.type === "css") {
          const result = await this.transport.send(slaveId, "DOM.querySelector", {
            nodeId: frameNodeId,
            selector: inner.selector
          });
          if (!result?.nodeId) return null;
          return {
            nodeId: result.nodeId,
            backendNodeId: 0,
            selector: `frame(${frameChain.join("/")}) > ${inner.selector}`,
            confidence: 0.9,
            matchedBy: selector
          };
        }
        return null;
      }
      async resolveByVisual(slaveId, region, description) {
        const base64 = await this.transport.send(slaveId, "Page.captureScreenshot", {
          format: "png"
        });
        if (!base64) return null;
        try {
          const backendNodeId = await this.getNodeAtPoint(
            slaveId,
            region.x + region.width / 2,
            region.y + region.height / 2
          );
          if (!backendNodeId) return null;
          return {
            nodeId: 0,
            backendNodeId,
            selector: `[visual-match at ${region.x},${region.y}]`,
            confidence: 0.6,
            matchedBy: { type: "visual", screenshotRegion: region, description }
          };
        } catch {
          return null;
        }
      }
      async resolveCSS(slaveId, selector) {
        try {
          if (selector.includes(">>>")) {
            return this.resolveShadowCSS(slaveId, selector);
          }
          const doc = await this.transport.send(slaveId, "DOM.getDocument");
          const result = await this.transport.send(slaveId, "DOM.querySelector", {
            nodeId: doc.root.nodeId,
            selector
          });
          if (!result?.nodeId) return null;
          return {
            nodeId: result.nodeId,
            backendNodeId: 0,
            selector,
            confidence: 1,
            matchedBy: { type: "css", selector }
          };
        } catch {
          return null;
        }
      }
      async resolveShadowCSS(slaveId, selector) {
        const parts = selector.split(">>>").map((s) => s.trim());
        let currentNodeId = 0;
        const doc = await this.transport.send(slaveId, "DOM.getDocument", {
          depth: -1,
          pierce: true
        });
        currentNodeId = doc.root.nodeId;
        for (let i = 0; i < parts.length; i++) {
          const part = parts[i];
          const isLast = i === parts.length - 1;
          const result = await this.transport.send(slaveId, "DOM.querySelector", {
            nodeId: currentNodeId,
            selector: part
          });
          if (!result?.nodeId) return null;
          if (isLast) {
            return {
              nodeId: result.nodeId,
              backendNodeId: 0,
              selector,
              confidence: 1,
              matchedBy: { type: "css", selector }
            };
          }
          const shadow = await this.transport.send(slaveId, "DOM.describeNode", {
            nodeId: result.nodeId
          });
          const childNodeId = shadow?.node?.contentDocument?.nodeId;
          if (!childNodeId) return null;
          currentNodeId = childNodeId;
        }
        return null;
      }
      async resolveAllCSS(slaveId, selector) {
        try {
          if (selector.includes(">>>")) {
            const single = await this.resolveShadowCSS(slaveId, selector);
            return single ? [single] : [];
          }
          const doc = await this.transport.send(slaveId, "DOM.getDocument");
          const result = await this.transport.send(slaveId, "DOM.querySelectorAll", {
            nodeId: doc.root.nodeId,
            selector
          });
          if (!result?.nodeIds) return [];
          return result.nodeIds.map((nodeId) => ({
            nodeId,
            backendNodeId: 0,
            selector,
            confidence: 1,
            matchedBy: { type: "css", selector }
          }));
        } catch {
          return [];
        }
      }
      matchAria(root, selector) {
        const node = this.findAriaMatch(root, selector);
        if (!node) return null;
        return {
          nodeId: node.nodeId,
          backendNodeId: node.backendNodeId,
          selector: `[role="${selector.role}"]${selector.name ? `[name="${selector.name}"]` : ""}`,
          confidence: this.ariaConfidence(node, selector),
          matchedBy: selector
        };
      }
      matchAllAria(root, selector) {
        const results = [];
        this.collectAriaMatches(root, selector, results);
        return results;
      }
      collectAriaMatches(node, selector, results) {
        if (node.role === selector.role) {
          if (!selector.name || node.name === selector.name) {
            if (!selector.description || node.description === selector.description) {
              results.push({
                nodeId: node.nodeId,
                backendNodeId: node.backendNodeId,
                selector: `[role="${selector.role}"]${selector.name ? `[name="${selector.name}"]` : ""}`,
                confidence: this.ariaConfidence(node, selector),
                matchedBy: selector
              });
            }
          }
        }
        for (const child of node.children) this.collectAriaMatches(child, selector, results);
      }
      findAriaMatch(node, selector) {
        if (node.role === selector.role) {
          if (!selector.name || node.name === selector.name) {
            if (!selector.description || node.description === selector.description) return node;
          }
        }
        for (const child of node.children) {
          const found = this.findAriaMatch(child, selector);
          if (found) return found;
        }
        return null;
      }
      ariaConfidence(node, selector) {
        let score = 0.5;
        if (node.role === selector.role) score += 0.3;
        if (selector.name && node.name === selector.name) score += 0.15;
        if (selector.description && node.description === selector.description) score += 0.05;
        return Math.min(score, 1);
      }
      matchText(root, selector) {
        const node = this.findTextMatch(root, selector);
        if (!node) return null;
        return {
          nodeId: node.nodeId,
          backendNodeId: node.backendNodeId,
          selector: `text="${selector.text}"`,
          confidence: node.name?.includes(selector.text) ? 0.9 : 0.7,
          matchedBy: selector
        };
      }
      matchAllText(root, selector) {
        const results = [];
        this.collectTextMatches(root, selector, results);
        return results;
      }
      collectTextMatches(node, selector, results) {
        const matchesText = node.name?.includes(selector.text) || node.description?.includes(selector.text);
        const matchesRole = !selector.elementRole || node.role === selector.elementRole;
        if (matchesText && matchesRole) {
          results.push({
            nodeId: node.nodeId,
            backendNodeId: node.backendNodeId,
            selector: `text="${selector.text}"`,
            confidence: node.name?.includes(selector.text) ? 0.9 : 0.7,
            matchedBy: selector
          });
        }
        for (const child of node.children) this.collectTextMatches(child, selector, results);
      }
      findTextMatch(node, selector) {
        const matchesText = node.name?.includes(selector.text) || node.description?.includes(selector.text);
        const matchesRole = !selector.elementRole || node.role === selector.elementRole;
        if (matchesText && matchesRole) return node;
        for (const child of node.children) {
          const found = this.findTextMatch(child, selector);
          if (found) return found;
        }
        return null;
      }
      async getNodeAtPoint(slaveId, x, y) {
        try {
          const result = await this.transport.send(slaveId, "DOM.getNodeForLocation", {
            x: Math.round(x),
            y: Math.round(y),
            includeUserAgentShadowDOM: true
          });
          return result?.backendNodeId ?? null;
        } catch {
          return null;
        }
      }
    };
  }
});

// src/engines/autonomous-execution.ts
var autonomous_execution_exports = {};
__export(autonomous_execution_exports, {
  AutonomousExecutionEngine: () => AutonomousExecutionEngine,
  planStepsFromIntent: () => planStepsFromIntent,
  planStepsLocally: () => planStepsLocally,
  resolvePlanner: () => resolvePlanner
});
function classificationAtLeast2(classification, threshold) {
  return (CLASSIFICATION_PRIORITY2[classification] ?? 0) >= (CLASSIFICATION_PRIORITY2[threshold] ?? 0);
}
function toAutonomousClassification(c) {
  return c === "system" ? "read" : c;
}
function assembleStep(index, goal, description, action, input) {
  const classification = toAutonomousClassification(input.classification ?? "read");
  return {
    id: newId(),
    taskId: "",
    stepIndex: index,
    description,
    action,
    actionInput: input,
    classification,
    status: "pending",
    result: null,
    error: null,
    startedAt: null,
    completedAt: null,
    requiresHumanApproval: classificationAtLeast2(classification, goal.requireApprovalAbove)
  };
}
function resolvePlanner(goal, opts) {
  const override = goal.llmProvider;
  if (!override || override === "local") {
    return { provider: "local", local: true };
  }
  if (!opts.consented) {
    throw new ConsentViolationError(override);
  }
  return { provider: override, local: false };
}
function planStepsFromIntent(goal, intent) {
  if (!intent || !intent.capabilityId) return [];
  const nodes = [intent, ...intent.alternatives ?? []];
  const steps = [];
  for (const node of nodes) {
    if (!node.capabilityId) continue;
    const classification = toAutonomousClassification(node.classification ?? "read");
    steps.push(
      assembleStep(steps.length, goal, node.intent || goal.description, node.capabilityId, {
        ...node.input,
        inputMapping: node.input,
        classification
      })
    );
  }
  if (steps.length > goal.maxSteps) return steps.slice(0, goal.maxSteps);
  return steps;
}
function planStepsLocally(goal) {
  const text = goal.description.toLowerCase();
  const steps = [];
  const add = (action, classification, extra = {}) => {
    if (steps.length >= goal.maxSteps) return;
    steps.push(
      assembleStep(steps.length, goal, goal.description, action, { ...extra, classification })
    );
  };
  const urlMatch = goal.description.match(/https?:\/\/[^\s]+|(?:\b[a-z0-9-]+\.)+[a-z]{2,}\b/i);
  const url = urlMatch ? urlMatch[0] : void 0;
  if (url) add("navigate", "navigate", { url });
  if (/\bscreenshot\b/.test(text)) add("screenshot", "read");
  if (/\b(delete|remove|permanent|drop|truncate|wipe)\b/.test(text))
    add("destructive", "destructive");
  if (/\b(summar|summarize)\b/.test(text)) add("summarize", "read");
  if (/\b(search|find)\b/.test(text) && !url) add("search", "read", { query: goal.description });
  if (steps.length === 0) add("task", "read");
  return steps;
}
var CLASSIFICATION_PRIORITY2, AutonomousExecutionEngine;
var init_autonomous_execution = __esm({
  "src/engines/autonomous-execution.ts"() {
    "use strict";
    init_errors();
    init_ids();
    init_autonomous_replay();
    CLASSIFICATION_PRIORITY2 = {
      read: 0,
      navigate: 1,
      communication: 2,
      write: 3,
      destructive: 4,
      financial: 5
    };
    AutonomousExecutionEngine = class {
      constructor(store, registry2, policyEngine, governor, eventBus, resolver, failoverRouter, airgap = true, consentCheck = () => false) {
        this.store = store;
        this.registry = registry2;
        this.policyEngine = policyEngine;
        this.governor = governor;
        this.eventBus = eventBus;
        this.resolver = resolver;
        this.failoverRouter = failoverRouter;
        this.airgap = airgap;
        this.consentCheck = consentCheck;
      }
      store;
      registry;
      policyEngine;
      governor;
      eventBus;
      resolver;
      failoverRouter;
      airgap;
      consentCheck;
      activeTasks = /* @__PURE__ */ new Map();
      gateWaiters = /* @__PURE__ */ new Map();
      async execute(goal) {
        const taskId = newId();
        const task = {
          id: taskId,
          goal,
          status: "planning",
          steps: [],
          startedAt: Date.now(),
          completedAt: null,
          result: null,
          error: null
        };
        await this.store.createTask({
          id: taskId,
          goalJson: JSON.stringify(goal),
          status: "planning",
          startedAt: task.startedAt
        });
        this.activeTasks.set(taskId, task);
        this.eventBus.emit({
          type: "autonomous:started",
          taskId,
          goal: goal.description
        });
        try {
          const plan = await this.planGoal(goal);
          task.steps = plan;
          for (const step of plan) {
            await this.store.createStep({
              id: step.id,
              taskId,
              stepIndex: step.stepIndex,
              description: step.description,
              action: step.action,
              actionInputJson: JSON.stringify(step.actionInput),
              classification: step.classification,
              status: "pending",
              requiresHumanApproval: step.requiresHumanApproval ? 1 : 0
            });
          }
          return this.runTask(task);
        } catch (err) {
          task.status = "failed";
          task.error = err instanceof Error ? err.message : String(err);
          task.completedAt = Date.now();
          await this.store.updateTask(taskId, {
            status: "failed",
            error: task.error,
            completedAt: task.completedAt
          });
        }
        return task;
      }
      // ── Pause / Resume (HITL v2) ──────────────────────────────────────────
      async runTask(task) {
        const taskId = task.id;
        if (task.status !== "executing") {
          task.status = "executing";
          await this.store.updateTask(taskId, { status: "executing" });
        }
        try {
          for (const step of task.steps) {
            if (task.status === "cancelled") break;
            if (task.status === "paused") break;
            if (step.status === "complete" || step.status === "skipped") continue;
            const decision = await this.policyEngine.evaluate(step.action, step.actionInput);
            step.requiresHumanApproval = decision.requiresApproval || classificationAtLeast2(step.classification, task.goal.requireApprovalAbove);
            if (step.requiresHumanApproval) {
              const gate = await this.createGate(taskId, step, decision);
              task.status = "waiting_approval";
              await this.store.updateTask(taskId, { status: "waiting_approval" });
              this.eventBus.emit({
                type: "autonomous:gate_created",
                taskId,
                gateId: gate.id,
                prompt: gate.prompt
              });
              const resolved = await this.waitForGateResolution(gate.id, 3e5);
              if (!resolved || resolved.status === "denied") {
                step.status = "skipped";
                await this.store.updateStep(step.id, { status: "skipped" });
                continue;
              }
              if (resolved.status === "expired") {
                task.status = "failed";
                task.error = "Gate expired";
                break;
              }
              task.status = "executing";
              await this.store.updateTask(taskId, { status: "executing" });
            }
            if (!decision.allowed) {
              step.status = "skipped";
              step.error = `Blocked by policy: ${decision.reason}`;
              await this.store.updateStep(step.id, { status: "skipped", error: step.error });
              continue;
            }
            step.status = "running";
            step.startedAt = Date.now();
            await this.store.updateStep(step.id, { status: "running", startedAt: step.startedAt });
            try {
              const result = await this.executeStepWithFailover(step, task);
              step.result = result;
              step.status = "complete";
              step.completedAt = Date.now();
              await this.store.updateStep(step.id, {
                status: "complete",
                resultJson: JSON.stringify(result),
                completedAt: step.completedAt
              });
              this.eventBus.emit({
                type: "autonomous:step_complete",
                taskId,
                stepId: step.id,
                stepIndex: step.stepIndex
              });
            } catch (err) {
              step.status = "failed";
              step.error = err instanceof Error ? err.message : String(err);
              step.completedAt = Date.now();
              await this.store.updateStep(step.id, {
                status: "failed",
                error: step.error,
                completedAt: step.completedAt
              });
              this.eventBus.emit({
                type: "autonomous:step_failed",
                taskId,
                stepId: step.id,
                error: step.error
              });
              if (step.classification === "destructive" || step.classification === "financial") {
                task.status = "failed";
                task.error = `Critical step failed: ${step.error}`;
                break;
              }
            }
          }
          if (task.status !== "failed" && task.status !== "cancelled" && task.status !== "paused") {
            task.status = "complete";
            task.result = task.steps.map((s) => ({ step: s.description, result: s.result }));
          }
          task.completedAt = Date.now();
          await this.store.updateTask(taskId, {
            status: task.status,
            resultJson: JSON.stringify(task.result),
            error: task.error,
            completedAt: task.completedAt
          });
          if (task.status === "complete") {
            this.eventBus.emit({
              type: "autonomous:complete",
              taskId,
              status: task.status,
              stepsCompleted: task.steps.filter((s) => s.status === "complete").length,
              stepsTotal: task.steps.length
            });
          }
        } catch (err) {
          task.status = "failed";
          task.error = err instanceof Error ? err.message : String(err);
          task.completedAt = Date.now();
          await this.store.updateTask(taskId, {
            status: "failed",
            error: task.error,
            completedAt: task.completedAt
          });
        } finally {
          this.activeTasks.delete(taskId);
        }
        return task;
      }
      async pause(taskId) {
        const task = this.activeTasks.get(taskId) ?? await this.getStatus(taskId);
        if (!task) return;
        task.status = "paused";
        await this.store.updateTask(taskId, { status: "paused" });
        this.eventBus.emit({ type: "autonomous:paused", taskId });
      }
      async resume(taskId) {
        const task = this.activeTasks.get(taskId) ?? await this.getStatus(taskId);
        if (!task) return null;
        if (task.status !== "paused") return task;
        this.activeTasks.set(taskId, task);
        return this.runTask(task);
      }
      async cancel(taskId) {
        const task = this.activeTasks.get(taskId);
        if (task) {
          task.status = "cancelled";
        }
        await this.store.updateTask(taskId, { status: "cancelled" });
        this.eventBus.emit({ type: "autonomous:cancelled", taskId });
      }
      async getStatus(taskId) {
        const active = this.activeTasks.get(taskId);
        if (active) return active;
        const row = await this.store.getTask(taskId);
        if (!row) return null;
        const steps = await this.store.getSteps(taskId);
        return {
          id: row.id,
          goal: JSON.parse(row.goalJson),
          status: row.status,
          steps: steps.map((s) => ({
            id: s.id,
            taskId: s.taskId,
            stepIndex: s.stepIndex,
            description: s.description,
            action: s.action,
            actionInput: JSON.parse(s.actionInputJson),
            classification: s.classification,
            status: s.status,
            result: s.resultJson ? JSON.parse(s.resultJson) : null,
            error: s.error,
            startedAt: s.startedAt,
            completedAt: s.completedAt,
            requiresHumanApproval: s.requiresHumanApproval === 1
          })),
          startedAt: row.startedAt,
          completedAt: row.completedAt,
          result: row.resultJson ? JSON.parse(row.resultJson) : null,
          error: row.error
        };
      }
      async listTasks(opts) {
        const rows = await this.store.listTasks(opts);
        const tasks = [];
        for (const row of rows) {
          const steps = await this.store.getSteps(row.id);
          tasks.push({
            id: row.id,
            goal: JSON.parse(row.goalJson),
            status: row.status,
            steps: steps.map((s) => ({
              id: s.id,
              taskId: s.taskId,
              stepIndex: s.stepIndex,
              description: s.description,
              action: s.action,
              actionInput: JSON.parse(s.actionInputJson),
              classification: s.classification,
              status: s.status,
              result: s.resultJson ? JSON.parse(s.resultJson) : null,
              error: s.error,
              startedAt: s.startedAt,
              completedAt: s.completedAt,
              requiresHumanApproval: s.requiresHumanApproval === 1
            })),
            startedAt: row.startedAt,
            completedAt: row.completedAt,
            result: row.resultJson ? JSON.parse(row.resultJson) : null,
            error: row.error
          });
        }
        return tasks;
      }
      async resolveGate(gateId, response, resolvedBy) {
        const gateRow = await this.store.getGate(gateId);
        if (!gateRow) throw new EngineError(`Gate not found: ${gateId}`);
        if (gateRow.status !== "pending") throw new EngineError(`Gate already resolved: ${gateId}`);
        const gateType = gateRow.gateType;
        const approvalResponses = ["approve", "deny", "skip"];
        if (gateType === "approval" && !approvalResponses.includes(response)) {
          throw new EngineError(`Invalid gate response: ${response}`);
        }
        const statusMap = {
          approve: "approved",
          deny: "denied",
          skip: "skipped"
        };
        const gateStatus = gateType === "approval" ? statusMap[response] ?? "denied" : "resolved";
        await this.store.updateHitlGate(gateId, {
          status: gateStatus,
          resolvedBy,
          resolvedAt: Date.now(),
          response
        });
        this.eventBus.emit({
          type: "autonomous:gate_resolved",
          gateId,
          taskId: gateRow.taskId,
          response,
          resolvedBy
        });
        const waiter = this.gateWaiters.get(gateId);
        if (waiter) {
          clearTimeout(waiter.timer);
          const gate = {
            id: gateId,
            taskId: gateRow.taskId,
            stepId: gateRow.stepId,
            gateType: gateRow.gateType,
            prompt: gateRow.prompt,
            options: JSON.parse(gateRow.optionsJson),
            defaultValue: gateRow.defaultValue,
            status: gateStatus,
            resolvedBy,
            resolvedAt: Date.now(),
            response,
            createdAt: gateRow.createdAt,
            expiresAt: gateRow.expiresAt
          };
          waiter.resolve(gate);
          this.gateWaiters.delete(gateId);
        }
      }
      async getPendingGates() {
        const rows = await this.store.getPendingGates();
        return rows.map((r) => ({
          id: r.id,
          taskId: r.taskId,
          stepId: r.stepId,
          gateType: r.gateType,
          prompt: r.prompt,
          options: JSON.parse(r.optionsJson),
          defaultValue: r.defaultValue,
          status: r.status,
          resolvedBy: r.resolvedBy,
          resolvedAt: r.resolvedAt,
          response: r.response,
          createdAt: r.createdAt,
          expiresAt: r.expiresAt
        }));
      }
      // ── Proactive clarification (HITL v2) ──────────────────────────────────
      // Opens a mid-task clarification gate (open question / option select /
      // file picker / URL input) and resolves with the human response.
      async clarify(step, prompt, kind, opts) {
        const timeoutMs = opts?.timeoutMs ?? 3e5;
        const gate = {
          id: newId(),
          taskId: step.taskId,
          stepId: step.id,
          gateType: kind,
          prompt,
          options: opts?.options ?? [],
          defaultValue: opts?.defaultValue ?? null,
          status: "pending",
          resolvedBy: null,
          resolvedAt: null,
          response: null,
          createdAt: Date.now(),
          expiresAt: Date.now() + timeoutMs
        };
        await this.store.createHitlGate({
          id: gate.id,
          taskId: gate.taskId,
          stepId: gate.stepId,
          gateType: gate.gateType,
          prompt: gate.prompt,
          optionsJson: JSON.stringify(gate.options),
          defaultValue: gate.defaultValue,
          status: "pending",
          createdAt: gate.createdAt,
          expiresAt: gate.expiresAt
        });
        this.eventBus.emit({
          type: "agent:clarify",
          gateId: gate.id,
          taskId: gate.taskId,
          stepId: gate.stepId,
          gateType: kind,
          prompt,
          options: gate.options
        });
        const resolved = await this.waitForGateResolution(gate.id, timeoutMs);
        return resolved?.response ?? null;
      }
      async replay(taskId, fromStep) {
        const prev = await this.getStatus(taskId);
        if (!prev) throw new EngineError(`Task not found: ${taskId}`);
        const newTask = await this.execute(prev.goal);
        if (fromStep) {
          const skipIdx = prev.steps.findIndex((s) => s.id === fromStep);
          if (skipIdx > 0) {
            for (let i = 0; i < skipIdx; i++) {
              const oldStep = prev.steps[i];
              const newStep = newTask.steps[i];
              if (oldStep?.status === "complete" && newStep) {
                newStep.result = oldStep.result;
                newStep.status = "complete";
                await this.store.updateStep(newStep.id, {
                  status: "complete",
                  resultJson: JSON.stringify(oldStep.result)
                });
              }
            }
          }
        }
        return newTask;
      }
      // Replay a finished task with branching (Unit 34.4). Delegates to
      // ReplayController; the branch re-executes capability steps via the
      // capability registry, leaving the original timeline untouched.
      async replayBranch(taskId, opts) {
        const controller = new ReplayController(
          this.store,
          async (step, branchId) => {
            if (!this.registry) throw new EngineError("No registry bound for replay branch");
            return this.registry.execute(
              step.actionInput.capabilitySlug ?? step.action,
              step.actionInput.input ?? {},
              { metadata: { taskId: branchId, branch: true } }
            );
          },
          this.eventBus
        );
        return controller.branch(taskId, opts);
      }
      // ── Private: Planning ────────────────────────────────────────────────
      // planGoal delegates to the IntentDecomposer (nlcl resolver): each resolved
      // CapabilityNode becomes an AutonomousStep whose `action` is the capability
      // slug, carrying its inputMapping + classification. No regex action parsing.
      async planGoal(goal) {
        const consented = await this.consentCheck();
        resolvePlanner(goal, { airgap: this.airgap, consented });
        if (this.resolver) {
          const ctx = { surface: "cli", metadata: {} };
          const intent = await this.resolver.resolve(goal.description, ctx);
          return planStepsFromIntent(goal, intent);
        }
        return planStepsLocally(goal);
      }
      // ── Private: Execution ───────────────────────────────────────────────
      async executeStepWithHealing(step, task) {
        try {
          return await this.executeStep(step, task);
        } catch (err) {
          if (this.isSelectorError(err) && step.actionInput.selector) {
            const healed = await this.healSelector(step.actionInput.selector, task);
            if (healed) {
              step.actionInput.selector = healed;
              return await this.executeStep(step, task);
            }
          }
          throw err;
        }
      }
      // Unit 34.5: provider failover. Attempts the step (with selector healing);
      // on failure, consults the failover router for fallback providers, opens an
      // `option` clarification gate, and on approval re-executes against the chosen
      // fallback with adapted input. With no fallback, the original error propagates.
      async executeStepWithFailover(step, task) {
        const attempted = step.actionInput.provider ?? "default";
        try {
          return await this.executeStepWithHealing(step, task);
        } catch (err) {
          const fallbacks = this.failoverRouter ? await this.failoverRouter.fallbacksFor(attempted) : [];
          if (fallbacks.length === 0) throw err;
          const choice = await this.clarify(
            step,
            `Provider "${attempted}" failed mid-task. Choose a fallback provider to continue.`,
            "option",
            { options: fallbacks }
          );
          if (!choice || !fallbacks.includes(choice)) throw err;
          step.actionInput = this.adaptInputForProvider(step.actionInput, choice);
          this.eventBus.emit({
            type: "autonomous:failover",
            taskId: task.id,
            fromProvider: attempted,
            toProvider: choice,
            stepId: step.id
          });
          return await this.executeStepWithHealing(step, task);
        }
      }
      adaptInputForProvider(input, provider) {
        return { ...input, provider };
      }
      async executeStep(step, task) {
        switch (step.action) {
          case "navigate": {
            const slave = await this.governor.ensureRunning("default");
            await this.governor.cdp.send(slave.slaveId, "Page.navigate", {
              url: step.actionInput.url
            });
            await this.waitForPageLoad(slave.slaveId);
            return { navigated: true, url: step.actionInput.url };
          }
          case "click": {
            const slave = await this.governor.ensureRunning("default");
            await this.governor.cdp.send(slave.slaveId, "Runtime.evaluate", {
              expression: `document.querySelector('${step.actionInput.target}')?.click()`
            });
            return { clicked: true, target: step.actionInput.target };
          }
          case "fill_form": {
            const slave = await this.governor.ensureRunning("default");
            return { filled: true, slaveId: slave.slaveId };
          }
          case "search": {
            const slave = await this.governor.ensureRunning("default");
            await this.governor.cdp.send(slave.slaveId, "Runtime.evaluate", {
              expression: `document.querySelector('input[type="search"], input[name="q"]')?.value = '${step.actionInput.query}'`
            });
            await this.governor.cdp.send(slave.slaveId, "Runtime.evaluate", {
              expression: `document.querySelector('form')?.submit()`
            });
            await this.waitForPageLoad(slave.slaveId);
            return { searched: true, query: step.actionInput.query };
          }
          case "screenshot": {
            const slave = await this.governor.ensureRunning("default");
            const screenshot = await this.governor.cdp.captureScreenshot(slave.slaveId);
            return { screenshot };
          }
          case "capability_call": {
            const result = await this.registry.execute(
              step.actionInput.capabilitySlug,
              step.actionInput.input,
              { metadata: { taskId: task.id } }
            );
            return result;
          }
          default:
            throw new EngineError(`Unknown action: ${step.action}`);
        }
      }
      isSelectorError(err) {
        if (err instanceof Error) {
          return err.message.includes("selector") || err.message.includes("querySelector");
        }
        return false;
      }
      async healSelector(failedSelector, task) {
        try {
          const { SelectorHealer: SelectorHealer2 } = await Promise.resolve().then(() => (init_selector_healer(), selector_healer_exports));
          const { SemanticGroundingEngine: SemanticGroundingEngine2 } = await Promise.resolve().then(() => (init_semantic_grounding(), semantic_grounding_exports));
          const slave = await this.governor.ensureRunning("default");
          const transport = this.governor.getTransport();
          if (!transport) return null;
          const grounding = new SemanticGroundingEngine2(transport);
          const healer = new SelectorHealer2(grounding);
          const result = await healer.heal({
            slaveId: slave.slaveId,
            failedSelector: { type: "css", selector: failedSelector },
            capabilityId: task.goal.description,
            providerId: "autonomous",
            context: `Autonomous task: ${task.goal.description}`
          });
          if (result?.healed.type === "css") return result.healed.selector;
          return null;
        } catch {
          return null;
        }
      }
      async waitForPageLoad(slaveId, timeoutMs = 15e3) {
        const deadline = Date.now() + timeoutMs;
        while (Date.now() < deadline) {
          try {
            const state = await this.governor.cdp.getPageState(slaveId);
            if (state?.readyState === "complete") return;
          } catch {
          }
          await new Promise((r) => setTimeout(r, 200));
        }
      }
      // ── Private: HITL Gates ─────────────────────────────────────────────
      async createGate(taskId, step, decision) {
        const gate = {
          id: newId(),
          taskId,
          stepId: step.id,
          gateType: "approval",
          prompt: `Step "${step.description}" requires approval.
Action: ${step.action}
Classification: ${decision.classification}
Reason: ${decision.reason}`,
          options: ["approve", "deny", "skip"],
          defaultValue: "deny",
          status: "pending",
          resolvedBy: null,
          resolvedAt: null,
          response: null,
          createdAt: Date.now(),
          expiresAt: Date.now() + 3e5
        };
        await this.store.createHitlGate({
          id: gate.id,
          taskId,
          stepId: step.id,
          gateType: gate.gateType,
          prompt: gate.prompt,
          optionsJson: JSON.stringify(gate.options),
          defaultValue: gate.defaultValue,
          status: "pending",
          createdAt: gate.createdAt,
          expiresAt: gate.expiresAt
        });
        return gate;
      }
      waitForGateResolution(gateId, timeoutMs) {
        return new Promise((resolve4) => {
          const timer = setTimeout(async () => {
            this.gateWaiters.delete(gateId);
            await this.store.updateHitlGate(gateId, { status: "expired" });
            resolve4(null);
          }, timeoutMs);
          this.gateWaiters.set(gateId, { resolve: resolve4, timer });
          this.store.getGate(gateId).then((gate) => {
            if (gate && gate.status !== "pending") {
              clearTimeout(timer);
              this.gateWaiters.delete(gateId);
              resolve4({
                id: gateId,
                taskId: gate.taskId,
                stepId: gate.stepId,
                gateType: gate.gateType,
                prompt: gate.prompt,
                options: JSON.parse(gate.optionsJson),
                defaultValue: gate.defaultValue,
                status: gate.status,
                resolvedBy: gate.resolvedBy,
                resolvedAt: gate.resolvedAt,
                response: gate.response,
                createdAt: gate.createdAt,
                expiresAt: gate.expiresAt
              });
            }
          });
        });
      }
    };
  }
});

// src/engines/execution-policy.ts
var execution_policy_exports = {};
__export(execution_policy_exports, {
  ExecutionPolicyEngine: () => ExecutionPolicyEngine
});
var DEFAULT_RULES, ExecutionPolicyEngine;
var init_execution_policy = __esm({
  "src/engines/execution-policy.ts"() {
    "use strict";
    init_errors();
    init_ids();
    DEFAULT_RULES = [
      {
        name: "read_only_safe",
        condition: "action matches ^(get|list|read|query|fetch|search)$",
        classification: "read",
        requiresApproval: false,
        cooldownMs: 0,
        maxOccurrences: 1e6,
        windowMs: 6e4,
        isActive: true
      },
      {
        name: "navigate_safe",
        condition: "action matches ^(navigate|goto|open|visit)$",
        classification: "navigate",
        requiresApproval: false,
        cooldownMs: 0,
        maxOccurrences: 1e6,
        windowMs: 6e4,
        isActive: true
      },
      {
        name: "write_needs_approval",
        condition: "action matches ^(create|update|delete|submit|save|write|patch)$",
        classification: "write",
        requiresApproval: true,
        cooldownMs: 0,
        maxOccurrences: 10,
        windowMs: 6e4,
        isActive: true
      },
      {
        name: "destructive_always",
        condition: "action matches ^(delete_permanent|format|reset|drop|purge|destroy)$",
        classification: "destructive",
        requiresApproval: true,
        cooldownMs: 5e3,
        maxOccurrences: 3,
        windowMs: 3e5,
        isActive: true
      },
      {
        name: "financial_always",
        condition: "action matches ^(purchase|pay|transfer|checkout|buy|subscribe)$",
        classification: "financial",
        requiresApproval: true,
        cooldownMs: 1e4,
        maxOccurrences: 5,
        windowMs: 6e5,
        isActive: true
      },
      {
        name: "communication_always",
        condition: "action matches ^(send_email|send_message|post_comment|reply)$",
        classification: "communication",
        requiresApproval: true,
        cooldownMs: 0,
        maxOccurrences: 20,
        windowMs: 6e4,
        isActive: true
      }
    ];
    ExecutionPolicyEngine = class {
      constructor(store) {
        this.store = store;
      }
      store;
      rules = [];
      initialized = false;
      async initialize() {
        if (this.initialized) return;
        const stored = await this.store.listRules();
        if (stored.length === 0) {
          for (const rule of DEFAULT_RULES) {
            const id = newId();
            await this.store.createRule({ ...rule, id });
            this.rules.push({ ...rule, id });
          }
        } else {
          this.rules = stored.map((r) => ({
            id: r.id,
            name: r.name,
            condition: r.condition,
            classification: r.classification,
            requiresApproval: r.requiresApproval,
            cooldownMs: r.cooldownMs,
            maxOccurrences: r.maxOccurrences,
            windowMs: r.windowMs,
            isActive: r.isActive
          }));
        }
        this.initialized = true;
      }
      async evaluate(action, _input) {
        await this.initialize();
        const classification = await this.classify(action);
        const matchedRule = this.findMatchingRule(action);
        if (!matchedRule) {
          return {
            allowed: true,
            classification,
            requiresApproval: false,
            reason: "No matching rule \u2014 default allow",
            matchedRule: null
          };
        }
        if (matchedRule.cooldownMs > 0) {
          const recentCount = await this.store.getRecentOccurrences(action, matchedRule.cooldownMs);
          if (recentCount > 0) {
            return {
              allowed: false,
              classification,
              requiresApproval: matchedRule.requiresApproval,
              reason: `Cooldown active (${matchedRule.cooldownMs}ms) \u2014 ${recentCount} recent occurrence(s)`,
              matchedRule: matchedRule.name
            };
          }
        }
        if (matchedRule.maxOccurrences < Number.POSITIVE_INFINITY && matchedRule.windowMs > 0) {
          const recentCount = await this.store.getRecentOccurrences(action, matchedRule.windowMs);
          if (recentCount >= matchedRule.maxOccurrences) {
            return {
              allowed: false,
              classification,
              requiresApproval: matchedRule.requiresApproval,
              reason: `Max occurrences (${matchedRule.maxOccurrences}) reached in ${matchedRule.windowMs}ms window`,
              matchedRule: matchedRule.name
            };
          }
        }
        return {
          allowed: true,
          classification,
          requiresApproval: matchedRule.requiresApproval,
          reason: matchedRule.requiresApproval ? `Requires approval: ${matchedRule.name}` : `Allowed by rule: ${matchedRule.name}`,
          matchedRule: matchedRule.name
        };
      }
      async classify(action) {
        await this.initialize();
        const rule = this.findMatchingRule(action);
        return rule?.classification ?? "read";
      }
      async addRule(rule) {
        await this.initialize();
        const id = newId();
        const newRule = { ...rule, id };
        await this.store.createRule(newRule);
        this.rules.push(newRule);
        return newRule;
      }
      async listRules() {
        await this.initialize();
        return [...this.rules];
      }
      async updateRule(id, patch) {
        await this.initialize();
        const idx = this.rules.findIndex((r) => r.id === id);
        if (idx === -1) throw new EngineError(`Rule not found: ${id}`);
        this.rules[idx] = { ...this.rules[idx], ...patch };
        await this.store.updateRule(id, patch);
      }
      findMatchingRule(action) {
        const lower = action.toLowerCase();
        for (const rule of this.rules) {
          if (!rule.isActive) continue;
          const pattern2 = rule.condition.replace(/^action matches\s+/, "");
          try {
            const regex = new RegExp(pattern2, "i");
            if (regex.test(lower)) return rule;
          } catch {
            if (lower === rule.name.toLowerCase()) return rule;
          }
        }
        return null;
      }
    };
  }
});

// src/storage/impl/autonomous-store-impl.ts
var autonomous_store_impl_exports = {};
__export(autonomous_store_impl_exports, {
  AutonomousStoreImpl: () => AutonomousStoreImpl
});
import { PrismaClient as PrismaClient2 } from "@prisma/client";
var prisma, AutonomousStoreImpl;
var init_autonomous_store_impl = __esm({
  "src/storage/impl/autonomous-store-impl.ts"() {
    "use strict";
    prisma = new PrismaClient2();
    AutonomousStoreImpl = class {
      async createTask(task) {
        await prisma.autonomousTask.create({
          data: {
            id: task.id,
            goalJson: task.goalJson,
            status: task.status ?? "pending",
            resultJson: task.resultJson ?? null,
            error: task.error ?? null,
            startedAt: task.startedAt,
            completedAt: task.completedAt ?? null
          }
        });
      }
      async updateTask(id, patch) {
        const data = {};
        if (patch.status !== void 0) data.status = patch.status;
        if (patch.resultJson !== void 0) data.resultJson = patch.resultJson;
        if (patch.error !== void 0) data.error = patch.error;
        if (patch.completedAt !== void 0) data.completedAt = patch.completedAt;
        await prisma.autonomousTask.update({ where: { id }, data });
      }
      async getTask(id) {
        const row = await prisma.autonomousTask.findUnique({ where: { id } });
        return row;
      }
      async listTasks(opts) {
        const where = opts?.status ? { status: opts.status } : {};
        const rows = await prisma.autonomousTask.findMany({
          where,
          orderBy: { startedAt: "desc" },
          take: opts?.limit ?? 50
        });
        return rows;
      }
      async createStep(step) {
        await prisma.autonomousStep.create({
          data: {
            id: step.id,
            taskId: step.taskId,
            stepIndex: step.stepIndex,
            description: step.description,
            action: step.action,
            actionInputJson: step.actionInputJson,
            classification: step.classification,
            status: step.status ?? "pending",
            resultJson: step.resultJson ?? null,
            error: step.error ?? null,
            startedAt: step.startedAt ?? null,
            completedAt: step.completedAt ?? null,
            requiresHumanApproval: step.requiresHumanApproval ?? 0
          }
        });
      }
      async updateStep(id, patch) {
        const data = {};
        if (patch.status !== void 0) data.status = patch.status;
        if (patch.resultJson !== void 0) data.resultJson = patch.resultJson;
        if (patch.error !== void 0) data.error = patch.error;
        if (patch.startedAt !== void 0) data.startedAt = patch.startedAt;
        if (patch.completedAt !== void 0) data.completedAt = patch.completedAt;
        await prisma.autonomousStep.update({ where: { id }, data });
      }
      async getSteps(taskId) {
        const rows = await prisma.autonomousStep.findMany({
          where: { taskId },
          orderBy: { stepIndex: "asc" }
        });
        return rows;
      }
      async getStep(id) {
        const row = await prisma.autonomousStep.findUnique({ where: { id } });
        return row;
      }
      async createHitlGate(gate) {
        await prisma.hitlGate.create({
          data: {
            id: gate.id,
            taskId: gate.taskId,
            stepId: gate.stepId,
            gateType: gate.gateType,
            prompt: gate.prompt,
            optionsJson: gate.optionsJson ?? "[]",
            defaultValue: gate.defaultValue ?? null,
            status: gate.status ?? "pending",
            resolvedBy: gate.resolvedBy ?? null,
            resolvedAt: gate.resolvedAt ?? null,
            response: gate.response ?? null,
            createdAt: gate.createdAt,
            expiresAt: gate.expiresAt ?? null
          }
        });
      }
      async updateHitlGate(id, patch) {
        const data = {};
        if (patch.status !== void 0) data.status = patch.status;
        if (patch.resolvedBy !== void 0) data.resolvedBy = patch.resolvedBy;
        if (patch.resolvedAt !== void 0) data.resolvedAt = patch.resolvedAt;
        if (patch.response !== void 0) data.response = patch.response;
        await prisma.hitlGate.update({ where: { id }, data });
      }
      async getPendingGates(taskId) {
        const where = { status: "pending" };
        if (taskId) where.taskId = taskId;
        const rows = await prisma.hitlGate.findMany({ where, orderBy: { createdAt: "asc" } });
        return rows;
      }
      async getGate(id) {
        const row = await prisma.hitlGate.findUnique({ where: { id } });
        return row;
      }
    };
  }
});

// src/storage/impl/policy-store-impl.ts
var policy_store_impl_exports = {};
__export(policy_store_impl_exports, {
  PolicyStoreImpl: () => PolicyStoreImpl
});
import { PrismaClient as PrismaClient3 } from "@prisma/client";
var prisma2, PolicyStoreImpl;
var init_policy_store_impl = __esm({
  "src/storage/impl/policy-store-impl.ts"() {
    "use strict";
    prisma2 = new PrismaClient3();
    PolicyStoreImpl = class {
      async createRule(rule) {
        await prisma2.policyRule.create({
          data: {
            id: rule.id,
            name: rule.name,
            condition: rule.condition,
            classification: rule.classification,
            requiresApproval: rule.requiresApproval ? 1 : 0,
            cooldownMs: rule.cooldownMs ?? 0,
            maxOccurrences: rule.maxOccurrences ?? 1e6,
            windowMs: rule.windowMs ?? 6e4,
            isActive: rule.isActive !== false ? 1 : 0
          }
        });
      }
      async updateRule(id, patch) {
        const data = {};
        if (patch.name !== void 0) data.name = patch.name;
        if (patch.condition !== void 0) data.condition = patch.condition;
        if (patch.classification !== void 0) data.classification = patch.classification;
        if (patch.requiresApproval !== void 0) data.requiresApproval = patch.requiresApproval ? 1 : 0;
        if (patch.cooldownMs !== void 0) data.cooldownMs = patch.cooldownMs;
        if (patch.maxOccurrences !== void 0) data.maxOccurrences = patch.maxOccurrences;
        if (patch.windowMs !== void 0) data.windowMs = patch.windowMs;
        if (patch.isActive !== void 0) data.isActive = patch.isActive ? 1 : 0;
        await prisma2.policyRule.update({ where: { id }, data });
      }
      async getRule(id) {
        const row = await prisma2.policyRule.findUnique({ where: { id } });
        return row;
      }
      async listRules() {
        const rows = await prisma2.policyRule.findMany();
        return rows.map((r) => ({
          ...r,
          requiresApproval: r.requiresApproval === 1,
          isActive: r.isActive === 1
        }));
      }
      async getRecentOccurrences(action, windowMs) {
        const cutoff = Date.now() - windowMs;
        const count = await prisma2.autonomousStep.count({
          where: {
            action,
            startedAt: { gte: cutoff }
          }
        });
        return count;
      }
    };
  }
});

// src/storage/impl/local-agent-store-impl.ts
var local_agent_store_impl_exports = {};
__export(local_agent_store_impl_exports, {
  LocalAgentStoreImpl: () => LocalAgentStoreImpl
});
var DEFAULT_TIMEOUT_MS, LocalAgentStoreImpl;
var init_local_agent_store_impl = __esm({
  "src/storage/impl/local-agent-store-impl.ts"() {
    "use strict";
    init_ids();
    DEFAULT_TIMEOUT_MS = 12e4;
    LocalAgentStoreImpl = class {
      db;
      constructor(db) {
        this.db = db;
      }
      get p() {
        return this.db.prisma;
      }
      async getAgentProvider(slug) {
        const def = await this.p.providerDefinition.findUnique({
          where: { slug },
          include: { providerModels: true }
        });
        if (!def) return null;
        const models = (def.providerModels ?? []).map((m) => ({
          slug: m.modelSlug,
          displayName: m.displayName,
          isDefault: m.isDefault === 1
        }));
        return {
          slug: def.slug,
          displayName: def.displayName,
          authType: "none",
          models
        };
      }
      async getAgentConfig(slug) {
        const def = await this.p.providerDefinition.findUnique({
          where: { slug },
          include: { providerModels: true, providerConfigs: true }
        });
        if (!def) return null;
        const configs = new Map(
          (def.providerConfigs ?? []).map((c) => [c.configKey, c.configValue])
        );
        const models = (def.providerModels ?? []).map((m) => m.modelSlug);
        const defaultModel = (def.providerModels ?? []).find((m) => m.isDefault === 1)?.modelSlug ?? models[0] ?? "";
        const timeoutRaw = configs.get("timeout_ms");
        const timeoutMs = timeoutRaw ? Number(timeoutRaw) || DEFAULT_TIMEOUT_MS : DEFAULT_TIMEOUT_MS;
        return {
          binary: configs.get("binary") ?? "opencode",
          timeoutMs,
          allowedModels: models,
          defaultModel
        };
      }
      async upsertAgentProvider(row, config2) {
        const now = Date.now();
        await this.p.providerDefinition.upsert({
          where: { slug: row.slug },
          create: {
            id: row.slug,
            slug: row.slug,
            displayName: row.displayName,
            providerType: "local-agent",
            authType: "none",
            isActive: 1,
            createdAt: now,
            updatedAt: now
          },
          update: { displayName: row.displayName, updatedAt: now }
        });
        for (const m of row.models) {
          await this.p.providerModel.upsert({
            where: { providerId_modelSlug: { providerId: row.slug, modelSlug: m.slug } },
            create: {
              id: newId(),
              providerId: row.slug,
              modelSlug: m.slug,
              displayName: m.displayName,
              isActive: 1,
              isDefault: m.isDefault ? 1 : 0,
              createdAt: now,
              updatedAt: now
            },
            update: { displayName: m.displayName, isDefault: m.isDefault ? 1 : 0, updatedAt: now }
          });
        }
        const cfgRows = [
          { key: "binary", value: config2.binary },
          { key: "timeout_ms", value: String(config2.timeoutMs) },
          { key: "allowed_models", value: config2.allowedModels.join(",") },
          { key: "default_model", value: config2.defaultModel }
        ];
        for (const c of cfgRows) {
          await this.p.providerConfig.upsert({
            where: { providerId_configKey: { providerId: row.slug, configKey: c.key } },
            create: {
              id: newId(),
              providerId: row.slug,
              configKey: c.key,
              configValue: c.value,
              configType: "string",
              isSecret: 0,
              createdAt: now,
              updatedAt: now
            },
            update: { configValue: c.value, updatedAt: now }
          });
        }
      }
      async isModelAllowed(slug, model) {
        const cfg = await this.getAgentConfig(slug);
        if (!cfg) return false;
        return cfg.allowedModels.includes(model);
      }
    };
  }
});

// src/engines/local-agent/local-agent-executor.ts
var local_agent_executor_exports = {};
__export(local_agent_executor_exports, {
  LOCAL_AGENT_SLUG: () => LOCAL_AGENT_SLUG,
  LocalAgentProviderExecutor: () => LocalAgentProviderExecutor,
  parseOpencodeJson: () => parseOpencodeJson
});
function parseOpencodeJson(raw) {
  const blocks = [];
  let cost = 0;
  const lines = raw.split(/\r?\n/).filter((l) => l.trim().length > 0);
  for (const line of lines) {
    let obj;
    try {
      obj = JSON.parse(line);
    } catch {
      continue;
    }
    if (typeof obj.tokens?.cost === "number") cost = obj.tokens.cost;
    const parts = obj.message?.parts ?? obj.parts;
    const role = obj.message?.role ?? obj.role;
    if (parts && role === "assistant") {
      for (const part of parts) {
        if (part.type === "text" && typeof part.text === "string") {
          blocks.push({ type: "text", text: part.text });
        } else if (part.type === "step-start") {
          blocks.push({ type: "step-start" });
        } else if (part.type === "step-finish") {
          blocks.push({ type: "step-finish" });
        } else if (part.type === "reasoning" && typeof part.text === "string") {
          blocks.push({ type: "reasoning", text: part.text });
        }
      }
    }
    if (obj.type === "error" || obj.subtype === "error") {
      blocks.push({
        type: "error",
        message: obj.error ?? "agent run failed",
        code: "AGENT_FAILED"
      });
    }
  }
  return { blocks, cost };
}
var LOCAL_AGENT_SLUG, LocalAgentProviderExecutor;
var init_local_agent_executor = __esm({
  "src/engines/local-agent/local-agent-executor.ts"() {
    "use strict";
    init_errors();
    LOCAL_AGENT_SLUG = "opencode";
    LocalAgentProviderExecutor = class {
      store;
      eventBus;
      constructor(store, eventBus) {
        this.store = store;
        this.eventBus = eventBus;
      }
      async run(input) {
        const start = Date.now();
        const provider = await this.store.getAgentProvider(LOCAL_AGENT_SLUG);
        if (!provider) {
          throw new EngineError(
            `local-agent provider '${LOCAL_AGENT_SLUG}' is not seeded`,
            "local-agent:not-seeded"
          );
        }
        const config2 = await this.store.getAgentConfig(LOCAL_AGENT_SLUG);
        if (!config2) {
          throw new EngineError(
            `local-agent provider '${LOCAL_AGENT_SLUG}' has no config`,
            "local-agent:no-config"
          );
        }
        const model = input.model ?? config2.defaultModel;
        if (!await this.store.isModelAllowed(LOCAL_AGENT_SLUG, model)) {
          throw new EngineError(
            `model '${model}' is not in the local-agent allow-list`,
            "local-agent:model-not-allowed"
          );
        }
        const args = ["run", "--auto", "--model", model, "--format", "json", input.prompt];
        const proc = Bun.spawn([config2.binary, ...args], {
          cwd: input.cwd ?? process.cwd(),
          stdout: "pipe",
          stderr: "pipe"
        });
        let timedOut = false;
        let raw = "";
        try {
          const timeout = new Promise(
            (resolve4) => setTimeout(() => resolve4("timeout"), config2.timeoutMs)
          );
          const read = (async () => {
            const out = await new Response(proc.stdout).text();
            const err = await new Response(proc.stderr).text();
            return out + (err ? `
${err}` : "");
          })();
          const race = await Promise.race([read, timeout]);
          if (race === "timeout") {
            timedOut = true;
            proc.kill();
            raw = await new Response(proc.stdout).text();
          } else {
            raw = race;
          }
        } catch {
          raw = "";
        }
        const exitCode = timedOut ? -1 : proc.exitCode ?? -1;
        const { blocks, cost } = parseOpencodeJson(raw);
        if (this.eventBus) {
          const latencyMs = Date.now() - start;
          if (exitCode === 0 && !timedOut) {
            this.eventBus.emit({
              type: "capability:executed",
              capabilityId: "cap:agent:run",
              providerId: LOCAL_AGENT_SLUG,
              traceId: `${LOCAL_AGENT_SLUG}-${start}`,
              ok: true,
              latencyMs
            });
          } else {
            this.eventBus.emit({
              type: "capability:failed",
              capabilityId: "cap:agent:run",
              providerId: LOCAL_AGENT_SLUG,
              traceId: `${LOCAL_AGENT_SLUG}-${start}`,
              error: timedOut ? "timeout" : `exit ${exitCode}`,
              recoveryBehavior: "retry_selector"
            });
          }
        }
        return { blocks, model, exitCode, cost, raw, timedOut };
      }
    };
  }
});

// src/engines/browser-automation/recipes.ts
function recipe(id, description, steps, extra = {}) {
  return {
    id,
    providerId: "generic",
    capabilitySlug: id,
    version: 1,
    description,
    steps,
    tags: ["auto"],
    ...extra
  };
}
function getRecipe(id) {
  return RECIPES.find((r) => r.id === id);
}
var RECIPES;
var init_recipes = __esm({
  "src/engines/browser-automation/recipes.ts"() {
    "use strict";
    RECIPES = [
      // ── research ──
      recipe(
        "auto:research:report",
        "Deep research report: crawl N sources, extract, summarize.",
        [
          { kind: "navigate", url: "{{queryUrl}}" },
          { kind: "wait_text", text: "", timeoutMs: 3e3 },
          { kind: "extract_markdown" },
          { kind: "observe", what: "screenshot" }
        ],
        { tags: ["auto", "research"] }
      ),
      recipe("auto:research:crawl", "Crawl a single URL and capture markdown + links.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "body", timeoutMs: 5e3 },
        { kind: "extract_markdown" },
        { kind: "observe", what: "dom" }
      ]),
      recipe("auto:research:deep", "Deep research: scroll infinitely then extract.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "scroll", y: 0, selector: "" },
        { kind: "wait", timeoutMs: 1e3 },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:research:compare", "Compare two URLs side by side (two tabs).", [
        { kind: "navigate", url: "{{urlA}}" },
        { kind: "tab_open", url: "{{urlB}}" },
        { kind: "extract_markdown" }
      ]),
      // ── monitor ──
      recipe("auto:monitor:watch", "Watch a URL and capture state for diffing.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait", timeoutMs: 2e3 },
        { kind: "observe", what: "dom" }
      ]),
      recipe("auto:monitor:price", "Extract a price element and report it.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "{{priceSelector}}", timeoutMs: 5e3 },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:monitor:availability", "Check stock availability text.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_text", text: "{{inStockText}}", timeoutMs: 5e3 },
        { kind: "observe", what: "dom" }
      ]),
      recipe("auto:monitor:diff", "Capture DOM snapshot for later diff.", [
        { kind: "observe", what: "dom" },
        { kind: "screenshot" }
      ]),
      // ── form ──
      recipe("auto:form:autofill", "Autofill a form from a profile map.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "form", timeoutMs: 5e3 },
        { kind: "type_text", selector: "{{nameSelector}}", text: "{{name}}" },
        { kind: "type_text", selector: "{{emailSelector}}", text: "{{email}}" },
        { kind: "type_text", selector: "{{phoneSelector}}", text: "{{phone}}" },
        { kind: "click", selector: "button[type=submit], button" }
      ]),
      recipe("auto:form:multi", "Fill multiple fields from a key/value list.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "form", timeoutMs: 5e3 },
        { kind: "type_text", selector: "{{fieldSelector}}", text: "{{value}}" }
      ]),
      // ── commerce ──
      recipe("auto:commerce:checkout", "Add to cart and proceed to checkout (destructive-gated).", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "{{addToCart}}", timeoutMs: 5e3 },
        { kind: "click", selector: "{{addToCart}}" },
        { kind: "wait_selector", selector: "{{checkout}}", timeoutMs: 5e3 },
        { kind: "human_gate", prompt: "Confirm checkout?" },
        { kind: "click", selector: "{{checkout}}" }
      ]),
      recipe("auto:commerce:cart", "Add item to cart.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "click", selector: "{{addToCart}}" }
      ]),
      recipe("auto:commerce:price-track", "Record price for tracking.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "{{priceSelector}}", timeoutMs: 5e3 },
        { kind: "extract_markdown" }
      ]),
      // ── data ──
      recipe("auto:data:scrape", "Scrape a page into structured markdown + links.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait", timeoutMs: 1500 },
        { kind: "extract_markdown" },
        { kind: "observe", what: "dom" }
      ]),
      recipe("auto:data:join", "Scrape multiple URLs (parallel tabs).", [
        {
          kind: "parallel",
          branches: [[{ kind: "navigate", url: "{{url1}}" }], [{ kind: "navigate", url: "{{url2}}" }]]
        }
      ]),
      recipe("auto:data:dedupe", "Scrape then dedupe links.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:data:export", "Scrape and write to a file.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      // ── auth ──
      recipe("auto:auth:session", "Login then extract protected content.", [
        { kind: "navigate", url: "{{loginUrl}}" },
        { kind: "type_text", selector: "input[type=email]", text: "{{user}}" },
        { kind: "type_text", selector: "input[type=password]", text: "{{pass}}" },
        { kind: "click", selector: "button[type=submit]" },
        { kind: "wait", timeoutMs: 2e3 },
        { kind: "navigate", url: "{{targetUrl}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:auth:persist", "Login and persist session cookies.", [
        { kind: "navigate", url: "{{loginUrl}}" },
        { kind: "type_text", selector: "input[type=email]", text: "{{user}}" },
        { kind: "type_text", selector: "input[type=password]", text: "{{pass}}" },
        { kind: "click", selector: "button[type=submit]" }
      ]),
      // ── content ──
      recipe("auto:content:summarize", "Navigate and extract a TL;DR-ready markdown.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "article, main, body", timeoutMs: 5e3 },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:content:translate", "Extract then hand to translator (markdown).", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:content:tldr", "Extract first N paragraphs.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:content:rewrite", "Extract source for rewrite.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      // ── extract ──
      recipe("auto:extract:structured", "Extract structured JSON-LD + tables.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:extract:table", "Extract all tables.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "wait_selector", selector: "table", timeoutMs: 5e3 },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:extract:feed", "Find and follow RSS feeds.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "observe", what: "dom" }
      ]),
      // ── test ──
      recipe("auto:test:ui", "UI smoke test: click through and capture console.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "observe", what: "console" },
        { kind: "click", selector: "{{cta}}" },
        { kind: "wait", timeoutMs: 1500 },
        { kind: "screenshot" }
      ]),
      recipe("auto:test:smoke", "Smoke test: load + assert title.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "assert", condition: 'document.readyState==="complete"' }
      ]),
      recipe("auto:test:regression", "Regression: screenshot + dom diff.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "observe", what: "dom" },
        { kind: "screenshot" }
      ]),
      recipe("auto:test:visual", "Visual test: capture full screenshot.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "screenshot" }
      ]),
      // ── booking ──
      recipe("auto:ticket:book", "Book a ticket (destructive-gated).", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "click", selector: "{{show}}" },
        { kind: "human_gate", prompt: "Confirm booking?" },
        { kind: "click", selector: "{{buy}}" }
      ]),
      recipe("auto:ticket:reserve", "Reserve a ticket.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "click", selector: "{{reserve}}" }
      ]),
      // ── social ──
      recipe("auto:social:post", "Compose and post to a social site (destructive-gated).", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "type_text", selector: "{{composer}}", text: "{{post}}" },
        { kind: "human_gate", prompt: "Post?" },
        { kind: "click", selector: "{{submit}}" }
      ]),
      recipe("auto:social:monitor", "Monitor a profile for new posts.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "observe", what: "dom" }
      ]),
      // ── misc ──
      recipe("auto:email:compose", "Compose an email draft.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "type_text", selector: "{{to}}", text: "{{recipient}}" },
        { kind: "type_text", selector: "{{body}}", text: "{{message}}" }
      ]),
      recipe("auto:doc:fill", "Fill a document template.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "type_text", selector: "{{field}}", text: "{{value}}" }
      ]),
      recipe("auto:pdf:extract", "Open a PDF and capture text.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:job:apply", "Apply to a job posting (destructive-gated).", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "click", selector: "{{apply}}" },
        { kind: "human_gate", prompt: "Apply?" },
        { kind: "click", selector: "{{submit}}" }
      ]),
      recipe("auto:lead:enrich", "Enrich a lead page into structured notes.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:report:generate", "Generate a markdown report from captured pages.", [
        { kind: "extract_markdown" },
        { kind: "observe", what: "dom" }
      ]),
      recipe("auto:search:query", "Run a search query and capture results.", [
        { kind: "navigate", url: "{{searchUrl}}" },
        { kind: "wait_selector", selector: "{{results}}", timeoutMs: 5e3 },
        { kind: "extract_markdown" }
      ]),
      recipe("auto:nav:click-link", "Click a link by text.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "click", selector: "{{linkSelector}}" }
      ]),
      recipe("auto:capture:full", "Capture a full-page screenshot.", [
        { kind: "navigate", url: "{{url}}" },
        { kind: "screenshot" }
      ]),
      recipe("auto:observe:state", "Capture full observation (dom + a11y + screenshot).", [
        { kind: "observe", what: "dom" },
        { kind: "observe", what: "a11y" },
        { kind: "screenshot" }
      ]),
      recipe("auto:flow:parallel-crawl", "Crawl multiple URLs in parallel tabs.", [
        {
          kind: "parallel",
          branches: [
            [{ kind: "navigate", url: "{{url1}}" }],
            [{ kind: "navigate", url: "{{url2}}" }],
            [{ kind: "navigate", url: "{{url3}}" }]
          ]
        }
      ])
    ];
  }
});

// src/engines/harness/recipe-compiler.ts
function stepToNode(step, index) {
  switch (step.kind) {
    case "type_text":
      return {
        type: "action",
        action: "type_text",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector,
        params: { text: step.text, composerType: step.composerType ?? "textarea" }
      };
    case "submit":
      return {
        type: "action",
        action: "submit",
        outputKey: step.outputKey ?? `s${index}`,
        params: { sendSelector: step.sendSelector }
      };
    case "click":
      return {
        type: "action",
        action: "click",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector
      };
    case "wait":
      return {
        type: "action",
        action: "wait",
        outputKey: step.outputKey ?? `s${index}`,
        params: { timeoutMs: step.timeoutMs }
      };
    case "navigate":
      return {
        type: "action",
        action: "navigate",
        outputKey: step.outputKey ?? `s${index}`,
        params: { url: step.url }
      };
    case "capture":
      return {
        type: "action",
        action: "capture",
        outputKey: step.outputKey ?? `s${index}`,
        params: { pattern: step.pattern, timeoutMs: step.timeoutMs }
      };
    case "evaluate":
      return {
        type: "action",
        action: "evaluate",
        outputKey: step.outputKey ?? `s${index}`,
        params: { expression: step.expression }
      };
    case "scroll":
      return {
        type: "action",
        action: "scroll",
        outputKey: step.outputKey ?? `s${index}`,
        params: { x: step.x, y: step.y, selector: step.selector }
      };
    case "hover":
      return {
        type: "action",
        action: "hover",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector
      };
    case "select":
      return {
        type: "action",
        action: "select",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector,
        params: { value: step.value, label: step.label }
      };
    case "press":
      return {
        type: "action",
        action: "press",
        outputKey: step.outputKey ?? `s${index}`,
        params: { key: step.key }
      };
    case "tab_open":
      return {
        type: "action",
        action: "tab_open",
        outputKey: step.outputKey ?? `s${index}`,
        params: { url: step.url }
      };
    case "tab_close":
      return {
        type: "action",
        action: "tab_close",
        outputKey: step.outputKey ?? `s${index}`,
        params: { targetId: step.targetId }
      };
    case "tab_switch":
      return {
        type: "action",
        action: "tab_switch",
        outputKey: step.outputKey ?? `s${index}`,
        params: { targetId: step.targetId }
      };
    case "observe":
      return {
        type: "action",
        action: "observe",
        outputKey: step.outputKey ?? `s${index}`,
        params: { what: step.what }
      };
    case "upload":
      return {
        type: "action",
        action: "upload",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector,
        params: { files: step.files }
      };
    case "extract_markdown":
      return {
        type: "action",
        action: "extract_markdown",
        outputKey: step.outputKey ?? `s${index}`
      };
    case "wait_selector":
      return {
        type: "action",
        action: "wait_selector",
        outputKey: step.outputKey ?? `s${index}`,
        selector: step.selector,
        params: { timeoutMs: step.timeoutMs }
      };
    case "wait_text":
      return {
        type: "action",
        action: "wait_text",
        outputKey: step.outputKey ?? `s${index}`,
        params: { text: step.text, timeoutMs: step.timeoutMs }
      };
    case "screenshot":
      return {
        type: "action",
        action: "screenshot",
        outputKey: step.outputKey ?? `s${index}`,
        params: { region: step.region }
      };
    case "assert":
      return {
        type: "action",
        action: "assert",
        outputKey: step.outputKey ?? `s${index}`,
        params: { condition: step.condition }
      };
    case "mock_request":
      return {
        type: "action",
        action: "mock_request",
        outputKey: step.outputKey ?? `s${index}`,
        params: { urlPattern: step.urlPattern, body: step.body, status: step.status }
      };
    case "cookie_set":
      return {
        type: "action",
        action: "cookie_set",
        outputKey: step.outputKey ?? `s${index}`,
        params: { name: step.name, value: step.value, path: step.path }
      };
    case "branch_if":
      return {
        type: "branch",
        action: "branch_if",
        outputKey: step.outputKey ?? `s${index}`,
        condition: { outputKey: step.condition, truthy: true },
        params: { steps: step.then }
      };
    case "loop_while":
      return {
        type: "action",
        action: "loop_while",
        outputKey: step.outputKey ?? `s${index}`,
        condition: { outputKey: step.condition, truthy: true },
        params: { steps: step.body, max: step.max }
      };
    case "parallel":
      return {
        type: "parallel",
        action: "parallel",
        outputKey: step.outputKey ?? `s${index}`,
        params: { branches: step.branches }
      };
    case "human_gate":
      return {
        type: "action",
        action: "human_gate",
        outputKey: step.outputKey ?? `s${index}`,
        params: { prompt: step.prompt }
      };
  }
}
function compileRecipe(recipe2) {
  const nodes = [];
  const edges = [];
  recipe2.steps.forEach((step, i) => {
    nodes.push(stepToNode(step, i));
    if (i > 0) edges.push({ from: i - 1, to: i });
  });
  let lastLinear = recipe2.steps.length - 1;
  if (recipe2.branches && recipe2.branches.length > 0) {
    for (const branch of recipe2.branches) {
      const base = nodes.length;
      for (let i = 0; i < branch.steps.length; i++) {
        const step = branch.steps[i];
        if (!step) continue;
        const node = stepToNode(step, base + i);
        node.condition = {
          outputKey: branch.when.outputKey,
          equals: branch.when.equals,
          truthy: branch.when.truthy
        };
        nodes.push(node);
        if (i > 0) edges.push({ from: base + i - 1, to: base + i });
      }
      if (nodes.length > base) edges.push({ from: Math.max(0, lastLinear), to: base });
      lastLinear = nodes.length - 1;
    }
  }
  return { nodes, edges };
}
var init_recipe_compiler = __esm({
  "src/engines/harness/recipe-compiler.ts"() {
    "use strict";
  }
});

// src/engines/automation/orchestrator.ts
var orchestrator_exports = {};
__export(orchestrator_exports, {
  AutomationOrchestrator: () => AutomationOrchestrator
});
var AutomationOrchestrator;
var init_orchestrator = __esm({
  "src/engines/automation/orchestrator.ts"() {
    "use strict";
    init_errors();
    init_recipes();
    init_recipe_compiler();
    init_agents();
    AutomationOrchestrator = class {
      constructor(governor) {
        this.governor = governor;
      }
      governor;
      /** List available composite recipes (for catalog/help). */
      listRecipes() {
        return RECIPES;
      }
      /** Run an automation goal via a recipe + agent role. */
      async run(goal) {
        const role = getAgentRole(goal.role);
        const recipe2 = goal.recipeId ? getRecipe(goal.recipeId) : getRecipe(role.defaultRecipe);
        if (!recipe2) throw new EngineError(`Unknown recipe: ${goal.recipeId ?? role.defaultRecipe}`);
        this.assertTrust(role.trust, goal);
        const bound = this.bindParams(recipe2, goal.params);
        const dag = compileRecipe(bound);
        const slave = await this.governor.ensureGenericBrowser();
        const result = await this.governor.runHarnessPlan(slave.slaveId, dag);
        if (!result.success) {
          const failed = dag.nodes[result.stepsCompleted]?.action ?? "?";
          throw new EngineError(`Automation failed at step ${failed}: ${result.error ?? "unknown"}`);
        }
        const observations = [
          { kind: "stepsCompleted", data: result.stepsCompleted },
          ...result.capturedBody ? [{ kind: "capturedBody", data: result.capturedBody }] : []
        ];
        return {
          role: role.id,
          recipeId: recipe2.id,
          steps: dag.nodes.length,
          observations,
          output: this.finalize(role, observations),
          trustLevel: role.trust.level,
          humanGated: role.trust.humanGate
        };
      }
      /** Interpolate {{key}} placeholders in a recipe's steps from goal.params. */
      bindParams(recipe2, params) {
        const interpolate = (v) => {
          if (typeof v === "string") {
            return v.replace(/\{\{(\w+)\}\}/g, (_, k) => params[k] ?? `{{${k}}}`);
          }
          return v;
        };
        const steps = recipe2.steps.map((step) => {
          const out = {};
          for (const [k, val] of Object.entries(step)) out[k] = interpolate(val);
          return out;
        });
        return { ...recipe2, steps };
      }
      assertTrust(trust, goal) {
        if (goal.destructive && trust.level === "read") {
          throw new EngineError(`Role trust=${trust.level} forbids destructive goal`);
        }
        if (goal.destructive && trust.requiresConfirmation && !goal.params.__confirmed) {
          throw new EngineError(`Destructive goal requires explicit confirmation (role=${trust.level})`);
        }
      }
      finalize(role, observations) {
        switch (role.output.aggregate) {
          case "report":
            return { kind: "report", sections: observations.length, format: role.output.format };
          case "collection":
            return observations.map((o) => o.data);
          case "snapshot":
            return observations[observations.length - 1]?.data ?? null;
          default:
            return observations[0]?.data ?? null;
        }
      }
    };
  }
});

// src/canvas/canvas-agent-tools.ts
function canvasCap(partial, handler) {
  return {
    ...partial,
    surfaces: [...ALL_SURFACES2],
    // The UI binding the audit (and runtime) look for is `ui`; `uiAction` is a
    // canvas-specific alias. Mirror it so the capability is bound on the UI
    // surface and the command-surface audit stays green.
    ...partial.uiAction ? {
      ui: {
        component: partial.uiAction.component,
        position: partial.uiAction.position,
        order: partial.uiAction.order
      }
    } : {},
    handler,
    isAsync: true,
    requiresConfirmation: false,
    tags: ["canvas"]
  };
}
function registerCanvasCapabilities(registry2, svc) {
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:spawn",
        slug: "canvas_spawn",
        name: "Spawn Canvas",
        description: "Spawn a layer instance from a definition (on demand, P3).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: { definitionId: { type: "string" } },
          required: ["definitionId"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas spawn",
          aliases: ["cs"],
          examples: ["canvas spawn --definitionId def:chat"]
        },
        uiAction: { component: "canvas-spawn", position: "toolbar", order: 1 },
        mcpToolName: "canvas_spawn",
        apiEndpoint: { method: "POST", path: "/api/canvas/spawn" }
      },
      async (i, ctx) => svc.mounter.spawn(String(i.definitionId), { spawnedBy: ctx.userId ? "user" : "agent" })
    )
  );
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:dismiss",
        slug: "canvas_dismiss",
        name: "Dismiss Canvas",
        description: "Dismiss a layer instance, releasing DOM + bindings (P3).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: { instanceId: { type: "string" } },
          required: ["instanceId"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas dismiss",
          aliases: ["cd"],
          examples: ["canvas dismiss --instanceId inst:chat:01"]
        },
        uiAction: { component: "canvas-dismiss", position: "toolbar", order: 2 },
        mcpToolName: "canvas_dismiss",
        apiEndpoint: { method: "DELETE", path: "/api/canvas/instance/{id}" }
      },
      async (i) => {
        await svc.mounter.dismiss(String(i.instanceId));
        return { ok: true };
      }
    )
  );
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:mutate",
        slug: "canvas_mutate",
        name: "Mutate Canvas",
        description: "Apply a structured region mutation to a live layer (mirror, P2).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            instanceId: { type: "string" },
            regionId: { type: "string" },
            state: { type: "object" }
          },
          required: ["instanceId", "regionId", "state"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas mutate",
          aliases: ["cm"],
          examples: ["canvas mutate --instanceId inst:1 --regionId body"]
        },
        uiAction: { component: "canvas-mutate", position: "toolbar", order: 3 },
        mcpToolName: "canvas_mutate",
        apiEndpoint: { method: "POST", path: "/api/canvas/instance/{id}/mutate" }
      },
      async (i) => svc.mirror.pushOptimistic(String(i.instanceId), String(i.regionId), i.state)
    )
  );
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:observe",
        slug: "canvas_observe",
        name: "Observe Canvas",
        description: "Snapshot oracle visibility or list live layer regions (P4/P9).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            op: { type: "string", enum: ["oracle", "manifest"] }
          }
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas observe",
          aliases: ["co"],
          examples: ["canvas observe --op oracle"]
        },
        uiAction: { component: "canvas-observe", position: "toolbar", order: 4 },
        mcpToolName: "canvas_observe",
        apiEndpoint: { method: "GET", path: "/api/canvas/observe" }
      },
      async (i) => {
        const op = i.op ?? "oracle";
        return op === "manifest" ? { manifest: await svc.oracle.buildManifest() } : { oracle: await svc.oracle.visibility() };
      }
    )
  );
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:define",
        slug: "canvas_define",
        name: "Define Canvas",
        description: "Publish a new layer definition from a draft (design-from-within, P9).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            slug: { type: "string" },
            name: { type: "string" },
            category: { type: "string" },
            html: { type: "string" },
            bindings: { type: "array" },
            layout: { type: "object" }
          },
          required: ["slug", "name", "html", "bindings", "layout"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas define",
          aliases: ["cdef"],
          examples: ['canvas define --slug my-layer --name "My Layer"']
        },
        uiAction: { component: "canvas-define", position: "toolbar", order: 5 },
        mcpToolName: "canvas_define",
        apiEndpoint: { method: "POST", path: "/api/canvas/definitions" }
      },
      async (i, ctx) => svc.designer.publish(
        {
          slug: String(i.slug),
          name: String(i.name),
          description: i.description ?? "",
          category: i.category ?? "plugin",
          html: String(i.html),
          css: i.css ?? "",
          scriptUrl: i.scriptUrl ?? void 0,
          bindings: i.bindings ?? [],
          layout: i.layout,
          sandbox: i.sandbox ?? void 0,
          tags: i.tags ?? []
        },
        ctx.userId ? "user" : "agent"
      )
    )
  );
  registry2.register(
    canvasCap(
      {
        id: "cap:canvas:list",
        slug: "canvas_list",
        name: "List Canvases",
        description: "List canvas definitions and live instances (P1/P3).",
        category: "canvas",
        inputSchema: { type: "object", properties: {} },
        outputSchema: { type: "object" },
        cliCommand: { name: "canvas list", aliases: ["cls"], examples: ["canvas list"] },
        uiAction: { component: "canvas-list", position: "sidebar", order: 1 },
        mcpToolName: "canvas_list",
        apiEndpoint: { method: "GET", path: "/api/canvas/definitions" }
      },
      async () => ({
        definitions: await svc.registry.list(),
        instances: await svc.mounter.list()
      })
    )
  );
  if (svc.kernelOracle?.query) {
    const query = svc.kernelOracle.query;
    registry2.register(
      canvasCap(
        {
          id: "cap:kernel:query",
          slug: "kernel_query",
          name: "Kernel Query",
          description: "Query kernel oracle (health, topology, capabilities, config, all)",
          category: "kernel",
          inputSchema: {
            type: "object",
            properties: {
              op: { type: "string", enum: ["health", "topology", "capability", "config", "all"] },
              filter: { type: "object" },
              limit: { type: "number" }
            }
          },
          outputSchema: { type: "object" },
          cliCommand: {
            name: "kernel query",
            aliases: ["koq"],
            examples: ["kernel query --op health"]
          },
          mcpToolName: "kernel_query",
          apiEndpoint: { method: "POST", path: "/api/kernel/oracle/query" },
          uiAction: { component: "kernel-query", position: "toolbar", order: 90 }
        },
        async (i) => query.query({
          type: i.op ?? "all",
          filter: i.filter,
          limit: i.limit
        })
      )
    );
  }
  registry2.register(
    canvasCap(
      {
        id: "cap:kernel:visibility",
        slug: "kernel_visibility",
        name: "Kernel Visibility",
        description: "Get oracle visibility snapshot (providers, engines, health)",
        category: "kernel",
        inputSchema: { type: "object", properties: {} },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "kernel visibility",
          aliases: ["kov"],
          examples: ["kernel visibility"]
        },
        uiAction: { component: "kernel-visibility", position: "toolbar", order: 100 },
        mcpToolName: "kernel_visibility",
        apiEndpoint: { method: "GET", path: "/api/kernel/oracle/visibility" }
      },
      async () => ({ visibility: await svc.oracle.visibility() })
    )
  );
  if (svc.kernelOracle?.actuator) {
    const actuator = svc.kernelOracle.actuator;
    registry2.register(
      canvasCap(
        {
          id: "cap:kernel:heal",
          slug: "kernel_heal",
          name: "Kernel Heal",
          description: "Trigger oracle self-healing for an issue",
          category: "kernel",
          inputSchema: {
            type: "object",
            properties: { issueId: { type: "string" } },
            required: ["issueId"]
          },
          outputSchema: { type: "object" },
          cliCommand: {
            name: "kernel heal",
            aliases: ["koh"],
            examples: ["kernel heal --issueId issue:123"]
          },
          mcpToolName: "kernel_heal",
          apiEndpoint: { method: "POST", path: "/api/kernel/oracle/heal" },
          uiAction: { component: "kernel-heal", position: "toolbar", order: 110 }
        },
        async (i) => actuator.heal(String(i.issueId))
      )
    );
  }
  if (svc.configSurface) {
    const configSurface = svc.configSurface;
    registry2.register(
      canvasCap(
        {
          id: "cap:config:list",
          slug: "config_list",
          name: "Config List",
          description: "List all config scopes",
          category: "config",
          inputSchema: { type: "object", properties: {} },
          outputSchema: { type: "object" },
          cliCommand: {
            name: "kernel config list",
            aliases: ["kcl"],
            examples: ["kernel config list"]
          },
          mcpToolName: "config_list",
          apiEndpoint: { method: "GET", path: "/api/kernel/config/scopes" },
          uiAction: { component: "config-list", position: "toolbar", order: 120 }
        },
        async () => ({ scopes: configSurface.listScopes() })
      )
    );
    registry2.register(
      canvasCap(
        {
          id: "cap:config:get",
          slug: "config_get",
          name: "Config Get",
          description: "Get a config value (scope.key format)",
          category: "config",
          inputSchema: {
            type: "object",
            properties: { key: { type: "string" } },
            required: ["key"]
          },
          outputSchema: { type: "object" },
          cliCommand: {
            name: "kernel config get",
            aliases: ["kcg"],
            examples: ["kernel config get autoheal.stalledEngineRestart.enabled"]
          },
          mcpToolName: "config_get",
          apiEndpoint: { method: "GET", path: "/api/kernel/config/{scope}/{key}" },
          uiAction: { component: "config-get", position: "toolbar", order: 121 }
        },
        async (i) => {
          const [scope, key] = (i.key ?? "").split(".");
          return { value: configSurface.get(scope ?? "", key ?? "") };
        }
      )
    );
    registry2.register(
      canvasCap(
        {
          id: "cap:config:set",
          slug: "config_set",
          name: "Config Set",
          description: "Set a config value (scope.key value format)",
          category: "config",
          inputSchema: {
            type: "object",
            properties: {
              key: { type: "string" },
              value: { type: "unknown" }
            },
            required: ["key", "value"]
          },
          outputSchema: { type: "object" },
          cliCommand: {
            name: "kernel config set",
            aliases: ["kcset"],
            examples: ["kernel config set autoheal.stalledEngineRestart.enabled true"]
          },
          mcpToolName: "config_set",
          apiEndpoint: { method: "PUT", path: "/api/kernel/config/{scope}/{key}" },
          uiAction: { component: "config-set", position: "toolbar", order: 122 }
        },
        async (i) => {
          const [scope, key] = (i.key ?? "").split(".");
          return configSurface.set(scope ?? "", key ?? "", i.value);
        }
      )
    );
  }
}
var ALL_SURFACES2;
var init_canvas_agent_tools = __esm({
  "src/canvas/canvas-agent-tools.ts"() {
    "use strict";
    ALL_SURFACES2 = ["cli", "ui", "workflow", "mcp", "api"];
  }
});

// src/canvas/canvas-mirror.ts
var LATENCY_BUDGETS, CanvasMirror, InMemoryCanvasMirrorStore;
var init_canvas_mirror = __esm({
  "src/canvas/canvas-mirror.ts"() {
    "use strict";
    LATENCY_BUDGETS = {
      bind: 5,
      capture: 3e4,
      store: 10,
      emit: 5,
      reconcile: 200
    };
    CanvasMirror = class {
      constructor(store, history = []) {
        this.store = store;
        this.history = history;
      }
      store;
      history;
      /** Unit 26.5: Push a mutation entry to history log. */
      pushMutation(entry) {
        this.history.push(entry);
        if (this.history.length > 100) this.history.shift();
      }
      /** Unit 26.5: Get mutation history for an instance. */
      getHistory(instanceId, limit = 20) {
        return this.history.filter((e) => e.instanceId === instanceId).slice(-limit).reverse();
      }
      /** Unit 26.5: Undo last N mutations, returns states to restore. */
      undo(steps = 1) {
        const undone = [];
        for (let i = 0; i < steps && this.history.length > 0; i++) {
          const entry = this.history.pop();
          if (entry) {
            this.pushMutation({ ...entry, op: "undo" });
            undone.push(entry);
          }
        }
        return undone;
      }
      /**
       * Optimistic push: record the expected state immediately (instant UI), keep
       * it pending until `confirm` reconciles with the real store/primitive.
       */
      async pushOptimistic(instanceId, regionId, expectedState) {
        const update = {
          updateId: `opt:${instanceId}:${regionId}:${Date.now()}`,
          instanceId,
          regionId,
          expectedState,
          confirmed: false,
          createdAt: Date.now()
        };
        await this.store.upsertRegionState({
          instanceId,
          regionId,
          state: expectedState,
          confirmedAt: null
        });
        return update;
      }
      /** Confirm an optimistic update once the backing store/primitive agrees. */
      async confirm(instanceId, regionId, actualState) {
        await this.store.upsertRegionState({
          instanceId,
          regionId,
          state: actualState,
          confirmedAt: Date.now()
        });
      }
      /** Revert: drop the pending optimistic state, restore last confirmed. */
      async revert(instanceId, regionId, reason) {
        await this.store.upsertRegionState({
          instanceId,
          regionId,
          state: { reverted: true, reason },
          confirmedAt: null
        });
      }
      getRegionState(instanceId, regionId) {
        return this.store.getRegionState(instanceId, regionId);
      }
      listRegionStates(instanceId) {
        return this.store.listRegionStates(instanceId);
      }
      enforceBudget(stage, actualMs) {
        const budgetMs = LATENCY_BUDGETS[stage] ?? 100;
        return {
          withinBudget: actualMs <= budgetMs,
          actualMs,
          budgetMs
        };
      }
    };
    InMemoryCanvasMirrorStore = class _InMemoryCanvasMirrorStore {
      states = /* @__PURE__ */ new Map();
      key(instanceId, regionId) {
        return `${instanceId}::${regionId}`;
      }
      async upsertRegionState(s) {
        this.states.set(this.key(s.instanceId, s.regionId), s);
      }
      async getRegionState(instanceId, regionId) {
        return this.states.get(this.key(instanceId, regionId)) ?? null;
      }
      async listRegionStates(instanceId) {
        return Array.from(this.states.values()).filter((s) => s.instanceId === instanceId);
      }
      // For CanvasMirror constructor with history
      static create() {
        return new _InMemoryCanvasMirrorStore();
      }
    };
  }
});

// src/storage/contracts/canvas-store.ts
function rowToDefinition(row) {
  return {
    id: row.id,
    slug: row.slug,
    name: row.name,
    description: row.description,
    category: row.category,
    version: row.version,
    html: row.html,
    css: row.css,
    scriptUrl: row.scriptUrl ?? void 0,
    bindings: JSON.parse(row.bindingsJson),
    layout: JSON.parse(row.layoutJson),
    author: row.author,
    sandbox: JSON.parse(row.sandboxJson),
    status: row.status,
    tags: JSON.parse(row.tagsJson),
    createdAt: row.createdAt,
    updatedAt: row.updatedAt
  };
}
function definitionToRow(def) {
  return {
    id: def.id,
    slug: def.slug,
    name: def.name,
    description: def.description,
    category: def.category,
    version: def.version,
    html: def.html,
    css: def.css,
    scriptUrl: def.scriptUrl ?? null,
    bindingsJson: JSON.stringify(def.bindings),
    layoutJson: JSON.stringify(def.layout),
    author: def.author,
    sandboxJson: JSON.stringify(def.sandbox),
    status: def.status,
    tagsJson: JSON.stringify(def.tags),
    createdAt: def.createdAt,
    updatedAt: def.updatedAt
  };
}
var init_canvas_store = __esm({
  "src/storage/contracts/canvas-store.ts"() {
    "use strict";
  }
});

// src/canvas/schema.ts
import { z as z7 } from "zod";
function assertNoInlineScript(def) {
  if (INLINE_SCRIPT.test(def.html)) {
    throw new SandboxPermissionError("definition", "inline <script> in html");
  }
  if (INLINE_SCRIPT.test(def.css)) {
    throw new SandboxPermissionError("definition", "inline <script> in css");
  }
  if (def.scriptUrl?.startsWith("javascript:")) {
    throw new SandboxPermissionError("definition", "disallowed javascript: script url");
  }
}
function validateDefinition(input) {
  const parsed = canvasDefinitionFullSchema.parse(input);
  assertNoInlineScript(parsed);
  return parsed;
}
function canUseCapability(def, capabilitySlug) {
  const allowed = Array.isArray(def) ? def[0]?.allowCapabilities ?? [] : def.sandbox.allowCapabilities;
  return allowed.includes(capabilitySlug);
}
function defaultSandbox(overrides) {
  return {
    csp: "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'none';",
    allowNetwork: false,
    allowCapabilities: [],
    budgetMs: 5e3,
    allowInlineScript: false,
    ...overrides
  };
}
function finalizeDefinition(input, opts) {
  const now = opts.now ?? Date.now();
  return {
    ...input,
    id: opts.id,
    version: opts.version ?? 1,
    createdAt: now,
    updatedAt: now
  };
}
var primitiveKindSchema, layerCategorySchema, layerAuthorSchema, layerStatusSchema, sandboxPolicySchema, canvasLayoutSchema, layerBindingSchema, canvasDefinitionSchema, canvasDefinitionInputSchema, canvasDefinitionFullSchema, INLINE_SCRIPT;
var init_schema = __esm({
  "src/canvas/schema.ts"() {
    "use strict";
    init_errors();
    primitiveKindSchema = z7.enum([
      "workspace",
      "projects",
      "knowledge",
      "agents",
      "providers",
      "conversations"
    ]);
    layerCategorySchema = z7.enum([
      "system",
      "chat",
      "automation",
      "agents",
      "projects",
      "knowledge",
      "designer",
      "plugin"
    ]);
    layerAuthorSchema = z7.enum(["system", "user", "agent"]);
    layerStatusSchema = z7.enum(["draft", "published", "deprecated"]);
    sandboxPolicySchema = z7.object({
      csp: z7.string().min(1),
      allowNetwork: z7.boolean(),
      allowCapabilities: z7.array(z7.string()),
      budgetMs: z7.number().int().positive().max(3e4),
      allowInlineScript: z7.literal(false)
    }).strict();
    canvasLayoutSchema = z7.object({
      x: z7.number(),
      y: z7.number(),
      z: z7.number().int(),
      w: z7.number().positive(),
      h: z7.number().positive(),
      minimized: z7.boolean().optional(),
      detailZoom: z7.number().positive().optional()
    }).strict();
    layerBindingSchema = z7.object({
      regionId: z7.string().min(1),
      role: z7.string().min(1),
      selector: z7.string().min(1),
      primitive: primitiveKindSchema.optional(),
      capabilitySlug: z7.string().min(1).optional(),
      direction: z7.enum(["read", "write", "bidirectional"])
    }).strict().refine((b) => !(b.primitive && b.capabilitySlug), {
      message: "binding must not reference both a primitive and a capability"
    });
    canvasDefinitionSchema = z7.object({
      id: z7.string().min(1),
      slug: z7.string().min(1).regex(/^[a-z0-9-]+$/, "slug must be kebab-case"),
      name: z7.string().min(1),
      description: z7.string(),
      category: layerCategorySchema,
      version: z7.number().int().positive(),
      html: z7.string(),
      css: z7.string(),
      scriptUrl: z7.string().url().optional(),
      bindings: z7.array(layerBindingSchema),
      layout: canvasLayoutSchema,
      author: layerAuthorSchema,
      sandbox: sandboxPolicySchema,
      status: layerStatusSchema,
      tags: z7.array(z7.string()),
      createdAt: z7.number().int().positive(),
      updatedAt: z7.number().int().positive()
    }).strict();
    canvasDefinitionInputSchema = z7.object({
      id: z7.string().min(1).optional(),
      slug: z7.string().min(1).regex(/^[a-z0-9-]+$/, "slug must be kebab-case"),
      name: z7.string().min(1),
      description: z7.string(),
      category: layerCategorySchema,
      html: z7.string(),
      css: z7.string(),
      scriptUrl: z7.string().url().optional(),
      bindings: z7.array(layerBindingSchema),
      layout: canvasLayoutSchema,
      author: layerAuthorSchema,
      sandbox: sandboxPolicySchema,
      status: layerStatusSchema.default("draft"),
      tags: z7.array(z7.string()).default([])
    }).strict();
    canvasDefinitionFullSchema = canvasDefinitionInputSchema.extend({
      id: z7.string().min(1).optional(),
      version: z7.number().int().positive().optional(),
      createdAt: z7.number().int().positive().optional(),
      updatedAt: z7.number().int().positive().optional()
    }).strict();
    INLINE_SCRIPT = /<script\b/i;
  }
});

// src/canvas/canvas-registry.ts
var CanvasRegistry;
var init_canvas_registry = __esm({
  "src/canvas/canvas-registry.ts"() {
    "use strict";
    init_errors();
    init_ids();
    init_canvas_store();
    init_schema();
    CanvasRegistry = class {
      constructor(store) {
        this.store = store;
      }
      store;
      // API accepts domain types, converts to row format for storage
      async define(input) {
        const validated = validateDefinition(input);
        const def = finalizeDefinition(validated, {
          id: validated.id ?? `def:${validated.slug}:${newId()}`,
          version: 1
        });
        const row = definitionToRow(def);
        const created = await this.store.createDefinition(row);
        return rowToDefinition(created);
      }
      // Fetch a definition by id.
      async get(id) {
        const row = await this.store.getDefinition(id);
        return row ? rowToDefinition(row) : null;
      }
      // Fetch a definition by slug (stable handle for layers).
      async getBySlug(slug) {
        const row = await this.store.getDefinitionBySlug(slug);
        return row ? rowToDefinition(row) : null;
      }
      // List definitions, optionally filtered.
      async list(opts) {
        const rows = await this.store.listDefinitions(opts);
        return rows.map(rowToDefinition);
      }
      // Patch a definition. Re-validates the whole definition, rejects inline
      // scripts, and bumps version (immutability signal per 3.1).
      async update(id, patch) {
        const existing = await this.store.getDefinition(id);
        if (!existing) throw new EngineError(`definition ${id} not found`);
        const merged = {
          ...rowToDefinition(existing),
          ...patch,
          id
        };
        validateDefinition(merged);
        const row = definitionToRow(merged);
        const updated = await this.store.updateDefinition(id, row);
        return rowToDefinition(updated);
      }
      // Soft-retire a definition (keeps history; instances can still dismiss).
      async deprecate(id) {
        return this.update(id, { status: "deprecated" });
      }
      // Hard delete a definition.
      async delete(id) {
        await this.store.deleteDefinition(id);
      }
      // Produce a safe default sandbox for a draft layer and merge user overrides —
      // used by the designer surface so authored layers start least-privilege.
      static sandbox(overrides) {
        return defaultSandbox(overrides);
      }
      static fromRow(row) {
        return rowToDefinition(row);
      }
    };
  }
});

// src/canvas/capability-bridge.ts
var SandboxBridge;
var init_capability_bridge = __esm({
  "src/canvas/capability-bridge.ts"() {
    "use strict";
    init_errors();
    init_canvas_store();
    init_schema();
    SandboxBridge = class {
      constructor(store, executor, reader) {
        this.store = store;
        this.executor = executor;
        this.reader = reader;
      }
      store;
      executor;
      reader;
      ports = /* @__PURE__ */ new Map();
      defCache = /* @__PURE__ */ new Map();
      /** Attach a sandboxed port for a live instance. */
      attach(instanceId, port) {
        this.ports.set(instanceId, port);
        port.onMessage((msg) => {
          void this.dispatch(instanceId, msg);
        });
      }
      detach(instanceId) {
        this.ports.get(instanceId)?.close();
        this.ports.delete(instanceId);
        this.defCache.delete(instanceId);
      }
      /** Push a state-apply message to the layer (host → layer mirror sync). */
      pushState(instanceId, regionId, state) {
        this.ports.get(instanceId)?.postMessage({
          type: "bridge:state:apply",
          instanceId,
          regionId,
          state
        });
      }
      async getDef(instanceId) {
        const cached = this.defCache.get(instanceId);
        if (cached) return cached;
        const inst = await this.store.getInstance(instanceId);
        if (!inst) return null;
        const row = await this.store.getDefinition(inst.definitionId);
        if (!row) return null;
        const def = rowToDefinition(row);
        this.defCache.set(instanceId, def);
        return def;
      }
      async dispatch(instanceId, msg) {
        const port = this.ports.get(instanceId);
        if (!port) return;
        if (msg.type === "bridge:ready") {
          return;
        }
        if (msg.type === "bridge:capability:request") {
          const def = await this.getDef(instanceId);
          if (!def || !canUseCapability(def, msg.capability)) {
            port.postMessage({
              type: "bridge:capability:response",
              instanceId,
              requestId: msg.requestId,
              ok: false,
              error: `capability ${msg.capability} not permitted for this layer`
            });
            return;
          }
          try {
            const output = await this.executor.execute(msg.capability, msg.input, {
              userId: void 0,
              metadata: { instanceId }
            });
            port.postMessage({
              type: "bridge:capability:response",
              instanceId,
              requestId: msg.requestId,
              ok: true,
              output
            });
          } catch (err) {
            port.postMessage({
              type: "bridge:capability:response",
              instanceId,
              requestId: msg.requestId,
              ok: false,
              error: err instanceof Error ? err.message : String(err)
            });
          }
          return;
        }
        if (msg.type === "bridge:observe:request") {
          try {
            const data = await this.reader.read(msg.primitive, msg.query);
            port.postMessage({
              type: "bridge:observe:response",
              instanceId,
              requestId: msg.requestId,
              ok: true,
              data
            });
          } catch (err) {
            port.postMessage({
              type: "bridge:observe:response",
              instanceId,
              requestId: msg.requestId,
              ok: false,
              error: err instanceof Error ? err.message : String(err)
            });
          }
          return;
        }
        if (msg.type === "bridge:state:push") {
          this.onLayerState?.(instanceId, msg.regionId, msg.state);
          return;
        }
      }
      /** Sink for layer→host state pushes (wired to CanvasMirror by the engine). */
      onLayerState;
    };
  }
});

// src/canvas/designer.ts
var CanvasDesigner;
var init_designer = __esm({
  "src/canvas/designer.ts"() {
    "use strict";
    init_schema();
    CanvasDesigner = class _CanvasDesigner {
      constructor(registry2) {
        this.registry = registry2;
      }
      registry;
      /** Compute the least-privilege sandbox for a draft. */
      static sandboxFor(draft) {
        const boundCaps = draft.bindings.map((b) => b.capabilitySlug).filter((c) => !!c);
        return defaultSandbox({
          allowCapabilities: Array.from(new Set(boundCaps)),
          ...draft.sandbox
        });
      }
      /**
       * Publish a draft as a CanvasDefinition. The draft itself is validated
       * (closed: no inline script, well-formed bindings) before it lands.
       */
      async publish(draft, author = "user") {
        const sandbox = _CanvasDesigner.sandboxFor(draft);
        const input = {
          slug: draft.slug,
          name: draft.name,
          description: draft.description ?? "",
          category: draft.category,
          html: draft.html,
          css: draft.css ?? "",
          scriptUrl: draft.scriptUrl,
          bindings: draft.bindings,
          layout: draft.layout,
          author,
          sandbox,
          status: "published",
          tags: draft.tags ?? []
        };
        validateDefinition(input);
        return this.registry.define(input);
      }
      /**
       * Preview a draft without persisting — returns the would-be definition so a
       * designer surface can render it live on the canvas before publishing.
       */
      preview(draft) {
        const sandbox = _CanvasDesigner.sandboxFor(draft);
        const now = Date.now();
        return {
          id: `preview:${draft.slug}`,
          slug: draft.slug,
          name: draft.name,
          description: draft.description ?? "",
          category: draft.category,
          version: 1,
          html: draft.html,
          css: draft.css ?? "",
          scriptUrl: draft.scriptUrl,
          bindings: draft.bindings,
          layout: draft.layout,
          author: "user",
          sandbox,
          status: "draft",
          tags: draft.tags ?? [],
          createdAt: now,
          updatedAt: now
        };
      }
    };
  }
});

// src/canvas/layer-mounter.ts
function rowToInstance(row) {
  return {
    instanceId: row.instanceId,
    definitionId: row.definitionId,
    slug: row.slug,
    category: row.category,
    status: row.status,
    hostNodeId: row.hostNodeId,
    bindingsActive: JSON.parse(row.bindingsActiveJson),
    spawnedBy: row.spawnedBy,
    mountedAt: row.mountedAt,
    dismissedAt: row.dismissedAt ?? void 0
  };
}
var LayerMounter;
var init_layer_mounter = __esm({
  "src/canvas/layer-mounter.ts"() {
    "use strict";
    init_errors();
    init_ids();
    LayerMounter = class {
      constructor(store, host, registry2) {
        this.store = store;
        this.host = host;
        this.registry = registry2;
      }
      store;
      host;
      registry;
      /**
       * Spawn a layer instance on demand. The definition is fetched, an instance
       * record is written (mounting), the host mounts it, and it flips to live.
       * Nothing all-at-once: only requested layers instantiate (P3).
       */
      async spawn(definitionId, opts = {}) {
        const def = await this.registry.get(definitionId);
        if (!def) {
          throw new CanvasSpawnError(`definition ${definitionId} not found`);
        }
        if (def.status === "deprecated") {
          throw new CanvasSpawnError(`definition ${definitionId} is deprecated`);
        }
        const instanceId = `inst:${def.slug}:${newId()}`;
        const hostNodeId = opts.hostNodeId ?? instanceId;
        await this.store.createInstance({
          instanceId,
          definitionId: def.id,
          slug: def.slug,
          category: def.category,
          status: "mounting",
          hostNodeId,
          bindingsActiveJson: JSON.stringify([]),
          spawnedBy: opts.spawnedBy ?? "system",
          mountedAt: Date.now()
        });
        try {
          await this.host.mount(instanceId, def);
        } catch (err) {
          await this.store.updateInstance(instanceId, { status: "error" });
          throw new CanvasSpawnError(
            `mount failed: ${err instanceof Error ? err.message : String(err)}`
          );
        }
        const live = await this.store.updateInstance(instanceId, { status: "live" });
        return rowToInstance(live);
      }
      /** List live/mounting instances (what is currently instantiated). */
      async list(opts) {
        const rows = await this.store.listInstances(opts ? { status: opts.status } : void 0);
        return rows.map(rowToInstance);
      }
      getInstance(instanceId) {
        return this.store.getInstance(instanceId).then((r) => r ? rowToInstance(r) : null);
      }
      /**
       * Record that a binding came alive for the instance (called by the bridge
       * once a region capability/primitive channel is established).
       */
      async markBindingActive(instanceId, regionId) {
        const inst = await this.store.getInstance(instanceId);
        if (!inst) return;
        const active = JSON.parse(inst.bindingsActiveJson);
        if (!active.includes(regionId)) {
          active.push(regionId);
          await this.store.updateInstance(instanceId, {
            bindingsActiveJson: JSON.stringify(active)
          });
        }
      }
      /**
       * Dismiss an instance: release DOM + bindings, keep the definition. The
       * canvas can *describe* a thousand layers while *instantiating* three (P3).
       */
      async dismiss(instanceId) {
        const inst = await this.store.getInstance(instanceId);
        if (!inst) return;
        await this.host.unmount(instanceId);
        await this.store.updateInstance(instanceId, {
          status: "dismissed",
          dismissedAt: Date.now()
        });
      }
      /** True if the host currently holds the instance. */
      isMounted(instanceId) {
        return this.host.isMounted(instanceId);
      }
    };
  }
});

// src/canvas/mutation-caps.ts
function makeMutationCap(partial, handler) {
  return {
    ...partial,
    surfaces: ["cli", "ui", "workflow", "mcp", "api"],
    handler,
    isAsync: true,
    requiresConfirmation: partial.requiresConfirmation ?? false,
    tags: ["canvas", "mutation"]
  };
}
function pushMutation(svc, op, instanceId, regionId, diff) {
  svc.mirror.pushMutation({
    op,
    instanceId,
    regionId,
    diff,
    timestamp: Date.now(),
    by: "api"
  });
}
function registerCanvasMutationCaps(registry2, svc) {
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:set_background",
        slug: "canvas_set_background",
        name: "Set Canvas Background",
        description: "Change the canvas background image (by base64 or query).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            imageBase64: { type: "string" },
            imageQuery: { type: "string" },
            layerId: { type: "string" }
          }
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas set background",
          aliases: ["csb"],
          examples: ['canvas set background --imageQuery "moon made of cheese"']
        },
        ui: { component: "action-button", position: "composer", order: 10 },
        mcpToolName: "canvas_set_background",
        apiEndpoint: { method: "POST", path: "/api/canvas/background" }
      },
      async (input, _ctx) => {
        const { imageBase64, imageQuery, layerId } = input;
        let dataUrl = imageBase64;
        if (!dataUrl && imageQuery && svc.imageGen) {
          const result = await svc.imageGen.generateImage(imageQuery);
          dataUrl = result.dataUrl;
        }
        if (!dataUrl) {
          throw new Error("Either imageBase64 or imageQuery must be provided");
        }
        const iid = layerId ?? "canvas";
        const diff = { imageBase64: dataUrl };
        pushMutation(svc, "set_background", iid, "background", diff);
        svc.eventBus?.emit({
          type: "canvas:mutated",
          instanceId: iid,
          regionId: "background",
          op: "set_background",
          diff
        });
        return { ok: true, dataUrl };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:add_layer",
        slug: "canvas_add_layer",
        name: "Add Canvas Layer",
        description: "Add a new layer to the canvas.",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            kind: { type: "string" },
            title: { type: "string" },
            config: { type: "object" }
          },
          required: ["kind"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas add layer",
          aliases: ["cal"],
          examples: ["canvas add layer --kind chat"]
        },
        ui: { component: "action-button", position: "composer", order: 11 },
        mcpToolName: "canvas_add_layer",
        apiEndpoint: { method: "POST", path: "/api/canvas/layers" }
      },
      async (input, ctx) => {
        const inp = input;
        const { kind, title, config: config2 } = inp;
        const slug = `layer:${kind}:${Date.now()}`;
        const draft = {
          slug,
          name: title ?? `${kind} layer`,
          category: "plugin",
          html: '<div data-region="layer-root" role="layer-root"></div>',
          bindings: [],
          layout: { x: 100, y: 100, z: 100, w: 400, h: 400 }
        };
        const def = await svc.designer.publish(draft, ctx.userId ? "user" : "agent");
        const instance2 = await svc.mounter.spawn(def.id, {
          spawnedBy: ctx.userId ? "user" : "agent"
        });
        const diff = { kind, title, config: config2, definitionId: def.id, instanceId: instance2.instanceId };
        pushMutation(svc, "add_layer", instance2.instanceId, "layer-added", diff);
        svc.eventBus?.emit({
          type: "canvas:mutated",
          instanceId: instance2.instanceId,
          regionId: "layer-added",
          op: "add_layer",
          diff
        });
        return { ok: true, definitionId: def.id, instanceId: instance2.instanceId };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:remove_layer",
        slug: "canvas_remove_layer",
        name: "Remove Canvas Layer",
        description: "Remove a layer from the canvas.",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            layerId: { type: "string" }
          },
          required: ["layerId"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas remove layer",
          aliases: ["crl"],
          examples: ["canvas remove layer --layerId inst:chat:abc"]
        },
        ui: { component: "action-button", position: "composer", order: 12 },
        mcpToolName: "canvas_remove_layer",
        apiEndpoint: { method: "DELETE", path: "/api/canvas/layers/{layerId}" },
        requiresConfirmation: true
      },
      async (input, _ctx) => {
        const { layerId } = input;
        await svc.mounter.dismiss(layerId);
        const diff = { layerId };
        pushMutation(svc, "remove_layer", layerId, "layer-removed", diff);
        svc.eventBus?.emit({
          type: "canvas:mutated",
          instanceId: layerId,
          regionId: "layer-removed",
          op: "remove_layer",
          diff
        });
        return { ok: true };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:set_layout",
        slug: "canvas_set_layout",
        name: "Set Canvas Layout",
        description: "Change the canvas layout mode.",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            layout: { type: "string", enum: ["grid", "list", "freeform"] }
          },
          required: ["layout"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas set layout",
          aliases: ["csl"],
          examples: ["canvas set layout --layout grid"]
        },
        ui: { component: "action-button", position: "composer", order: 13 },
        mcpToolName: "canvas_set_layout",
        apiEndpoint: { method: "POST", path: "/api/canvas/layout" }
      },
      async (input, _ctx) => {
        const { layout } = input;
        const diff = { layout };
        pushMutation(svc, "set_layout", "canvas", "layout", diff);
        svc.eventBus?.emit({
          type: "canvas:mutated",
          instanceId: "canvas",
          regionId: "layout",
          op: "set_layout",
          diff
        });
        return { ok: true, layout };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:set_theme",
        slug: "canvas_set_theme",
        name: "Set Canvas Theme",
        description: "Change the canvas theme (light/dark/auto).",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            theme: { type: "string", enum: ["light", "dark", "auto"] }
          },
          required: ["theme"]
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas set theme",
          aliases: ["cst"],
          examples: ["canvas set theme --theme dark"]
        },
        ui: { component: "action-button", position: "composer", order: 14 },
        mcpToolName: "canvas_set_theme",
        apiEndpoint: { method: "POST", path: "/api/canvas/theme" }
      },
      async (input, _ctx) => {
        const { theme } = input;
        const diff = { theme };
        pushMutation(svc, "set_theme", "canvas", "theme", diff);
        svc.eventBus?.emit({
          type: "canvas:mutated",
          instanceId: "canvas",
          regionId: "theme",
          op: "set_theme",
          diff
        });
        return { ok: true, theme };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:undo",
        slug: "canvas_undo",
        name: "Undo Canvas Mutations",
        description: "Undo the last N canvas mutations.",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            steps: { type: "number" },
            instanceId: { type: "string" }
          }
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas undo",
          aliases: ["cu"],
          examples: ["canvas undo", "canvas undo --steps 3"]
        },
        ui: { component: "action-button", position: "composer", order: 15 },
        mcpToolName: "canvas_undo",
        apiEndpoint: { method: "POST", path: "/api/canvas/undo" }
      },
      async (input, _ctx) => {
        const { steps } = input;
        const undone = svc.mirror.undo(steps ?? 1);
        for (const entry of undone) {
          svc.eventBus?.emit({
            type: "canvas:mutated",
            instanceId: entry.instanceId,
            regionId: entry.regionId,
            op: "undo",
            diff: entry.diff
          });
        }
        return { ok: true, undone: undone.length };
      }
    )
  );
  registry2.register(
    makeMutationCap(
      {
        id: "cap:canvas:history",
        slug: "canvas_history",
        name: "Canvas Mutation History",
        description: "Get the mutation history for a canvas instance.",
        category: "canvas",
        inputSchema: {
          type: "object",
          properties: {
            limit: { type: "number" },
            instanceId: { type: "string" }
          }
        },
        outputSchema: { type: "object" },
        cliCommand: {
          name: "canvas history",
          aliases: ["ch"],
          examples: ["canvas history", "canvas history --limit 10"]
        },
        ui: { component: "action-button", position: "composer", order: 16 },
        mcpToolName: "canvas_history",
        apiEndpoint: { method: "GET", path: "/api/canvas/history" }
      },
      async (input, _ctx) => {
        const { limit, instanceId } = input;
        const iid = instanceId ?? "canvas";
        const history = svc.mirror.getHistory(iid, limit ?? 20);
        return { ok: true, history };
      }
    )
  );
}
var init_mutation_caps = __esm({
  "src/canvas/mutation-caps.ts"() {
    "use strict";
  }
});

// src/canvas/types.ts
var PRIMITIVE_KINDS;
var init_types2 = __esm({
  "src/canvas/types.ts"() {
    "use strict";
    PRIMITIVE_KINDS = [
      "workspace",
      "projects",
      "knowledge",
      "agents",
      "providers",
      "conversations"
    ];
  }
});

// src/canvas/oracle-reader.ts
var OracleReader;
var init_oracle_reader = __esm({
  "src/canvas/oracle-reader.ts"() {
    "use strict";
    init_types2();
    OracleReader = class {
      constructor(sources) {
        this.sources = sources;
      }
      sources;
      /** Total visibility snapshot of the whole system (P4). */
      async visibility() {
        return this.sources.visibility.visibility();
      }
      /**
       * Build the living manifest (P9): a machine-readable description of every
       * definition's regions, the open instances, and current oracle visibility.
       * Agents ground their canvas mutations in this instead of brittle coordinates.
       */
      async buildManifest() {
        const defs = await this.sources.listDefinitions();
        const visibility = await this.sources.visibility.visibility();
        const _instances = await this.sources.listInstances({ status: "live" });
        const definitions = defs.map((def) => ({
          definitionId: def.id,
          slug: def.slug,
          category: def.category,
          regions: def.bindings.map((b) => ({
            regionId: b.regionId,
            role: b.role,
            selector: b.selector,
            boundPrimitive: b.primitive,
            boundCapability: b.capabilitySlug,
            readScope: def.category === "system" ? "oracle" : "scoped"
          }))
        }));
        return {
          version: 1,
          generatedAt: Date.now(),
          definitions,
          oracle: visibility
        };
      }
      /** Confirm a manifest region binds only to the closed primitive set (P6). */
      static regionIsWellFormed(region) {
        if (region.boundPrimitive) {
          return PRIMITIVE_KINDS.includes(region.boundPrimitive);
        }
        return true;
      }
    };
  }
});

// src/canvas/primitives.ts
function fnPrimitive(kind, fn) {
  return { kind, read: fn };
}
var CorePrimitiveRegistry;
var init_primitives = __esm({
  "src/canvas/primitives.ts"() {
    "use strict";
    init_types2();
    CorePrimitiveRegistry = class {
      providers = /* @__PURE__ */ new Map();
      register(provider) {
        if (this.providers.has(provider.kind)) {
          throw new Error(`primitive ${provider.kind} already registered`);
        }
        this.providers.set(provider.kind, provider);
      }
      unregister(kind) {
        this.providers.delete(kind);
      }
      has(kind) {
        return this.providers.has(kind);
      }
      kinds() {
        return PRIMITIVE_KINDS;
      }
      /** A `PrimitiveReader` bound to this registry — what layers/canvas consume. */
      reader() {
        return {
          read: async (kind, query) => {
            const provider = this.providers.get(kind);
            if (!provider) {
              throw new Error(`no provider registered for primitive ${kind}`);
            }
            return provider.read(query);
          }
        };
      }
      getProvider(kind) {
        return this.providers.get(kind) ?? null;
      }
    };
  }
});

// src/canvas/canvas-engine.ts
var canvas_engine_exports = {};
__export(canvas_engine_exports, {
  CanvasEngine: () => CanvasEngine
});
var CanvasEngine, CORE_LAYER_SEED;
var init_canvas_engine = __esm({
  "src/canvas/canvas-engine.ts"() {
    "use strict";
    init_canvas_agent_tools();
    init_canvas_mirror();
    init_canvas_registry();
    init_capability_bridge();
    init_designer();
    init_layer_mounter();
    init_mutation_caps();
    init_oracle_reader();
    init_primitives();
    init_schema();
    CanvasEngine = class {
      registry;
      mounter;
      mirror;
      bridge;
      oracle;
      primities;
      designer;
      store;
      host;
      eventBus;
      constructor(deps) {
        this.store = deps.store;
        this.host = deps.host;
        this.eventBus = deps.eventBus;
        this.registry = new CanvasRegistry(deps.store);
        this.mounter = new LayerMounter(deps.store, deps.host, this.registry);
        this.mirror = new CanvasMirror(deps.mirrorStore ?? new InMemoryCanvasMirrorStore());
        this.bridge = new SandboxBridge(deps.store, deps.executor, {
          read: async () => null
          // wired to primities.reader() below
        });
        this.primities = new CorePrimitiveRegistry();
        for (const p of deps.primities ?? []) this.primities.register(p);
        this.bridge = new SandboxBridge(deps.store, deps.executor, this.primities.reader());
        this.oracle = new OracleReader({
          visibility: deps.oracle,
          listDefinitions: () => this.registry.list(),
          listInstances: (opts) => this.mounter.list(opts)
        });
        this.designer = new CanvasDesigner(this.registry);
      }
      /** Register all canvas capabilities into the unified registry (P5). */
      registerCapabilities(registry2) {
        registerCanvasCapabilities(registry2, {
          registry: this.registry,
          mounter: this.mounter,
          mirror: this.mirror,
          oracle: this.oracle,
          designer: this.designer
        });
        registerCanvasMutationCaps(registry2, {
          registry: this.registry,
          mounter: this.mounter,
          mirror: this.mirror,
          designer: this.designer,
          eventBus: this.eventBus
        });
      }
      /** Seed the closed core layer set if absent (vision §3.2 seed table). */
      async seedCoreLayers() {
        const seeded = [];
        for (const def of CORE_LAYER_SEED) {
          const existing = await this.registry.getBySlug(def.slug);
          if (!existing) seeded.push(await this.registry.define(def));
        }
        return seeded;
      }
    };
    CORE_LAYER_SEED = [
      {
        slug: "system",
        name: "System",
        description: "Visual map of what is open; core settings; engine health.",
        category: "system",
        html: '<div data-region="system-map" role="system-map"></div>',
        css: "",
        bindings: [
          {
            regionId: "system-map",
            role: "system-map",
            selector: '[data-region="system-map"]',
            direction: "read"
          }
        ],
        layout: { x: 0, y: 0, z: 0, w: 480, h: 360 },
        author: "system",
        sandbox: defaultSandbox({ allowCapabilities: [] }),
        status: "published",
        tags: ["core", "oracle"]
      },
      {
        slug: "chat",
        name: "Chat",
        description: "Traditional conversation surface.",
        category: "chat",
        html: '<div data-region="chat-thread" role="chat-thread"></div><textarea data-region="chat-input" role="chat-input"></textarea>',
        css: "",
        bindings: [
          {
            regionId: "chat-thread",
            role: "chat-thread",
            selector: '[data-region="chat-thread"]',
            primitive: "conversations",
            direction: "bidirectional"
          },
          {
            regionId: "chat-input",
            role: "chat-input",
            selector: '[data-region="chat-input"]',
            capabilitySlug: "conversation_create",
            direction: "write"
          }
        ],
        layout: { x: 520, y: 0, z: 1, w: 480, h: 520 },
        author: "system",
        sandbox: defaultSandbox({ allowCapabilities: ["conversation_create", "conversation_list"] }),
        status: "published",
        tags: ["core"]
      },
      {
        slug: "designer",
        name: "Designer",
        description: "Design layers from within the canvas.",
        category: "designer",
        html: '<form data-region="designer-form" role="designer-form"></form>',
        css: "",
        bindings: [
          {
            regionId: "designer-form",
            role: "designer-form",
            selector: '[data-region="designer-form"]',
            capabilitySlug: "canvas_define",
            direction: "write"
          }
        ],
        layout: { x: 0, y: 400, z: 2, w: 480, h: 420 },
        author: "system",
        sandbox: defaultSandbox({ allowCapabilities: ["canvas_define"] }),
        status: "published",
        tags: ["core", "reflexive"]
      }
    ];
  }
});

// src/canvas/in-memory-store.ts
var in_memory_store_exports = {};
__export(in_memory_store_exports, {
  InMemoryCanvasStore: () => InMemoryCanvasStore
});
var InMemoryCanvasStore;
var init_in_memory_store = __esm({
  "src/canvas/in-memory-store.ts"() {
    "use strict";
    InMemoryCanvasStore = class {
      defs = /* @__PURE__ */ new Map();
      bySlug = /* @__PURE__ */ new Map();
      instances = /* @__PURE__ */ new Map();
      async createDefinition(input) {
        if (this.bySlug.has(input.slug)) {
          throw new Error(`slug ${input.slug} already exists`);
        }
        const now = input.createdAt ?? Date.now();
        const row = {
          id: input.id ?? `def:${input.slug}`,
          slug: input.slug,
          name: input.name,
          description: input.description,
          category: input.category,
          version: input.version ?? 1,
          html: input.html,
          css: input.css,
          scriptUrl: input.scriptUrl ?? null,
          bindingsJson: input.bindingsJson,
          layoutJson: input.layoutJson,
          author: input.author,
          sandboxJson: input.sandboxJson,
          status: input.status,
          tagsJson: input.tagsJson,
          createdAt: now,
          updatedAt: input.updatedAt ?? now
        };
        this.defs.set(row.id, row);
        this.bySlug.set(row.slug, row.id);
        return row;
      }
      async getDefinition(id) {
        return this.defs.get(id) ?? null;
      }
      async getDefinitionBySlug(slug) {
        const id = this.bySlug.get(slug);
        return id ? this.defs.get(id) ?? null : null;
      }
      async listDefinitions(opts) {
        let rows = Array.from(this.defs.values());
        if (opts?.category) rows = rows.filter((r) => r.category === opts.category);
        if (opts?.author) rows = rows.filter((r) => r.author === opts.author);
        if (opts?.status) rows = rows.filter((r) => r.status === opts.status);
        return rows;
      }
      async updateDefinition(id, patch) {
        const row = this.defs.get(id);
        if (!row) throw new Error(`definition ${id} not found`);
        const next = {
          ...row,
          ...patch,
          version: row.version + 1,
          updatedAt: Date.now()
        };
        if (patch.slug && patch.slug !== row.slug) {
          this.bySlug.delete(row.slug);
          this.bySlug.set(patch.slug, id);
        }
        this.defs.set(id, next);
        return next;
      }
      async deleteDefinition(id) {
        const row = this.defs.get(id);
        if (row) {
          this.bySlug.delete(row.slug);
          this.defs.delete(id);
        }
      }
      async createInstance(input) {
        const row = { ...input, dismissedAt: input.dismissedAt ?? null };
        this.instances.set(row.instanceId, row);
        return row;
      }
      async getInstance(instanceId) {
        return this.instances.get(instanceId) ?? null;
      }
      async listInstances(opts) {
        let rows = Array.from(this.instances.values());
        if (opts?.status) rows = rows.filter((r) => r.status === opts.status);
        return rows;
      }
      async updateInstance(instanceId, patch) {
        const row = this.instances.get(instanceId);
        if (!row) throw new Error(`instance ${instanceId} not found`);
        const next = {
          ...row,
          ...patch,
          dismissedAt: patch.dismissedAt === void 0 ? row.dismissedAt : patch.dismissedAt
        };
        this.instances.set(instanceId, next);
        return next;
      }
      async deleteInstance(instanceId) {
        this.instances.delete(instanceId);
      }
    };
  }
});

// src/server/canvas-router.ts
var canvas_router_exports = {};
__export(canvas_router_exports, {
  createCanvasRouter: () => createCanvasRouter
});
function ctxFrom(req) {
  const userId = req.headers.get("x-user-id") ?? void 0;
  return { userId, metadata: {} };
}
function createCanvasRouter(ctx) {
  const registry2 = ctx.registry;
  if (!registry2) {
    return async (_req, _url) => errorResponse("Canvas not initialized", "CanvasUnavailable", 503);
  }
  return async (req, url) => {
    const _source = extractSource(req);
    const cap = (id, input) => registry2.execute(id, input, ctxFrom(req));
    if (url.pathname === "/api/canvas/definitions" && req.method === "GET") {
      try {
        return json({ ok: true, result: await cap("cap:canvas:list", {}) });
      } catch (e) {
        return errorResponse(e.message, "CanvasListFailed", 500);
      }
    }
    if (url.pathname === "/api/canvas/definitions" && req.method === "POST") {
      try {
        const body = await req.json();
        return json({ ok: true, result: await cap("cap:canvas:define", body) });
      } catch (e) {
        return errorResponse(e.message, "CanvasDefineFailed", 500);
      }
    }
    if (url.pathname === "/api/canvas/spawn" && req.method === "POST") {
      try {
        const body = await req.json();
        return json({ ok: true, result: await cap("cap:canvas:spawn", body) });
      } catch (e) {
        return errorResponse(e.message, "CanvasSpawnFailed", 500);
      }
    }
    if (url.pathname === "/api/canvas/observe" && req.method === "GET") {
      try {
        const op = url.searchParams.get("op") ?? "oracle";
        return json({ ok: true, result: await cap("cap:canvas:observe", { op }) });
      } catch (e) {
        return errorResponse(e.message, "CanvasObserveFailed", 500);
      }
    }
    const dismissMatch = url.pathname.match(/^\/api\/canvas\/instance\/([^/]+)$/);
    if (dismissMatch && req.method === "DELETE") {
      try {
        return json({
          ok: true,
          result: await cap("cap:canvas:dismiss", { instanceId: dismissMatch[1] })
        });
      } catch (e) {
        return errorResponse(e.message, "CanvasDismissFailed", 500);
      }
    }
    const mutateMatch = url.pathname.match(/^\/api\/canvas\/instance\/([^/]+)\/mutate$/);
    if (mutateMatch && req.method === "POST") {
      try {
        const body = await req.json();
        return json({
          ok: true,
          result: await cap("cap:canvas:mutate", {
            instanceId: mutateMatch[1],
            regionId: body.regionId,
            state: body.state
          })
        });
      } catch (e) {
        return errorResponse(e.message, "CanvasMutateFailed", 500);
      }
    }
    return errorResponse("Unknown canvas route", "NotFound", 404);
  };
}
var init_canvas_router = __esm({
  "src/server/canvas-router.ts"() {
    "use strict";
    init_response();
    init_source_middleware();
  }
});

// src/server/canvas-ws.ts
var canvas_ws_exports = {};
__export(canvas_ws_exports, {
  RegistryCapabilityExecutor: () => RegistryCapabilityExecutor,
  ServerLayerHost: () => ServerLayerHost,
  attachCanvasWs: () => attachCanvasWs,
  corePrimitiveProviders: () => corePrimitiveProviders,
  createOracleVisibility: () => createOracleVisibility
});
function corePrimitiveProviders(db) {
  const prisma3 = db.prisma;
  const count = async (model) => {
    try {
      const m = prisma3[model];
      if (m?.count) return await m.count();
    } catch {
    }
    return 0;
  };
  const make = (kind, fn) => fnPrimitive(kind, fn);
  return [
    make("workspace", async () => ({ profiles: await count("providerAccount") })),
    make("providers", async (q) => ({
      total: await count("providerDefinition"),
      active: await count("providerHealth"),
      filter: q
    })),
    make("projects", async () => ({ total: await count("project") })),
    make("knowledge", async () => ({ nodes: await count("knowledgeNode") })),
    make("agents", async () => ({ total: await count("autonomousAgent") })),
    make("conversations", async (q) => ({
      total: await count("conversation"),
      filter: q
    }))
  ];
}
function createOracleVisibility(db) {
  const prisma3 = db.prisma;
  const count = async (model) => {
    try {
      const m = prisma3[model];
      if (m?.count) return await m.count();
    } catch {
    }
    return 0;
  };
  return {
    visibility: async () => ({
      providers: await count("providerDefinition"),
      engines: await count("kernelTopologyNode"),
      openLayers: 0,
      // filled by the engine at call time
      projects: await count("project"),
      knowledgeNodes: await count("knowledgeNode"),
      agents: await count("autonomousAgent"),
      health: {}
    })
  };
}
function attachCanvasWs(engine) {
  const ports = /* @__PURE__ */ new Map();
  const instanceWs = /* @__PURE__ */ new Map();
  engine.bridge.onLayerState = (instanceId, regionId, state) => {
    const ws = instanceWs.get(instanceId);
    if (ws) {
      try {
        ws.send(JSON.stringify({ type: "canvas:state", instanceId, regionId, state }));
      } catch {
      }
    }
  };
  return (ws, raw) => {
    let msg;
    try {
      msg = JSON.parse(raw);
    } catch {
      return;
    }
    if (msg.type === "canvas:ready" && typeof msg.instanceId === "string") {
      const port = new CanvasWsPort(ws, msg.instanceId);
      ports.set(msg.instanceId, port);
      instanceWs.set(msg.instanceId, ws);
      engine.bridge.attach(
        msg.instanceId,
        port
      );
      return;
    }
    if (msg.type === "canvas:state" && typeof msg.instanceId === "string") {
      engine.mirror.pushOptimistic(msg.instanceId, String(msg.regionId), msg.state).catch(() => {
      });
      return;
    }
    if (typeof msg.instanceId === "string" && String(msg.type).startsWith("bridge:")) {
      ports.get(msg.instanceId)?.handleRaw(raw);
      return;
    }
    if (msg.type === "canvas:spawn" && typeof msg.definitionId === "string") {
      engine.mounter.spawn(msg.definitionId).then(
        (inst) => ws.send(JSON.stringify({ type: "canvas:spawned", instance: inst }))
      ).catch(
        (e) => ws.send(
          JSON.stringify({
            type: "canvas:error",
            error: e instanceof Error ? e.message : String(e)
          })
        )
      );
      return;
    }
    if (msg.type === "canvas:dismiss" && typeof msg.instanceId === "string") {
      engine.mounter.dismiss(msg.instanceId).then(
        () => ws.send(JSON.stringify({ type: "canvas:dismissed", instanceId: msg.instanceId }))
      ).catch(
        (e) => ws.send(
          JSON.stringify({
            type: "canvas:error",
            error: e instanceof Error ? e.message : String(e)
          })
        )
      );
      return;
    }
  };
}
var ServerLayerHost, RegistryCapabilityExecutor, CanvasWsPort;
var init_canvas_ws = __esm({
  "src/server/canvas-ws.ts"() {
    "use strict";
    init_primitives();
    ServerLayerHost = class {
      mounted = /* @__PURE__ */ new Map();
      async mount(instanceId) {
        const hostNodeId = `server:${instanceId}`;
        this.mounted.set(instanceId, { hostNodeId });
        return { hostNodeId };
      }
      async unmount(instanceId) {
        this.mounted.delete(instanceId);
      }
      isMounted(instanceId) {
        return this.mounted.has(instanceId);
      }
    };
    RegistryCapabilityExecutor = class {
      constructor(registry2) {
        this.registry = registry2;
      }
      registry;
      async execute(slug, input, ctx) {
        const cap = this.registry.getBySlug(slug);
        if (!cap) throw new Error(`Canvas capability not registered: ${slug}`);
        return cap.handler(input, { ...ctx, metadata: ctx.metadata ?? {} });
      }
    };
    CanvasWsPort = class {
      constructor(ws, instanceId) {
        this.ws = ws;
        this.instanceId = instanceId;
      }
      ws;
      instanceId;
      cb = null;
      postMessage(msg) {
        try {
          this.ws.send(JSON.stringify(msg));
        } catch {
        }
      }
      onMessage(cb) {
        this.cb = cb;
      }
      handleRaw(raw) {
        try {
          this.cb?.(JSON.parse(raw));
        } catch {
        }
      }
      close() {
      }
    };
  }
});

// src/engines/provider-health.ts
var provider_health_exports = {};
__export(provider_health_exports, {
  ProviderHealthKernel: () => ProviderHealthKernel
});
function round(n) {
  return Math.round(n * 100) / 100;
}
function toStatus(score, noData) {
  if (noData) return "unknown";
  if (score >= STATUS_THRESHOLD_HEALTHY) return "healthy";
  if (score >= STATUS_THRESHOLD_DEGRADED) return "degraded";
  return "unhealthy";
}
var STATUS_THRESHOLD_HEALTHY, STATUS_THRESHOLD_DEGRADED, SIGNAL_WEIGHTS, WINDOW_24H_MS, DEFAULT_INTERVAL_MS, ProviderHealthKernel;
var init_provider_health = __esm({
  "src/engines/provider-health.ts"() {
    "use strict";
    init_ids();
    init_capability_event_bus();
    STATUS_THRESHOLD_HEALTHY = 80;
    STATUS_THRESHOLD_DEGRADED = 50;
    SIGNAL_WEIGHTS = {
      parserConfidence: 30,
      emptyStreams1h: 20,
      selectorHitRate: 20,
      chromeLiveness: 15,
      circuitBreaker: 10,
      drift24h: 5
    };
    WINDOW_24H_MS = 24 * 60 * 60 * 1e3;
    DEFAULT_INTERVAL_MS = 3e4;
    ProviderHealthKernel = class {
      governor;
      store;
      eventBus;
      intervalMs;
      timer = null;
      cache = /* @__PURE__ */ new Map();
      debounceTimers = /* @__PURE__ */ new Map();
      constructor(options) {
        this.governor = options.governor;
        this.store = options.store;
        this.eventBus = options.eventBus ?? CapabilityEventBus.getInstance();
        this.intervalMs = options.intervalMs ?? DEFAULT_INTERVAL_MS;
      }
      // ── Lifecycle ────────────────────────────────────────────────────────────
      start() {
        if (this.timer) return;
        const unsub = Promise.all([
          this.eventBus.on("capability:confidence_changed", () => this.debouncedRefreshAll()),
          this.eventBus.on("capability:selector_drifted", () => this.debouncedRefreshAll()),
          this.eventBus.on("fleet:slave_status", () => this.debouncedRefreshAll()),
          this.eventBus.on("fleet:circuit_changed", () => this.debouncedRefreshAll())
        ]);
        void unsub;
        this.timer = setInterval(() => {
          void this.refreshAll();
        }, this.intervalMs);
      }
      stop() {
        if (this.timer) {
          clearInterval(this.timer);
          this.timer = null;
        }
        for (const t of this.debounceTimers.values()) clearTimeout(t);
        this.debounceTimers.clear();
      }
      // ── Public read API ────────────────────────────────────────────────────────
      getHealth(providerId) {
        return this.cache.get(providerId) ?? null;
      }
      getAllHealth() {
        return new Map(this.cache);
      }
      // ── Refresh ────────────────────────────────────────────────────────────────
      debouncedRefreshAll() {
        const key = "*";
        const existing = this.debounceTimers.get(key);
        if (existing) clearTimeout(existing);
        const t = setTimeout(() => {
          this.debounceTimers.delete(key);
          void this.refreshAll();
        }, 1e3);
        this.debounceTimers.set(key, t);
      }
      async refreshAll() {
        const providers = await this.store.getActiveProviders();
        for (const providerId of providers) {
          await this.computeProvider(providerId);
        }
      }
      async computeProvider(providerId) {
        const [caps, windows, circuits, drifts, slaveHealth] = await Promise.all([
          this.store.getCapabilityHealth(providerId),
          this.store.getParserWindows(providerId),
          this.store.getCircuitStates(providerId),
          this.store.getRecentDrifts(providerId, WINDOW_24H_MS),
          this.fetchSlaveHealth(providerId)
        ]);
        const previous = this.cache.get(providerId);
        const health = this.score({
          providerId,
          capabilities: caps,
          windows,
          circuits,
          drifts,
          slaveHealth
        });
        this.cache.set(providerId, health);
        await this.persist(providerId, health);
        this.emitIfChanged(providerId, previous, health);
        return health;
      }
      // ── Signal computation ──────────────────────────────────────────────────────
      async fetchSlaveHealth(providerId) {
        const all = await this.governor.getAllHealth();
        const out = [];
        for (const [slaveId, h] of all) {
          if (slaveId.startsWith(`slave:${providerId}:`)) {
            out.push(h.status);
          }
        }
        return out;
      }
      score(ctx) {
        const signals = [];
        const w = SIGNAL_WEIGHTS;
        let confidenceAvg = 0;
        if (ctx.capabilities.length > 0) {
          confidenceAvg = ctx.capabilities.reduce((s, c) => s + c.confidence, 0) / ctx.capabilities.length;
        }
        const confValue = ctx.capabilities.length > 0 ? confidenceAvg * 100 : 0;
        signals.push({
          signal: "parser_confidence",
          weight: w.parserConfidence,
          value: round(confValue),
          contribution: round(confValue / 100 * w.parserConfidence),
          detail: `avg confidence ${(confidenceAvg * 100).toFixed(0)}% over ${ctx.capabilities.length} capabilities`
        });
        let execsTotal = 0;
        let successTotal = 0;
        for (const win of ctx.windows) {
          execsTotal += win.window1hExecutions;
          successTotal += win.window1hSuccessCount;
        }
        const emptyStreamRatio1h = execsTotal > 0 ? (execsTotal - successTotal) / execsTotal : 0;
        const emptyValue = execsTotal > 0 ? (1 - emptyStreamRatio1h) * 100 : 0;
        signals.push({
          signal: "empty_streams_1h",
          weight: w.emptyStreams1h,
          value: round(emptyValue),
          contribution: round(emptyValue / 100 * w.emptyStreams1h),
          detail: `1h empty/zero-success ratio ${(emptyStreamRatio1h * 100).toFixed(0)}% over ${execsTotal} executions`
        });
        let selectorHits = 0;
        let selectorMisses = 0;
        for (const c of ctx.capabilities) {
          selectorHits += c.selectorHitCount;
          selectorMisses += c.selectorMissCount;
        }
        const selectorTotal = selectorHits + selectorMisses;
        const selectorHitRate = selectorTotal > 0 ? selectorHits / selectorTotal : 0;
        const selValue = selectorTotal > 0 ? selectorHitRate * 100 : 0;
        signals.push({
          signal: "selector_hit_rate",
          weight: w.selectorHitRate,
          value: round(selValue),
          contribution: round(selValue / 100 * w.selectorHitRate),
          detail: `selector hit rate ${(selectorHitRate * 100).toFixed(0)}% (${selectorHits}/${selectorTotal})`
        });
        let running = 0;
        let stopped = 0;
        let error2 = 0;
        for (const status2 of ctx.slaveHealth) {
          if (status2 === "running") running++;
          else if (status2 === "stopped") stopped++;
          else error2++;
        }
        const slaveTotal = ctx.slaveHealth.length;
        const livenessValue = slaveTotal > 0 ? running / slaveTotal * 100 : 0;
        signals.push({
          signal: "chrome_liveness",
          weight: w.chromeLiveness,
          value: round(livenessValue),
          contribution: round(livenessValue / 100 * w.chromeLiveness),
          detail: `${running}/${slaveTotal} slaves running`
        });
        const circuitOpen = ctx.circuits.filter((c) => c.state === "open").length;
        const circuitTotal = ctx.circuits.length;
        const circuitValue = circuitTotal > 0 ? (circuitTotal - circuitOpen) / circuitTotal * 100 : 100;
        signals.push({
          signal: "circuit_breaker",
          weight: w.circuitBreaker,
          value: round(circuitValue),
          contribution: round(circuitValue / 100 * w.circuitBreaker),
          detail: `${circuitOpen}/${circuitTotal} circuits open`
        });
        const now = Date.now();
        const driftRecent = ctx.drifts.filter((d) => now - d.detectedAt <= WINDOW_24H_MS).length;
        const driftUnresolved = ctx.drifts.filter((d) => d.resolved === 0).length;
        const driftValue = driftRecent === 0 ? 100 : Math.max(0, 100 - driftRecent * 20);
        signals.push({
          signal: "drift_24h",
          weight: w.drift24h,
          value: round(driftValue),
          contribution: round(driftValue / 100 * w.drift24h),
          detail: `${driftRecent} drifts in 24h, ${driftUnresolved} unresolved`
        });
        const score = round(signals.reduce((s, sig) => s + sig.contribution, 0));
        const status = toStatus(score, ctx.capabilities.length === 0);
        const prospectCount = ctx.capabilities.filter((c) => c.bindingStatus === "prospect").length;
        return {
          status,
          score,
          signals,
          updatedAt: now,
          parsers: {
            confidenceAvg: round(confidenceAvg * 100),
            emptyStreamRatio1h: round(emptyStreamRatio1h * 100)
          },
          capabilities: { selectorHitRate: round(selectorHitRate * 100), prospectCount },
          fleet: { running, stopped, error: error2 },
          circuitBreakers: { open: circuitOpen, total: circuitTotal },
          drifts: { recent: driftRecent, unresolved: driftUnresolved }
        };
      }
      // ── Persistence + events ────────────────────────────────────────────────────
      async persist(providerId, health) {
        const report = {
          id: newId(),
          providerId,
          overallStatus: health.status,
          overallScore: health.score,
          signalsJson: JSON.stringify(health.signals),
          ts: health.updatedAt
        };
        await this.store.upsertProviderHealth(report);
      }
      emitIfChanged(providerId, previous, current) {
        const from = previous?.status ?? "unknown";
        if (from !== current.status) {
          this.eventBus.emit({
            type: "provider:health_changed",
            providerId,
            from,
            to: current.status,
            score: current.score
          });
        }
      }
    };
  }
});

// src/storage/impl/health-store-impl.ts
var health_store_impl_exports = {};
__export(health_store_impl_exports, {
  HealthStoreImpl: () => HealthStoreImpl
});
function toCircuitRow2(r) {
  return {
    id: r.id,
    slaveId: r.slaveId,
    state: r.state,
    failureCount: r.failCount,
    lastFailureAt: r.lastFailAt,
    lastSuccessAt: r.lastSuccessAt,
    openedAt: r.openedAt
  };
}
function toDrift(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    capabilityId: r.capabilityId,
    bindingId: r.bindingId,
    driftType: r.driftType,
    severity: r.severity,
    description: r.description,
    resolved: r.resolved,
    detectedAt: r.detectedAt,
    resolvedAt: r.resolvedAt
  };
}
function toReport(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    overallStatus: r.overallStatus,
    overallScore: r.overallScore,
    signalsJson: r.signalsJson,
    ts: r.lastCheckAt ?? r.updatedAt
  };
}
function toHistoryRow(r) {
  return {
    id: r.id,
    providerId: r.providerId,
    runtimeState: r.runtimeState,
    activeSessions: r.activeSessions,
    totalConversations: r.totalConversations,
    totalMessages: r.totalMessages,
    capabilityExecutions: r.capabilityExecutions,
    capabilitySuccesses: r.capabilitySuccesses,
    capabilityFailures: r.capabilityFailures,
    errorCount: r.errorCount,
    parserConfidenceAvg: r.parserConfidenceAvg,
    selectorHitRateAvg: r.selectorHitRateAvg,
    avgResponseLatencyMs: r.avgResponseLatencyMs,
    p50ResponseLatencyMs: r.p50ResponseLatencyMs,
    p95ResponseLatencyMs: r.p95ResponseLatencyMs,
    p99ResponseLatencyMs: r.p99ResponseLatencyMs,
    circuitBreakerState: r.circuitBreakerState,
    fleetRestarts: r.fleetRestarts,
    driftEventsUnresolved: r.driftEventsUnresolved,
    windowStartTs: r.windowStartTs,
    windowEndTs: r.windowEndTs,
    snapshotTs: r.snapshotTs,
    schemaVersion: r.schemaVersion
  };
}
var HealthStoreImpl;
var init_health_store_impl = __esm({
  "src/storage/impl/health-store-impl.ts"() {
    "use strict";
    HealthStoreImpl = class {
      constructor(db) {
        this.db = db;
      }
      db;
      async getCircuitStates(providerId) {
        const rows = await this.db.prisma.circuitBreakerState.findMany({});
        return rows.map((r) => r).filter((r) => r.slaveId.includes(providerId)).map(toCircuitRow2);
      }
      async getRecentDrifts(providerId, windowMs) {
        const cutoff = Date.now() - windowMs;
        const rows = await this.db.prisma.driftEvent.findMany({
          where: { providerId, detectedAt: { gte: cutoff } },
          orderBy: { detectedAt: "desc" }
        });
        return rows.map((r) => toDrift(r));
      }
      async upsertProviderHealth(report) {
        const now = Date.now();
        await this.db.prisma.providerHealth.upsert({
          where: { providerId: report.providerId },
          create: {
            id: report.id,
            providerId: report.providerId,
            overallStatus: report.overallStatus,
            overallScore: report.overallScore,
            signalsJson: report.signalsJson,
            lastCheckAt: report.ts,
            createdAt: now,
            updatedAt: now
          },
          update: {
            overallStatus: report.overallStatus,
            overallScore: report.overallScore,
            signalsJson: report.signalsJson,
            lastCheckAt: report.ts,
            updatedAt: now
          }
        });
      }
      async getProviderHealth(providerId) {
        const row = await this.db.prisma.providerHealth.findUnique({ where: { providerId } });
        return row ? toReport(row) : null;
      }
      async getHealthHistory(providerId, limit) {
        const rows = await this.db.prisma.providerHealthHistory.findMany({
          where: { providerId },
          orderBy: { snapshotTs: "desc" },
          take: limit ?? 100
        });
        return rows.map((r) => toHistoryRow(r));
      }
      async getActiveProviders() {
        const rows = await this.db.prisma.providerHealth.findMany({
          where: { overallStatus: { not: "unknown" } }
        });
        return rows.map((r) => r.providerId);
      }
      async getCapabilityHealth(providerId) {
        const [caps, bindings] = await Promise.all([
          this.db.prisma.providerCapability.findMany({ where: { providerId } }),
          this.db.prisma.capabilityBinding.findMany({ where: { providerId } })
        ]);
        const statusByGlobalId = /* @__PURE__ */ new Map();
        for (const b of bindings) {
          const row = b;
          statusByGlobalId.set(row.globalId, row.status);
        }
        return caps.map((c) => {
          const row = c;
          return {
            capabilityId: row.globalCapabilityId,
            confidence: row.confidence,
            selectorHitCount: row.selectorHitCount,
            selectorMissCount: row.selectorMissCount,
            bindingStatus: statusByGlobalId.get(row.globalCapabilityId) ?? "prospect"
          };
        });
      }
      async getParserWindows(providerId) {
        const rows = await this.db.prisma.capabilityTelemetry.findMany({ where: { providerId } });
        return rows.map((r) => {
          const row = r;
          return {
            capabilityId: row.capabilityId,
            window1hExecutions: row.window1hExecutions,
            window1hSuccessCount: row.window1hSuccessCount
          };
        });
      }
    };
  }
});

// src/engines/lock-manager.ts
var lock_manager_exports = {};
__export(lock_manager_exports, {
  LockManager: () => LockManager
});
var DEFAULT_POLICY2, LockManager;
var init_lock_manager = __esm({
  "src/engines/lock-manager.ts"() {
    "use strict";
    init_errors();
    DEFAULT_POLICY2 = {
      scope: "conversation",
      strategy: "mutex",
      timeoutMs: 6e4,
      queueMaxSize: 5
    };
    LockManager = class {
      locks = /* @__PURE__ */ new Map();
      queues = /* @__PURE__ */ new Map();
      policy = DEFAULT_POLICY2;
      async acquire(key, owner) {
        if (this.policy.strategy === "reject") {
          const existing = this.locks.get(key);
          if (existing) {
            throw new EngineError(`Lock held by ${existing.owner}`);
          }
        }
        if (this.policy.strategy === "queue") {
          return this.acquireQueued(key, owner);
        }
        return this.acquireMutex(key, owner);
      }
      release(key, owner) {
        const entry = this.locks.get(key);
        if (entry && entry.owner === owner) {
          clearTimeout(entry.timeout);
          this.locks.delete(key);
          const queue = this.queues.get(key);
          if (queue && queue.length > 0) {
            const next = queue.shift();
            if (!next) return;
            this.locks.set(key, {
              owner,
              acquiredAt: Date.now(),
              timeout: setTimeout(() => this.release(key, owner), this.policy.timeoutMs)
            });
            next.resolve();
          }
        }
      }
      isLocked(key) {
        return this.locks.has(key);
      }
      getLockOwner(key) {
        return this.locks.get(key)?.owner ?? null;
      }
      async acquireMutex(key, owner) {
        const existing = this.locks.get(key);
        if (!existing) {
          this.locks.set(key, {
            owner,
            acquiredAt: Date.now(),
            timeout: setTimeout(() => this.release(key, owner), this.policy.timeoutMs)
          });
          return;
        }
        return new Promise((resolve4, reject) => {
          const timeout = setTimeout(() => {
            reject(new EngineError(`Lock timeout after ${this.policy.timeoutMs}ms`));
          }, this.policy.timeoutMs);
          const check = setInterval(() => {
            if (!this.locks.has(key)) {
              clearInterval(check);
              clearTimeout(timeout);
              this.locks.set(key, {
                owner,
                acquiredAt: Date.now(),
                timeout: setTimeout(() => this.release(key, owner), this.policy.timeoutMs)
              });
              resolve4();
            }
          }, 50);
        });
      }
      async acquireQueued(key, owner) {
        const existing = this.locks.get(key);
        if (!existing) {
          this.locks.set(key, {
            owner,
            acquiredAt: Date.now(),
            timeout: setTimeout(() => this.release(key, owner), this.policy.timeoutMs)
          });
          return;
        }
        const queue = this.queues.get(key) ?? [];
        if (queue.length >= this.policy.queueMaxSize) {
          throw new EngineError(`Lock queue full (${this.policy.queueMaxSize})`);
        }
        return new Promise((resolve4, reject) => {
          queue.push({ resolve: resolve4, reject });
          this.queues.set(key, queue);
        });
      }
    };
  }
});

// src/engines/idempotency-guard.ts
var idempotency_guard_exports = {};
__export(idempotency_guard_exports, {
  IdempotencyGuard: () => IdempotencyGuard
});
function simpleHash(str) {
  let hash = 0;
  for (let i = 0; i < str.length; i++) {
    const char = str.charCodeAt(i);
    hash = (hash << 5) - hash + char;
    hash |= 0;
  }
  return `h_${Math.abs(hash).toString(36)}`;
}
var DEFAULT_POLICY3, IdempotencyGuard;
var init_idempotency_guard = __esm({
  "src/engines/idempotency-guard.ts"() {
    "use strict";
    DEFAULT_POLICY3 = {
      enabled: true,
      windowMs: 1e4,
      keyStrategy: "message_hash+conversation",
      maxRecentKeys: 1e3,
      onDuplicate: "return_cached"
    };
    IdempotencyGuard = class {
      recent = /* @__PURE__ */ new Map();
      policy = DEFAULT_POLICY3;
      async check(conversationId, message) {
        if (!this.policy.enabled) return { duplicate: false, key: "" };
        const key = this.computeKey(conversationId, message);
        const now = Date.now();
        this.prune(now);
        const existing = this.recent.get(key);
        if (existing && now - existing.timestamp < this.policy.windowMs) {
          switch (this.policy.onDuplicate) {
            case "reject":
              return { duplicate: true, key };
            case "return_cached":
              return { duplicate: true, cachedResult: existing.result, key };
            case "allow":
              return { duplicate: false, key };
          }
        }
        return { duplicate: false, key };
      }
      record(key, result) {
        this.recent.set(key, { result, timestamp: Date.now() });
        if (this.recent.size > this.policy.maxRecentKeys) {
          const oldest = this.recent.keys().next().value;
          if (oldest) this.recent.delete(oldest);
        }
      }
      computeKey(conversationId, message) {
        if (this.policy.keyStrategy === "message_hash") {
          return simpleHash(message);
        }
        if (this.policy.keyStrategy === "client_key") {
          return simpleHash(message);
        }
        return `${conversationId}:${simpleHash(message)}`;
      }
      prune(now) {
        for (const [key, entry] of this.recent) {
          if (now - entry.timestamp > this.policy.windowMs) {
            this.recent.delete(key);
          }
        }
      }
    };
  }
});

// src/engines/retry-engine.ts
var retry_engine_exports = {};
__export(retry_engine_exports, {
  RetryEngine: () => RetryEngine
});
var DEFAULT_POLICY4, RetryEngine;
var init_retry_engine = __esm({
  "src/engines/retry-engine.ts"() {
    "use strict";
    init_errors();
    DEFAULT_POLICY4 = {
      maxAttempts: 3,
      backoffStrategy: "exponential",
      initialDelayMs: 1e3,
      maxDelayMs: 3e4,
      multiplier: 2,
      retryableErrors: [
        "Slave not running",
        "Circuit breaker",
        "CDP command failed",
        "CDP client not connected",
        "ECONNRESET",
        "ETIMEDOUT"
      ],
      nonRetryableErrors: ["Conversation not found", "Account not found", "Capability not found"],
      onRetry: "log"
    };
    RetryEngine = class {
      policies = /* @__PURE__ */ new Map();
      getPolicy(operationKey) {
        return this.policies.get(operationKey) ?? DEFAULT_POLICY4;
      }
      setPolicy(operationKey, policy) {
        this.policies.set(operationKey, { ...DEFAULT_POLICY4, ...policy });
      }
      async execute(operationKey, fn, opts) {
        const policy = { ...this.getPolicy(operationKey), ...opts?.policy };
        let lastError = null;
        for (let attempt = 1; attempt <= policy.maxAttempts; attempt++) {
          try {
            return await fn();
          } catch (err) {
            lastError = err instanceof Error ? err : new EngineError(String(err));
            const errorMsg = lastError.message;
            if (policy.nonRetryableErrors.some((p) => errorMsg.includes(p))) {
              throw lastError;
            }
            const isRetryable = policy.retryableErrors.some((p) => errorMsg.includes(p));
            if (!isRetryable && attempt < policy.maxAttempts) {
              throw lastError;
            }
            if (attempt < policy.maxAttempts) {
              const delay = this.computeDelay(attempt, policy);
              if (policy.onRetry === "log") {
                console.log(
                  `[retry] ${operationKey} attempt ${attempt} failed: ${errorMsg}, retrying in ${delay}ms`
                );
              }
              await new Promise((r) => setTimeout(r, delay));
            }
          }
        }
        throw lastError ?? new EngineError("Retry failed");
      }
      computeDelay(attempt, policy) {
        switch (policy.backoffStrategy) {
          case "fixed":
            return policy.initialDelayMs;
          case "linear":
            return Math.min(policy.initialDelayMs * attempt, policy.maxDelayMs);
          case "exponential":
            return Math.min(
              policy.initialDelayMs * policy.multiplier ** (attempt - 1),
              policy.maxDelayMs
            );
          case "exponential_jitter": {
            const base = policy.initialDelayMs * policy.multiplier ** (attempt - 1);
            const jitter = base * 0.2 * Math.random();
            return Math.min(base + jitter, policy.maxDelayMs);
          }
        }
      }
    };
  }
});

// src/server/index.ts
var index_exports = {};
__export(index_exports, {
  createServer: () => createServer,
  createServerWithEngines: () => createServerWithEngines,
  onShutdown: () => onShutdown
});
function onShutdown(hook) {
  shutdownHooks.push(hook);
}
async function gracefulShutdown(signal) {
  if (isShuttingDown) return;
  isShuttingDown = true;
  console.log(`
${signal} received \u2014 shutting down gracefully...`);
  for (const hook of shutdownHooks) {
    try {
      await hook();
    } catch (err) {
      console.error("Shutdown hook error:", err);
    }
  }
  console.log("Shutdown complete.");
  process.exit(0);
}
async function createServer(port = 9420) {
  const db = getDb();
  const eventBus = CapabilityEventBus.getInstance();
  const nlclEngine = new NLCLEngine({ db });
  const ctx = { port, db, eventBus, nlclEngine };
  const auth = createAuthMiddleware();
  const conversationRouter = createConversationRouter(ctx);
  const knowledgeRouter = createKnowledgeRouter(ctx);
  const setupRouter = createSetupRouter(ctx);
  const muxRouter = createMuxRouter(ctx);
  const nlclRouter = createNLCLRouter(nlclEngine);
  let ready = false;
  Bun.serve({
    port,
    fetch(req, server) {
      const url = new URL(req.url);
      if (url.pathname === "/health") {
        return json({ status: "ok", version: "1.0.0" });
      }
      if (url.pathname === "/readyz") {
        if (!ready) {
          return json({ status: "not_ready", reason: "server still starting" }, 503);
        }
        return json({ status: "ready", uptime: process.uptime() });
      }
      if (url.pathname.startsWith("/api/setup/")) {
        return setupRouter(req);
      }
      if (url.pathname === "/ws") {
        const ok = server.upgrade(req);
        return ok ? void 0 : errorResponse("WebSocket upgrade failed", "UpgradeFailed", 400);
      }
      if (isShuttingDown) {
        return json({ error: "Server shutting down", code: "ShuttingDown" }, 503);
      }
      const authResult = auth(req);
      if (authResult) return authResult;
      if (url.pathname.startsWith("/api/route/")) {
        return muxRouter(req);
      }
      if (url.pathname.startsWith("/api/nlcl/")) {
        return nlclRouter(req);
      }
      if (url.pathname.startsWith("/api/knowledge/")) {
        return knowledgeRouter(req);
      }
      return conversationRouter(req);
    },
    websocket: {
      open(ws) {
        handleWebSocket.open(ws);
      },
      message(ws, message) {
        handleWebSocket.message(ws, message, eventBus);
      },
      close(ws) {
        handleWebSocket.close(ws, eventBus);
      }
    }
  });
  ready = true;
  process.on("SIGTERM", () => gracefulShutdown("SIGTERM"));
  process.on("SIGINT", () => gracefulShutdown("SIGINT"));
  return ctx;
}
async function createServerWithEngines(port = 9420) {
  const db = getDb();
  const eventBus = CapabilityEventBus.getInstance();
  const { ConversationManager: ConversationManager2 } = await Promise.resolve().then(() => (init_conversation_manager(), conversation_manager_exports));
  const { CapabilityResolutionEngine: CapabilityResolutionEngine2 } = await Promise.resolve().then(() => (init_capability_resolution(), capability_resolution_exports));
  const { ChromeGovernor: ChromeGovernor2 } = await Promise.resolve().then(() => (init_chrome_governor(), chrome_governor_exports));
  const { StreamParserEngine: StreamParserEngine2 } = await Promise.resolve().then(() => (init_stream_parser(), stream_parser_exports));
  const { SandboxRunner: SandboxRunner2 } = await Promise.resolve().then(() => (init_sandbox_runner(), sandbox_runner_exports));
  const { SandboxAuditStoreImpl: SandboxAuditStoreImpl2 } = await Promise.resolve().then(() => (init_sandbox_audit_store_impl(), sandbox_audit_store_impl_exports));
  const { StreamBlockStore: StreamBlockStore2 } = await Promise.resolve().then(() => (init_stream_block_store(), stream_block_store_exports));
  const { NodeStoreImpl: NodeStoreImpl2 } = await Promise.resolve().then(() => (init_node_store_impl(), node_store_impl_exports));
  const { ExecutionMemoizer: ExecutionMemoizer2 } = await Promise.resolve().then(() => (init_execution_memoizer(), execution_memoizer_exports));
  const { MemoryEngine: MemoryEngine2 } = await Promise.resolve().then(() => (init_memory_engine(), memory_engine_exports));
  const { ConversationStoreImpl: ConversationStoreImpl2 } = await Promise.resolve().then(() => (init_conversation_store_impl(), conversation_store_impl_exports));
  const { GovernorStoreImpl: GovernorStoreImpl2 } = await Promise.resolve().then(() => (init_governor_store_impl(), governor_store_impl_exports));
  const { CapabilityResolutionStoreImpl: CapabilityResolutionStoreImpl2 } = await Promise.resolve().then(() => (init_capability_resolution_store_impl(), capability_resolution_store_impl_exports));
  const { ParserStoreImpl: ParserStoreImpl2 } = await Promise.resolve().then(() => (init_parser_store_impl(), parser_store_impl_exports));
  const { ParserExecutionLogStoreImpl: ParserExecutionLogStoreImpl2 } = await Promise.resolve().then(() => (init_parser_execution_log_store_impl(), parser_execution_log_store_impl_exports));
  const { ContentUnitStoreImpl: ContentUnitStoreImpl2 } = await Promise.resolve().then(() => (init_content_unit_store_impl(), content_unit_store_impl_exports));
  const { CapabilityStoreImpl: CapabilityStoreImpl2 } = await Promise.resolve().then(() => (init_capability_store_impl(), capability_store_impl_exports));
  const { CapabilitySnapshot: CapabilitySnapshot2 } = await Promise.resolve().then(() => (init_capability_snapshot(), capability_snapshot_exports));
  const { EpisodicMemoryStoreImpl: EpisodicMemoryStoreImpl2 } = await Promise.resolve().then(() => (init_episodic_memory_store_impl(), episodic_memory_store_impl_exports));
  const { SemanticMemoryStoreImpl: SemanticMemoryStoreImpl2 } = await Promise.resolve().then(() => (init_semantic_memory_store_impl(), semantic_memory_store_impl_exports));
  const { ProceduralMemoryStoreImpl: ProceduralMemoryStoreImpl2 } = await Promise.resolve().then(() => (init_procedural_memory_store_impl(), procedural_memory_store_impl_exports));
  const { ProviderStoreImpl: ProviderStoreImpl2 } = await Promise.resolve().then(() => (init_provider_store_impl(), provider_store_impl_exports));
  const { ProviderRegistrar: ProviderRegistrar2 } = await Promise.resolve().then(() => (init_provider_registrar(), provider_registrar_exports));
  const providerStore = new ProviderStoreImpl2(db);
  const registrar = new ProviderRegistrar2(providerStore, void 0, eventBus);
  const seedResult = await registrar.seedAll();
  console.log(
    `[boot] Seeded ${seedResult.seeded.length} providers, ${seedResult.errors.length} errors`
  );
  if (seedResult.errors.length > 0) {
    console.warn("[boot] Seed errors:", seedResult.errors);
  }
  try {
    const { seedHarvestedParsers: seedHarvestedParsers2 } = await Promise.resolve().then(() => (init_harvest_seed(), harvest_seed_exports));
    const harvested = await seedHarvestedParsers2(providerStore);
    console.log(`[boot] Harvested ${harvested} parser variants into DB`);
  } catch (err) {
    console.warn("[boot] harvest parser seed skipped:", err);
  }
  const { createProviderRegistry: createProviderRegistry2 } = await Promise.resolve().then(() => (init_provider_registry(), provider_registry_exports));
  const providerRegistry = createProviderRegistry2(db);
  await providerRegistry.initialize();
  console.log(
    `[boot] Provider registry initialized: ${providerRegistry.getProviderList().length} providers cached`
  );
  try {
    const { seedAutomation: seedAutomation2 } = await Promise.resolve().then(() => (init_automation_seed(), automation_seed_exports));
    const autoCount = await seedAutomation2(db);
    console.log(`[boot] Seeded ${autoCount} browser-automation records`);
  } catch (err) {
    console.warn("[boot] automation seed skipped:", err);
  }
  const convStore = new ConversationStoreImpl2(db);
  const govStore = new GovernorStoreImpl2(db);
  const resStore = new CapabilityResolutionStoreImpl2(db.prisma);
  const parserStore = new ParserStoreImpl2(db);
  const parserExecLogStore = new ParserExecutionLogStoreImpl2(db.prisma);
  const contentUnitStore = new ContentUnitStoreImpl2(db.prisma);
  const capabilityStore = new CapabilityStoreImpl2(db);
  const episodicStore = new EpisodicMemoryStoreImpl2(db);
  const semanticStore = new SemanticMemoryStoreImpl2(db);
  const proceduralStore = new ProceduralMemoryStoreImpl2(db);
  const resolutionEngine = new CapabilityResolutionEngine2(resStore);
  const sandboxRunner = new SandboxRunner2(new SandboxAuditStoreImpl2(db));
  const parserEngine = new StreamParserEngine2(parserStore, void 0, sandboxRunner, parserExecLogStore);
  const streamBlocks = new StreamBlockStore2(db);
  try {
    const { loadProviderProtocol: loadProviderProtocol2, normalizeProtocolSource: normalizeProtocolSource2 } = await Promise.resolve().then(() => (init_provider_protocol_loader(), provider_protocol_loader_exports));
    const source = normalizeProtocolSource2(process.env.PROVIDER_PROTOCOL_SOURCE);
    const { protocol: protocol3 } = await loadProviderProtocol2(source);
    await parserEngine.primeFromProtocol(protocol3);
    console.log(`[boot] Stream parser cache primed from ${source} protocol`);
  } catch (err) {
    console.warn("[boot] protocol parser priming skipped:", err);
  }
  const memoizer = new ExecutionMemoizer2({
    emit: (event, data) => {
      eventBus.emit({ type: event, ...data });
    },
    on: (_event, _handler) => {
    }
  });
  const memoryEngine = new MemoryEngine2(episodicStore, semanticStore, proceduralStore, eventBus);
  const workspaceHint = await db.getWorkspaceHint() ?? "chrome-profiles";
  const governor = new ChromeGovernor2(govStore, {
    portRange: [9300, 9400],
    healthProbeIntervalMs: 3e4,
    healthProbeTimeoutMs: 5e3,
    autoRestart: true,
    maxRestarts: 3,
    circuitBreakerThreshold: 5,
    circuitBreakerResetMs: 6e4,
    profileBaseDir: workspaceHint
  });
  const conversationManager = new ConversationManager2(
    governor,
    resolutionEngine,
    parserEngine,
    streamBlocks,
    convStore,
    eventBus,
    memoizer,
    memoryEngine,
    void 0,
    void 0,
    new NodeStoreImpl2(db.prisma),
    contentUnitStore
  );
  const { CdpTransportImpl: CdpTransportImpl2 } = await Promise.resolve().then(() => (init_cdp_transport(), cdp_transport_exports));
  const cdpTransport = new CdpTransportImpl2();
  governor.setCdpTransport(cdpTransport);
  governor.setTraceLog(govStore);
  governor.setHealthMonitor(govStore);
  onShutdown(async () => {
    await cdpTransport.disconnectAll();
    await governor.killAll();
  });
  await governor.boot();
  let knowledgeIngestion;
  let semanticSearch;
  let synthesizer;
  let exportEngine;
  try {
    const { KnowledgeIngestionEngine: KnowledgeIngestionEngine2 } = await Promise.resolve().then(() => (init_knowledge_ingestion(), knowledge_ingestion_exports));
    const { KnowledgeIngestionStoreImpl: KnowledgeIngestionStoreImpl2 } = await Promise.resolve().then(() => (init_knowledge_ingestion_store_impl(), knowledge_ingestion_store_impl_exports));
    const { KnowledgeExtractor: KnowledgeExtractor2 } = await Promise.resolve().then(() => (init_knowledge_extractor(), knowledge_extractor_exports));
    const { KnowledgeExtractorStoreImpl: KnowledgeExtractorStoreImpl2 } = await Promise.resolve().then(() => (init_knowledge_extractor_store_impl(), knowledge_extractor_store_impl_exports));
    const kexStore = new KnowledgeExtractorStoreImpl2(db);
    const extractor = new KnowledgeExtractor2(kexStore, {
      batchSize: 50,
      confidenceThreshold: 0.3,
      enableEntityExtraction: true,
      enableDecisionExtraction: true,
      enablePatternMining: false
    });
    const kiStore = new KnowledgeIngestionStoreImpl2(db);
    knowledgeIngestion = new KnowledgeIngestionEngine2(
      kiStore,
      convStore,
      streamBlocks,
      extractor,
      eventBus
    );
  } catch {
  }
  try {
    const { SemanticSearchEngine: SemanticSearchEngine2 } = await Promise.resolve().then(() => (init_semantic_search(), semantic_search_exports));
    const { SemanticSearchStoreImpl: SemanticSearchStoreImpl2 } = await Promise.resolve().then(() => (init_semantic_search_store_impl(), semantic_search_store_impl_exports));
    const ssStore = new SemanticSearchStoreImpl2(db);
    const noopEmbedding = {
      name: "noop",
      dimensions: 384,
      embed: async (_t) => new Array(384).fill(0),
      embedBatch: async (ts) => ts.map(() => new Array(384).fill(0))
    };
    semanticSearch = new SemanticSearchEngine2(ssStore, noopEmbedding);
  } catch {
  }
  try {
    const { CrossConversationSynthesizer: CrossConversationSynthesizer2 } = await Promise.resolve().then(() => (init_cross_conversation_synthesis(), cross_conversation_synthesis_exports));
    const { CrossConversationSynthesizerStoreImpl: CrossConversationSynthesizerStoreImpl2 } = await Promise.resolve().then(() => (init_cross_conversation_synth_store_impl(), cross_conversation_synth_store_impl_exports));
    const synthStore = new CrossConversationSynthesizerStoreImpl2(db);
    const noopLlm = { synthesize: async () => ({ text: "LLM not configured", confidence: 0 }) };
    if (semanticSearch)
      synthesizer = new CrossConversationSynthesizer2(synthStore, semanticSearch, noopLlm);
  } catch {
  }
  try {
    const { ExportEngine: ExportEngine2 } = await Promise.resolve().then(() => (init_export(), export_exports));
    exportEngine = new ExportEngine2({
      async listConversations(opts) {
        return db.prisma.conversation.findMany({
          where: opts?.dateFrom || opts?.dateTo ? {
            createdAt: {
              ...opts?.dateFrom ? { gte: opts.dateFrom } : {},
              ...opts?.dateTo ? { lte: opts.dateTo } : {}
            }
          } : void 0,
          select: { id: true, state: true, title: true }
        });
      },
      async listMessages(conversationId) {
        const rows = await db.prisma.conversationMessage.findMany({
          where: { conversationId },
          select: { id: true, role: true, content: true, createdAt: true },
          orderBy: { sequenceIndex: "asc" }
        });
        return rows.map((r) => ({
          id: r.id,
          role: r.role,
          content: r.content ?? "",
          ts: Number(r.createdAt)
        }));
      },
      async listMemory() {
        const episodic = await db.prisma.episodicMemory.findMany({
          select: { id: true, action: true, inputJson: true, timestamp: true }
        });
        const semantic = await db.prisma.semanticMemory.findMany({
          select: { id: true, subject: true, objectJson: true, timestamp: true }
        });
        const result = [];
        for (const e of episodic) {
          result.push({ id: e.id, key: e.action, value: e.inputJson, namespace: "episodic" });
        }
        for (const s of semantic) {
          result.push({ id: s.id, key: s.subject, value: s.objectJson, namespace: "semantic" });
        }
        return result;
      },
      async listProviders() {
        return db.prisma.providerDefinition.findMany({
          select: { id: true, slug: true, displayName: true }
        });
      },
      async listConfig() {
        return db.prisma.configEntry.findMany({
          select: { id: true, engineId: true, configJson: true }
        });
      }
    });
  } catch {
  }
  let providerMux;
  let costOptimizer;
  try {
    const { CostOptimizer: CostOptimizer2 } = await Promise.resolve().then(() => (init_cost_optimizer(), cost_optimizer_exports));
    const { CostStoreImpl: CostStoreImpl2 } = await Promise.resolve().then(() => (init_cost_store_impl(), cost_store_impl_exports));
    const costStore = new CostStoreImpl2(db);
    costOptimizer = new CostOptimizer2(costStore);
  } catch {
  }
  try {
    const { ProviderMuxEngine: ProviderMuxEngine2 } = await Promise.resolve().then(() => (init_provider_mux(), provider_mux_exports));
    const { MuxStoreImpl: MuxStoreImpl2 } = await Promise.resolve().then(() => (init_mux_store_impl(), mux_store_impl_exports));
    const { Router: Router2 } = await Promise.resolve().then(() => (init_router(), router_exports));
    const { RouterStoreImpl: RouterStoreImpl2 } = await Promise.resolve().then(() => (init_router_store_impl(), router_store_impl_exports));
    const muxStore = new MuxStoreImpl2(db);
    const routerStore = new RouterStoreImpl2(db);
    const muxDispatcher = {
      async dispatchToProvider(providerId, message, conversationId) {
        const start = Date.now();
        try {
          let convId = conversationId;
          if (!convId) {
            const session = await convStore.ensureProviderSession({ providerId });
            const conv = await convStore.createConversation({
              providerSessionId: session.id,
              providerId,
              title: `Mux: ${message.slice(0, 50)}`
            });
            convId = conv.id;
          }
          const result = await conversationManager.send(convId, message);
          const latencyMs = Date.now() - start;
          const estCost = await estimateCost(providerId, latencyMs);
          if (costOptimizer) {
            await costOptimizer.recordCost(providerId, estCost, 0, 0);
          }
          return {
            ok: result.ok,
            response: result.text || "",
            latencyMs,
            costCents: estCost,
            error: result.error
          };
        } catch (err) {
          return {
            ok: false,
            response: "",
            latencyMs: Date.now() - start,
            costCents: 0,
            error: err instanceof Error ? err.message : String(err)
          };
        }
      }
    };
    async function estimateCost(providerId, _latencyMs) {
      if (costOptimizer) {
        return costOptimizer.estimateCost(providerId, 1e3);
      }
      return 0;
    }
    const noopDispatcher = { dispatch: async () => ({ ok: true }) };
    const router = new Router2(routerStore, noopDispatcher);
    providerMux = new ProviderMuxEngine2(muxStore, muxDispatcher, router, eventBus);
  } catch {
  }
  let autonomousEngine;
  let policyEngine;
  let registry2;
  try {
    const { AutonomousExecutionEngine: AutonomousExecutionEngine2 } = await Promise.resolve().then(() => (init_autonomous_execution(), autonomous_execution_exports));
    const { ExecutionPolicyEngine: ExecutionPolicyEngine2 } = await Promise.resolve().then(() => (init_execution_policy(), execution_policy_exports));
    const { AutonomousStoreImpl: AutonomousStoreImpl2 } = await Promise.resolve().then(() => (init_autonomous_store_impl(), autonomous_store_impl_exports));
    const { PolicyStoreImpl: PolicyStoreImpl2 } = await Promise.resolve().then(() => (init_policy_store_impl(), policy_store_impl_exports));
    const { ProfileAllocator: ProfileAllocator2 } = await Promise.resolve().then(() => (init_profile_allocator(), profile_allocator_exports));
    const autonomousStore = new AutonomousStoreImpl2();
    const pStore = new PolicyStoreImpl2();
    const profileAllocator = new ProfileAllocator2();
    registry2 = new UnifiedCapabilityRegistry();
    const { LocalAgentStoreImpl: LocalAgentStoreImpl2 } = await Promise.resolve().then(() => (init_local_agent_store_impl(), local_agent_store_impl_exports));
    const { LocalAgentProviderExecutor: LocalAgentProviderExecutor2 } = await Promise.resolve().then(() => (init_local_agent_executor(), local_agent_executor_exports));
    const localAgentStore = new LocalAgentStoreImpl2(db);
    const localAgentExecutor = new LocalAgentProviderExecutor2(localAgentStore, eventBus);
    registerDefaultCapabilities(registry2, {
      db,
      conversationStore: convStore,
      governor,
      conversationManager,
      profileAllocator,
      memoryEngine,
      semanticSearch,
      knowledgeIngestion,
      synthesizer,
      localAgentStore,
      localAgentExecutor
    });
    registerGeneratedCapabilities(registry2, {
      db,
      conversationStore: convStore,
      governor,
      conversationManager,
      profileAllocator,
      memoryEngine,
      semanticSearch,
      knowledgeIngestion,
      synthesizer
    });
    const cdpBindingStore = {
      async ensureCdpBinding(args) {
        const now = Date.now();
        await db.prisma.capabilityTaxonomy.upsert({
          where: { id: args.capabilityId },
          create: {
            id: args.capabilityId,
            slug: args.capabilityId.replace(/:/g, "-"),
            name: args.capabilityId,
            category: "cdp",
            description: `Discovered CDP capability ${args.capabilityId}`,
            createdAt: now,
            updatedAt: now
          },
          update: { updatedAt: now }
        }).catch(() => {
        });
        await db.prisma.capabilityBinding.upsert({
          where: {
            globalId_providerId: { globalId: args.capabilityId, providerId: args.providerId }
          },
          create: {
            id: `bind:${args.providerId}:${args.capabilityId}`,
            globalId: args.capabilityId,
            providerId: args.providerId,
            status: args.status,
            confidence: args.confidence,
            promotionHistoryJson: JSON.stringify([
              { ts: now, from: "none", to: args.status, reason: args.reason ?? "boot" }
            ]),
            createdAt: now,
            updatedAt: now
          },
          update: {
            status: args.status,
            confidence: args.confidence,
            promotionHistoryJson: JSON.stringify([
              { ts: now, from: "prospect", to: args.status, reason: args.reason ?? "boot" }
            ]),
            updatedAt: now
          }
        }).catch(() => {
        });
      }
    };
    const cdpResult = registerDiscoveredCdpMethods(registry2, CDP_PROTOCOL_CATALOG, {
      executeCdp: (method, params, ctx2) => {
        const ref = ctx2?.conversationId ?? ctx2?.providerId ?? "generic";
        return governor.executeCdpMethod(ref, method, params);
      },
      providerId: "generic",
      bindingStore: cdpBindingStore
      // Boot registration is unverified → prospect (D2 light gate pending).
    });
    console.log(
      `[boot] CDP capabilities: registered=${cdpResult.registered.length} bound=${cdpResult.bound.length} skipped=${cdpResult.skipped.length}`
    );
    const registeredProviders = (await providerStore.listDefinitions({ isActive: true })).map(
      (d) => d.id
    );
    const capabilitySnapshot = new CapabilitySnapshot2(capabilityStore);
    const snapshotCount = await capabilitySnapshot.load(registeredProviders);
    governor.setCapabilitySnapshot(capabilitySnapshot);
    console.log(
      `[boot] Capability snapshot: loaded=${snapshotCount} for ${registeredProviders.length} providers`
    );
    connectCapabilityRegistry(registry2);
    policyEngine = new ExecutionPolicyEngine2(pStore);
    await policyEngine.initialize();
    autonomousEngine = new AutonomousExecutionEngine2(
      autonomousStore,
      registry2,
      policyEngine,
      governor,
      eventBus
    );
  } catch {
  }
  const automationOrchestrator = new (await Promise.resolve().then(() => (init_orchestrator(), orchestrator_exports))).AutomationOrchestrator(governor);
  const nlclEngine = new NLCLEngine({
    governor,
    automationOrchestrator,
    conversationManager,
    conversationStore: convStore,
    registry: registry2,
    db
  });
  console.log(
    `[boot] NLCL engine initialized \u2014 ${nlclEngine.listCommands().length} command patterns`
  );
  if (registry2) {
    registerNlInterpretCapability(registry2, nlclEngine);
  }
  let canvasRouter = null;
  try {
    const { CanvasEngine: CanvasEngine2 } = await Promise.resolve().then(() => (init_canvas_engine(), canvas_engine_exports));
    const { InMemoryCanvasStore: InMemoryCanvasStore2 } = await Promise.resolve().then(() => (init_in_memory_store(), in_memory_store_exports));
    const { createCanvasRouter: createCanvasRouter2 } = await Promise.resolve().then(() => (init_canvas_router(), canvas_router_exports));
    const {
      attachCanvasWs: attachCanvasWs2,
      ServerLayerHost: ServerLayerHost2,
      corePrimitiveProviders: corePrimitiveProviders2,
      createOracleVisibility: createOracleVisibility2,
      RegistryCapabilityExecutor: RegistryCapabilityExecutor2
    } = await Promise.resolve().then(() => (init_canvas_ws(), canvas_ws_exports));
    if (registry2) {
      const canvasStore = new InMemoryCanvasStore2();
      const host = new ServerLayerHost2();
      const executor = new RegistryCapabilityExecutor2(registry2);
      const engine = new CanvasEngine2({
        store: canvasStore,
        host,
        executor,
        oracle: createOracleVisibility2(db),
        primities: corePrimitiveProviders2(db)
      });
      await engine.seedCoreLayers();
      engine.registerCapabilities(registry2);
      canvasRouter = createCanvasRouter2({ registry: registry2 });
      setCanvasWsHandler(attachCanvasWs2(engine));
      console.log("[boot] vivim-canvas engine wired (store: in-memory, local-first)");
    }
  } catch (err) {
    console.warn("[boot] vivim-canvas not available:", err);
  }
  const kernel = bootstrapKernel({
    eventBus,
    governor,
    conversationManager,
    registry: registry2,
    nlclEngine,
    db
  });
  const kctx = kernel.context();
  await kernel.start();
  const topologyTimer = setInterval(() => {
    const snapshot = kctx.registry.describe();
    kctx.logger.info("topology snapshot", { engines: snapshot.engines.length });
  }, 6e4);
  onShutdown(async () => {
    clearInterval(topologyTimer);
    await kernel.stop();
  });
  const { ProviderHealthKernel: ProviderHealthKernel2 } = await Promise.resolve().then(() => (init_provider_health(), provider_health_exports));
  const { HealthStoreImpl: HealthStoreImpl2 } = await Promise.resolve().then(() => (init_health_store_impl(), health_store_impl_exports));
  const healthStore = new HealthStoreImpl2(db);
  const healthKernel = new ProviderHealthKernel2({
    governor,
    store: healthStore,
    eventBus,
    intervalMs: 3e4
  });
  healthKernel.start();
  onShutdown(async () => {
    healthKernel.stop();
  });
  const { LockManager: LockManager2 } = await Promise.resolve().then(() => (init_lock_manager(), lock_manager_exports));
  const { IdempotencyGuard: IdempotencyGuard2 } = await Promise.resolve().then(() => (init_idempotency_guard(), idempotency_guard_exports));
  const { RetryEngine: RetryEngine2 } = await Promise.resolve().then(() => (init_retry_engine(), retry_engine_exports));
  const { configurePrisma: configureDbPragmas } = await Promise.resolve().then(() => (init_db(), db_exports));
  await configureDbPragmas(db);
  const lockManager = new LockManager2();
  const idempotencyGuard = new IdempotencyGuard2();
  const retryEngine = new RetryEngine2();
  const ctx = {
    port,
    db,
    eventBus,
    conversationManager,
    resolutionEngine,
    governor,
    knowledgeIngestion,
    semanticSearch,
    synthesizer,
    exportEngine,
    providerMux,
    costOptimizer,
    autonomousEngine,
    policyEngine,
    registry: registry2,
    nlclEngine,
    automationOrchestrator,
    kernel,
    healthKernel,
    lockManager,
    idempotencyGuard,
    retryEngine
  };
  const auth = createAuthMiddleware();
  const conversationRouter = createConversationRouter(ctx);
  const knowledgeRouter = createKnowledgeRouter(ctx);
  const setupRouter = createSetupRouter(ctx);
  const muxRouter = createMuxRouter(ctx);
  const autonomousRouter = autonomousEngine && policyEngine ? createAutonomousRouter({ autonomousEngine, policyEngine }) : null;
  const nlclRouter = createNLCLRouter(nlclEngine);
  const capabilityRouter = ctx.registry ? createCapabilityRouter(ctx) : null;
  const automationRouter = createAutomationRouter({ orchestrator: automationOrchestrator });
  registerConversationForwarder(eventBus);
  let ready = false;
  Bun.serve({
    port,
    fetch(req, server) {
      const url = new URL(req.url);
      if (url.pathname === "/health") {
        return json({ status: "ok", version: "1.0.0" });
      }
      if (url.pathname === "/readyz") {
        if (!ready) {
          return json({ status: "not_ready", reason: "server still starting" }, 503);
        }
        return json({ status: "ready", uptime: process.uptime() });
      }
      if (url.pathname.startsWith("/api/setup/")) {
        return setupRouter(req);
      }
      if (url.pathname === "/ws") {
        const ok = server.upgrade(req);
        return ok ? void 0 : errorResponse("WebSocket upgrade failed", "UpgradeFailed", 400);
      }
      if (isShuttingDown) {
        return json({ error: "Server shutting down", code: "ShuttingDown" }, 503);
      }
      const authResult = auth(req);
      if (authResult) return authResult;
      if (url.pathname.startsWith("/api/route/")) {
        return muxRouter(req);
      }
      if (url.pathname.startsWith("/api/autonomous/") && autonomousRouter) {
        return autonomousRouter(req, url).then((r) => r ?? conversationRouter(req));
      }
      if (url.pathname.startsWith("/api/nlcl/")) {
        return nlclRouter(req);
      }
      if (url.pathname.startsWith("/api/automate/")) {
        return automationRouter(req, url).then((r) => r ?? conversationRouter(req));
      }
      if (url.pathname.startsWith("/api/knowledge/")) {
        return knowledgeRouter(req);
      }
      if (url.pathname.startsWith("/api/canvas/") && canvasRouter) {
        return canvasRouter(req, url);
      }
      if (url.pathname.startsWith("/api/capabilities") && capabilityRouter) {
        return capabilityRouter(req, url);
      }
      return conversationRouter(req);
    },
    websocket: {
      open(ws) {
        handleWebSocket.open(ws);
      },
      message(ws, message) {
        handleWebSocket.message(ws, message, eventBus);
      },
      close(ws) {
        handleWebSocket.close(ws, eventBus);
      }
    }
  });
  ready = true;
  process.on("SIGTERM", () => gracefulShutdown("SIGTERM"));
  process.on("SIGINT", () => gracefulShutdown("SIGINT"));
  return ctx;
}
var shutdownHooks, isShuttingDown;
var init_index = __esm({
  async "src/server/index.ts"() {
    init_cli();
    init_capability_bootstrap_generated();
    init_capability_bootstrap();
    init_capability_bootstrap();
    init_capability_event_bus();
    init_cdp_capability_registrar();
    init_cdp_discovery();
    init_kernel_bootstrap();
    init_nlcl_engine();
    init_unified_registry();
    init_db();
    init_auth_gate();
    init_automation_router();
    init_autonomous_router();
    init_capability_router();
    init_conversation_router();
    init_knowledge_router();
    init_mux_router();
    init_nlcl_router();
    init_response();
    init_setup_router();
    init_websocket();
    shutdownHooks = [];
    isShuttingDown = false;
    if (import.meta.main) {
      const port = Number(process.env.PORT ?? 9420);
      const ctx = await createServerWithEngines(port);
      console.log(`vivim server listening on :${ctx.port}`);
    }
  }
});
await init_index();
export {
  createServer,
  createServerWithEngines,
  onShutdown
};
//# sourceMappingURL=index.js.map