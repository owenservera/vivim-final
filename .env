# Prisma SQLite database URL
DATABASE_URL="file:./dev.db"

# OpenCode serve (local provider) — disabled by default; takes ~10s to start
# OPENCODE_SERVE_ENABLED=1
# OPENCODE_SERVER_PASSWORD=opencode-test-pw
