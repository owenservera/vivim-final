# Quality Checklists — All 4 Specs

**Date**: 2025-07-17

---

## Spec 001: Production Hardening

- [ ] **Gate**: `bun run devops invariants check --category B` → 0 violations
- [ ] **Gate**: `bun test tests/unit/engines/consent-engine.test.ts` → all pass
- [ ] **Gate**: `bun test tests/unit/engines/trust-score.test.ts` → all pass
- [ ] **Gate**: `bun run devops verify-cross-surface` → all 196 resolve
- [ ] **Accuracy**: ConsentEngine denies `financial` when threshold is `write`
- [ ] **Accuracy**: TrustScoreEngine returns ≥90 for perfect provider, ≤30 for failing
- [ ] **Edge**: Expired grant is cleaned from memory
- [ ] **Edge**: `require()` throws `ConsentViolationError` with clear message
- [ ] **Completeness**: CHANGELOG Phase 31 matches actual engine state
- [ ] **Regression**: Existing tests still pass after consent gate changes

## Spec 002: Canvas Surface

- [ ] **Gate**: All 12 `cap:canvas:*` capabilities registered in `canvas-caps.ts`
- [ ] **Gate**: `bun run devops verify-cross-surface` → new canvas caps resolve
- [ ] **Accuracy**: `GET /api/canvas/manifest` returns definitions + oracle data
- [ ] **Accuracy**: `POST /api/canvas/definitions/export` returns valid JSON
- [ ] **Edge**: Import rejects invalid `.vivim-layer.json`
- [ ] **Edge**: Manifest returns empty array when no definitions exist
- [ ] **Completeness**: CanvasDesigner frontend renders definition editor
- [ ] **Completeness**: Semantic zoom thresholds: >0.5 full, 0.2-0.5 compact, <0.2 dot

## Spec 003: Chat Advanced

- [ ] **Gate**: All 5 provider capabilities resolve in `verify-cross-surface`
- [ ] **Gate**: `POST /api/conversations/search` returns results for valid query
- [ ] **Gate**: `POST /api/conversations/search` returns 400 for missing query
- [ ] **Accuracy**: Model selector fetches model list from API
- [ ] **Accuracy**: FTS5 search returns matches within 500ms
- [ ] **Edge**: Search returns empty array for unknown query terms
- [ ] **Edge**: Upload file handles large files gracefully (reject > limit)
- [ ] **Completeness**: Memory context panel shows facts/episodes/rules
- [ ] **Completeness**: Synthesis panel shows themes/entities/decisions

## Spec 004: Provider Ops + E2E

- [ ] **Gate**: `GET /api/admin/drifts` returns drift array
- [ ] **Gate**: `POST /api/admin/drifts/:id/resolve` marks drift resolved
- [ ] **Gate**: `bun test tests/e2e/send-pipeline.test.ts` — all pass
- [ ] **Gate**: `bun test tests/e2e/setup-wizard.test.ts` — all pass
- [ ] **Gate**: `bun test tests/e2e/import-export.test.ts` — all pass
- [ ] **Accuracy**: Drift route filters by providerId when param present
- [ ] **Accuracy**: Fleet restart succeeds for existing slave, fails for unknown
- [ ] **Edge**: Fleet circuit override validates state (reject invalid)
- [ ] **Edge**: E2E tests skip Chrome-dependent steps when SKIP_CHROME_INTEGRATION set
- [ ] **Completeness**: Health dashboard shows live scores from WS events
