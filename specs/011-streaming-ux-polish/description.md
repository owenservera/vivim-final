Polish the streaming response user experience to feel fast and reliable. The backend StreamBlockStore and useStreamBlocks hook already handle progressive content delivery. Build the frontend streaming UX: (1) Thinking indicator — show an animated "..." or "Claude is thinking..." while waiting for the first token. (2) Progressive text rendering — characters appear one by one (or in small chunks) with a subtle fade-in effect. (3) Tool use visualization — when the provider uses a tool (search, code execution, image gen), show an inline card with the tool name, input, and a spinner while waiting for the result. Once complete, show the tool output and continue streaming. (4) Streaming error recovery — if the stream breaks mid-response, show what was received so far with a "Response interrupted — click to retry" banner at the bottom. (5) Cancel button — a visible stop button during streaming that sends an abort signal. (6) Token/speed counter — show "345 tokens · 12 tk/s" in the corner while streaming. Wire everything through the existing StreamBlockStore and useStreamBlocks hook.
