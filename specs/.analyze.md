# Cross-Spec Consistency Analysis

**Date**: 2025-07-17 | **Specs**: 001, 002, 003, 004

## File Conflict Audit

| File | Touched By | Conflict | Status |
|------|-----------|----------|--------|
| `capability-bootstrap.ts` | 001 (consent gates), 003 (new caps) | Merge order: 003 first, 001 wraps | ✅ No actual conflict — both already done |
| `provider-health.ts` | 001 (trust score), 004 (alert push) | Different methods | ✅ Standard merge — both already in place |
| `conversation-surface.tsx` | 003 (UI features) | Solo file | ✅ No cross-spec conflict |

## Cross-Spec Dependencies

| Dependency | From | To | Status |
|------------|------|----|--------|
| Canvas caps registered | Converge 002 | E2E 004 (canvas-layers test) | ❌ Blocks 004 task |
| Provider list models cap | Converge 003 | UI model selector | ❌ Needs registration |
| ConsentEngine available | Spec 001 | Spec 003 (send/pipeline caps gated) | ✅ Done |
| TrustScoreEngine available | Spec 001 | Spec 004 (health kernel signal) | ✅ Done |
| Search route available | Spec 003 | Spec 004 (import-export test) | ✅ Done |

## Coverage Gaps

| Area | Gap | Severity |
|------|-----|----------|
| Canvas capabilities | 12 cap:canvas:* capabilities referenced but unregistered | 🔴 Blocking |
| Frontend UI | 4 specs depend on UI wiring (designer, memory panels, model selector, health WS) | 🟡 Medium |
| E2E tests | canvas-layers.test.ts can't run until canvas caps registered | 🟡 Medium |
| Typecheck | Full `tsc --noEmit` timed out — needs local verification | 🟡 Low |

## Terminology Consistency

| Term | Spec 001 | Spec 002 | Spec 003 | Spec 004 | Notes |
|------|----------|----------|----------|----------|-------|
| Capability ID format | `cap:category:action` | `cap:canvas:*` | `cap:provider:*` | `cap:system:*` | ✅ Consistent |
| Store contract naming | `ConsentStore` | N/A | N/A | `RegistrationStore` | ✅ Follows convention |
| Entity naming | CamelCase | CamelCase | CamelCase | CamelCase | ✅ Consistent |
| Error class naming | `ConsentViolationError` | `CanvasSpawnError` | N/A | `HitlGateExpiredError` | ✅ All extend CapStoreError |

## Verdict

**No blocking cross-spec conflicts.** The primary blocker is the unregistered canvas capabilities (spec 002) which blocks the E2E canvas layers test (spec 004). Frontend UI gaps across all 4 specs are non-blocking for the backend-focused agent session.
