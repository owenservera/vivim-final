Make canvas layer positions and state persist across app restarts. The CanvasMirror engine already exists with undo/revert and in-memory state tracking. Upgrade it to persist layer positions, sizes, visibility, and lock state to the database so users don't lose their canvas layout when the server restarts. When the canvas mounts, restore the last-saved layout from the mirror store. Save on every drag stop (debounced 500ms) and on manual save. Add a "Save Layout" button and a "Reset to Default" button in the canvas toolbar. Show a subtle "Layout saved" checkmark animation on save. The InMemoryCanvasMirrorStore already exists as a contract — implement a CanvasMirrorStoreImpl backed by the Prisma database. Also add a canvas auto-save indicator in the status bar (e.g. "All changes saved" / "Saving...").
