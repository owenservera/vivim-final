Make conversations resilient to Chrome slave crashes, CDP disconnections, and provider session expirations. When a send is in progress and the Chrome slave crashes, catch the error gracefully, show a clear error message in the chat ("Chrome disconnected — your message was not sent. Click Retry to reconnect and resend."), and offer a one-click retry that reconnects the slave and resends the last message. When a provider session expires mid-conversation (detected via cookie/auth check), show an inline warning with a "Re-login to ChatGPT/Claude/Gemini" button. Before sending any message, pre-flight check: is Chrome running, is CDP connected, is the session valid? If any check fails, show the appropriate recovery action inline instead of failing silently. Add an auto-recovery timer that attempts to reconnect the slave once before showing the error. Wire the existing circuit breaker state into the send pipeline so the user sees "Provider temporarily unavailable — waiting for recovery" when the circuit is open.
