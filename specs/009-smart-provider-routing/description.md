Build an intelligent provider routing system that lets users configure which AI provider handles which types of requests. The backend ProviderMuxEngine already exists with routing logic. Build a frontend configuration panel where users can set routing rules: (1) Default provider — which provider handles general chat. (2) Per-capability routing — route "code generation" to Claude, "image generation" to Gemini, "research" to ChatGPT. (3) Cost-aware routing — prefer cheaper providers for simple questions, escalate to premium for complex ones using the existing CostOptimizer. (4) Health-aware fallback — if the primary provider is unhealthy (circuit open, session expired), auto-fallback to the next available. Show a visual routing map in the UI: a simple diagram of "Your Request → [Router] → Provider" with active rules highlighted. The backend routing engine, cost optimizer, and health kernel all exist — this spec builds the configuration UI and user-facing routing visualization.
