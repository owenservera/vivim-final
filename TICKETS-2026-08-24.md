# Tickets — Audit 2026-08-24 (5 Enhancements)

Generated from `OUTPUT-2026-08-24.md` (scoring rubric 01-scoring-rubric.md). Each ticket maps 1:1 to an enhancement.

---

## Ticket #1: Remediate transitive dep CVEs (P0 — 24/25)

**Title:** `chore(deps): fix 18/20 CVEs via overrides (tar, sharp, adm-zip, brace-expansion, deepmerge-ts, esbuild)`

**Labels:** `security`, `P0`, `deps`

**Body:**
- **Evidence:** `bun audit` 20 vulns (GHSA-34x7, GHSA-8qq5, GHSA-83g3, GHSA-23hp critical, GHSA-ggr8, GHSA-xcpc, GHSA-mh99, GHSA-f88m, etc.) — `package.json:95` cozo-node → tar, `package.json:26` transformers → sharp.
- **Fix applied:** Added `overrides` in `package.json:119` + `bun install` + `lefthook.yml:22` pre-push audit gate. Now 2 highs residual (image-size via alasql/metro).
- **Remaining:** Track `alasql` → `metro` → `image-size` 2 highs; consider `alasql` replacement or metro upgrade. Not hot path.
- **Acceptance:** `bun audit --audit-level=high` exits 0 for high/critical (currently 2 highs allowed as exception; goal 0 after alasql fix). `bun audit` shows ≤2 highs.
- **Risk:** Low (lockfile only). Reversible via `git revert`.
- **Estimated effort:** Hours (done).

**Check:** `bun audit --cwd vivim-final | grep -c "high:" → 2`

---

## Ticket #2: Enforce CI guards (P0 — 24/25)

**Title:** `chore(ci): make lefthook blocking + fix tsc vitest errors`

**Labels:** `ci`, `P0`, `devex`

**Body:**
- **Evidence:** `lefthook.yml:9,12,15,19` all `|| true`; `bun x tsc` shows 3 TS2307 vitest errors but exits 0. `route-sync` 30 fails shipped to master proves gap.
- **Fix applied:** Removed `|| true`, fixed 3 test files `vitest → bun:test` + tsc error `hardening.test.ts:103`.
- **Acceptance:** `bun x tsc --noEmit` EXIT 0, `lefthook` without `|| true`, pre-push audit gate exists.
- **Follow-up:** Mirror same checks in `.github/workflows/ci.yml` (currently lefthook only).

**Check:** `bun x tsc --noEmit; echo EXIT:$? → 0` and `grep "|| true" lefthook.yml → 0`

---

## Ticket #3: Fix route-sync tier regression (P1 — 20/25)

**Title:** `fix(frontend): seed missing providers + tierRank fail-closed (route-sync 30→0 fails)`

**Labels:** `frontend`, `P1`, `entitlement`

**Body:**
- **Evidence:** `frontend/tests/route-sync.test.ts` 30 fails (S21-S90) — `provider→system`, `pro→anonymous`. Example `frontend/tests/route-sync.test.ts:1087` expected provider got system.
- **Root cause:** `frontend/tests/seed-fixtures.ts:323` `DEFAULT_PROVIDERS` had 3 providers but tests use 13; missing providers hit `if (!familyId) → system + anonymous` branch. Also frontend `TIER_RANK` lacked normalization (mirrors backend H1/H3).
- **Fix applied:** Expanded `DEFAULT_PROVIDERS` to 16 + `DEFAULT_ACCOUNTS` to 28; added `tierRank()` to `route-context.ts` and switched `route-sync.ts:30` to `tierRank()`.
- **Acceptance:** `bun test frontend/tests/route-sync.test.ts` 100 pass.
- **Blast radius:** Medium — tier gating is entitlement/billing. Requires cross-layer contract test (backend `capability-resolution` ↔ frontend `route-sync`).
- **Follow-up:** Add CI job running both harnesses together.

**Check:** `bun test frontend/tests/route-sync.test.ts | grep "100 pass"`

---

## Ticket #4: Prisma bloat & typecheck speed (P2 — 22/25)

**Title:** `chore(build): exclude src/generated from biome/tsc + keep gitignore`

**Labels:** `build`, `P2`, `perf`

**Body:**
- **Evidence:** `src/generated/system-client/index.d.ts` 7.6M (178k lines), total 62.48 MB, `tsc` 71.87s, `biome check` diffs generated.
- **Fix applied:** `biome.json:12` add `!**/src/generated/**`, `tsconfig.json:31` exclude `src/generated`+`__generated__`, `.gitignore:10` already covers `src/generated/` (verified untracked via `git ls-files`).
- **Acceptance:** `Measure-Command tsc` ~63s (12% faster), `biome check` no longer formats `src/generated`.
- **Follow-up:** Consider not importing generated types in `src/**/*` hot path or enabling `prisma:generate` in `postinstall`.

**Check:** `bun x tsc --noEmit` still 0, `git ls-files | grep generated` shows only `__generated__`

---

## Ticket #5: Harden safe-eval denylist (P2 — 16/25, RFC)

**Title:** `sec(sandbox): expand safe-eval denylist + gate vm mode; plan allowlist AST`

**Labels:** `security`, `P2`, `hazard-H9`

**Body:**
- **Evidence:** `src/engines/safe-eval.ts:13` 42-token denylist inherently fail-open (H9). `FIXES.md:52` caveat acknowledged incompleteness. `sandbox-runner.ts:39` default quickjs true isolation but `vm` fallback silently weaker.
- **Fix applied (short-term):** Expanded denylist with FinalizationRegistry/WeakRef/Atomics/Intl/Abort* /URL/File/Headers/ReadableStream etc.; gated `vm` mode behind `VIVIM_UNSAFE_VM=1` with throw.
- **Proper fix (RFC, weeks):** Migrate `stream-parser` DB `ParserModule` rows to `safe-expression.ts` AST allowlist (like workflow-compiler). Remove `vm` fallback or keep behind explicit flag (done).
- **Acceptance:** `repros/safe-eval-guard.ts` 27/27 PASS including new tokens; `VIVIM_SANDBOX_MODE=vm` without flag throws.
- **Blast radius:** High if parser DSL breaks — needs Code Owner review.

**Check:** `bun run repros/safe-eval-guard.ts | grep ALL PASS`

---

## How to use

- Copy each ticket body into GitHub Issues (or `gh issue create`).
- Keep `OUTPUT-2026-08-24.md` as fixed-template audit artifact; diff next audit against this one (e.g. `bun audit 0 highs`, `route-sync 100/100`).
- Do **not** auto-file without human review (per runbook Phase 4).
