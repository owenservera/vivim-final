/**
 * embed-runtime stub — placeholder until Phase 3 ports the real ML layer.
 */

export async function embedText(_text: string): Promise<number[]> {
  return [];
}

export async function cosineSimilarity(_a: number[], _b: number[]): Promise<number> {
  return 0;
}

export function cosine(a: number[], b: number[]): number {
  if (a.length !== b.length || a.length === 0) return 0;
  let dot = 0;
  let normA = 0;
  let normB = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i]! * b[i]!;
    normA += a[i]! * a[i]!;
    normB += b[i]! * b[i]!;
  }
  const denom = Math.sqrt(normA) * Math.sqrt(normB);
  return denom === 0 ? 0 : dot / denom;
}
