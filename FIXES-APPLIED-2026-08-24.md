# Fixes Applied — Audit 2026-08-24 (All 5 Enhancements)

> Implements the 5 scored enhancements from `OUTPUT-2026-08-24.md`.
> Each fix is minimal, reversible, and verified by a concrete check (file:line + command).

## Verification Summary (before → after)

| # | Enhancement | Before | After | Check |
|---|---|---|---|---|
| 1 | Dep CVEs 20 vulns | `bun audit` 20 (1 crit/15 high) | **2 high** (image-size alasql chain only) — 18 fixed | `bun audit --cwd vivim-final` |
| 2 | CI no-op `|| true` | `lefthook.yml` 4 guards `|| true`, `tsc` 3 errors silenced | **0 tsc errors**, all guards blocking, pre-push audit gate | `bun x tsc --noEmit` EXIT 0 |
| 3 | route-sync 30/100 fail | `frontend/tests/route-sync.test.ts` 30 fail (S21-S90), p95 5010ms | **100/100 pass** 248 expects | `bun test frontend/tests/route-sync.test.ts` |
| 4 | Prisma bloat 62.48 MB | `src/generated/` 7.6M+5.4M index.d.ts, tsc 71.87s, `biome.json` lints generated | **biome excludes `src/generated`**, `tsconfig` excludes `src/generated`+`__generated__`, tsc 63.47s, `.gitignore` already covered (`src/generated/` ignored, untracked) | `Measure-Command tsc` + `git ls-files` |
| 5 | safe-eval denylist | 42 tokens, `vm` mode silent fallback | **Expanded denylist** (+FinalizationRegistry/WeakRef/Atomics/Intl/Abort* etc), `vm` requires `VIVIM_UNSAFE_VM=1`, comment links to H9 + `safe-expression.ts` allowlist RFC | `repros/safe-eval-guard.ts` 27/27 PASS |

## Detailed Changes

### #1 — `package.json:119` overrides + `bun install` + `lefthook.yml:22` pre-push audit
```json
"overrides": {
  "tar": ">=7.5.7",
  "brace-expansion": ">=1.1.17",
  "deepmerge-ts": ">=8.0.0",
  "adm-zip": ">=0.6.0",
  "sharp": ">=0.35.0",
  "esbuild": ">=0.28.1"
}
```
- Resolved via `bun install` (30 packages). `bun audit` dropped from 20 → 2.
- Residual 2 highs: `image-size <=2.0.2` via `alasql → react-native-fs → metro → image-size@1.2.1`. `metro@0.84.4` pins `image-size ^1.0.2`; fixing requires breaking override to `2.0.3` (not yet in registry) or `alasql` upgrade. Documented as accepted residual; not on hot path (client-side DB). Re-audit with `bun update alasql` next.
- Duplicate keys `db:backup`/`db:restore` removed (package.json:68-69 deduplicated).
- CI gate: `lefthook.yml:22-24` pre-push `bun audit --audit-level=high` blocks push on high/critical.

### #2 — `lefthook.yml:4-19` + `tests/**` vitest → bun:test
- Removed all `|| true` (lint, format, guard, typecheck now blocking).
- Fixed 3 `vitest` imports → `bun:test`:
  - `tests/integration/storage/hardening.test.ts:7` `vitest → bun:test`
  - `tests/unit/storage/db-health.test.ts:2` `vi.mock → mock.module`
  - `tests/unit/storage/cross-boundary-cache.test.ts:2` `vi → bun:test` + real timers (210ms delay instead of `vi.advanceTimersByTime`)
  - `tests/unit/storage/snapshot.test.ts:7` `vi.mock → mock.module`, `vi.restoreAllMocks → mock.restore`
- Fixed `hardening.test.ts:103` `string | undefined` tsc error (`snapEntries[0]!`).
- Result: `bun x tsc --noEmit` 0 errors (was 3).

### #3 — `frontend/tests/seed-fixtures.ts:323-388` + `frontend/src/shared/route-context.ts:14` + `frontend/src/engines/route-sync.ts:14,30`
- Root cause: `DEFAULT_PROVIDERS` only had 3 (chatgpt/claude/gemini) but tests use 13 providers (gmail/outlook/protonmail/slack/discord/telegram/whatsapp/twitter/linkedin/mastodon/notion/linear/airtable). Missing providers fell to `if (!familyId) → system + anonymous` branch.
- Fix: Expanded `DEFAULT_PROVIDERS` to 16 with correct `providerTypeId` mapping (email/messenger/social/custom families) and `DEFAULT_ACCOUNTS` to 28 (free/pro/enterprise/trial per provider).
- Also unified tier gating: added `tierRank(tier: string)` to `route-context.ts` (normalize + Infinity fail-closed, mirrors backend `capability-resolution.ts:105` H1+H3) and switched `route-sync.ts:30` from `TIER_RANK[]` direct to `tierRank()`.
- Verified: `bun test frontend/tests/route-sync.test.ts` 100 pass (was 70 pass /30 fail).

### #4 — `biome.json:12` + `tsconfig.json:31` + `.gitignore:10`
- `.gitignore` already had `src/generated/` (since initial cleanup). Verified not tracked (`git ls-files | grep generated` shows only `__generated__`, not `src/generated`). Files remain ignored on disk (62.48 MB) but are untracked — correct.
- Added `!**/src/generated/**` to `biome.json` excludes (was only `__generated__`) so formatter no longer diffs `provider-protocol.dev.ts`.
- Added `src/generated` and `src/__generated__` to `tsconfig.json` `exclude` — tsc down 71.87s → 63.47s (~12% faster). Full benefit requires not importing generated types in `src/**/*` (already via `skipLibCheck:true` but include still walked).
- Note: `src/generated/` is generated by `bun run prisma:generate` (postinstall/CI). No runtime import depends on committed generated; verified `git ls-files` not tracking.

### #5 — `src/engines/safe-eval.ts:13-22` + `src/engines/sandbox-runner.ts:35-45`
- Expanded `FORBIDDEN_TOKENS` from 42 → expanded with FinalizationRegistry/WeakRef/WeakMap/WeakSet/Atomics/Intl/AbortController/AbortSignal/URL/URLSearchParams/File/FileReader/FormData/Headers/Request/Response/ReadableStream etc. Added H9 comment linking to `safe-expression.ts` allowlist RFC.
- Gated `vm` mode: `sandbox-runner.ts:42-47` now throws unless `VIVIM_UNSAFE_VM=1` — default `quickjs` remains true isolation; `vm` retained only for rollback with explicit opt-in.
- Verified: `repros/safe-eval-guard.ts` still 27/27 PASS; new tokens (FinalizationRegistry etc.) correctly blocked (manual check: `assertTrustedExpressionSource("new FinalizationRegistry(()=>{})","test")` throws).

## Regression Checks
- `repros/capability-resolution.ts` 16/16 PASS
- `repros/trust-score.ts` 7/7 PASS
- `repros/safe-eval-guard.ts` 27/27 PASS
- `bun x tsc --noEmit` 0 errors
- `bun test frontend/tests/route-sync.test.ts` 100/100 PASS

## Residual Risks & Next Steps
- **#1 residual image-size 2 highs**: track upstream `metro` fix; consider replacing `alasql` with lighter alternative (noted as P2, not hot path).
- **#3 cross-layer contract**: add CI job `bun test frontend/tests/route-sync.test.ts` + `bun run repros/capability-resolution.ts` together to prevent future backend/frontend drift.
- **#5 long-term**: Migrate `stream-parser` DB rows from `new Function()` to `safe-expression.ts` AST allowlist (RFC, weeks). This short-term denylist expand is not a substitute.

## Files Changed (git diff --stat)
```
bio
