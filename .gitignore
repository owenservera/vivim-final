# Dependencies
node_modules/

# Build output
dist/
build/
.build-test/

# Environment (local overrides only)
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db


# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# Kilo
.kilo/

# Test artifacts
tests/**/.tmp-export-test/

# Runtime scratch (dev probes, dumps, debug scripts — never commit)
.runtime/
.runtime-scratch/

# Bun
.bunv/

# Harvest targets
harvest-targets/

# Local tool config (session/settings/cache stay machine-local)
.opencode/*
# ...except project-authored skills, which ship with the repo (portable)
!.opencode/skill

# Temporary files
tmp-debug.ts
ho1.md

# Windows reserved device-name artifact
NUL

# Web workspace artifacts
web/*/node_modules/
web/*/bun.lock
web/*/scripts/


# Dev tool artifacts (regenerable)
docs/dev-code-impl/
docs/audits/
frontend/tool-results/
frontend/playwright-report/
frontend/test-results/

# Kilo skills (duplicate of .opencode/skill/)
.skills/

# Dev database backups
prisma/dev.db
prisma/dev.db.bak-*
prisma/prisma/dev.db

# Package lockfiles (bun.lock is canonical)
package-lock.json

# Legacy docs archive (kept for reference but not active)
.archive/
# Specific archived documentation folders
.archive/docs-legacy-2026-08-15/
.archive/frontend-docs-assessment-reviews/
.archive/frontend-download/
.archive/frontend-upload/
.archive/intelligence-pack-acu-dcb-storage/
.archive/prd-merged/
.archive/context-pack-md/
.archive/runtime-scratch/
.archive/claude-investigate/

# Regenerable docs (not source of truth)
docs/dev-code-impl/
docs/audits/
docs/review-system/runs/

# Dev scratch / proof-of-concept
dev-poc/

# Local browser-export / cache dumps (never commit)
chatgpt/
claude/
gemini/
deepseek/
chrome-profiles/
prov_claude/
uploads/
.playwright-mcp/
C0-BlackBoxProject-0/
nil/
agent-brief/
.opencode/.claude/
--url/

# Build test artifacts
.build-test/

# Audit artifacts (large, local-only)
audit-report.json
audit-results.sarif
src/audit-report.json
src/audit-results.sarif

# Local model downloads
data/


# Tauri desktop build artifacts (source is kept!)
src-tauri/target/
# Tauri generated schemas (regenerable)
src-tauri/gen/
# Desktop seed snapshot is a compile-time copy of seeds/seed-snapshot.db (never commit)
src-tauri/data/seed-snapshot.db
# Sidecar build outputs (compiled exe + bundle intermediates regenerated by compile-sidecar.ts)
src-tauri/binaries/
# SQLite transient sidecar files (only prisma/dev.db is tracked)
prisma/dev.db-shm
prisma/dev.db-wal
.opencode/skill/desktop-build-testing/
.opencode/skill/production-build/

# Build artifacts (Bun compile)
test-sidecar*
*.exe
*.bun-build

# Temporary / scratch directories
bench/
specs/
schemas/

# Docker (not used in this project)
docker-compose.yml
Dockerfile
.dockerignore

# Legacy config
kilo.json

# Frontend workspace/upload artifacts
frontend/workspace-*
frontend/frontend/

# embedded agent clones (scratch, not source)
.clones/

# embedded research clones (scratch, not source)
research-clones/

# Review system run artifacts (regenerable)
docs/review-system/runs/

# Local-only seed snapshot (contains live user data — never commit)
seeds/seed-snapshot.db

# Dev implementation bundles (zip archives, not source)
docs/dev-code-impl/*.zip

# Binary assets inside the round-5 source snapshot (screenshots, fonts)
docs/dev-code-impl/vivim-final-round-5/frontend/download/
docs/dev-code-impl/vivim-final-round-5/frontend/public/fonts/
docs/dev-code-impl/vivim-final-round-5/docs/dev-code-impl/
