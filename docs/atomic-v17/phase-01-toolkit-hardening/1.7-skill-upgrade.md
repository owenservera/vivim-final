# Unit 1.7 — Skill upgrade (reconcile + new commands)

**Source:** PRD R7, R8; all scenarios
**Depends on:** 1.1–1.6
**Produces:** `devops-fullstack` SKILL.md reconciled — loop uses PS1-launched servers (doesn't
self-contradict "launch once"), browser-unavailable → API-only + flag, and documents all new
commands + the process-guard.

---

## Context

Two logical contradictions in the prior SKILL: (1) it said "launch once via PS1" yet the `loop`
self-spawns a server via the supervisor; (2) it said "agent builds" yet the loop was verify-only.
The skill also lacked any browser-unavailable fallback branch and didn't document the new v17
tooling (offline discover, migrate, codegen, ensure-browser, watchdog, guard, goal-gate).

## Changes to `.kilo/skills/devops-fullstack/SKILL.md`
- Command Catalog: add `discover --offline`, `catalog-gen`, `build backend --cap=`, `migrate`,
  `ensure-browser`, `watchdog`, `guard`.
- Agent-safety: document the goal-resolution gate, the in-process `process-guard` ("hook that
  intercepts context just in case"), non-interactive `migrate`, and PID-file reaping.
- Decision points: add "browser unavailable → API-only + flag UI-unverified".

## Acceptance
- SKILL.md accurately lists every v17 subcommand and the reconciliation notes.
- No contradiction between "launch once via PS1" and the loop's teardown behavior.
