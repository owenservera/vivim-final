# Unit 1.1 — Fix arch-audit TypeScript errors

**Source:** PRD R1; skills upgrade design P1.1
**Depends on:** none
**Produces:** `bun run typecheck` passes with zero errors from `devops/audit-arch/`

---

## Context

`devops/audit-arch/` has 4 TypeScript errors that prevent `bun run typecheck` from passing:
1. `findings.ts(82)`: `Dimension` type mismatch — `"commands"` not in the union
2. `passes/commands.ts(23)`: `ModuleGraph` not exported from `priority.ts`
3. `passes/commands.ts(120)`: `Object is possibly 'undefined'`
4. `passes/commands.ts(163)`: `string | undefined` not assignable to `string`

These errors block the typecheck gate and make the `source-audit` and `arch-audit` skills unreliable.

## Interface

### Fix 1: `devops/audit-arch/priority.ts`
- Add `ModuleGraph` to the exports (it's used in `commands.ts` line 23)
- Add `'commands'` to the `Dimension` type union

### Fix 2: `devops/audit-arch/passes/commands.ts`
- Line 120: Add null check before accessing `Object` property
- Line 163: Add fallback for `string | undefined` → `string`

## Steps
1. Read `devops/audit-arch/priority.ts` — add `ModuleGraph` export and `'commands'` to `Dimension`
2. Read `devops/audit-arch/passes/commands.ts` — fix lines 120 and 163
3. Run `bun run typecheck` — verify zero errors from `devops/audit-arch/`
4. Commit

## Acceptance
- `bun run typecheck` exits 0 (no custom-skill TS errors)
- `devops/audit-arch/` compiles cleanly
- `bun run devops audit-arch surface` runs without crash
