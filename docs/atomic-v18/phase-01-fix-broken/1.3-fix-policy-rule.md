# Unit 1.3 — Fix policyRule maxOccurrences

**Source:** PRD R3; session observation
**Depends on:** none
**Produces:** `PolicyStoreImpl.createRule()` works without crashing

---

## Context

`PolicyStoreImpl.createRule()` crashes with:
```
Argument `maxOccurrences` is missing.
```

The `policyRule` table has a required `maxOccurrences` field (Int) but the seed/runtime
code doesn't provide it.

## Interface

### Fix options (choose one):
1. **Schema fix:** Make `maxOccurrences` nullable with a default (`maxOccurrences Int? @default(0)`)
2. **Code fix:** Add `maxOccurrences` to the seed data and to `PolicyStoreImpl.createRule()`

### Verification
- `bun run devops runtime-test bootstrap` completes without the `maxOccurrences` error
- Policy rules are created successfully

## Steps
1. Read `prisma/schema.prisma` — check `policyRule` model for `maxOccurrences`
2. Read `src/storage/impl/policy-store-impl.ts:11` — check what data is passed
3. Apply the minimal fix (schema default or code fix)
4. If schema changes: `bunx prisma migrate dev --name fix-policy-max-occurrences`
5. Verify with `bun run devops runtime-test bootstrap`
6. Commit

## Acceptance
- `bun run devops runtime-test bootstrap` exits 0
- No `maxOccurrences` error in output
- Policy rules created successfully
