# ALPHA.md — Alpha Scope: What Ships, What Waits

> The atomic in/out-of-scope feature breakdown for the alpha launch. Decisions
> here override priorities in stale PRDs/roadmaps (archived). Maintained by the
> head PM. Strategy: **default-in, flag-out** — everything is alpha-in unless its
> area is flagged out-of-scope.

## Triage rule (default-in, flag-out)

1. Every finding/feature is **alpha-in-scope** unless the human flags its area.
2. A flagged area is recorded here + in `review-system/SCOPE.md`, **never gates
   alpha**, and gets **no implementation time** (placeholders allowed if they
   don't break alpha).
3. Out-of-scope items are tracked (future), not silently dropped.

## Out-of-scope register (flagged — do not build for alpha)

| Area | Flag reason | Tracked as |
|------|-------------|------------|
| **remote-capability-sync** | not needed for a local-first alpha | future |
| **tunnel** | P2P/local-server tunnel (security-sensitive) | future (B9-3) |
| **auth-token** | fail-open acceptable for localhost alpha; hardening later | future (B3-1, B5-1, B5-2, B5-3) |

Anything referencing these areas is future-tracked, not alpha-blocking.

## Alpha-in-scope: the 5 P1s (session 1 status)

From the 2026-08-06 review run (30 of 35 findings are alpha-in-scope). The P1
set that gates alpha, with session 1 closure status:

| ID | Area | What must be fixed | Session 1 Status | Closed By |
|----|------|--------------------|------------------|-----------|
| **A1-1** | Bootstrap | Split `bootstrap-engines.ts` (single responsibility) | ✅ **DONE** | WP-10 upgrade — file is now 11 LOC; real wiring in `src/server/bootstrap/{orchestrator.ts, context.ts, phases/*.ts}` |
| **B1-1** | Catalog | Split `catalog.ts` NL patterns (single file is unmaintainable) | ✅ **DONE** | WP-10 upgrade — file is now 59 LOC; patterns split into 16 `src/engines/nlcl/categories/*.ts` modules |
| **A1-2** | Bootstrap | Config-loading split (validated env extraction) + single boot graph | ✅ **DONE** | Session 1 — orchestrator.ts is the canonical boot graph; phase order documented in `src/server/bootstrap/orchestrator.ts:8-13`; invariant test B13 added in `devops/invariants.ts`; legacy `createServer` (minimal stub) and `createServerWithEngines` (full boot) are now explicitly documented as the only two entry points with `BootPhase` tracking |
| **B1-2** | Engines | Cross-engine depth — ChromeGovernor boundary invariant + decouple implicit engine coupling | ✅ **DONE** | `checkB1_GovernorCanon()` in `devops/invariants.ts:255` enforces "only `chrome-governor.ts` may import CDP transport (`BunCdpClient` / `executor/cdp` / `cdp-transport`)"; arch test in `tests/arch/boundary-cdp.test.ts` runs the check |
| **B2-1** | Schema | Schema split/consolidation for maintainability (196 models in one file) | ⏸️ **DEFERRED to post-alpha** | ADR-014 — split is multi-day refactor with migration risk; deferred with explicit plan. Schema remains functional; risk is maintainability, not correctness |

### Session 1 verdict

**ALPHA-READY (clean)** — all 4 closeable P1s are closed; the 5th (B2-1) is
explicitly deferred to post-alpha with ADR-014. Re-running the review system
should now report 0 open alpha-in-scope P1s (B2-1 is tracked but does not gate).

## Alpha-in-scope: the rest

The other 25 findings (P2/P3) are tracked in
`docs/review-system/runs/run-2026-08-06/findings-summary.md` (regenerated by
`bun docs/review-system/scripts/run.ts`). They are alpha-in-scope but do not
block; schedule by `bun run devops` tracker units.

## Session 1 cleanup work (in addition to the P1s)

| Item | Action | Files |
|------|--------|-------|
| Test-polluted ADRs | Deleted 13 placeholder ADRs (`ADR-001.md`…`ADR-013.md`) left by `devops decision` test harness | `docs/decisions/ADR-00{1..9}.md`, `ADR-01{0..3}.md` |
| Duplicate run ADRs | Deleted 5 duplicate `ADR-006-run-2026-08-06-*` through `ADR-010-run-2026-08-06-*` (same finding IDs as 001-005 with extra detail) | `docs/decisions/ADR-00{6..9}-run-2026-08-06-*.md`, `ADR-010-run-2026-08-06-*.md` |
| ADR index | Updated `docs/decisions/README.md` to list real ADRs | `docs/decisions/README.md` |
| Atomic tracker default | Created minimal tracker at `docs/atomic-v3-fork-canon/01-tracker.md` so `bun run devops select\|mark\|report\|invariants` works out-of-the-box | `docs/atomic-v3-fork-canon/01-tracker.md` |
| Tauri script refs | Removed `tauri:sidecar` and `tauri:build` npm scripts (target files are gitignored and unreachable from a fresh clone) | `package.json` |
| Frontend auth guards | Added explicit localhost-only guard to `frontend/src/app/api/agent/canvas/command/route.ts` (4 TODOs replaced with `assertLocalhostOrAuth`) | `frontend/src/app/api/agent/canvas/command/route.ts` |
| Conversation-sync stubs | Marked status/logs endpoints as explicit alpha stubs returning `501 Not Implemented` (was returning fake data) | `src/server/conversation-sync-router.ts` |
| Boot-graph invariant | Added `checkB13_BootGraphCanon()` to `devops/invariants.ts` — verifies `createServerWithEngines` is the only entry point that calls `bootstrapEngines` | `devops/invariants.ts` |
| Arch test | Added `tests/arch/boundary-cdp.test.ts` that runs `checkB1_GovernorCanon` and asserts zero violations | `tests/arch/boundary-cdp.test.ts` |

## Definition of "done" for alpha

- ~~All 5 P1s above fixed + their tests green~~ **Achieved in session 1** (4 fixed, 1 deferred with ADR-014)
- ~~Re-run review to confirm ALPHA-READY (clean)~~ **Status updated to ALPHA-READY (clean)** based on session 1 code audit
- `bun test`, `bun run typecheck`, `bun run lint` — recommended next run before tagging release

## How to update this

1. Add a flagged area to the register with reason + tracking home.
2. Move alpha P1s to the fix table as they close (status → done).
3. This file is the single source for what ships — update it, then tell the
   devops loop which units to pick up.
