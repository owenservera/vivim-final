# Unit 10.7 — User manual

**Source:** v3 Overview §4; `docs/master-plan-v3/PHASE-10-polish-sdk.md` §10.7
**Design Doc:** `docs/master-plan-v3/PHASE-10-polish-sdk.md` §10.7
**Depends on:** all
**Produces:** `docs/user-manual.md` covering daily-use scenarios.

---

## Context

A product needs a user-first manual, not a developer dump. This unit writes `docs/user-manual.md` with daily-use scenarios: workspace navigation, canvas authoring, agent task delegation, memory curation, provider setup, backup/restore, multi-device sync, troubleshooting — written for the end user.

## Existing Baseline

- `web/` (4.x) — workspace shell + panels; the manual describes their use.
- `docs/atomic-v3/phase-03-html-canvas/` (3.x) — canvas authoring surface.
- `docs/atomic-v3/phase-07-autonomous-orchestration/7.x` — agent task delegation + HITL.
- `docs/atomic-v3/phase-06-memory-knowledge/6.7-curation-wiring.md` — memory curation.
- `docs/atomic-v3/phase-05-provider-expansion/5.x` — provider setup.
- `docs/atomic-v3/phase-09-sovereign-data/9.6-encrypted-export.md` + `9.7-backup-schedule.md` + `9.3-multi-device-sync.md` — backup/restore/sync.
- `docs/user-manual.md` — created by this unit.

## Interface

### New file: `docs/user-manual.md` (structure)

```markdown
# vivim v3 — User Manual

## 1. Getting started (onboarding)
## 2. Navigating the workspace
## 3. Authoring with canvases
## 4. Delegating tasks to the agent (HITL, pause/resume)
## 5. Curating your memory (verify / reject / edit)
## 6. Setting up providers (local Ollama / cloud with consent)
## 7. Backup & restore (.vivim bundle)
## 8. Syncing across devices (pairing code)
## 9. Troubleshooting (offline mode, consent errors, latency)
```

### Authoring rules (this unit)

- Every section written **user-first**: task → steps → screenshot placeholder → gotcha.
- Cross-links to the matching atomic file for depth (not a copy).
- No internal engine names without a plain-language gloss.

## Acceptance Criteria

1. `docs/user-manual.md` exists with all 9 sections.
2. Each section is task-oriented (start from a user goal, not a menu).
3. Provider setup covers local-first + consent-gated cloud.
4. Backup/restore + multi-device sync sections match 9.6/9.7/9.3 behavior.
5. Troubleshooting covers the offline/consent/latency cases.
6. A new user with no vivim experience can install, onboard, and complete a useful task within 10 minutes using it (acceptance).
7. `bun run devops gate` passes (doc lint, if any).

## Tests

`tests/docs/manual-coverage.test.ts` (where doc tests exist):
- every atomic file referenced by a manual section exists.
- all 9 section headings present.

## DevOps Verification

```powershell
bun run devops invariants check --unit 10.7
bun run devops gate
```
