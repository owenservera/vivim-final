# Unit 18.6: Adaptive Workspace Modes

**Phase:** 18 | **File:** src/engines/adaptive-workspace.ts
**Depends:** — | **Source:** upgrade-07-composable-interface.md §Part 2

## Purpose
Three workspace modes (chat, expert, agent) with progressive panel disclosure.

## Interface
`	ypescript
export type WorkspaceMode = 'chat' | 'expert' | 'agent'

export class AdaptiveWorkspaceEngine {
  async getMode(userId: string): Promise<WorkspaceMode>
  async setMode(userId: string, mode: WorkspaceMode): Promise<void>
  async checkPromotion(userId: string): Promise<WorkspaceMode | null>
  async getPanelConfig(mode: WorkspaceMode): Promise<PanelConfig[]>
}
`

## Tests
- Default mode is 'chat'
- Promotion triggers based on usage
- Panel config correct per mode

## Gate
- un run typecheck passes
