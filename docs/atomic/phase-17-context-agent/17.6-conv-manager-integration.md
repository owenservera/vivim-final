# Unit 17.6: ConversationManager Integration

**Phase:** 17 | **File:** src/engines/conversation-manager.ts
**Depends:** 17.2 ContextAssembly | **Source:** upgrade-05-memory-intelligence.md

## Purpose
Wire ContextAssemblyEngine into ConversationManager.send() pipeline.

## Changes
Replace the single memory.getAgentContext() call (line 171) with:
`	ypescript
const assembled = await this.contextAssembly?.assemble(conversationId, message)
const memoryContext = assembled ? this.assembledToMemoryContext(assembled) : undefined
`

Add contextAssembly?: ContextAssemblyEngine to constructor.

## Tests
- Context assembled before send
- Assembled context stored in conversation.contextJson

## Gate
- un run typecheck passes
