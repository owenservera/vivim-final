# Unit 16.8: Server Routes — Mux

**Phase:** 16 | **File:** src/server/mux-router.ts
**Depends:** 16.1 ProviderMux | **Source:** upgrade-06-muxing-routing.md

## Purpose
REST API endpoints for provider muxing.

## Routes
- POST /api/route/auto — auto-route to best provider
- POST /api/route/mux — multi-provider mux with strategy
- GET /api/route/cost-report — cost report
- GET /api/route/preferences — learned routing preferences

## Gate
- un run typecheck passes
