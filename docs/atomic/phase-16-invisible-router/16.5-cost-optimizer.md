# Unit 16.5: CostOptimizer

**Phase:** 16 | **File:** src/engines/cost-optimizer.ts
**Depends:** 16.1 ProviderMux | **Source:** upgrade-06-muxing-routing.md

## Purpose
Track and optimize per-provider costs.

## Interface
`	ypescript
export class CostOptimizer {
  constructor(store: CostStore)
  async recordCost(providerId: string, costCents: number, tokensInput: number, tokensOutput: number, model?: string): Promise<void>
  async getCostReport(providerId: string, from: number, to: number): Promise<CostReport>
  async estimateCost(providerId: string, messageLength: number): Promise<number>
  async getCheapestProvider(capabilityId?: string): Promise<string | null>
}
`

## Tests
- Cost recorded correctly
- Report aggregates by day
- Cheapest provider found correctly

## Gate
- un run typecheck passes
