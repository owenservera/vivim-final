# Unit 90.3 — web/api-client Typed SDK

**Phase:** 90 (Frontend Sandbox)
**Depends:** 90.1
**Source:** `.kilo/plans/1783690988012-sandbox-frontend-system.md` (Phase 1)
**Status:** DONE

## Interface

```typescript
// web/api-client/src/index.ts
export async function listProviders(): Promise<Provider[]>
export async function getFleetStatus(): Promise<FleetStatus>
export async function createConversation(params: CreateConversationParams): Promise<Conversation>
export async function sendMessage(conversationId: string, content: string): Promise<Message>
export async function resolveCapabilities(providerId: string, planTier: string): Promise<ResolvedCapabilities>
export async function resolveCapabilitiesByConversation(conversationId: string, planTier: string): Promise<ResolvedCapabilities>
export function subscribeWs(sessionId: string): WebSocket
```

Types generated from `docs/merged-design-v2/03-merged-schema.md`:
- `CapabilityUIContract` (21-field)
- `ResolvedCapabilities`
- `Provider`, `Conversation`, `Message`

## Store Contract

N/A (API client)

## Gate Criteria

- [x] `web/api-client/src/index.ts` exports all functions
- [x] Types match `03-merged-schema.md` CapabilityUIContract
- [x] `bun run typecheck` passes

## Notes

- Sandbox currently uses raw `fetch()` calls — migrate to this client after creation
- Backend endpoints from `07-merged-api.md`: `GET /api/providers`, `GET /api/providers/:id/capabilities`
