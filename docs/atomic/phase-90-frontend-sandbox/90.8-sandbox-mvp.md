# Unit 90.8 — Sandbox App MVP

**Phase:** 90 (Frontend Sandbox)
**Depends:** 90.2, 90.3, 90.4, 90.5, 90.6
**Source:** `.kilo/plans/1783690988012-sandbox-frontend-system.md` (Phase 3)
**Status:** PARTIAL (catalog + harness exist, debug panel missing)

## Interface

```typescript
// web/sandbox/src/app/sandbox-app.tsx — current
// Already has:
//   - CapabilityCatalog (sidebar listing capabilities)
//   - CapabilityHarness (selected capability details + execute button)
//
// Missing:
//   - Debug panel (WS events, latency, traces)
//   - State reset via /api/sandbox/debug
```

## Store Contract

- `web/sandbox/src/store/capability-store.ts` — loads capabilities from API
- `web/sandbox/src/features/capability-catalog.tsx` — sidebar UI
- `web/sandbox/src/features/capability-harness.tsx` — capability details + execute

## Gate Criteria

- [ ] `bun run dev:sandbox` boots
- [ ] Catalog shows real capabilities resolved from DB
- [ ] Execute button drives real backend execution
- [ ] Debug panel shows live WS events
- [ ] `/api/sandbox/debug` reset works

## Notes

- Catalog and harness already built — verify they work with real backend
- Debug panel is new — needs WS subscription to engine events
- All buttons must use `<ActionTrigger>` (B8)
