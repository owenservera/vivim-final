# Unit 90.1 — Monorepo Scaffold

**Phase:** 90 (Frontend Sandbox)
**Depends:** None
**Source:** `.kilo/plans/1783690988012-sandbox-frontend-system.md` (Phase 0)
**Status:** DONE

## Interface

Create `web/` workspace with:
- `web/sandbox/` — Vite + React 19 app (already exists)
- `web/app/` — Production frontend stub
- `web/ui/` — Shared package (ActionRegistry, AgentBridge)
- `web/api-client/` — Typed SDK client
- Root `web/package.json` with workspace config

## Store Contract

N/A (scaffold only)

## Gate Criteria

- [x] `web/` directory structure exists
- [x] `web/package.json` defines workspace
- [x] `bun run dev:sandbox` script works
- [x] `bun run typecheck` passes across `web/`

## Notes

- Existing `web/sandbox/` and `web/ui/` already built — verify they fit the workspace structure
- `web/app/` is a stub (empty React app) until features proven in sandbox
