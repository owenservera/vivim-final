# Unit 90.2 — Shared tsconfig + Path Aliases

**Phase:** 90 (Frontend Sandbox)
**Depends:** 90.1
**Source:** `.kilo/plans/1783690988012-sandbox-frontend-system.md` (Phase 0)
**Status:** DONE

## Interface

Configure TypeScript path aliases:
- `@ui` → `web/ui/src`
- `@api-client` → `web/api-client/src`
- Shared `tsconfig.base.json` in `web/`
- Per-package `tsconfig.json` extending base

## Store Contract

N/A (config only)

## Gate Criteria

- [x] `web/tsconfig.base.json` exists with path aliases
- [x] `web/sandbox/tsconfig.json` extends base, resolves `@ui`
- [x] `web/ui/tsconfig.json` extends base
- [x] `bun run typecheck` passes in `web/sandbox/`

## Notes

- `web/sandbox/src/features/capability-harness.tsx` already imports `@ui` — verify alias works
- `web/ui/src/actions/agent-bridge.ts` imports `ActionRegistry` — verify internal alias
