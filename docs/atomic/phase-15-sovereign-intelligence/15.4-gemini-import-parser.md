# Unit 15.4: Gemini Export Parser

**Phase:** 15 | **File:** `src/engines/parsers/gemini-import.ts`
**Depends:** 15.1 KnowledgeIngestionEngine | **Produces:** Parse Gemini/Google Takeout format
**Source:** upgrade-05-memory-intelligence.md

## Purpose

Parse Google Gemini conversation exports.

## Interface

```typescript
export class GeminiExportParserImpl {
  parse(rawJson: string): ParsedConversation[]
}
```

## Implementation

Parse Gemini export with `messages` array. Map `author: "user"` → `"user"`, `author: "model"` → `"assistant"`. External ID = `id`.

## Tests

- Parse sample Gemini export → correct conversations
- Author mapping correct

## Gate

- `bun run typecheck` passes
