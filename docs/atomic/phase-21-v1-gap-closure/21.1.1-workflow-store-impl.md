# Unit 21.1.1: WorkflowStore Implementation

**Phase:** 21.1 | **File:** `src/storage/impl/workflow-store-impl.ts`
**Depends:** Phase 4 (engines exist), Prisma schema | **Produces:** Prisma-backed WorkflowStore
**Gap Source:** `docs/roadmap/TRUTH-GAPS.md` #1
**Design Doc:** `docs/merged-design-v2/sota-04-visual-workflow-engine.md`

---

## Purpose

Implement `WorkflowStoreImpl` backed by Prisma. The `WorkflowStore` interface is currently defined inline in `src/engines/workflow-engine.ts:59-65` and has no storage implementation.

## Interface Definition (from workflow-engine.ts:59-65)

```typescript
export interface WorkflowStore {
  save(workflow: WorkflowDefinition): Promise<void>
  findById(id: string): Promise<WorkflowDefinition | null>
  list(): Promise<WorkflowDefinition[]>
  delete(id: string): Promise<void>
  saveRun(run: WorkflowRun): Promise<void>
  getRuns(workflowId: string, limit?: number): Promise<WorkflowRun[]>
}
```

## Types Required

- `WorkflowDefinition` — from `src/engines/workflow-engine.ts`
- `WorkflowRun` — from `src/engines/workflow-engine.ts`

## Acceptance Criteria

1. Create `src/storage/impl/workflow-store-impl.ts` implementing `WorkflowStore`
2. Uses Prisma client for all operations
3. Handles `WorkflowDefinition.steps` as JSON (Prisma `Json` field)
4. Handles `WorkflowRun` persistence with foreign key to `WorkflowDefinition`
5. No `any` types — proper typing throughout
6. Unit test: `tests/unit/storage/impl/workflow-store-impl.test.ts`

## Prisma Schema Check

Verify `workflow_definitions` and `workflow_runs` tables exist in `prisma/schema.prisma`. If not, add them before implementing.
