# 21.1 — Store Implementations (6 units)

**Status:** PLANNED
**Priority:** P1 — CRITICAL (engines depend on these stores)
**Depends on:** Phases 4-10 (v1 engines exist)

---

## Purpose

Six engines define store interfaces inline but have no Prisma-backed implementations. Without these, the engines either crash at runtime or silently no-op.

---

## 21.1.1 — WorkflowStoreImpl

**Gap:** No implementation of `WorkflowStore` interface
**Engine:** `src/engines/workflow-engine.ts:59-65`
**Interface:**
```typescript
export interface WorkflowStore {
  getWorkflow(id: string): Promise<WorkflowDefinition | null>
  saveWorkflow(def: WorkflowDefinition): Promise<void>
  deleteWorkflow(id: string): Promise<void>
  saveExecution(exec: WorkflowExecution): Promise<void>
  getExecution(id: string): Promise<WorkflowExecution | null>
}
```
**Design Doc:** `sota-04-visual-workflow-engine.md` §Architecture
**Target:** `src/storage/impl/workflow-store-impl.ts`
**Prisma Model:** `workflow_definition` + `workflow_execution` (L14 schema delta)
**Test:** `tests/unit/engines/workflow-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/workflow-store-impl.test.ts` passes

---

## 21.1.2 — CapabilityMacroStoreImpl

**Gap:** No implementation of `CapabilityMacroStore` interface
**Engine:** `src/engines/capability-macro.ts:34-40`
**Interface:**
```typescript
export interface CapabilityMacroStore {
  list(opts?: { providerId?: string; activeOnly?: boolean }): Promise<CapabilityMacroRow[]>
  get(id: string): Promise<CapabilityMacroRow | null>
  create(input: CapabilityMacroRow): Promise<CapabilityMacroRow>
  update(id: string, patch: Partial<CapabilityMacroRow>): Promise<void>
  delete(id: string): Promise<void>
}
```
**Design Doc:** `04-merged-engines.md`
**Target:** `src/storage/impl/capability-macro-store-impl.ts`
**Prisma Model:** `capability_macro` (L14 schema delta)
**Test:** `tests/unit/engines/capability-macro-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/capability-macro-store-impl.test.ts` passes

---

## 21.1.3 — HarnessCheckpointStoreImpl

**Gap:** No implementation of `HarnessCheckpointStore` interface
**Engine:** `src/engines/harness-checkpoint.ts:34-39`
**Interface:**
```typescript
export interface HarnessCheckpointStore {
  create(input: HarnessCheckpointRow): Promise<HarnessCheckpointRow>
  getLatestBySlave(slaveId: string): Promise<HarnessCheckpointRow | null>
  getLatestByConversation(conversationId: string): Promise<HarnessCheckpointRow | null>
  deleteBySlave(slaveId: string): Promise<void>
}
```
**Design Doc:** `sota-03-agentic-observation-loop.md`
**Target:** `src/storage/impl/harness-checkpoint-store-impl.ts`
**Prisma Model:** `harness_checkpoint` (L14 schema delta)
**Test:** `tests/unit/engines/harness-checkpoint-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/harness-checkpoint-store-impl.test.ts` passes

---

## 21.1.4 — AlertStoreImpl

**Gap:** No AlertStore contract or implementation
**Engine:** `src/engines/alerter.ts` (exists, depends on undefined store)
**Interface:** Define `AlertStore` contract first, then implement
**Design Doc:** `04-merged-engines.md`
**Target:**
- `src/storage/contracts/alert-store.ts` (contract)
- `src/storage/impl/alert-store-impl.ts` (implementation)
**Prisma Model:** `alert` + `alert_rule` (L14 schema delta)
**Test:** `tests/unit/engines/alert-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/alert-store-impl.test.ts` passes

---

## 21.1.5 — AutomationStoreImpl

**Gap:** No AutomationStore contract or implementation
**Engine:** `src/engines/scheduler.ts` (exists, depends on undefined store)
**Interface:** Define `AutomationStore` contract first, then implement
**Design Doc:** `04-merged-engines.md`
**Target:**
- `src/storage/contracts/automation-store.ts` (contract)
- `src/storage/impl/automation-store-impl.ts` (implementation)
**Prisma Model:** `automation` + `automation_execution` (L14 schema delta)
**Test:** `tests/unit/engines/automation-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/automation-store-impl.test.ts` passes

---

## 21.1.6 — HpeSessionStoreImpl

**Gap:** No implementation of `HpeSessionStoreContract`
**Contract:** `src/storage/contracts/hpe-session-store.ts:15-20`
**Interface:**
```typescript
export interface HpeSessionStoreContract {
  save(session: HpeSession): Promise<void>
  findById(id: string): Promise<HpeSession | null>
  findByAgent(agentId: string, limit?: number): Promise<HpeSession[]>
  updateStatus(id: string, status: HpeSession['status']): Promise<void>
}
```
**Design Doc:** `sota-09-harness-protocol-engine.md`
**Target:** `src/storage/impl/hpe-session-store-impl.ts`
**Prisma Model:** `hpe_session` (L14 schema delta)
**Test:** `tests/unit/engines/hpe-session-store-impl.test.ts`
**Gate:** `bun test tests/unit/engines/hpe-session-store-impl.test.ts` passes

---

## Gate Criteria

```bash
bun run typecheck              # zero errors
bun run lint                   # zero errors
bun test tests/unit/engines/   # all 6 store impl tests pass
```
