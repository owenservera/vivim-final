# Apply these changes to the Gemfile.
# UX8-01: remove coffee-rails.
# UX8-02: replace turbolinks with turbo-rails.
# UX8-03: remove jquery-rails.

# REMOVE these lines:
#   gem 'coffee-rails', '~> 4.0.0'
#   gem 'jquery-rails'
#   gem 'turbolinks'
#   gem 'sass-rails', '~> 5.0'  # only if migrating to dart-sass

# ADD these lines:
#   gem 'turbo-rails', '~> 1.4'
#   gem 'cssbundling-rails', '~> 1.3'   # for dart-sass + npm-managed CSS
#   gem 'jsbundling-rails', '~> 1.2'    # for esbuild + npm-managed JS

# Then run:
#   bundle install
#   bundle exec rails css:install:sass
#   bundle exec rails javascript:install:esbuild
#   yarn add @hotwired/turbo-rails glightbox
#   yarn remove jquery
