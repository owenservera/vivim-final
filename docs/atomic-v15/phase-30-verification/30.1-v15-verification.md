# 30.1 — v15-verification

**Depends:** 29.2 · **Phase:** 30 · **Closes:** the audit to P1=0, P2=0

## Intent
Final gate for atomic-v15: prove every command-surface finding is closed and the codebase is clean.

## Procedure
1. `bun run devops gate` → typecheck + lint + bun test all green.
2. `bun run devops audit-arch standard --pass commands` → assert **P1=0, P2=0** (and P3=0).
3. `bun run devops audit-arch deep --pass commands` → confirm parity holds at deep scope too.
4. `bun run devops report --tracker docs/atomic-v15/01-tracker.md --atomic-dir docs/atomic-v15` → all units `done`.
5. Mark all tracker units `done`; commit atomic-v15 with message
   `feat(command-surface): close audit findings (atomic-v15) — implement 6 caps, consolidate oracle, bind ui, refine audit`.

## Acceptance
- P1=0 (no dangling NL→capability bindings).
- P2=0 (no unbound declared surface, no unbound interactive capability, no colliding cliCommand).
- `devops gate` green.

## Gate
This unit is `done` only when the three audit runs and `devops gate` all pass.
